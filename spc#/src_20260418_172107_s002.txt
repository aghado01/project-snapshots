idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
9 | hashish/tlsh.cs | {4473 439 0.2951 4.7777} | 4629 | using System;\nusing System.Runtime.CompilerServices;\nusing System.Runtime.InteropServices;\nusing System.Text;\n\nnamespace Hashish;\n\npublic static class TrendLocalitySensitiveHash\n{\n    private const int WindowSize = 5;\n    private const int BucketCount = 256;\n    private const int MinLength = 50;\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public static string Compute(string content)\n    {\n        if (string.IsNullOrEmpty(content) || content.Length < MinLength)\n            return string.Empty;\n\n        ReadOnlySpan<char> span = content.AsSpan();\n\n        Span<int> buckets = stackalloc int[BucketCount];\n        ComputeBuckets(span, buckets);\n\n        Span<int> sorted = stackalloc int[BucketCount];\n        buckets.CopyTo(sorted);\n        MemoryExtensions.Sort(sorted);\n\n        int q1 = sorted[BucketCount / 4];\n        int q2 = sorted[BucketCount / 2];\n        int q3 = sorted[BucketCount * 3 / 4];\n\n        string body = BuildBody(buckets, q1, q2, q3);\n        string checksum = ComputeChecksum(span);\n        string lenCode = EncodeLength(content.Length);\n\n        return $\"T1{checksum}{lenCode}{body}\";\n    }\n\n    public static int Compare(string hash1, string hash2)\n    {\n        if (!hash1.StartsWith(\"T1\", StringComparison.Ordinal) ||\n            !hash2.StartsWith(\"T1\", StringComparison.Ordinal))\n            throw new ArgumentException(\"Invalid TLSH format — expected 'T1' prefix.\");\n\n        ReadOnlySpan<char> s1 = hash1.AsSpan(2);\n        ReadOnlySpan<char> s2 = hash2.AsSpan(2);\n\n        int checksumDist = Math.Abs(\n            Convert.ToInt32(s1[..2].ToString(), 16) -\n            Convert.ToInt32(s2[..2].ToString(), 16));\n\n        int lenDist = Math.Abs(\n            Convert.ToInt32(s1[2..3].ToString(), 16) -\n            Convert.ToInt32(s2[2..3].ToString(), 16));\n\n        ReadOnlySpan<char> body1 = s1[3..];\n        ReadOnlySpan<char> body2 = s2[3..];\n\n        int bodyDist = 0;\n        int minLen = Math.Min(body1.Length, body2.Length);\n        for (int i = 0; i < minLen; i++)\n            if (body1[i] != body2[i]) bodyDist++;\n\n        return (lenDist * 12) + checksumDist + bodyDist;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    private static void ComputeBuckets(ReadOnlySpan<char> content, Span<int> buckets)\n    {\n        int limit = content.Length - WindowSize;\n        for (int i = 0; i <= limit; i++)\n        {\n            byte h = PearsonHash(content.Slice(i, WindowSize));\n            buckets[h]++;\n        }\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static byte PearsonHash(ReadOnlySpan<char> window)\n    {\n        byte hash = 0;\n        for (int i = 0; i < window.Length; i++)\n        {\n            byte b = (byte)((int)window[i] & 0xFF);\n            hash = (byte)(((hash ^ b) * 31) & 0xFF);\n        }\n        return hash;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    private static string BuildBody(ReadOnlySpan<int> buckets, int q1, int q2, int q3)\n    {\n\n        Span<byte> body = stackalloc byte[BucketCount * 2 / 8];\n\n        for (int i = 0; i < BucketCount; i++)\n        {\n            int code = buckets[i] <= q1 ? 0\n                     : buckets[i] <= q2 ? 1\n                     : buckets[i] <= q3 ? 2\n                     : 3;\n\n            int byteIdx = i * 2 / 8;\n            int bitShift = (i * 2) % 8;\n            body[byteIdx] |= (byte)(code << bitShift);\n        }\n\n        return Convert.ToHexString(body).ToLowerInvariant();\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    private static string ComputeChecksum(ReadOnlySpan<char> content)\n    {\n        int maxBytes = Encoding.UTF8.GetMaxByteCount(content.Length);\n        int sum = 0;\n\n        if (maxBytes <= 1024)\n        {\n            Span<byte> buf = stackalloc byte[1024];\n            int written = Encoding.UTF8.GetBytes(content, buf);\n            ReadOnlySpan<byte> bytes = buf[..written];\n            for (int i = 0; i < bytes.Length; i++) sum += bytes[i];\n        }\n        else\n        {\n            byte[] buf = Encoding.UTF8.GetBytes(content.ToString());\n            for (int i = 0; i < buf.Length; i++) sum += buf[i];\n        }\n\n        return (sum % 256).ToString(\"x2\");\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static string EncodeLength(int length)\n    {\n        if (length <= 0) return \"0\";\n        int code = Math.Min((int)Math.Log(length, 2), 15);\n        return code.ToString(\"x\");\n    }\n} |
10 | kernels/SpcBatch.KernelCauchy.cs | {245 32 0.3265 4.1796} | 255 | namespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static double CauchyCoupling(double distance, double delta)\n        {\n            return 1.0 / (1.0 + (distance * distance) / (delta * delta));\n        }\n    }\n} |
11 | kernels/SpcBatch.KernelGaussian.cs | {307 36 0.3160 4.3242} | 320 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static double GaussianCoupling(double distance, double delta)\n        {\n            double twoSigmaSq = 2.0 * delta * delta;\n            return Math.Exp(-(distance * distance) / twoSigmaSq);\n        }\n    }\n} |
12 | kernels/SpcBatch.KernelLaplacian.cs | {237 26 0.3165 4.2159} | 249 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static double LaplacianCoupling(double distance, double delta)\n        {\n            return Math.Exp(-distance / delta);\n        }\n    }\n} |
13 | kernels/SpcBatch.KernelLinear.cs | {244 29 0.3197 4.2768} | 256 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static double LinearCoupling(double distance, double delta)\n        {\n            return Math.Max(0.0, 1.0 - distance / delta);\n        }\n    }\n} |
14 | metrics/SpcBatch.Canberra.cs | {1006 97 0.3509 4.3049} | 1039 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildCanberraGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Canberra metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                CanberraDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double CanberraDistance(double[] a, double[] b)\n        {\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double denom = Math.Abs(a[i]) + Math.Abs(b[i]);\n                if (denom < 1e-15) continue;\n                sum += Math.Abs(a[i] - b[i]) / denom;\n            }\n            return sum;\n        }\n    }\n} |
15 | metrics/SpcBatch.Cosine.cs | {1005 88 0.2836 4.5546} | 1038 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildCosineGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Cosine metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                CosineDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double CosineDistance(double[] a, double[] b)\n        {\n            double cosSim = TensorPrimitives.CosineSimilarity<double>(a, b);\n            if (double.IsNaN(cosSim)) return 1.0; // zero vector → max distance\n\n            cosSim = Math.Max(-1.0, Math.Min(1.0, cosSim));\n            return Math.Acos(cosSim) / Math.PI;\n        }\n    }\n} |
16 | metrics/SpcBatch.Euclidean.cs | {811 67 0.2799 4.4533} | 838 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildEuclideanGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Euclidean metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                EuclideanDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double EuclideanDistance(double[] a, double[] b)\n        {\n            return TensorPrimitives.Distance<double>(a, b);\n        }\n    }\n} |
17 | metrics/SpcBatch.FisherRao.cs | {1018 99 0.3497 4.3589} | 1052 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildFisherRaoGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Fisher-Rao metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                FisherRaoDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double FisherRaoDistance(double[] p, double[] q)\n        {\n            double bhat = 0;\n            for (int i = 0; i < p.Length; i++)\n            {\n                if (p[i] > 0 && q[i] > 0)\n                    bhat += Math.Sqrt(p[i] * q[i]);\n            }\n\n            bhat = Math.Max(-1.0, Math.Min(1.0, bhat));\n            return 2.0 * Math.Acos(bhat);\n        }\n    }\n} |
18 | metrics/SpcBatch.Hamming.cs | {865 76 0.2740 4.5576} | 892 | using System;\nusing System.Numerics;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildHammingGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.SimHashes == null || request.SimHashes.Length == 0)\n                throw new ArgumentException(\"SimHashes must be provided for Hamming metric.\");\n\n            var hashes = request.SimHashes;\n            n = hashes.Length;\n\n            Func<int, int, double> pairDistance = (i, j) =>\n                (double)BitOperations.PopCount(hashes[i] ^ hashes[j]);\n\n            var (allNeighbors, nnDistances) = SelectNeighbors(\n                request.Proximity, n, request.K, request.Epsilon, pairDistance);\n\n            return ConvertToCoupling(allNeighbors, nnDistances, request.Delta, request.Estimator, request.CouplingKernel);\n        }\n    }\n} |
19 | metrics/SpcBatch.Jaccard.cs | {1228 130 0.3469 4.2958} | 1266 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildJaccardGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Jaccard metric.\");\n\n            n = request.Features.Length;\n            double threshold = request.JaccardThreshold;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                (a, b) => JaccardDistance(a, b, threshold), request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double JaccardDistance(double[] a, double[] b, double threshold)\n        {\n            int intersection = 0, union = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                bool ai = a[i] > threshold;\n                bool bi = b[i] > threshold;\n                if (ai || bi) union++;\n                if (ai && bi) intersection++;\n            }\n            if (union == 0) return 0.0; // both empty → identical\n            return 1.0 - (double)intersection / union;\n        }\n    }\n} |
20 | metrics/SpcBatch.JensenShannon.cs | {1107 116 0.3514 4.3965} | 1142 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildJensenShannonGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Jensen-Shannon metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                JensenShannonDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double JensenShannonDistance(double[] p, double[] q)\n        {\n            double jsd = 0;\n            for (int i = 0; i < p.Length; i++)\n            {\n                double m = 0.5 * (p[i] + q[i]);\n                if (m < 1e-15) continue;\n                if (p[i] > 1e-15) jsd += p[i] * Math.Log(p[i] / m);\n                if (q[i] > 1e-15) jsd += q[i] * Math.Log(q[i] / m);\n            }\n\n            return Math.Sqrt(0.5 * jsd);\n        }\n    }\n} |
21 | metrics/SpcBatch.Mahalanobis.cs | {3281 289 0.3581 4.3817} | 3376 | using System;\nusing System.Buffers;\nusing System.Runtime.CompilerServices;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildMahalanobisGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Mahalanobis metric.\");\n            if (request.CovarianceInverse == null)\n                throw new ArgumentException(\"CovarianceInverse must be provided for Mahalanobis metric.\");\n\n            n = request.Features.Length;\n            int dim = request.Features[0].Length;\n            if (request.CovarianceInverse.GetLength(0) != dim || request.CovarianceInverse.GetLength(1) != dim)\n                throw new ArgumentException(\"CovarianceInverse dimensions must match feature dimensionality.\");\n\n            var invCov = request.CovarianceInverse;\n\n            Func<double[], double[], double> distFunc = (a, b) => MahalanobisDistance(a, b, invCov, dim);\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                distFunc, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static readonly ThreadLocal<(double[] diff, double[] temp)> _mahalScratch =\n            new(() => (new double[512], new double[512]));\n\n        private static double MahalanobisDistance(double[] a, double[] b, double[,] invCov, int dim)\n        {\n            if (dim <= 64)\n            {\n                Span<double> diff = stackalloc double[dim];\n                Span<double> temp = stackalloc double[dim];\n                return MahalanobisCore(a, b, invCov, diff, temp, dim);\n            }\n\n            if (dim <= 512)\n            {\n                var (diffArr, tempArr) = _mahalScratch.Value;\n                return MahalanobisCore(a, b, invCov,\n                    diffArr.AsSpan(0, dim),\n                    tempArr.AsSpan(0, dim), dim);\n            }\n\n            double[] rentedDiff = ArrayPool<double>.Shared.Rent(dim);\n            double[] rentedTemp = ArrayPool<double>.Shared.Rent(dim);\n            try\n            {\n                return MahalanobisCore(a, b, invCov,\n                    rentedDiff.AsSpan(0, dim),\n                    rentedTemp.AsSpan(0, dim), dim);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedDiff);\n                ArrayPool<double>.Shared.Return(rentedTemp);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double MahalanobisCore(\n            double[] a, double[] b, double[,] invCov,\n            Span<double> diff, Span<double> temp, int dim)\n        {\n\n            for (int i = 0; i < dim; i++)\n                diff[i] = a[i] - b[i];\n\n            for (int i = 0; i < dim; i++)\n            {\n                double s = 0;\n                for (int j = 0; j < dim; j++)\n                    s += invCov[i, j] * diff[j];\n                temp[i] = s;\n            }\n\n            double quad = 0;\n            for (int i = 0; i < dim; i++)\n                quad += diff[i] * temp[i];\n\n            return Math.Sqrt(Math.Max(0, quad));\n        }\n    }\n} |
22 | metrics/SpcBatch.Manhattan.cs | {827 67 0.2745 4.4886} | 854 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildManhattanGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Manhattan metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                ManhattanDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double ManhattanDistance(double[] a, double[] b)\n        {\n            return TensorPrimitives.SumOfAbsoluteDifferences<double>(a, b);\n        }\n    }\n} |
23 | metrics/SpcBatch.Minkowski.cs | {1500 157 0.3520 4.4107} | 1546 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildMinkowskiGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Minkowski metric.\");\n\n            n = request.Features.Length;\n            double p = request.MinkowskiP;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                (a, b) => MinkowskiDistance(a, b, p), request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double MinkowskiDistance(double[] a, double[] b, double p)\n        {\n\n            if (p == 1.0) return TensorPrimitives.SumOfAbsoluteDifferences<double>(a, b);\n            if (p == 2.0) return TensorPrimitives.Distance<double>(a, b);\n\n            double maxDiff = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double d = Math.Abs(a[i] - b[i]);\n                if (d > maxDiff) maxDiff = d;\n            }\n            if (maxDiff == 0) return 0.0;\n\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double scaled = Math.Abs(a[i] - b[i]) / maxDiff;\n                sum += Math.Pow(scaled, p);\n            }\n            return maxDiff * Math.Pow(sum, 1.0 / p);\n        }\n    }\n} |
24 | metrics/SpcBatch.Wasserstein.cs | {1148 114 0.3659 4.2832} | 1187 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        private static Edge[] BuildWassersteinGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Wasserstein metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                Wasserstein1D, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double Wasserstein1D(double[] a, double[] b)\n        {\n\n            var sa = (double[])a.Clone();\n            var sb = (double[])b.Clone();\n            Array.Sort(sa);\n            Array.Sort(sb);\n\n            double cumA = 0, cumB = 0, sum = 0;\n            for (int i = 0; i < sa.Length; i++)\n            {\n                cumA += sa[i];\n                cumB += sb[i];\n                sum += Math.Abs(cumA - cumB);\n            }\n            return sa.Length > 0 ? sum / sa.Length : 0.0;\n        }\n    }\n} |
25 | proximities/SpcBatch.EpsilonBall.cs | {1842 186 0.4142 4.0938} | 1901 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighborsEpsilonBall(\n            int n, double epsilon, Func<int, int, double> pairDistance)\n        {\n            if (epsilon <= 0)\n                throw new ArgumentException(\"Epsilon must be positive for EpsilonBall proximity.\");\n\n            var halfLists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                halfLists[i] = new List<Neighbor>();\n\n            Parallel.For(0, n, i =>\n            {\n                for (int j = i + 1; j < n; j++)\n                {\n                    double d = pairDistance(i, j);\n                    if (d < epsilon)\n                        halfLists[i].Add(new Neighbor { Index = j, Distance = d });\n                }\n            });\n\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in halfLists[i])\n                {\n                    lists[i].Add(nb);\n                    lists[nb.Index].Add(new Neighbor { Index = i, Distance = nb.Distance });\n                }\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return (allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
26 | proximities/SpcBatch.Knn.cs | {2310 227 0.4203 4.1020} | 2380 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighborsKnn(\n            int n, int k, Func<int, int, double> pairDistance)\n        {\n\n            var directedNeighbors = new Neighbor[n][];\n\n            Parallel.For(0, n, i =>\n            {\n                var heap = new BoundedMinHeap(k);\n                for (int j = 0; j < n; j++)\n                {\n                    if (i == j) continue;\n                    double d = pairDistance(i, j);\n                    heap.TryAdd(j, d);\n                }\n                directedNeighbors[i] = heap.GetSorted();\n            });\n\n            var pairMap = new Dictionary<long, double>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in directedNeighbors[i])\n                {\n                    int lo = Math.Min(i, nb.Index);\n                    int hi = Math.Max(i, nb.Index);\n                    long key = (long)lo * n + hi;\n\n                    if (!pairMap.TryGetValue(key, out double existing) || nb.Distance < existing)\n                        pairMap[key] = nb.Distance;\n                }\n            }\n\n            var symmetricLists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                symmetricLists[i] = new List<Neighbor>();\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                double d = kvp.Value;\n                symmetricLists[lo].Add(new Neighbor { Index = hi, Distance = d });\n                symmetricLists[hi].Add(new Neighbor { Index = lo, Distance = d });\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                symmetricLists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = symmetricLists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return (allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
27 | proximities/SpcBatch.MstAugmented.cs | {2902 289 0.4559 3.9445} | 2992 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighborsMstAugmented(\n            int n, int k, Func<int, int, double> pairDistance)\n        {\n\n            var (mutualNeighbors, _) = SelectNeighborsMutualKnn(n, k, pairDistance);\n\n            var existingPairs = new HashSet<long>();\n            var pairMap = new Dictionary<long, double>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in mutualNeighbors[i])\n                {\n                    int lo = Math.Min(i, nb.Index);\n                    int hi = Math.Max(i, nb.Index);\n                    long key = (long)lo * n + hi;\n                    existingPairs.Add(key);\n                    if (!pairMap.ContainsKey(key))\n                        pairMap[key] = nb.Distance;\n                }\n            }\n\n            var candidates = new List<(int i, int j, double d)>();\n            for (int i = 0; i < n; i++)\n            {\n                for (int j = i + 1; j < n; j++)\n                {\n                    candidates.Add((i, j, pairDistance(i, j)));\n                }\n            }\n            candidates.Sort((a, b) => a.d.CompareTo(b.d));\n\n            var uf = new FastUnionFind(n);\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                uf.Union(lo, hi);\n            }\n\n            foreach (var (ci, cj, cd) in candidates)\n            {\n                if (uf.Find(ci) != uf.Find(cj))\n                {\n                    long key = (long)ci * n + cj;\n                    if (!existingPairs.Contains(key))\n                    {\n                        pairMap[key] = cd;\n                    }\n                    uf.Union(ci, cj);\n                }\n            }\n\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                double d = kvp.Value;\n                lists[lo].Add(new Neighbor { Index = hi, Distance = d });\n                lists[hi].Add(new Neighbor { Index = lo, Distance = d });\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return (allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
28 | proximities/SpcBatch.MutualKnn.cs | {2492 240 0.4334 4.0323} | 2567 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighborsMutualKnn(\n            int n, int k, Func<int, int, double> pairDistance)\n        {\n\n            var directedNeighbors = new Neighbor[n][];\n            var directedSets = new HashSet<int>[n];\n\n            Parallel.For(0, n, i =>\n            {\n                var heap = new BoundedMinHeap(k);\n                for (int j = 0; j < n; j++)\n                {\n                    if (i == j) continue;\n                    heap.TryAdd(j, pairDistance(i, j));\n                }\n                directedNeighbors[i] = heap.GetSorted();\n                directedSets[i] = new HashSet<int>();\n                foreach (var nb in directedNeighbors[i])\n                    directedSets[i].Add(nb.Index);\n            });\n\n            var pairMap = new Dictionary<long, double>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in directedNeighbors[i])\n                {\n                    int j = nb.Index;\n                    if (i < j && directedSets[j].Contains(i))\n                    {\n                        long key = (long)i * n + j;\n\n                        if (!pairMap.TryGetValue(key, out double existing) || nb.Distance < existing)\n                            pairMap[key] = nb.Distance;\n                    }\n                }\n            }\n\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                double d = kvp.Value;\n                lists[lo].Add(new Neighbor { Index = hi, Distance = d });\n                lists[hi].Add(new Neighbor { Index = lo, Distance = d });\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return (allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
