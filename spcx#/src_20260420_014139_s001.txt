idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | analysis/analysis.chi.cs | {3100 257 0.2952 4.3095} | 3175 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcAnalysis\n    {\n        // =====================================================================\n        // Susceptibility result bundle\n        // =====================================================================\n\n        /// <summary>\n        /// Susceptibility-based analysis result, corresponding to the original\n        /// Blatt/Wiseman/Domany analytical approach. One value per temperature.\n        /// </summary>\n        public sealed class SusceptibilityResult\n        {\n            public double[] Temperatures { get; set; }\n            /// <summary>\n            /// Susceptibility (variance of cluster sizes in equilibrium).\n            /// Computed inline in RunSimulation — carried through here for\n            /// convenience alongside derived quantities.\n            /// </summary>\n            public double[] Susceptibility { get; set; }\n            /// <summary>Distinct spin-label count per temperature.</summary>\n            public int[] ClusterCount { get; set; }\n            /// <summary>\n            /// Per-temperature cluster-size histograms on common support.\n            /// </summary>\n            public double[][] ClusterSizeHistograms { get; set; }\n            /// <summary>\n            /// Critical temperatures detected from susceptibility peaks.\n            /// Multiple peaks = multi-scale structure or multiple transitions.\n            /// </summary>\n            public double[] CriticalTemperatures { get; set; }\n        }\n\n        // =====================================================================\n        // Susceptibility analysis pipeline\n        // =====================================================================\n\n        /// <summary>\n        /// Full susceptibility-based analysis. Builds histograms, counts\n        /// clusters per T, detects critical temperatures from chi peaks.\n        /// Temperatures must be sorted ascending.\n        /// </summary>\n        public static SusceptibilityResult AnalyzeSusceptibility(\n            SpcBatchResult batchResult,\n            double peakProminence = 0.1)\n        {\n            int numT = batchResult.Temperatures.Length;\n            if (numT == 0)\n                throw new ArgumentException(\"SpcBatchResult has no temperatures.\");\n\n            var histograms = BuildHistograms(batchResult);\n            var clusterCounts = CountClusters(batchResult);\n\n            var peakIndices = DetectPeaks(batchResult.Susceptibility, peakProminence);\n            var criticalTs = new double[peakIndices.Length];\n            for (int i = 0; i < peakIndices.Length; i++)\n                criticalTs[i] = batchResult.Temperatures[peakIndices[i]];\n\n            return new SusceptibilityResult\n            {\n                Temperatures = (double[])batchResult.Temperatures.Clone(),\n                Susceptibility = (double[])batchResult.Susceptibility.Clone(),\n                ClusterCount = clusterCounts,\n                ClusterSizeHistograms = histograms,\n                CriticalTemperatures = criticalTs\n            };\n        }\n    }\n} |
1 | analysis/analysis.kl.cs | {16383 1566 0.3883 4.2551} | 16805 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    public static partial class SpcAnalysis\n    {\n        // =====================================================================\n        // KL result bundles\n        // =====================================================================\n\n        /// <summary>Global KL analysis — one number per temperature gap.</summary>\n        public sealed class GlobalKLResult\n        {\n            public double[] Temperatures { get; set; }\n            /// <summary>\n            /// Forward KL between adjacent T slices. Length = numT - 1.\n            /// KL[i] = KL(P_i || P_{i+1}). Plot at midpoint T.\n            /// </summary>\n            public double[] ForwardKL { get; set; }\n            /// <summary>Fisher information: 2 * KL / dT^2.</summary>\n            public double[] FisherInT { get; set; }\n            /// <summary>Critical temperatures from KL peaks.</summary>\n            public double[] CriticalTemperatures { get; set; }\n        }\n\n        /// <summary>Fan-out analysis — per-medoid-pair, per-temperature.</summary>\n        public sealed class FanOutResult\n        {\n            public double[] Temperatures { get; set; }\n            public int[] MedoidIndices { get; set; }\n            /// <summary>\n            /// DivergenceByT[t][i][j] = KL(P_i(T_t) || P_j(T_t)) between\n            /// medoid local neighborhoods.\n            /// </summary>\n            public double[][][] DivergenceByT { get; set; }\n            /// <summary>\n            /// Fisher matrix: 2 * |D_ij(T+dT) - D_ij(T)| / dT^2.\n            /// Shape: [numT-1][numMedoids][numMedoids].\n            /// </summary>\n            public double[][][] FisherByT { get; set; }\n            /// <summary>\n            /// Per-pair merge temperature. NaN if never merges.\n            /// Shape: [numMedoids][numMedoids].\n            /// </summary>\n            public double[][] MergeTemperatures { get; set; }\n        }\n\n        /// <summary>Radial profile for one medoid at one temperature.</summary>\n        public sealed class RadialProfile\n        {\n            public int MedoidIndex { get; set; }\n            /// <summary>\n            /// Histograms by graph-distance radius. Index 0 = medoid itself.\n            /// </summary>\n            public double[][] HistogramsByRadius { get; set; }\n            /// <summary>Node count per shell.</summary>\n            public int[] ShellSizes { get; set; }\n            /// <summary>\n            /// KL between adjacent shells. Spike at r = cluster boundary.\n            /// </summary>\n            public double[] RadialKL { get; set; }\n        }\n\n        /// <summary>\n        /// Radial heatmap — per medoid, per temperature. The 2D view\n        /// (radius × temperature) shows cluster dissolution from boundary\n        /// inward as T rises.\n        /// </summary>\n        public sealed class RadialHeatmapResult\n        {\n            public double[] Temperatures { get; set; }\n            public int[] MedoidIndices { get; set; }\n            /// <summary>Profiles[t][m] = radial profile for medoid m at T_t.</summary>\n            public RadialProfile[][] Profiles { get; set; }\n            public int MaxRadius { get; set; }\n        }\n\n        // =====================================================================\n        // KL primitive\n        // =====================================================================\n\n        /// <summary>\n        /// Forward KL divergence: KL(P || Q) = sum_i p_i * log(p_i / q_i).\n        /// Dense fixed-support. Both arrays must have equal length.\n        ///\n        /// Zero conventions:\n        ///   p[i] = 0 → skip (0 * log(0/q) = 0)\n        ///   q[i] = 0, p[i] > 0 → floor q at eps\n        /// </summary>\n        public static double ForwardKL(double[] p, double[] q, double eps = 1e-12)\n        {\n            if (p.Length != q.Length)\n                throw new ArgumentException(\n                    $\"P and Q must have same support size (got {p.Length} and {q.Length}).\");\n            if (eps <= 0)\n                throw new ArgumentException(\"eps must be positive.\");\n\n            double sum = 0.0;\n            for (int i = 0; i < p.Length; i++)\n            {\n                double pi = p[i];\n                if (pi <= 0.0) continue;\n                double qi = q[i] > eps ? q[i] : eps;\n                sum += pi * Math.Log(pi / qi);\n            }\n            return sum;\n        }\n\n        /// <summary>Jeffreys divergence: KL(P||Q) + KL(Q||P).</summary>\n        public static double SymmetricKL(double[] p, double[] q, double eps = 1e-12)\n        {\n            return ForwardKL(p, q, eps) + ForwardKL(q, p, eps);\n        }\n\n        /// <summary>Fisher information: F(T) ~ 2 * KL / dT^2.</summary>\n        public static double ComputeFisherInT(double kl, double deltaT)\n        {\n            if (deltaT <= 0) return 0.0;\n            return 2.0 * kl / (deltaT * deltaT);\n        }\n\n        // =====================================================================\n        // Global KL analysis\n        // =====================================================================\n\n        /// <summary>\n        /// Global KL analysis: Forward KL between adjacent T slices of the\n        /// cluster-size histogram, Fisher information, critical T detection.\n        /// Temperatures must be sorted ascending.\n        /// </summary>\n        public static GlobalKLResult AnalyzeGlobalKL(\n            SpcBatchResult batchResult,\n            double eps = 1e-12,\n            double peakProminence = 0.1)\n        {\n            int numT = batchResult.Temperatures.Length;\n            if (numT == 0)\n                throw new ArgumentException(\"SpcBatchResult has no temperatures.\");\n\n            var histograms = BuildHistograms(batchResult);\n\n            double[] kl = new double[Math.Max(0, numT - 1)];\n            double[] fisher = new double[kl.Length];\n            for (int i = 0; i < kl.Length; i++)\n            {\n                kl[i] = ForwardKL(histograms[i], histograms[i + 1], eps);\n                double dT = batchResult.Temperatures[i + 1] - batchResult.Temperatures[i];\n                fisher[i] = ComputeFisherInT(kl[i], dT);\n            }\n\n            // Detect peaks in KL curve; map gap indices to midpoint temperatures\n            var peakIndices = DetectPeaks(kl, peakProminence);\n            var criticalTs = new double[peakIndices.Length];\n            for (int i = 0; i < peakIndices.Length; i++)\n            {\n                int idx = peakIndices[i];\n                criticalTs[i] = 0.5 * (batchResult.Temperatures[idx]\n                                      + batchResult.Temperatures[idx + 1]);\n            }\n\n            return new GlobalKLResult\n            {\n                Temperatures = (double[])batchResult.Temperatures.Clone(),\n                ForwardKL = kl,\n                FisherInT = fisher,\n                CriticalTemperatures = criticalTs\n            };\n        }\n\n        // =====================================================================\n        // Fan-out analysis — spatially-resolved phase diagram\n        // =====================================================================\n\n        /// <summary>\n        /// Fan-out: medoids at reference T, local histograms per medoid per T,\n        /// pairwise KL, Fisher matrix, merge temperature detection.\n        /// </summary>\n        public static FanOutResult AnalyzeFanOut(\n            SpcBatchResult batchResult,\n            int referenceTemperatureIndex = 0,\n            double mergeThreshold = 0.05,\n            double eps = 1e-12)\n        {\n            int numT = batchResult.Temperatures.Length;\n            if (numT == 0)\n                throw new ArgumentException(\"SpcBatchResult has no temperatures.\");\n            if (batchResult.Graph.NodeCount == 0)\n                throw new ArgumentException(\n                    \"SpcBatchResult.Graph is empty. Stash CsrGraph on the result DTO.\");\n            if (referenceTemperatureIndex < 0 || referenceTemperatureIndex >= numT)\n                throw new ArgumentOutOfRangeException(nameof(referenceTemperatureIndex));\n\n            double refT = batchResult.Temperatures[referenceTemperatureIndex];\n            if (!batchResult.FinalSpins.TryGetValue(refT, out var refSpins))\n                throw new ArgumentException($\"No spin data at reference T={refT}.\");\n\n            var medoids = FindMedoids(refSpins, batchResult.Graph);\n            int numMedoids = medoids.Length;\n            if (numMedoids == 0)\n                return new FanOutResult\n                {\n                    Temperatures = (double[])batchResult.Temperatures.Clone(),\n                    MedoidIndices = Array.Empty<int>(),\n                    DivergenceByT = Array.Empty<double[][]>(),\n                    FisherByT = Array.Empty<double[][]>(),\n                    MergeTemperatures = Array.Empty<double[]>()\n                };\n\n            // Pairwise KL at each temperature\n            var divByT = new double[numT][][];\n            for (int t = 0; t < numT; t++)\n            {\n                double T = batchResult.Temperatures[t];\n                batchResult.FinalSpins.TryGetValue(T, out var spins);\n\n                var localHists = new double[numMedoids][];\n                for (int m = 0; m < numMedoids; m++)\n                    localHists[m] = BuildLocalHistogram(\n                        medoids[m], spins, batchResult.Graph);\n\n                var klMatrix = new double[numMedoids][];\n                for (int i = 0; i < numMedoids; i++)\n                {\n                    klMatrix[i] = new double[numMedoids];\n                    for (int j = 0; j < numMedoids; j++)\n                    {\n                        if (i == j)\n                            klMatrix[i][j] = 0.0;\n                        else\n                            klMatrix[i][j] = ForwardKL(localHists[i], localHists[j], eps);\n                    }\n                }\n                divByT[t] = klMatrix;\n            }\n\n            // Fisher information matrix\n            var fisherByT = new double[Math.Max(0, numT - 1)][][];\n            for (int t = 0; t < fisherByT.Length; t++)\n            {\n                double dT = batchResult.Temperatures[t + 1] - batchResult.Temperatures[t];\n                double dT2 = dT * dT;\n                var fMatrix = new double[numMedoids][];\n                for (int i = 0; i < numMedoids; i++)\n                {\n                    fMatrix[i] = new double[numMedoids];\n                    for (int j = 0; j < numMedoids; j++)\n                    {\n                        if (dT2 > 0)\n                        {\n                            double dD = Math.Abs(divByT[t + 1][i][j] - divByT[t][i][j]);\n                            fMatrix[i][j] = 2.0 * dD / dT2;\n                        }\n                    }\n                }\n                fisherByT[t] = fMatrix;\n            }\n\n            // Merge temperature detection\n            var mergeTs = new double[numMedoids][];\n            for (int i = 0; i < numMedoids; i++)\n            {\n                mergeTs[i] = new double[numMedoids];\n                for (int j = 0; j < numMedoids; j++)\n                {\n                    mergeTs[i][j] = double.NaN;\n                    if (i == j) { mergeTs[i][j] = 0.0; continue; }\n                    for (int t = 0; t < numT; t++)\n                    {\n                        if (divByT[t][i][j] < mergeThreshold)\n                        {\n                            mergeTs[i][j] = batchResult.Temperatures[t];\n                            break;\n                        }\n                    }\n                }\n            }\n\n            return new FanOutResult\n            {\n                Temperatures = (double[])batchResult.Temperatures.Clone(),\n                MedoidIndices = medoids,\n                DivergenceByT = divByT,\n                FisherByT = fisherByT,\n                MergeTemperatures = mergeTs\n            };\n        }\n\n        // =====================================================================\n        // Radial coherence heatmap\n        // =====================================================================\n\n        /// <summary>\n        /// Radial coherence: BFS outward from each medoid, build spin-label\n        /// histograms per shell, compute KL between adjacent shells. BFS\n        /// shells are computed once (topology fixed); only spin-label\n        /// binning repeats per temperature.\n        ///\n        /// The 2D heatmap (x=T, y=radius, color=KL) for a single medoid\n        /// shows cluster dissolution as a wave moving inward from the\n        /// boundary as T rises.\n        /// </summary>\n        public static RadialHeatmapResult AnalyzeRadialCoherence(\n            SpcBatchResult batchResult,\n            int[] medoids,\n            int maxRadius = 4,\n            double eps = 1e-12)\n        {\n            int numT = batchResult.Temperatures.Length;\n            if (numT == 0)\n                throw new ArgumentException(\"SpcBatchResult has no temperatures.\");\n            if (batchResult.Graph.NodeCount == 0)\n                throw new ArgumentException(\"SpcBatchResult.Graph is empty.\");\n            if (medoids == null || medoids.Length == 0)\n                throw new ArgumentException(\"No medoids provided.\");\n\n            int numMedoids = medoids.Length;\n            var profiles = new RadialProfile[numT][];\n\n            // BFS shells once per medoid\n            var shellsByMedoid = new int[numMedoids][][];\n            for (int m = 0; m < numMedoids; m++)\n                shellsByMedoid[m] = BfsShells(medoids[m], batchResult.Graph, maxRadius);\n\n            for (int t = 0; t < numT; t++)\n            {\n                double T = batchResult.Temperatures[t];\n                batchResult.FinalSpins.TryGetValue(T, out var spins);\n\n                int maxLabel = 0;\n                if (spins != null)\n                    for (int i = 0; i < spins.Length; i++)\n                        if (spins[i] > maxLabel) maxLabel = spins[i];\n                int support = maxLabel + 1;\n\n                profiles[t] = new RadialProfile[numMedoids];\n                for (int m = 0; m < numMedoids; m++)\n                {\n                    var shells = shellsByMedoid[m];\n                    int actualDepth = shells.Length;\n                    var histsByR = new double[actualDepth][];\n                    var shellSizes = new int[actualDepth];\n\n                    for (int r = 0; r < actualDepth; r++)\n                    {\n                        var shell = shells[r];\n                        shellSizes[r] = shell.Length;\n                        var hist = new double[support];\n                        if (spins != null)\n                            for (int s = 0; s < shell.Length; s++)\n                                hist[spins[shell[s]]]++;\n                        if (shell.Length > 0)\n                            for (int k = 0; k < support; k++) hist[k] /= shell.Length;\n                        histsByR[r] = hist;\n                    }\n\n                    var radialKL = new double[Math.Max(0, actualDepth - 1)];\n                    for (int r = 0; r < radialKL.Length; r++)\n                        radialKL[r] = ForwardKL(histsByR[r], histsByR[r + 1], eps);\n\n                    profiles[t][m] = new RadialProfile\n                    {\n                        MedoidIndex = medoids[m],\n                        HistogramsByRadius = histsByR,\n                        ShellSizes = shellSizes,\n                        RadialKL = radialKL\n                    };\n                }\n            }\n\n            return new RadialHeatmapResult\n            {\n                Temperatures = (double[])batchResult.Temperatures.Clone(),\n                MedoidIndices = (int[])medoids.Clone(),\n                Profiles = profiles,\n                MaxRadius = maxRadius\n            };\n        }\n\n        // =====================================================================\n        // Future: Mixture-Q overload (reserved)\n        // =====================================================================\n\n        // TODO: KL against averaged reference distribution across a temperature\n        // window. More robust than adjacent-slice near the transition.\n        //\n        // public static double[] ForwardKLAgainstMixture(\n        //     double[][] histograms,\n        //     int windowSize,\n        //     double eps = 1e-12) { ... }\n    }\n} |
2 | analysis/analysis.mhbs.cs | {1042 151 0.1440 4.6719} | 1064 | // Mahalanobis-based thermodynamic analysis — DESIGN FROZEN.\n//\n// Intent: a Mahalanobis pillar in the expanded thermodynamic analysis\n// framework, sitting alongside the KL / Fisher-in-T / susceptibility /\n// fan-out / radial-coherence diagnostics already shipped in\n// spc.analysis.cs. This would summarise per-cluster covariance geometry\n// during the temperature sweep, not pairwise distance.\n//\n// Why frozen: the broader analysis-layer shape is still under design.\n// SGLD as a potential outer-loop sampler over coupling / kernel scale\n// may reshape what \"thermodynamic analysis\" even means here (posterior\n// over couplings vs. point estimates per T), and committing to a\n// Mahalanobis API now risks rework. All direct analysis-layer\n// development is paused until the SGLD / RunEpochs control-plane\n// decisions land.\n//\n// Distinct from: SpcBatch.Mahalanobis.cs (metrics/) which provides the\n// pairwise *metric* form for graph construction. That is shipping and\n// performance-tuned. This file is reserved for the *analysis* form. |
3 | deltas/SpcBatch.EstimatorMean.cs | {712 82 0.3230 4.3092} | 736 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Mean estimator: arithmetic mean of 1-NN distances.\n        /// Parametric — assumes roughly Gaussian distance distribution.\n        /// Fast (O(N)) but outlier-sensitive: a few distant points inflate Δ\n        /// globally, smearing cluster boundaries.\n        /// </summary>\n        internal static double EstimateDeltaMean(double[] nnDistances)\n        {\n            if (nnDistances.Length == 0) return 1.0;\n            double sum = 0;\n            for (int i = 0; i < nnDistances.Length; i++)\n                sum += nnDistances[i];\n            return sum / nnDistances.Length;\n        }\n    }\n} |
4 | deltas/SpcBatch.EstimatorMedian.cs | {1080 119 0.3370 4.3453} | 1116 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Median estimator: median of 1-NN distances.\n        /// Non-parametric — 50% breakdown point, robust to outliers.\n        /// The \"microscopic\" change that moves the Gaussian kernel's bandwidth\n        /// selection from the parametric to non-parametric camp.\n        /// O(N log N) for the sort.\n        /// </summary>\n        internal static double EstimateDeltaMedian(double[] nnDistances)\n        {\n            if (nnDistances.Length == 0) return 1.0;\n            double[] sorted = (double[])nnDistances.Clone();\n            Array.Sort(sorted);\n            return MedianOfSorted(sorted);\n        }\n\n        /// <summary>\n        /// Shared helper: median of a pre-sorted array.\n        /// </summary>\n        private static double MedianOfSorted(double[] sorted)\n        {\n            int mid = sorted.Length / 2;\n            return sorted.Length % 2 == 0\n                ? (sorted[mid - 1] + sorted[mid]) / 2.0\n                : sorted[mid];\n        }\n    }\n} |
5 | hashish/bm25-stats.cs | {3619 357 0.2675 4.7087} | 3730 | using System;\nusing System.Collections.Frozen;\nusing System.Collections.Generic;\nusing System.Runtime.CompilerServices;\nusing System.Runtime.InteropServices;\nusing System.Text.RegularExpressions;\n\nnamespace Hashish;\n\n/// <summary>\n/// Computes BM25 document-frequency statistics from a text corpus.\n/// Output feeds directly into <see cref=\"SimHash\"/> as an immutable IDF map.\n/// </summary>\npublic static class Bm25Stats\n{\n    // Shared with SimHash — compiled once, linear-time (NonBacktracking), no per-call overhead.\n    internal static readonly Regex WordRegex = new(\n        @\"\\w+\",\n        RegexOptions.Compiled | RegexOptions.CultureInvariant | RegexOptions.NonBacktracking\n    );\n\n    /// <summary>\n    /// Computes average document length and IDF weights from a collection of documents.\n    /// IDF formula: log((N + 1) / (df + 1)) + 1  (Lucene/BM25+ smoothed variant).\n    /// </summary>\n    /// <param name=\"documents\">Input corpus.</param>\n    /// <param name=\"ignoreCase\">Fold tokens to lowercase before counting. Default true.</param>\n    /// <returns>\n    /// A tuple of <c>AvgDocLength</c> (mean token count) and a frozen IDF map\n    /// ready for direct use in <see cref=\"SimHash\"/>.\n    /// </returns>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public static (double AvgDocLength, FrozenDictionary<string, double> IdfMap)\n        Compute(IReadOnlyList<string> documents, bool ignoreCase = true)\n    {\n        ArgumentNullException.ThrowIfNull(documents);\n\n        int docCount = documents.Count;\n        if (docCount == 0)\n            return (0.0, FrozenDictionary<string, double>.Empty);\n\n        double totalTokens = 0.0;\n\n        // doc-frequency: how many documents contain each token (not total occurrences).\n        var docFreq = new Dictionary<string, int>(\n            capacity: 1024,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        // per-document seen-set reused across iterations.\n        var seen = new HashSet<string>(\n            capacity: 256,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        for (int d = 0; d < docCount; d++)\n        {\n            string doc = documents[d];\n            if (string.IsNullOrEmpty(doc)) continue;\n\n            var matches = WordRegex.Matches(doc);\n            totalTokens += matches.Count;\n\n            seen.Clear();\n            foreach (System.Text.RegularExpressions.Match m in matches)\n            {\n                // AsSpan avoids allocating a lowercase string until we need it for insertion.\n                string token = ignoreCase ? m.Value.ToLowerInvariant() : m.Value;\n                seen.Add(token);\n            }\n\n            foreach (string token in seen)\n            {\n                ref int freq = ref CollectionsMarshal.GetValueRefOrAddDefault(docFreq, token, out _);\n                freq++;\n            }\n        }\n\n        double avgDocLength = totalTokens / docCount;\n\n        // Build IDF map and freeze — one allocation, then permanently immutable.\n        var idfBuilder = new Dictionary<string, double>(\n            capacity: docFreq.Count,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        double n1 = docCount + 1.0;\n        foreach (var kvp in docFreq)\n        {\n            idfBuilder[kvp.Key] = Math.Log(n1 / (kvp.Value + 1.0)) + 1.0;\n        }\n\n        return (avgDocLength, idfBuilder.ToFrozenDictionary(\n            ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        ));\n    }\n} |
