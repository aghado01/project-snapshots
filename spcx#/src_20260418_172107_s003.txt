idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
29 | spc.analysis.cs | {8597 873 0.4477 3.9362} | 8837 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n\n    public static partial class SpcAnalysis\n    {\n\n        public static double[][] BuildHistograms(SpcBatchResult batchResult)\n        {\n            int numT = batchResult.Temperatures.Length;\n            var histograms = new double[numT][];\n\n            int maxLabel = -1;\n            for (int i = 0; i < numT; i++)\n            {\n                double T = batchResult.Temperatures[i];\n                if (!batchResult.FinalSpins.TryGetValue(T, out var spins)) continue;\n                for (int j = 0; j < spins.Length; j++)\n                    if (spins[j] > maxLabel) maxLabel = spins[j];\n            }\n            int support = maxLabel + 1;\n            if (support <= 0) support = 1;\n\n            for (int i = 0; i < numT; i++)\n            {\n                double T = batchResult.Temperatures[i];\n                var hist = new double[support];\n                if (batchResult.FinalSpins.TryGetValue(T, out var spins))\n                {\n                    int n = spins.Length;\n                    for (int j = 0; j < n; j++) hist[spins[j]]++;\n                    if (n > 0)\n                        for (int k = 0; k < support; k++) hist[k] /= n;\n                }\n                histograms[i] = hist;\n            }\n            return histograms;\n        }\n\n        public static double[] BuildHistogram(int[] spins)\n        {\n            if (spins == null || spins.Length == 0) return new double[0];\n            int maxLabel = -1;\n            for (int i = 0; i < spins.Length; i++)\n                if (spins[i] > maxLabel) maxLabel = spins[i];\n            int support = maxLabel + 1;\n            var hist = new double[support];\n            for (int i = 0; i < spins.Length; i++) hist[spins[i]]++;\n            for (int k = 0; k < support; k++) hist[k] /= spins.Length;\n            return hist;\n        }\n\n        public static double[] BuildLocalHistogram(\n            int nodeIndex, int[] spins, CsrGraph graph)\n        {\n            int maxLabel = 0;\n            for (int i = 0; i < spins.Length; i++)\n                if (spins[i] > maxLabel) maxLabel = spins[i];\n            int support = maxLabel + 1;\n\n            var hist = new double[support];\n            int count = 0;\n\n            hist[spins[nodeIndex]]++;\n            count++;\n\n            int start = graph.RowPointers[nodeIndex];\n            int end = graph.RowPointers[nodeIndex + 1];\n            for (int e = start; e < end; e++)\n            {\n                hist[spins[graph.Targets[e]]]++;\n                count++;\n            }\n\n            if (count > 0)\n                for (int k = 0; k < support; k++) hist[k] /= count;\n            return hist;\n        }\n\n        public static int[] CountClusters(SpcBatchResult batchResult)\n        {\n            int numT = batchResult.Temperatures.Length;\n            var counts = new int[numT];\n            for (int i = 0; i < numT; i++)\n            {\n                double T = batchResult.Temperatures[i];\n                if (!batchResult.FinalSpins.TryGetValue(T, out var spins)) continue;\n                var seen = new HashSet<int>();\n                for (int j = 0; j < spins.Length; j++) seen.Add(spins[j]);\n                counts[i] = seen.Count;\n            }\n            return counts;\n        }\n\n        public static int[] FindMedoids(int[] spins, CsrGraph graph)\n        {\n            int n = spins.Length;\n\n            var clusters = new Dictionary<int, List<int>>();\n            for (int i = 0; i < n; i++)\n            {\n                int label = spins[i];\n                if (!clusters.TryGetValue(label, out var members))\n                {\n                    members = new List<int>();\n                    clusters[label] = members;\n                }\n                members.Add(i);\n            }\n\n            var medoidList = new List<(int label, int node)>();\n            foreach (var kvp in clusters)\n            {\n                int clusterLabel = kvp.Key;\n                var members = kvp.Value;\n                if (members.Count == 0) continue;\n\n                int bestNode = members[0];\n                double bestScore = double.MinValue;\n\n                foreach (int node in members)\n                {\n                    double score = 0;\n                    int start = graph.RowPointers[node];\n                    int end = graph.RowPointers[node + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        if (spins[graph.Targets[e]] == clusterLabel)\n                            score += graph.Weights[e];\n                    }\n                    if (score > bestScore)\n                    {\n                        bestScore = score;\n                        bestNode = node;\n                    }\n                }\n                medoidList.Add((clusterLabel, bestNode));\n            }\n\n            medoidList.Sort((a, b) => a.label.CompareTo(b.label));\n            var medoids = new int[medoidList.Count];\n            for (int i = 0; i < medoidList.Count; i++)\n                medoids[i] = medoidList[i].node;\n            return medoids;\n        }\n\n        public static int[][] BfsShells(int source, CsrGraph graph, int maxRadius)\n        {\n            int n = graph.NodeCount;\n            var distance = new int[n];\n            for (int i = 0; i < n; i++) distance[i] = -1;\n            distance[source] = 0;\n\n            var shells = new List<int[]>();\n            var currentShell = new List<int> { source };\n            shells.Add(currentShell.ToArray());\n\n            for (int r = 1; r <= maxRadius; r++)\n            {\n                var nextShell = new List<int>();\n                foreach (int node in currentShell)\n                {\n                    int start = graph.RowPointers[node];\n                    int end = graph.RowPointers[node + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        int neighbor = graph.Targets[e];\n                        if (distance[neighbor] == -1)\n                        {\n                            distance[neighbor] = r;\n                            nextShell.Add(neighbor);\n                        }\n                    }\n                }\n                if (nextShell.Count == 0) break;\n                shells.Add(nextShell.ToArray());\n                currentShell = nextShell;\n            }\n            return shells.ToArray();\n        }\n\n        public static int[] DetectPeaks(double[] signal, double prominence = 0.1)\n        {\n            if (signal == null || signal.Length < 3) return Array.Empty<int>();\n\n            double maxVal = 0.0;\n            for (int i = 0; i < signal.Length; i++)\n                if (signal[i] > maxVal) maxVal = signal[i];\n            if (maxVal <= 0) return Array.Empty<int>();\n            double threshold = prominence * maxVal;\n\n            var peaks = new List<int>();\n            for (int i = 1; i < signal.Length - 1; i++)\n            {\n                if (signal[i] < threshold) continue;\n                bool isPeak = signal[i] >= signal[i - 1]\n                           && signal[i] >= signal[i + 1];\n                bool strict = signal[i] > signal[i - 1]\n                           || signal[i] > signal[i + 1];\n                if (isPeak && strict) peaks.Add(i);\n            }\n            return peaks.ToArray();\n        }\n\n        public static double ComputePurity(int[] predicted, int[] trueLabels)\n        {\n            if (predicted.Length != trueLabels.Length)\n                throw new ArgumentException(\"predicted and trueLabels must have same length.\");\n            int n = predicted.Length;\n            if (n == 0) return 0.0;\n\n            var clusterLabelCounts = new Dictionary<int, Dictionary<int, int>>();\n            for (int i = 0; i < n; i++)\n            {\n                int pred = predicted[i];\n                int truth = trueLabels[i];\n                if (!clusterLabelCounts.TryGetValue(pred, out var inner))\n                {\n                    inner = new Dictionary<int, int>();\n                    clusterLabelCounts[pred] = inner;\n                }\n                inner.TryGetValue(truth, out int count);\n                inner[truth] = count + 1;\n            }\n\n            int totalCorrect = 0;\n            foreach (var cluster in clusterLabelCounts.Values)\n            {\n                int maxCount = 0;\n                foreach (var count in cluster.Values)\n                    if (count > maxCount) maxCount = count;\n                totalCorrect += maxCount;\n            }\n            return (double)totalCorrect / n;\n        }\n    }\n} |
30 | spc.batch.cs | {13523 1143 0.3758 4.2664} | 13904 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n\n    public enum SpcMetric { Hamming, Euclidean, Canberra, Manhattan, Minkowski, Cosine, JensenShannon, Jaccard, Wasserstein, Mahalanobis, FisherRao }\n\n    public enum DeltaEstimator { Mean, Median }\n\n    public enum SpcProximity { Knn, MutualKnn, EpsilonBall, MstAugmented }\n\n    public enum CouplingKernel { Gaussian, Cauchy, Laplacian, Linear }\n\n    public sealed class SpcBatchRequest\n    {\n        public SpcMetric Metric { get; set; } = SpcMetric.Hamming;\n\n        public SpcProximity Proximity { get; set; } = SpcProximity.Knn;\n\n        public double Epsilon { get; set; } = 0.0;\n\n        public ulong[] SimHashes { get; set; }\n\n        public double[][] Features { get; set; }\n\n        public int K { get; set; }\n        public double[] Temperatures { get; set; } = Array.Empty<double>();\n        public int Steps { get; set; }\n        public int BaseSeed { get; set; } = -1;\n\n        public double Delta { get; set; } = 0.0;\n\n        public DeltaEstimator Estimator { get; set; } = DeltaEstimator.Mean;\n\n        public CouplingKernel CouplingKernel { get; set; } = CouplingKernel.Gaussian;\n\n        public double MinkowskiP { get; set; } = 2.0;\n\n        public double JaccardThreshold { get; set; } = 0.5;\n\n        public double[,] CovarianceInverse { get; set; }\n\n        public SpcCheckpoint Checkpoint { get; set; }\n\n        public string ResumeDirectory { get; set; }\n\n        public string CheckpointDirectory { get; set; }\n    }\n\n    public sealed class SpcBatchResult\n    {\n        public double[] Temperatures { get; set; } = Array.Empty<double>();\n        public double[] Susceptibility { get; set; } = Array.Empty<double>();\n        public Dictionary<double, int[]> FinalSpins { get; set; } = new Dictionary<double, int[]>();\n\n        public CsrGraph Graph { get; set; }\n\n        public string[] Warnings { get; set; } = Array.Empty<string>();\n\n        public string RunDirectory { get; set; }\n\n        public SpcCheckpoint ToCheckpoint()\n        {\n            var cp = new SpcCheckpoint();\n            for (int i = 0; i < Temperatures.Length; i++)\n            {\n                double T = Temperatures[i];\n                if (FinalSpins.TryGetValue(T, out var spins))\n                {\n                    cp.Spins[T] = spins;\n                    cp.Susceptibility[T] = Susceptibility[i];\n                }\n            }\n            return cp;\n        }\n    }\n\n    public sealed class SpcCheckpoint\n    {\n\n        public Dictionary<double, int[]> Spins { get; set; } = new Dictionary<double, int[]>();\n        public Dictionary<double, double> Susceptibility { get; set; } = new Dictionary<double, double>();\n    }\n\n    public static partial class SpcBatch\n    {\n\n        public static SpcBatchResult Run(SpcBatchRequest request,\n            System.Threading.CancellationToken ct = default)\n        {\n            Edge[] edges;\n            int n;\n\n            switch (request.Metric)\n            {\n                case SpcMetric.Hamming:\n                    edges = BuildHammingGraph(request, out n);\n                    break;\n                case SpcMetric.Euclidean:\n                    edges = BuildEuclideanGraph(request, out n);\n                    break;\n                case SpcMetric.Canberra:\n                    edges = BuildCanberraGraph(request, out n);\n                    break;\n                case SpcMetric.Manhattan:\n                    edges = BuildManhattanGraph(request, out n);\n                    break;\n                case SpcMetric.Minkowski:\n                    edges = BuildMinkowskiGraph(request, out n);\n                    break;\n                case SpcMetric.Cosine:\n                    edges = BuildCosineGraph(request, out n);\n                    break;\n                case SpcMetric.JensenShannon:\n                    edges = BuildJensenShannonGraph(request, out n);\n                    break;\n                case SpcMetric.Jaccard:\n                    edges = BuildJaccardGraph(request, out n);\n                    break;\n                case SpcMetric.Wasserstein:\n                    edges = BuildWassersteinGraph(request, out n);\n                    break;\n                case SpcMetric.Mahalanobis:\n                    edges = BuildMahalanobisGraph(request, out n);\n                    break;\n                case SpcMetric.FisherRao:\n                    edges = BuildFisherRaoGraph(request, out n);\n                    break;\n                default:\n                    throw new NotSupportedException($\"Metric {request.Metric} not implemented.\");\n            }\n\n            return RunSimulationCore(edges, n, request,\n                ValidateGraph(edges, n, request.Proximity), ct);\n        }\n\n        internal static double EstimateDelta(DeltaEstimator estimator, double[] nnDistances)\n        {\n            switch (estimator)\n            {\n                case DeltaEstimator.Mean: return EstimateDeltaMean(nnDistances);\n                case DeltaEstimator.Median: return EstimateDeltaMedian(nnDistances);\n                default: throw new NotSupportedException($\"Estimator {estimator} not implemented.\");\n            }\n        }\n\n        internal static List<string> ValidateGraph(Edge[] edges, int n, SpcProximity proximity)\n        {\n            var warnings = new List<string>();\n\n            if (edges.Length == 0)\n            {\n                warnings.Add(\"Graph has zero edges. Simulation will produce no structure.\");\n                return warnings;\n            }\n\n            var uf = new FastUnionFind(n);\n            for (int i = 0; i < edges.Length; i++)\n                uf.Union(edges[i].Source, edges[i].Target);\n\n            var componentSizes = new Dictionary<int, int>();\n            for (int i = 0; i < n; i++)\n            {\n                int root = uf.Find(i);\n                if (!componentSizes.ContainsKey(root))\n                    componentSizes[root] = 0;\n                componentSizes[root]++;\n            }\n\n            int numComponents = componentSizes.Count;\n            int largestComponent = 0;\n            foreach (var kvp in componentSizes)\n                if (kvp.Value > largestComponent)\n                    largestComponent = kvp.Value;\n\n            double coverage = (double)largestComponent / n;\n            int isolatedNodes = 0;\n            foreach (var kvp in componentSizes)\n                if (kvp.Value == 1)\n                    isolatedNodes++;\n\n            if (numComponents > 1)\n            {\n                warnings.Add(string.Format(\n                    \"Graph is disconnected: {0} components. Largest covers {1:P1} of nodes ({2}/{3}). Proximity: {4}.\",\n                    numComponents, coverage, largestComponent, n, proximity));\n            }\n\n            if (coverage < 0.9)\n            {\n                warnings.Add(string.Format(\n                    \"Largest connected component covers only {0:P1} of nodes. \" +\n                    \"Potts phase transitions may not reflect global data structure. \" +\n                    \"Consider MstAugmented proximity or increasing K.\",\n                    coverage));\n            }\n\n            if (isolatedNodes > 0)\n            {\n                warnings.Add(string.Format(\n                    \"{0} isolated node(s) with no edges. These will never join any cluster.\",\n                    isolatedNodes));\n            }\n\n            return warnings;\n        }\n\n        private static SpcBatchResult RunSimulationCore(Edge[] edges, int n, SpcBatchRequest r,\n            List<string> warnings, System.Threading.CancellationToken ct = default)\n        {\n            var result = new SpcBatchResult\n            {\n                Temperatures = (double[])r.Temperatures.Clone(),\n                Susceptibility = new double[r.Temperatures.Length],\n                FinalSpins = new Dictionary<double, int[]>(r.Temperatures.Length),\n                Warnings = warnings.ToArray()\n            };\n\n            var graph = CsrGraph.FromEdges(edges, n);\n            result.Graph = graph;\n\n            string runStamp = null;\n            if (!string.IsNullOrEmpty(r.CheckpointDirectory))\n            {\n                runStamp = DateTime.UtcNow.ToString(\"yyyyMMdd_HHmmss\");\n                result.RunDirectory = runStamp;\n            }\n\n            SpcCheckpoint checkpoint = r.Checkpoint;\n            if (checkpoint == null && !string.IsNullOrEmpty(r.CheckpointDirectory))\n            {\n                string loadStamp = !string.IsNullOrEmpty(r.ResumeDirectory) ? r.ResumeDirectory : runStamp;\n                checkpoint = LoadCheckpointFromDirectory(r.CheckpointDirectory, loadStamp);\n            }\n\n            var completed = new System.Collections.Concurrent.ConcurrentDictionary<double, SimulationResult>();\n\n            var opts = new ParallelOptions { CancellationToken = ct };\n\n            try\n            {\n                Parallel.For(0, r.Temperatures.Length, opts, i =>\n                {\n                    double T = r.Temperatures[i];\n\n                    if (checkpoint != null &&\n                        checkpoint.Spins.TryGetValue(T, out var cpSpins) &&\n                        checkpoint.Susceptibility.TryGetValue(T, out var cpSusc))\n                    {\n                        completed[T] = new SimulationResult { FinalSpins = cpSpins, Susceptibility = cpSusc };\n                        return;\n                    }\n\n                    int seed = r.BaseSeed < 0\n                        ? -1\n                        : Math.Abs(r.BaseSeed ^ T.GetHashCode()) % int.MaxValue;\n\n                    var model = new PottsModel(n, q: 20, seed: seed);\n\n                    if (checkpoint != null && checkpoint.Spins.TryGetValue(T, out var warmSpins))\n                        model.SetSpins(warmSpins);\n\n                    model.LoadGraph(graph);\n\n                    var sr = model.RunSimulation(T, r.Steps);\n\n                    if (!string.IsNullOrEmpty(r.CheckpointDirectory))\n                        FlushTemperatureCheckpoint(r.CheckpointDirectory, runStamp, T, sr);\n\n                    completed[T] = sr;\n                });\n            }\n            catch (OperationCanceledException) {  }\n\n            for (int i = 0; i < r.Temperatures.Length; i++)\n            {\n                double T = r.Temperatures[i];\n                if (completed.TryGetValue(T, out var sr))\n                {\n                    result.Susceptibility[i] = sr.Susceptibility;\n                    result.FinalSpins[T] = sr.FinalSpins;\n                }\n            }\n\n            return result;\n        }\n\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighbors(\n            SpcProximity proximity, int n, int k, double epsilon,\n            Func<int, int, double> pairDistance)\n        {\n            switch (proximity)\n            {\n                case SpcProximity.Knn:\n                    return SelectNeighborsKnn(n, k, pairDistance);\n                case SpcProximity.MutualKnn:\n                    return SelectNeighborsMutualKnn(n, k, pairDistance);\n                case SpcProximity.EpsilonBall:\n                    return SelectNeighborsEpsilonBall(n, epsilon, pairDistance);\n                case SpcProximity.MstAugmented:\n                    return SelectNeighborsMstAugmented(n, k, pairDistance);\n                default:\n                    throw new NotSupportedException($\"Proximity {proximity} not implemented.\");\n            }\n        }\n\n        internal static Edge[] ConvertToCoupling(\n            Neighbor[][] allNeighbors, double[] nnDistances,\n            double delta, DeltaEstimator estimator,\n            CouplingKernel kernel = CouplingKernel.Gaussian)\n        {\n            double effectiveDelta = delta > 0 ? delta : EstimateDelta(estimator, nnDistances);\n\n            Func<double, double, double> kernelFunc;\n            switch (kernel)\n            {\n                case CouplingKernel.Gaussian: kernelFunc = GaussianCoupling; break;\n                case CouplingKernel.Cauchy: kernelFunc = CauchyCoupling; break;\n                case CouplingKernel.Laplacian: kernelFunc = LaplacianCoupling; break;\n                case CouplingKernel.Linear: kernelFunc = LinearCoupling; break;\n                default: throw new NotSupportedException($\"CouplingKernel {kernel} not implemented.\");\n            }\n\n            var edges = new List<Edge>();\n            int n = allNeighbors.Length;\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in allNeighbors[i])\n                {\n\n                    if (i > nb.Index) continue;\n\n                    double J = kernelFunc(nb.Distance, effectiveDelta);\n                    if (J > 1e-10)\n                        edges.Add(new Edge(i, nb.Index, J));\n                }\n            }\n\n            return edges.ToArray();\n        }\n\n        internal static Edge[] BuildGraphFromFeatures(\n            double[][] features, int k, double delta, DeltaEstimator estimator,\n            Func<double[], double[], double> distanceFunc,\n            SpcProximity proximity = SpcProximity.Knn, double epsilon = 0.0,\n            CouplingKernel kernel = CouplingKernel.Gaussian)\n        {\n            int n = features.Length;\n\n            Func<int, int, double> pairDistance = (i, j) => distanceFunc(features[i], features[j]);\n\n            var (allNeighbors, nnDistances) = SelectNeighbors(proximity, n, k, epsilon, pairDistance);\n\n            return ConvertToCoupling(allNeighbors, nnDistances, delta, estimator, kernel);\n        }\n    }\n} |
31 | spc.foundation.cs | {5245 597 0.4128 4.1004} | 5415 | using System;\n\nnamespace SpcCore\n{\n\n    public struct Edge\n    {\n        public int Source;\n        public int Target;\n        public double J;\n        public Edge(int s, int t, double j) { Source = s; Target = t; J = j; }\n    }\n\n    public struct CsrGraph\n    {\n        public int[] Targets;      // concatenated neighbor indices\n        public double[] Weights;   // corresponding coupling values J\n        public int[] RowPointers;  // RowPointers[i]..RowPointers[i+1] spans row i\n        public int NodeCount;\n\n        public static CsrGraph FromEdges(Edge[] edges, int nodeCount)\n        {\n\n            int[] degree = new int[nodeCount];\n            for (int e = 0; e < edges.Length; e++)\n            {\n                degree[edges[e].Source]++;\n                degree[edges[e].Target]++;\n            }\n\n            int[] rowPointers = new int[nodeCount + 1];\n            for (int i = 0; i < nodeCount; i++)\n                rowPointers[i + 1] = rowPointers[i] + degree[i];\n\n            int totalEntries = rowPointers[nodeCount];\n            int[] targets = new int[totalEntries];\n            double[] weights = new double[totalEntries];\n\n            int[] cursor = new int[nodeCount];\n            Array.Copy(rowPointers, cursor, nodeCount);\n\n            for (int e = 0; e < edges.Length; e++)\n            {\n                int s = edges[e].Source, t = edges[e].Target;\n                double j = edges[e].J;\n\n                targets[cursor[s]] = t;\n                weights[cursor[s]] = j;\n                cursor[s]++;\n\n                targets[cursor[t]] = s;\n                weights[cursor[t]] = j;\n                cursor[t]++;\n            }\n\n            return new CsrGraph\n            {\n                Targets = targets,\n                Weights = weights,\n                RowPointers = rowPointers,\n                NodeCount = nodeCount\n            };\n        }\n    }\n\n    internal struct Neighbor : IComparable<Neighbor>\n    {\n        public int Index;\n        public double Distance;\n        public int CompareTo(Neighbor other) => other.Distance.CompareTo(Distance); // reversed for min-extraction\n    }\n\n    internal class BoundedMinHeap\n    {\n        private readonly Neighbor[] _heap;\n        private int _count;\n        private readonly int _capacity;\n\n        public BoundedMinHeap(int k)\n        {\n            _capacity = k;\n            _heap = new Neighbor[k];\n            _count = 0;\n        }\n\n        public int Count => _count;\n        public double WorstDistance => _count > 0 ? _heap[0].Distance : double.MaxValue;\n\n        public void TryAdd(int index, double distance)\n        {\n            if (_count < _capacity)\n            {\n                _heap[_count] = new Neighbor { Index = index, Distance = distance };\n                _count++;\n                if (_count == _capacity) BuildHeap();\n            }\n            else if (distance < _heap[0].Distance)\n            {\n                _heap[0] = new Neighbor { Index = index, Distance = distance };\n                SiftDown(0);\n            }\n        }\n\n        public Neighbor[] GetSorted()\n        {\n            var result = new Neighbor[_count];\n            Array.Copy(_heap, result, _count);\n            Array.Sort(result, (a, b) => a.Distance.CompareTo(b.Distance));\n            return result;\n        }\n\n        private void BuildHeap()\n        {\n            for (int i = _count / 2 - 1; i >= 0; i--) SiftDown(i);\n        }\n\n        private void SiftDown(int i)\n        {\n            while (true)\n            {\n                int largest = i;\n                int left = 2 * i + 1;\n                int right = 2 * i + 2;\n                if (left < _count && _heap[left].Distance > _heap[largest].Distance) largest = left;\n                if (right < _count && _heap[right].Distance > _heap[largest].Distance) largest = right;\n                if (largest == i) break;\n                var tmp = _heap[i]; _heap[i] = _heap[largest]; _heap[largest] = tmp;\n                i = largest;\n            }\n        }\n    }\n\n    public class FastUnionFind\n    {\n        private int[] parent;\n        private int[] size;\n        public FastUnionFind(int n)\n        {\n            parent = new int[n];\n            size = new int[n];\n            for (int i = 0; i < n; i++) { parent[i] = i; size[i] = 1; }\n        }\n        public int Find(int p)\n        {\n            int root = p;\n            while (root != parent[root]) root = parent[root];\n            while (p != root) { int newp = parent[p]; parent[p] = root; p = newp; }\n            return root;\n        }\n        public void Union(int p, int q)\n        {\n            int rootP = Find(p);\n            int rootQ = Find(q);\n            if (rootP == rootQ) return;\n            if (size[rootP] < size[rootQ]) { parent[rootP] = rootQ; size[rootQ] += size[rootP]; }\n            else { parent[rootQ] = rootP; size[rootP] += size[rootQ]; }\n        }\n        public int[] GetLabels()\n        {\n            int n = parent.Length;\n            int[] labels = new int[n];\n            for (int i = 0; i < n; i++) labels[i] = Find(i);\n            return labels;\n        }\n        public void Reset()\n        {\n            int n = parent.Length;\n            for (int i = 0; i < n; i++) { parent[i] = i; size[i] = 1; }\n        }\n    }\n} |
32 | spc.potts.cs | {3798 384 0.4497 3.9749} | 3925 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n\n    public readonly struct SimulationResult\n    {\n        public readonly double Temperature;\n        public readonly double Susceptibility;\n        public readonly int[] FinalSpins;\n\n        public SimulationResult(double temperature, double susceptibility, int[] finalSpins)\n        {\n            Temperature = temperature;\n            Susceptibility = susceptibility;\n            FinalSpins = finalSpins;\n        }\n    }\n\n    public class PottsModel\n    {\n        private int N;\n        private CsrGraph Graph;\n        private int[] Spins;\n        private Random Rng;\n        private int Q;\n\n        public PottsModel(int n, int q = 20, int seed = -1)\n        {\n            N = n;\n            Q = q;\n            Graph = default;\n            Spins = new int[N];\n            Rng = seed >= 0 ? new Random(seed) : new Random();\n\n            for (int i = 0; i < N; i++) Spins[i] = Rng.Next(Q);\n        }\n\n        public void SetSpins(int[] savedSpins)\n        {\n            if (savedSpins.Length != N) throw new ArgumentException(\"Spin array size mismatch\");\n            Array.Copy(savedSpins, Spins, N);\n        }\n\n        public void LoadGraph(CsrGraph graph)\n        {\n            Graph = graph;\n        }\n\n        public SimulationResult RunSimulation(double temperature, int steps)\n        {\n            int entryCount = Graph.RowPointers[N];\n            var bondProb = new double[entryCount];\n\n            TensorPrimitives.Multiply(Graph.Weights.AsSpan(0, entryCount), -1.0 / temperature, bondProb);\n            TensorPrimitives.Exp(bondProb, bondProb);\n            TensorPrimitives.Negate(bondProb, bondProb);\n            TensorPrimitives.Add(bondProb, 1.0, bondProb);\n\n            double susceptibilitySum = 0;\n\n            var uf = new FastUnionFind(N);\n            int[] labels = new int[N];\n            int[] clusterMap = new int[N];   // root → assigned spin (-1 = unassigned)\n            int[] sizes = new int[N];        // root → cluster member count\n\n            for (int k = 0; k < steps; k++)\n            {\n                uf.Reset();\n\n                for (int i = 0; i < N; i++)\n                {\n                    int start = Graph.RowPointers[i];\n                    int end = Graph.RowPointers[i + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        int j = Graph.Targets[e];\n                        if (j <= i) continue;\n                        if (Spins[i] == Spins[j] && Rng.NextDouble() < bondProb[e])\n                            uf.Union(i, j);\n                    }\n                }\n\n                for (int i = 0; i < N; i++)\n                {\n                    labels[i] = uf.Find(i);\n                    clusterMap[i] = -1;\n                }\n\n                for (int i = 0; i < N; i++)\n                {\n                    int root = labels[i];\n                    if (clusterMap[root] == -1)\n                        clusterMap[root] = Rng.Next(Q);\n                    Spins[i] = clusterMap[root];\n                }\n\n                if (k > steps / 2)\n                {\n                    Array.Clear(sizes, 0, N);\n                    for (int i = 0; i < N; i++)\n                        sizes[labels[i]]++;\n\n                    double sumSq = 0;\n                    for (int i = 0; i < N; i++)\n                        if (sizes[i] > 0) sumSq += (double)sizes[i] * sizes[i];\n                    susceptibilitySum += (sumSq / N);\n                }\n            }\n\n            int sampleCount = steps / 2;\n            if (sampleCount == 0) sampleCount = 1;\n\n            return new SimulationResult(\n                temperature,\n                susceptibilitySum / sampleCount,\n                (int[])Spins.Clone()\n            );\n        }\n    }\n} |
