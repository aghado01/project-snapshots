idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
32 | spc.foundation.cs | {6990 771 0.3562 4.2441} | 7201 | using System;\n\nnamespace SpcCore\n{\n\n    // =========================================================================\n    // Edge — weighted undirected connection between two data points\n    // =========================================================================\n\n    public struct Edge\n    {\n        public int Source;\n        public int Target;\n        public double J;\n        public Edge(int s, int t, double j) { Source = s; Target = t; J = j; }\n    }\n\n    // =========================================================================\n    // CsrGraph — Compressed Sparse Row representation for cache-coherent SW\n    // =========================================================================\n\n    /// <summary>\n    /// Symmetric CSR adjacency: each undirected edge (i,j) is stored in both\n    /// row i and row j. Bond formation skips j &lt;= i to avoid double-processing.\n    /// Memory layout: Targets and Weights are contiguous per row, enabling\n    /// sequential cache-line access in the SW inner loop.\n    /// </summary>\n    public struct CsrGraph\n    {\n        public int[] Targets;      // concatenated neighbor indices\n        public double[] Weights;   // corresponding coupling values J\n        public int[] RowPointers;  // RowPointers[i]..RowPointers[i+1] spans row i\n        public int NodeCount;\n\n        /// <summary>\n        /// Build symmetric CSR from an undirected Edge[] where each edge appears once.\n        /// </summary>\n        public static CsrGraph FromEdges(Edge[] edges, int nodeCount)\n        {\n            // Pass 1: count degree per node\n            int[] degree = new int[nodeCount];\n            for (int e = 0; e < edges.Length; e++)\n            {\n                degree[edges[e].Source]++;\n                degree[edges[e].Target]++;\n            }\n\n            // Build row pointers (prefix sum)\n            int[] rowPointers = new int[nodeCount + 1];\n            for (int i = 0; i < nodeCount; i++)\n                rowPointers[i + 1] = rowPointers[i] + degree[i];\n\n            int totalEntries = rowPointers[nodeCount];\n            int[] targets = new int[totalEntries];\n            double[] weights = new double[totalEntries];\n\n            // Pass 2: fill using insertion cursors\n            int[] cursor = new int[nodeCount];\n            Array.Copy(rowPointers, cursor, nodeCount);\n\n            for (int e = 0; e < edges.Length; e++)\n            {\n                int s = edges[e].Source, t = edges[e].Target;\n                double j = edges[e].J;\n\n                targets[cursor[s]] = t;\n                weights[cursor[s]] = j;\n                cursor[s]++;\n\n                targets[cursor[t]] = s;\n                weights[cursor[t]] = j;\n                cursor[t]++;\n            }\n\n            return new CsrGraph\n            {\n                Targets = targets,\n                Weights = weights,\n                RowPointers = rowPointers,\n                NodeCount = nodeCount\n            };\n        }\n    }\n\n    // =========================================================================\n    // Graph primitives — heap/neighbor types used by proximity builders\n    // =========================================================================\n\n    /// <summary>\n    /// Bounded max-heap of size K that keeps the K smallest distances.\n    /// Used to avoid O(N log N) sort per node — gives O(N log K) instead.\n    /// </summary>\n    internal struct Neighbor : IComparable<Neighbor>\n    {\n        public int Index;\n        public double Distance;\n        public int CompareTo(Neighbor other) => other.Distance.CompareTo(Distance); // reversed for min-extraction\n    }\n\n    internal class BoundedMinHeap\n    {\n        private readonly Neighbor[] _heap;\n        private int _count;\n        private readonly int _capacity;\n\n        public BoundedMinHeap(int k)\n        {\n            _capacity = k;\n            _heap = new Neighbor[k];\n            _count = 0;\n        }\n\n        public int Count => _count;\n        public double WorstDistance => _count > 0 ? _heap[0].Distance : double.MaxValue;\n\n        public void TryAdd(int index, double distance)\n        {\n            if (_count < _capacity)\n            {\n                _heap[_count] = new Neighbor { Index = index, Distance = distance };\n                _count++;\n                if (_count == _capacity) BuildHeap();\n            }\n            else if (distance < _heap[0].Distance)\n            {\n                _heap[0] = new Neighbor { Index = index, Distance = distance };\n                SiftDown(0);\n            }\n        }\n\n        public Neighbor[] GetSorted()\n        {\n            var result = new Neighbor[_count];\n            Array.Copy(_heap, result, _count);\n            Array.Sort(result, (a, b) => a.Distance.CompareTo(b.Distance));\n            return result;\n        }\n\n        private void BuildHeap()\n        {\n            for (int i = _count / 2 - 1; i >= 0; i--) SiftDown(i);\n        }\n\n        private void SiftDown(int i)\n        {\n            while (true)\n            {\n                int largest = i;\n                int left = 2 * i + 1;\n                int right = 2 * i + 2;\n                if (left < _count && _heap[left].Distance > _heap[largest].Distance) largest = left;\n                if (right < _count && _heap[right].Distance > _heap[largest].Distance) largest = right;\n                if (largest == i) break;\n                var tmp = _heap[i]; _heap[i] = _heap[largest]; _heap[largest] = tmp;\n                i = largest;\n            }\n        }\n    }\n\n    // =========================================================================\n    // FastUnionFind — path-compressed union-by-size for Swendsen-Wang clusters\n    // =========================================================================\n\n    public class FastUnionFind\n    {\n        private int[] parent;\n        private int[] size;\n        public FastUnionFind(int n)\n        {\n            parent = new int[n];\n            size = new int[n];\n            for (int i = 0; i < n; i++) { parent[i] = i; size[i] = 1; }\n        }\n        public int Find(int p)\n        {\n            int root = p;\n            while (root != parent[root]) root = parent[root];\n            while (p != root) { int newp = parent[p]; parent[p] = root; p = newp; }\n            return root;\n        }\n        public void Union(int p, int q)\n        {\n            int rootP = Find(p);\n            int rootQ = Find(q);\n            if (rootP == rootQ) return;\n            if (size[rootP] < size[rootQ]) { parent[rootP] = rootQ; size[rootQ] += size[rootP]; }\n            else { parent[rootQ] = rootP; size[rootP] += size[rootQ]; }\n        }\n        public int[] GetLabels()\n        {\n            int n = parent.Length;\n            int[] labels = new int[n];\n            for (int i = 0; i < n; i++) labels[i] = Find(i);\n            return labels;\n        }\n        public void Reset()\n        {\n            int n = parent.Length;\n            for (int i = 0; i < n; i++) { parent[i] = i; size[i] = 1; }\n        }\n    }\n} |
33 | spc.potts.cs | {5077 524 0.4089 4.1612} | 5238 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n\n    // =========================================================================\n    // PottsModel — Swendsen-Wang Monte Carlo on a proximity graph\n    // =========================================================================\n\n    public readonly struct SimulationResult\n    {\n        public readonly double Temperature;\n        public readonly double Susceptibility;\n        public readonly int[] FinalSpins;\n\n        public SimulationResult(double temperature, double susceptibility, int[] finalSpins)\n        {\n            Temperature = temperature;\n            Susceptibility = susceptibility;\n            FinalSpins = finalSpins;\n        }\n    }\n\n    public class PottsModel\n    {\n        private int N;\n        private CsrGraph Graph;\n        private int[] Spins;\n        private Random Rng;\n        private int Q;\n\n        public PottsModel(int n, int q = 20, int seed = -1)\n        {\n            N = n;\n            Q = q;\n            Graph = default;\n            Spins = new int[N];\n            Rng = seed >= 0 ? new Random(seed) : new Random();\n            // Default random initialization\n            for (int i = 0; i < N; i++) Spins[i] = Rng.Next(Q);\n        }\n\n        /// <summary>\n        /// Inject spin state from a checkpoint (resume support).\n        /// </summary>\n        public void SetSpins(int[] savedSpins)\n        {\n            if (savedSpins.Length != N) throw new ArgumentException(\"Spin array size mismatch\");\n            Array.Copy(savedSpins, Spins, N);\n        }\n\n        /// <summary>\n        /// Load CSR graph topology for simulation.\n        /// </summary>\n        public void LoadGraph(CsrGraph graph)\n        {\n            Graph = graph;\n        }\n\n        /// <summary>\n        /// Run Swendsen-Wang Monte Carlo simulation at the given temperature.\n        /// </summary>\n        public SimulationResult RunSimulation(double temperature, int steps)\n        {\n            int entryCount = Graph.RowPointers[N];\n            var bondProb = new double[entryCount];\n\n            // Vectorised bond-probability init: bondProb[e] = 1 - exp(-J/T)\n            // Steps: multiply weights by -1/T → exp in-place → negate → add 1.0\n            TensorPrimitives.Multiply(Graph.Weights.AsSpan(0, entryCount), -1.0 / temperature, bondProb);\n            TensorPrimitives.Exp(bondProb, bondProb);\n            TensorPrimitives.Negate(bondProb, bondProb);\n            TensorPrimitives.Add(bondProb, 1.0, bondProb);\n\n            double susceptibilitySum = 0;\n\n            // Pre-allocate reusable structures outside the step loop (HPC 1+2)\n            var uf = new FastUnionFind(N);\n            int[] labels = new int[N];\n            int[] clusterMap = new int[N];   // root → assigned spin (-1 = unassigned)\n            int[] sizes = new int[N];        // root → cluster member count\n\n            for (int k = 0; k < steps; k++)\n            {\n                uf.Reset();\n\n                // Bond Formation — CSR row-major traversal (HPC 3)\n                // Sequential access through Targets/Weights arrays per row.\n                // Skip j <= i to process each undirected edge exactly once.\n                for (int i = 0; i < N; i++)\n                {\n                    int start = Graph.RowPointers[i];\n                    int end = Graph.RowPointers[i + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        int j = Graph.Targets[e];\n                        if (j <= i) continue;\n                        if (Spins[i] == Spins[j] && Rng.NextDouble() < bondProb[e])\n                            uf.Union(i, j);\n                    }\n                }\n\n                // Cluster Flipping — flat array replaces Dictionary<int,int>\n                for (int i = 0; i < N; i++)\n                {\n                    labels[i] = uf.Find(i);\n                    clusterMap[i] = -1;\n                }\n\n                for (int i = 0; i < N; i++)\n                {\n                    int root = labels[i];\n                    if (clusterMap[root] == -1)\n                        clusterMap[root] = Rng.Next(Q);\n                    Spins[i] = clusterMap[root];\n                }\n\n                // Susceptibility Measurement (Variance of Cluster Sizes)\n                // Only measure in second half (equilibrium)\n                if (k > steps / 2)\n                {\n                    Array.Clear(sizes, 0, N);\n                    for (int i = 0; i < N; i++)\n                        sizes[labels[i]]++;\n\n                    double sumSq = 0;\n                    for (int i = 0; i < N; i++)\n                        if (sizes[i] > 0) sumSq += (double)sizes[i] * sizes[i];\n                    susceptibilitySum += (sumSq / N);\n                }\n            }\n\n            int sampleCount = steps / 2;\n            if (sampleCount == 0) sampleCount = 1;\n\n            return new SimulationResult(\n                temperature,\n                susceptibilitySum / sampleCount,\n                (int[])Spins.Clone()\n            );\n        }\n    }\n} |
34 | spc.synthetic.cs | {11753 1320 0.3606 4.2994} | 12064 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    // =========================================================================\n    // SyntheticData — ground-truth datasets for empirical SPC experiments\n    // =========================================================================\n    /// <summary>\n    /// Generators for labeled synthetic datasets used to characterize how\n    /// different metric / proximity / delta-estimator combinations behave on\n    /// controlled geometries. No generator prescribes a configuration; each\n    /// dataset exposes a specific structural feature (density voids, sparse\n    /// supports, simplex-valued points, anisotropy, parametric manifolds,\n    /// hierarchical scales, non-convex shapes) so experiment harnesses can\n    /// sweep configurations and measure outcomes directly.\n    ///\n    /// This root file owns the shared primitives:\n    ///   Sampling  — SampleStandardNormal, SampleGamma, SampleDirichlet, Normalize\n    ///   Geometry  — PlaceCentroidsOnSphere, RandomRotationMatrix,\n    ///               GramSchmidtOrthonormalize, MultiplyMatrixVector,\n    ///               BuildCovariance\n    ///   DTO       — SyntheticDataset\n    ///   Adapter   — ToSpcBatchRequest (SyntheticDataset → SpcBatchRequest)\n    ///\n    /// Each dataset generator lives in its own partial class under\n    /// src/synthetic/.\n    ///\n    /// Cluster purity scoring lives in SpcAnalysis.ComputePurity — it\n    /// consumes analysis output, not data-generation artifacts.\n    /// </summary>\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // Result bundle — ground truth + features + generation metadata\n        // =====================================================================\n        public sealed class SyntheticDataset\n        {\n            public double[][] Features { get; set; }\n            public int[] Labels { get; set; }\n            public int ClusterCount { get; set; }\n            public Dictionary<string, object> Parameters { get; set; }\n\n            // Hierarchical ground truth for multi-scale datasets.\n            // Null for flat datasets. LabelsByLevel[0] = coarsest,\n            // LabelsByLevel[L-1] = finest.\n            public int[][] LabelsByLevel { get; set; }\n\n            // Per-cluster covariance matrices when the generator produces\n            // anisotropic clusters. Consumers may pool these to build a\n            // CovarianceInverse for Mahalanobis, or ignore.\n            public double[][,] ClusterCovariances { get; set; }\n        }\n\n        // =====================================================================\n        // Adapter — SyntheticDataset → SpcBatchRequest\n        // =====================================================================\n\n        /// <summary>\n        /// Build an SpcBatchRequest from a generated dataset and a chosen\n        /// sweep configuration. Maps Features through directly; the rest\n        /// (metric, K, temperatures, steps, proximity, kernel, estimator)\n        /// is the harness's choice and must be supplied.\n        ///\n        /// Mahalanobis caveat: SpcBatchRequest.CovarianceInverse is left null.\n        /// dataset.ClusterCovariances exposes raw per-cluster Σ_k matrices,\n        /// not a pooled inverse — pooling and inversion is the caller's\n        /// responsibility (different harnesses pool differently).\n        /// </summary>\n        public static SpcBatchRequest ToSpcBatchRequest(\n            this SyntheticDataset dataset,\n            SpcMetric metric,\n            int k,\n            double[] temperatures,\n            int steps,\n            SpcProximity proximity = SpcProximity.Knn,\n            CouplingKernel kernel = CouplingKernel.Gaussian,\n            DeltaEstimator estimator = DeltaEstimator.Mean,\n            double delta = 0.0,\n            double epsilon = 0.0,\n            double minkowskiP = 2.0,\n            double jaccardThreshold = 0.5,\n            int baseSeed = -1)\n        {\n            if (dataset == null) throw new ArgumentNullException(nameof(dataset));\n            if (dataset.Features == null || dataset.Features.Length == 0)\n                throw new ArgumentException(\"Dataset has no features.\", nameof(dataset));\n\n            return new SpcBatchRequest\n            {\n                Metric = metric,\n                Features = dataset.Features,\n                K = k,\n                Temperatures = temperatures ?? Array.Empty<double>(),\n                Steps = steps,\n                Proximity = proximity,\n                CouplingKernel = kernel,\n                Estimator = estimator,\n                Delta = delta,\n                Epsilon = epsilon,\n                MinkowskiP = minkowskiP,\n                JaccardThreshold = jaccardThreshold,\n                BaseSeed = baseSeed,\n            };\n        }\n\n        // =====================================================================\n        // Sampling primitives — distribution families\n        // =====================================================================\n\n        /// <summary>\n        /// Standard normal sample via Box-Muller. Uses 1 - NextDouble() to\n        /// avoid log(0).\n        /// </summary>\n        internal static double SampleStandardNormal(Random rng)\n        {\n            double u1 = 1.0 - rng.NextDouble();\n            double u2 = 1.0 - rng.NextDouble();\n            return Math.Sqrt(-2.0 * Math.Log(u1)) * Math.Cos(2.0 * Math.PI * u2);\n        }\n\n        /// <summary>\n        /// Gamma(alpha, 1) sample via Marsaglia-Tsang for alpha &gt;= 1, with\n        /// Stuart's scaling trick for alpha &lt; 1.\n        /// </summary>\n        internal static double SampleGamma(double alpha, Random rng)\n        {\n            if (alpha < 1.0)\n            {\n                double u = rng.NextDouble();\n                return SampleGamma(alpha + 1.0, rng) * Math.Pow(u, 1.0 / alpha);\n            }\n            double d = alpha - 1.0 / 3.0;\n            double c = 1.0 / Math.Sqrt(9.0 * d);\n            while (true)\n            {\n                double x = SampleStandardNormal(rng);\n                double v = 1.0 + c * x;\n                if (v <= 0) continue;\n                v = v * v * v;\n                double uniform = rng.NextDouble();\n                double xSq = x * x;\n                if (uniform < 1.0 - 0.0331 * xSq * xSq) return d * v;\n                if (Math.Log(uniform) < 0.5 * xSq + d * (1.0 - v + Math.Log(v)))\n                    return d * v;\n            }\n        }\n\n        /// <summary>\n        /// Dirichlet(alpha) sample. Component i is Gamma(alpha[i], 1)\n        /// normalized by the sum. Result is a probability vector summing to 1.\n        /// </summary>\n        internal static double[] SampleDirichlet(double[] alpha, Random rng)\n        {\n            var sample = new double[alpha.Length];\n            double sum = 0;\n            for (int i = 0; i < alpha.Length; i++)\n            {\n                sample[i] = SampleGamma(alpha[i], rng);\n                sum += sample[i];\n            }\n            if (sum <= 0) sum = 1.0;\n            for (int i = 0; i < alpha.Length; i++) sample[i] /= sum;\n            return sample;\n        }\n\n        /// <summary>Project a nonnegative vector onto the simplex (sum to 1).</summary>\n        internal static void Normalize(double[] v)\n        {\n            double sum = 0;\n            for (int i = 0; i < v.Length; i++) sum += v[i];\n            if (sum <= 0) return;\n            for (int i = 0; i < v.Length; i++) v[i] /= sum;\n        }\n\n        // =====================================================================\n        // Geometry primitives — spatial arrangement and linalg\n        // =====================================================================\n\n        /// <summary>\n        /// Place <paramref name=\"count\"/> centroids on the surface of a\n        /// hypersphere of the given radius. All centers are exactly radius\n        /// from the origin; expected pairwise distance is approximately\n        /// radius * sqrt(2) with predictable spread.\n        /// </summary>\n        internal static double[][] PlaceCentroidsOnSphere(\n            int count, int dim, double radius, Random rng)\n        {\n            var centroids = new double[count][];\n            for (int c = 0; c < count; c++)\n            {\n                var centroid = new double[dim];\n                double normSq = 0;\n                for (int d = 0; d < dim; d++)\n                {\n                    centroid[d] = SampleStandardNormal(rng);\n                    normSq += centroid[d] * centroid[d];\n                }\n                double norm = Math.Sqrt(normSq);\n                for (int d = 0; d < dim; d++)\n                    centroid[d] = radius * centroid[d] / norm;\n                centroids[c] = centroid;\n            }\n            return centroids;\n        }\n\n        /// <summary>\n        /// Haar-distributed random rotation matrix. 2D closed form; higher\n        /// dimensions via Gram-Schmidt on a Gaussian random matrix.\n        /// </summary>\n        internal static double[,] RandomRotationMatrix(int dim, Random rng)\n        {\n            if (dim == 2)\n            {\n                double angle = 2.0 * Math.PI * rng.NextDouble();\n                double c = Math.Cos(angle), s = Math.Sin(angle);\n                return new double[,] { { c, -s }, { s, c } };\n            }\n            var a = new double[dim, dim];\n            for (int i = 0; i < dim; i++)\n                for (int j = 0; j < dim; j++)\n                    a[i, j] = SampleStandardNormal(rng);\n            return GramSchmidtOrthonormalize(a, dim);\n        }\n\n        /// <summary>Classical Gram-Schmidt orthonormalization of column vectors.</summary>\n        internal static double[,] GramSchmidtOrthonormalize(double[,] a, int dim)\n        {\n            var q = new double[dim, dim];\n            for (int k = 0; k < dim; k++)\n            {\n                var v = new double[dim];\n                for (int i = 0; i < dim; i++) v[i] = a[i, k];\n                for (int j = 0; j < k; j++)\n                {\n                    double dot = 0;\n                    for (int i = 0; i < dim; i++) dot += v[i] * q[i, j];\n                    for (int i = 0; i < dim; i++) v[i] -= dot * q[i, j];\n                }\n                double normSq = 0;\n                for (int i = 0; i < dim; i++) normSq += v[i] * v[i];\n                double norm = Math.Sqrt(normSq);\n                if (norm < 1e-12) norm = 1.0;\n                for (int i = 0; i < dim; i++) q[i, k] = v[i] / norm;\n            }\n            return q;\n        }\n\n        internal static double[] MultiplyMatrixVector(double[,] m, double[] v)\n        {\n            int rows = m.GetLength(0), cols = m.GetLength(1);\n            var result = new double[rows];\n            for (int i = 0; i < rows; i++)\n            {\n                double sum = 0;\n                for (int j = 0; j < cols; j++) sum += m[i, j] * v[j];\n                result[i] = sum;\n            }\n            return result;\n        }\n\n        /// <summary>\n        /// Build a covariance matrix from a rotation and per-axis scale:\n        /// Cov = R * diag(scale^2) * R^T.\n        /// </summary>\n        internal static double[,] BuildCovariance(double[,] r, double[] scale)\n        {\n            int dim = scale.Length;\n            var cov = new double[dim, dim];\n            for (int i = 0; i < dim; i++)\n                for (int j = 0; j < dim; j++)\n                {\n                    double sum = 0;\n                    for (int k = 0; k < dim; k++)\n                        sum += r[i, k] * scale[k] * scale[k] * r[j, k];\n                    cov[i, j] = sum;\n                }\n            return cov;\n        }\n    }\n} |
35 | synthetic/SyntheticData.AnisotropicGaussian.cs | {3267 302 0.3838 4.0545} | 3361 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // AnisotropicGaussian — correlated Gaussian clusters\n        // =====================================================================\n        /// <summary>\n        /// Gaussian clusters with anisotropic (elongated) shapes along random\n        /// rotations. Cluster means are placed close enough that raw\n        /// feature-space distances overlap substantially between clusters\n        /// while covariance structure remains distinct. Per-cluster\n        /// covariance matrices are returned so callers can build a pooled\n        /// CovarianceInverse (or construct their own) if desired.\n        /// </summary>\n        public static SyntheticDataset GenerateAnisotropicGaussian(\n            int clusterCount = 3,\n            int pointsPerCluster = 60,\n            int dimensions = 2,\n            double meanSeparation = 2.0,\n            double anisotropyRatio = 5.0,\n            int seed = 42)\n        {\n            if (dimensions < 2)\n                throw new ArgumentException(\"AnisotropicGaussian requires dimensions >= 2.\");\n            var rng = new Random(seed);\n            int n = clusterCount * pointsPerCluster;\n            var features = new double[n][];\n            var labels = new int[n];\n            var covariances = new double[clusterCount][,];\n            var means = PlaceCentroidsOnSphere(clusterCount, dimensions, meanSeparation, rng);\n\n            int idx = 0;\n            for (int c = 0; c < clusterCount; c++)\n            {\n                var scale = new double[dimensions];\n                scale[0] = anisotropyRatio;\n                for (int d = 1; d < dimensions; d++) scale[d] = 1.0;\n\n                var rotation = RandomRotationMatrix(dimensions, rng);\n                covariances[c] = BuildCovariance(rotation, scale);\n\n                for (int p = 0; p < pointsPerCluster; p++)\n                {\n                    var z = new double[dimensions];\n                    for (int d = 0; d < dimensions; d++)\n                        z[d] = scale[d] * SampleStandardNormal(rng);\n                    var rotated = MultiplyMatrixVector(rotation, z);\n                    var point = new double[dimensions];\n                    for (int d = 0; d < dimensions; d++)\n                        point[d] = means[c][d] + rotated[d];\n                    features[idx] = point;\n                    labels[idx] = c;\n                    idx++;\n                }\n            }\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = clusterCount,\n                ClusterCovariances = covariances,\n                Parameters = new Dictionary<string, object>\n                {\n                    [\"clusterCount\"] = clusterCount,\n                    [\"pointsPerCluster\"] = pointsPerCluster,\n                    [\"dimensions\"] = dimensions,\n                    [\"meanSeparation\"] = meanSeparation,\n                    [\"anisotropyRatio\"] = anisotropyRatio,\n                    [\"seed\"] = seed\n                }\n            };\n        }\n    }\n} |
