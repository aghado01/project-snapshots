idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
30 | spc.batch.cs | {22156 2055 0.3118 4.4759} | 22705 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n    // =========================================================================\n    // Metric Enum — extensible dispatch key for partial-class metric kernels\n    // =========================================================================\n    public enum SpcMetric { Hamming, Euclidean, Canberra, Manhattan, Minkowski, Cosine, JensenShannon, Jaccard, Wasserstein, Mahalanobis, FisherRao }\n\n    // =========================================================================\n    // Delta Estimator — bandwidth selection for Gaussian coupling auto-delta\n    // =========================================================================\n    public enum DeltaEstimator { Mean, Median }\n\n    // =========================================================================\n    // Proximity — graph construction rule (Axis 2)\n    // Determines which pairs become edges from the distance field.\n    // =========================================================================\n    public enum SpcProximity { Knn, MutualKnn, EpsilonBall, MstAugmented }\n\n    // =========================================================================\n    // Coupling Kernel — edge weight function J(d, δ) (Axis 4)\n    // Maps pairwise distance and bandwidth to a coupling strength in [0, 1].\n    // =========================================================================\n    public enum CouplingKernel { Gaussian, Cauchy, Laplacian, Linear }\n\n    // =========================================================================\n    // Batch Request / Result DTOs — the PS↔C# contract\n    // =========================================================================\n\n    public sealed class SpcBatchRequest\n    {\n        public SpcMetric Metric { get; set; } = SpcMetric.Hamming;\n\n        // Proximity rule: how to select which pairs become edges (Axis 2).\n        // Default Knn applies OR-symmetrization to produce an undirected graph.\n        public SpcProximity Proximity { get; set; } = SpcProximity.Knn;\n\n        // Epsilon radius for EpsilonBall proximity (ignored by other rules).\n        public double Epsilon { get; set; } = 0.0;\n\n        // Hamming path: 64-bit SimHash signatures produced upstream by\n        // Hashish.SimHash.Compute. The metric kernel never parses text or hex.\n        public ulong[] SimHashes { get; set; }\n\n        // Feature-vector path (Euclidean, Canberra, future metrics)\n        public double[][] Features { get; set; }\n\n        public int K { get; set; }\n        public double[] Temperatures { get; set; } = Array.Empty<double>();\n        public int Steps { get; set; }\n        public int BaseSeed { get; set; } = -1;\n\n        // Gaussian coupling scale: J = exp(-d^2 / 2*Delta^2).\n        // If 0 or negative, auto-computed from 1-NN distances using Estimator.\n        public double Delta { get; set; } = 0.0;\n\n        // Bandwidth estimator for auto-delta: Mean (validated by prior work), Median (theoretically\n        // more robust to outliers but untested in this context — use with caution).\n        public DeltaEstimator Estimator { get; set; } = DeltaEstimator.Mean;\n\n        // Coupling kernel (Axis 4): edge weight function J(d, δ).\n        // Gaussian is the canonical SPC formulation; others alter tail behaviour.\n        public CouplingKernel CouplingKernel { get; set; } = CouplingKernel.Gaussian;\n\n        // Minkowski p-norm exponent (Manhattan=1, Euclidean=2, Chebyshev→∞)\n        public double MinkowskiP { get; set; } = 2.0;\n\n        // Binarization threshold for Jaccard distance (feature > threshold → present).\n        public double JaccardThreshold { get; set; } = 0.5;\n\n        // Inverse covariance matrix for Mahalanobis distance (caller pre-computes)\n        public double[,] CovarianceInverse { get; set; }\n\n        // In-memory checkpoint — pass SpcBatchResult.ToCheckpoint() from a previous run.\n        // Takes precedence over ResumeDirectory. Opt-in; null means fresh start.\n        public SpcCheckpoint Checkpoint { get; set; }\n\n        // Opt-in disk resume: the yyyyMMdd_HHmmss runStamp returned in\n        // SpcBatchResult.RunDirectory from a previous run. Pass together with\n        // CheckpointDirectory to reload that run's .ckpt files on resume.\n        // Ignored if Checkpoint is provided. Null means fresh start.\n        public string ResumeDirectory { get; set; }\n\n        // Base directory for checkpoint artifacts. If set, each temperature's\n        // result is atomically flushed to a spc_{stamp}_{X16}.ckpt file here\n        // (flat directory — no subdirectories). The stamp is returned in RunDirectory.\n        public string CheckpointDirectory { get; set; }\n    }\n\n    public sealed class SpcBatchResult\n    {\n        public double[] Temperatures { get; set; } = Array.Empty<double>();\n        public double[] Susceptibility { get; set; } = Array.Empty<double>();\n        public Dictionary<double, int[]> FinalSpins { get; set; } = new Dictionary<double, int[]>();\n\n        // The proximity graph that produced this result. Fixed topology — shared\n        // read-only across all temperatures. Required by post-hoc analysis methods.\n        public CsrGraph Graph { get; set; }\n\n        // Post-construction diagnostics surfaced to the PS orchestration layer.\n        public string[] Warnings { get; set; } = Array.Empty<string>();\n\n        /// <summary>\n        /// The yyyyMMdd_HHmmss stamp used as the filename prefix for this run's checkpoint files.\n        /// Pass as SpcBatchRequest.ResumeDirectory (together with CheckpointDirectory) to resume.\n        /// Null if CheckpointDirectory was not set.\n        /// </summary>\n        public string RunDirectory { get; set; }\n\n        // Produces a checkpoint from whatever temperatures completed.\n        // Pass as SpcBatchRequest.Checkpoint on resume to skip completed\n        // temperatures and warm-start any that were interrupted mid-run.\n        public SpcCheckpoint ToCheckpoint()\n        {\n            var cp = new SpcCheckpoint();\n            for (int i = 0; i < Temperatures.Length; i++)\n            {\n                double T = Temperatures[i];\n                if (FinalSpins.TryGetValue(T, out var spins))\n                {\n                    cp.Spins[T] = spins;\n                    cp.Susceptibility[T] = Susceptibility[i];\n                }\n            }\n            return cp;\n        }\n    }\n\n    // =========================================================================\n    // SpcCheckpoint — pause/resume contract between PS and the simulation core\n    // =========================================================================\n\n    public sealed class SpcCheckpoint\n    {\n        // Temperatures with an entry in both Spins and Susceptibility are\n        // considered complete and will be skipped on resume.\n        // Temperatures with an entry only in Spins get a warm-start.\n        public Dictionary<double, int[]> Spins { get; set; } = new Dictionary<double, int[]>();\n        public Dictionary<double, double> Susceptibility { get; set; } = new Dictionary<double, double>();\n    }\n\n    // =========================================================================\n    // SpcBatch — partial class dispatcher + shared simulation core\n    // =========================================================================\n\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Single entry point from PowerShell. Dispatches to the metric-specific\n        /// graph builder, then runs the shared simulation core over all temperatures.\n        /// Pass a CancellationToken to support graceful pause — completed temperatures\n        /// are returned in the result and can be checkpointed via ToCheckpoint().\n        /// </summary>\n        public static SpcBatchResult Run(SpcBatchRequest request,\n            System.Threading.CancellationToken ct = default)\n        {\n            Edge[] edges;\n            int n;\n\n            switch (request.Metric)\n            {\n                case SpcMetric.Hamming:\n                    edges = BuildHammingGraph(request, out n);\n                    break;\n                case SpcMetric.Euclidean:\n                    edges = BuildEuclideanGraph(request, out n);\n                    break;\n                case SpcMetric.Canberra:\n                    edges = BuildCanberraGraph(request, out n);\n                    break;\n                case SpcMetric.Manhattan:\n                    edges = BuildManhattanGraph(request, out n);\n                    break;\n                case SpcMetric.Minkowski:\n                    edges = BuildMinkowskiGraph(request, out n);\n                    break;\n                case SpcMetric.Cosine:\n                    edges = BuildCosineGraph(request, out n);\n                    break;\n                case SpcMetric.JensenShannon:\n                    edges = BuildJensenShannonGraph(request, out n);\n                    break;\n                case SpcMetric.Jaccard:\n                    edges = BuildJaccardGraph(request, out n);\n                    break;\n                case SpcMetric.Wasserstein:\n                    edges = BuildWassersteinGraph(request, out n);\n                    break;\n                case SpcMetric.Mahalanobis:\n                    edges = BuildMahalanobisGraph(request, out n);\n                    break;\n                case SpcMetric.FisherRao:\n                    edges = BuildFisherRaoGraph(request, out n);\n                    break;\n                default:\n                    throw new NotSupportedException($\"Metric {request.Metric} not implemented.\");\n            }\n\n            return RunSimulationCore(edges, n, request,\n                ValidateGraph(edges, n, request.Proximity), ct);\n        }\n\n        // -----------------------------------------------------------------\n        // Delta estimation dispatcher — parallels metric dispatch pattern.\n        // Each estimator lives in its own partial class under src/deltas/.\n        // -----------------------------------------------------------------\n        internal static double EstimateDelta(DeltaEstimator estimator, double[] nnDistances)\n        {\n            switch (estimator)\n            {\n                case DeltaEstimator.Mean: return EstimateDeltaMean(nnDistances);\n                case DeltaEstimator.Median: return EstimateDeltaMedian(nnDistances);\n                default: throw new NotSupportedException($\"Estimator {estimator} not implemented.\");\n            }\n        }\n\n        // -----------------------------------------------------------------\n        // Post-construction graph validation.\n        // Checks connectivity via union-find on the final edge set.\n        // Returns warnings for the PS layer; does not block simulation.\n        // -----------------------------------------------------------------\n        internal static List<string> ValidateGraph(Edge[] edges, int n, SpcProximity proximity)\n        {\n            var warnings = new List<string>();\n\n            if (edges.Length == 0)\n            {\n                warnings.Add(\"Graph has zero edges. Simulation will produce no structure.\");\n                return warnings;\n            }\n\n            // Find connected components via union-find\n            var uf = new FastUnionFind(n);\n            for (int i = 0; i < edges.Length; i++)\n                uf.Union(edges[i].Source, edges[i].Target);\n\n            // Count component sizes\n            var componentSizes = new Dictionary<int, int>();\n            for (int i = 0; i < n; i++)\n            {\n                int root = uf.Find(i);\n                if (!componentSizes.ContainsKey(root))\n                    componentSizes[root] = 0;\n                componentSizes[root]++;\n            }\n\n            int numComponents = componentSizes.Count;\n            int largestComponent = 0;\n            foreach (var kvp in componentSizes)\n                if (kvp.Value > largestComponent)\n                    largestComponent = kvp.Value;\n\n            double coverage = (double)largestComponent / n;\n            int isolatedNodes = 0;\n            foreach (var kvp in componentSizes)\n                if (kvp.Value == 1)\n                    isolatedNodes++;\n\n            if (numComponents > 1)\n            {\n                warnings.Add(string.Format(\n                    \"Graph is disconnected: {0} components. Largest covers {1:P1} of nodes ({2}/{3}). Proximity: {4}.\",\n                    numComponents, coverage, largestComponent, n, proximity));\n            }\n\n            if (coverage < 0.9)\n            {\n                warnings.Add(string.Format(\n                    \"Largest connected component covers only {0:P1} of nodes. \" +\n                    \"Potts phase transitions may not reflect global data structure. \" +\n                    \"Consider MstAugmented proximity or increasing K.\",\n                    coverage));\n            }\n\n            if (isolatedNodes > 0)\n            {\n                warnings.Add(string.Format(\n                    \"{0} isolated node(s) with no edges. These will never join any cluster.\",\n                    isolatedNodes));\n            }\n\n            return warnings;\n        }\n\n        // -----------------------------------------------------------------\n        // Shared simulation core: runs SW for all temperatures on one Edge[].\n        // Supports graceful cancellation — partial results are returned for\n        // any temperatures that completed before cancellation was observed.\n        // -----------------------------------------------------------------\n        private static SpcBatchResult RunSimulationCore(Edge[] edges, int n, SpcBatchRequest r,\n            List<string> warnings, System.Threading.CancellationToken ct = default)\n        {\n            var result = new SpcBatchResult\n            {\n                Temperatures = (double[])r.Temperatures.Clone(),\n                Susceptibility = new double[r.Temperatures.Length],\n                FinalSpins = new Dictionary<double, int[]>(r.Temperatures.Length),\n                Warnings = warnings.ToArray()\n            };\n\n            // Build CSR once — shared read-only across all temperature threads (HPC 3).\n            var graph = CsrGraph.FromEdges(edges, n);\n            result.Graph = graph;\n\n            // Auto-load checkpoint from disk if directory is set but no explicit checkpoint provided.\n            // This is the primary crash-recovery path — the caller just re-runs with the same directory.\n            string runStamp = null;\n            if (!string.IsNullOrEmpty(r.CheckpointDirectory))\n            {\n                runStamp = DateTime.UtcNow.ToString(\"yyyyMMdd_HHmmss\");\n                result.RunDirectory = runStamp;\n            }\n\n            SpcCheckpoint checkpoint = r.Checkpoint;\n            if (checkpoint == null && !string.IsNullOrEmpty(r.CheckpointDirectory))\n            {\n                string loadStamp = !string.IsNullOrEmpty(r.ResumeDirectory) ? r.ResumeDirectory : runStamp;\n                checkpoint = LoadCheckpointFromDirectory(r.CheckpointDirectory, loadStamp);\n            }\n\n            // Eager flush: each temperature writes its result as soon as it finishes.\n            // ConcurrentDictionary ensures partial results survive graceful cancellation.\n            // Disk flush (if CheckpointDirectory is set) survives hard power loss.\n            var completed = new System.Collections.Concurrent.ConcurrentDictionary<double, SimulationResult>();\n\n            var opts = new ParallelOptions { CancellationToken = ct };\n\n            try\n            {\n                Parallel.For(0, r.Temperatures.Length, opts, i =>\n                {\n                    double T = r.Temperatures[i];\n\n                    // Skip temperatures that are fully checkpointed (spins + susceptibility).\n                    if (checkpoint != null &&\n                        checkpoint.Spins.TryGetValue(T, out var cpSpins) &&\n                        checkpoint.Susceptibility.TryGetValue(T, out var cpSusc))\n                    {\n                        completed[T] = new SimulationResult { FinalSpins = cpSpins, Susceptibility = cpSusc };\n                        return;\n                    }\n\n                    int seed = r.BaseSeed < 0\n                        ? -1\n                        : Math.Abs(r.BaseSeed ^ T.GetHashCode()) % int.MaxValue;\n\n                    var model = new PottsModel(n, q: 20, seed: seed);\n\n                    // Warm-start: checkpoint spins present but no susceptibility — resume from this state.\n                    if (checkpoint != null && checkpoint.Spins.TryGetValue(T, out var warmSpins))\n                        model.SetSpins(warmSpins);\n\n                    model.LoadGraph(graph);\n\n                    var sr = model.RunSimulation(T, r.Steps);\n\n                    // Atomic disk flush before in-memory write — if power is lost between\n                    // these two lines, the .ckpt file exists and will be loaded on resume.\n                    if (!string.IsNullOrEmpty(r.CheckpointDirectory))\n                        FlushTemperatureCheckpoint(r.CheckpointDirectory, runStamp, T, sr);\n\n                    completed[T] = sr;\n                });\n            }\n            catch (OperationCanceledException) { /* partial results already in completed / on disk */ }\n\n            // Merge whatever completed — skipped temperatures leave default zeros in Susceptibility[].\n            for (int i = 0; i < r.Temperatures.Length; i++)\n            {\n                double T = r.Temperatures[i];\n                if (completed.TryGetValue(T, out var sr))\n                {\n                    result.Susceptibility[i] = sr.Susceptibility;\n                    result.FinalSpins[T] = sr.FinalSpins;\n                }\n            }\n\n            return result;\n        }\n\n        // -----------------------------------------------------------------\n        // Proximity dispatch — selects neighbors based on the proximity rule.\n        // Each proximity type lives in its own partial class under src/proximities/.\n        // Returns per-node neighbor lists (undirected) and 1-NN distances.\n        // -----------------------------------------------------------------\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighbors(\n            SpcProximity proximity, int n, int k, double epsilon,\n            Func<int, int, double> pairDistance)\n        {\n            switch (proximity)\n            {\n                case SpcProximity.Knn:\n                    return SelectNeighborsKnn(n, k, pairDistance);\n                case SpcProximity.MutualKnn:\n                    return SelectNeighborsMutualKnn(n, k, pairDistance);\n                case SpcProximity.EpsilonBall:\n                    return SelectNeighborsEpsilonBall(n, epsilon, pairDistance);\n                case SpcProximity.MstAugmented:\n                    return SelectNeighborsMstAugmented(n, k, pairDistance);\n                default:\n                    throw new NotSupportedException($\"Proximity {proximity} not implemented.\");\n            }\n        }\n\n        // -----------------------------------------------------------------\n        // Coupling conversion — transforms raw neighbor distances to coupling\n        // strengths J(d, δ) via the selected kernel (Axis 4).\n        // Kernel dispatch is resolved once outside the loop.\n        // -----------------------------------------------------------------\n        internal static Edge[] ConvertToCoupling(\n            Neighbor[][] allNeighbors, double[] nnDistances,\n            double delta, DeltaEstimator estimator,\n            CouplingKernel kernel = CouplingKernel.Gaussian)\n        {\n            double effectiveDelta = delta > 0 ? delta : EstimateDelta(estimator, nnDistances);\n\n            Func<double, double, double> kernelFunc;\n            switch (kernel)\n            {\n                case CouplingKernel.Gaussian: kernelFunc = GaussianCoupling; break;\n                case CouplingKernel.Cauchy: kernelFunc = CauchyCoupling; break;\n                case CouplingKernel.Laplacian: kernelFunc = LaplacianCoupling; break;\n                case CouplingKernel.Linear: kernelFunc = LinearCoupling; break;\n                default: throw new NotSupportedException($\"CouplingKernel {kernel} not implemented.\");\n            }\n\n            var edges = new List<Edge>();\n            int n = allNeighbors.Length;\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in allNeighbors[i])\n                {\n                    // After symmetrization, each pair appears in both nodes' neighbor lists.\n                    // Emit only the canonical direction (lo → hi) to avoid duplicate edges.\n                    if (i > nb.Index) continue;\n\n                    double J = kernelFunc(nb.Distance, effectiveDelta);\n                    if (J > 1e-10)\n                        edges.Add(new Edge(i, nb.Index, J));\n                }\n            }\n\n            return edges.ToArray();\n        }\n\n        // -----------------------------------------------------------------\n        // Shared helper: build graph from pairwise distance function.\n        // Dispatches through proximity (Axis 2) then coupling conversion.\n        // distanceFunc(featureA, featureB) → raw distance scalar\n        // -----------------------------------------------------------------\n        internal static Edge[] BuildGraphFromFeatures(\n            double[][] features, int k, double delta, DeltaEstimator estimator,\n            Func<double[], double[], double> distanceFunc,\n            SpcProximity proximity = SpcProximity.Knn, double epsilon = 0.0,\n            CouplingKernel kernel = CouplingKernel.Gaussian)\n        {\n            int n = features.Length;\n\n            // Wrap feature-level distance as index-level distance for proximity dispatch\n            Func<int, int, double> pairDistance = (i, j) => distanceFunc(features[i], features[j]);\n\n            var (allNeighbors, nnDistances) = SelectNeighbors(proximity, n, k, epsilon, pairDistance);\n\n            return ConvertToCoupling(allNeighbors, nnDistances, delta, estimator, kernel);\n        }\n    }\n} |
31 | spc.checkpoint.cs | {3279 300 0.3327 4.4480} | 3385 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.IO;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        // -----------------------------------------------------------------\n        // Checkpoint I/O — temperature-granular, crash-safe persistence.\n        //\n        // Each completed temperature is atomically flushed to its own file:\n        //   1. Write full content to {key}.tmp\n        //   2. Rename to {key}.ckpt  ← NTFS rename is atomic\n        //\n        // A partial .tmp left by a power loss is never promoted to .ckpt\n        // and is silently ignored on reload. Only complete .ckpt files\n        // contribute to the resumed checkpoint.\n        //\n        // File key: spc_{runStamp}_{X16} where runStamp is \"yyyyMMdd_HHmmss\"\n        // (stamped once per Run() call) and X16 is the IEEE-754 bit pattern of T\n        // as 16-char hex — no dots, no minus signs, stable, collision-free.\n        // The X16 segment is parsed from file content (lines[0]), not the filename.\n        // -----------------------------------------------------------------\n\n        internal static void FlushTemperatureCheckpoint(\n            string directory, string runStamp, double T, SimulationResult sr)\n        {\n            Directory.CreateDirectory(directory);\n\n            string key = $\"spc_{runStamp}_{BitConverter.DoubleToInt64Bits(T):X16}\";\n            string tmpPath = Path.Combine(directory, key + \".tmp\");\n            string ckptPath = Path.Combine(directory, key + \".ckpt\");\n\n            using (var sw = new StreamWriter(tmpPath))\n            {\n                sw.WriteLine(T.ToString(\"G17\", CultureInfo.InvariantCulture));\n                sw.WriteLine(sr.Susceptibility.ToString(\"G17\", CultureInfo.InvariantCulture));\n                sw.WriteLine(string.Join(\" \", sr.FinalSpins));\n            }\n\n            // Atomic promotion — only a fully written file becomes .ckpt\n            File.Move(tmpPath, ckptPath, overwrite: true);\n        }\n\n        internal static SpcCheckpoint LoadCheckpointFromDirectory(string directory, string runStamp)\n        {\n            var cp = new SpcCheckpoint();\n            if (!Directory.Exists(directory))\n                return cp;\n\n            string pattern = $\"spc_{runStamp}_*.ckpt\";\n            foreach (string file in Directory.GetFiles(directory, pattern))\n            {\n                try\n                {\n                    string[] lines = File.ReadAllLines(file);\n                    if (lines.Length < 3) continue;\n\n                    double T = double.Parse(lines[0], CultureInfo.InvariantCulture);\n                    double susc = double.Parse(lines[1], CultureInfo.InvariantCulture);\n\n                    string[] parts = lines[2].Split(' ');\n                    var spins = new int[parts.Length];\n                    for (int i = 0; i < parts.Length; i++)\n                        spins[i] = int.Parse(parts[i], CultureInfo.InvariantCulture);\n\n                    cp.Spins[T] = spins;\n                    cp.Susceptibility[T] = susc;\n                }\n                catch\n                {\n                    // Corrupt or partial file — skip silently; temperature will re-run.\n                }\n            }\n\n            return cp;\n        }\n    }\n} |
