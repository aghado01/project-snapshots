idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | coupling/Cauchy.cs | {439 62 0.3212 4.3598} | 457 | namespace CouplingKernels\n{\n    public static class Cauchy\n    {\n        /// <summary>\n        /// J = 1 / (1 + d² / δ²). Heavy-tailed — decays much slower than Gaussian.\n        /// Retains weak long-range coupling that Gaussian would zero out.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            return 1.0 / (1.0 + (distance * distance) / (delta * delta));\n        }\n    }\n} |
1 | coupling/Gaussian.cs | {463 54 0.3153 4.4384} | 482 | using System;\n\nnamespace CouplingKernels\n{\n    public static class Gaussian\n    {\n        /// <summary>\n        /// J = exp(-d² / 2δ²). Smooth, differentiable decay.\n        /// Natural choice for Euclidean-geometry metrics.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            double twoSigmaSq = 2.0 * delta * delta;\n            return Math.Exp(-(distance * distance) / twoSigmaSq);\n        }\n    }\n} |
2 | coupling/Laplacian.cs | {434 53 0.3065 4.3458} | 453 | using System;\n\nnamespace CouplingKernels\n{\n    public static class Laplacian\n    {\n        /// <summary>\n        /// J = exp(-d / δ). Decays on distance, not distance². Sharper local contrast\n        /// than Gaussian — near neighbors are favoured more aggressively.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            return Math.Exp(-distance / delta);\n        }\n    }\n} |
3 | coupling/Linear.cs | {441 58 0.3129 4.4077} | 459 | using System;\n\nnamespace CouplingKernels\n{\n    public static class Linear\n    {\n        /// <summary>\n        /// J = max(0, 1 - d / δ). Compact support — exactly zero beyond one scale length.\n        /// Hard locality boundary; no long-range coupling whatsoever.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            return Math.Max(0.0, 1.0 - distance / delta);\n        }\n    }\n} |
4 | estimators/GeometricMean.cs | {1934 141 0.2921 4.4406} | 1986 | // ============================================================================\n// Estimators/Location/Mean.cs\n// ============================================================================\nusing System;\nusing System.Buffers;\nusing Optimization;\nusing Losses;\nusing Manifolds;\nusing Estimators.Scale;\n\nnamespace Estimators.Location\n{\n    public static class Mean\n    {\n        /// <summary>μ only (hot path). destination is mutated in place.</summary>\n        public static void Compute<TManifold>(\n            TManifold manifold,\n            ReadOnlySpan<double[]> data,\n            ReadOnlySpan<double> weights,\n            double[] destination,\n            IrlsOptions opts = default)\n            where TManifold : struct, IRiemannianManifold\n        {\n            Irls.Solve<TManifold, L2Loss>(manifold, data, weights, destination, opts);\n        }\n\n        /// <summary>(μ, Σ) hot path. Both destinations supplied by caller; zero allocation.</summary>\n        public static void ComputeWithScatter<TManifold>(\n            TManifold manifold,\n            ReadOnlySpan<double[]> data,\n            ReadOnlySpan<double> weights,\n            double[] locationDestination,\n            Span<double> scatterDestination,\n            IrlsOptions opts = default,\n            double consistencyFactor = 1.0)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int n = data.Length;\n            double[] finalW = ArrayPool<double>.Shared.Rent(n);\n            try\n            {\n                Span<double> wSpan = finalW.AsSpan(0, n);\n                Irls.Solve<TManifold, L2Loss>(manifold, data, weights, locationDestination, opts, wSpan);\n                ScatterAccumulator.Accumulate(manifold, data, locationDestination, wSpan,\n                                               scatterDestination, consistencyFactor);\n            }\n            finally { ArrayPool<double>.Shared.Return(finalW); }\n        }\n    }\n} |
5 | estimators/GeometricMedian.cs | {2639 237 0.2815 4.5525} | 2830 | // ============================================================================\n// Estimators/Location/Median.Wrappers.cs\n// ============================================================================\nusing System;\nusing Optimization;\nusing Manifolds;\n\nnamespace Estimators.Location\n{\n    public static partial class Median\n    {\n        // ─── Style 1: MATLAB-like tuple return (Allocating) ─────────────────\n\n        /// <summary>\n        /// Ergonomic multivariate overload. Allocates and returns the (μ, Σ) pair.\n        /// </summary>\n        public static (double[] Location, double[,] Scatter) ComputeWithScatter<TManifold>(\n            TManifold manifold,\n            ReadOnlySpan<double[]> data,\n            ReadOnlySpan<double> weights,\n            double[] initialLocation,\n            IrlsOptions opts = default,\n            double consistencyFactor = 1.0)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int dim = manifold.Dimension;\n            double[] location = (double[])initialLocation.Clone();\n            double[,] scatter = new double[dim, dim];\n\n            // Span cast to alias the 2D array memory safely\n            Span<double> scatterFlat = System.Runtime.InteropServices.MemoryMarshal.CreateSpan(ref scatter[0, 0], dim * dim);\n\n            ComputeWithScatter(manifold, data, weights, location, scatterFlat, opts, consistencyFactor);\n\n            return (location, scatter);\n        }\n\n        // ─── Style 2: 1D Scalar Wrapper ──────────────────────────────────────\n\n        /// <summary>\n        /// Fast 1D scalar overload. Wraps the inputs into single-element arrays\n        /// and extracts the scalars from the result, avoiding [0,0] indexing at the call site.\n        /// </summary>\n        public static (double Location, double Scatter) ComputeWithScatter(\n            ReadOnlySpan<double> data1D,\n            ReadOnlySpan<double> weights,\n            double initialLocation,\n            IrlsOptions opts = default,\n            double consistencyFactor = 1.0)\n        {\n            int n = data1D.Length;\n            double[][] dataWrapped = new double[n][];\n            for (int i = 0; i < n; i++)\n            {\n                dataWrapped[i] = new double[] { data1D[i] };\n            }\n\n            double[] locationDest = new double[] { initialLocation };\n            double[] scatterDest = new double[1];\n\n            var manifold = new EuclideanVectorManifold(dimension: 1);\n\n            ComputeWithScatter(manifold, dataWrapped, weights, locationDest, scatterDest, opts, consistencyFactor);\n\n            return (locationDest[0], scatterDest[0]);\n        }\n    }\n} |
6 | estimators/PointEstimates.cs | {734 83 0.3215 4.2494} | 762 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Diagnostics returned by iterative or closed-form location estimators.\n    /// </summary>\n    public readonly struct LocationEstimate\n    {\n        public LocationEstimate(bool converged, int iterations, double objective, double shift, double totalWeight)\n        {\n            Converged = converged;\n            Iterations = iterations;\n            Objective = objective;\n            Shift = shift;\n            TotalWeight = totalWeight;\n        }\n\n        public bool Converged { get; }\n\n        public int Iterations { get; }\n\n        public double Objective { get; }\n\n        public double Shift { get; }\n\n        public double TotalWeight { get; }\n    }\n} |
7 | estimators/ScatterAccumulator.cs | {1833 185 0.3191 4.4574} | 1909 | // ============================================================================\n// Estimators/Scale/ScatterAccumulator.cs (internal helper)\n// ============================================================================\nnamespace Estimators.Scale;\n\ninternal static class ScatterAccumulator\n{\n    /// <summary>\n    /// Σ = (consistencyFactor / Σᵢ ŵᵢ) · Σᵢ ŵᵢ vᵢ vᵢᵀ\n    /// where vᵢ = log_μ(xᵢ) and ŵᵢ are the converged IRLS weights from Optimization.Irls.Solve.\n    /// Destination is a row-major D×D Span<double> of length D².\n    /// </summary>\n    public static void Accumulate<TManifold>(\n        TManifold manifold,\n        ReadOnlySpan<double[]> data,\n        in double[] location,\n        ReadOnlySpan<double> finalIrlsWeights,\n        Span<double> scatterDestination,\n        double consistencyFactor)\n        where TManifold : struct, IRiemannianManifold<double[]>\n    {\n        int dim = manifold.Dimension;\n        double[] logBuf = ArrayPool<double>.Shared.Rent(dim);\n        try\n        {\n            Span<double> v = logBuf.AsSpan(0, dim);\n            scatterDestination.Clear();\n            double sumW = 0;\n            for (int i = 0; i < data.Length; i++)\n            {\n                double w = finalIrlsWeights[i];\n                if (w == 0) continue;\n                sumW += w;\n                manifold.LogMap(in location, data[i], v);\n                for (int r = 0; r < dim; r++)\n                    for (int c = 0; c < dim; c++)\n                        scatterDestination[r * dim + c] += w * v[r] * v[c];\n            }\n            if (sumW > 0)\n            {\n                double scale = consistencyFactor / sumW;\n                for (int k = 0; k < scatterDestination.Length; k++) scatterDestination[k] *= scale;\n            }\n        }\n        finally { ArrayPool<double>.Shared.Return(logBuf); }\n    }\n} |
8 | gmm/GaussianComponent.cs | {7102 740 0.2747 4.6827} | 8003 | using System;\nusing DistanceMetrics;\nusing LinearAlgebra;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// A single Gaussian component: weight π_k, mean μ_k, covariance Σ_k, and the\n    /// derived quantities needed for efficient E-Step evaluation.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// <strong>Zero-allocation M-Step.</strong>\n    /// Pre-allocated scratchpads (<see cref=\"ScratchMean\"/>, <see cref=\"ScratchCov\"/>)\n    /// are accumulated in place during the M-Step and committed via\n    /// <see cref=\"CommitScratch\"/>. No heap allocations occur during the EM loop.\n    /// </para>\n    /// <para>\n    /// <strong>Cholesky preservation.</strong>\n    /// The lower-triangular factor L (Σ = LLᵀ) is retained in a dedicated field after\n    /// <see cref=\"UpdateCache\"/> completes. Future sampling support can be added as\n    /// x = μ + L·z, z ~ N(0,I) without recomputing the decomposition.\n    /// </para>\n    /// </remarks>\n    public sealed class GaussianComponent\n    {\n        private readonly int _dim;\n\n        // ── Parameters ──────────────────────────────────────────────────────────\n\n        /// <summary>Mixture weight π_k.</summary>\n        public double Weight { get; set; }\n\n        /// <summary>Mean vector μ_k. Write elements in place; do not replace the array reference.</summary>\n        public double[] Mean { get; }\n\n        /// <summary>Covariance Σ_k. Write elements in place; do not replace the array reference.</summary>\n        public double[,] Covariance { get; }\n\n        // ── Cached quantities (updated by UpdateCache) ───────────────────────────\n\n        /// <summary>Σ⁻¹ — used in Mahalanobis distance computation during the E-Step.</summary>\n        public double[,] CovarianceInverse { get; }\n\n        /// <summary>\n        /// −½(D·ln(2π) + ln|Σ|). Cached to avoid recomputing the log-determinant on every\n        /// PDF evaluation. Set by <see cref=\"UpdateCache\"/>.\n        /// </summary>\n        public double LogNormalizationFactor { get; private set; }\n\n        // ── M-Step scratchpads (pre-allocated; internal to GaussianMixtureModel) ─\n\n        internal double[] ScratchMean { get; }\n        internal double[,] ScratchCov { get; }\n\n        // ── Cholesky work arrays ─────────────────────────────────────────────────\n\n        private readonly CholeskyDecomposition _chol;\n\n        public GaussianComponent(int dim)\n        {\n            _dim = dim;\n            Mean = new double[dim];\n            Covariance = new double[dim, dim];\n            CovarianceInverse = new double[dim, dim];\n            ScratchMean = new double[dim];\n            ScratchCov = new double[dim, dim];\n            _chol = new CholeskyDecomposition(dim);\n        }\n\n        // ── M-Step helpers ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Zeroes both scratchpad arrays. Call once per component per EM iteration\n        /// before accumulating weighted observations.\n        /// </summary>\n        internal void ResetScratch()\n        {\n            Array.Clear(ScratchMean, 0, ScratchMean.Length);\n            Array.Clear(ScratchCov, 0, ScratchCov.Length);\n        }\n\n        /// <summary>\n        /// Copies the normalised <see cref=\"ScratchMean\"/> into <see cref=\"Mean\"/>,\n        /// normalises <see cref=\"ScratchCov\"/> by <paramref name=\"effectiveCount\"/> into\n        /// <see cref=\"Covariance\"/> (with diagonal regularisation), updates\n        /// <see cref=\"Weight\"/>, and calls <see cref=\"UpdateCache\"/>.\n        /// </summary>\n        /// <param name=\"effectiveCount\">\n        /// Σ_i γ(z_{ik}) — the soft cluster population. <see cref=\"ScratchMean\"/> must\n        /// already be divided by this value before calling (done by the M-Step caller).\n        /// </param>\n        /// <param name=\"n\">Total data count N (used to compute the new weight).</param>\n        /// <param name=\"regularization\">Ridge term added to the diagonal of Σ to prevent singularity.</param>\n        internal void CommitScratch(double effectiveCount, int n, double regularization = 1e-6)\n        {\n            Weight = effectiveCount / n;\n\n            if (effectiveCount > 1e-12)\n            {\n                // ScratchMean has already been normalised by the caller; copy directly.\n                Array.Copy(ScratchMean, Mean, _dim);\n\n                double invCount = 1.0 / effectiveCount;\n                for (int d1 = 0; d1 < _dim; d1++)\n                    for (int d2 = 0; d2 < _dim; d2++)\n                    {\n                        Covariance[d1, d2] = ScratchCov[d1, d2] * invCount\n                                             + (d1 == d2 ? regularization : 0.0);\n                    }\n            }\n            // Dead cluster: leave Mean/Covariance at prior values.\n            // Weight ≈ 0 suppresses this component's contribution in future E-Steps.\n\n            UpdateCache();\n        }\n\n        // ── Cache update ─────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Decomposes <see cref=\"Covariance\"/> via <see cref=\"CholeskyDecomposition\"/>,\n        /// writes Σ⁻¹ into <see cref=\"CovarianceInverse\"/>, and caches\n        /// <see cref=\"LogNormalizationFactor\"/>. Call after writing new values directly\n        /// into <see cref=\"Covariance\"/> from external initialisation or the M-Step.\n        /// </summary>\n        public void UpdateCache()\n        {\n            _chol.Decompose(Covariance);\n            _chol.WriteInverseTo(CovarianceInverse);\n            LogNormalizationFactor = -0.5 * (_dim * Math.Log(2.0 * Math.PI) + _chol.LogDet);\n        }\n\n        // ── PDF evaluation ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Evaluates ln N(x | μ, Σ). Numerically safe in all dimensions; use in the E-Step.\n        /// </summary>\n        public double EvaluateLogPdf(double[] x) =>\n            LogNormalizationFactor - 0.5 * ComputeMahalanobisSquared(x);\n\n        /// <summary>\n        /// Evaluates N(x | μ, Σ) directly. May underflow to 0 in high dimensions;\n        /// prefer <see cref=\"EvaluateLogPdf\"/> for all EM arithmetic.\n        /// </summary>\n        public double EvaluatePdf(double[] x) =>\n            Math.Exp(EvaluateLogPdf(x));\n\n        /// <summary>\n        /// Returns the squared Mahalanobis distance from <paramref name=\"x\"/> to this component's mean.\n        /// D² = (x−μ)ᵀ Σ⁻¹ (x−μ). Exposed for use by <see cref=\"GaussianMixtureModel.Mahal\"/>.\n        /// Delegates to <see cref=\"Mahalanobis.DistanceSquared\"/> in <c>DistanceMetrics</c>.\n        /// </summary>\n        public double MahalanobisSquared(double[] x)\n            => Mahalanobis.DistanceSquared(x, Mean, CovarianceInverse);\n\n        /// <summary>\n        /// Draws a single sample via the <see cref=\"CholeskyDecomposition\"/> preserved\n        /// from the last <see cref=\"UpdateCache\"/> call.\n        /// </summary>\n        public double[] Sample(Random rng) => _chol.Sample(rng, Mean);\n\n        private double ComputeMahalanobisSquared(double[] x)\n            => Mahalanobis.DistanceSquared(x, Mean, CovarianceInverse);\n    }\n} |
