idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
45 | clustering/spc/runtime/execution/SpcExecutor.cs | {10032 750 0.2998 4.5039} | 10341 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.IO;\nusing System.Threading;\nusing System.Threading.Tasks;\nusing Clustering.SPC.Runtime.Sampler;\nusing Clustering.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Execution;\n\npublic sealed class SpcExecutor : ISpcExecutor\n{\n\n    public List<SpcTaskSpec> BuildTaskList(\n        IReadOnlyList<double> temperatures,\n        int numReplicas,\n        int cyclesPerTask,\n        int q,\n        PottsObservableTier observableTier,\n        string checkpointDirectory,\n        int? baseSeed = null,\n        int burnInCyclesPerTask = 0)\n    {\n        if (temperatures is null || temperatures.Count == 0)\n            throw new ArgumentException(\"Temperatures must be non-empty.\", nameof(temperatures));\n        if (numReplicas <= 0)\n            throw new ArgumentOutOfRangeException(nameof(numReplicas), \"Replica count must be positive.\");\n        if (cyclesPerTask <= 0)\n            throw new ArgumentOutOfRangeException(nameof(cyclesPerTask), \"Cycles per task must be positive.\");\n        if (burnInCyclesPerTask < 0)\n            throw new ArgumentOutOfRangeException(nameof(burnInCyclesPerTask), \"Burn-in cycle count cannot be negative.\");\n        if (string.IsNullOrWhiteSpace(checkpointDirectory))\n            throw new ArgumentException(\"Checkpoint directory must be provided.\", nameof(checkpointDirectory));\n\n        var tasks = new List<SpcTaskSpec>(temperatures.Count * numReplicas);\n        foreach (double temperature in temperatures)\n        {\n            for (int replica = 0; replica < numReplicas; replica++)\n            {\n                tasks.Add(new SpcTaskSpec\n                {\n                    Temperature = temperature,\n                    ReplicaIndex = replica,\n                    Cycles = cyclesPerTask,\n                    Q = q,\n                    ObservableTier = observableTier,\n                    CheckpointPath = Path.Combine(checkpointDirectory, FileNameFor(temperature, replica)),\n                    BaseSeed = baseSeed,\n                    BurnInCycles = burnInCyclesPerTask,\n                });\n            }\n        }\n\n        return tasks;\n    }\n\n    public void Run(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (tasks is null)\n            throw new ArgumentNullException(nameof(tasks));\n        if (tasks.Count == 0)\n            return;\n\n        bool persist = executionOptions?.PersistArtifacts ?? true;\n        if (persist)\n            EnsureOutputDirectories(tasks);\n\n        Parallel.ForEach(tasks, CreateParallelOptions(ct, executionOptions), task =>\n        {\n            if (persist && File.Exists(task.CheckpointPath))\n                return;\n\n            var result = ExecuteTask(graph, task, executionOptions);\n            if (persist)\n                WriteResultArtifacts(task, result);\n        });\n    }\n\n    public SpcBatchResult RunBatch(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (tasks is null)\n            throw new ArgumentNullException(nameof(tasks));\n\n        bool persist = executionOptions?.PersistArtifacts ?? true;\n        if (persist)\n            EnsureOutputDirectories(tasks);\n\n        var sw = System.Diagnostics.Stopwatch.StartNew();\n        var results = new SpcRunResult?[tasks.Count];\n        var executedCount = 0;\n        var skippedCount = 0;\n\n        Parallel.For(0, tasks.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            var task = tasks[i];\n            if (persist && File.Exists(task.CheckpointPath))\n            {\n                System.Threading.Interlocked.Increment(ref skippedCount);\n                return;\n            }\n\n            var result = ExecuteTask(graph, task, executionOptions);\n            if (persist)\n                WriteResultArtifacts(task, result);\n            results[i] = result;\n            System.Threading.Interlocked.Increment(ref executedCount);\n        });\n\n        sw.Stop();\n        var executedRuns = new List<SpcRunResult>(executedCount);\n        foreach (var result in results)\n        {\n            if (result is not null)\n                executedRuns.Add(result);\n        }\n\n        return new SpcBatchResult\n        {\n            Runs = executedRuns,\n            RequestedTaskCount = tasks.Count,\n            ExecutedTaskCount = executedCount,\n            SkippedTaskCount = skippedCount,\n            Elapsed = sw.Elapsed,\n        };\n    }\n\n    public IReadOnlyList<SpcRunResult> ExecuteAll(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (tasks is null)\n            throw new ArgumentNullException(nameof(tasks));\n\n        var results = new SpcRunResult[tasks.Count];\n        Parallel.For(0, tasks.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            results[i] = ExecuteTask(graph, tasks[i], executionOptions);\n        });\n\n        return results;\n    }\n\n    public IReadOnlyList<SpcRunResult> ExecuteAll(\n        IReadOnlyList<PottsRunSpec> specs,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (specs is null)\n            throw new ArgumentNullException(nameof(specs));\n\n        var results = new SpcRunResult[specs.Count];\n        Parallel.For(0, specs.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            results[i] = ExecuteTask(specs[i], executionOptions);\n        });\n\n        return results;\n    }\n\n    public SpcBatchResult ExecuteBatch(\n        IReadOnlyList<PottsRunSpec> specs,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (specs is null)\n            throw new ArgumentNullException(nameof(specs));\n\n        var sw = System.Diagnostics.Stopwatch.StartNew();\n        var results = new SpcRunResult[specs.Count];\n\n        Parallel.For(0, specs.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            results[i] = ExecuteTask(specs[i], executionOptions);\n        });\n\n        sw.Stop();\n        return new SpcBatchResult\n        {\n            Runs = results,\n            RequestedTaskCount = specs.Count,\n            ExecutedTaskCount = specs.Count,\n            SkippedTaskCount = 0,\n            Elapsed = sw.Elapsed,\n        };\n    }\n\n    public SpcRunResult ExecuteTask(CsrGraph graph, SpcTaskSpec task, SpcExecutionOptions? executionOptions = null)\n    {\n        if (task is null)\n            throw new ArgumentNullException(nameof(task));\n\n        return ExecuteTask(new PottsRunSpec\n        {\n            Graph = graph,\n            Temperature = task.Temperature,\n            Q = task.Q,\n            ObservableTier = task.ObservableTier,\n            Seed = ResolveSeed(task.BaseSeed, task.ReplicaIndex, task.Temperature, executionOptions),\n            BurnInCycles = task.BurnInCycles,\n            Cycles = task.Cycles,\n            ReplicaIndex = task.ReplicaIndex,\n        }, executionOptions);\n    }\n\n    public SpcRunResult ExecuteTask(PottsRunSpec spec, SpcExecutionOptions? executionOptions = null)\n    {\n        if (spec is null)\n            throw new ArgumentNullException(nameof(spec));\n\n        var resolvedSpec = spec with\n        {\n            Seed = ResolveSeed(spec.Seed, spec.ReplicaIndex, spec.Temperature, executionOptions),\n        };\n\n        var pottsResult = PottsRunner.Run(resolvedSpec);\n        return new SpcRunResult\n        {\n            Graph = spec.Graph,\n            Frame = pottsResult.Frame,\n            EdgeObservables = pottsResult.EdgeObservables,\n        };\n    }\n\n    private static void EnsureOutputDirectories(IReadOnlyList<SpcTaskSpec> tasks)\n    {\n        var directories = new HashSet<string>(StringComparer.OrdinalIgnoreCase);\n        foreach (var task in tasks)\n        {\n            var dir = Path.GetDirectoryName(task.CheckpointPath);\n            if (!string.IsNullOrEmpty(dir) && directories.Add(dir))\n                Directory.CreateDirectory(dir);\n        }\n    }\n\n    private static void WriteResultArtifacts(SpcTaskSpec task, SpcRunResult result)\n    {\n        WriteCheckpoint(task.CheckpointPath, result.Frame);\n\n        if (result.EdgeObservables is not null)\n        {\n            string spcePath = Path.ChangeExtension(task.CheckpointPath, \".spce\");\n            PottsEdgeObservablesSerializer.WriteToFile(result.EdgeObservables, spcePath);\n        }\n    }\n\n    private static void WriteCheckpoint(string checkpointPath, PottsModelCoreObservables frame)\n    {\n        PottsCoreObservablesSerializer.WriteToFile(frame, checkpointPath);\n    }\n\n    private static ParallelOptions CreateParallelOptions(\n        CancellationToken ct,\n        SpcExecutionOptions? executionOptions)\n    {\n        var maxDegreeOfParallelism = executionOptions?.MaxDegreeOfParallelism ?? -1;\n        return new ParallelOptions\n        {\n            CancellationToken = ct,\n            MaxDegreeOfParallelism = maxDegreeOfParallelism > 0\n                ? maxDegreeOfParallelism\n                : Environment.ProcessorCount,\n        };\n    }\n\n    private static int? ResolveSeed(\n        int? explicitSeed,\n        int replicaIndex,\n        double temperature,\n        SpcExecutionOptions? executionOptions)\n    {\n        if (explicitSeed.HasValue)\n            return explicitSeed;\n        if (executionOptions?.BaseSeed is not int baseSeed)\n            return null;\n\n        int tQuantized = (int)(temperature * executionOptions.TemperatureQuantizationFactor);\n        return HashCode.Combine(baseSeed, replicaIndex, tQuantized);\n    }\n\n    private static string FileNameFor(double temperature, int replicaIndex)\n        => $\"T_{temperature.ToString(\"F5\", CultureInfo.InvariantCulture)}_rep_{replicaIndex}.spcx\";\n}
46 | clustering/spc/runtime/execution/SpcRunResult.cs | {391 35 0.2583 4.3970} | 401 | using Clustering.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Execution;\n\npublic sealed record SpcRunResult\n{\n    public required CsrGraph                   Graph           { get; init; }\n    public required PottsModelCoreObservables       Frame           { get; init; }\n    public          PottsModelEdgeObservables? EdgeObservables { get; init; }\n}
47 | clustering/spc/runtime/execution/SpcTaskSpec.cs | {657 74 0.3196 4.1335} | 673 | using Clustering.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Execution;\n\npublic sealed record SpcTaskSpec\n{\n    public required double              Temperature    { get; init; }\n    public required int                 ReplicaIndex   { get; init; }\n    public required int                 Cycles         { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsObservableTier ObservableTier { get; init; }\n    public required string              CheckpointPath { get; init; }\n\n    public int? BaseSeed { get; init; }\n    public int   BurnInCycles { get; init; }\n}
48 | clustering/spc/runtime/sampler/potts/CoreObservables.cs | {1220 154 0.2648 4.3129} | 1252 | using System;\n\nnamespace Clustering.SPC.Runtime.Sampler.Potts;\n\npublic sealed record PottsModelCoreObservables\n{\n\n    public required double              Temperature    { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsObservableTier ObservableTier { get; init; }\n\n    public int ReplicaIndex { get; init; }\n\n    public required int CycleCount { get; init; }\n\n    public required int[] Spins                { get; init; }\n    public required int[] ClusterSizeHistogram { get; init; }\n\n    public required ulong RngState0 { get; init; }\n    public required ulong RngState1 { get; init; }\n    public required ulong RngState2 { get; init; }\n    public required ulong RngState3 { get; init; }\n\n    public required double RunningSumSqClusterSizes     { get; init; }\n    public required double RunningSumSqClusterSizesExcl { get; init; }\n    public required double RunningSumEnergy             { get; init; }\n    public required double RunningSumEnergySq           { get; init; }\n\n    public required double RunningSumMag   { get; init; }\n    public required double RunningSumMagSq { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n}
49 | clustering/spc/runtime/sampler/potts/EdgeObservables.cs | {471 58 0.2314 4.4866} | 486 | using System;\n\nnamespace Clustering.SPC.Runtime.Sampler.Potts;\n\npublic sealed record PottsModelEdgeObservables\n{\n\n    public required double Temperature  { get; init; }\n    public          int    ReplicaIndex { get; init; }\n    public required int    CycleCount   { get; init; }\n\n    public required int[]  BondFormedCount    { get; init; }\n    public required int[]  SpinAgreementCount { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n}
50 | clustering/spc/runtime/sampler/potts/IPottsConfig.cs | {434 49 0.1728 4.6784} | 458 | namespace Clustering.SPC.Runtime.Sampler.Potts;\n\npublic interface IPottsConfig\n{\n\n    static abstract bool EdgeObservables { get; }\n}\n\npublic readonly struct StandardObservables : IPottsConfig\n{\n    public static bool EdgeObservables => false;\n}\n\npublic readonly struct WithEdgeObservables : IPottsConfig\n{\n    public static bool EdgeObservables => true;\n}\n\npublic enum PottsObservableTier\n{\n\n    Standard,\n\n    WithEdgeObservables,\n}
51 | clustering/spc/runtime/sampler/potts/IPottsModel.cs | {758 84 0.2137 4.6828} | 787 | using System;\n\nnamespace Clustering.SPC.Runtime.Sampler.Potts;\n\ninternal interface IPottsModel\n{\n    void Cycle();\n    void Run(int cycleCount);\n    void BurnIn(int cycles);\n\n    int CycleCount { get; }\n    double Temperature { get; }\n    int Q { get; }\n    int N { get; }\n\n    ReadOnlySpan<int> Spins { get; }\n    ReadOnlySpan<int> ClusterSizeHistogram { get; }\n\n    double RunningSumSqClusterSizes { get; }\n    double RunningSumSqClusterSizesExcl { get; }\n    double RunningSumEnergy { get; }\n    double RunningSumEnergySq { get; }\n    double RunningSumMag { get; }\n    double RunningSumMagSq { get; }\n\n    PottsModelCoreObservables GetCheckpoint();\n    void Restore(PottsModelCoreObservables result);\n\n    PottsModelEdgeObservables? GetEdgeCheckpoint();\n}
52 | clustering/spc/runtime/sampler/potts/PottsModel.cs | {11386 1035 0.3179 4.5936} | 11710 | using System;\nusing System.Numerics;\nusing System.Runtime.CompilerServices;\nusing Graphs.Primitives;\nusing Maths.Rng;\n\nnamespace Clustering.SPC.Runtime.Sampler.Potts;\n\ninternal sealed class PottsModel<TConfig> : IPottsModel\n    where TConfig : struct, IPottsConfig\n{\n\n    private readonly CsrGraph _graph;\n    private readonly double _temperature;\n    private readonly int _q;\n    private readonly int _n;\n\n    private readonly int[] _spins;             // [N] Potts color of each node, in [0, q)\n    private readonly UnionFind _uf;            // bond-cluster structure, rebuilt every cycle\n    private readonly double[] _bondTable;      // [E] p_e = 1 - exp(-J_e / T), precomputed once\n    private readonly Xoshiro256PlusPlus _rng;\n\n    private readonly int[] _rootSizeScratch;   // [N] compacted UF root sizes after WriteRootSizesTo\n    private readonly int[] _clusterMap;        // [N] root index -> new color, sentinel -1\n    private readonly int[] _colorBucketScratch; // [Q] color counts (Blatt is always on)\n\n    private readonly int[]? _bondFormedCount;     // [E] cycles where bond formed at edge e\n    private readonly int[]? _spinAgreementCount;  // [E] cycles where _spins[i] == _spins[j] at edge e\n\n    private readonly int[] _clusterSizeHistogram;  // log-binned; bin k = sizes [2^k, 2^(k+1))\n\n    private double _runningSumSqClusterSizes;       // Σ_cycles Σ_c |c|², the FK accumulator\n    private double _runningSumSqClusterSizesExcl;   // Σ_cycles (Σ_c |c|² − max_c|c|²), percolation diagnostic\n    private double _runningSumEnergy;               // Σ_cycles ⟨H⟩\n    private double _runningSumEnergySq;             // Σ_cycles ⟨H²⟩, for specific heat\n    private double _runningSumMag;                  // Σ_cycles m  (Blatt; always populated)\n    private double _runningSumMagSq;                // Σ_cycles m² (Blatt; always populated)\n\n    private int _cycleCount;\n\n    public PottsModel(CsrGraph graph, double temperature, int q, int? seed)\n    {\n        if (graph.NodeCount <= 0) throw new ArgumentException(\"Graph must have at least one node.\", nameof(graph));\n        if (graph.Targets is null || graph.Weights is null || graph.RowPointers is null)\n            throw new ArgumentException(\"Graph CSR arrays are not initialized.\", nameof(graph));\n        if (temperature <= 0.0) throw new ArgumentOutOfRangeException(nameof(temperature), \"Temperature must be strictly positive.\");\n        if (q < 2) throw new ArgumentOutOfRangeException(nameof(q), \"Q must be at least 2.\");\n\n        _graph = graph;\n        _temperature = temperature;\n        _q = q;\n        _n = graph.NodeCount;\n\n        _spins = new int[_n];\n        _uf = new UnionFind(_n);\n        _bondTable = new double[graph.Targets.Length];\n        _rng = new Xoshiro256PlusPlus(seed);\n\n        double invT = 1.0 / _temperature;\n        for (int e = 0; e < _bondTable.Length; e++)\n        {\n            _bondTable[e] = 1.0 - Math.Exp(-graph.Weights[e] * invT);\n        }\n\n        _rootSizeScratch = new int[_n];\n        _clusterMap = new int[_n];\n\n        _colorBucketScratch = new int[_q];\n\n        _bondFormedCount    = TConfig.EdgeObservables ? new int[_bondTable.Length] : null;\n        _spinAgreementCount = TConfig.EdgeObservables ? new int[_bondTable.Length] : null;\n\n        int maxBin = BitOperations.Log2((uint)Math.Max(1, _n)) + 1;\n        _clusterSizeHistogram = new int[maxBin];\n\n        for (int i = 0; i < _n; i++)\n            _spins[i] = _rng.NextInt(_q);\n    }\n\n    public int CycleCount => _cycleCount;\n    public double Temperature => _temperature;\n    public int Q => _q;\n    public int N => _n;\n\n    public ReadOnlySpan<int> Spins => _spins;\n    public ReadOnlySpan<int> ClusterSizeHistogram => _clusterSizeHistogram;\n\n    public double RunningSumSqClusterSizes     => _runningSumSqClusterSizes;\n    public double RunningSumSqClusterSizesExcl => _runningSumSqClusterSizesExcl;\n    public double RunningSumEnergy             => _runningSumEnergy;\n    public double RunningSumEnergySq           => _runningSumEnergySq;\n    public double RunningSumMag                => _runningSumMag;\n    public double RunningSumMagSq              => _runningSumMagSq;\n\n    public void Run(int cycleCount)\n    {\n        if (cycleCount < 0) throw new ArgumentOutOfRangeException(nameof(cycleCount));\n        for (int i = 0; i < cycleCount; i++)\n            Cycle();\n    }\n\n    public void BurnIn(int cycles)\n    {\n        if (cycles < 0) throw new ArgumentOutOfRangeException(nameof(cycles));\n        for (int i = 0; i < cycles; i++)\n            Cycle();\n        ResetAccumulators();\n    }\n\n    public void Cycle()\n    {\n        _uf.Reset();\n\n        double cycleEnergy = 0.0;\n        for (int i = 0; i < _n; i++)\n        {\n            int rowEnd = _graph.RowPointers[i + 1];\n            for (int e = _graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = _graph.Targets[e];\n                if (j <= i) continue;  // undirected: visit each edge once\n\n                if (_spins[i] == _spins[j])\n                {\n                    if (TConfig.EdgeObservables) _spinAgreementCount![e]++;\n                    cycleEnergy -= _graph.Weights[e];\n                    if (_rng.NextDouble() < _bondTable[e])\n                    {\n                        if (TConfig.EdgeObservables) _bondFormedCount![e]++;\n                        _uf.Union(i, j);\n                    }\n                }\n            }\n        }\n\n        _clusterMap.AsSpan().Fill(-1);\n        for (int i = 0; i < _n; i++)\n        {\n            int root = _uf.Find(i);\n            int newColor = _clusterMap[root];\n            if (newColor < 0)\n            {\n                newColor = _rng.NextInt(_q);\n                _clusterMap[root] = newColor;\n            }\n            _spins[i] = newColor;\n        }\n\n        int rootCount = _uf.WriteRootSizesTo(_rootSizeScratch);\n        ReadOnlySpan<int> rootSizes = _rootSizeScratch.AsSpan(0, rootCount);\n        long sumSq = 0L;\n        long maxSize = 0L;\n        for (int r = 0; r < rootCount; r++)\n        {\n            long sz = rootSizes[r];\n            sumSq += sz * sz;\n            if (sz > maxSize) maxSize = sz;\n        }\n        _runningSumSqClusterSizes     += sumSq;\n        _runningSumSqClusterSizesExcl += sumSq - maxSize * maxSize;\n\n        int nMax = MaxColorBucket(_spins, _q, _colorBucketScratch);\n        double m = ((double)nMax * _q / _n - 1.0) / (_q - 1.0);\n        _runningSumMag   += m;\n        _runningSumMagSq += m * m;\n\n        for (int r = 0; r < rootCount; r++)\n        {\n            int bin = BitOperations.Log2((uint)rootSizes[r]);\n            _clusterSizeHistogram[bin]++;\n        }\n\n        _runningSumEnergy   += cycleEnergy;\n        _runningSumEnergySq += cycleEnergy * cycleEnergy;\n        _cycleCount++;\n    }\n\n    public PottsModelEdgeObservables? GetEdgeCheckpoint()\n    {\n        if (!TConfig.EdgeObservables) return null;\n        return new PottsModelEdgeObservables\n        {\n            Temperature        = _temperature,\n            CycleCount         = _cycleCount,\n            BondFormedCount    = (int[])_bondFormedCount!.Clone(),\n            SpinAgreementCount = (int[])_spinAgreementCount!.Clone(),\n        };\n    }\n\n    public PottsModelCoreObservables GetCheckpoint()\n    {\n        var (s0, s1, s2, s3) = _rng.SaveState();\n        var tier = TConfig.EdgeObservables\n            ? PottsObservableTier.WithEdgeObservables\n            : PottsObservableTier.Standard;\n\n        return new PottsModelCoreObservables\n        {\n            Temperature              = _temperature,\n            Q                        = _q,\n            ObservableTier           = tier,\n            CycleCount               = _cycleCount,\n            Spins                    = (int[])_spins.Clone(),\n            ClusterSizeHistogram     = (int[])_clusterSizeHistogram.Clone(),\n            RngState0                = s0,\n            RngState1                = s1,\n            RngState2                = s2,\n            RngState3                = s3,\n            RunningSumSqClusterSizes     = _runningSumSqClusterSizes,\n            RunningSumSqClusterSizesExcl = _runningSumSqClusterSizesExcl,\n            RunningSumEnergy             = _runningSumEnergy,\n            RunningSumEnergySq           = _runningSumEnergySq,\n            RunningSumMag                = _runningSumMag,\n            RunningSumMagSq              = _runningSumMagSq,\n        };\n    }\n\n    public void Restore(PottsModelCoreObservables result)\n    {\n\n        var thisTier = TConfig.EdgeObservables\n            ? PottsObservableTier.WithEdgeObservables\n            : PottsObservableTier.Standard;\n\n        if (result.ObservableTier != thisTier)\n            throw new InvalidOperationException(\n                $\"Snapshot observable tier {result.ObservableTier} does not match model {thisTier}.\");\n        if (result.Temperature != _temperature)\n            throw new InvalidOperationException(\n                $\"Snapshot temperature {result.Temperature} does not match model temperature {_temperature}.\");\n        if (result.Q != _q)\n            throw new InvalidOperationException(\n                $\"Snapshot Q {result.Q} does not match model Q {_q}.\");\n        if (result.Spins is null || result.Spins.Length != _n)\n            throw new InvalidOperationException(\n                $\"Snapshot spin count {result.Spins?.Length ?? 0} does not match model node count {_n}.\");\n        if (result.ClusterSizeHistogram is null || result.ClusterSizeHistogram.Length != _clusterSizeHistogram.Length)\n            throw new InvalidOperationException(\n                $\"Snapshot histogram bin count {result.ClusterSizeHistogram?.Length ?? 0} does not match model bin count {_clusterSizeHistogram.Length}.\");\n\n        Array.Copy(result.Spins, _spins, _n);\n        Array.Copy(result.ClusterSizeHistogram, _clusterSizeHistogram, _clusterSizeHistogram.Length);\n\n        _rng.LoadState(result.RngState0, result.RngState1, result.RngState2, result.RngState3);\n\n        _cycleCount                   = result.CycleCount;\n        _runningSumSqClusterSizes     = result.RunningSumSqClusterSizes;\n        _runningSumSqClusterSizesExcl = result.RunningSumSqClusterSizesExcl;\n        _runningSumEnergy             = result.RunningSumEnergy;\n        _runningSumEnergySq           = result.RunningSumEnergySq;\n        _runningSumMag                = result.RunningSumMag;\n        _runningSumMagSq              = result.RunningSumMagSq;\n    }\n\n    private void ResetAccumulators()\n    {\n        _runningSumSqClusterSizes     = 0.0;\n        _runningSumSqClusterSizesExcl = 0.0;\n        _runningSumEnergy             = 0.0;\n        _runningSumEnergySq           = 0.0;\n        _runningSumMag                = 0.0;\n        _runningSumMagSq              = 0.0;\n        _cycleCount                   = 0;\n        _clusterSizeHistogram.AsSpan().Clear();\n        if (TConfig.EdgeObservables)\n        {\n            _bondFormedCount!.AsSpan().Clear();\n            _spinAgreementCount!.AsSpan().Clear();\n        }\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static int MaxColorBucket(ReadOnlySpan<int> spins, int q, Span<int> scratch)\n    {\n        scratch.Clear();\n        for (int i = 0; i < spins.Length; i++)\n            scratch[spins[i]]++;\n\n        int max = 0;\n        for (int c = 0; c < q; c++)\n            if (scratch[c] > max) max = scratch[c];\n        return max;\n    }\n}
53 | clustering/spc/runtime/sampler/PottsRunner.cs | {2225 182 0.2719 4.5971} | 2294 | using System;\nusing Clustering.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Sampler;\n\npublic static class PottsRunner\n{\n    public static PottsRunResult Run(PottsRunSpec spec)\n    {\n        if (spec is null)\n            throw new ArgumentNullException(nameof(spec));\n        if (spec.Graph.NodeCount <= 0)\n            throw new ArgumentException(\"Graph must be provided and non-empty.\", nameof(spec));\n        if (spec.Temperature <= 0)\n            throw new ArgumentOutOfRangeException(nameof(spec.Temperature), \"Temperature must be positive.\");\n        if (spec.Q < 2)\n            throw new ArgumentOutOfRangeException(nameof(spec.Q), \"Q must be at least 2.\");\n        if (spec.BurnInCycles < 0)\n            throw new ArgumentOutOfRangeException(nameof(spec.BurnInCycles), \"Burn-in cycle count cannot be negative.\");\n        if (spec.Cycles < 0)\n            throw new ArgumentOutOfRangeException(nameof(spec.Cycles), \"Cycle count cannot be negative.\");\n\n        var model = CreateModel(\n            spec.Graph,\n            spec.Temperature,\n            spec.Q,\n            spec.ObservableTier,\n            spec.Seed);\n\n        if (spec.BurnInCycles > 0)\n            model.BurnIn(spec.BurnInCycles);\n\n        model.Run(spec.Cycles);\n\n        var frame = model.GetCheckpoint() with { ReplicaIndex = spec.ReplicaIndex };\n        var edgeObservables = model.GetEdgeCheckpoint();\n        if (edgeObservables is not null)\n            edgeObservables = edgeObservables with { ReplicaIndex = spec.ReplicaIndex };\n\n        return new PottsRunResult\n        {\n            Frame = frame,\n            EdgeObservables = edgeObservables,\n        };\n    }\n\n    private static IPottsModel CreateModel(\n        CsrGraph graph,\n        double temperature,\n        int q,\n        PottsObservableTier observableTier,\n        int? seed)\n        => observableTier switch\n        {\n            PottsObservableTier.Standard => new PottsModel<StandardObservables>(graph, temperature, q, seed),\n            PottsObservableTier.WithEdgeObservables => new PottsModel<WithEdgeObservables>(graph, temperature, q, seed),\n            _ => throw new ArgumentOutOfRangeException(nameof(observableTier)),\n        };\n}
