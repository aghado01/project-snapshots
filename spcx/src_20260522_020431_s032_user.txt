idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
220 | user/SpcTabularProjections.cs | {4176 292 0.3412 4.3138} | 4349 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.SPC;\nusing Clustering.SPC.Partitions;\nusing Clustering.SPC.Profiling;\nusing Tabular;\n\nnamespace User;\n\npublic static class SpcTabularProjections\n{\n    public static TabularProjection ToTabular(this SweepProfile profile, string tableName = \"spc_sweep\")\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n\n        var rows = Enumerable.Range(0, profile.Count)\n            .Select(index => new\n            {\n                Index = index,\n                Temperature = profile.Temperatures[index],\n                Susceptibility = profile.Susceptibility[index],\n                SpecificHeat = profile.SpecificHeat[index],\n                LabelEntropy = profile.LabelEntropy[index],\n                BondEntropy = profile.BondEntropy,\n            });\n\n        return TabularProjectionFactory.Create(\n            tableName,\n            new[] { \"Index\", \"Temperature\", \"Susceptibility\", \"SpecificHeat\", \"LabelEntropy\", \"BondEntropy\" },\n            rows,\n            item => item.Index,\n            item => item.Temperature,\n            item => item.Susceptibility,\n            item => item.SpecificHeat,\n            item => item.LabelEntropy,\n            item => item.BondEntropy);\n    }\n\n    public static TabularProjection ToTabular(this ProfileCriteria criteria, string tableName = \"spc_criteria\")\n    {\n        if (criteria is null)\n            throw new ArgumentNullException(nameof(criteria));\n\n        return new TabularProjection(\n            tableName,\n            new[] { \"AnchorTemperature\", \"AnchorLo\", \"AnchorHi\", \"RefinedTemperature\", \"CorroborationScore\", \"EnrichmentKeys\", \"EnrichmentValues\" },\n            new[]\n            {\n                new object?[]\n                {\n                    criteria.AnchorTemperature,\n                    criteria.AnchorBand.Lo,\n                    criteria.AnchorBand.Hi,\n                    criteria.RefinedTemperature,\n                    criteria.CorroborationScore,\n                    string.Join(';', criteria.Enrichments.Keys),\n                    string.Join(';', criteria.Enrichments.Values),\n                }\n            });\n    }\n\n    public static TabularProjection ToTabular(this Partition partition, string tableName = \"spc_partition\")\n    {\n        if (partition is null)\n            throw new ArgumentNullException(nameof(partition));\n\n        var rows = Enumerable.Range(0, partition.Labels.Length)\n            .Select(index => new { Index = index, Cluster = partition.Labels[index] });\n\n        return TabularProjectionFactory.Create(\n            tableName,\n            new[] { \"Index\", \"Cluster\" },\n            rows,\n            item => item.Index,\n            item => item.Cluster);\n    }\n\n    public static TabularProjection ToTabular(this SpcSessionResult result, string tableName = \"spc_session\")\n    {\n        if (result is null)\n            throw new ArgumentNullException(nameof(result));\n\n        return new TabularProjection(\n            tableName,\n            new[] { \"ClusterCount\", \"Purity\", \"AnchorTemperature\", \"RefinedTemperature\", \"CorroborationScore\" },\n            new[]\n            {\n                new object?[]\n                {\n                    result.Partition.ClusterCount,\n                    result.Purity,\n                    result.ProfileCriteria.AnchorTemperature,\n                    result.ProfileCriteria.RefinedTemperature,\n                    result.ProfileCriteria.CorroborationScore,\n                }\n            });\n    }\n\n    public static TabularProjection ToTabular(this SpcUserDataset dataset, string tableName = \"spc_dataset\")\n    {\n        if (dataset is null)\n            throw new ArgumentNullException(nameof(dataset));\n\n        var rows = Enumerable.Range(0, dataset.Labels.Length)\n            .Select(index => new\n            {\n                Index = index,\n                Label = dataset.Labels[index],\n            });\n\n        return TabularProjectionFactory.Create(\n            tableName,\n            new[] { \"Index\", \"Label\" },\n            rows,\n            item => item.Index,\n            item => item.Label);\n    }\n}
221 | user/SpcUserApi.cs | {5928 401 0.2763 4.5300} | 6114 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Linq;\nusing Clustering.Evaluation.External;\nusing Clustering.SPC;\nusing Clustering.SPC.Graph;\nusing Clustering.SPC.Output;\nusing Clustering.SPC.Partitions;\nusing Synthetic.Euclidean;\nusing Synthetic.Manifolds;\nusing Tabular;\n\nnamespace User;\n\npublic enum SpcDatasetKind\n{\n    BlattHierarchy,\n    BlattThreeCluster,\n    HyperbolicHierarchy,\n}\n\npublic sealed record SpcUserDataset(\n    double[][] Features,\n    int[] Labels,\n    int ClusterCount,\n    int[][]? LabelsByLevel,\n    IReadOnlyDictionary<string, object?> Metadata)\n{\n    public static SpcUserDataset FromBlattHierarchy(int seed)\n    {\n        var dataset = BlattHierarchy.Generate(seed: seed);\n        return new SpcUserDataset(\n            Features: dataset.Features,\n            Labels: dataset.Labels,\n            ClusterCount: dataset.ClusterCount,\n            LabelsByLevel: dataset.LabelsByLevel,\n            Metadata: new Dictionary<string, object?>\n            {\n                [\"Generator\"] = nameof(BlattHierarchy),\n                [\"Seed\"] = seed,\n            });\n    }\n\n    public static SpcUserDataset FromBlattThreeCluster(int pointsPerCluster, double stdDev, int seed)\n    {\n        var dataset = BlattThreeCluster.Generate(pointsPerCluster: pointsPerCluster, stdDev: stdDev, seed: seed);\n        return new SpcUserDataset(\n            Features: dataset.Features,\n            Labels: dataset.Labels,\n            ClusterCount: dataset.ClusterCount,\n            LabelsByLevel: dataset.LabelsByLevel,\n            Metadata: new Dictionary<string, object?>\n            {\n                [\"Generator\"] = nameof(BlattThreeCluster),\n                [\"PointsPerCluster\"] = pointsPerCluster,\n                [\"StdDev\"] = stdDev,\n                [\"Seed\"] = seed,\n            });\n    }\n\n    public static SpcUserDataset FromHyperbolicHierarchy(int seed)\n    {\n        var dataset = HyperbolicHierarchy.Generate(seed: seed);\n        return new SpcUserDataset(\n            Features: dataset.Features,\n            Labels: dataset.Labels,\n            ClusterCount: dataset.ClusterCount,\n            LabelsByLevel: dataset.LabelsByLevel,\n            Metadata: new Dictionary<string, object?>\n            {\n                [\"Generator\"] = nameof(HyperbolicHierarchy),\n                [\"Seed\"] = seed,\n            });\n    }\n}\n\npublic sealed record SpcUserRunResult(\n    SpcSessionResult SessionResult,\n    SpcUserDataset Dataset,\n    string RunDirectory,\n    string SweepCsvPath,\n    string PartitionCsvPath,\n    string CriteriaCsvPath,\n    string SessionCsvPath,\n    string SummaryJsonPath);\n\npublic static class SpcUserSession\n{\n    public static SpcUserDataset GenerateDataset(\n        SpcDatasetKind kind,\n        int seed = 42,\n        int pointsPerCluster = 100,\n        double stdDev = 1.0)\n    {\n        return kind switch\n        {\n            SpcDatasetKind.BlattHierarchy => SpcUserDataset.FromBlattHierarchy(seed),\n            SpcDatasetKind.BlattThreeCluster => SpcUserDataset.FromBlattThreeCluster(pointsPerCluster, stdDev, seed),\n            SpcDatasetKind.HyperbolicHierarchy => SpcUserDataset.FromHyperbolicHierarchy(seed),\n            _ => throw new ArgumentOutOfRangeException(nameof(kind), kind, null),\n        };\n    }\n\n    public static SpcUserRunResult Run(\n        SpcUserDataset dataset,\n        SpcGraphConfig graphConfig,\n        string baseDirectory,\n        string runName,\n        bool includeGuid = true,\n        IExternalClusterEvaluator? evaluator = null,\n        int[]? referenceLabels = null)\n    {\n        if (dataset is null)\n            throw new ArgumentNullException(nameof(dataset));\n\n        if (string.IsNullOrWhiteSpace(baseDirectory))\n            throw new ArgumentException(\"Base directory must be provided.\", nameof(baseDirectory));\n\n        string runDirectory = SpcOutputPathHelper.CreateRunDirectory(baseDirectory, runName, includeGuid: includeGuid);\n        string sweepPath = SpcOutputPathHelper.GetSweepCsvPath(runDirectory);\n        string partitionPath = SpcOutputPathHelper.GetPartitionCsvPath(runDirectory);\n        string criteriaPath = SpcOutputPathHelper.GetCriteriaCsvPath(runDirectory);\n        string sessionPath = SpcOutputPathHelper.GetSessionCsvPath(runDirectory);\n\n        SpcSessionResult result = SpcClusteringSession.Run(\n            dataset.Features,\n            graphConfig: graphConfig,\n            evaluator: evaluator,\n            referenceLabels: referenceLabels);\n\n        SpcCsvWriter.WriteSweepProfile(result.Profile, sweepPath);\n        SpcCsvWriter.WritePartition(result.Partition, partitionPath, features: dataset.Features, trueLabels: dataset.Labels);\n        SpcCsvWriter.WriteCriteria(result.ProfileCriteria, result.Purity, criteriaPath);\n        SpcCsvWriter.WriteSessionSummary(result, sessionPath);\n\n        string summaryPath = Path.Combine(runDirectory, \"summary.json\");\n        File.WriteAllText(summaryPath, System.Text.Json.JsonSerializer.Serialize(new\n        {\n            Dataset = dataset.Metadata,\n            Graph = graphConfig,\n            Partition = new\n            {\n                ClusterCount = result.Partition.ClusterCount,\n                Purity = result.Purity,\n            },\n            Run = new\n            {\n                RunDirectory = runDirectory,\n                SweepCsv = sweepPath,\n                PartitionCsv = partitionPath,\n                CriteriaCsv = criteriaPath,\n                SessionCsv = sessionPath,\n            }\n        }, new System.Text.Json.JsonSerializerOptions { WriteIndented = true }));\n\n        return new SpcUserRunResult(\n            SessionResult: result,\n            Dataset: dataset,\n            RunDirectory: runDirectory,\n            SweepCsvPath: sweepPath,\n            PartitionCsvPath: partitionPath,\n            CriteriaCsvPath: criteriaPath,\n            SessionCsvPath: sessionPath,\n            SummaryJsonPath: summaryPath);\n    }\n}
