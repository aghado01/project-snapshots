idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
75 | viz-core/viz_core.cs | {12006 1551 0.2259 4.8135} | 12606 | // The minimal thing the viz engine needs to render any scene.\n// All indices are into the N-point array. All arrays are readonly.\n\nusing System;\nusing System.Collections.Generic;\n\nnamespace Viz;\n\n/// <summary>\n/// The invariant core: N points in D dimensions, optionally named,\n/// with no opinion about where they came from.\n/// </summary>\npublic sealed class PointCloud\n{\n    public ReadOnlyMemory<double> Features { get; }  // N x D, row-major\n    public int N { get; }\n    public int D { get; }\n    public string? Label { get; }  // human name, e.g. \"Crescent + Ellipsoid (N=500)\"\n\n    public PointCloud(double[] features, int n, int d, string? label = null)\n    {\n        Features = features;\n        N = n;\n        D = d;\n        Label = label;\n    }\n}\n\n/// <summary>\n/// Shared contract for all named layer types.\n/// Enables type-safe filtering in SceneBuilder without dynamic dispatch.\n/// </summary>\npublic interface INamedLayer\n{\n    string Name { get; }\n}\n\n/// <summary>\n/// A named assignment of each of the N points to an integer label.\n/// \"True labels\", \"SPC @ T=0.1\", \"GMM component\", \"GMM cluster (via map)\" are\n/// all just LabelLayers with different names and sources.\n/// Negative values are legal: -1 = unassigned / noise.\n/// </summary>\npublic sealed class LabelLayer : INamedLayer\n{\n    public string Name { get; }           // shown in UI / legend\n    public ReadOnlyMemory<int> Labels { get; }  // length N\n    public LabelLayerKind Kind { get; }   // see below\n\n    public LabelLayer(string name, int[] labels, LabelLayerKind kind)\n    {\n        Name = name;\n        Labels = labels;\n        Kind = kind;\n    }\n}\n\npublic enum LabelLayerKind\n{\n    GroundTruth,      // synthetic or annotated\n    SpinColor,        // SW per-sweep colors (diagnostic, not cluster assignments)\n    EquilibriumCluster,  // derived from co-occurrence threshold\n    GmmComponent,     // flat GMM component index (topology-blind)\n    GmmCluster,       // via ComponentToClusterMap (topology-aware)\n    Custom,\n}\n\n/// <summary>\n/// A named per-point scalar annotation: coherence scores, Mahalanobis distances,\n/// percolation arrival sweeps, responsibility[k], log-likelihood, etc.\n/// </summary>\npublic sealed class ScalarLayer : INamedLayer\n{\n    public string Name { get; }\n    public ReadOnlyMemory<double> Values { get; }  // length N\n    public ScalarLayerKind Kind { get; }\n\n    public ScalarLayer(string name, double[] values, ScalarLayerKind kind)\n    {\n        Name = name;\n        Values = values;\n        Kind = kind;\n    }\n}\n\npublic enum ScalarLayerKind\n{\n    CoherenceScore,\n    MahalanobisDistance,\n    PercolationArrival,\n    Responsibility,     // for a specific component k\n    LogLikelihood,\n    Custom,\n}\n\n// ── Metric enum ───────────────────────────────────────────────────────────────\n//\n// VizMetric mirrors SpcMetric (spc.batch.cs) but is owned by the Viz namespace\n// so VizCore.dll has zero dependency on SpcCore.  The two enums can drift:\n// a metric can be added to SpcCore before it's visualizable, which is fine.\n// JsonStringEnumConverter serialises values as strings in JSON.\n\npublic enum VizMetric\n{\n    Euclidean,\n    Manhattan,\n    Minkowski,\n    Cosine,\n    Mahalanobis,\n    Poincare,\n    FisherRao,\n    JensenShannon,\n    Wasserstein,\n    Hamming,\n    Jaccard,\n    Canberra,\n}\n\n// ── Proximity graph specification ─────────────────────────────────────────────\n//\n// ProximitySpec is a sealed record hierarchy that carries the graph construction\n// rule together with its type-specific parameters.  K is a property of the KNN\n// family; Epsilon is a property of the epsilon-ball family.  Neither belongs as\n// a nullable flat field on the base type.\n//\n// New graph types: add a new sealed record subtype — no existing callers change.\n\n/// <summary>\n/// Discriminated union for proximity graph construction rules.\n/// Carry the rule AND its parameters together.\n/// </summary>\npublic abstract record ProximitySpec(NeighborRule Kind);\n\n/// <summary>KNN (OR-symmetrized): edge (i,j) exists if i∈KNN(j) OR j∈KNN(i).</summary>\npublic sealed record KnnSpec(int K) : ProximitySpec(NeighborRule.Knn);\n\n/// <summary>Mutual KNN (AND-rule): edge (i,j) only if i∈KNN(j) AND j∈KNN(i). Sparser, higher-confidence.</summary>\npublic sealed record MutualKnnSpec(int K) : ProximitySpec(NeighborRule.MutualKnn);\n\n/// <summary>Epsilon-ball: edge (i,j) if d(i,j) &lt; Epsilon. Variable degree; no K parameter.</summary>\npublic sealed record EpsilonBallSpec(double Epsilon) : ProximitySpec(NeighborRule.EpsilonBall);\n\n/// <summary>MST-augmented KNN: KNN skeleton reinforced by minimum spanning tree edges to guarantee connectivity.</summary>\npublic sealed record MstAugmentedSpec(int K) : ProximitySpec(NeighborRule.MstAugmented);\n\npublic enum NeighborRule { Knn, MutualKnn, EpsilonBall, MstAugmented }\n\n/// <summary>\n/// A named sparse graph over the N points: k-NN, mutual k-NN, MST edges, etc.\n/// Used to render connectivity and highlight false bridges.\n///\n/// Metric is the distance function used to build the graph (e.g. \"euclidean\",\n/// \"poincare\", \"mahalanobis\"). Stored as a string for forward compatibility —\n/// the viz engine displays it; it does not interpret it.\n///\n/// Proximity carries the graph construction rule and its parameters.\n/// Both are nullable so edge layers built externally or via custom rules\n/// can omit structured metadata.\n/// </summary>\npublic sealed class EdgeLayer : INamedLayer\n{\n    public string Name { get; }\n    public ReadOnlyMemory<int> Src { get; }      // length E\n    public ReadOnlyMemory<int> Dst { get; }      // length E\n    public ReadOnlyMemory<double> Weight { get; } // length E, optional (may be all 1.0)\n\n    /// <summary>\n    /// Name of the distance function used to build this graph.\n    /// Null if the metric is unknown or not applicable (e.g. custom edges).\n    /// </summary>\n    public VizMetric? Metric { get; }\n\n    /// <summary>\n    /// Graph construction rule with its type-specific parameters.\n    /// Null if the rule is unknown or custom.\n    /// </summary>\n    public ProximitySpec? Proximity { get; }\n\n    public ReadOnlyMemory<int>? EdgeClusterSrc { get; }  // GT cluster of src node\n    public ReadOnlyMemory<int>? EdgeClusterDst { get; }  // GT cluster of dst node\n    // IsWithinCluster = EdgeClusterSrc[i] == EdgeClusterDst[i]\n    // renderer colors cross-cluster edges as \"false bridges\" without any extra logic\n\n    public EdgeLayer(\n        string name,\n        int[] src,\n        int[] dst,\n        double[] weight,\n        VizMetric? metric = null,\n        ProximitySpec? proximity = null,\n        int[]? edgeClusterSrc = null,\n        int[]? edgeClusterDst = null)\n    {\n        Name = name;\n        Src = src;\n        Dst = dst;\n        Weight = weight;\n        Metric = metric;\n        Proximity = proximity;\n        EdgeClusterSrc = edgeClusterSrc;\n        EdgeClusterDst = edgeClusterDst;\n    }\n}\n\n/// <summary>\n/// A named sequence of Gaussian ellipsoids: GMM components or recursive-GMM tiling.\n/// ComponentToClusterMap is null for flat GMMs; populated for ManifoldMixture outputs.\n/// </summary>\npublic sealed class GaussianLayer : INamedLayer\n{\n    public string Name { get; }\n    public ReadOnlyMemory<double> Means { get; }        // K x D\n    public ReadOnlyMemory<double> Covariances { get; }  // K x D x D\n    public ReadOnlyMemory<double> Weights { get; }      // K\n    public int K { get; }\n    public int D { get; }\n    public ReadOnlyMemory<int>? ComponentToClusterMap { get; }  // length K, null = flat\n\n    public GaussianLayer(\n        string name,\n        double[] means,\n        double[] covariances,\n        double[] weights,\n        int k,\n        int d,\n        int[]? componentToClusterMap = null)\n    {\n        Name = name;\n        Means = means;\n        Covariances = covariances;\n        Weights = weights;\n        K = k;\n        D = d;\n        ComponentToClusterMap = componentToClusterMap;\n    }\n}\n\n/// <summary>\n/// A named overlay curve or manifold spine, separate from the N-point data cloud.\n/// Carries the clean generating geometry of a synthetic cluster — the arc or manifold\n/// that data points were sampled from — for educational overlay rendering.\n/// Not a property of data points; not indexed into the N-point array.\n/// </summary>\npublic sealed class SpineLayer : INamedLayer\n{\n    public string Name { get; }\n    public int ClusterIdx { get; }           // which cluster this spine belongs to\n    public SpineLayerKind Kind { get; }\n    public double[][] SpineSamples { get; }  // M × D, the clean generating curve\n    public double[][][]? TangentBases { get; } // null for arcs, populated for manifolds\n\n    public SpineLayer(\n        string name,\n        int clusterIdx,\n        SpineLayerKind kind,\n        double[][] spineSamples,\n        double[][][]? tangentBases)\n    {\n        Name = name;\n        ClusterIdx = clusterIdx;\n        Kind = kind;\n        SpineSamples = spineSamples;\n        TangentBases = tangentBases;\n    }\n}\n\npublic enum SpineLayerKind { Arc, Manifold }\n\n/// <summary>\n/// The complete dataset for a single viz scene. Layers are additive and optional.\n/// The scene controller decides which layers to activate and how to render them.\n/// </summary>\npublic sealed class VizDataset\n{\n    public PointCloud Points { get; }\n\n    // All optional. A minimal scene has Points + one LabelLayer.\n    public IReadOnlyList<LabelLayer> LabelLayers { get; }\n    public IReadOnlyList<ScalarLayer> ScalarLayers { get; }\n    public IReadOnlyList<EdgeLayer> EdgeLayers { get; }\n    public IReadOnlyList<GaussianLayer> GaussianLayers { get; }\n\n    // Sequence layers: indexed by a score axis (temperature, EM iteration, depth).\n    // The IPartitionSequence seam from project-primer.md lives here when Clustering lands.\n    // For now, just a named list of label snapshots ordered by their index.\n    public IReadOnlyList<TemporalLabelSequence> TemporalSequences { get; }\n\n    // Overlay spines: clean generating curves/manifolds for synthetic clusters.\n    public IReadOnlyList<SpineLayer> SpineLayers { get; }\n\n    // Generator metadata: key/value params used to produce this dataset.\n    // Non-null for synthetic datasets; null for all other sources.\n    // Serialized to SCENE.generator_params so the viewer can offer a live regen panel.\n    public IReadOnlyDictionary<string, object>? GeneratorParams { get; }\n\n    public VizDataset(\n        PointCloud points,\n        IReadOnlyList<LabelLayer> labelLayers,\n        IReadOnlyList<ScalarLayer> scalarLayers,\n        IReadOnlyList<EdgeLayer> edgeLayers,\n        IReadOnlyList<GaussianLayer> gaussianLayers,\n        IReadOnlyList<TemporalLabelSequence> temporalSequences,\n        IReadOnlyList<SpineLayer> spineLayers,\n        IReadOnlyDictionary<string, object>? generatorParams = null)\n    {\n        Points = points;\n        LabelLayers = labelLayers;\n        ScalarLayers = scalarLayers;\n        EdgeLayers = edgeLayers;\n        GaussianLayers = gaussianLayers;\n        TemporalSequences = temporalSequences;\n        SpineLayers = spineLayers;\n        GeneratorParams = generatorParams;\n    }\n}\n\n/// <summary>\n/// Ordered snapshots of LabelLayers along a progression axis.\n/// Examples: SPC temperature sweep (T-indexed), GMM EM iterations (iter-indexed),\n/// recursive GMM depth sweep (depth-indexed).\n/// </summary>\npublic sealed class TemporalLabelSequence\n{\n    public string Name { get; }\n    public TemporalAxis Axis { get; }         // Temperature, Iteration, Depth, Custom\n    public ReadOnlyMemory<double> IndexValues { get; }  // the T/iter/depth at each frame\n    public IReadOnlyList<LabelLayer> Frames { get; }    // one per index value\n\n    public TemporalLabelSequence(\n        string name,\n        TemporalAxis axis,\n        double[] indexValues,\n        IReadOnlyList<LabelLayer> frames)\n    {\n        Name = name;\n        Axis = axis;\n        IndexValues = indexValues;\n        Frames = frames;\n    }\n}\n\npublic enum TemporalAxis { Temperature, Iteration, Depth, Custom } |
