idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
17 | clustering/spc/diagnostics/Susceptibility.cs | {1539 143 0.2762 4.5940} | 1581 | using System.Collections.Generic;\nusing Clustering.SPC.Potts;\n\nnamespace Clustering.SPC.Diagnostics;\n\npublic readonly record struct SusceptibilityCurve(\n    double[] Temperatures,\n    double[] Chi);\n\npublic static class Susceptibility\n{\n    public static SusceptibilityCurve From(IReadOnlyList<PottsModelStepResult> frames)\n    {\n        if (frames.Count == 0)\n            return new SusceptibilityCurve(System.Array.Empty<double>(), System.Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double SumChi, int Count)>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelStepResult frame = frames[i];\n            double chi = frame.CycleCount > 0 && frame.Spins.Length > 0\n                ? frame.RunningSumSqClusterSizes / (frame.CycleCount * (double)frame.Spins.Length)\n                : 0.0;\n\n            if (grouped.TryGetValue(frame.Temperature, out (double SumChi, int Count) acc))\n                grouped[frame.Temperature] = (acc.SumChi + chi, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (chi, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var chiValues = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, (double sumChi, int count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            chiValues[index] = count > 0 ? sumChi / count : 0.0;\n            index++;\n        }\n\n        return new SusceptibilityCurve(temperatures, chiValues);\n    }\n} |
18 | clustering/spc/heuristics/CriticalTemperatureEstimator.cs | {1191 121 0.2603 4.6972} | 1229 | using System;\nusing Graphs.Diagnostics;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Heuristics;\n\npublic static class CriticalTemperatureEstimator\n{\n    public static double Estimate(CsrGraph graph, int q)\n    {\n        if (graph.NodeCount == 0 || graph.Targets.Length == 0 || q < 2)\n            return 1.0;\n\n        EdgeWeightSummary weights = EdgeWeights.Summary(graph);\n        double meanWeightedDegree = 2.0 * weights.MeanWeight * weights.EdgeCount / graph.NodeCount;\n        double logQ = Math.Log(q);\n        if (logQ <= 0.0 || weights.MedianWeight <= 0.0)\n            return 1.0;\n\n        return weights.MedianWeight * meanWeightedDegree / logQ;\n    }\n\n    public static (double TempMin, double TempMax) EstimateBracket(\n        CsrGraph graph,\n        int q,\n        double coldOvershoot = 0.05,\n        double hotOvershoot = 5.0)\n    {\n        if (coldOvershoot <= 0.0)\n            throw new ArgumentOutOfRangeException(nameof(coldOvershoot));\n        if (hotOvershoot <= coldOvershoot)\n            throw new ArgumentException(\"hotOvershoot must exceed coldOvershoot.\");\n\n        double tc = Estimate(graph, q);\n        return (tc * coldOvershoot, tc * hotOvershoot);\n    }\n} |
19 | clustering/spc/IPottsConfig.cs | {3928 512 0.1683 4.8484} | 4061 | namespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Compile-time configuration for <see cref=\"PottsRunner{TConfig}\"/>. Members are\n/// <c>static abstract</c> so the JIT can fold them as compile-time constants per\n/// specialization and DCE branches whose guard evaluates to <c>false</c>.\n/// </summary>\n/// <remarks>\n/// Adding a new accumulator dimension here doubles the number of specializations,\n/// so this pattern is reserved for accumulators that are (a) selected once at\n/// construction, (b) cheap individually, and (c) genuinely worth removing from\n/// the hot path's IL when disabled. Heavier or runtime-toggleable accumulators\n/// belong behind a non-generic bool gate, not here.\n/// </remarks>\npublic interface IPottsConfig\n{\n    /// <summary>\n    /// When <c>false</c>, the Blatt 1996 magnetization accumulator and its\n    /// supporting <c>MaxColorBucket</c> pass are eliminated from the JITed code\n    /// for the specialization, and the per-color bucket scratch buffer is not\n    /// allocated by the constructor.\n    /// </summary>\n    static abstract bool BlattMagnetization { get; }\n\n    /// <summary>\n    /// When <c>false</c>, the per-edge <c>⟨ω(e)⟩</c> bond-frequency and\n    /// <c>⟨1_{s_i = s_j}⟩</c> spin-agreement accumulators (and their <c>int[]</c>\n    /// scratch arrays sized to CSR length) are absent from the specialization.\n    /// When <c>true</c>, both are tracked together — they share a hot-loop\n    /// branch and are conceptually paired (the second is the marginal of the\n    /// first over the bond-formation random draw).\n    /// </summary>\n    static abstract bool EdgeObservables { get; }\n}\n\n/// <summary>\n/// FK (Fortuin-Kasteleyn) susceptibility estimator only. The canonical modern\n/// default — lowest variance per MC sample, immune to color-bucket bias, and\n/// effectively free as a byproduct of the SW union-find pass.\n/// </summary>\npublic readonly struct FkOnly : IPottsConfig\n{\n    public static bool BlattMagnetization => false;\n    public static bool EdgeObservables    => false;\n}\n\n/// <summary>\n/// FK estimator plus Blatt 1996 magnetization-based susceptibility (eq. 3 of the\n/// original SPC paper). Use for paper-faithful reproductions, cross-validation\n/// against FK, low-T equilibration diagnostics (χ_B → 0 is cleaner than the FK\n/// plateau), or Q-dependence studies.\n/// </summary>\npublic readonly struct FkAndBlatt : IPottsConfig\n{\n    public static bool BlattMagnetization => true;\n    public static bool EdgeObservables    => false;\n}\n\n/// <summary>\n/// FK estimator plus per-edge <c>⟨ω(e)⟩</c> and <c>⟨1_{s_i = s_j}⟩</c>\n/// observables. Use for percolation diagnostics, friends-of-friends cut policies\n/// (which need spin-agreement at neighbor pairs), and any analysis that needs\n/// edge-resolved equilibrium correlations.\n/// </summary>\npublic readonly struct FkAndEdges : IPottsConfig\n{\n    public static bool BlattMagnetization => false;\n    public static bool EdgeObservables    => true;\n}\n\n/// <summary>\n/// FK estimator plus Blatt magnetization plus per-edge observables. The\n/// \"everything on\" variant — useful for cross-validation across estimator\n/// families on the same sample.\n/// </summary>\npublic readonly struct FkAndBlattAndEdges : IPottsConfig\n{\n    public static bool BlattMagnetization => true;\n    public static bool EdgeObservables    => true;\n}\n\n/// <summary>\n/// Public enum selecting which <see cref=\"IPottsConfig\"/> specialization to\n/// instantiate from <see cref=\"PottsModel\"/>'s constructor.\n/// </summary>\npublic enum PottsSusceptibility\n{\n    /// <summary>Fortuin-Kasteleyn estimator only. Canonical default.</summary>\n    FkOnly,\n\n    /// <summary>FK plus Blatt 1996 magnetization-based estimator.</summary>\n    FkAndBlatt,\n\n    /// <summary>FK plus per-edge bond frequency and spin agreement.</summary>\n    FkAndEdges,\n\n    /// <summary>FK + Blatt + per-edge observables. Everything on.</summary>\n    FkAndBlattAndEdges,\n} |
20 | clustering/spc/IPottsRunner.cs | {1525 196 0.1928 4.7825} | 1577 | using System;\n\nnamespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Type-erased contract over <see cref=\"PottsRunner{TConfig}\"/>. The public\n/// <see cref=\"PottsModel\"/> façade holds an <see cref=\"IPottsRunner\"/> so that\n/// the susceptibility-config choice (made once at construction) does not leak\n/// into the public API surface. All members forward to the underlying generic\n/// runner via virtual dispatch; this dispatch is one indirection per public\n/// call, not per SWCycle, so it is paid at the orchestration boundary, not in\n/// the hot path.\n/// </summary>\ninternal interface IPottsRunner\n{\n    void Cycle();\n    void Run(int cycleCount);\n    void BurnIn(int cycles);\n\n    int CycleCount { get; }\n    double Temperature { get; }\n    int Q { get; }\n    int N { get; }\n\n    ReadOnlySpan<int> Spins { get; }\n    ReadOnlySpan<int> ClusterSizeHistogram { get; }\n\n    double RunningSumSqClusterSizes { get; }\n    double RunningSumSqClusterSizesExcl { get; }\n    double RunningSumEnergy { get; }\n    double RunningSumEnergySq { get; }\n    double RunningSumMag { get; }\n    double RunningSumMagSq { get; }\n\n    PottsModelStepResult GetCheckpoint();\n    void Restore(PottsModelStepResult result);\n\n    /// <summary>\n    /// Snapshot the per-edge accumulators (bond formation count, spin agreement\n    /// count). Returns <see langword=\"null\"/> when the specialization does not\n    /// track edge observables; consumers should branch on that, not throw.\n    /// </summary>\n    PottsModelEdgeObservables? GetEdgeCheckpoint();\n} |
21 | clustering/spc/PottsModel.cs | {8035 945 0.1890 4.8553} | 8288 | using System;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Swendsen-Wang sampler for the q-state Potts model on a weighted CSR graph\n/// at a single fixed temperature. The SPC core MCMC primitive; consumed by the\n/// orchestration layer ('schedule of Steps across temperatures'), which the\n/// sampler itself knows nothing about.\n/// </summary>\n/// <remarks>\n/// <para><b>Color semantics.</b> <see cref=\"Spins\"/> are Potts colors in\n/// <c>[0, Q)</c>, not cluster identifiers. Two nodes with the same color may\n/// or may not belong to the same bond cluster — the cluster structure built\n/// by each SWCycle's union-find pass is the ground truth, and what the FK\n/// accumulator integrates over. The Blatt estimator reads color buckets out\n/// of <see cref=\"Spins\"/> directly and so is affected by coincidental color\n/// collisions, which matters when the number of distinct bond clusters\n/// approaches or exceeds <see cref=\"Q\"/>.</para>\n///\n/// <para><b>Thread safety.</b> A single <see cref=\"PottsModel\"/> is not\n/// thread-safe. The schedule scheduler parallelizes across temperatures by\n/// constructing one <see cref=\"PottsModel\"/> per task (per thread).</para>\n///\n/// <para><b>Terminology.</b> A <i>SWCycle</i> is one call to <see cref=\"Cycle\"/>:\n/// bond formation, cluster flip, accumulator update. A <i>Step</i> (added in\n/// Phase 2) is a fixed number of SWCycles at one temperature, the checkpointable\n/// unit. A <i>Schedule</i> is a sequence of Steps across temperatures, owned by\n/// the scheduler.</para>\n/// </remarks>\npublic sealed class PottsModel\n{\n    private readonly IPottsRunner _runner;\n\n    /// <param name=\"graph\">Weighted CSR graph. Weights are interpreted as\n    /// Potts couplings J_ij; bond probabilities are 1 - exp(-J_ij / T).</param>\n    /// <param name=\"temperature\">Strictly positive temperature in units where\n    /// J is dimensionless (i.e., T carries the same units as J).</param>\n    /// <param name=\"q\">Number of Potts states (colors). Must be at least 2.</param>\n    /// <param name=\"susceptibility\">Which susceptibility accumulator(s) to\n    /// instantiate. <see cref=\"PottsSusceptibility.FkOnly\"/> is the canonical\n    /// default; <see cref=\"PottsSusceptibility.FkAndBlatt\"/> additionally\n    /// accumulates the Blatt 1996 magnetization-based estimator for\n    /// cross-validation or paper-faithful reproduction.</param>\n    /// <param name=\"seed\">Optional seed. <c>null</c> draws from the OS entropy\n    /// source. For reproducibility across runs (and replica generation in the\n    /// scheduler), pass a deterministic seed.</param>\n    public PottsModel(\n        CsrGraph graph,\n        double temperature,\n        int q,\n        PottsSusceptibility susceptibility = PottsSusceptibility.FkOnly,\n        int? seed = null)\n    {\n        _runner = susceptibility switch\n        {\n            PottsSusceptibility.FkOnly             => new PottsRunner<FkOnly>            (graph, temperature, q, seed),\n            PottsSusceptibility.FkAndBlatt         => new PottsRunner<FkAndBlatt>        (graph, temperature, q, seed),\n            PottsSusceptibility.FkAndEdges         => new PottsRunner<FkAndEdges>        (graph, temperature, q, seed),\n            PottsSusceptibility.FkAndBlattAndEdges => new PottsRunner<FkAndBlattAndEdges>(graph, temperature, q, seed),\n            _ => throw new ArgumentOutOfRangeException(nameof(susceptibility)),\n        };\n    }\n\n    /// <summary>One Swendsen-Wang cycle (the hot-path atomic unit).</summary>\n    public void Cycle() => _runner.Cycle();\n\n    /// <summary>Run <paramref name=\"cycleCount\"/> SWCycles in sequence.</summary>\n    public void Run(int cycleCount) => _runner.Run(cycleCount);\n\n    /// <summary>\n    /// Run <paramref name=\"cycles\"/> SWCycles to equilibrate the model away\n    /// from its random initial spin configuration, then reset all running\n    /// accumulators (cluster-size sums, energy sums, edge counts,\n    /// histograms) and the cycle counter to zero. Subsequent\n    /// <see cref=\"Run\"/>/<see cref=\"Cycle\"/> calls accumulate from a clean\n    /// slate — discarding the out-of-equilibrium transient that would\n    /// otherwise bias the running averages, especially at low temperatures\n    /// where the disordered head is far from the Boltzmann distribution.\n    /// </summary>\n    /// <remarks>\n    /// RNG state advances during burn-in and is NOT reset. The discarded\n    /// quantity is statistical information, not pseudorandomness.\n    /// </remarks>\n    public void BurnIn(int cycles) => _runner.BurnIn(cycles);\n\n    /// <summary>\n    /// Capture a full state snapshot — spins, RNG, accumulators, cycle count,\n    /// identifying constants — as a <see cref=\"PottsModelStepResult\"/> the\n    /// scheduler can serialize. The returned record is decoupled from this\n    /// model: subsequent <see cref=\"Cycle\"/> calls do not mutate it.\n    /// </summary>\n    public PottsModelStepResult GetCheckpoint() => _runner.GetCheckpoint();\n\n    /// <summary>\n    /// Capture the per-edge accumulator snapshot (bond frequency,\n    /// spin agreement) as a sidecar DTO. Returns <see langword=\"null\"/> when\n    /// the model was constructed with a susceptibility variant that does\n    /// not track edge observables.\n    /// </summary>\n    public PottsModelEdgeObservables? GetEdgeCheckpoint() => _runner.GetEdgeCheckpoint();\n\n    /// <summary>\n    /// Overwrite this model's mutable state from a previous snapshot.\n    /// Throws <see cref=\"InvalidOperationException\"/> if the snapshot's\n    /// identifying constants (temperature, Q, susceptibility variant,\n    /// node count) do not match this model — resuming a mismatched\n    /// snapshot would silently produce wrong results against a stale\n    /// bond table.\n    /// </summary>\n    public void Restore(PottsModelStepResult result) => _runner.Restore(result);\n\n    public int CycleCount => _runner.CycleCount;\n    public double Temperature => _runner.Temperature;\n    public int Q => _runner.Q;\n    public int N => _runner.N;\n\n    /// <summary>Current Potts colors of each node, in <c>[0, Q)</c>.</summary>\n    public ReadOnlySpan<int> Spins => _runner.Spins;\n\n    /// <summary>\n    /// Running sum of <c>Σ_c |c|²</c> over BOND CLUSTERS, summed across all\n    /// SWCycles. The Fortuin-Kasteleyn accumulator. The canonical estimator\n    /// is <c>χ_FK(T) = RunningSumSqClusterSizes / (CycleCount · N)</c>.\n    /// </summary>\n    public double RunningSumSqClusterSizes => _runner.RunningSumSqClusterSizes;\n\n    /// <summary>\n    /// Running sum of <c>Σ_c |c|² − max_c|c|²</c> — the FK second-moment with\n    /// the largest bond cluster removed. Used as a percolation diagnostic:\n    /// in the percolating phase the largest cluster dominates <c>χ_FK</c> and\n    /// hides substructure, so <c>χ_excl(T) = RunningSumSqClusterSizesExcl /\n    /// (CycleCount · N)</c> peaks where the *non-percolating* clusters are\n    /// most heterogeneous.\n    /// </summary>\n    public double RunningSumSqClusterSizesExcl => _runner.RunningSumSqClusterSizesExcl;\n\n    /// <summary>Running sum of <c>⟨H⟩</c> over SWCycles, where\n    /// <c>H = -Σ_⟨i,j⟩ J_ij δ(s_i, s_j)</c>.</summary>\n    public double RunningSumEnergy => _runner.RunningSumEnergy;\n\n    /// <summary>Running sum of <c>⟨H²⟩</c>. Specific heat is\n    /// <c>C(T) = β² · (⟨H²⟩ - ⟨H⟩²)</c>.</summary>\n    public double RunningSumEnergySq => _runner.RunningSumEnergySq;\n\n    /// <summary>Running sum of <c>m = (N_max·q/N - 1)/(q-1)</c>. Zero unless the\n    /// Blatt accumulator is enabled at construction.</summary>\n    public double RunningSumMag => _runner.RunningSumMag;\n\n    /// <summary>Running sum of <c>m²</c>. Zero unless the Blatt accumulator is\n    /// enabled.</summary>\n    public double RunningSumMagSq => _runner.RunningSumMagSq;\n\n    /// <summary>Log-binned cluster size histogram. Bin <c>k</c> counts SWCycles'\n    /// worth of bond clusters whose size is in <c>[2^k, 2^(k+1))</c>.</summary>\n    public ReadOnlySpan<int> ClusterSizeHistogram => _runner.ClusterSizeHistogram;\n} |
22 | clustering/spc/PottsModelEdgeObservables.cs | {2234 295 0.1670 4.9560} | 2374 | using System;\n\nnamespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Sidecar snapshot of <see cref=\"PottsModel\"/>'s per-edge accumulators —\n/// bond-formation counts and spin-agreement counts indexed by CSR entry.\n/// Lives in its own DTO and its own on-disk file (<c>.spce</c>) alongside\n/// the main <see cref=\"PottsModelStepResult\"/> (<c>.spcx</c>) so consumers\n/// that don't care about per-edge data can skip the I/O cost.\n/// </summary>\n/// <remarks>\n/// <para><b>Pairing keys.</b> <see cref=\"Temperature\"/> and\n/// <see cref=\"ReplicaIndex\"/> match the corresponding fields in the main\n/// .spcx; <see cref=\"CycleCount\"/> must also match (the edge accumulators\n/// advance lockstep with the cycle counter).</para>\n///\n/// <para><b>Estimators.</b>\n/// <code>\n///   ⟨ω(e)⟩          = BondFormedCount[e]    / CycleCount  — equilibrium bond frequency\n///   ⟨1_{s_i=s_j}⟩   = SpinAgreementCount[e] / CycleCount  — equilibrium spin alignment\n/// </code>\n/// In the joint FK-Potts representation,\n/// <c>⟨ω(e)⟩ = p_e · ⟨1_{s_i=s_j}⟩</c> where <c>p_e = 1 − exp(−J_e/T)</c>.\n/// Bond frequency is what FK theory and percolation diagnostics consume;\n/// spin agreement is what Blatt-style friends-of-friends cut policies key\n/// off of.</para>\n///\n/// <para><b>Indexing.</b> Array indices are CSR entry positions — same\n/// indexing as <see cref=\"Graphs.Primitives.CsrGraph.Targets\"/> and\n/// <see cref=\"Graphs.Primitives.CsrGraph.Weights\"/>. The symmetric CSR\n/// stores each undirected edge twice; the SW inner loop skips\n/// <c>j ≤ i</c>, so only the upper-triangular CSR slots are ever\n/// incremented (the lower-triangular mirror entries stay at zero).</para>\n/// </remarks>\npublic sealed record PottsModelEdgeObservables\n{\n    // ── Pairing keys (must match the corresponding .spcx fields) ──────────\n    public required double Temperature  { get; init; }\n    public          int    ReplicaIndex { get; init; }\n    public required int    CycleCount   { get; init; }\n\n    // ── Per-edge accumulator arrays, sized to CSR.Targets.Length ─────────\n    public required int[]  BondFormedCount    { get; init; }\n    public required int[]  SpinAgreementCount { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n} |
23 | clustering/spc/PottsModelEdgeObservablesIO.cs | {3583 361 0.2400 4.9022} | 3703 | using System;\nusing System.IO;\nusing System.Text;\n\nnamespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Custom binary serializer for <see cref=\"PottsModelEdgeObservables\"/> —\n/// the sidecar SPCE format (SPC Edges) carrying per-edge accumulator\n/// arrays alongside the main SPCX state checkpoint.\n/// </summary>\n/// <remarks>\n/// <para><b>Format (little-endian):</b></para>\n/// <code>\n/// [Magic: uint32 'SPCE']\n/// [Version: int32]\n/// [Temperature: double]\n/// [ReplicaIndex: int32]\n/// [CycleCount: int32]\n/// [EdgeCount: int32]\n/// [BondFormedCount: EdgeCount × int32]\n/// [SpinAgreementCount: EdgeCount × int32]\n/// [TimestampUtcTicks: int64]\n/// </code>\n///\n/// <para>Sidecar pairing: an SPCE file shares its <see cref=\"PottsModelEdgeObservables.Temperature\"/>,\n/// <see cref=\"PottsModelEdgeObservables.ReplicaIndex\"/>, and\n/// <see cref=\"PottsModelEdgeObservables.CycleCount\"/> with the matching\n/// SPCX file at the same base path; analyzers should validate all three\n/// match before joining the records.</para>\n/// </remarks>\npublic static class PottsModelEdgeObservablesIO\n{\n    // \"SPCE\" little-endian: 'S'=0x53, 'P'=0x50, 'C'=0x43, 'E'=0x45\n    private const uint Magic = 0x45435053;\n    private const int  Version = 1;\n\n    public static void WriteTo(PottsModelEdgeObservables edges, Stream stream)\n    {\n        using var w = new BinaryWriter(stream, Encoding.UTF8, leaveOpen: true);\n        w.Write(Magic);\n        w.Write(Version);\n        w.Write(edges.Temperature);\n        w.Write(edges.ReplicaIndex);\n        w.Write(edges.CycleCount);\n\n        w.Write(edges.BondFormedCount.Length);\n        foreach (int x in edges.BondFormedCount)    w.Write(x);\n        foreach (int x in edges.SpinAgreementCount) w.Write(x);\n\n        w.Write(edges.TimestampUtc.Ticks);\n    }\n\n    public static PottsModelEdgeObservables ReadFrom(Stream stream)\n    {\n        using var r = new BinaryReader(stream, Encoding.UTF8, leaveOpen: true);\n\n        uint magic = r.ReadUInt32();\n        if (magic != Magic)\n            throw new InvalidDataException(\n                $\"Not an SPCE checkpoint (magic 0x{magic:X8}, expected 0x{Magic:X8}).\");\n        int version = r.ReadInt32();\n        if (version != Version)\n            throw new InvalidDataException(\n                $\"Unsupported SPCE version {version} (this reader handles version {Version}).\");\n\n        double temperature  = r.ReadDouble();\n        int    replicaIndex = r.ReadInt32();\n        int    cycleCount   = r.ReadInt32();\n\n        int edgeCount = r.ReadInt32();\n        var bond = new int[edgeCount];\n        for (int i = 0; i < edgeCount; i++) bond[i] = r.ReadInt32();\n        var spin = new int[edgeCount];\n        for (int i = 0; i < edgeCount; i++) spin[i] = r.ReadInt32();\n\n        long ticks = r.ReadInt64();\n\n        return new PottsModelEdgeObservables\n        {\n            Temperature        = temperature,\n            ReplicaIndex       = replicaIndex,\n            CycleCount         = cycleCount,\n            BondFormedCount    = bond,\n            SpinAgreementCount = spin,\n            TimestampUtc       = new DateTime(ticks, DateTimeKind.Utc),\n        };\n    }\n\n    /// <summary>Convenience: write to a file path.</summary>\n    public static void WriteToFile(PottsModelEdgeObservables edges, string path)\n    {\n        using var fs = File.Create(path);\n        WriteTo(edges, fs);\n    }\n\n    /// <summary>Convenience: read from a file path.</summary>\n    public static PottsModelEdgeObservables ReadFromFile(string path)\n    {\n        using var fs = File.OpenRead(path);\n        return ReadFrom(fs);\n    }\n} |
24 | clustering/spc/PottsModelStepResult.cs | {3169 387 0.1852 4.7873} | 3824 | using System;\n\nnamespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Atomic snapshot of a <see cref=\"PottsModel\"/> at a cycle boundary, carrying\n/// everything needed for bit-identical resume: the full spin configuration,\n/// the raw four-word xoshiro256++ state, every running accumulator, and the\n/// identifying constants (<see cref=\"Temperature\"/>, <see cref=\"Q\"/>,\n/// <see cref=\"Susceptibility\"/>) that let <see cref=\"PottsModel.Restore\"/>\n/// reject snapshot/model mismatches before contaminating state.\n/// </summary>\n/// <remarks>\n/// <para><b>Atomic unit.</b> One snapshot corresponds to one <i>Step</i> in\n/// scheduler vocabulary — a checkpointable boundary between sequences of\n/// SWCycles. The flat-list scheduler writes one DTO per\n/// <c>(Temperature, ReplicaIndex)</c> task.</para>\n///\n/// <para><b>Pure data.</b> No serialization attributes are baked in — the\n/// choice of binary format (MessagePack, JSON, custom binary, ...) is the\n/// scheduler's, not this DTO's. The DTO compiles against stdlib only so\n/// that swapping serializers does not require changing the data shape.</para>\n///\n/// <para><b>Replica index.</b> <see cref=\"ReplicaIndex\"/> defaults to 0 because\n/// <see cref=\"PottsModel\"/> has no notion of replicas. The scheduler sets it\n/// via <c>checkpoint with { ReplicaIndex = task.ReplicaIndex }</c> after\n/// <see cref=\"PottsModel.GetCheckpoint\"/> returns.</para>\n/// </remarks>\npublic sealed record PottsModelStepResult\n{\n    // ── Identifying constants (validated on Restore) ──────────────────────\n    public required double              Temperature    { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsSusceptibility Susceptibility { get; init; }\n\n    // ── Scheduler-supplied; GetCheckpoint returns 0 ───────────────────────\n    public int ReplicaIndex { get; init; }\n\n    // ── Cycle position ────────────────────────────────────────────────────\n    public required int CycleCount { get; init; }\n\n    // ── Configuration + accumulator state ─────────────────────────────────\n    public required int[] Spins                { get; init; }\n    public required int[] ClusterSizeHistogram { get; init; }\n\n    // ── RNG state — four words of xoshiro256++ ────────────────────────────\n    public required ulong RngState0 { get; init; }\n    public required ulong RngState1 { get; init; }\n    public required ulong RngState2 { get; init; }\n    public required ulong RngState3 { get; init; }\n\n    // ── Always-on accumulators ────────────────────────────────────────────\n    public required double RunningSumSqClusterSizes     { get; init; }\n    public required double RunningSumSqClusterSizesExcl { get; init; }\n    public required double RunningSumEnergy             { get; init; }\n    public required double RunningSumEnergySq           { get; init; }\n\n    // ── Blatt accumulators — zero when Susceptibility == FkOnly ──────────\n    public double RunningSumMag   { get; init; }\n    public double RunningSumMagSq { get; init; }\n\n    // ── Provenance ────────────────────────────────────────────────────────\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n} |
