idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | estimators/DeltaMuMean.cs | {802 84 0.3416 4.3206} | 829 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Arithmetic mean estimator for scalar distance samples.\n    /// </summary>\n    public static class DeltaMean\n    {\n        public static double Estimate(ReadOnlySpan<double> distances, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n\n            double sum = 0;\n            for (int i = 0; i < distances.Length; i++)\n            {\n                double value = distances[i];\n                if (double.IsNaN(value) || double.IsInfinity(value) || value < 0)\n                    throw new ArgumentOutOfRangeException(nameof(distances), \"Distances must be finite and non-negative.\");\n                sum += value;\n            }\n\n            return sum / distances.Length;\n        }\n    }\n} |
1 | estimators/DeltaMuMedian.cs | {1704 161 0.3298 4.3798} | 1754 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Median estimator for scalar distance samples.\n    /// </summary>\n    public static class DeltaMedian\n    {\n        public static double Estimate(ReadOnlySpan<double> distances, Span<double> scratch, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n            if (scratch.Length < distances.Length)\n                throw new ArgumentException(\"Scratch span is shorter than the distance sample.\", nameof(scratch));\n\n            for (int i = 0; i < distances.Length; i++)\n            {\n                double value = distances[i];\n                if (double.IsNaN(value) || double.IsInfinity(value) || value < 0)\n                    throw new ArgumentOutOfRangeException(nameof(distances), \"Distances must be finite and non-negative.\");\n                scratch[i] = value;\n            }\n\n            Span<double> sorted = scratch.Slice(0, distances.Length);\n            sorted.Sort();\n            return MedianOfSorted(sorted);\n        }\n\n        public static double Estimate(double[] distances, double fallback = 1.0)\n        {\n            if (distances == null)\n                throw new ArgumentNullException(nameof(distances));\n            if (distances.Length == 0) return fallback;\n\n            double[] scratch = (double[])distances.Clone();\n            return Estimate(distances, scratch, fallback);\n        }\n\n        private static double MedianOfSorted(ReadOnlySpan<double> sorted)\n        {\n            int mid = sorted.Length / 2;\n            return sorted.Length % 2 == 0\n                ? (sorted[mid - 1] + sorted[mid]) / 2.0\n                : sorted[mid];\n        }\n    }\n} |
2 | estimators/ManifoldMedian.cs | {15713 1503 0.3589 4.3666} | 16116 | using System;\nusing System.Buffers;\nusing System.Numerics.Tensors;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Represents a Riemannian manifold providing necessary geometric primitives.\n    /// Uses 'in' modifiers to avoid copying large structs (e.g., matrices).\n    /// </summary>\n    /// <remarks>\n    /// Currently lives in <c>StatisticalEstimators</c> because <see cref=\"ProductManifoldMedian\"/>\n    /// is the only consumer. Extract to a dedicated <c>Geometry</c> or <c>Manifolds</c> project\n    /// once a second consumer appears (e.g. Karcher/Fréchet mean, SPD-matrix estimators,\n    /// or hyperbolic embedding support).\n    /// REFERENCE: Geometric Medians on Product Manifolds, Jiewon Park[^1] & Kisung You[^2], 1^Department of Mathematical Sciences KAIST, 2^Department of Mathematics, Baruch University (2026)\n    ///     DOI:https://doi.org/10.48550/arXiv.2505.18844\n    /// </remarks>\n    public interface IRiemannianManifold<TPoint, TTangent>\n    {\n        double Distance(in TPoint p, in TPoint q);\n\n        TTangent LogMap(in TPoint p, in TPoint q);\n\n        TPoint ExpMap(in TPoint p, in TTangent v);\n\n        TTangent Scale(in TTangent v, double scalar);\n\n        TTangent Add(in TTangent a, in TTangent b);\n\n        // Returns the additive identity for the tangent space at point p.\n        TTangent ZeroTangent(in TPoint p);\n\n        // Returns the Riemannian norm of tangent vector v at base point p.\n        // Derived from the inner product: ||v||_p = sqrt(<v, v>_p).\n        double Norm(in TPoint p, in TTangent v);\n    }\n\n    /// <summary>\n    /// Computes the Fréchet mean (Riemannian centre of mass) on a product manifold M × N\n    /// via Karcher flow: iteratively maps to the tangent space, forms the weighted mean,\n    /// and retracts back. Minimises the sum of <em>squared</em> distances (L² objective).\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// <strong>Parametric vs non-parametric framing.</strong>\n    /// The Fréchet mean is the Riemannian analogue of the sample mean: it is the\n    /// maximum-likelihood estimator under isotropic Gaussian noise on the manifold.\n    /// <see cref=\"ProductManifoldMedian\"/> minimises the L¹ objective and is the analogue\n    /// of the sample median — no distributional assumption is required and the breakdown\n    /// point is 50 %. Prefer the Fréchet mean when the data are well-concentrated and\n    /// noise is approximately Gaussian; prefer the geometric median when outlier clusters\n    /// or heavy tails are expected (the typical SPC regime).\n    /// </para>\n    /// <para>\n    /// <strong>Warm-start use.</strong>\n    /// Because Karcher flow is cheaper per iteration (no per-point distance denominator,\n    /// no singularity handling), a common pattern is to run <c>Compute</c> first and\n    /// pass its result as <c>initialP</c>/<c>initialQ</c> to\n    /// <see cref=\"ProductManifoldMedian.ComputeWeiszfeld\"/>, giving the median solver a\n    /// geometrically sensible starting point and reducing the number of Weiszfeld iterations.\n    /// </para>\n    /// <para>\n    /// <strong>Decoupling.</strong>\n    /// The L² objective splits additively over M and N, so the Karcher updates on each\n    /// factor are independent. The geometric median does not decouple because\n    /// <c>sqrt(dM² + dN²)</c> couples the two factors through the shared denominator.\n    /// </para>\n    /// <para>\n    /// No per-iteration scratch allocation is required: the gradient\n    /// <c>−2 Σ w̃ᵢ log_p(xᵢ)</c> is smooth everywhere, so there are no singularity cases.\n    /// </para>\n    /// </remarks>\n    public static class ProductManifoldFrechetMean\n    {\n        /// <summary>\n        /// Computes the weighted Fréchet mean using Karcher flow (gradient descent on the\n        /// squared-distance sum). Converges locally at a linear rate when the data are\n        /// contained in a geodesically convex neighbourhood.\n        /// </summary>\n        public static (TPointM p, TPointN q) Compute<TPointM, TTangentM, TPointN, TTangentN>(\n            IRiemannianManifold<TPointM, TTangentM> manifoldM,\n            IRiemannianManifold<TPointN, TTangentN> manifoldN,\n            ReadOnlySpan<TPointM> dataM,\n            ReadOnlySpan<TPointN> dataN,\n            ReadOnlySpan<double> weights,\n            in TPointM initialP,\n            in TPointN initialQ,\n            int maxIterations = 100,\n            double tolerance = 1e-6)\n        {\n            if (dataM.Length != dataN.Length || dataM.Length != weights.Length)\n            {\n                throw new ArgumentException(\"Data spans and weights must have identical lengths.\");\n            }\n\n            int n = dataM.Length;\n            TPointM pCurrent = initialP;\n            TPointN qCurrent = initialQ;\n\n            // Normalise weights once so the inner loop is a simple weighted sum.\n            double weightSum = 0.0;\n            for (int i = 0; i < n; i++) weightSum += weights[i];\n            double invWeightSum = weightSum > 0.0 ? 1.0 / weightSum : 1.0;\n\n            for (int iter = 0; iter < maxIterations; iter++)\n            {\n                TTangentM tangentSumM = manifoldM.ZeroTangent(pCurrent);\n                TTangentN tangentSumN = manifoldN.ZeroTangent(qCurrent);\n\n                // Gradient: −2 Σ w̃ᵢ log_p(xᵢ); step = −½ gradient = Σ w̃ᵢ log_p(xᵢ).\n                for (int i = 0; i < n; i++)\n                {\n                    double w = weights[i] * invWeightSum;\n\n                    tangentSumM = manifoldM.Add(tangentSumM,\n                        manifoldM.Scale(manifoldM.LogMap(pCurrent, dataM[i]), w));\n\n                    tangentSumN = manifoldN.Add(tangentSumN,\n                        manifoldN.Scale(manifoldN.LogMap(qCurrent, dataN[i]), w));\n                }\n\n                TPointM pNext = manifoldM.ExpMap(pCurrent, tangentSumM);\n                TPointN qNext = manifoldN.ExpMap(qCurrent, tangentSumN);\n\n                double shiftM = manifoldM.Distance(pCurrent, pNext);\n                double shiftN = manifoldN.Distance(qCurrent, qNext);\n\n                pCurrent = pNext;\n                qCurrent = qNext;\n\n                if (shiftM < tolerance && shiftN < tolerance)\n                {\n                    break;\n                }\n            }\n\n            return (pCurrent, qCurrent);\n        }\n    }\n\n    public static class ProductManifoldMedian\n    {\n        private const double Epsilon = 1e-10;\n\n        /// <summary>\n        /// Computes the geometric median using the Riemannian Weiszfeld algorithm.\n        /// Achieves local linear convergence, trapping singularities at data points.\n        /// </summary>\n        public static (TPointM p, TPointN q) ComputeWeiszfeld<TPointM, TTangentM, TPointN, TTangentN>(\n            IRiemannianManifold<TPointM, TTangentM> manifoldM,\n            IRiemannianManifold<TPointN, TTangentN> manifoldN,\n            ReadOnlySpan<TPointM> dataM,\n            ReadOnlySpan<TPointN> dataN,\n            ReadOnlySpan<double> weights,\n            in TPointM initialP,\n            in TPointN initialQ,\n            int maxIterations = 100,\n            double tolerance = 1e-6)\n        {\n            if (dataM.Length != dataN.Length || dataM.Length != weights.Length)\n            {\n                throw new ArgumentException(\"Data spans and weights must have the identical lengths.\");\n            }\n\n            int n = dataM.Length;\n            TPointM pCurrent = initialP;\n            TPointN qCurrent = initialQ;\n\n            // Stack alloc for small n; ArrayPool for large n to ensure zero allocations.\n            double[]? pooledArray = null;\n            Span<double> inverseDistances = n <= 1024\n                ? stackalloc double[n]\n                : (pooledArray = ArrayPool<double>.Shared.Rent(n)).AsSpan(0, n);\n\n            try\n            {\n                for (int iter = 0; iter < maxIterations; iter++)\n                {\n                    int coincidentIndex = -1;\n\n                    // Step 1: Compute coupled distances; flag coincidences but do not break early.\n                    for (int i = 0; i < n; i++)\n                    {\n                        double dM = manifoldM.Distance(pCurrent, dataM[i]);\n                        double dN = manifoldN.Distance(qCurrent, dataN[i]);\n                        double productDist = Math.Sqrt((dM * dM) + (dN * dN));\n\n                        if (productDist < Epsilon)\n                        {\n                            // Exclude this point from the weighted sum for this iteration.\n                            // The optimality check below determines whether to return or continue.\n                            coincidentIndex = i;\n                            inverseDistances[i] = 0.0;\n                        }\n                        else\n                        {\n                            inverseDistances[i] = weights[i] / productDist;\n                        }\n                    }\n\n                    // Handle exact data-point coincidence.\n                    // x_i is the geometric median iff:\n                    //   || sum_{j != i} (w_j / d_j) * log_p(x_j) || <= w_i\n                    // If the condition holds, return immediately. Otherwise exclude the\n                    // coincident point from this step's update and continue iterating;\n                    // the iterate will drift off the singularity toward the true median.\n                    if (coincidentIndex != -1)\n                    {\n                        TTangentM gradCheckM = manifoldM.ZeroTangent(pCurrent);\n                        TTangentN gradCheckN = manifoldN.ZeroTangent(qCurrent);\n\n                        for (int j = 0; j < n; j++)\n                        {\n                            if (j == coincidentIndex) continue;\n                            gradCheckM = manifoldM.Add(gradCheckM,\n                                manifoldM.Scale(manifoldM.LogMap(pCurrent, dataM[j]), inverseDistances[j]));\n                            gradCheckN = manifoldN.Add(gradCheckN,\n                                manifoldN.Scale(manifoldN.LogMap(qCurrent, dataN[j]), inverseDistances[j]));\n                        }\n\n                        double normM = manifoldM.Norm(pCurrent, gradCheckM);\n                        double normN = manifoldN.Norm(qCurrent, gradCheckN);\n                        double gradNorm = Math.Sqrt((normM * normM) + (normN * normN));\n\n                        if (gradNorm <= weights[coincidentIndex])\n                        {\n                            return (pCurrent, qCurrent);\n                        }\n\n                        // Condition failed: x_i is not the median.\n                        // inverseDistances[coincidentIndex] is already 0.0, so the update\n                        // below will step away from the singularity naturally.\n                        coincidentIndex = -1;\n                    }\n\n                    // Vectorized SIMD sum using .NET Tensors\n                    double sumInverseDistances = TensorPrimitives.Sum(inverseDistances);\n\n                    TTangentM tangentSumM = manifoldM.ZeroTangent(pCurrent);\n                    TTangentN tangentSumN = manifoldN.ZeroTangent(qCurrent);\n\n                    // Step 2: Compute adaptive weights and aggregate\n                    for (int i = 0; i < n; i++)\n                    {\n                        double adaptiveWeight = inverseDistances[i] / sumInverseDistances;\n\n                        TTangentM logM = manifoldM.LogMap(pCurrent, dataM[i]);\n                        tangentSumM = manifoldM.Add(tangentSumM, manifoldM.Scale(logM, adaptiveWeight));\n\n                        TTangentN logN = manifoldN.LogMap(qCurrent, dataN[i]);\n                        tangentSumN = manifoldN.Add(tangentSumN, manifoldN.Scale(logN, adaptiveWeight));\n                    }\n\n                    // Step 3: Retract back to the manifold\n                    TPointM pNext = manifoldM.ExpMap(pCurrent, tangentSumM);\n                    TPointN qNext = manifoldN.ExpMap(qCurrent, tangentSumN);\n\n                    double shiftM = manifoldM.Distance(pCurrent, pNext);\n                    double shiftN = manifoldN.Distance(qCurrent, qNext);\n\n                    if (shiftM < tolerance && shiftN < tolerance)\n                    {\n                        return (pNext, qNext);\n                    }\n\n                    pCurrent = pNext;\n                    qCurrent = qNext;\n                }\n\n                return (pCurrent, qCurrent);\n            }\n            finally\n            {\n                if (pooledArray != null)\n                {\n                    ArrayPool<double>.Shared.Return(pooledArray);\n                }\n            }\n        }\n\n        /// <summary>\n        /// Computes the geometric median using the Riemannian Subgradient Descent.\n        /// Provides robust global sublinear convergence by tracking the best-historical state.\n        /// </summary>\n        public static (TPointM p, TPointN q) ComputeSubgradient<TPointM, TTangentM, TPointN, TTangentN>(\n            IRiemannianManifold<TPointM, TTangentM> manifoldM,\n            IRiemannianManifold<TPointN, TTangentN> manifoldN,\n            ReadOnlySpan<TPointM> dataM,\n            ReadOnlySpan<TPointN> dataN,\n            ReadOnlySpan<double> weights,\n            in TPointM initialP,\n            in TPointN initialQ,\n            double initialStepSize = 1.0,\n            int maxIterations = 200)\n        {\n            if (dataM.Length != dataN.Length || dataM.Length != weights.Length)\n            {\n                throw new ArgumentException(\"Data spans and weights must have the identical lengths.\");\n            }\n\n            int n = dataM.Length;\n            TPointM pCurrent = initialP;\n            TPointN qCurrent = initialQ;\n\n            TPointM bestP = pCurrent;\n            TPointN bestQ = qCurrent;\n            double bestObjective = double.MaxValue;\n\n            for (int k = 0; k < maxIterations; k++)\n            {\n                TTangentM subgradM = manifoldM.ZeroTangent(pCurrent);\n                TTangentN subgradN = manifoldN.ZeroTangent(qCurrent);\n\n                double currentObjective = 0.0;\n\n                for (int i = 0; i < n; i++)\n                {\n                    double dM = manifoldM.Distance(pCurrent, dataM[i]);\n                    double dN = manifoldN.Distance(qCurrent, dataN[i]);\n                    double productDist = Math.Sqrt((dM * dM) + (dN * dN));\n\n                    currentObjective += weights[i] * productDist;\n\n                    // Prevent div-by-zero at non-differentiable points\n                    double distSafe = Math.Max(productDist, Epsilon);\n                    double weightFactor = weights[i] / distSafe;\n\n                    TTangentM logM = manifoldM.LogMap(pCurrent, dataM[i]);\n                    subgradM = manifoldM.Add(subgradM, manifoldM.Scale(logM, -weightFactor));\n\n                    TTangentN logN = manifoldN.LogMap(qCurrent, dataN[i]);\n                    subgradN = manifoldN.Add(subgradN, manifoldN.Scale(logN, -weightFactor));\n                }\n\n                // Track historical best due to non-monotonicity of subgradient steps\n                if (currentObjective < bestObjective)\n                {\n                    bestObjective = currentObjective;\n                    bestP = pCurrent;\n                    bestQ = qCurrent;\n                }\n\n                // Decaying step size: eta_k = eta_0 / sqrt(k + 1)\n                double stepSize = initialStepSize / Math.Sqrt(k + 1);\n\n                pCurrent = manifoldM.ExpMap(pCurrent, manifoldM.Scale(subgradM, -stepSize));\n                qCurrent = manifoldN.ExpMap(qCurrent, manifoldN.Scale(subgradN, -stepSize));\n            }\n\n            return (bestP, bestQ);\n        }\n    }\n} |
3 | estimators/Medoid.cs | {672 71 0.3006 4.3512} | 695 | namespace StatisticalEstimators\n{\n    /// <summary>\n    /// Result of a weighted metric-medoid search over observed candidates.\n    /// </summary>\n    public readonly struct LocationMedoidEstimate\n    {\n        public LocationMedoidEstimate(int pointIndex, int candidateOffset, double objective, double totalWeight)\n        {\n            PointIndex = pointIndex;\n            CandidateOffset = candidateOffset;\n            Objective = objective;\n            TotalWeight = totalWeight;\n        }\n\n        public int PointIndex { get; }\n\n        public int CandidateOffset { get; }\n\n        public double Objective { get; }\n\n        public double TotalWeight { get; }\n    }\n} |
4 | estimators/MetricMedoid.cs | {4437 356 0.3347 4.2989} | 4555 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Weighted metric medoid search over observed feature vectors.\n    /// </summary>\n    public static class LocationMetricMedoid\n    {\n        public delegate double PointDistance(double[] a, double[] b);\n\n        public static LocationMedoidEstimate Weighted(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights,\n            PointDistance distance)\n        {\n            return Weighted(points, indices, indices, weights, distance);\n        }\n\n        public static LocationMedoidEstimate Weighted(\n            double[][] points,\n            ReadOnlySpan<int> candidateIndices,\n            ReadOnlySpan<int> observationIndices,\n            ReadOnlySpan<double> weights,\n            PointDistance distance)\n        {\n            if (points == null)\n                throw new ArgumentNullException(nameof(points));\n            if (distance == null)\n                throw new ArgumentNullException(nameof(distance));\n            if (candidateIndices.Length == 0)\n                throw new ArgumentException(\"At least one candidate index is required.\", nameof(candidateIndices));\n            if (observationIndices.Length == 0)\n                throw new ArgumentException(\"At least one observation index is required.\", nameof(observationIndices));\n            if (observationIndices.Length != weights.Length)\n                throw new ArgumentException(\"Observation indices and weights must have the same length.\", nameof(weights));\n\n            ValidateIndices(points, candidateIndices, nameof(candidateIndices));\n            ValidateObservationInputs(points, observationIndices, weights);\n\n            double totalWeight = 0;\n            for (int i = 0; i < weights.Length; i++)\n                totalWeight += weights[i];\n            if (totalWeight <= 0)\n                throw new ArgumentException(\"At least one weight must be positive.\", nameof(weights));\n\n            int bestPointIndex = candidateIndices[0];\n            int bestCandidateOffset = 0;\n            double bestObjective = double.PositiveInfinity;\n\n            for (int candidateOffset = 0; candidateOffset < candidateIndices.Length; candidateOffset++)\n            {\n                int candidateIndex = candidateIndices[candidateOffset];\n                double[] candidate = points[candidateIndex];\n                double objective = 0;\n\n                for (int obsOffset = 0; obsOffset < observationIndices.Length; obsOffset++)\n                {\n                    double weight = weights[obsOffset];\n                    if (weight == 0) continue;\n\n                    double value = distance(candidate, points[observationIndices[obsOffset]]);\n                    if (double.IsNaN(value) || double.IsInfinity(value) || value < 0)\n                        throw new InvalidOperationException(\"Distance function must return a finite non-negative value.\");\n\n                    objective += weight * value;\n                }\n\n                if (objective < bestObjective)\n                {\n                    bestObjective = objective;\n                    bestPointIndex = candidateIndex;\n                    bestCandidateOffset = candidateOffset;\n                }\n            }\n\n            return new LocationMedoidEstimate(bestPointIndex, bestCandidateOffset, bestObjective, totalWeight);\n        }\n\n        private static void ValidateObservationInputs(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights)\n        {\n            ValidateIndices(points, indices, nameof(indices));\n            for (int i = 0; i < weights.Length; i++)\n            {\n                double weight = weights[i];\n                if (double.IsNaN(weight) || double.IsInfinity(weight) || weight < 0)\n                    throw new ArgumentOutOfRangeException(nameof(weights), \"Weights must be finite and non-negative.\");\n            }\n        }\n\n        private static void ValidateIndices(double[][] points, ReadOnlySpan<int> indices, string paramName)\n        {\n            for (int i = 0; i < indices.Length; i++)\n            {\n                int pointIndex = indices[i];\n                if ((uint)pointIndex >= (uint)points.Length || points[pointIndex] == null)\n                    throw new ArgumentOutOfRangeException(paramName, \"Point index is out of range.\");\n            }\n        }\n    }\n} |
5 | estimators/PointEstimates.cs | {734 83 0.3215 4.2494} | 762 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Diagnostics returned by iterative or closed-form location estimators.\n    /// </summary>\n    public readonly struct LocationEstimate\n    {\n        public LocationEstimate(bool converged, int iterations, double objective, double shift, double totalWeight)\n        {\n            Converged = converged;\n            Iterations = iterations;\n            Objective = objective;\n            Shift = shift;\n            TotalWeight = totalWeight;\n        }\n\n        public bool Converged { get; }\n\n        public int Iterations { get; }\n\n        public double Objective { get; }\n\n        public double Shift { get; }\n\n        public double TotalWeight { get; }\n    }\n} |
6 | estimators/SigmaEstimatorMad.cs | {4554 515 0.2916 4.5344} | 4704 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Median Absolute Deviation (MAD) — distribution-agnostic robust scale estimator.\n    /// Breakdown point: 50% on both location and scale. O(N log N) — two sorts.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// The core computation is purely ordinal: <c>median(|xᵢ − location|)</c>.\n    /// No distributional assumption is embedded in the raw estimate.\n    /// </para>\n    /// <para>\n    /// Consistency factors calibrate the raw MAD into an unbiased estimator of the\n    /// scale parameter of a specific distribution. The factors are named constants\n    /// exposed for caller use; the computation itself never applies them implicitly.\n    /// Use <see cref=\"ComputeScaled\"/> to apply a factor explicitly.\n    /// </para>\n    /// <para>\n    /// <strong>Location injection.</strong>\n    /// The primary overloads accept a caller-supplied location estimate so that the\n    /// location assumption is explicit. The convenience overloads compute the sample\n    /// median internally.\n    /// </para>\n    /// </remarks>\n    public static class Mad\n    {\n        /// <summary>\n        /// Gaussian consistency factor: 1 / Φ⁻¹(0.75) ≈ 1.4826.\n        /// Calibrates raw MAD to an unbiased estimator of σ under a normal distribution.\n        /// </summary>\n        public const double GaussianFactor = 1.4826;\n\n        /// <summary>\n        /// Laplace consistency factor: 1 / ln(2) ≈ 1.4427.\n        /// Calibrates raw MAD to an unbiased estimator of the Laplace scale parameter b.\n        /// </summary>\n        public const double LaplaceFactor = 1.4427;\n\n        /// <summary>\n        /// Returns the raw MAD: <c>median(|xᵢ − location|)</c>.\n        /// No consistency factor applied. Scratch must be ≥ values.Length.\n        /// </summary>\n        public static double Compute(ReadOnlySpan<double> values, double location, Span<double> scratch)\n        {\n            int n = values.Length;\n            if (n == 0) return 0.0;\n            if (scratch.Length < n)\n                throw new ArgumentException(\"Scratch span is shorter than values.\", nameof(scratch));\n\n            Span<double> work = scratch.Slice(0, n);\n            for (int i = 0; i < n; i++)\n                work[i] = Math.Abs(values[i] - location);\n            work.Sort();\n            return MedianOfSorted(work);\n        }\n\n        /// <summary>\n        /// Returns the raw MAD, computing the sample median as location internally.\n        /// Scratch must be ≥ values.Length (reused for both sort passes).\n        /// </summary>\n        public static double Compute(ReadOnlySpan<double> values, Span<double> scratch)\n        {\n            int n = values.Length;\n            if (n == 0) return 0.0;\n            if (scratch.Length < n)\n                throw new ArgumentException(\"Scratch span is shorter than values.\", nameof(scratch));\n\n            // First pass: sort to find median location.\n            Span<double> work = scratch.Slice(0, n);\n            values.CopyTo(work);\n            work.Sort();\n            double location = MedianOfSorted(work);\n\n            // Second pass: absolute deviations from location, sort, median.\n            // Read from original values — work is now scratch for deviations.\n            for (int i = 0; i < n; i++)\n                work[i] = Math.Abs(values[i] - location);\n            work.Sort();\n            return MedianOfSorted(work);\n        }\n\n        /// <summary>\n        /// Returns <c>consistencyFactor × median(|xᵢ − location|)</c>.\n        /// Pass <see cref=\"GaussianFactor\"/> or <see cref=\"LaplaceFactor\"/> for standard\n        /// calibrations, or any positive value for domain-specific calibration.\n        /// </summary>\n        public static double ComputeScaled(ReadOnlySpan<double> values, double location, Span<double> scratch,\n            double consistencyFactor)\n            => Compute(values, location, scratch) * consistencyFactor;\n\n        /// <summary>\n        /// Returns <c>consistencyFactor × MAD</c> with internally computed median location.\n        /// </summary>\n        public static double ComputeScaled(ReadOnlySpan<double> values, Span<double> scratch,\n            double consistencyFactor)\n            => Compute(values, scratch) * consistencyFactor;\n\n        private static double MedianOfSorted(ReadOnlySpan<double> sorted)\n        {\n            int mid = sorted.Length / 2;\n            return sorted.Length % 2 == 0\n                ? (sorted[mid - 1] + sorted[mid]) / 2.0\n                : sorted[mid];\n        }\n    }\n} |
