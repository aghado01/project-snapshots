idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
10 | gmm/GaussianMixtureModel.cs | {19147 2076 0.3503 4.4380} | 20750 | using System;\nusing System.Threading.Tasks;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// General-purpose Gaussian Mixture Model fitted via Expectation-Maximization.\n    /// Designed as a scientific library primitive analogous to MATLAB's <c>fitgmdist</c>\n    /// or scikit-learn's <c>GaussianMixture</c>.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// <strong>SPC handoff.</strong>\n    /// Call <see cref=\"InitializeWithParameters\"/> with means and covariances derived\n    /// from SPC output (e.g. via <see cref=\"ProductManifoldFrechetMean\"/> or\n    /// <see cref=\"ProductManifoldMedian\"/>) before <see cref=\"Fit(double[][], int, double)\"/>\n    /// to provide a geometrically motivated warm start rather than relying on random\n    /// initialisation.\n    /// </para>\n    /// <para>\n    /// <strong>Semi-supervised path.</strong>\n    /// <see cref=\"Fit(double[][], int[], int, double)\"/> accepts a locked-assignment\n    /// array. Points with a non-negative entry have their responsibilities pinned to a\n    /// one-hot vector (the SPC core assignment), while boundary points float freely\n    /// in the standard E-Step. Locked points act as absolute anchors: their full\n    /// coordinates contribute directly to the M-Step mean and covariance of their\n    /// assigned component.\n    /// </para>\n    /// <para>\n    /// <strong>Allocation profile.</strong>\n    /// The responsibility matrix and per-point log-likelihood accumulator are allocated\n    /// once in <c>FitCore</c> and reused across all EM iterations. The M-Step uses\n    /// pre-allocated scratchpads on each <see cref=\"GaussianComponent\"/>; no per-iteration\n    /// heap allocations occur inside the EM loop.\n    /// </para>\n    /// </remarks>\n    public sealed class GaussianMixtureModel\n    {\n        public int K { get; }\n        public int Dimension { get; }\n        public GaussianComponent[] Components { get; }\n\n        /// <summary>True if the last <see cref=\"Fit\"/> call converged within the tolerance.</summary>\n        public bool IsConverged { get; private set; }\n\n        /// <summary>Log-likelihood at the end of the last <see cref=\"Fit\"/> call.</summary>\n        public double FinalLogLikelihood { get; private set; }\n\n        /// <summary>Number of EM iterations performed in the last <see cref=\"Fit\"/> call.</summary>\n        public int NumIterations { get; private set; }\n\n        // Persisted from the most recent Fit call. Exposed via GetFinalResponsibilities().\n        private double[,]? _responsibilities;\n\n        // True once InitializeWithParameters has been called or RandomInitialize has run.\n        private bool _isInitialized;\n\n        public GaussianMixtureModel(int k, int dimension)\n        {\n            K = k;\n            Dimension = dimension;\n            Components = new GaussianComponent[K];\n            for (int i = 0; i < K; i++)\n                Components[i] = new GaussianComponent(dimension);\n        }\n\n        // ── Initialisation ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Injects starting parameters. Call before\n        /// <see cref=\"Fit(double[][], int, double)\"/> to provide a warm start\n        /// (e.g. from SPC or <see cref=\"ProductManifoldFrechetMean\"/> output).\n        /// </summary>\n        /// <param name=\"means\">K mean vectors, each of length <see cref=\"Dimension\"/>.</param>\n        /// <param name=\"covariances\">K covariance matrices, each <see cref=\"Dimension\"/> × <see cref=\"Dimension\"/>.</param>\n        /// <param name=\"weights\">K mixture weights; normalised internally so they need not sum to 1.</param>\n        public void InitializeWithParameters(double[][] means, double[][,] covariances, double[] weights)\n        {\n            double weightSum = 0.0;\n            for (int i = 0; i < K; i++) weightSum += weights[i];\n            double invWeightSum = weightSum > 0.0 ? 1.0 / weightSum : 1.0;\n\n            for (int i = 0; i < K; i++)\n            {\n                Array.Copy(means[i], Components[i].Mean, Dimension);\n                Array.Copy(covariances[i], Components[i].Covariance, Dimension * Dimension);\n                Components[i].Weight = weights[i] * invWeightSum;\n                Components[i].UpdateCache();\n            }\n            _isInitialized = true;\n        }\n\n        // ── Fitting ──────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Fits the GMM using standard (fully unsupervised) EM.\n        /// </summary>\n        public void Fit(double[][] data, int maxIterations = 100, double tolerance = 1e-4)\n            => FitCore(data, lockedAssignments: null, maxIterations, tolerance);\n\n        /// <summary>\n        /// Fits the GMM using semi-supervised EM.\n        /// </summary>\n        /// <remarks>\n        /// Locked points are excluded from the log-likelihood accumulation; convergence\n        /// is therefore measured on the floating (boundary) points only. This matches the\n        /// generative framing where the locked labels are treated as given rather than\n        /// inferred.\n        /// </remarks>\n        /// <param name=\"lockedAssignments\">\n        /// Length-N array. −1 = unlabelled (evaluated in the standard E-Step).\n        /// ≥ 0 = component index (responsibility pinned to one-hot, PDF skipped).\n        /// </param>\n        public void Fit(double[][] data, int[] lockedAssignments, int maxIterations = 100, double tolerance = 1e-4)\n            => FitCore(data, lockedAssignments, maxIterations, tolerance);\n\n        private void FitCore(double[][] data, int[]? lockedAssignments, int maxIterations, double tolerance)\n        {\n            int n = data.Length;\n\n            if (!_isInitialized)\n                RandomInitialize(data);\n\n            // Allocate / resize responsibility matrix only when data size changes.\n            if (_responsibilities == null || _responsibilities.GetLength(0) != n)\n                _responsibilities = new double[n, K];\n\n            // Per-point log-likelihood accumulator; allocated once, reused across iterations.\n            double[] perPointLL = new double[n];\n\n            double prevLogLikelihood = double.MinValue;\n            IsConverged = false;\n            NumIterations = 0;\n\n            for (int iter = 0; iter < maxIterations; iter++)\n            {\n                double currentLogLikelihood = ExpectationStep(data, lockedAssignments, _responsibilities, perPointLL);\n                NumIterations = iter + 1;\n\n                if (Math.Abs(currentLogLikelihood - prevLogLikelihood) < tolerance)\n                {\n                    IsConverged = true;\n                    FinalLogLikelihood = currentLogLikelihood;\n                    return;\n                }\n\n                prevLogLikelihood = currentLogLikelihood;\n                MaximizationStep(data, _responsibilities, n);\n            }\n\n            FinalLogLikelihood = prevLogLikelihood;\n        }\n\n        // ── Random initialisation (randSample) ───────────────────────────────────\n\n        /// <summary>\n        /// Picks K data points uniformly at random as initial means, sets uniform weights,\n        /// and uses the per-dimension sample variance as the initial diagonal covariance.\n        /// Analogous to MATLAB's <c>fitgmdist(..., 'Start', 'randSample')</c>.\n        /// </summary>\n        private void RandomInitialize(double[][] data, Random? rng = null)\n        {\n            rng ??= new Random();\n            int n = data.Length;\n\n            // Per-dimension mean and variance over the full dataset.\n            double[] mean = new double[Dimension];\n            for (int i = 0; i < n; i++)\n                for (int d = 0; d < Dimension; d++)\n                    mean[d] += data[i][d];\n            for (int d = 0; d < Dimension; d++) mean[d] /= n;\n\n            double[] variance = new double[Dimension];\n            for (int i = 0; i < n; i++)\n                for (int d = 0; d < Dimension; d++)\n                {\n                    double diff = data[i][d] - mean[d];\n                    variance[d] += diff * diff;\n                }\n            // Bessel-corrected; floor at regularisation value to prevent zero.\n            double invNm1 = n > 1 ? 1.0 / (n - 1) : 1.0;\n            for (int d = 0; d < Dimension; d++)\n                variance[d] = variance[d] * invNm1 + 1e-6;\n\n            // Partial Fisher-Yates to sample K distinct row indices.\n            int[] indices = new int[n];\n            for (int i = 0; i < n; i++) indices[i] = i;\n            for (int i = 0; i < K; i++)\n            {\n                int j = i + rng.Next(n - i);\n                (indices[i], indices[j]) = (indices[j], indices[i]);\n            }\n\n            double uniformWeight = 1.0 / K;\n            for (int k = 0; k < K; k++)\n            {\n                Array.Copy(data[indices[k]], Components[k].Mean, Dimension);\n                Components[k].Weight = uniformWeight;\n                Array.Clear(Components[k].Covariance, 0, Dimension * Dimension);\n                for (int d = 0; d < Dimension; d++)\n                    Components[k].Covariance[d, d] = variance[d];\n                Components[k].UpdateCache();\n            }\n            _isInitialized = true;\n        }\n\n        // ── E-Step ───────────────────────────────────────────────────────────────\n\n        private double ExpectationStep(\n            double[][] data,\n            int[]? lockedAssignments,\n            double[,] responsibilities,\n            double[] perPointLL)\n        {\n            int n = data.Length;\n            int localK = K; // captured by the parallel lambda\n\n            Parallel.For(0, n, i =>\n            {\n                // Semi-supervised: pin locked points to one-hot; skip PDF evaluation.\n                if (lockedAssignments != null && lockedAssignments[i] >= 0)\n                {\n                    int lockedComp = lockedAssignments[i];\n                    for (int k = 0; k < localK; k++)\n                        responsibilities[i, k] = (k == lockedComp) ? 1.0 : 0.0;\n                    perPointLL[i] = 0.0; // excluded from convergence check\n                    return;\n                }\n\n                // Log-Sum-Exp prevents underflow in high dimensions.\n                // stackalloc is safe here: each parallel invocation has its own stack frame.\n                // K is expected to be small (< ~100); for very large K, rent from ArrayPool instead.\n                Span<double> logW = stackalloc double[localK];\n                double maxLogW = double.MinValue;\n\n                for (int k = 0; k < localK; k++)\n                {\n                    logW[k] = Math.Log(Math.Max(Components[k].Weight, 1e-300))\n                               + Components[k].EvaluateLogPdf(data[i]);\n                    if (logW[k] > maxLogW) maxLogW = logW[k];\n                }\n\n                double sumExp = 0.0;\n                for (int k = 0; k < localK; k++)\n                {\n                    double e = Math.Exp(logW[k] - maxLogW);\n                    logW[k] = e;\n                    sumExp += e;\n                }\n\n                double invSumExp = 1.0 / sumExp;\n                for (int k = 0; k < localK; k++)\n                    responsibilities[i, k] = logW[k] * invSumExp;\n\n                perPointLL[i] = maxLogW + Math.Log(sumExp);\n            });\n\n            // Accumulate outside the parallel block — no lock contention.\n            double total = 0.0;\n            for (int i = 0; i < n; i++) total += perPointLL[i];\n            return total;\n        }\n\n        // ── M-Step ───────────────────────────────────────────────────────────────\n\n        private void MaximizationStep(double[][] data, double[,] responsibilities, int n)\n        {\n            for (int k = 0; k < K; k++)\n            {\n                GaussianComponent comp = Components[k];\n                comp.ResetScratch();\n\n                double effectiveCount = 0.0;\n\n                // Pass 1: accumulate weighted mean (raw sum into ScratchMean).\n                for (int i = 0; i < n; i++)\n                {\n                    double resp = responsibilities[i, k];\n                    effectiveCount += resp;\n                    for (int d = 0; d < Dimension; d++)\n                        comp.ScratchMean[d] += resp * data[i][d];\n                }\n\n                // Normalise mean in-place before covariance accumulation.\n                // CommitScratch expects ScratchMean to already be divided by effectiveCount.\n                if (effectiveCount > 1e-12)\n                {\n                    double invCount = 1.0 / effectiveCount;\n                    for (int d = 0; d < Dimension; d++)\n                        comp.ScratchMean[d] *= invCount;\n                }\n\n                // Pass 2: accumulate weighted centred outer products into ScratchCov.\n                // Only the upper triangle is computed; the lower is mirrored immediately\n                // to keep ScratchCov symmetric throughout (matches CommitScratch's assumption).\n                for (int i = 0; i < n; i++)\n                {\n                    double resp = responsibilities[i, k];\n                    if (resp < 1e-15) continue; // skip negligible contributions\n\n                    for (int d1 = 0; d1 < Dimension; d1++)\n                    {\n                        double diff1 = data[i][d1] - comp.ScratchMean[d1];\n                        for (int d2 = d1; d2 < Dimension; d2++)\n                        {\n                            double contrib = resp * diff1 * (data[i][d2] - comp.ScratchMean[d2]);\n                            comp.ScratchCov[d1, d2] += contrib;\n                            if (d1 != d2) comp.ScratchCov[d2, d1] += contrib;\n                        }\n                    }\n                }\n\n                comp.CommitScratch(effectiveCount, n);\n            }\n        }\n\n        // ── Inference ────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Returns the hard cluster assignment (argmax responsibility) for each data point.\n        /// </summary>\n        public int[] Predict(double[][] data)\n        {\n            int n = data.Length;\n            int[] predictions = new int[n];\n\n            for (int i = 0; i < n; i++)\n            {\n                int bestK = 0;\n                double maxLogPdf = double.MinValue;\n\n                for (int k = 0; k < K; k++)\n                {\n                    double logPdf = Math.Log(Math.Max(Components[k].Weight, 1e-300))\n                                    + Components[k].EvaluateLogPdf(data[i]);\n                    if (logPdf > maxLogPdf)\n                    {\n                        maxLogPdf = logPdf;\n                        bestK = k;\n                    }\n                }\n\n                predictions[i] = bestK;\n            }\n\n            return predictions;\n        }\n\n        /// <summary>\n        /// Returns the soft posterior P(component k | x_i) for each data point.\n        /// Result shape: [n, K]. Useful for uncertainty-weighted downstream processing\n        /// and for routing data to child nodes in the Blatt hierarchy.\n        /// </summary>\n        public double[,] PredictProba(double[][] data)\n        {\n            int n = data.Length;\n            double[,] result = new double[n, K];\n            double[] perPointLL = new double[n];\n            ExpectationStep(data, lockedAssignments: null, result, perPointLL);\n            return result;\n        }\n\n        /// <summary>\n        /// Returns the responsibility matrix [N, K] from the most recent\n        /// <see cref=\"Fit(double[][], int, double)\"/> or\n        /// <see cref=\"Fit(double[][], int[], int, double)\"/> call.\n        /// Required by the hierarchical nesting engine (<see cref=\"ISpcShatterOracle\"/>)\n        /// to route data to child nodes after a local EM solve.\n        /// Returns <see langword=\"null\"/> if <c>Fit</c> has not yet been called.\n        /// </summary>\n        /// <remarks>\n        /// The returned array is the live internal buffer. Do not modify it. Take a copy\n        /// if the array must outlive the next <c>Fit</c> call on this instance.\n        /// </remarks>\n        public double[,]? GetFinalResponsibilities() => _responsibilities;\n\n        // ── Density ──────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Returns the mixture PDF value at each point: Y[i] = Σ_k π_k · N(x_i | μ_k, Σ_k).\n        /// May underflow to 0 in high dimensions; prefer the log-likelihood via\n        /// <see cref=\"FinalLogLikelihood\"/> or the internal E-Step for EM arithmetic.\n        /// Analogous to MATLAB's <c>pdf(obj, X)</c>.\n        /// </summary>\n        public double[] Pdf(double[][] data)\n        {\n            int n = data.Length;\n            double[] result = new double[n];\n            for (int i = 0; i < n; i++)\n                for (int k = 0; k < K; k++)\n                    result[i] += Components[k].Weight * Components[k].EvaluatePdf(data[i]);\n            return result;\n        }\n\n        // ── Distance ─────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Returns the squared Mahalanobis distance from each point to each component mean.\n        /// Result[i, k] = (x_i − μ_k)ᵀ Σ_k⁻¹ (x_i − μ_k).\n        /// Analogous to MATLAB's <c>mahal(obj, X)</c>.\n        /// </summary>\n        public double[,] Mahal(double[][] data)\n        {\n            int n = data.Length;\n            double[,] result = new double[n, K];\n            for (int i = 0; i < n; i++)\n                for (int k = 0; k < K; k++)\n                    result[i, k] = Components[k].MahalanobisSquared(data[i]);\n            return result;\n        }\n\n        // ── Sampling ─────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Draws <paramref name=\"n\"/> samples from the mixture distribution.\n        /// A component is chosen by its weight, then a point is drawn from that\n        /// component's Gaussian via the preserved Cholesky factor.\n        /// Analogous to MATLAB's <c>random(obj, N)</c>.\n        /// </summary>\n        /// <param name=\"componentIndices\">\n        /// Optional output array (length <paramref name=\"n\"/>). Filled with the\n        /// 0-based index of the component that generated each sample.\n        /// </param>\n        public double[][] Sample(int n, Random? rng = null, int[]? componentIndices = null)\n        {\n            rng ??= new Random();\n            double[][] result = new double[n][];\n\n            // Build weight CDF for O(K) multinomial draw.\n            double[] cdf = new double[K];\n            cdf[0] = Components[0].Weight;\n            for (int k = 1; k < K; k++)\n                cdf[k] = cdf[k - 1] + Components[k].Weight;\n\n            for (int i = 0; i < n; i++)\n            {\n                double u = rng.NextDouble();\n                int compIdx = K - 1;\n                for (int k = 0; k < K - 1; k++)\n                    if (u < cdf[k]) { compIdx = k; break; }\n\n                result[i] = Components[compIdx].Sample(rng);\n                if (componentIndices != null) componentIndices[i] = compIdx;\n            }\n            return result;\n        }\n    }\n} |
11 | gmm/ISpcShatterOracle.cs | {2582 331 0.2308 4.6451} | 2643 | namespace StatisticalEstimators\n{\n    /// <summary>\n    /// Bridges the SPC temperature checkpoint state into the hierarchical GMM nesting engine.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// NOTE THAT THIS IS ONLY A PLACEHOLDER FOR PROTOTYPE/CONCEPTUAL DESIGN\n    /// INTERFACE BETWEEN SPC AND GMM + Locking IS NOT PRIMARY FOCUS UNTIL SPC and GMM standalone implementations/apis are tested and more mature on their own\n    /// Implementors read from the SPC checkpoint store (e.g. <c>spc.checkpoint.cs</c>) to\n    /// answer whether a given parent cluster shattered at the next temperature step, and if\n    /// so, to provide the locked core assignments for the resulting child clusters.\n    /// </para>\n    /// <para>\n    /// The hierarchical nesting engine calls this oracle once per node during the top-down\n    /// recursive fit. The typical flow is:\n    /// <list type=\"number\">\n    ///   <item>Root node calls <c>GetChildClusters</c> for the entire dataset.</item>\n    ///   <item>Oracle inspects the temperature ladder; if the cluster is stable, returns K = 1 (leaf).</item>\n    ///   <item>If K &gt; 1, the engine instantiates a <see cref=\"GaussianMixtureModel\"/> bounded\n    ///   to the local data subset, fitting it via the semi-supervised path using\n    ///   <c>LockedAssignments</c> as the SPC core anchors.</item>\n    ///   <item>Data is then routed to K child nodes via <see cref=\"GaussianMixtureModel.GetFinalResponsibilities\"/>\n    ///   and the process recurses.</item>\n    /// </list>\n    /// </para>\n    /// </remarks>\n    public interface ISpcShatterOracle\n    {\n        /// <summary>\n        /// Returns the number of child clusters and the locked assignment array for\n        /// the given parent cluster at the next temperature step.\n        /// </summary>\n        /// <param name=\"parentClusterId\">\n        /// Identifier of the parent cluster at the current temperature level.\n        /// </param>\n        /// <param name=\"dataSubsetIndices\">\n        /// Global indices of the data points that were routed to this parent node.\n        /// </param>\n        /// <returns>\n        /// <c>K</c>: number of child clusters. 1 signals a leaf; no GMM is instantiated.\n        /// <c>LockedAssignments</c>: array of length <c>dataSubsetIndices.Length</c>;\n        ///   −1 = unlabelled boundary point (floats in E-Step),\n        ///   ≥ 0 = component index of a locked SPC core point (one-hot responsibility).\n        /// </returns>\n        (int K, int[] LockedAssignments) GetChildClusters(string parentClusterId, int[] dataSubsetIndices);\n    }\n} |
12 | graphs/EpsilonBall.cs | {2670 294 0.3723 4.2667} | 2748 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace ProximityGraphs\n{\n    public static partial class ProximityGraph\n    {\n        /// <summary>\n        /// Epsilon-ball proximity.\n        /// Edge (i,j) exists if d(i,j) &lt; epsilon.\n        /// Inherently symmetric given a symmetric distance function.\n        /// K parameter is not used — all pairs within radius are included.\n        /// Produces a variable-degree graph: dense regions get more edges,\n        /// sparse regions get fewer. Can fail in high dimensions where\n        /// distance concentration makes epsilon selection fragile.\n        ///\n        /// Returns per-node neighbor lists and 1-NN distances for auto-delta.\n        /// </summary>\n        public static NeighborSelection SelectEpsilonBall(\n            int n, double epsilon, Func<int, int, double> pairDistance)\n        {\n            if (epsilon <= 0)\n                throw new ArgumentException(\"Epsilon must be positive for EpsilonBall proximity.\");\n\n            // Phase 1: parallel upper-triangle scan — each row i writes only to halfLists[i].\n            // No cross-row contention; j's symmetric entry is added in Phase 2.\n            var halfLists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                halfLists[i] = new List<Neighbor>();\n\n            Parallel.For(0, n, i =>\n            {\n                for (int j = i + 1; j < n; j++)\n                {\n                    double d = pairDistance(i, j);\n                    if (d < epsilon)\n                        halfLists[i].Add(new Neighbor { Index = j, Distance = d });\n                }\n            });\n\n            // Phase 2: sequential symmetrization — O(E), no contention.\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in halfLists[i])\n                {\n                    lists[i].Add(nb);\n                    lists[nb.Index].Add(new Neighbor { Index = i, Distance = nb.Distance });\n                }\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return new NeighborSelection(allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
13 | graphs/GraphSelection.cs | {4180 402 0.4103 4.1058} | 4327 | using System;\n\nnamespace ProximityGraphs\n{\n    public readonly struct NeighborSelection\n    {\n        public NeighborSelection(Neighbor[][] allNeighbors, double[] nearestNeighborDistances)\n        {\n            AllNeighbors = allNeighbors ?? throw new ArgumentNullException(nameof(allNeighbors));\n            NearestNeighborDistances = nearestNeighborDistances ?? throw new ArgumentNullException(nameof(nearestNeighborDistances));\n        }\n\n        public Neighbor[][] AllNeighbors { get; }\n\n        public double[] NearestNeighborDistances { get; }\n\n        public void Deconstruct(out Neighbor[][] allNeighbors, out double[] nearestNeighborDistances)\n        {\n            allNeighbors = AllNeighbors;\n            nearestNeighborDistances = NearestNeighborDistances;\n        }\n    }\n\n    public struct Neighbor : IComparable<Neighbor>\n    {\n        public int Index;\n        public double Distance;\n\n        public int CompareTo(Neighbor other) => other.Distance.CompareTo(Distance);\n    }\n\n    internal sealed class BoundedMinHeap\n    {\n        private readonly Neighbor[] _heap;\n        private int _count;\n        private readonly int _capacity;\n\n        public BoundedMinHeap(int k)\n        {\n            if (k <= 0)\n                throw new ArgumentOutOfRangeException(nameof(k), \"K must be positive.\");\n\n            _capacity = k;\n            _heap = new Neighbor[k];\n        }\n\n        public void TryAdd(int index, double distance)\n        {\n            if (_count < _capacity)\n            {\n                _heap[_count] = new Neighbor { Index = index, Distance = distance };\n                _count++;\n                if (_count == _capacity) BuildHeap();\n            }\n            else if (distance < _heap[0].Distance)\n            {\n                _heap[0] = new Neighbor { Index = index, Distance = distance };\n                SiftDown(0);\n            }\n        }\n\n        public Neighbor[] GetSorted()\n        {\n            var result = new Neighbor[_count];\n            Array.Copy(_heap, result, _count);\n            Array.Sort(result, (a, b) => a.Distance.CompareTo(b.Distance));\n            return result;\n        }\n\n        private void BuildHeap()\n        {\n            for (int i = _count / 2 - 1; i >= 0; i--)\n                SiftDown(i);\n        }\n\n        private void SiftDown(int i)\n        {\n            while (true)\n            {\n                int largest = i;\n                int left = 2 * i + 1;\n                int right = 2 * i + 2;\n\n                if (left < _count && _heap[left].Distance > _heap[largest].Distance) largest = left;\n                if (right < _count && _heap[right].Distance > _heap[largest].Distance) largest = right;\n                if (largest == i) break;\n\n                var tmp = _heap[i];\n                _heap[i] = _heap[largest];\n                _heap[largest] = tmp;\n                i = largest;\n            }\n        }\n    }\n\n    internal sealed class DisjointSet\n    {\n        private readonly int[] _parent;\n        private readonly int[] _size;\n\n        public DisjointSet(int count)\n        {\n            _parent = new int[count];\n            _size = new int[count];\n            for (int i = 0; i < count; i++)\n            {\n                _parent[i] = i;\n                _size[i] = 1;\n            }\n        }\n\n        public int Find(int value)\n        {\n            int root = value;\n            while (root != _parent[root])\n                root = _parent[root];\n\n            while (value != root)\n            {\n                int next = _parent[value];\n                _parent[value] = root;\n                value = next;\n            }\n\n            return root;\n        }\n\n        public void Union(int left, int right)\n        {\n            int rootLeft = Find(left);\n            int rootRight = Find(right);\n            if (rootLeft == rootRight) return;\n\n            if (_size[rootLeft] < _size[rootRight])\n            {\n                _parent[rootLeft] = rootRight;\n                _size[rootRight] += _size[rootLeft];\n            }\n            else\n            {\n                _parent[rootRight] = rootLeft;\n                _size[rootLeft] += _size[rootRight];\n            }\n        }\n    }\n} |
