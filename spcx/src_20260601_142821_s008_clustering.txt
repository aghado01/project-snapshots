idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
84 | clustering/graphical/spc/runtime/scheduling/AdaptiveSweepStrategy.cs | {10681 802 0.3270 4.4784} | 10986 | using System;\nusing System.Collections.Generic;\nusing System.Diagnostics;\nusing System.Globalization;\nusing System.IO;\nusing System.Linq;\nusing Clustering.Graphical.SPC.Profiling;\nusing Clustering.Graphical.SPC.Profiling.Signals;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Execution.Sinks;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Scheduling;\n\npublic sealed class AdaptiveSweepStrategy : ISweepStrategy\n{\n    private readonly AdaptiveSchedulerConfig _config;\n\n    public AdaptiveSweepStrategy(AdaptiveSchedulerConfig? config = null)\n    {\n        _config = config ?? new AdaptiveSchedulerConfig();\n    }\n\n    public SweepResult Run(CsrGraph graph, ISignalAnalyzer? analyzer = null)\n    {\n        var cfg = _config;\n        var signalAnalyzer = analyzer ?? new ChiPeakSignalAnalyzer();\n        var sw = Stopwatch.StartNew();\n\n        double tempMin;\n        double tempMax;\n        if (cfg.TempMin.HasValue && cfg.TempMax.HasValue)\n        {\n            tempMin = cfg.TempMin.Value;\n            tempMax = cfg.TempMax.Value;\n        }\n        else\n        {\n            (double bracketMin, double bracketMax) = SpcScheduleHelpers.EstimateBracket(\n                graph,\n                cfg.Sampler.Q,\n                cfg.ColdEndOvershoot,\n                cfg.HotEndOvershoot);\n            tempMin = cfg.TempMin ?? bracketMin;\n            tempMax = cfg.TempMax ?? bracketMax;\n        }\n\n        if (graph.NodeCount < 2)\n            return SweepKernel.BuildTrivialResult(graph, cfg.Sampler.Q, sw.Elapsed);\n\n        var executor = new SpcExecutor();\n        var execOptions = new SpcExecutionOptions\n        {\n            FrameSink              = ResolveSink(cfg.CheckpointDirectory),\n            MaxDegreeOfParallelism = cfg.MaxDegreeOfParallelism,\n        };\n\n        int totalCycles = 0;\n        var stageTimings = new Dictionary<string, TimeSpan>(StringComparer.Ordinal);\n\n        var stageSw = Stopwatch.StartNew();\n        var coarseRuns = RunCoarseProbe(executor, execOptions, graph, cfg, tempMin, tempMax, ref totalCycles);\n        stageTimings[\"coarse\"] = stageSw.Elapsed;\n        var coarseProfile = SweepProfile.From(coarseRuns);\n        double coarseStability = SpcProfileAnalysis.ComputeStability(coarseProfile);\n\n        if (coarseStability >= cfg.EarlyStopStability)\n        {\n            double earlyT = SpcProfileAnalysis.PickPeakTemperature(coarseProfile);\n            stageSw.Restart();\n            var earlyFinalRun = RunFinalEquilibrium(executor, execOptions, graph, earlyT, cfg, ref totalCycles);\n            stageTimings[\"final_equilibrium\"] = stageSw.Elapsed;\n            ProfileCriteria earlyCriteria = signalAnalyzer.Analyze(coarseProfile);\n\n            return new SweepResult\n            {\n                Summary = new SweepSummary\n                {\n                    SubgraphNodes     = graph.NodeCount,\n                    SubgraphEdges     = graph.Targets.Length / 2,\n                    ChosenTemperature = earlyT,\n                    StabilityScore    = coarseStability,\n                    TotalCyclesUsed   = totalCycles,\n                    EarlyStopped      = true,\n                    Elapsed           = sw.Elapsed,\n                    StageTimings      = stageTimings,\n                    Profile           = coarseProfile,\n                },\n                SweepRuns = coarseRuns,\n                FinalRun = earlyFinalRun,\n                ProfileCriteria = earlyCriteria,\n                EdgeObservables = earlyFinalRun.EdgeObservables,\n            };\n        }\n\n        stageSw.Restart();\n        var refinedRuns = RunDenseRefinement(executor, execOptions, graph, coarseRuns, cfg, ref totalCycles);\n        stageTimings[\"dense\"] = stageSw.Elapsed;\n        var allRuns = coarseRuns.Concat(refinedRuns).ToList();\n        var fullProfile = SweepProfile.From(allRuns);\n        double chosenT = SpcProfileAnalysis.PickPeakTemperature(fullProfile);\n        double stability = SpcProfileAnalysis.ComputeStability(fullProfile);\n\n        stageSw.Restart();\n        var finalRun = RunFinalEquilibrium(executor, execOptions, graph, chosenT, cfg, ref totalCycles);\n        stageTimings[\"final_equilibrium\"] = stageSw.Elapsed;\n        ProfileCriteria criteria = signalAnalyzer.Analyze(fullProfile);\n\n        return new SweepResult\n        {\n            Summary = new SweepSummary\n            {\n                SubgraphNodes     = graph.NodeCount,\n                SubgraphEdges     = graph.Targets.Length / 2,\n                ChosenTemperature = chosenT,\n                StabilityScore    = stability,\n                TotalCyclesUsed   = totalCycles,\n                EarlyStopped      = false,\n                Elapsed           = sw.Elapsed,\n                StageTimings      = stageTimings,\n                Profile           = fullProfile,\n            },\n            SweepRuns = allRuns,\n            FinalRun = finalRun,\n            ProfileCriteria = criteria,\n            EdgeObservables = finalRun.EdgeObservables,\n        };\n    }\n\n    private static List<SpcRunResult> RunCoarseProbe(\n        SpcExecutor executor,\n        SpcExecutionOptions execOptions,\n        CsrGraph graph,\n        AdaptiveSchedulerConfig cfg,\n        double tempMin,\n        double tempMax,\n        ref int totalCycles)\n    {\n        var grid = SpcScheduleHelpers.LogSpaceGrid(tempMin, tempMax, cfg.CoarseSteps);\n        var tasks = new List<SpcTaskSpec>(grid.Length);\n        foreach (double T in grid)\n        {\n            tasks.Add(new SpcTaskSpec\n            {\n                Temperature    = T,\n                ReplicaIndex   = 0,\n                Cycles         = cfg.CoarseCycles,\n                BurnInCycles   = cfg.CoarseBurn,\n                Q              = cfg.Sampler.Q,\n                ObservableTier = PottsObservableTier.Standard,\n                BaseSeed       = SpcSeedHelper.Derive(cfg.BaseSeed, T, replica: 0, round: 0),\n                CheckpointPath = ProbeCheckpointPath(cfg.CheckpointDirectory, \"coarse\", T, replica: 0, round: 0),\n            });\n        }\n\n        SpcBatchResult batch = executor.RunBatch(graph, tasks, default, execOptions);\n        totalCycles += grid.Length * (cfg.CoarseBurn + cfg.CoarseCycles);\n        return HydrateAlignedRuns(batch, tasks, \"coarse\");\n    }\n\n    private static List<SpcRunResult> RunDenseRefinement(\n        SpcExecutor executor,\n        SpcExecutionOptions execOptions,\n        CsrGraph graph,\n        IReadOnlyList<SpcRunResult> coarseRuns,\n        AdaptiveSchedulerConfig cfg,\n        ref int totalCycles)\n    {\n        var coarseProfile = SweepProfile.From(coarseRuns);\n        var (lo, hi) = SpcProfileAnalysis.ComputeHalfMaximumBand(coarseProfile);\n        var grid = SpcScheduleHelpers.LogSpaceGrid(lo, hi, cfg.DenseSteps);\n\n        var tasks = new List<SpcTaskSpec>(grid.Length * cfg.DenseReplicas * cfg.DenseRounds);\n        for (int round = 0; round < cfg.DenseRounds; round++)\n        {\n            for (int rep = 0; rep < cfg.DenseReplicas; rep++)\n            {\n                foreach (double T in grid)\n                {\n                    tasks.Add(new SpcTaskSpec\n                    {\n                        Temperature    = T,\n                        ReplicaIndex   = rep,\n                        Cycles         = cfg.DenseCycles,\n                        BurnInCycles   = cfg.DenseBurn,\n                        Q              = cfg.Sampler.Q,\n                        ObservableTier = PottsObservableTier.Standard,\n                        BaseSeed       = SpcSeedHelper.Derive(cfg.BaseSeed, T, replica: rep, round: round),\n                        CheckpointPath = ProbeCheckpointPath(cfg.CheckpointDirectory, \"dense\", T, rep, round),\n                    });\n                }\n            }\n        }\n\n        SpcBatchResult batch = executor.RunBatch(graph, tasks, default, execOptions);\n        totalCycles += tasks.Count * (cfg.DenseBurn + cfg.DenseCycles);\n        return HydrateAlignedRuns(batch, tasks, \"dense\");\n    }\n\n    private static SpcRunResult RunFinalEquilibrium(\n        SpcExecutor executor,\n        SpcExecutionOptions execOptions,\n        CsrGraph graph, double T, AdaptiveSchedulerConfig cfg, ref int totalCycles)\n    {\n        var task = new SpcTaskSpec\n        {\n            Temperature    = T,\n            ReplicaIndex   = 0,\n            Cycles         = cfg.FinalCycles,\n            BurnInCycles   = cfg.FinalBurn,\n            Q              = cfg.Sampler.Q,\n            ObservableTier = cfg.FinalPassTier,\n\n            BaseSeed       = SpcSeedHelper.Derive(cfg.BaseSeed, T, replica: 0, round: -1),\n            CheckpointPath = FinalCheckpointPath(cfg.CheckpointDirectory, T),\n        };\n\n        SpcBatchResult batch = executor.RunBatch(graph, new[] { task }, default, execOptions);\n        totalCycles += cfg.FinalBurn + cfg.FinalCycles;\n\n        return batch.AlignedRuns[0]\n            ?? throw new InvalidOperationException(\n                \"Final-equilibrium task produced no result. Indicates a sink/path \" +\n                \"configuration issue.\");\n    }\n\n    private static List<SpcRunResult> HydrateAlignedRuns(\n        SpcBatchResult batch,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        string stageLabel)\n    {\n        var list = new List<SpcRunResult>(tasks.Count);\n        for (int i = 0; i < tasks.Count; i++)\n        {\n            SpcRunResult? r = batch.AlignedRuns[i];\n            if (r is null)\n            {\n                var task = tasks[i];\n                throw new InvalidOperationException(\n                    $\"AdaptiveSweepStrategy {stageLabel} task {i} \" +\n                    $\"(T={task.Temperature.ToString(\"G6\", CultureInfo.InvariantCulture)}, \" +\n                    $\"replica={task.ReplicaIndex}) produced no result and no cache hit.\");\n            }\n            list.Add(r);\n        }\n        return list;\n    }\n\n    private static IFrameSink ResolveSink(string? checkpointDirectory)\n        => checkpointDirectory is null\n            ? NullFrameSink.Instance\n            : SpcxDiskFrameSink.Instance;\n\n    private static string? ProbeCheckpointPath(string? root, string stage, double T, int replica, int round)\n    {\n        if (root is null) return null;\n        string fileName = $\"T_{T.ToString(\"F5\", CultureInfo.InvariantCulture)}_rep_{replica}_round_{round}.spcx\";\n        return Path.Combine(root, stage, fileName);\n    }\n\n    private static string? FinalCheckpointPath(string? root, double T)\n    {\n        if (root is null) return null;\n        string fileName = $\"T_{T.ToString(\"F5\", CultureInfo.InvariantCulture)}_final.spcx\";\n        return Path.Combine(root, \"final\", fileName);\n    }\n}
85 | clustering/graphical/spc/runtime/scheduling/FixedGridSweepStrategy.cs | {7129 599 0.3052 4.5818} | 7348 | using System;\nusing System.Collections.Generic;\nusing System.Diagnostics;\nusing System.Globalization;\nusing System.IO;\nusing Clustering.Graphical.SPC.Profiling;\nusing Clustering.Graphical.SPC.Profiling.Signals;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Execution.Sinks;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Scheduling;\n\npublic sealed class FixedGridSweepConfig\n{\n\n    public required IReadOnlyList<double> Temperatures { get; init; }\n\n    public int Replicas { get; init; } = 1;\n\n    public int BurnInCycles { get; init; } = 200;\n\n    public int MeasureCycles { get; init; } = 1000;\n\n    public PottsSamplerConfig Sampler { get; init; } = new();\n\n    public int FinalBurn { get; init; } = 1000;\n\n    public int FinalCycles { get; init; } = 5000;\n\n    public PottsObservableTier FinalPassTier { get; init; } = PottsObservableTier.WithEdgeObservables;\n\n    public int? BaseSeed { get; init; }\n\n    public int MaxDegreeOfParallelism { get; init; } = -1;\n\n    public string? CheckpointDirectory { get; init; }\n}\n\npublic sealed class FixedGridSweepStrategy : ISweepStrategy\n{\n    private readonly FixedGridSweepConfig _config;\n\n    public FixedGridSweepStrategy(FixedGridSweepConfig config)\n    {\n        ArgumentNullException.ThrowIfNull(config);\n        if (config.Temperatures.Count == 0)\n            throw new ArgumentException(\n                \"FixedGridSweepConfig.Temperatures must be non-empty.\", nameof(config));\n        _config = config;\n    }\n\n    public SweepResult Run(CsrGraph graph, ISignalAnalyzer? analyzer = null)\n    {\n        var cfg = _config;\n        var signalAnalyzer = analyzer ?? new ChiPeakSignalAnalyzer();\n        var sw = Stopwatch.StartNew();\n\n        if (graph.NodeCount < 2)\n            return SweepKernel.BuildTrivialResult(graph, cfg.Sampler.Q, sw.Elapsed);\n\n        var executor = new SpcExecutor();\n        var execOptions = new SpcExecutionOptions\n        {\n            FrameSink              = ResolveSink(cfg.CheckpointDirectory),\n            MaxDegreeOfParallelism = cfg.MaxDegreeOfParallelism,\n        };\n\n        int tCount = cfg.Temperatures.Count;\n        var probeTasks = new List<SpcTaskSpec>(tCount * cfg.Replicas);\n        for (int rep = 0; rep < cfg.Replicas; rep++)\n        {\n            for (int tIdx = 0; tIdx < tCount; tIdx++)\n            {\n                double T = cfg.Temperatures[tIdx];\n                probeTasks.Add(new SpcTaskSpec\n                {\n                    Temperature    = T,\n                    ReplicaIndex   = rep,\n                    Cycles         = cfg.MeasureCycles,\n                    BurnInCycles   = cfg.BurnInCycles,\n                    Q              = cfg.Sampler.Q,\n                    ObservableTier = PottsObservableTier.Standard,\n                    BaseSeed       = SpcSeedHelper.Derive(cfg.BaseSeed, T, replica: rep, round: 0),\n                    CheckpointPath = ProbeCheckpointPath(cfg.CheckpointDirectory, T, rep),\n                });\n            }\n        }\n\n        var stageTimings = new Dictionary<string, TimeSpan>(StringComparer.Ordinal);\n        var stageSw = Stopwatch.StartNew();\n        SpcBatchResult probeBatch = executor.RunBatch(graph, probeTasks, default, execOptions);\n        var sweepRuns = HydrateAlignedRuns(probeBatch, probeTasks, \"probe\");\n        stageTimings[\"probes\"] = stageSw.Elapsed;\n\n        var profile = SweepProfile.From(sweepRuns);\n        double chosenT = SpcProfileAnalysis.PickPeakTemperature(profile);\n        double stability = SpcProfileAnalysis.ComputeStability(profile);\n\n        stageSw.Restart();\n        var finalTask = new SpcTaskSpec\n        {\n            Temperature    = chosenT,\n            ReplicaIndex   = 0,\n            Cycles         = cfg.FinalCycles,\n            BurnInCycles   = cfg.FinalBurn,\n            Q              = cfg.Sampler.Q,\n            ObservableTier = cfg.FinalPassTier,\n            BaseSeed       = SpcSeedHelper.Derive(cfg.BaseSeed, chosenT, replica: 0, round: -1),\n            CheckpointPath = FinalCheckpointPath(cfg.CheckpointDirectory, chosenT),\n        };\n        SpcBatchResult finalBatch = executor.RunBatch(graph, new[] { finalTask }, default, execOptions);\n        SpcRunResult finalRun = finalBatch.AlignedRuns[0]\n            ?? throw new InvalidOperationException(\n                \"Final-equilibrium task produced no result. This indicates a sink configuration \" +\n                \"issue — disk sink + a missing checkpoint path, or a hydration failure.\");\n        stageTimings[\"final_equilibrium\"] = stageSw.Elapsed;\n\n        int totalCycles = probeTasks.Count * (cfg.BurnInCycles + cfg.MeasureCycles)\n                        + cfg.FinalBurn + cfg.FinalCycles;\n\n        ProfileCriteria criteria = signalAnalyzer.Analyze(profile);\n\n        return new SweepResult\n        {\n            Summary = new SweepSummary\n            {\n                SubgraphNodes     = graph.NodeCount,\n                SubgraphEdges     = graph.Targets.Length / 2,\n                ChosenTemperature = chosenT,\n                StabilityScore    = stability,\n                TotalCyclesUsed   = totalCycles,\n                EarlyStopped      = false,\n                Elapsed           = sw.Elapsed,\n                StageTimings      = stageTimings,\n                Profile           = profile,\n            },\n            SweepRuns       = sweepRuns,\n            FinalRun        = finalRun,\n            ProfileCriteria = criteria,\n            EdgeObservables = finalRun.EdgeObservables,\n        };\n    }\n\n    private static List<SpcRunResult> HydrateAlignedRuns(\n        SpcBatchResult batch,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        string stageLabel)\n    {\n        var list = new List<SpcRunResult>(tasks.Count);\n        for (int i = 0; i < tasks.Count; i++)\n        {\n            SpcRunResult? r = batch.AlignedRuns[i];\n            if (r is null)\n            {\n                var task = tasks[i];\n                throw new InvalidOperationException(\n                    $\"FixedGridSweepStrategy {stageLabel} task {i} \" +\n                    $\"(T={task.Temperature.ToString(\"G6\", CultureInfo.InvariantCulture)}, \" +\n                    $\"replica={task.ReplicaIndex}) produced no result and no cache hit.\");\n            }\n            list.Add(r);\n        }\n        return list;\n    }\n\n    private static IFrameSink ResolveSink(string? checkpointDirectory)\n        => checkpointDirectory is null\n            ? NullFrameSink.Instance\n            : SpcxDiskFrameSink.Instance;\n\n    private static string? ProbeCheckpointPath(string? root, double T, int replica)\n    {\n        if (root is null) return null;\n        string fileName = $\"T_{T.ToString(\"F5\", CultureInfo.InvariantCulture)}_rep_{replica}.spcx\";\n        return Path.Combine(root, \"probes\", fileName);\n    }\n\n    private static string? FinalCheckpointPath(string? root, double T)\n    {\n        if (root is null) return null;\n        string fileName = $\"T_{T.ToString(\"F5\", CultureInfo.InvariantCulture)}_final.spcx\";\n        return Path.Combine(root, \"final\", fileName);\n    }\n}
86 | clustering/graphical/spc/runtime/scheduling/ISweepStrategy.cs | {239 18 0.1004 4.7673} | 248 | using Clustering.Graphical.SPC.Profiling.Signals;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Scheduling;\n\npublic interface ISweepStrategy\n{\n\n    SweepResult Run(CsrGraph graph, ISignalAnalyzer? analyzer = null);\n}
87 | clustering/graphical/spc/runtime/scheduling/SpcScheduleHelpers.cs | {5320 506 0.2748 4.6811} | 5474 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.Evaluation.External;\nusing Clustering.Graphical.SPC.Partitions.Strategies;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Diagnostics;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Scheduling;\n\npublic static class SpcScheduleHelpers\n{\n    public static double[] LogSpaceGrid(double lo, double hi, int steps)\n    {\n        if (steps <= 1) return new[] { 0.5 * (lo + hi) };\n        if (lo <= 0.0 || hi <= 0.0)\n            throw new ArgumentException(\"LogSpaceGrid requires strictly positive endpoints.\");\n        if (hi <= lo) (lo, hi) = (hi, lo);\n\n        double logLo = Math.Log(lo);\n        double logHi = Math.Log(hi);\n        var grid = new double[steps];\n        for (int i = 0; i < steps; i++)\n            grid[i] = Math.Exp(logLo + i * (logHi - logLo) / (steps - 1));\n        return grid;\n    }\n\n    public static double Estimate(CsrGraph graph, int q)\n    {\n        if (graph.NodeCount == 0 || graph.Targets.Length == 0 || q < 2)\n            return 1.0;\n\n        EdgeWeightSummary weights = EdgeWeights.Summary(graph);\n        double meanWeightedDegree = 2.0 * weights.MeanWeight * weights.EdgeCount / graph.NodeCount;\n        double logQ = Math.Log(q);\n        if (logQ <= 0.0 || weights.MedianWeight <= 0.0)\n            return 1.0;\n\n        return weights.MedianWeight * meanWeightedDegree / logQ;\n    }\n\n    public static (double TempMin, double TempMax) EstimateBracket(\n        CsrGraph graph,\n        int q,\n        double coldOvershoot = 0.05,\n        double hotOvershoot = 5.0)\n    {\n        if (coldOvershoot <= 0.0)\n            throw new ArgumentOutOfRangeException(nameof(coldOvershoot));\n        if (hotOvershoot <= coldOvershoot)\n            throw new ArgumentException(\"hotOvershoot must exceed coldOvershoot.\");\n\n        double tc = Estimate(graph, q);\n        return (tc * coldOvershoot, tc * hotOvershoot);\n    }\n\n    public static IReadOnlyList<AdaptiveSchedulePartitionRollup> BuildPartitionScheduleRollups(\n        CsrGraph graph,\n        IEnumerable<PottsModelEdgeObservables> observables,\n        int[][] groundTruthLevels,\n        double theta = 0.5,\n        IReadOnlyList<string>? levelNames = null)\n    {\n        ValidateGraph(graph, nameof(graph));\n        if (observables is null) throw new ArgumentNullException(nameof(observables));\n        if (groundTruthLevels is null) throw new ArgumentNullException(nameof(groundTruthLevels));\n\n        levelNames ??= new[] { \"coarse\", \"medium\", \"fine\" };\n\n        var grouped = observables\n            .GroupBy(o => o.Temperature)\n            .OrderByDescending(g => g.Key);\n\n        var rollups = new List<AdaptiveSchedulePartitionRollup>();\n        foreach (var group in grouped)\n        {\n            var replicas = group.ToList();\n            int csrLength = replicas[0].SpinAgreementCount.Length;\n            var pooledSpin = new int[csrLength];\n            int pooledCycles = 0;\n\n            foreach (var e in replicas)\n            {\n                pooledCycles += e.CycleCount;\n                for (int i = 0; i < csrLength; i++)\n                    pooledSpin[i] += e.SpinAgreementCount[i];\n            }\n\n            var partition = ThresholdSpinAgreement.Apply(graph, pooledSpin, pooledCycles, theta);\n            var purities = new double[groundTruthLevels.Length];\n            for (int li = 0; li < groundTruthLevels.Length; li++)\n                purities[li] = Purity.Compute(partition.Labels, groundTruthLevels[li]);\n\n            rollups.Add(new AdaptiveSchedulePartitionRollup(\n                group.Key,\n                replicas.Count,\n                partition.ClusterCount,\n                pooledCycles,\n                purities,\n                levelNames.Take(groundTruthLevels.Length).ToArray()));\n        }\n\n        return rollups;\n    }\n\n    private static void ValidateGraph(CsrGraph graph, string paramName)\n    {\n        if (graph.Targets is null)\n            throw new ArgumentException(\"CSR graph Targets buffer must be initialized.\", paramName);\n        if (graph.Weights is null)\n            throw new ArgumentException(\"CSR graph Weights buffer must be initialized.\", paramName);\n        if (graph.RowPointers is null)\n            throw new ArgumentException(\"CSR graph RowPointers buffer must be initialized.\", paramName);\n        if (graph.NodeCount < 0)\n            throw new ArgumentException(\"CSR graph NodeCount must be non-negative.\", paramName);\n        if (graph.RowPointers.Length != graph.NodeCount + 1)\n            throw new ArgumentException($\"CSR graph RowPointers length must equal NodeCount + 1 ({graph.NodeCount + 1}).\", paramName);\n        if (graph.Targets.Length != graph.Weights.Length)\n            throw new ArgumentException(\"CSR graph Targets and Weights must have the same length.\", paramName);\n        if (graph.RowPointers.Length > 0 && graph.RowPointers[^1] != graph.Targets.Length)\n            throw new ArgumentException(\"CSR graph RowPointers must end at the number of target entries.\", paramName);\n        for (int i = 1; i < graph.RowPointers.Length; i++)\n        {\n            if (graph.RowPointers[i] < graph.RowPointers[i - 1])\n                throw new ArgumentException(\"CSR graph RowPointers must be non-decreasing.\", paramName);\n        }\n    }\n}
88 | clustering/graphical/spc/runtime/scheduling/SpcSeedHelper.cs | {571 53 0.2382 4.4932} | 591 | using System;\n\nnamespace Clustering.Graphical.SPC.Runtime.Scheduling;\n\npublic static class SpcSeedHelper\n{\n\n    public const int DefaultTemperatureQuantizationFactor = 1000;\n\n    public static int? Derive(\n        int? baseSeed,\n        double temperature,\n        int replica,\n        int round = 0,\n        int quantizationFactor = DefaultTemperatureQuantizationFactor)\n    {\n        if (baseSeed is null) return null;\n        int tQuantized = (int)(temperature * quantizationFactor);\n        return HashCode.Combine(baseSeed.Value, tQuantized, replica, round);\n    }\n}
89 | clustering/graphical/spc/runtime/scheduling/SweepKernel.cs | {4269 296 0.4854 3.7439} | 4397 | using System;\nusing System.Collections.Generic;\nusing Clustering.Graphical.SPC.Profiling;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Scheduling;\n\npublic static class SweepKernel\n{\n\n    public static SpcRunResult RunProbePoint(\n        CsrGraph graph,\n        double   T,\n        int      q,\n        int      burnCycles,\n        int      measureCycles,\n        int?     seed)\n    {\n        var runResult = PottsRunner.Run(new PottsRunSpec\n        {\n            Graph          = graph,\n            Temperature    = T,\n            Q              = q,\n            ObservableTier = PottsObservableTier.Standard,\n            Seed           = seed,\n            BurnInCycles   = burnCycles,\n            Cycles         = measureCycles,\n            ReplicaIndex   = 0,\n        });\n\n        return new SpcRunResult\n        {\n            Graph           = graph,\n            Frame           = runResult.Frame,\n            EdgeObservables = null,\n        };\n    }\n\n    public static SpcRunResult RunFinalEquilibrium(\n        CsrGraph            graph,\n        double              T,\n        int                 q,\n        int                 burnCycles,\n        int                 measureCycles,\n        PottsObservableTier tier,\n        int?                seed)\n    {\n        var runResult = PottsRunner.Run(new PottsRunSpec\n        {\n            Graph          = graph,\n            Temperature    = T,\n            Q              = q,\n            ObservableTier = tier,\n            Seed           = seed,\n            BurnInCycles   = burnCycles,\n            Cycles         = measureCycles,\n            ReplicaIndex   = 0,\n        });\n\n        if (tier == PottsObservableTier.WithEdgeObservables && runResult.EdgeObservables is null)\n        {\n            throw new InvalidOperationException(\n                \"WithEdgeObservables specialization did not produce edge observables — unexpected.\");\n        }\n\n        return new SpcRunResult\n        {\n            Graph           = graph,\n            Frame           = runResult.Frame,\n            EdgeObservables = runResult.EdgeObservables,\n        };\n    }\n\n    public static SweepResult BuildTrivialResult(\n        CsrGraph graph,\n        int      q,\n        TimeSpan elapsed)\n    {\n        return new SweepResult\n        {\n            Summary = new SweepSummary\n            {\n                SubgraphNodes = graph.NodeCount,\n                SubgraphEdges = 0,\n                Elapsed       = elapsed,\n                Profile       = SweepProfile.Empty,\n            },\n            SweepRuns = Array.Empty<SpcRunResult>(),\n            FinalRun = new SpcRunResult\n            {\n                Graph = graph,\n                Frame = new PottsModelCoreObservables\n                {\n                    Temperature                  = 0.0,\n                    Q                            = q,\n                    ObservableTier               = PottsObservableTier.Standard,\n                    CycleCount                   = 0,\n                    Spins                        = Array.Empty<int>(),\n                    ClusterSizeHistogram         = Array.Empty<int>(),\n                    RngState0                    = 0UL,\n                    RngState1                    = 0UL,\n                    RngState2                    = 0UL,\n                    RngState3                    = 0UL,\n                    RunningSumSqClusterSizes     = 0.0,\n                    RunningSumSqClusterSizesExcl = 0.0,\n                    RunningSumEnergy             = 0.0,\n                    RunningSumEnergySq           = 0.0,\n                    RunningSumMag                = 0.0,\n                    RunningSumMagSq              = 0.0,\n                },\n                EdgeObservables = null,\n            },\n            ProfileCriteria = new ProfileCriteria(\n                AnchorTemperature:   0.0,\n                AnchorBand:          (0.0, 0.0),\n                RefinedTemperature:  0.0,\n                CorroborationScore:  0.0,\n                Enrichments:         new Dictionary<string, double>()),\n            EdgeObservables = null,\n        };\n    }\n}
