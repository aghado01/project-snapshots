idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
27 | clustering/graphical/spc/export/SpcTabularProjections.cs | {12012 860 0.2694 4.6597} | 12416 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.Graphical.SPC.Partitions;\nusing Clustering.Graphical.SPC.Profiling;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Clustering.Graphical.SPC.Runtime.Scheduling;\nusing Archivory.Tabular;\nusing Graphs.Primitives;\nusing Maths.Information;\n\nnamespace Clustering.Graphical.SPC.Export;\n\npublic static class SpcTabularProjections\n{\n    public static TabularProjection CreateSweepProfileProjection(\n        SweepProfile profile,\n        string tableName = \"spc_sweep\")\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n\n        return TabularProjectionFactory.CreateIndexed(tableName, profile.Count)\n            .Column(\"T\",            i => profile.Temperatures[i])\n            .Column(\"Chi\",          i => profile.Susceptibility[i])\n            .Column(\"Cv\",           i => profile.SpecificHeat[i])\n            .Column(\"LabelEntropy\", i => profile.LabelEntropy[i])\n            .ColumnIf(profile.BondEntropy.HasValue, \"BondEntropy\", _ => profile.BondEntropy!.Value)\n            .ColumnsFromDictionary(profile.AdditionalChannels, (channel, i) => channel[i])\n            .Build();\n    }\n\n    public static TabularProjection CreatePartitionProjection(\n        Partition partition,\n        double[][]? features = null,\n        int[]? trueLabels = null,\n        string tableName = \"spc_partition\")\n    {\n        if (partition is null)\n            throw new ArgumentNullException(nameof(partition));\n        if (features is not null && features.Length != partition.Labels.Length)\n            throw new ArgumentException(\"Feature rows must match partition node count.\", nameof(features));\n        if (trueLabels is not null && trueLabels.Length != partition.Labels.Length)\n            throw new ArgumentException(\"True label length must match partition node count.\", nameof(trueLabels));\n\n        int dimension = features?.Length > 0 ? features[0].Length : 0;\n        if (features is not null)\n        {\n            foreach (var featureRow in features)\n            {\n                if (featureRow.Length != dimension)\n                    throw new ArgumentException(\"All feature rows must have the same length.\", nameof(features));\n            }\n        }\n\n        var featureCols = features is null\n            ? Array.Empty<(string Name, Func<int, object?> Selector)>()\n            : Enumerable.Range(0, dimension)\n                .Select(d => ($\"x{d}\", (Func<int, object?>)(i => features[i][d])))\n                .ToArray();\n\n        return TabularProjectionFactory.CreateIndexed(tableName, partition.Labels.Length)\n            .Column(\"node_index\", i => i)\n            .Columns(featureCols)\n            .ColumnIf(trueLabels is not null, \"true_label\", i => trueLabels![i])\n            .Column(\"spc_label\", i => partition.Labels[i])\n            .Build();\n    }\n\n    public static TabularProjection CreateDatasetProjection(\n        double[][] features,\n        int[] labels,\n        string tableName = \"spc_dataset\")\n    {\n        if (features is null)\n            throw new ArgumentNullException(nameof(features));\n        if (labels is null)\n            throw new ArgumentNullException(nameof(labels));\n        if (features.Length != labels.Length)\n            throw new ArgumentException(\"Feature rows must match label count.\", nameof(labels));\n\n        int dimension = features.Length > 0 ? features[0].Length : 0;\n        for (int i = 0; i < features.Length; i++)\n        {\n            if (features[i] is null)\n                throw new ArgumentException($\"Feature row {i} cannot be null.\", nameof(features));\n            if (features[i].Length != dimension)\n                throw new ArgumentException(\"All feature rows must have the same length.\", nameof(features));\n        }\n\n        var featureCols = Enumerable.Range(0, dimension)\n            .Select(d => ($\"x{d}\", (Func<int, object?>)(i => features[i][d])))\n            .ToArray();\n\n        return TabularProjectionFactory.CreateIndexed(tableName, features.Length)\n            .Column(\"Index\", i => i)\n            .Columns(featureCols)\n            .Column(\"label\", i => labels[i])\n            .Build();\n    }\n\n    public static TabularProjection CreateCriteriaProjection(\n        ProfileCriteria criteria,\n        string tableName = \"spc_criteria\")\n    {\n        if (criteria is null)\n            throw new ArgumentNullException(nameof(criteria));\n\n        return TabularProjectionFactory.CreateScalar(tableName)\n            .Column(\"AnchorTemperature\",  criteria.AnchorTemperature)\n            .Column(\"AnchorBandLo\",       criteria.AnchorBand.Lo)\n            .Column(\"AnchorBandHi\",       criteria.AnchorBand.Hi)\n            .Column(\"RefinedTemperature\", criteria.RefinedTemperature)\n            .Column(\"CorroborationScore\", criteria.CorroborationScore)\n            .ColumnsFromDictionary(criteria.Enrichments)\n            .Build();\n    }\n\n    public static TabularProjection CreateSessionSummaryProjection(\n        SpcSessionResult result,\n        string tableName = \"spc_session\")\n    {\n        if (result is null)\n            throw new ArgumentNullException(nameof(result));\n\n        return TabularProjectionFactory.CreateScalar(tableName)\n            .Column(\"AnchorTemperature\",  result.ProfileCriteria.AnchorTemperature)\n            .Column(\"AnchorBandLo\",       result.ProfileCriteria.AnchorBand.Lo)\n            .Column(\"AnchorBandHi\",       result.ProfileCriteria.AnchorBand.Hi)\n            .Column(\"RefinedTemperature\", result.ProfileCriteria.RefinedTemperature)\n            .Column(\"CorroborationScore\", result.ProfileCriteria.CorroborationScore)\n            .Column(\"ClusterCount\",       result.Partition.ClusterCount)\n            .Column(\"SubgraphNodes\",      result.ScheduleSummary.SubgraphNodes)\n            .Column(\"SubgraphEdges\",      result.ScheduleSummary.SubgraphEdges)\n            .Column(\"ChosenTemperature\",  result.ScheduleSummary.ChosenTemperature)\n            .Column(\"StabilityScore\",     result.ScheduleSummary.StabilityScore)\n            .Column(\"TotalCyclesUsed\",    result.ScheduleSummary.TotalCyclesUsed)\n            .Column(\"EarlyStopped\",       result.ScheduleSummary.EarlyStopped)\n            .Column(\"ElapsedSeconds\",     result.ScheduleSummary.Elapsed.TotalSeconds)\n            .ColumnIf(result.Profile.BondEntropy.HasValue, \"BondEntropy\", result.Profile.BondEntropy)\n            .ColumnsFromDictionary(result.ProfileCriteria.Enrichments)\n            .ColumnsFromDictionary(result.EvaluatorScores)\n            .Build();\n    }\n\n    public static TabularProjection CreatePartitionScheduleRollupProjection(\n        IReadOnlyList<AdaptiveSchedulePartitionRollup> rollups,\n        string tableName = \"spc_partition_schedule\")\n    {\n        if (rollups is null)\n            throw new ArgumentNullException(nameof(rollups));\n\n        var levelNames = rollups.Count > 0\n            ? rollups[0].LevelNames\n            : Array.Empty<string>();\n\n        foreach (var rollup in rollups)\n        {\n            if (rollup.LevelNames.Count != levelNames.Count)\n                throw new ArgumentException(\"All rollups must have the same level names.\", nameof(rollups));\n            if (rollup.Purities.Count != levelNames.Count)\n                throw new ArgumentException(\"Rollup purity vector length must match level names.\", nameof(rollups));\n        }\n\n        var purityCols = Enumerable.Range(0, levelNames.Count)\n            .Select(level => ($\"purity_{levelNames[level]}\", (Func<int, object?>)(i => rollups[i].Purities[level])))\n            .ToArray();\n\n        return TabularProjectionFactory.CreateIndexed(tableName, rollups.Count)\n            .Column(\"T\",             i => rollups[i].Temperature)\n            .Column(\"replicas\",      i => rollups[i].ReplicaCount)\n            .Column(\"clusters\",      i => rollups[i].ClusterCount)\n            .Column(\"pooled_cycles\", i => rollups[i].PooledCycleCount)\n            .Columns(purityCols)\n            .Build();\n    }\n\n    public static TabularProjection CreateReplicaTracesProjection(\n        IReadOnlyList<SpcRunResult> runs,\n        string tableName = \"spc_replica_traces\")\n    {\n        if (runs is null)\n            throw new ArgumentNullException(nameof(runs));\n\n        return TabularProjectionFactory.CreateIndexed(tableName, runs.Count)\n            .Column(\"temperature\",            i => runs[i].Frame.Temperature)\n            .Column(\"replica_index\",          i => runs[i].Frame.ReplicaIndex)\n            .Column(\"cycle_count\",            i => runs[i].Frame.CycleCount)\n            .Column(\"chi_fk\",                 i => ChiFK(runs[i].Frame))\n            .Column(\"cv\",                     i => SpecificHeat(runs[i].Frame))\n            .Column(\"label_entropy\",          i => LabelEntropy(runs[i].Frame))\n            .Column(\"mean_energy\",            i => MeanEnergy(runs[i].Frame))\n            .Column(\"mean_magnetization\",     i => MeanMag(runs[i].Frame))\n            .Column(\"magnetization_variance\", i => MagVariance(runs[i].Frame))\n            .Build();\n    }\n\n    private static double ChiFK(PottsModelCoreObservables f)\n    {\n        if (f.CycleCount <= 0 || f.Spins.Length == 0) return 0.0;\n        return f.RunningSumSqClusterSizes / ((double)f.CycleCount * f.Spins.Length);\n    }\n\n    private static double SpecificHeat(PottsModelCoreObservables f)\n    {\n        if (f.CycleCount <= 0 || f.Temperature <= 0.0) return 0.0;\n        double meanE   = f.RunningSumEnergy   / f.CycleCount;\n        double meanE2  = f.RunningSumEnergySq / f.CycleCount;\n        double varE    = Math.Max(0.0, meanE2 - meanE * meanE);\n        return varE / (f.Temperature * f.Temperature);\n    }\n\n    private static double LabelEntropy(PottsModelCoreObservables f)\n        => Shannon.EntropyNats(f.ClusterSizeHistogram);\n\n    private static double MeanEnergy(PottsModelCoreObservables f)\n        => f.CycleCount > 0 ? f.RunningSumEnergy / f.CycleCount : 0.0;\n\n    private static double MeanMag(PottsModelCoreObservables f)\n        => f.CycleCount > 0 ? f.RunningSumMag / f.CycleCount : 0.0;\n\n    private static double MagVariance(PottsModelCoreObservables f)\n    {\n        if (f.CycleCount <= 0) return 0.0;\n        double m  = f.RunningSumMag   / f.CycleCount;\n        double m2 = f.RunningSumMagSq / f.CycleCount;\n        return Math.Max(0.0, m2 - m * m);\n    }\n\n    public static TabularProjection CreateFinalEdgesProjection(\n        CsrGraph graph,\n        PottsModelEdgeObservables edges,\n        string tableName = \"spc_final_edges\")\n    {\n        ArgumentNullException.ThrowIfNull(graph);\n        ArgumentNullException.ThrowIfNull(edges);\n        if (edges.CycleCount <= 0)\n            throw new ArgumentException(\"EdgeObservables.CycleCount must be positive.\", nameof(edges));\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new ArgumentException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not match \" +\n                $\"CSR slot count ({graph.Targets.Length}).\", nameof(edges));\n\n        var rows = new List<(int Slot, int I, int J)>(graph.Targets.Length / 2);\n        int n = graph.NodeCount;\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                rows.Add((e, i, j));\n            }\n        }\n        double cyc = edges.CycleCount;\n\n        return TabularProjectionFactory.CreateIndexed(tableName, rows.Count)\n            .Column(\"source\",         k => rows[k].I)\n            .Column(\"target\",         k => rows[k].J)\n            .Column(\"weight\",         k => graph.Weights[rows[k].Slot])\n            .Column(\"bond_frequency\", k => edges.BondFormedCount[rows[k].Slot] / cyc)\n            .Column(\"spin_agreement\", k => edges.SpinAgreementCount[rows[k].Slot] / cyc)\n            .Build();\n    }\n}
28 | clustering/graphical/spc/observables/projections/BondFrequencyBinaryEntropySum.cs | {1949 186 0.2996 4.6277} | 2010 | using System;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Observables.Projections;\n\npublic sealed class BondFrequencyBinaryEntropySum : IGraphSignal\n{\n    private static readonly double Ln2 = Math.Log(2.0);\n\n    public double[] Compute(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(BondFrequencyBinaryEntropySum)} requires Tier 1 edge observables.\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        if (edges.CycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {edges.CycleCount}.\");\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        double invCycles = 1.0 / edges.CycleCount;\n        var result = new double[n];\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                double p = edges.BondFormedCount[e] * invCycles;\n                double hBits = BinaryEntropyNats(p) / Ln2;\n                result[i] += hBits;\n                result[j] += hBits;\n            }\n        }\n        return result;\n    }\n\n    private static double BinaryEntropyNats(double p)\n    {\n        if (p <= 0.0 || p >= 1.0) return 0.0;\n        return -(p * Math.Log(p) + (1.0 - p) * Math.Log(1.0 - p));\n    }\n}
29 | clustering/graphical/spc/observables/projections/BondFrequencyDegree.cs | {1749 159 0.3002 4.5779} | 1804 | using System;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Observables.Projections;\n\npublic sealed class BondFrequencyDegree : IGraphSignal\n{\n    public double[] Compute(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(BondFrequencyDegree)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null.\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        if (edges.CycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {edges.CycleCount}.\");\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        double invCycles = 1.0 / edges.CycleCount;\n        var result = new double[n];\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;   // walk only the upper triangle (where SW writes)\n                double freq = edges.BondFormedCount[e] * invCycles;\n                result[i] += freq;\n                result[j] += freq;\n            }\n        }\n        return result;\n    }\n}
30 | clustering/graphical/spc/observables/projections/IGraphSignal.cs | {185 12 0.0919 4.8178} | 192 | using Clustering.Graphical.SPC.Runtime.Execution;\n\nnamespace Clustering.Graphical.SPC.Observables.Projections;\n\npublic interface IGraphSignal\n{\n    double[] Compute(SpcRunResult run);\n}
31 | clustering/graphical/spc/observables/projections/SpinCorrelationEigenvectorCentrality.cs | {2901 302 0.3047 4.5647} | 2993 | using System;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\nusing Maths.LinAlg;\n\nnamespace Clustering.Graphical.SPC.Observables.Projections;\n\npublic sealed class SpinCorrelationEigenvectorCentrality : IGraphSignal\n{\n\n    public int MaxSweeps { get; init; } = 256;\n\n    public double Tolerance { get; init; } = 1e-12;\n\n    public DenseEigenOptions EigenOptions { get; init; } = default;\n\n    public double[] Compute(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(SpinCorrelationEigenvectorCentrality)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null. Configure the runner to track edge \" +\n                \"observables (e.g. PottsObservableTier.WithEdgeObservables).\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        if (edges.CycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {edges.CycleCount}.\");\n        if (edges.SpinAgreementCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"SpinAgreementCount length ({edges.SpinAgreementCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        if (n == 0) return Array.Empty<double>();\n        if (n == 1) return new double[] { 1.0 };\n\n        var g = new double[n, n];\n        double invCycles = 1.0 / edges.CycleCount;\n\n        for (int i = 0; i < n; i++)\n            g[i, i] = 1.0;  // self-agreement (correlation-matrix convention)\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                double gij = edges.SpinAgreementCount[e] * invCycles;\n                g[i, j] = gij;\n                g[j, i] = gij;\n            }\n        }\n\n        EigenResult eig = DenseEigen.DecomposeSymmetric(g, MaxSweeps, Tolerance, EigenOptions);\n        double[] top = eig.Eigenvectors[0];\n        if (top.Length != n)\n            throw new InvalidOperationException(\n                $\"Eigenvector length ({top.Length}) does not match node count ({n}).\");\n\n        double signMass = 0.0;\n        for (int i = 0; i < n; i++) signMass += top[i];\n        if (signMass < 0.0)\n        {\n            var flipped = new double[n];\n            for (int i = 0; i < n; i++) flipped[i] = -top[i];\n            return flipped;\n        }\n\n        var result = new double[n];\n        Array.Copy(top, result, n);\n        return result;\n    }\n}
32 | clustering/graphical/spc/observables/reductions/BondFrequency.cs | {627 52 0.2089 4.7557} | 646 | using Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Maths.Information;\n\nnamespace Clustering.Graphical.SPC.Observables.Reductions;\n\npublic static class BondFrequencyReduction\n{\n    public static double EntropyNats(PottsModelEdgeObservables edges)\n    {\n        if (edges.CycleCount <= 0)\n            return 0.0;\n\n        double invCycles = 1.0 / edges.CycleCount;\n        var frequencies = new double[edges.BondFormedCount.Length];\n        for (int i = 0; i < edges.BondFormedCount.Length; i++)\n            frequencies[i] = edges.BondFormedCount[i] * invCycles;\n\n        return Shannon.EntropyNats(frequencies);\n    }\n}
33 | clustering/graphical/spc/observables/reductions/LabelEntropy.cs | {1746 134 0.2749 4.5868} | 1795 | using System;\nusing System.Collections.Generic;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Maths.Information;\n\nnamespace Clustering.Graphical.SPC.Observables.Reductions;\n\npublic readonly record struct LabelEntropyCurve(\n    double[] Temperatures,\n    double[] Entropy);\n\npublic static class LabelEntropy\n{\n    public static LabelEntropyCurve From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames.Count == 0)\n            return new LabelEntropyCurve(Array.Empty<double>(), Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, int[]>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelCoreObservables frame = frames[i];\n            if (!grouped.TryGetValue(frame.Temperature, out int[]? histogram))\n            {\n                histogram = new int[frame.ClusterSizeHistogram.Length];\n                grouped[frame.Temperature] = histogram;\n            }\n            else if (histogram.Length < frame.ClusterSizeHistogram.Length)\n            {\n                Array.Resize(ref histogram, frame.ClusterSizeHistogram.Length);\n                grouped[frame.Temperature] = histogram;\n            }\n\n            for (int j = 0; j < frame.ClusterSizeHistogram.Length; j++)\n                histogram[j] += frame.ClusterSizeHistogram[j];\n        }\n\n        var temperatures = new double[grouped.Count];\n        var entropy = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, int[] histogram) in grouped)\n        {\n            temperatures[index] = temperature;\n            entropy[index] = Shannon.EntropyNats(histogram);\n            index++;\n        }\n\n        return new LabelEntropyCurve(temperatures, entropy);\n    }\n}
34 | clustering/graphical/spc/observables/reductions/Magnetization.cs | {2765 232 0.2948 4.4799} | 2838 | using System;\nusing System.Collections.Generic;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.Graphical.SPC.Observables.Reductions;\n\npublic readonly record struct MagnetizationCurves(\n    double[] Temperatures,\n    double[] Mean,\n    double[] SecondMoment,\n    double[] Variance);\n\npublic static class Magnetization\n{\n    public static MagnetizationCurves From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames is null)\n            throw new ArgumentNullException(nameof(frames));\n\n        if (frames.Count == 0)\n            return new MagnetizationCurves(\n                Array.Empty<double>(),\n                Array.Empty<double>(),\n                Array.Empty<double>(),\n                Array.Empty<double>());\n\n        var groupedMean = new SortedDictionary<double, (double Sum, int Count)>();\n        var groupedSecondMoment = new SortedDictionary<double, (double Sum, int Count)>();\n\n        foreach (var frame in frames)\n        {\n            double meanMag = frame.CycleCount > 0\n                ? frame.RunningSumMag / frame.CycleCount\n                : 0.0;\n            double meanMagSq = frame.CycleCount > 0\n                ? frame.RunningSumMagSq / frame.CycleCount\n                : 0.0;\n\n            if (groupedMean.TryGetValue(frame.Temperature, out var accMean))\n                groupedMean[frame.Temperature] = (accMean.Sum + meanMag, accMean.Count + 1);\n            else\n                groupedMean[frame.Temperature] = (meanMag, 1);\n\n            if (groupedSecondMoment.TryGetValue(frame.Temperature, out var accSecond))\n                groupedSecondMoment[frame.Temperature] = (accSecond.Sum + meanMagSq, accSecond.Count + 1);\n            else\n                groupedSecondMoment[frame.Temperature] = (meanMagSq, 1);\n        }\n\n        int count = groupedMean.Count;\n        var temperatures = new double[count];\n        var means = new double[count];\n        var secondMoments = new double[count];\n        var variances = new double[count];\n        int index = 0;\n\n        foreach (var temperature in groupedMean.Keys)\n        {\n            var meanAcc = groupedMean[temperature];\n            var secondAcc = groupedSecondMoment[temperature];\n            double mean = meanAcc.Count > 0 ? meanAcc.Sum / meanAcc.Count : 0.0;\n            double secondMoment = secondAcc.Count > 0 ? secondAcc.Sum / secondAcc.Count : 0.0;\n            double variance = Math.Max(0.0, secondMoment - mean * mean);\n\n            temperatures[index] = temperature;\n            means[index] = mean;\n            secondMoments[index] = secondMoment;\n            variances[index] = variance;\n            index++;\n        }\n\n        return new MagnetizationCurves(temperatures, means, secondMoments, variances);\n    }\n}
35 | clustering/graphical/spc/observables/reductions/MeanEnergy.cs | {1507 133 0.2814 4.5464} | 1552 | using System;\nusing System.Collections.Generic;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.Graphical.SPC.Observables.Reductions;\n\npublic readonly record struct MeanEnergyCurve(\n    double[] Temperatures,\n    double[] Energy);\n\npublic static class MeanEnergy\n{\n    public static MeanEnergyCurve From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames is null)\n            throw new ArgumentNullException(nameof(frames));\n\n        if (frames.Count == 0)\n            return new MeanEnergyCurve(Array.Empty<double>(), Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double Sum, int Count)>();\n        foreach (var frame in frames)\n        {\n            double energy = frame.CycleCount > 0\n                ? frame.RunningSumEnergy / frame.CycleCount\n                : 0.0;\n\n            if (grouped.TryGetValue(frame.Temperature, out var acc))\n                grouped[frame.Temperature] = (acc.Sum + energy, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (energy, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var energyValues = new double[grouped.Count];\n        int index = 0;\n        foreach (var (temperature, (sum, count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            energyValues[index] = count > 0 ? sum / count : 0.0;\n            index++;\n        }\n\n        return new MeanEnergyCurve(temperatures, energyValues);\n    }\n}
36 | clustering/graphical/spc/observables/reductions/SpecificHeat.cs | {1827 169 0.2841 4.5719} | 1875 | using System;\nusing System.Collections.Generic;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.Graphical.SPC.Observables.Reductions;\n\npublic readonly record struct SpecificHeatCurve(\n    double[] Temperatures,\n    double[] Cv);\n\npublic static class SpecificHeat\n{\n    public static SpecificHeatCurve From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames.Count == 0)\n            return new SpecificHeatCurve(Array.Empty<double>(), Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double SumCv, int Count)>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelCoreObservables frame = frames[i];\n            double cv = 0.0;\n            if (frame.Temperature > 0.0 && frame.CycleCount > 0)\n            {\n                double meanEnergy = frame.RunningSumEnergy / frame.CycleCount;\n                double meanEnergySq = frame.RunningSumEnergySq / frame.CycleCount;\n                double variance = Math.Max(0.0, meanEnergySq - meanEnergy * meanEnergy);\n                cv = variance / (frame.Temperature * frame.Temperature);\n            }\n\n            if (grouped.TryGetValue(frame.Temperature, out (double SumCv, int Count) acc))\n                grouped[frame.Temperature] = (acc.SumCv + cv, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (cv, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var cvValues = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, (double sumCv, int count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            cvValues[index] = count > 0 ? sumCv / count : 0.0;\n            index++;\n        }\n\n        return new SpecificHeatCurve(temperatures, cvValues);\n    }\n}
37 | clustering/graphical/spc/observables/reductions/Susceptibility.cs | {1596 143 0.2663 4.6258} | 1638 | using System.Collections.Generic;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.Graphical.SPC.Observables.Reductions;\n\npublic readonly record struct SusceptibilityCurve(\n    double[] Temperatures,\n    double[] Chi);\n\npublic static class Susceptibility\n{\n    public static SusceptibilityCurve From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames.Count == 0)\n            return new SusceptibilityCurve(System.Array.Empty<double>(), System.Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double SumChi, int Count)>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelCoreObservables frame = frames[i];\n            double chi = frame.CycleCount > 0 && frame.Spins.Length > 0\n                ? frame.RunningSumSqClusterSizes / (frame.CycleCount * (double)frame.Spins.Length)\n                : 0.0;\n\n            if (grouped.TryGetValue(frame.Temperature, out (double SumChi, int Count) acc))\n                grouped[frame.Temperature] = (acc.SumChi + chi, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (chi, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var chiValues = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, (double sumChi, int count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            chiValues[index] = count > 0 ? sumChi / count : 0.0;\n            index++;\n        }\n\n        return new SusceptibilityCurve(temperatures, chiValues);\n    }\n}
