idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
14 | clustering/spc/graph.cs | {8564 614 0.2895 4.5686} | 8847 | using System;\nusing System.Collections.Generic;\nusing CouplingKernels;\nusing DistanceMetrics;\nusing ProximityGraphs;\nusing StatisticalEstimators;\n\nnamespace SpcCore\n{\n    // =========================================================================\n    // SPC graph initialization and diagnostics\n    // Edge and CsrGraph now live in ProximityGraphs (src/graphs/).\n    // Graph construction delegates to GraphBuilder — SpcBatch only resolves\n    // SPC-specific enums and feature/hash inputs.\n    // =========================================================================\n\n    public static partial class SpcBatch\n    {\n        internal static CsrGraph BuildGraphFromMetric(SpcBatchRequest request, out int n)\n        {\n            switch (request.Metric)\n            {\n                case SpcMetric.Hamming:\n                    return BuildGraphFromSimHashes(request, out n);\n                case SpcMetric.Euclidean:\n                    return BuildFeatureMetricGraph(request, Euclidean.Distance, \"Euclidean\", out n);\n                case SpcMetric.Canberra:\n                    return BuildFeatureMetricGraph(request, Canberra.Distance, \"Canberra\", out n);\n                case SpcMetric.Manhattan:\n                    return BuildFeatureMetricGraph(request, Manhattan.Distance, \"Manhattan\", out n);\n                case SpcMetric.Minkowski:\n                    double p = request.MinkowskiP;\n                    return BuildFeatureMetricGraph(request, (a, b) => Minkowski.Distance(a, b, p), \"Minkowski\", out n);\n                case SpcMetric.Cosine:\n                    return BuildFeatureMetricGraph(request, Cosine.Distance, \"Cosine\", out n);\n                case SpcMetric.JensenShannon:\n                    return BuildFeatureMetricGraph(request, JensenShannon.Distance, \"Jensen-Shannon\", out n);\n                case SpcMetric.Jaccard:\n                    double threshold = request.JaccardThreshold;\n                    return BuildFeatureMetricGraph(request, (a, b) => Jaccard.Distance(a, b, threshold), \"Jaccard\", out n);\n                case SpcMetric.Wasserstein:\n                    return BuildFeatureMetricGraph(request, Wasserstein.Distance1D, \"Wasserstein\", out n);\n                case SpcMetric.Mahalanobis:\n                    return BuildMahalanobisMetricGraph(request, out n);\n                case SpcMetric.FisherRaoSimplex:\n                    return BuildFeatureMetricGraph(request, FisherRaoSimplex.Distance, \"Fisher-Rao (Simplex)\", out n);\n                case SpcMetric.FisherRaoHalfPlane:\n                    return BuildFeatureMetricGraph(request, FisherRaoHalfPlane.Distance, \"Fisher-Rao (Half-Plane)\", out n);\n                case SpcMetric.Poincare:\n                    return BuildFeatureMetricGraph(request, Poincare.Distance, \"Poincare\", out n);\n                default:\n                    throw new NotSupportedException($\"Metric {request.Metric} not implemented.\");\n            }\n        }\n\n        internal static double EstimateDelta(DeltaEstimator estimator, double[] nnDistances)\n        {\n            switch (estimator)\n            {\n                case DeltaEstimator.Mean: return DeltaMean.Estimate(nnDistances);\n                case DeltaEstimator.Median: return DeltaMedian.Estimate(nnDistances);\n                default: throw new NotSupportedException($\"Estimator {estimator} not implemented.\");\n            }\n        }\n\n        internal static List<string> ValidateGraph(CsrGraph graph, int n, SpcProximity proximity)\n        {\n            var warnings = new List<string>();\n\n            if (graph.Targets.Length == 0)\n            {\n                warnings.Add(\"Graph has zero edges. Simulation will produce no structure.\");\n                return warnings;\n            }\n\n            GraphDiagnostics d = GraphBuilder.Validate(graph);\n\n            if (d.ComponentCount > 1)\n                warnings.Add(string.Format(\n                    \"Graph is disconnected: {0} components. Largest covers {1:P1} of nodes ({2}/{3}). Proximity: {4}.\",\n                    d.ComponentCount, d.LargestCoverage, d.LargestComponent, n, proximity));\n\n            if (d.LargestCoverage < 0.9)\n                warnings.Add(\n                    $\"Largest connected component covers only {d.LargestCoverage:P1} of nodes. \" +\n                    \"Potts phase transitions may not reflect global data structure. \" +\n                    \"Consider MstAugmented proximity or increasing K.\");\n\n            if (d.IsolatedNodes > 0)\n                warnings.Add($\"{d.IsolatedNodes} isolated node(s) with no edges. These will never join any cluster.\");\n\n            return warnings;\n        }\n\n        // ── SPC enum → ProximityGraphs enum adapters ─────────────────────────\n\n        private static ProximityRule SpcToProximityRule(SpcProximity p) => p switch\n        {\n            SpcProximity.Knn => ProximityRule.Knn,\n            SpcProximity.MutualKnn => ProximityRule.MutualKnn,\n            SpcProximity.EpsilonBall => ProximityRule.EpsilonBall,\n            SpcProximity.MstAugmented => ProximityRule.MstAugmented,\n            _ => throw new NotSupportedException($\"SpcProximity {p} not implemented.\")\n        };\n\n        private static KernelType SpcToKernelType(CouplingKernel k) => k switch\n        {\n            CouplingKernel.Gaussian => KernelType.Gaussian,\n            CouplingKernel.Cauchy => KernelType.Cauchy,\n            CouplingKernel.Laplacian => KernelType.Laplacian,\n            CouplingKernel.Linear => KernelType.Linear,\n            _ => throw new NotSupportedException($\"CouplingKernel {k} not implemented.\")\n        };\n\n        private static CsrGraph BuildFeatureMetricGraph(\n            SpcBatchRequest request,\n            Func<double[], double[], double> distance,\n            string metricName,\n            out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException($\"Features must be provided for {metricName} metric.\");\n\n            n = request.Features.Length;\n            double[][] features = request.Features;\n            return GraphBuilder.Build(n, (i, j) => distance(features[i], features[j]),\n                SpcToProximityRule(request.Proximity), request.K, request.Epsilon,\n                SpcToKernelType(request.CouplingKernel), request.Delta,\n                ensureConnected: SpcToEnsureConnected(request.Proximity));\n        }\n\n        private static CsrGraph BuildGraphFromSimHashes(SpcBatchRequest request, out int n)\n        {\n            if (request.SimHashes == null || request.SimHashes.Length == 0)\n                throw new ArgumentException(\"SimHashes must be provided for Hamming metric.\");\n\n            var hashes = request.SimHashes;\n            n = hashes.Length;\n\n            return GraphBuilder.Build(n, (i, j) => Hamming.Distance(hashes[i], hashes[j]),\n                SpcToProximityRule(request.Proximity), request.K, request.Epsilon,\n                SpcToKernelType(request.CouplingKernel), request.Delta,\n                ensureConnected: SpcToEnsureConnected(request.Proximity));\n        }\n\n        private static CsrGraph BuildMahalanobisMetricGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Mahalanobis metric.\");\n            if (request.CovarianceInverse == null)\n                throw new ArgumentException(\"CovarianceInverse must be provided for Mahalanobis metric.\");\n\n            n = request.Features.Length;\n            int dim = request.Features[0].Length;\n            if (request.CovarianceInverse.GetLength(0) != dim || request.CovarianceInverse.GetLength(1) != dim)\n                throw new ArgumentException(\"CovarianceInverse dimensions must match feature dimensionality.\");\n\n            var invCov = request.CovarianceInverse;\n            double[][] features = request.Features;\n            return GraphBuilder.Build(n, (i, j) => Mahalanobis.Distance(features[i], features[j], invCov, dim),\n                SpcToProximityRule(request.Proximity), request.K, request.Epsilon,\n                SpcToKernelType(request.CouplingKernel), request.Delta,\n                ensureConnected: SpcToEnsureConnected(request.Proximity));\n        }\n\n        public static double NormalizedCompressionDistance(\n            byte[] a,\n            byte[] b,\n            NcdCompressor compressor = NcdCompressor.Brotli)\n        {\n            return global::DistanceMetrics.NormalizedCompressionDistance.Distance(a, b, compressor);\n        }\n    }\n} |
15 | clustering/spc/potts.cs | {6846 636 0.4055 4.1187} | 7062 | using System;\nusing System.Numerics.Tensors;\nusing ProximityGraphs;\n\nnamespace SpcCore\n{\n\n    // =========================================================================\n    // PottsModel — Swendsen-Wang Monte Carlo on a proximity graph\n    // =========================================================================\n\n    public readonly struct SimulationResult\n    {\n        public readonly double Temperature;\n        public readonly int[] FinalSpins;\n        public readonly int SweepCount;\n        public readonly int EpochCount;\n        public readonly bool IsComplete;\n\n        public SimulationResult(double temperature, int[] finalSpins)\n            : this(\n                temperature,\n                finalSpins,\n                0,\n                0,\n                true)\n        {\n        }\n\n        public SimulationResult(\n            double temperature,\n            int[] finalSpins,\n            int sweepCount,\n            int epochCount,\n            bool isComplete)\n        {\n            Temperature = temperature;\n            FinalSpins = finalSpins;\n            SweepCount = sweepCount;\n            EpochCount = epochCount;\n            IsComplete = isComplete;\n        }\n    }\n\n    public readonly struct SimulationEpochResult\n    {\n        public readonly double Temperature;\n        public readonly int EpochCount;\n        public readonly int SweepCount;\n        public readonly int[] Spins;\n        public readonly bool IsComplete;\n\n        public SimulationEpochResult(\n            double temperature,\n            int epochCount,\n            int sweepCount,\n            int[] spins,\n            bool isComplete)\n        {\n            Temperature = temperature;\n            EpochCount = epochCount;\n            SweepCount = sweepCount;\n            Spins = spins;\n            IsComplete = isComplete;\n        }\n    }\n\n    // =========================================================================\n    // PottsModel\n    // =========================================================================\n\n    public class PottsModel\n    {\n        private int N;\n        private CsrGraph Graph;\n        private int[] Spins;\n        private Random Rng;\n        private int Q;\n\n        public PottsModel(int n, int q = 20, int seed = -1)\n        {\n            N = n;\n            Q = q;\n            Graph = default;\n            Spins = new int[N];\n            Rng = seed >= 0 ? new Random(seed) : new Random();\n            // Default random initialization\n            for (int i = 0; i < N; i++) Spins[i] = Rng.Next(Q);\n        }\n\n        /// <summary>\n        /// Inject spin state from a checkpoint (resume support).\n        /// </summary>\n        public void SetSpins(int[] savedSpins)\n        {\n            if (savedSpins.Length != N) throw new ArgumentException(\"Spin array size mismatch\");\n            Array.Copy(savedSpins, Spins, N);\n        }\n\n        /// <summary>\n        /// Load CSR graph topology for simulation.\n        /// </summary>\n        public void LoadGraph(CsrGraph graph)\n        {\n            Graph = graph;\n        }\n\n        /// <summary>\n        /// Run Swendsen-Wang Monte Carlo simulation at the given temperature.\n        /// Should support checkpointing by accepting completed sweep/epoch counts and returning intermediate results via callback.\n        /// </summary>\n        public SimulationResult RunSimulation(\n            double temperature,\n            int steps,\n            Action<SimulationEpochResult> checkpointEpoch = null,\n            int checkpointEpochSweeps = 0,\n            int completedSweeps = 0,\n            int completedEpochs = 0,\n            int totalSweeps = 0)\n        {\n            int entryCount = Graph.RowPointers[N];\n            var bondProb = new double[entryCount];\n\n            // Vectorised bond-probability init: bondProb[e] = 1 - exp(-J/T)\n            // Steps: multiply weights by -1/T → exp in-place → negate → add 1.0\n            TensorPrimitives.Multiply(Graph.Weights.AsSpan(0, entryCount), -1.0 / temperature, bondProb);\n            TensorPrimitives.Exp(bondProb, bondProb);\n            TensorPrimitives.Negate(bondProb, bondProb);\n            TensorPrimitives.Add(bondProb, 1.0, bondProb);\n\n            int totalSweepCount = totalSweeps > 0 ? totalSweeps : completedSweeps + steps;\n            int emittedEpochs = 0;\n\n            // Pre-allocate reusable structures outside the step loop (HPC 1+2)\n            var uf = new UnionFind(N);\n            int[] labels = new int[N];\n            int[] clusterMap = new int[N];   // root → assigned spin (-1 = unassigned)\n\n            for (int k = 0; k < steps; k++)\n            {\n                uf.Reset();\n\n                // Bond Formation — CSR row-major traversal (HPC 3)\n                // Sequential access through Targets/Weights arrays per row.\n                // Skip j <= i to process each undirected edge exactly once.\n                for (int i = 0; i < N; i++)\n                {\n                    int start = Graph.RowPointers[i];\n                    int end = Graph.RowPointers[i + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        int j = Graph.Targets[e];\n                        if (j <= i) continue;\n                        if (Spins[i] == Spins[j] && Rng.NextDouble() < bondProb[e])\n                            uf.Union(i, j);\n                    }\n                }\n\n                // Cluster Flipping — flat array replaces Dictionary<int,int>\n                for (int i = 0; i < N; i++)\n                {\n                    labels[i] = uf.Find(i);\n                    clusterMap[i] = -1;\n                }\n\n                for (int i = 0; i < N; i++)\n                {\n                    int root = labels[i];\n                    if (clusterMap[root] == -1)\n                        clusterMap[root] = Rng.Next(Q);\n                    Spins[i] = clusterMap[root];\n                }\n\n                int sweepCount = completedSweeps + k + 1;\n                if (checkpointEpoch != null &&\n                    checkpointEpochSweeps > 0 &&\n                    sweepCount < totalSweepCount &&\n                    sweepCount % checkpointEpochSweeps == 0)\n                {\n                    emittedEpochs++;\n                    checkpointEpoch(new SimulationEpochResult(\n                        temperature,\n                        completedEpochs + emittedEpochs,\n                        sweepCount,\n                        (int[])Spins.Clone(),\n                        false));\n                }\n            }\n\n            int finalEpochCount = completedEpochs + emittedEpochs + (steps > 0 ? 1 : 0);\n\n            return new SimulationResult(\n                temperature,\n                (int[])Spins.Clone(),\n                completedSweeps + steps,\n                finalEpochCount,\n                true\n            );\n        }\n    }\n} |
16 | clustering/spc/synthetic.cs | {2091 193 0.3587 4.2229} | 2147 | using System;\nusing SyntheticDatasets;\n\nnamespace SpcCore\n{\n    public static class SyntheticDatasetSpcExtensions\n    {\n        /// <summary>\n        /// Build an SpcBatchRequest from a generated dataset and a chosen\n        /// sweep configuration. Maps Features through directly; the rest\n        /// (metric, K, temperatures, steps, proximity, kernel, estimator)\n        /// is the harness's choice and must be supplied.\n        ///\n        /// Mahalanobis caveat: SpcBatchRequest.CovarianceInverse is left null.\n        /// Pooling and inversion is the caller's responsibility (different harnesses\n        /// pool differently).\n        /// </summary>\n        public static SpcBatchRequest ToSpcBatchRequest(\n            this SyntheticData.SyntheticDataset dataset,\n            SpcMetric metric,\n            int k,\n            double[] temperatures,\n            int steps,\n            SpcProximity proximity = SpcProximity.Knn,\n            CouplingKernel kernel = CouplingKernel.Gaussian,\n            DeltaEstimator estimator = DeltaEstimator.Mean,\n            double delta = 0.0,\n            double epsilon = 0.0,\n            double minkowskiP = 2.0,\n            double jaccardThreshold = 0.5,\n            int baseSeed = -1)\n        {\n            if (dataset == null) throw new ArgumentNullException(nameof(dataset));\n            if (dataset.Features == null || dataset.Features.Length == 0)\n                throw new ArgumentException(\"Dataset has no features.\", nameof(dataset));\n\n            return new SpcBatchRequest\n            {\n                Metric = metric,\n                Features = dataset.Features,\n                K = k,\n                Temperatures = temperatures ?? Array.Empty<double>(),\n                Steps = steps,\n                Proximity = proximity,\n                CouplingKernel = kernel,\n                Estimator = estimator,\n                Delta = delta,\n                Epsilon = epsilon,\n                MinkowskiP = minkowskiP,\n                JaccardThreshold = jaccardThreshold,\n                BaseSeed = baseSeed,\n            };\n        }\n    }\n} |
17 | clustering/spc/thermo/chi.cs | {8141 632 0.3389 4.2886} | 8343 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    public static partial class SpcAnalysis\n    {\n        // =====================================================================\n        // Susceptibility result bundle\n        // =====================================================================\n\n        /// <summary>\n        /// Susceptibility-based analysis result, corresponding to the original\n        /// Blatt/Wiseman/Domany analytical approach. One value per temperature.\n        /// </summary>\n        public sealed class SusceptibilityResult\n        {\n            public double[] Temperatures { get; set; }\n            /// <summary>\n            /// Susceptibility (variance of cluster sizes in equilibrium).\n            /// Computed by the chi analysis layer from materialized spin states.\n            /// </summary>\n            public double[] Susceptibility { get; set; }\n            /// <summary>Distinct spin-label count per temperature.</summary>\n            public int[] ClusterCount { get; set; }\n            /// <summary>\n            /// Per-temperature cluster-size histograms on common support.\n            /// </summary>\n            public double[][] ClusterSizeHistograms { get; set; }\n            /// <summary>\n            /// Critical temperatures detected from susceptibility peaks.\n            /// Multiple peaks = multi-scale structure or multiple transitions.\n            /// </summary>\n            public double[] CriticalTemperatures { get; set; }\n        }\n\n        // =====================================================================\n        // Susceptibility analysis pipeline\n        // =====================================================================\n\n        public static double ComputeSusceptibility(int[] spins)\n        {\n            if (spins == null || spins.Length == 0)\n                return double.NaN;\n\n            var counts = new Dictionary<int, int>();\n            for (int index = 0; index < spins.Length; index++)\n            {\n                int spin = spins[index];\n                counts.TryGetValue(spin, out int count);\n                counts[spin] = count + 1;\n            }\n\n            double sumSquares = 0.0;\n            foreach (int count in counts.Values)\n                sumSquares += (double)count * count;\n\n            return sumSquares / spins.Length;\n        }\n\n        public static double ComputeMeanSusceptibility(IEnumerable<int[]> spinStates)\n        {\n            if (spinStates == null)\n                return double.NaN;\n\n            double sum = 0.0;\n            int samples = 0;\n            foreach (var spins in spinStates)\n            {\n                double susceptibility = ComputeSusceptibility(spins);\n                if (double.IsNaN(susceptibility))\n                    continue;\n\n                sum += susceptibility;\n                samples++;\n            }\n\n            return samples == 0 ? double.NaN : sum / samples;\n        }\n\n        public static double[] ComputeSusceptibilityProfile(SpcBatchResult batchResult)\n        {\n            if (batchResult == null)\n                throw new ArgumentNullException(nameof(batchResult));\n\n            int numT = batchResult.Temperatures.Length;\n            var susceptibility = new double[numT];\n            for (int index = 0; index < numT; index++)\n            {\n                double temperature = batchResult.Temperatures[index];\n                susceptibility[index] = batchResult.FinalSpins.TryGetValue(temperature, out var spins)\n                    ? ComputeSusceptibility(spins)\n                    : double.NaN;\n            }\n\n            return susceptibility;\n        }\n\n        public static SusceptibilityResult AnalyzeCheckpointSusceptibility(\n            string directory,\n            string runStamp,\n            double[] temperatures = null,\n            double peakProminence = 0.1)\n        {\n            double[] selectedTemperatures = temperatures == null || temperatures.Length == 0\n                ? GetCheckpointTemperatures(directory, runStamp)\n                : (double[])temperatures.Clone();\n\n            var latestStates = new SpcBatchResult\n            {\n                Temperatures = selectedTemperatures,\n                FinalSpins = new Dictionary<double, int[]>(selectedTemperatures.Length)\n            };\n\n            var susceptibility = new double[selectedTemperatures.Length];\n            for (int index = 0; index < selectedTemperatures.Length; index++)\n            {\n                double temperature = selectedTemperatures[index];\n                var history = SpcBatch.LoadTemperatureSpinHistory(directory, runStamp, temperature);\n                var spinStates = new List<int[]>(history.Count);\n                for (int frameIndex = 0; frameIndex < history.Count; frameIndex++)\n                {\n                    int[] spins = history[frameIndex].Spins;\n                    if (spins != null && spins.Length > 0)\n                        spinStates.Add(spins);\n                }\n\n                susceptibility[index] = ComputeMeanSusceptibility(spinStates);\n                if (spinStates.Count > 0)\n                    latestStates.FinalSpins[temperature] = spinStates[spinStates.Count - 1];\n            }\n\n            var peakIndices = DetectPeaks(susceptibility, peakProminence);\n            var criticalTs = new double[peakIndices.Length];\n            for (int index = 0; index < peakIndices.Length; index++)\n                criticalTs[index] = selectedTemperatures[peakIndices[index]];\n\n            return new SusceptibilityResult\n            {\n                Temperatures = selectedTemperatures,\n                Susceptibility = susceptibility,\n                ClusterCount = CountClusters(latestStates),\n                ClusterSizeHistograms = BuildHistograms(latestStates),\n                CriticalTemperatures = criticalTs\n            };\n        }\n\n        private static double[] GetCheckpointTemperatures(string directory, string runStamp)\n        {\n            var manifest = SpcBatch.LoadStateManifest(directory, runStamp);\n            var temperatures = new List<double>();\n            for (int index = 0; index < manifest.Artifacts.Count; index++)\n            {\n                var artifact = manifest.Artifacts[index];\n                if (artifact.Kind != SpcStateArtifactKind.TemperatureObservation &&\n                    artifact.Kind != SpcStateArtifactKind.TemperatureCheckpoint)\n                    continue;\n\n                if (double.IsNaN(artifact.Temperature) || temperatures.Contains(artifact.Temperature))\n                    continue;\n\n                temperatures.Add(artifact.Temperature);\n            }\n\n            temperatures.Sort();\n            return temperatures.ToArray();\n        }\n\n        /// <summary>\n        /// Full susceptibility-based analysis. Builds histograms, counts\n        /// clusters per T, detects critical temperatures from chi peaks.\n        /// Temperatures must be sorted ascending.\n        /// </summary>\n        public static SusceptibilityResult AnalyzeSusceptibility(\n            SpcBatchResult batchResult,\n            double peakProminence = 0.1)\n        {\n            int numT = batchResult.Temperatures.Length;\n            if (numT == 0)\n                throw new ArgumentException(\"SpcBatchResult has no temperatures.\");\n\n            var histograms = BuildHistograms(batchResult);\n            var clusterCounts = CountClusters(batchResult);\n            var susceptibility = ComputeSusceptibilityProfile(batchResult);\n\n            var peakIndices = DetectPeaks(susceptibility, peakProminence);\n            var criticalTs = new double[peakIndices.Length];\n            for (int i = 0; i < peakIndices.Length; i++)\n                criticalTs[i] = batchResult.Temperatures[peakIndices[i]];\n\n            return new SusceptibilityResult\n            {\n                Temperatures = (double[])batchResult.Temperatures.Clone(),\n                Susceptibility = susceptibility,\n                ClusterCount = clusterCounts,\n                ClusterSizeHistograms = histograms,\n                CriticalTemperatures = criticalTs\n            };\n        }\n    }\n} |
