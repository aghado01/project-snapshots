idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
126 | synthetic/euclidean/BlattThreeCluster.cs | {2127 205 0.4386 3.9802} | 2200 | using System;\nusing System.Collections.Generic;\n\nnamespace Synthetic\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // BlattThreeCluster — 1996 PRL three-Gaussian toy\n        // =====================================================================\n        /// <summary>\n        /// Three compact Gaussian clusters at fixed corners (-6, 0), (6, 0),\n        /// (0, 6). Canonical SPC engine-validation dataset from\n        /// Blatt, Wiseman, Domany 1996 PRL 76:3251.\n        /// </summary>\n        public static SyntheticDataset GenerateBlattThreeCluster(\n            int pointsPerCluster = 100,\n            double stdDev = 1.0,\n            int seed = 42)\n        {\n            var rng = new Random(seed);\n            var centers = new double[][]\n            {\n                new double[] { -6.0, 0.0 },\n                new double[] {  6.0, 0.0 },\n                new double[] {  0.0, 6.0 }\n            };\n            int n = 3 * pointsPerCluster;\n            var features = new double[n][];\n            var labels = new int[n];\n\n            int idx = 0;\n            for (int c = 0; c < 3; c++)\n            {\n                for (int p = 0; p < pointsPerCluster; p++)\n                {\n                    features[idx] = new double[]\n                    {\n                        centers[c][0] + stdDev * SampleStandardNormal(rng),\n                        centers[c][1] + stdDev * SampleStandardNormal(rng)\n                    };\n                    labels[idx] = c;\n                    idx++;\n                }\n            }\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = 3,\n                Parameters = new Dictionary<string, object>\n                {\n                    [\"pointsPerCluster\"] = pointsPerCluster,\n                    [\"stdDev\"] = stdDev,\n                    [\"seed\"] = seed,\n                    [\"reference\"] = \"Blatt, Wiseman, Domany 1996 PRL 76:3251\"\n                }\n            };\n        }\n    }\n} |
127 | synthetic/euclidean/CrescentEllipsoid.cs | {21322 2167 0.3859 4.2594} | 21931 | using System;\nusing System.Collections.Generic;\n\nnamespace Synthetic\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // CrescentAndEllipsoid — v2: full parametric control\n        // =====================================================================\n        /// <summary>\n        /// Two clusters with fundamentally different topologies:\n        ///\n        ///   Cluster 0 — a crescent (half-annulus arc) in 3D ambient space.\n        ///     Non-convex; no single Gaussian can represent it faithfully.\n        ///     Exposes false-bridge formation in Euclidean k-NN graphs.\n        ///\n        ///   Cluster 1 — a compact anisotropic ellipsoid.\n        ///     Convex; a single full-covariance Gaussian fits it well.\n        ///\n        /// v2 adds full positional and orientational control so you can construct\n        /// diagnostically interesting arrangements:\n        ///\n        ///   • Default: ellipsoid near the crescent's open face (original behaviour)\n        ///   • Orthogonal intersection: ellipsoid semi-major axis perpendicular to\n        ///     the crescent's XY plane, piercing through the elbow. This arrangement\n        ///     is the canonical stress test for manifold-aware metrics (Poincaré,\n        ///     mutual k-NN, MST) — Euclidean proximity fuses the clusters at the\n        ///     intersection while geodesic/graph distances correctly separate them.\n        ///\n        /// Crescent arc spans [-arcHalfAngle + π, arcHalfAngle + π] so it opens\n        /// downward (+Y side) by default. The elbows are the arc endpoints at\n        /// ±arcHalfAngle from π. \"Elbow\" in parameter names refers to the midpoint\n        /// of the arc (angle = π exactly, the bottom of the crescent in XY).\n        /// </summary>\n        public static SyntheticDataset GenerateCrescentAndEllipsoid(\n            // --- Crescent ---\n            int crescentPoints = 300,\n            double crescentRadius = 3.0,   // arc spine radius\n            double crescentWidth = 0.40,  // noise scale at elbow (tapers to 0 at tips)\n            double arcHalfAngle = 2.04,  // half-angle of arc in radians (~234 deg total)\n                                         // --- Ellipsoid ---\n            int ellipsoidPoints = 200,\n            double[]? ellipsoidAxes = null, // per-axis scale [a, b, c]; default [0.6, 1.1, 0.4]\n            EllipsoidShellMode ellipsoidShellMode = EllipsoidShellMode.Solid,\n            // --- Relative placement ---\n            EllipsoidPlacement placement = EllipsoidPlacement.NearOpenFace,\n            // Manual override: if placement == Manual, these are used directly\n            double[]? ellipsoidCenter = null, // 3-element; overrides placement\n            double[]? ellipsoidEulerXYZ = null, // Euler angles (radians) for ellipsoid rotation\n                                                // applied as Rz * Ry * Rx to the axes\n                                                // --- Intersection knobs (used when placement == OrthogonalElbowIntersect) ---\n            double intersectDepth = 0.0,  // how far the ellipsoid center is offset along\n                                          // the crescent's elbow normal (Z axis).\n                                          // 0 = center at elbow, + = above, - = below.\n            double intersectRadialShift = 0.0,  // shift along the radial (X) direction at elbow.\n                                                // 0 = centered on the arc spine.\n                                                // --- Proximity stress knobs ---\n            double gapScale = 1.0,  // multiplier on the default inter-cluster gap.\n                                    // < 1 = closer (more overlap pressure),\n                                    // > 1 = further apart.\n                                    // --- Misc ---\n            int seed = 42)\n        {\n            // Default: X is the long axis (3.0); Y, Z are short (0.5).\n            // All preset rotations below are calibrated for this X-major convention.\n            ellipsoidAxes ??= new[] { 3.0, 0.5, 0.5 };\n\n            if (ellipsoidAxes.Length != 3)\n                throw new ArgumentException(\"ellipsoidAxes must have 3 elements.\");\n\n            var rng = new Random(seed);\n            int n = crescentPoints + ellipsoidPoints;\n            var features = new double[n][];\n            var labels = new int[n];\n\n            // ------------------------------------------------------------------\n            // Cluster 0: crescent arc\n            // ------------------------------------------------------------------\n            double arcStart = Math.PI - arcHalfAngle;\n            double arcEnd = Math.PI + arcHalfAngle;\n\n            // Elbow is the arc midpoint at angle = π → position (-crescentRadius, 0, 0)\n            // We expose this for placement calculations below.\n            double elbowX = crescentRadius * Math.Cos(Math.PI); // = -crescentRadius\n            double elbowY = crescentRadius * Math.Sin(Math.PI); // = 0\n            double elbowZ = 0.0;\n\n            int spineRes = Math.Max(64, crescentPoints / 4);\n            var spineSamples = new double[spineRes][];\n            for (int s = 0; s < spineRes; s++)\n            {\n                double t = arcStart + (arcEnd - arcStart) * s / (spineRes - 1);\n                spineSamples[s] = new[] {\n                    crescentRadius * Math.Cos(t),\n                    crescentRadius * Math.Sin(t),\n                    0.0\n                };\n            }\n\n            for (int p = 0; p < crescentPoints; p++)\n            {\n                // Gaussian density bias: concentrates mass toward the elbow (angle=π).\n                // u ∈ [-0.5, 0.5]; mapped to arc offset via N(0, 0.35), clamped.\n                double u = SampleStandardNormal(rng) * 0.35;\n                u = Math.Max(-0.5, Math.Min(0.5, u));\n                double t = Math.PI + u * (arcEnd - arcStart);  // t ∈ [arcStart, arcEnd]\n\n                double cos = Math.Cos(t), sin = Math.Sin(t);\n                double nx = cos, ny = sin;    // radial outward\n                double txArc = -sin, tyArc = cos; // tangential\n\n                // Cosine taper: full crescentWidth at elbow (u=0), zero at tips (u=±0.5).\n                double normalizedAngle = u * 2.0;  // ∈ [-1, 1]\n                double taper = Math.Cos(normalizedAngle * Math.PI / 2.0);\n                double localWidth = crescentWidth * taper;\n\n                double nr = localWidth * SampleStandardNormal(rng);             // radial noise\n                double nt = crescentWidth * 0.12 * SampleStandardNormal(rng);  // tiny tangential\n                double nz = localWidth * SampleStandardNormal(rng);             // out-of-plane\n\n                features[p] = new[] {\n                    crescentRadius * cos + nr * nx + nt * txArc,\n                    crescentRadius * sin + nr * ny + nt * tyArc,\n                    nz\n                };\n                labels[p] = 0;\n            }\n\n            // ------------------------------------------------------------------\n            // Cluster 1: anisotropic ellipsoid\n            // ------------------------------------------------------------------\n\n            // Resolve center\n            double[] center = ResolveCenter(\n                placement, ellipsoidCenter, ellipsoidAxes,\n                crescentRadius, crescentWidth, arcHalfAngle, gapScale,\n                elbowX, elbowY, elbowZ,\n                intersectDepth, intersectRadialShift);\n\n            // Resolve rotation matrix\n            double[,] rot = ResolveRotation(\n                placement, ellipsoidEulerXYZ, arcHalfAngle, rng);\n\n            // Build analytic covariance: Σ = R * diag(axes²) * Rᵀ\n            // Note: gapScale does not rescale the ellipsoid itself, only the placement gap.\n            double[,] cov = BuildCovariance(rot, ellipsoidAxes);\n\n            // Cholesky factor L s.t. LL^T = cov, for sampling\n            double[,] L = CholeskyLower(cov, 3);\n\n            for (int p = 0; p < ellipsoidPoints; p++)\n            {\n                double[] z = {\n                    SampleStandardNormal(rng),\n                    SampleStandardNormal(rng),\n                    SampleStandardNormal(rng)\n                };\n                double norm = Math.Sqrt(z[0] * z[0] + z[1] * z[1] + z[2] * z[2]);\n                if (norm < 1e-12) norm = 1.0;\n\n                double r = ellipsoidShellMode switch\n                {\n                    // Gaussian: raw normal vector — soft Gaussian cloud, density ∝ exp(-r²/2)\n                    EllipsoidShellMode.Gaussian => norm,\n                    // Solid: uniform volume density via cbrt-CDF rejection mapping\n                    EllipsoidShellMode.Solid => Math.Pow(rng.NextDouble(), 1.0 / 3.0),\n                    // Hollow: all points on the ellipsoid surface (2-manifold shell)\n                    EllipsoidShellMode.Hollow => 1.0,\n                    // Annular: thick shell, inner radius 0.75, outer 1.0\n                    EllipsoidShellMode.Annular => 0.75 + 0.25 * rng.NextDouble(),\n                    _ => Math.Pow(rng.NextDouble(), 1.0 / 3.0),\n                };\n\n                z[0] = z[0] / norm * r;\n                z[1] = z[1] / norm * r;\n                z[2] = z[2] / norm * r;\n\n                double[] pt = MultiplyMatrixVector(L, z);\n                features[crescentPoints + p] = new[] {\n                    center[0] + pt[0],\n                    center[1] + pt[1],\n                    center[2] + pt[2]\n                };\n                labels[crescentPoints + p] = 1;\n            }\n\n            // ------------------------------------------------------------------\n            // Geometry metadata\n            // ------------------------------------------------------------------\n            var arcGeom = new ArcGeometry\n            {\n                SpineSamples = spineSamples,\n                Radius = crescentRadius,\n                NoiseScale = crescentWidth,\n            };\n\n            var ellipsoidGeom = new EllipsoidGeometry\n            {\n                Center = center,\n                Covariance = cov,\n            };\n\n            var parameters = new Dictionary<string, object>\n            {\n                [\"generator\"] = \"CrescentAndEllipsoid\",\n                [\"crescentPoints\"] = crescentPoints,\n                [\"ellipsoidPoints\"] = ellipsoidPoints,\n                [\"crescentRadius\"] = crescentRadius,\n                [\"crescentWidth\"] = crescentWidth,\n                [\"arcHalfAngle\"] = arcHalfAngle,\n                [\"ellipsoidAxes\"] = ellipsoidAxes,\n                [\"placement\"] = placement.ToString(),\n                [\"ellipsoidCenter\"] = center,\n                [\"gapScale\"] = gapScale,\n                [\"intersectDepth\"] = intersectDepth,\n                [\"intersectRadialShift\"] = intersectRadialShift,\n                [\"ellipsoidShellMode\"] = ellipsoidShellMode.ToString(),\n                [\"seed\"] = seed,\n            };\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = 2,\n                Parameters = parameters,\n                ClusterGeometries = new ClusterGeometry[] { arcGeom, ellipsoidGeom },\n            };\n        }\n\n        // ======================================================================\n        // Placement enum\n        // ======================================================================\n\n        public enum EllipsoidPlacement\n        {\n            /// <summary>\n            /// Ellipsoid near the crescent's open face (original v1 default).\n            /// Stresses proximity rules at the open gap.\n            /// </summary>\n            NearOpenFace,\n\n            /// <summary>\n            /// Ellipsoid semi-major axis aligned with Z (perpendicular to the\n            /// crescent plane), center placed at the crescent elbow.\n            /// The canonical stress test for manifold-aware metrics:\n            /// Euclidean distance fuses the clusters at the intersection while\n            /// geodesic / graph-based distances correctly separate them.\n            /// </summary>\n            OrthogonalElbowIntersect,\n\n            /// <summary>\n            /// Ellipsoid centered at the upper arc tip (angle = π − arcHalfAngle).\n            /// Semi-major axis aligned outward along the radial direction at that tip.\n            /// Tests proximity disambiguation at a non-convex endpoint of the crescent.\n            /// Use intersectDepth to shift along Z, intersectRadialShift to shift\n            /// along/against the radial direction (positive = away from arc center).\n            /// </summary>\n            IntersectUpperTip,\n\n            /// <summary>\n            /// Ellipsoid centered at the lower arc tip (angle = π + arcHalfAngle).\n            /// Mirror of IntersectUpperTip about the XZ plane.\n            /// </summary>\n            IntersectLowerTip,\n\n            /// <summary>\n            /// Fully manual: use ellipsoidCenter and ellipsoidEulerXYZ directly.\n            /// </summary>\n            Manual,\n        }\n\n        // ======================================================================\n        // Shell sampling mode for the ellipsoid cluster\n        // ======================================================================\n\n        /// <summary>\n        /// Controls the radial density profile used when sampling the ellipsoid cluster.\n        /// All modes apply the same Cholesky transform — only the radial scaling differs.\n        /// </summary>\n        public enum EllipsoidShellMode\n        {\n            /// <summary>\n            /// Uniform volume density: cbrt-CDF radial scaling. Hard boundary — the\n            /// ellipsoid surface is the outer limit of the point cloud.\n            /// Good for GMM / SVM stress tests that expect clear separation.\n            /// </summary>\n            Solid,\n\n            /// <summary>\n            /// True multivariate Gaussian: raw standard-normal vector without\n            /// normalization / radial scaling. Infinite support; density decays as\n            /// exp(−r²/2) after the Cholesky warp. Clusters technically overlap\n            /// everywhere — best for testing GMM and Bayesian approaches.\n            /// </summary>\n            Gaussian,\n\n            /// <summary>\n            /// Surface shell only: all points projected onto the ellipsoid boundary\n            /// (r = 1 before Cholesky warp). Turns the ellipsoid into a 2-manifold;\n            /// stresses topology-aware algorithms the same way the Möbius tube does.\n            /// </summary>\n            Hollow,\n\n            /// <summary>\n            /// Thick shell: inner radius 0.75, outer radius 1.0 (uniform in that band).\n            /// Combines the hollow-manifold challenge with a small amount of\n            /// radial thickness to avoid degenerate nearest-neighbour graphs.\n            /// </summary>\n            Annular,\n        }\n        private static double[] ResolveCenter(\n            EllipsoidPlacement placement,\n            double[]? manualCenter,\n            double[] axes,\n            double crescentRadius,\n            double crescentWidth,\n            double arcHalfAngle,\n            double gapScale,\n            double elbowX, double elbowY, double elbowZ,\n            double intersectDepth,\n            double intersectRadialShift)\n        {\n            switch (placement)\n            {\n                case EllipsoidPlacement.Manual:\n                    if (manualCenter is null || manualCenter.Length != 3)\n                        throw new ArgumentException(\n                            \"ellipsoidCenter must be 3-element when placement == Manual.\");\n                    return (double[])manualCenter.Clone();\n\n                case EllipsoidPlacement.OrthogonalElbowIntersect:\n                    // Center the ellipsoid at the elbow point, offset by intersectDepth\n                    // along Z (the crescent's out-of-plane axis) and intersectRadialShift\n                    // along the elbow's radial direction (negative X at angle = π).\n                    return new[] {\n                        elbowX + intersectRadialShift,\n                        elbowY,\n                        elbowZ + intersectDepth\n                    };\n\n                case EllipsoidPlacement.IntersectUpperTip:\n                case EllipsoidPlacement.IntersectLowerTip:\n                    {\n                        // Arc tip angle: θ = π ± arcHalfAngle.\n                        // Upper tip → angle π − φ: radial dir = (−cos φ, +sin φ, 0)\n                        // Lower tip → angle π + φ: radial dir = (−cos φ, −sin φ, 0)\n                        double ySgn = placement == EllipsoidPlacement.IntersectUpperTip ? 1.0 : -1.0;\n                        double radX = -Math.Cos(arcHalfAngle);\n                        double radY = ySgn * Math.Sin(arcHalfAngle);\n                        double spine = crescentRadius + intersectRadialShift;\n                        return new[] {\n                        radX * spine,\n                        radY * spine,\n                        elbowZ + intersectDepth\n                    };\n                    }\n\n                case EllipsoidPlacement.NearOpenFace:\n                default:\n                    // Ellipsoid sits inside the hollow of the C-shaped crescent.\n                    // The crescent arc circle is centred at the origin; (0,0,0) is\n                    // the geometric centre of the hollow — equidistant from all arc\n                    // points and topologically \"inside\" the open face of the C.\n                    return new[] {\n                        0.0 + intersectRadialShift,\n                        0.0,\n                        intersectDepth\n                    };\n            }\n        }\n\n        private static double[,] ResolveRotation(\n            EllipsoidPlacement placement,\n            double[]? eulerXYZ,\n            double arcHalfAngle,\n            Random rng)\n        {\n            if (eulerXYZ is not null)\n            {\n                if (eulerXYZ.Length != 3)\n                    throw new ArgumentException(\"ellipsoidEulerXYZ must be 3-element.\");\n                return EulerToRotation(eulerXYZ[0], eulerXYZ[1], eulerXYZ[2]);\n            }\n\n            switch (placement)\n            {\n                case EllipsoidPlacement.OrthogonalElbowIntersect:\n                    // X-major (3.0) → Z axis: Ry(π/2) maps local X → world -Z.\n                    // Ellipsoid stabs through the crescent plane at the elbow.\n                    return EulerToRotation(0.0, Math.PI / 2.0, 0.0);\n\n                case EllipsoidPlacement.IntersectUpperTip:\n                    // X-major → outward radial at upper tip angle (π − arcHalfAngle).\n                    // Rz(θ) maps local X → (cos θ, sin θ, 0).\n                    // Need θ = π − arcHalfAngle so X points to (-cosφ, +sinφ, 0).\n                    return EulerToRotation(0.0, 0.0, Math.PI - arcHalfAngle);\n\n                case EllipsoidPlacement.IntersectLowerTip:\n                    // X-major → outward radial at lower tip angle (π + arcHalfAngle).\n                    // θ = π + arcHalfAngle so X points to (-cosφ, -sinφ, 0).\n                    return EulerToRotation(0.0, 0.0, Math.PI + arcHalfAngle);\n\n                case EllipsoidPlacement.NearOpenFace:\n                default:\n                    // X-major → Y axis: Rz(π/2) rotates the long axis to span vertically\n                    // (in Y), bridging between the upper and lower tips of the crescent\n                    // from inside the hollow.\n                    return EulerToRotation(0.0, 0.0, Math.PI / 2.0);\n            }\n        }\n\n        // ======================================================================\n        // Math helpers\n        // ======================================================================\n\n        /// <summary>\n        /// Euler rotation Rz(rz) * Ry(ry) * Rx(rx), intrinsic XYZ convention.\n        /// </summary>\n        private static double[,] EulerToRotation(double rx, double ry, double rz)\n        {\n            double cx = Math.Cos(rx), sx = Math.Sin(rx);\n            double cy = Math.Cos(ry), sy = Math.Sin(ry);\n            double cz = Math.Cos(rz), sz = Math.Sin(rz);\n\n            // Rz * Ry * Rx\n            return new double[,] {\n                {  cy*cz,  cz*sx*sy - cx*sz,  cx*cz*sy + sx*sz },\n                {  cy*sz,  cx*cz + sx*sy*sz,  cx*sy*sz - cz*sx },\n                { -sy,     cy*sx,             cx*cy            }\n            };\n        }\n\n        /// <summary>\n        /// Lower-triangular Cholesky factor of a 3×3 positive-definite matrix.\n        /// L s.t. L * Lᵀ = A.\n        /// </summary>\n        private static double[,] CholeskyLower(double[,] A, int dim)\n        {\n            var L = new double[dim, dim];\n            for (int i = 0; i < dim; i++)\n            {\n                for (int j = 0; j <= i; j++)\n                {\n                    double sum = A[i, j];\n                    for (int k = 0; k < j; k++)\n                        sum -= L[i, k] * L[j, k];\n                    L[i, j] = i == j\n                        ? Math.Sqrt(Math.Max(sum, 1e-14))\n                        : sum / (L[j, j] + 1e-14);\n                }\n            }\n            return L;\n        }\n\n    }\n} |
