idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
25 | estimators/WelfordMahal.cs | {7514 832 0.2405 4.7312} | 7746 | // ============================================================================\n// Estimators/Online/OnlineMahalanobis.cs\n// ============================================================================\n// Incremental diagonal Mahalanobis distance via Welford's online algorithm.\n//\n// Maintains per-dimension running mean and M2 (sum of squared deviations)\n// using Welford's method — numerically stable, single-pass, O(D) memory.\n//\n// This is NOT a substitute for WeiszfeldScatter or metrics/Mahalanobis.cs:\n//   - WeiszfeldScatter: full D×D robust batch scatter (requires all data)\n//   - metrics/Mahalanobis.cs: full covariance-inverse distance (batch)\n//   - This type: diagonal approximation, streaming/memory-constrained contexts\n//\n// The resulting distance is standardized Euclidean — each dimension is\n// normalized by its sample standard deviation. This satisfies the triangle\n// inequality and is a proper metric, but does not capture inter-dimensional\n// correlations. Use when: (a) data arrives as a stream, (b) D is large enough\n// that a full D×D covariance matrix is impractical, or (c) dimensions are\n// approximately uncorrelated by construction (e.g. PCA-reduced features).\n//\n// Thread safety: none. Wrap in a lock or use separate instances per thread.\n// ============================================================================\n\n#nullable enable\nusing System;\nusing System.Runtime.CompilerServices;\n\nnamespace Estimators.Online;\n\n/// <summary>\n/// Incremental diagonal Mahalanobis distance estimator using Welford's\n/// online algorithm. Update with observations one at a time; query distance\n/// at any point with at least two observations accumulated.\n/// </summary>\npublic sealed class OnlineMahalanobis\n{\n    private readonly double[] _mean;\n    private readonly double[] _m2;       // sum of squared deviations from mean\n    private readonly double[] _scratch;  // reused per Distance() call\n    private int _count;\n\n    /// <summary>Dimensionality of the observation space.</summary>\n    public int Dimension { get; }\n\n    /// <summary>Number of observations accumulated so far.</summary>\n    public int Count => _count;\n\n    /// <summary>\n    /// Whether at least two observations have been accumulated.\n    /// Distance() returns 0 for any query until this is true.\n    /// </summary>\n    public bool IsReady => _count >= 2;\n\n    /// <param name=\"dimension\">\n    /// Number of dimensions per observation. Must be &gt; 0.\n    /// </param>\n    public OnlineMahalanobis(int dimension)\n    {\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(dimension, 0, nameof(dimension));\n        Dimension = dimension;\n        _mean = new double[dimension];\n        _m2 = new double[dimension];\n        _scratch = new double[dimension];\n    }\n\n    /// <summary>\n    /// Incorporates a new observation into the running statistics.\n    /// Uses Welford's numerically stable update.\n    /// </summary>\n    /// <param name=\"observation\">\n    /// Array of length <see cref=\"Dimension\"/>. Not mutated.\n    /// </param>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public void Update(ReadOnlySpan<double> observation)\n    {\n        if (observation.Length != Dimension)\n            throw new ArgumentException(\n                $\"Observation length {observation.Length} does not match Dimension {Dimension}.\",\n                nameof(observation));\n\n        _count++;\n        for (int i = 0; i < Dimension; i++)\n        {\n            double delta = observation[i] - _mean[i];\n            _mean[i] += delta / _count;\n            double delta2 = observation[i] - _mean[i];\n            _m2[i] += delta * delta2;\n        }\n    }\n\n    /// <summary>Array overload of <see cref=\"Update(ReadOnlySpan{double})\"/>.</summary>\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Update(double[] observation)\n        => Update(observation.AsSpan());\n\n    /// <summary>\n    /// Computes the diagonal Mahalanobis distance from\n    /// <paramref name=\"observation\"/> to the current running mean.\n    /// Returns 0 if fewer than two observations have been accumulated.\n    /// </summary>\n    /// <param name=\"observation\">\n    /// Array of length <see cref=\"Dimension\"/>. Not mutated.\n    /// </param>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public double Distance(ReadOnlySpan<double> observation)\n    {\n        if (observation.Length != Dimension)\n            throw new ArgumentException(\n                $\"Observation length {observation.Length} does not match Dimension {Dimension}.\",\n                nameof(observation));\n\n        if (_count < 2) return 0.0;\n\n        double sumSq = 0.0;\n        double invCountMinus1 = 1.0 / (_count - 1);\n\n        for (int i = 0; i < Dimension; i++)\n        {\n            double variance = _m2[i] * invCountMinus1;\n            if (variance <= 0.0) continue;  // constant dimension — skip\n            double diff = observation[i] - _mean[i];\n            sumSq += (diff * diff) / variance;\n        }\n\n        return Math.Sqrt(sumSq);\n    }\n\n    /// <summary>Array overload of <see cref=\"Distance(ReadOnlySpan{double})\"/>.</summary>\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public double Distance(double[] observation)\n        => Distance(observation.AsSpan());\n\n    /// <summary>\n    /// Computes the diagonal Mahalanobis distance between two arbitrary\n    /// observations using the current running statistics as the normalizer.\n    /// Useful for pairwise distance queries against a reference distribution.\n    /// Returns 0 if fewer than two observations have been accumulated.\n    /// </summary>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public double Distance(ReadOnlySpan<double> a, ReadOnlySpan<double> b)\n    {\n        if (a.Length != Dimension)\n            throw new ArgumentException(\n                $\"Length {a.Length} does not match Dimension {Dimension}.\", nameof(a));\n        if (b.Length != Dimension)\n            throw new ArgumentException(\n                $\"Length {b.Length} does not match Dimension {Dimension}.\", nameof(b));\n\n        if (_count < 2) return 0.0;\n\n        double sumSq = 0.0;\n        double invCountMinus1 = 1.0 / (_count - 1);\n\n        for (int i = 0; i < Dimension; i++)\n        {\n            double variance = _m2[i] * invCountMinus1;\n            if (variance <= 0.0) continue;\n            double diff = a[i] - b[i];\n            sumSq += (diff * diff) / variance;\n        }\n\n        return Math.Sqrt(sumSq);\n    }\n\n    /// <summary>\n    /// Returns a snapshot of the current per-dimension sample variances.\n    /// Variance[i] = M2[i] / (Count - 1). Returns zeros if Count &lt; 2.\n    /// </summary>\n    public double[] GetVariances()\n    {\n        var variances = new double[Dimension];\n        if (_count < 2) return variances;\n\n        double invCountMinus1 = 1.0 / (_count - 1);\n        for (int i = 0; i < Dimension; i++)\n            variances[i] = _m2[i] * invCountMinus1;\n\n        return variances;\n    }\n\n    /// <summary>\n    /// Returns a snapshot of the current running mean.\n    /// </summary>\n    public double[] GetMean()\n    {\n        var mean = new double[Dimension];\n        _mean.AsSpan().CopyTo(mean);\n        return mean;\n    }\n\n    /// <summary>\n    /// Resets all accumulated statistics. Dimension is preserved.\n    /// </summary>\n    public void Reset()\n    {\n        _count = 0;\n        Array.Clear(_mean, 0, Dimension);\n        Array.Clear(_m2, 0, Dimension);\n        Array.Clear(_scratch, 0, Dimension);\n    }\n} |
26 | graphs/coupling/BandwidthEstimation.cs | {5856 594 0.3101 4.5407} | 6427 | // ============================================================================\n// Coupling/BandwidthEstimation.cs\n// ============================================================================\n// Bandwidth (δ) estimation for coupling kernels from a span of scalar edge\n// distances (e.g. k-NN edge lengths per node).\n//\n// Two layers:\n//   Per-kernel routes    — ForGaussian / ForLaplacian / ForCauchy / ForLinear\n//                          apply the natural estimator + consistency factor.\n//   Agnostic primitives  — Mean, Median, Mad, Max\n//                          exposed for callers who need custom routing.\n// ============================================================================\nusing System;\n\nnamespace CouplingKernels\n{\n    public static class BandwidthEstimation\n    {\n        // ── Consistency factors ───────────────────────────────────────────────\n        // Scale raw MAD into an unbiased scale estimate under each distribution.\n        //   Gaussian:  1 / Φ⁻¹(0.75)  ≈ 1.4826\n        //   Laplacian: 1 / ln(2)       ≈ 1.4427\n        //   Cauchy:    MAD(Cauchy(0,γ)) = γ exactly → 1.0\n        public const double GaussianFactor  = 1.4826;\n        public const double LaplacianFactor = 1.4427;\n        public const double CauchyFactor    = 1.0;\n\n        // ── Per-kernel routes ─────────────────────────────────────────────────\n\n        public static double ForGaussian(\n            ReadOnlySpan<double> distances, Span<double> scratch, double fallback = 1.0)\n            => distances.Length == 0 ? fallback : Mad(distances, scratch) * GaussianFactor;\n\n        public static double ForLaplacian(\n            ReadOnlySpan<double> distances, Span<double> scratch, double fallback = 1.0)\n            => distances.Length == 0 ? fallback : Mad(distances, scratch) * LaplacianFactor;\n\n        public static double ForCauchy(\n            ReadOnlySpan<double> distances, Span<double> scratch, double fallback = 1.0)\n            => distances.Length == 0 ? fallback : Mad(distances, scratch);\n\n        // Linear kernel: compact support — δ should span the neighbourhood exactly.\n        public static double ForLinear(\n            ReadOnlySpan<double> distances, double fallback = 1.0)\n            => distances.Length == 0 ? fallback : Max(distances);\n\n        // ── Kernel-agnostic primitives ────────────────────────────────────────\n\n        public static double Mean(ReadOnlySpan<double> distances, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n            double sum = 0;\n            for (int i = 0; i < distances.Length; i++)\n            {\n                Validate(distances[i]);\n                sum += distances[i];\n            }\n            return sum / distances.Length;\n        }\n\n        public static double Median(\n            ReadOnlySpan<double> distances, Span<double> scratch, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n            int n = distances.Length;\n            EnsureScratch(scratch, n);\n            Span<double> work = scratch.Slice(0, n);\n            for (int i = 0; i < n; i++) { Validate(distances[i]); work[i] = distances[i]; }\n            work.Sort();\n            return MedianOfSorted(work);\n        }\n\n        /// <summary>Raw MAD with caller-supplied location.</summary>\n        public static double Mad(\n            ReadOnlySpan<double> distances, double location, Span<double> scratch, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n            int n = distances.Length;\n            EnsureScratch(scratch, n);\n            Span<double> work = scratch.Slice(0, n);\n            for (int i = 0; i < n; i++) { Validate(distances[i]); work[i] = Math.Abs(distances[i] - location); }\n            work.Sort();\n            return MedianOfSorted(work);\n        }\n\n        /// <summary>Raw MAD with internally computed sample median as location.</summary>\n        public static double Mad(\n            ReadOnlySpan<double> distances, Span<double> scratch, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n            int n = distances.Length;\n            EnsureScratch(scratch, n);\n            Span<double> work = scratch.Slice(0, n);\n            for (int i = 0; i < n; i++) { Validate(distances[i]); work[i] = distances[i]; }\n            work.Sort();\n            double location = MedianOfSorted(work);\n            for (int i = 0; i < n; i++) work[i] = Math.Abs(distances[i] - location);\n            work.Sort();\n            return MedianOfSorted(work);\n        }\n\n        public static double Max(ReadOnlySpan<double> distances, double fallback = 1.0)\n        {\n            if (distances.Length == 0) return fallback;\n            double max = 0;\n            for (int i = 0; i < distances.Length; i++)\n            {\n                Validate(distances[i]);\n                if (distances[i] > max) max = distances[i];\n            }\n            return max;\n        }\n\n        // ── Helpers ───────────────────────────────────────────────────────────\n\n        private static double MedianOfSorted(ReadOnlySpan<double> sorted)\n        {\n            int mid = sorted.Length / 2;\n            return sorted.Length % 2 == 0\n                ? (sorted[mid - 1] + sorted[mid]) / 2.0\n                : sorted[mid];\n        }\n\n        private static void Validate(double d)\n        {\n            if (double.IsNaN(d) || double.IsInfinity(d) || d < 0)\n                throw new ArgumentOutOfRangeException(\"distances\",\n                    \"Distances must be finite and non-negative.\");\n        }\n\n        private static void EnsureScratch(Span<double> scratch, int n)\n        {\n            if (scratch.Length < n)\n                throw new ArgumentException(\n                    \"Scratch span is shorter than the distance sample.\", \"scratch\");\n        }\n    }\n} |
27 | graphs/coupling/Cauchy.cs | {439 62 0.3212 4.3598} | 457 | namespace CouplingKernels\n{\n    public static class Cauchy\n    {\n        /// <summary>\n        /// J = 1 / (1 + d² / δ²). Heavy-tailed — decays much slower than Gaussian.\n        /// Retains weak long-range coupling that Gaussian would zero out.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            return 1.0 / (1.0 + (distance * distance) / (delta * delta));\n        }\n    }\n} |
28 | graphs/coupling/Gaussian.cs | {463 54 0.3153 4.4384} | 482 | using System;\n\nnamespace CouplingKernels\n{\n    public static class Gaussian\n    {\n        /// <summary>\n        /// J = exp(-d² / 2δ²). Smooth, differentiable decay.\n        /// Natural choice for Euclidean-geometry metrics.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            double twoSigmaSq = 2.0 * delta * delta;\n            return Math.Exp(-(distance * distance) / twoSigmaSq);\n        }\n    }\n} |
29 | graphs/coupling/Laplacian.cs | {434 53 0.3065 4.3458} | 453 | using System;\n\nnamespace CouplingKernels\n{\n    public static class Laplacian\n    {\n        /// <summary>\n        /// J = exp(-d / δ). Decays on distance, not distance². Sharper local contrast\n        /// than Gaussian — near neighbors are favoured more aggressively.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            return Math.Exp(-distance / delta);\n        }\n    }\n} |
30 | graphs/coupling/Linear.cs | {441 58 0.3129 4.4077} | 459 | using System;\n\nnamespace CouplingKernels\n{\n    public static class Linear\n    {\n        /// <summary>\n        /// J = max(0, 1 - d / δ). Compact support — exactly zero beyond one scale length.\n        /// Hard locality boundary; no long-range coupling whatsoever.\n        /// </summary>\n        public static double Evaluate(double distance, double delta)\n        {\n            return Math.Max(0.0, 1.0 - distance / delta);\n        }\n    }\n} |
31 | graphs/Edge.cs | {488 59 0.3504 4.1772} | 507 | namespace ProximityGraphs\n{\n    /// <summary>\n    /// Weighted undirected edge between two data points.\n    /// J is the coupling strength (kernel-transformed distance), not raw distance.\n    /// </summary>\n    public struct Edge\n    {\n        public int Source;\n        public int Target;\n        public double J;\n\n        public Edge(int source, int target, double coupling)\n        {\n            Source = source;\n            Target = target;\n            J = coupling;\n        }\n    }\n} |
32 | graphs/GraphBuilder.cs | {8493 809 0.3257 4.4449} | 8839 | using System;\nusing System.Collections.Generic;\nusing CouplingKernels;\n\nnamespace ProximityGraphs\n{\n    // =========================================================================\n    // Proximity rule and kernel enumerations\n    // =========================================================================\n    // These live here (not in SpcCore) so that any consumer — viz engine,\n    // diagnostic pipeline, future clustering algorithms — can reference the\n    // graph construction layer without a dependency on SPC.\n\n    public enum ProximityRule { Knn, MutualKnn, EpsilonBall }\n\n    public enum KernelType { Gaussian, Cauchy, Laplacian, Linear }\n\n    // =========================================================================\n    // GraphDiagnostics - connectivity summary returned by Validate\n    // =========================================================================\n\n    public struct GraphDiagnostics\n    {\n        public int ComponentCount;\n        public int LargestComponent;\n        public double LargestCoverage;   // LargestComponent / NodeCount\n        public int IsolatedNodes;\n        public int EdgeCount;\n        public int NodeCount;\n    }\n\n    // =========================================================================\n    // GraphBuilder - three-phase parallel graph construction\n    // =========================================================================\n    // Phase 1 (parallel): O(N²) pairwise distance + neighbor selection.\n    //   Each construction method (Knn, MutualKnn, etc.) runs Parallel.For\n    //   internally over rows — no additional threading here.\n    //\n    // Phase 2 (sequential): bandwidth estimation over all NN distances,\n    //   then kernel weight application → Edge[].\n    //   Sequential because BandwidthEstimation needs the full distance\n    //   distribution before any coupling weight can be computed.\n    //\n    // Phase 3 (consumers): CsrGraph is immutable after Build returns.\n    //   LocalTangent, BFS orientation, SPC Swendsen-Wang all read from it\n    //   safely in parallel.\n\n    public static class GraphBuilder\n    {\n        /// <summary>\n        /// Full pipeline: N points, pairwise distance function → CsrGraph.\n        /// bandwidth = 0 triggers automatic estimation via BandwidthEstimation.\n        /// </summary>\n        public static CsrGraph Build(\n            int n,\n            Func<int, int, double> dist,\n            ProximityRule rule = ProximityRule.Knn,\n            int k = 10,\n            double epsilon = 0.0,\n            KernelType kernel = KernelType.Gaussian,\n            double bandwidth = 0.0,\n            bool ensureConnected = false)\n        {\n            // Phase 1: neighbor selection (parallel internally)\n            NeighborSelection sel = SelectNeighbors(n, dist, rule, k, epsilon);\n\n            // Optional connectivity repair via MST bridging\n            if (ensureConnected)\n                sel = ProximityGraph.EnsureConnected(sel, n, dist);\n\n            // Phase 2: bandwidth + coupling weights\n            return ToGraph(sel, kernel, bandwidth);\n        }\n\n        /// <summary>\n        /// Phase 2 only: convert pre-selected neighbors to a kernel-weighted CsrGraph.\n        /// Use when you already have a NeighborSelection (e.g. to try different\n        /// kernels on the same topology without re-running Phase 1).\n        /// bandwidth = 0 triggers automatic estimation.\n        /// </summary>\n        public static CsrGraph ToGraph(\n            NeighborSelection sel,\n            KernelType kernel = KernelType.Gaussian,\n            double bandwidth = 0.0)\n        {\n            Neighbor[][] allNeighbors = sel.AllNeighbors;\n            double[] nnDistances = sel.NearestNeighborDistances;\n            int n = allNeighbors.Length;\n\n            double delta = bandwidth > 0.0\n                ? bandwidth\n                : EstimateBandwidth(kernel, nnDistances);\n\n            Func<double, double, double> kernelFn = KernelFunction(kernel);\n\n            var edges = new List<Edge>(n * (allNeighbors.Length > 0 ? allNeighbors[0].Length : 0));\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in allNeighbors[i])\n                {\n                    if (i > nb.Index) continue;   // store each undirected edge once\n\n                    double coupling = kernelFn(nb.Distance, delta);\n                    if (coupling > 1e-10)\n                        edges.Add(new Edge(i, nb.Index, coupling));\n                }\n            }\n\n            return CsrGraph.FromEdges(edges.ToArray(), n);\n        }\n\n        /// <summary>\n        /// Connectivity diagnostics: component count, largest component coverage,\n        /// isolated node count. O(E α(N)) via union-find.\n        /// </summary>\n        public static GraphDiagnostics Validate(CsrGraph graph)\n        {\n            int n = graph.NodeCount;\n            var uf = new UnionFind(n);\n\n            // Iterate each edge once (source < target direction only)\n            for (int i = 0; i < n; i++)\n            {\n                int rowStart = graph.RowPointers[i];\n                int rowEnd = graph.RowPointers[i + 1];\n                for (int idx = rowStart; idx < rowEnd; idx++)\n                {\n                    int j = graph.Targets[idx];\n                    if (j > i) uf.Union(i, j);\n                }\n            }\n\n            int edgeCount = graph.Targets.Length / 2;  // symmetric storage\n            var compSizes = new Dictionary<int, int>(n / 4);\n            for (int node = 0; node < n; node++)\n            {\n                int root = uf.Find(node);\n                if (!compSizes.TryGetValue(root, out int sz))\n                    compSizes[root] = 1;\n                else\n                    compSizes[root] = sz + 1;\n            }\n\n            int largest = 0;\n            int isolated = 0;\n            foreach (int sz in compSizes.Values)\n            {\n                if (sz > largest) largest = sz;\n                if (sz == 1) isolated++;\n            }\n\n            return new GraphDiagnostics\n            {\n                ComponentCount = compSizes.Count,\n                LargestComponent = largest,\n                LargestCoverage = n > 0 ? (double)largest / n : 0.0,\n                IsolatedNodes = isolated,\n                EdgeCount = edgeCount,\n                NodeCount = n\n            };\n        }\n\n        // ── Helpers ──────────────────────────────────────────────────────────\n\n        private static NeighborSelection SelectNeighbors(\n            int n, Func<int, int, double> dist,\n            ProximityRule rule, int k, double epsilon)\n        {\n            return rule switch\n            {\n                ProximityRule.Knn => ProximityGraph.SelectKnn(n, k, dist),\n                ProximityRule.MutualKnn => ProximityGraph.SelectMutualKnn(n, k, dist),\n                ProximityRule.EpsilonBall => ProximityGraph.SelectEpsilonBall(n, epsilon, dist),\n\n                _ => throw new NotSupportedException($\"ProximityRule {rule} not implemented.\")\n            };\n        }\n\n        private static double EstimateBandwidth(KernelType kernel, double[] nnDistances)\n        {\n            ReadOnlySpan<double> span = nnDistances;\n            Span<double> scratch = stackalloc double[nnDistances.Length <= 512 ? nnDistances.Length : 0];\n            double[]? scratchArr = scratch.Length == 0 ? new double[nnDistances.Length] : null;\n            Span<double> effectiveScratch = scratch.Length > 0 ? scratch : scratchArr!.AsSpan();\n\n            return kernel switch\n            {\n                KernelType.Gaussian => BandwidthEstimation.ForGaussian(span, effectiveScratch),\n                KernelType.Laplacian => BandwidthEstimation.ForLaplacian(span, effectiveScratch),\n                KernelType.Cauchy => BandwidthEstimation.ForCauchy(span, effectiveScratch),\n                KernelType.Linear => BandwidthEstimation.ForLinear(span),\n                _ => throw new NotSupportedException($\"KernelType {kernel} not implemented.\")\n            };\n        }\n\n        private static Func<double, double, double> KernelFunction(KernelType kernel)\n        {\n            return kernel switch\n            {\n                KernelType.Gaussian => Gaussian.Evaluate,\n                KernelType.Cauchy => Cauchy.Evaluate,\n                KernelType.Laplacian => Laplacian.Evaluate,\n                KernelType.Linear => Linear.Evaluate,\n                _ => throw new NotSupportedException($\"KernelType {kernel} not implemented.\")\n            };\n        }\n    }\n} |
33 | graphs/GraphSelection.cs | {2896 281 0.3753 4.2524} | 2992 | using System;\n\nnamespace ProximityGraphs\n{\n    public readonly struct NeighborSelection\n    {\n        public NeighborSelection(Neighbor[][] allNeighbors, double[] nearestNeighborDistances)\n        {\n            AllNeighbors = allNeighbors ?? throw new ArgumentNullException(nameof(allNeighbors));\n            NearestNeighborDistances = nearestNeighborDistances ?? throw new ArgumentNullException(nameof(nearestNeighborDistances));\n        }\n\n        public Neighbor[][] AllNeighbors { get; }\n\n        public double[] NearestNeighborDistances { get; }\n\n        public void Deconstruct(out Neighbor[][] allNeighbors, out double[] nearestNeighborDistances)\n        {\n            allNeighbors = AllNeighbors;\n            nearestNeighborDistances = NearestNeighborDistances;\n        }\n    }\n\n    public struct Neighbor : IComparable<Neighbor>\n    {\n        public int Index;\n        public double Distance;\n\n        public int CompareTo(Neighbor other) => other.Distance.CompareTo(Distance);\n    }\n\n    internal sealed class BoundedMinHeap\n    {\n        private readonly Neighbor[] _heap;\n        private int _count;\n        private readonly int _capacity;\n\n        public BoundedMinHeap(int k)\n        {\n            if (k <= 0)\n                throw new ArgumentOutOfRangeException(nameof(k), \"K must be positive.\");\n\n            _capacity = k;\n            _heap = new Neighbor[k];\n        }\n\n        public void TryAdd(int index, double distance)\n        {\n            if (_count < _capacity)\n            {\n                _heap[_count] = new Neighbor { Index = index, Distance = distance };\n                _count++;\n                if (_count == _capacity) BuildHeap();\n            }\n            else if (distance < _heap[0].Distance)\n            {\n                _heap[0] = new Neighbor { Index = index, Distance = distance };\n                SiftDown(0);\n            }\n        }\n\n        public Neighbor[] GetSorted()\n        {\n            var result = new Neighbor[_count];\n            Array.Copy(_heap, result, _count);\n            Array.Sort(result, (a, b) => a.Distance.CompareTo(b.Distance));\n            return result;\n        }\n\n        private void BuildHeap()\n        {\n            for (int i = _count / 2 - 1; i >= 0; i--)\n                SiftDown(i);\n        }\n\n        private void SiftDown(int i)\n        {\n            while (true)\n            {\n                int largest = i;\n                int left = 2 * i + 1;\n                int right = 2 * i + 2;\n\n                if (left < _count && _heap[left].Distance > _heap[largest].Distance) largest = left;\n                if (right < _count && _heap[right].Distance > _heap[largest].Distance) largest = right;\n                if (largest == i) break;\n\n                var tmp = _heap[i];\n                _heap[i] = _heap[largest];\n                _heap[largest] = tmp;\n                i = largest;\n            }\n        }\n    }\n} |
