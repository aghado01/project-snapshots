idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
59 | spc.thermo.cs | {13572 1382 0.3711 4.1519} | 13927 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    // =========================================================================\n    // SpcAnalysis — shared analysis infrastructure\n    // =========================================================================\n    /// <summary>\n    /// Post-hoc analysis of SpcBatchResult output. This base file owns the\n    /// shared primitives that all analytical methods build on:\n    ///\n    ///   Histogram construction   — global and local spin-label distributions\n    ///   Graph utilities          — medoid identification, BFS shell decomposition\n    ///   Peak detection           — generic local-maxima finder on any 1D signal\n    ///   Scoring                  — cluster purity against ground truth\n    ///   Cluster counting         — distinct labels per temperature\n    ///\n    /// Specific analytical workflows live in partial classes under src/spc-thermo/:\n    ///   chi.cs  — chi-based phase transition detection\n    ///   kl.cs   — KL divergence family (global, fan-out,\n    ///             radial coherence, Fisher information)\n    ///   mhbs.cs — multi-hop boundary sharpness analysis\n    ///\n    /// All methods are coin-agnostic. They operate on the CSR graph and spin\n    /// arrays from SpcBatchResult without knowledge of the metric, proximity,\n    /// or delta estimator that produced them.\n    /// </summary>\n    public static partial class SpcAnalysis\n    {\n        // =====================================================================\n        // Global histogram construction\n        // =====================================================================\n\n        /// <summary>\n        /// Build per-temperature cluster-size histograms from FinalSpins.\n        /// All histograms share a common support (max label across all T)\n        /// for direct comparability.\n        /// </summary>\n        public static double[][] BuildHistograms(SpcBatchResult batchResult)\n        {\n            int numT = batchResult.Temperatures.Length;\n            var histograms = new double[numT][];\n\n            int maxLabel = -1;\n            for (int i = 0; i < numT; i++)\n            {\n                double T = batchResult.Temperatures[i];\n                if (!batchResult.FinalSpins.TryGetValue(T, out var spins)) continue;\n                for (int j = 0; j < spins.Length; j++)\n                    if (spins[j] > maxLabel) maxLabel = spins[j];\n            }\n            int support = maxLabel + 1;\n            if (support <= 0) support = 1;\n\n            for (int i = 0; i < numT; i++)\n            {\n                double T = batchResult.Temperatures[i];\n                var hist = new double[support];\n                if (batchResult.FinalSpins.TryGetValue(T, out var spins))\n                {\n                    int n = spins.Length;\n                    for (int j = 0; j < n; j++) hist[spins[j]]++;\n                    if (n > 0)\n                        for (int k = 0; k < support; k++) hist[k] /= n;\n                }\n                histograms[i] = hist;\n            }\n            return histograms;\n        }\n\n        /// <summary>Single spin array to histogram. Self-contained support.</summary>\n        public static double[] BuildHistogram(int[] spins)\n        {\n            if (spins == null || spins.Length == 0) return new double[0];\n            int maxLabel = -1;\n            for (int i = 0; i < spins.Length; i++)\n                if (spins[i] > maxLabel) maxLabel = spins[i];\n            int support = maxLabel + 1;\n            var hist = new double[support];\n            for (int i = 0; i < spins.Length; i++) hist[spins[i]]++;\n            for (int k = 0; k < support; k++) hist[k] /= spins.Length;\n            return hist;\n        }\n\n        // =====================================================================\n        // Local histogram from CSR neighborhood\n        // =====================================================================\n\n        /// <summary>\n        /// Spin-label histogram for a node's CSR neighbors (plus itself).\n        /// Support = max spin label + 1 from the full spin array so all\n        /// local histograms from the same T are directly comparable.\n        /// </summary>\n        public static double[] BuildLocalHistogram(\n            int nodeIndex, int[] spins, CsrGraph graph)\n        {\n            int maxLabel = 0;\n            for (int i = 0; i < spins.Length; i++)\n                if (spins[i] > maxLabel) maxLabel = spins[i];\n            int support = maxLabel + 1;\n\n            var hist = new double[support];\n            int count = 0;\n\n            hist[spins[nodeIndex]]++;\n            count++;\n\n            int start = graph.RowPointers[nodeIndex];\n            int end = graph.RowPointers[nodeIndex + 1];\n            for (int e = start; e < end; e++)\n            {\n                hist[spins[graph.Targets[e]]]++;\n                count++;\n            }\n\n            if (count > 0)\n                for (int k = 0; k < support; k++) hist[k] /= count;\n            return hist;\n        }\n\n        // =====================================================================\n        // Cluster counting\n        // =====================================================================\n\n        /// <summary>\n        /// Count distinct spin labels per temperature.\n        /// </summary>\n        public static int[] CountClusters(SpcBatchResult batchResult)\n        {\n            int numT = batchResult.Temperatures.Length;\n            var counts = new int[numT];\n            for (int i = 0; i < numT; i++)\n            {\n                double T = batchResult.Temperatures[i];\n                if (!batchResult.FinalSpins.TryGetValue(T, out var spins)) continue;\n                var seen = new HashSet<int>();\n                for (int j = 0; j < spins.Length; j++) seen.Add(spins[j]);\n                counts[i] = seen.Count;\n            }\n            return counts;\n        }\n\n        // =====================================================================\n        // Medoid identification\n        // =====================================================================\n\n        /// <summary>\n        /// Find the medoid of each cluster: the node with highest sum of\n        /// coupling weights (J) to same-cluster neighbors in the CSR graph.\n        /// Metric-agnostic — J values encode coupling strength on [0,1]\n        /// after the Gaussian kernel, regardless of which coin produced them.\n        /// Returns one medoid per cluster, sorted by cluster label.\n        /// </summary>\n        public static int[] FindMedoids(int[] spins, CsrGraph graph)\n        {\n            int n = spins.Length;\n\n            var clusters = new Dictionary<int, List<int>>();\n            for (int i = 0; i < n; i++)\n            {\n                int label = spins[i];\n                if (!clusters.TryGetValue(label, out var members))\n                {\n                    members = new List<int>();\n                    clusters[label] = members;\n                }\n                members.Add(i);\n            }\n\n            var medoidList = new List<(int label, int node)>();\n            foreach (var kvp in clusters)\n            {\n                int clusterLabel = kvp.Key;\n                var members = kvp.Value;\n                if (members.Count == 0) continue;\n\n                int bestNode = members[0];\n                double bestScore = double.MinValue;\n\n                foreach (int node in members)\n                {\n                    double score = 0;\n                    int start = graph.RowPointers[node];\n                    int end = graph.RowPointers[node + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        if (spins[graph.Targets[e]] == clusterLabel)\n                            score += graph.Weights[e];\n                    }\n                    if (score > bestScore)\n                    {\n                        bestScore = score;\n                        bestNode = node;\n                    }\n                }\n                medoidList.Add((clusterLabel, bestNode));\n            }\n\n            medoidList.Sort((a, b) => a.label.CompareTo(b.label));\n            var medoids = new int[medoidList.Count];\n            for (int i = 0; i < medoidList.Count; i++)\n                medoids[i] = medoidList[i].node;\n            return medoids;\n        }\n\n        // =====================================================================\n        // BFS shell decomposition on CSR graph\n        // =====================================================================\n\n        /// <summary>\n        /// BFS from source node, returning nodes grouped by hop distance.\n        /// shells[0] = {source}, shells[r] = nodes exactly r hops away.\n        /// Computed once per medoid — topology is fixed across temperatures.\n        /// </summary>\n        public static int[][] BfsShells(int source, CsrGraph graph, int maxRadius)\n        {\n            int n = graph.NodeCount;\n            var distance = new int[n];\n            for (int i = 0; i < n; i++) distance[i] = -1;\n            distance[source] = 0;\n\n            var shells = new List<int[]>();\n            var currentShell = new List<int> { source };\n            shells.Add(currentShell.ToArray());\n\n            for (int r = 1; r <= maxRadius; r++)\n            {\n                var nextShell = new List<int>();\n                foreach (int node in currentShell)\n                {\n                    int start = graph.RowPointers[node];\n                    int end = graph.RowPointers[node + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        int neighbor = graph.Targets[e];\n                        if (distance[neighbor] == -1)\n                        {\n                            distance[neighbor] = r;\n                            nextShell.Add(neighbor);\n                        }\n                    }\n                }\n                if (nextShell.Count == 0) break;\n                shells.Add(nextShell.ToArray());\n                currentShell = nextShell;\n            }\n            return shells.ToArray();\n        }\n\n        // =====================================================================\n        // Generic peak detection\n        // =====================================================================\n\n        /// <summary>\n        /// Detect local maxima in a 1D signal that exceed a prominence\n        /// threshold (fraction of the signal's global maximum). Returns\n        /// the indices of detected peaks. Used by both susceptibility and\n        /// KL analysis modules for their respective signals.\n        ///\n        /// Handles both per-T signals (susceptibility) where the peak index\n        /// maps directly to a temperature, and per-gap signals (KL) where\n        /// the caller maps the index to a midpoint temperature.\n        /// </summary>\n        public static int[] DetectPeaks(double[] signal, double prominence = 0.1)\n        {\n            if (signal == null || signal.Length < 3) return Array.Empty<int>();\n\n            double maxVal = 0.0;\n            for (int i = 0; i < signal.Length; i++)\n                if (signal[i] > maxVal) maxVal = signal[i];\n            if (maxVal <= 0) return Array.Empty<int>();\n            double threshold = prominence * maxVal;\n\n            var peaks = new List<int>();\n            for (int i = 1; i < signal.Length - 1; i++)\n            {\n                if (signal[i] < threshold) continue;\n                bool isPeak = signal[i] >= signal[i - 1]\n                           && signal[i] >= signal[i + 1];\n                bool strict = signal[i] > signal[i - 1]\n                           || signal[i] > signal[i + 1];\n                if (isPeak && strict) peaks.Add(i);\n            }\n            return peaks.ToArray();\n        }\n\n        // =====================================================================\n        // Scoring — cluster purity against ground truth\n        // =====================================================================\n\n        /// <summary>\n        /// Cluster purity: for each predicted cluster, count the most common\n        /// true label; sum and divide by total. Range [0, 1]. 1.0 = perfect\n        /// recovery (up to label permutation). Does not penalize\n        /// over-clustering.\n        /// </summary>\n        public static double ComputePurity(int[] predicted, int[] trueLabels)\n        {\n            if (predicted.Length != trueLabels.Length)\n                throw new ArgumentException(\"predicted and trueLabels must have same length.\");\n            int n = predicted.Length;\n            if (n == 0) return 0.0;\n\n            var clusterLabelCounts = new Dictionary<int, Dictionary<int, int>>();\n            for (int i = 0; i < n; i++)\n            {\n                int pred = predicted[i];\n                int truth = trueLabels[i];\n                if (!clusterLabelCounts.TryGetValue(pred, out var inner))\n                {\n                    inner = new Dictionary<int, int>();\n                    clusterLabelCounts[pred] = inner;\n                }\n                inner.TryGetValue(truth, out int count);\n                inner[truth] = count + 1;\n            }\n\n            int totalCorrect = 0;\n            foreach (var cluster in clusterLabelCounts.Values)\n            {\n                int maxCount = 0;\n                foreach (var count in cluster.Values)\n                    if (count > maxCount) maxCount = count;\n                totalCorrect += maxCount;\n            }\n            return (double)totalCorrect / n;\n        }\n    }\n} |
60 | spc/thermo/chi.cs | {8141 632 0.3389 4.2886} | 8343 | using System;\nusing System.Collections.Generic;\n\nnamespace SpcCore\n{\n    public static partial class SpcAnalysis\n    {\n        // =====================================================================\n        // Susceptibility result bundle\n        // =====================================================================\n\n        /// <summary>\n        /// Susceptibility-based analysis result, corresponding to the original\n        /// Blatt/Wiseman/Domany analytical approach. One value per temperature.\n        /// </summary>\n        public sealed class SusceptibilityResult\n        {\n            public double[] Temperatures { get; set; }\n            /// <summary>\n            /// Susceptibility (variance of cluster sizes in equilibrium).\n            /// Computed by the chi analysis layer from materialized spin states.\n            /// </summary>\n            public double[] Susceptibility { get; set; }\n            /// <summary>Distinct spin-label count per temperature.</summary>\n            public int[] ClusterCount { get; set; }\n            /// <summary>\n            /// Per-temperature cluster-size histograms on common support.\n            /// </summary>\n            public double[][] ClusterSizeHistograms { get; set; }\n            /// <summary>\n            /// Critical temperatures detected from susceptibility peaks.\n            /// Multiple peaks = multi-scale structure or multiple transitions.\n            /// </summary>\n            public double[] CriticalTemperatures { get; set; }\n        }\n\n        // =====================================================================\n        // Susceptibility analysis pipeline\n        // =====================================================================\n\n        public static double ComputeSusceptibility(int[] spins)\n        {\n            if (spins == null || spins.Length == 0)\n                return double.NaN;\n\n            var counts = new Dictionary<int, int>();\n            for (int index = 0; index < spins.Length; index++)\n            {\n                int spin = spins[index];\n                counts.TryGetValue(spin, out int count);\n                counts[spin] = count + 1;\n            }\n\n            double sumSquares = 0.0;\n            foreach (int count in counts.Values)\n                sumSquares += (double)count * count;\n\n            return sumSquares / spins.Length;\n        }\n\n        public static double ComputeMeanSusceptibility(IEnumerable<int[]> spinStates)\n        {\n            if (spinStates == null)\n                return double.NaN;\n\n            double sum = 0.0;\n            int samples = 0;\n            foreach (var spins in spinStates)\n            {\n                double susceptibility = ComputeSusceptibility(spins);\n                if (double.IsNaN(susceptibility))\n                    continue;\n\n                sum += susceptibility;\n                samples++;\n            }\n\n            return samples == 0 ? double.NaN : sum / samples;\n        }\n\n        public static double[] ComputeSusceptibilityProfile(SpcBatchResult batchResult)\n        {\n            if (batchResult == null)\n                throw new ArgumentNullException(nameof(batchResult));\n\n            int numT = batchResult.Temperatures.Length;\n            var susceptibility = new double[numT];\n            for (int index = 0; index < numT; index++)\n            {\n                double temperature = batchResult.Temperatures[index];\n                susceptibility[index] = batchResult.FinalSpins.TryGetValue(temperature, out var spins)\n                    ? ComputeSusceptibility(spins)\n                    : double.NaN;\n            }\n\n            return susceptibility;\n        }\n\n        public static SusceptibilityResult AnalyzeCheckpointSusceptibility(\n            string directory,\n            string runStamp,\n            double[] temperatures = null,\n            double peakProminence = 0.1)\n        {\n            double[] selectedTemperatures = temperatures == null || temperatures.Length == 0\n                ? GetCheckpointTemperatures(directory, runStamp)\n                : (double[])temperatures.Clone();\n\n            var latestStates = new SpcBatchResult\n            {\n                Temperatures = selectedTemperatures,\n                FinalSpins = new Dictionary<double, int[]>(selectedTemperatures.Length)\n            };\n\n            var susceptibility = new double[selectedTemperatures.Length];\n            for (int index = 0; index < selectedTemperatures.Length; index++)\n            {\n                double temperature = selectedTemperatures[index];\n                var history = SpcBatch.LoadTemperatureSpinHistory(directory, runStamp, temperature);\n                var spinStates = new List<int[]>(history.Count);\n                for (int frameIndex = 0; frameIndex < history.Count; frameIndex++)\n                {\n                    int[] spins = history[frameIndex].Spins;\n                    if (spins != null && spins.Length > 0)\n                        spinStates.Add(spins);\n                }\n\n                susceptibility[index] = ComputeMeanSusceptibility(spinStates);\n                if (spinStates.Count > 0)\n                    latestStates.FinalSpins[temperature] = spinStates[spinStates.Count - 1];\n            }\n\n            var peakIndices = DetectPeaks(susceptibility, peakProminence);\n            var criticalTs = new double[peakIndices.Length];\n            for (int index = 0; index < peakIndices.Length; index++)\n                criticalTs[index] = selectedTemperatures[peakIndices[index]];\n\n            return new SusceptibilityResult\n            {\n                Temperatures = selectedTemperatures,\n                Susceptibility = susceptibility,\n                ClusterCount = CountClusters(latestStates),\n                ClusterSizeHistograms = BuildHistograms(latestStates),\n                CriticalTemperatures = criticalTs\n            };\n        }\n\n        private static double[] GetCheckpointTemperatures(string directory, string runStamp)\n        {\n            var manifest = SpcBatch.LoadStateManifest(directory, runStamp);\n            var temperatures = new List<double>();\n            for (int index = 0; index < manifest.Artifacts.Count; index++)\n            {\n                var artifact = manifest.Artifacts[index];\n                if (artifact.Kind != SpcStateArtifactKind.TemperatureObservation &&\n                    artifact.Kind != SpcStateArtifactKind.TemperatureCheckpoint)\n                    continue;\n\n                if (double.IsNaN(artifact.Temperature) || temperatures.Contains(artifact.Temperature))\n                    continue;\n\n                temperatures.Add(artifact.Temperature);\n            }\n\n            temperatures.Sort();\n            return temperatures.ToArray();\n        }\n\n        /// <summary>\n        /// Full susceptibility-based analysis. Builds histograms, counts\n        /// clusters per T, detects critical temperatures from chi peaks.\n        /// Temperatures must be sorted ascending.\n        /// </summary>\n        public static SusceptibilityResult AnalyzeSusceptibility(\n            SpcBatchResult batchResult,\n            double peakProminence = 0.1)\n        {\n            int numT = batchResult.Temperatures.Length;\n            if (numT == 0)\n                throw new ArgumentException(\"SpcBatchResult has no temperatures.\");\n\n            var histograms = BuildHistograms(batchResult);\n            var clusterCounts = CountClusters(batchResult);\n            var susceptibility = ComputeSusceptibilityProfile(batchResult);\n\n            var peakIndices = DetectPeaks(susceptibility, peakProminence);\n            var criticalTs = new double[peakIndices.Length];\n            for (int i = 0; i < peakIndices.Length; i++)\n                criticalTs[i] = batchResult.Temperatures[peakIndices[i]];\n\n            return new SusceptibilityResult\n            {\n                Temperatures = (double[])batchResult.Temperatures.Clone(),\n                Susceptibility = susceptibility,\n                ClusterCount = clusterCounts,\n                ClusterSizeHistograms = histograms,\n                CriticalTemperatures = criticalTs\n            };\n        }\n    }\n} |
