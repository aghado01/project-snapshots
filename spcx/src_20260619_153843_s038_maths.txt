idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
355 | maths/samplers/rjmcmc/ReversibleJumpChain.cs | {4544 440 0.2903 4.6500} | 4690 | using System;\nusing System.Collections.Generic;\nusing Maths.Rng;\nusing Maths.Samplers.Ensemble;\n\nnamespace Maths.Samplers.Rjmcmc;\n\npublic sealed class ReversibleJumpChain<TState> : IChain<TState>\n{\n    private readonly IReadOnlyList<IRjMove<TState>> _moves;\n    private readonly Dictionary<string, IRjMove<TState>> _byKey;\n    private readonly IRjTarget<TState> _target;\n    private readonly Xoshiro256PlusPlus _rng;\n    private double _currentLogTarget;   // cached log-density of Current; only the candidate is evaluated per step\n    private readonly long[] _moveAttempts;\n    private readonly long[] _moveAccepts;\n\n    public TState Current { get; private set; }\n\n    public long Attempts { get; private set; }\n\n    public long Accepted { get; private set; }\n\n    public ReversibleJumpChain(\n        IReadOnlyList<IRjMove<TState>> moves,\n        IRjTarget<TState> target,\n        TState start,\n        Xoshiro256PlusPlus rng)\n    {\n        ArgumentNullException.ThrowIfNull(moves);\n        ArgumentNullException.ThrowIfNull(target);\n        ArgumentNullException.ThrowIfNull(rng);\n        if (moves.Count == 0)\n            throw new ArgumentException(\"A reversible-jump chain needs at least one move.\", nameof(moves));\n\n        _byKey = new Dictionary<string, IRjMove<TState>>(moves.Count);\n        foreach (IRjMove<TState> m in moves)\n        {\n            ArgumentNullException.ThrowIfNull(m);\n            if (!_byKey.TryAdd(m.Key, m))\n                throw new ArgumentException($\"Duplicate move key '{m.Key}'.\", nameof(moves));\n        }\n        foreach (IRjMove<TState> m in moves)\n            if (!_byKey.ContainsKey(m.ReverseKey))\n                throw new ArgumentException($\"Move '{m.Key}' names a reverse '{m.ReverseKey}' not in the palette.\", nameof(moves));\n\n        _moves = moves;\n        _target = target;\n        _rng = rng;\n        _moveAttempts = new long[moves.Count];\n        _moveAccepts = new long[moves.Count];\n        Current = start;\n        _currentLogTarget = target.LogPosterior(start);\n    }\n\n    public TState Step()\n    {\n        double totalCur = TotalWeight(Current);\n        if (!(totalCur > 0.0))\n            return Current;   // no move available here → stay\n\n        int idx = SelectMove(Current, totalCur);\n        IRjMove<TState> move = _moves[idx];\n        if (move.Propose(Current, _rng) is not Proposal<TState> p)\n            return Current;   // move unavailable from here → stay\n\n        Attempts++;\n        _moveAttempts[idx]++;\n\n        double totalCand = TotalWeight(p.Candidate);\n        double wReverse = _byKey[move.ReverseKey].Weight(p.Candidate);\n        if (!(wReverse > 0.0) || !(totalCand > 0.0))\n            return Current;   // proposed state can't reverse this move → reject\n\n        double logSelection = Math.Log(wReverse / totalCand) - Math.Log(move.Weight(Current) / totalCur);\n\n        double candidateLogTarget = _target.LogPosterior(p.Candidate);\n        double logAccept = (candidateLogTarget - _currentLogTarget)\n                         + p.LogProposalRatio + p.LogJacobian + logSelection;\n\n        if (logAccept >= 0.0 || _rng.NextDouble() < Math.Exp(logAccept))\n        {\n            Current = p.Candidate;\n            _currentLogTarget = candidateLogTarget;\n            Accepted++;\n            _moveAccepts[idx]++;\n        }\n        return Current;\n    }\n\n    public IReadOnlyList<MoveStat> MoveStats()\n    {\n        var stats = new MoveStat[_moves.Count];\n        for (int i = 0; i < _moves.Count; i++)\n            stats[i] = new MoveStat(_moves[i].Key, _moveAttempts[i], _moveAccepts[i]);\n        return stats;\n    }\n\n    public double CurrentLogTarget => _currentLogTarget;\n\n    public void SetState(TState state, double logTarget)\n    {\n        Current = state;\n        _currentLogTarget = logTarget;\n    }\n\n    private double TotalWeight(TState state)\n    {\n        double total = 0.0;\n        for (int i = 0; i < _moves.Count; i++)\n        {\n            double w = _moves[i].Weight(state);\n            if (w > 0.0) total += w;\n        }\n        return total;\n    }\n\n    private int SelectMove(TState state, double total)\n    {\n        if (_moves.Count == 1)\n            return 0;\n\n        double u = _rng.NextDouble() * total;\n        double cumulative = 0.0;\n        for (int i = 0; i < _moves.Count; i++)\n        {\n            double w = _moves[i].Weight(state);\n            if (w <= 0.0) continue;\n            cumulative += w;\n            if (u < cumulative)\n                return i;\n        }\n        return _moves.Count - 1;   // floating-point guard\n    }\n}
356 | maths/samplers/rjmcmc/ScheduledMove.cs | {723 64 0.1867 4.9001} | 745 | using System;\nusing Maths.Rng;\n\nnamespace Maths.Samplers.Rjmcmc;\n\npublic sealed class ScheduledMove<TState> : IRjMove<TState>\n{\n    private readonly IRjMove<TState> _inner;\n    private readonly Func<TState, double> _weight;\n\n    public ScheduledMove(IRjMove<TState> inner, Func<TState, double> weight)\n    {\n        ArgumentNullException.ThrowIfNull(inner);\n        ArgumentNullException.ThrowIfNull(weight);\n        _inner = inner;\n        _weight = weight;\n    }\n\n    public string Key => _inner.Key;\n    public string ReverseKey => _inner.ReverseKey;\n    public double Weight(TState state) => _weight(state);\n    public Proposal<TState>? Propose(TState current, Xoshiro256PlusPlus rng) => _inner.Propose(current, rng);\n}
357 | maths/samplers/rjmcmc/TemperedTarget.cs | {584 56 0.1986 4.7565} | 605 | using System;\n\nnamespace Maths.Samplers.Rjmcmc;\n\npublic sealed class TemperedTarget<TState> : IRjTarget<TState>\n{\n    private readonly IRjTarget<TState> _base;\n\n    public double Beta { get; }\n\n    public TemperedTarget(IRjTarget<TState> baseTarget, double beta)\n    {\n        ArgumentNullException.ThrowIfNull(baseTarget);\n        if (!(beta > 0.0)) throw new ArgumentOutOfRangeException(nameof(beta), \"Inverse temperature must be positive.\");\n        _base = baseTarget;\n        Beta = beta;\n    }\n\n    public double LogPosterior(TState state) => Beta * _base.LogPosterior(state);\n}
358 | maths/samplers/SpectralNoiseField.cs | {5208 741 0.3470 4.4990} | 5368 | using System;\nusing System.Numerics;\nusing Maths.LinAlg;\nusing Maths.Rng;\n\nnamespace Maths.Samplers;\n\npublic sealed class SpectralNoiseField\n{\n    public const double White = 0.0;\n    public const double Pink = 1.0;\n    public const double Brown = 2.0;\n\n    private readonly int[] _dims;\n    private readonly int[] _stride;\n    private readonly double[] _min;   // box lower corner per axis\n    private readonly double[] _span;  // box extent per axis\n    private readonly double[] _field; // standardized real field, length ∏ dims\n\n    public int Dimension => _dims.Length;\n\n    public double Max { get; }\n\n    public double Min { get; }\n\n    private SpectralNoiseField(\n        int[] dims, int[] stride, double[] min, double[] span, double[] field, double lo, double hi)\n    {\n        _dims = dims; _stride = stride; _min = min; _span = span; _field = field;\n        Min = lo; Max = hi;\n    }\n\n    public static SpectralNoiseField Generate(\n        Xoshiro256PlusPlus rng, int gridSize, double beta, double[] boxMin, double[] boxMax)\n    {\n        ArgumentNullException.ThrowIfNull(rng);\n        ArgumentNullException.ThrowIfNull(boxMin);\n        ArgumentNullException.ThrowIfNull(boxMax);\n        if (!Fft.IsPowerOfTwo(gridSize))\n            throw new ArgumentException($\"gridSize must be a power of two; got {gridSize}.\", nameof(gridSize));\n        if (boxMin.Length != boxMax.Length || boxMin.Length == 0)\n            throw new ArgumentException(\"boxMin and boxMax must be non-empty and equal length.\");\n\n        int d = boxMin.Length;\n        var dims = new int[d];\n        for (int i = 0; i < d; i++) dims[i] = gridSize;\n\n        var stride = new int[d];\n        stride[d - 1] = 1;\n        for (int i = d - 2; i >= 0; i--) stride[i] = stride[i + 1] * dims[i + 1];\n\n        int total = 1;\n        for (int i = 0; i < d; i++) total *= gridSize;\n\n        var grid = new Complex[total];\n        for (int i = 0; i < total; i++)\n            grid[i] = new Complex(NextGaussian(rng), 0.0);\n\n        Fft.ForwardND(grid, dims);\n\n        int half = gridSize / 2;\n        for (int idx = 0; idx < total; idx++)\n        {\n            double k2 = 0.0;\n            int rem = idx;\n            for (int ax = 0; ax < d; ax++)\n            {\n                int digit = rem / stride[ax];\n                rem -= digit * stride[ax];\n                double f = digit <= half ? digit : digit - gridSize;\n                k2 += f * f;\n            }\n            double filter = k2 == 0.0 ? 0.0 : Math.Pow(k2, -beta / 4.0);\n            grid[idx] *= filter;\n        }\n\n        Fft.InverseND(grid, dims);\n\n        var field = new double[total];\n        double mean = 0.0;\n        for (int i = 0; i < total; i++) { field[i] = grid[i].Real; mean += field[i]; }\n        mean /= total;\n\n        double variance = 0.0;\n        for (int i = 0; i < total; i++) { field[i] -= mean; variance += field[i] * field[i]; }\n        variance /= total;\n        double sd = Math.Sqrt(variance);\n        if (sd < 1e-300) sd = 1.0;\n\n        double lo = double.PositiveInfinity, hi = double.NegativeInfinity;\n        for (int i = 0; i < total; i++)\n        {\n            field[i] /= sd;\n            if (field[i] < lo) lo = field[i];\n            if (field[i] > hi) hi = field[i];\n        }\n\n        var min = new double[d];\n        var span = new double[d];\n        for (int i = 0; i < d; i++) { min[i] = boxMin[i]; span[i] = boxMax[i] - boxMin[i]; }\n        return new SpectralNoiseField(dims, stride, min, span, field, lo, hi);\n    }\n\n    public double Sample(ReadOnlySpan<double> point)\n    {\n        int d = _dims.Length;\n        if (point.Length < d)\n            throw new ArgumentException($\"point must have at least {d} components.\", nameof(point));\n\n        Span<int> lo = d <= 16 ? stackalloc int[d] : new int[d];\n        Span<double> frac = d <= 16 ? stackalloc double[d] : new double[d];\n        for (int ax = 0; ax < d; ax++)\n        {\n            double t = _span[ax] <= 0.0 ? 0.0 : (point[ax] - _min[ax]) / _span[ax];\n            t = Math.Clamp(t, 0.0, 1.0) * (_dims[ax] - 1);\n            int l = (int)Math.Floor(t);\n            if (l >= _dims[ax] - 1) { l = _dims[ax] - 1; frac[ax] = 0.0; }\n            else frac[ax] = t - l;\n            lo[ax] = l;\n        }\n\n        double acc = 0.0;\n        int corners = 1 << d;\n        for (int mask = 0; mask < corners; mask++)\n        {\n            double w = 1.0;\n            int flat = 0;\n            for (int ax = 0; ax < d; ax++)\n            {\n                int bit = (mask >> ax) & 1;\n                int coord = lo[ax] + bit;\n                if (coord >= _dims[ax]) coord = _dims[ax] - 1;\n                w *= bit == 1 ? frac[ax] : 1.0 - frac[ax];\n                flat += coord * _stride[ax];\n            }\n            if (w != 0.0) acc += w * _field[flat];\n        }\n        return acc;\n    }\n\n    public double Sample(double x, double y, double z)\n    {\n        Span<double> p = stackalloc double[3] { x, y, z };\n        return Sample(p);\n    }\n\n    private static double NextGaussian(Xoshiro256PlusPlus rng)\n    {\n        double u1 = 1.0 - rng.NextDouble();\n        double u2 = 1.0 - rng.NextDouble();\n        return Math.Sqrt(-2.0 * Math.Log(u1)) * Math.Cos(2.0 * Math.PI * u2);\n    }\n}
359 | maths/topology/Barcode.cs | {690 75 0.2246 4.7520} | 721 | #nullable enable\nusing System;\nusing System.Collections.Generic;\n\nnamespace Maths.Topology;\n\npublic readonly record struct Bar(\n    double Birth,\n    double Death,\n    int Dimension,\n    int? Generator = null)\n{\n    public double Persistence =>\n        double.IsPositiveInfinity(Death) ? double.PositiveInfinity : Death - Birth;\n\n    public bool IsInfinite => double.IsPositiveInfinity(Death);\n}\n\npublic sealed class Barcode\n{\n    public IReadOnlyList<Bar> Bars { get; }\n    public string AxisLabel { get; }\n\n    public Barcode(IReadOnlyList<Bar> bars, string axisLabel = \"\")\n    {\n        ArgumentNullException.ThrowIfNull(bars);\n        Bars = bars;\n        AxisLabel = axisLabel;\n    }\n}
360 | maths/topology/DiagramMetrics.cs | {5896 714 0.4194 4.1823} | 6100 | #nullable enable\nusing System;\nusing System.Collections.Generic;\n\nnamespace Maths.Topology;\n\npublic static class DiagramMetrics\n{\n    internal enum EssentialKind { InfiniteOnMismatch, FinitePenalty }\n\n    public readonly record struct EssentialPolicy\n    {\n        internal EssentialKind Kind { get; init; }\n        internal double PerBar { get; init; }\n\n        public static EssentialPolicy InfiniteOnMismatch => default;\n\n        public static EssentialPolicy FinitePenalty(double perBar) =>\n            new() { Kind = EssentialKind.FinitePenalty, PerBar = perBar };\n    }\n\n    public static double Wasserstein(\n        Barcode a,\n        Barcode b,\n        int dimension,\n        double p = 2.0,\n        EssentialPolicy essential = default)\n    {\n        ArgumentNullException.ThrowIfNull(a);\n        ArgumentNullException.ThrowIfNull(b);\n        ValidateP(p);\n\n        int essA = CountEssential(a, dimension);\n        int essB = CountEssential(b, dimension);\n\n        if (essential.Kind == EssentialKind.InfiniteOnMismatch && essA != essB)\n            return double.PositiveInfinity;\n\n        var finiteA = CollectFinite(a, dimension);\n        var finiteB = CollectFinite(b, dimension);\n\n        double[,] cost = BuildCost(finiteA, finiteB, p, out double big);\n        int s = finiteA.Count + finiteB.Count;\n        double assignmentSum = s == 0 ? 0.0 : MinAssignment(cost, s);\n\n        if (essential.Kind == EssentialKind.FinitePenalty)\n        {\n            int surplus = Math.Abs(essA - essB);\n            if (surplus > 0)\n                assignmentSum += surplus * Math.Pow(essential.PerBar, p);\n        }\n\n        return Math.Pow(assignmentSum, 1.0 / p);\n    }\n\n    public static double Bottleneck(\n        Barcode a,\n        Barcode b,\n        int dimension,\n        EssentialPolicy essential = default) =>\n        throw new NotImplementedException(\"Bottleneck (Hopcroft–Karp threshold matching) is P1.\");\n\n    static void ValidateP(double p)\n    {\n        if (p < 1.0)\n            throw new ArgumentOutOfRangeException(nameof(p), \"Wasserstein requires p >= 1.\");\n        if (double.IsPositiveInfinity(p))\n            throw new ArgumentOutOfRangeException(nameof(p), \"Use Bottleneck for p = infinity.\");\n    }\n\n    static int CountEssential(Barcode barcode, int dimension)\n    {\n        int count = 0;\n        foreach (Bar bar in barcode.Bars)\n            if (bar.Dimension == dimension && bar.IsInfinite)\n                count++;\n        return count;\n    }\n\n    static List<Bar> CollectFinite(Barcode barcode, int dimension)\n    {\n        var list = new List<Bar>();\n        foreach (Bar bar in barcode.Bars)\n            if (bar.Dimension == dimension && !bar.IsInfinite)\n                list.Add(bar);\n        return list;\n    }\n\n    static double Dinf(in Bar p, in Bar q) =>\n        Math.Max(Math.Abs(p.Birth - q.Birth), Math.Abs(p.Death - q.Death));\n\n    static double Diag(in Bar p) => 0.5 * (p.Death - p.Birth);\n\n    static double[,] BuildCost(IReadOnlyList<Bar> a, IReadOnlyList<Bar> b, double p, out double big)\n    {\n        int n = a.Count, m = b.Count, s = n + m;\n        var c = new double[s, s];\n        double maxReal = 0.0;\n\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < m; j++)\n                maxReal = Math.Max(maxReal, Dinf(a[i], b[j]));\n        for (int i = 0; i < n; i++)\n            maxReal = Math.Max(maxReal, Diag(a[i]));\n        for (int j = 0; j < m; j++)\n            maxReal = Math.Max(maxReal, Diag(b[j]));\n\n        big = Math.Pow(maxReal + 1.0, p) * (s + 1) + 1.0;\n\n        for (int i = 0; i < n; i++)\n        {\n            for (int j = 0; j < m; j++)\n                c[i, j] = Math.Pow(Dinf(a[i], b[j]), p);\n            for (int k = 0; k < n; k++)\n                c[i, m + k] = k == i ? Math.Pow(Diag(a[i]), p) : big;\n        }\n\n        for (int j = 0; j < m; j++)\n        {\n            for (int jj = 0; jj < m; jj++)\n                c[n + j, jj] = jj == j ? Math.Pow(Diag(b[j]), p) : big;\n            for (int k = 0; k < n; k++)\n                c[n + j, m + k] = 0.0;\n        }\n\n        return c;\n    }\n\n    static double MinAssignment(double[,] a, int n)\n    {\n        const double inf = double.PositiveInfinity;\n        var u = new double[n + 1];\n        var v = new double[n + 1];\n        var p = new int[n + 1];\n        var way = new int[n + 1];\n\n        for (int i = 1; i <= n; i++)\n        {\n            p[0] = i;\n            int j0 = 0;\n            var minv = new double[n + 1];\n            var used = new bool[n + 1];\n            Array.Fill(minv, inf);\n\n            do\n            {\n                used[j0] = true;\n                int i0 = p[j0];\n                int j1 = -1;\n                double delta = inf;\n\n                for (int j = 1; j <= n; j++)\n                {\n                    if (used[j]) continue;\n                    double cur = a[i0 - 1, j - 1] - u[i0] - v[j];\n                    if (cur < minv[j])\n                    {\n                        minv[j] = cur;\n                        way[j] = j0;\n                    }\n                    if (minv[j] < delta)\n                    {\n                        delta = minv[j];\n                        j1 = j;\n                    }\n                }\n\n                for (int j = 0; j <= n; j++)\n                {\n                    if (used[j])\n                    {\n                        u[p[j]] += delta;\n                        v[j] -= delta;\n                    }\n                    else\n                    {\n                        minv[j] -= delta;\n                    }\n                }\n\n                j0 = j1;\n            } while (p[j0] != 0);\n\n            do\n            {\n                int j1 = way[j0];\n                p[j0] = p[j1];\n                j0 = j1;\n            } while (j0 != 0);\n        }\n\n        double res = 0.0;\n        for (int j = 1; j <= n; j++)\n            res += a[p[j] - 1, j - 1];\n        return res;\n    }\n}
361 | maths/topology/PersistentCohomology.cs | {6204 546 0.3873 4.2188} | 6384 | #nullable enable\nusing System;\nusing System.Collections.Generic;\nusing System.Linq;\n\nnamespace Maths.Topology;\n\npublic static class PersistentCohomology\n{\n    public static Barcode Compute(SimplicialFiltration filtration, int maxDimension = int.MaxValue, double cutoff = 0.0)\n    {\n        ArgumentNullException.ThrowIfNull(filtration);\n\n        int n = filtration.Simplices.Count;\n        var bars = new List<Bar>();\n        (List<Bar> h0Bars, HashSet<int> toReduce, HashSet<int> toSkip) =\n            ComputeH0(filtration, maxDimension, cutoff);\n        bars.AddRange(h0Bars);\n\n        if (maxDimension <= 0)\n            return new Barcode(bars, filtration.Label);\n\n        var pivotOwner = new Dictionary<int, int>();\n        var reduced = new SortedSet<int>[n];\n        var reducedKeys = new HashSet<int>();\n        var deathSimplices = new HashSet<int>();\n\n        var columnsToReduce = toReduce.ToList();\n        var columnsToSkip = toSkip.ToList();\n\n        for (int dim = 1; dim <= maxDimension; dim++)\n        {\n            var columns = new List<int>(columnsToReduce);\n            if (dim > 1)\n                columns.Sort((a, b) => b.CompareTo(a));\n            else\n                columns.Sort();\n\n            foreach (int j in columns)\n            {\n                int dimJ = filtration.Simplices[j].Dimension;\n\n                var col = new SortedSet<int>(filtration.GetCoboundaryIndices(j));\n                while (col.Count > 0 && pivotOwner.TryGetValue(col.Max, out int owner))\n                    foreach (int r in reduced[owner])\n                    {\n                        if (!col.Remove(r)) col.Add(r);\n                    }\n\n                reduced[j] = col;\n\n                if (col.Count == 0)\n                {\n                    if (!deathSimplices.Contains(j)\n                        && dimJ <= maxDimension\n                        && Persistence(filtration.Simplices[j].FiltrationValue, double.PositiveInfinity) > cutoff)\n                        bars.Add(new Bar(\n                            filtration.Simplices[j].FiltrationValue,\n                            double.PositiveInfinity,\n                            dimJ, j));\n                    continue;\n                }\n\n                int pivot = col.Max;\n                pivotOwner[pivot] = j;\n                reducedKeys.Add(pivot);\n                deathSimplices.Add(pivot);\n\n                double birth = filtration.Simplices[j].FiltrationValue;\n                double death = filtration.Simplices[pivot].FiltrationValue;\n                if (dimJ <= maxDimension && Persistence(birth, death) > cutoff)\n                    bars.Add(new Bar(birth, death, dimJ, j));\n            }\n\n            if (dim >= maxDimension)\n                break;\n\n            var nextReduce = new HashSet<int>();\n            var nextSkip = new HashSet<int>();\n            foreach (int sigma in columnsToReduce.Concat(columnsToSkip))\n            {\n                foreach (int tau in filtration.GetCoboundaryIndices(sigma))\n                {\n                    if (filtration.Simplices[tau].Dimension != dim + 1)\n                        continue;\n\n                    if (reducedKeys.Contains(tau))\n                        nextSkip.Add(tau);\n                    else\n                        nextReduce.Add(tau);\n                }\n            }\n\n            columnsToReduce = nextReduce.ToList();\n            columnsToSkip = nextSkip.ToList();\n        }\n\n        return new Barcode(bars, filtration.Label);\n    }\n\n    static double Persistence(double birth, double death) =>\n        double.IsPositiveInfinity(death) ? double.PositiveInfinity : death - birth;\n\n    static (List<Bar> Bars, HashSet<int> ToReduce, HashSet<int> ToSkip) ComputeH0(\n        SimplicialFiltration filtration, int maxDimension, double cutoff)\n    {\n        var bars = new List<Bar>();\n        var toReduce = new HashSet<int>();\n        var toSkip = new HashSet<int>();\n        int n = filtration.Simplices.Count;\n\n        var vertexIndex = new Dictionary<int, int>();\n        var birth = new double[n];\n        var parent = new int[n];\n        var birthId = new int[n];\n        int nextId = 0;\n\n        for (int i = 0; i < n; i++)\n        {\n            parent[i] = i;\n            if (filtration.Simplices[i].Dimension == 0)\n            {\n                int v = filtration.Simplices[i].Vertices[0];\n                vertexIndex[v] = i;\n                birth[i] = filtration.Simplices[i].FiltrationValue;\n                birthId[i] = nextId++;\n            }\n        }\n\n        var edges = new List<int>();\n        for (int i = 0; i < n; i++)\n            if (filtration.Simplices[i].Dimension == 1)\n                edges.Add(i);\n\n        edges.Sort((a, b) => filtration.Simplices[a].FiltrationValue\n            .CompareTo(filtration.Simplices[b].FiltrationValue));\n\n        int Find(int x)\n        {\n            int r = x;\n            while (parent[r] != r) r = parent[r];\n            while (parent[x] != r) { int p = parent[x]; parent[x] = r; x = p; }\n            return r;\n        }\n\n        foreach (int e in edges)\n        {\n            int u = filtration.Simplices[e].Vertices[0];\n            int v = filtration.Simplices[e].Vertices[1];\n            int i = vertexIndex[u];\n            int j = vertexIndex[v];\n            int ri = Find(i);\n            int rj = Find(j);\n            double t = filtration.Simplices[e].FiltrationValue;\n\n            if (ri != rj)\n            {\n                int elder = birth[ri] > birth[rj] || (birth[ri] == birth[rj] && birthId[ri] > birthId[rj]) ? ri : rj;\n                int younger = elder == ri ? rj : ri;\n                if (maxDimension >= 0 && Persistence(birth[younger], t) > cutoff)\n                    bars.Add(new Bar(birth[younger], t, 0));\n\n                parent[younger] = elder;\n                toSkip.Add(e);\n            }\n            else\n            {\n                toReduce.Add(e);\n            }\n        }\n\n        for (int i = 0; i < n; i++)\n        {\n            if (filtration.Simplices[i].Dimension != 0) continue;\n            if (Find(i) == i && maxDimension >= 0)\n                bars.Add(new Bar(birth[i], double.PositiveInfinity, 0));\n        }\n\n        return (bars, toReduce, toSkip);\n    }\n}
362 | maths/topology/PersistentHomology.cs | {1836 153 0.3954 4.1263} | 1894 | #nullable enable\nusing System;\nusing System.Collections.Generic;\n\nnamespace Maths.Topology;\n\npublic static class PersistentHomology\n{\n    public static Barcode Compute(SimplicialFiltration filtration, int maxDimension = int.MaxValue)\n    {\n        ArgumentNullException.ThrowIfNull(filtration);\n\n        int n = filtration.Simplices.Count;\n        var bars = new List<Bar>();\n        var paired = new bool[n];\n        var pivotCol = new Dictionary<int, int>(n);\n        var reducedCols = new SortedSet<int>[n];\n\n        for (int j = 0; j < n; j++)\n        {\n            var col = new SortedSet<int>(filtration.GetBoundaryIndices(j));\n\n            while (col.Count > 0 && pivotCol.TryGetValue(col.Max, out int i))\n                foreach (int r in reducedCols[i])\n                {\n                    if (!col.Remove(r)) col.Add(r);\n                }\n\n            reducedCols[j] = col;\n\n            if (col.Count > 0)\n            {\n                int pivot = col.Max;\n                pivotCol[pivot] = j;\n                paired[pivot] = true;\n                paired[j] = true;\n\n                int dim = filtration.Simplices[pivot].Dimension;\n                if (dim <= maxDimension)\n                    bars.Add(new Bar(\n                        filtration.Simplices[pivot].FiltrationValue,\n                        filtration.Simplices[j].FiltrationValue,\n                        dim, pivot));\n            }\n        }\n\n        for (int i = 0; i < n; i++)\n            if (!paired[i])\n            {\n                int dim = filtration.Simplices[i].Dimension;\n                if (dim <= maxDimension)\n                    bars.Add(new Bar(\n                        filtration.Simplices[i].FiltrationValue,\n                        double.PositiveInfinity, dim, i));\n            }\n\n        return new Barcode(bars, filtration.Label);\n    }\n}
363 | maths/topology/Simplex.cs | {428 50 0.2523 4.5455} | 446 | #nullable enable\nusing System;\n\nnamespace Maths.Topology;\n\npublic readonly struct Simplex\n{\n    public int[] Vertices { get; }\n    public double FiltrationValue { get; }\n    public int Dimension => Vertices.Length - 1;\n\n    public Simplex(double filtrationValue, params int[] vertices)\n    {\n        var v = (int[])vertices.Clone();\n        Array.Sort(v);\n        Vertices = v;\n        FiltrationValue = filtrationValue;\n    }\n}
364 | maths/topology/SimplicialFiltration.cs | {2516 275 0.3700 4.3939} | 2609 | #nullable enable\nusing System;\nusing System.Collections.Generic;\nusing System.Linq;\n\nnamespace Maths.Topology;\n\npublic sealed class SimplicialFiltration\n{\n    public IReadOnlyList<Simplex> Simplices { get; }\n    public string Label { get; }\n\n    private readonly Dictionary<string, int> _index;\n\n    public SimplicialFiltration(IEnumerable<Simplex> simplices, string label = \"\")\n    {\n        ArgumentNullException.ThrowIfNull(simplices);\n\n        var sorted = simplices\n            .OrderBy(s => s.FiltrationValue)\n            .ThenBy(s => s.Dimension)\n            .ToList();\n\n        Simplices = sorted;\n        Label = label;\n\n        _index = new Dictionary<string, int>(sorted.Count);\n        for (int i = 0; i < sorted.Count; i++)\n            _index[Key(sorted[i].Vertices)] = i;\n\n        for (int j = 0; j < sorted.Count; j++)\n        {\n            var v = sorted[j].Vertices;\n            if (v.Length <= 1) continue;\n            for (int k = 0; k < v.Length; k++)\n            {\n                string faceKey = Key(RemoveAt(v, k));\n                if (!_index.TryGetValue(faceKey, out int fi))\n                    throw new ArgumentException(\n                        $\"Simplex [{Key(v)}] at index {j} is missing face [{faceKey}].\");\n                if (fi > j)\n                    throw new ArgumentException(\n                        $\"Face [{faceKey}] (index {fi}) appears after coface [{Key(v)}] (index {j}).\");\n            }\n        }\n    }\n\n    internal int[] GetBoundaryIndices(int j)\n    {\n        var v = Simplices[j].Vertices;\n        if (v.Length <= 1) return Array.Empty<int>();\n        var result = new int[v.Length];\n        for (int k = 0; k < v.Length; k++)\n            result[k] = _index[Key(RemoveAt(v, k))];\n        return result;\n    }\n\n    internal int[] GetCoboundaryIndices(int j)\n    {\n        var cofaces = new List<int>();\n        for (int k = j + 1; k < Simplices.Count; k++)\n        {\n            int[] boundary = GetBoundaryIndices(k);\n            for (int b = 0; b < boundary.Length; b++)\n            {\n                if (boundary[b] == j)\n                {\n                    cofaces.Add(k);\n                    break;\n                }\n            }\n        }\n\n        return cofaces.ToArray();\n    }\n\n    static string Key(int[] verts) => string.Join(\",\", verts);\n\n    static int[] RemoveAt(int[] v, int k)\n    {\n        var face = new int[v.Length - 1];\n        for (int i = 0, w = 0; i < v.Length; i++)\n            if (i != k) face[w++] = v[i];\n        return face;\n    }\n}
