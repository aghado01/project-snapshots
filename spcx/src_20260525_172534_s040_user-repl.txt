idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
260 | user-repl/commands/MixtureSpecParser.cs | {2136 191 0.2898 4.5979} | 2235 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.Linq;\nusing Graphs.Coupling;\n\nnamespace UserRepl.Commands;\n\npublic static class MixtureSpecParser\n{\n    public static MixtureWeights ParseWeights(string spec)\n    {\n        var dict = ParseKeyValuePairs(spec);\n        return new MixtureWeights(\n            Gaussian:  Get(dict, \"gauss\", \"gaussian\", \"g\"),\n            Cauchy:    Get(dict, \"cauchy\", \"c\"),\n            Laplacian: Get(dict, \"laplace\", \"laplacian\", \"l\"));\n    }\n\n    public static MixtureBandwidth ParseBandwidth(string spec)\n    {\n        var dict = ParseKeyValuePairs(spec);\n        return new MixtureBandwidth(\n            Gaussian:  Get(dict, \"gauss\", \"gaussian\", \"g\"),\n            Cauchy:    Get(dict, \"cauchy\", \"c\"),\n            Laplacian: Get(dict, \"laplace\", \"laplacian\", \"l\"));\n    }\n\n    private static Dictionary<string, double> ParseKeyValuePairs(string spec)\n    {\n        if (string.IsNullOrWhiteSpace(spec))\n            throw new ArgumentException(\"Mixture spec must be non-empty.\", nameof(spec));\n\n        var dict = new Dictionary<string, double>(StringComparer.OrdinalIgnoreCase);\n        foreach (string raw in spec.Split(',', StringSplitOptions.RemoveEmptyEntries))\n        {\n            string token = raw.Trim();\n            int eq = token.IndexOf('=');\n            if (eq <= 0)\n                throw new ArgumentException(\n                    $\"Mixture token '{token}' must be in the form name=value.\");\n\n            string key = token.Substring(0, eq).Trim();\n            string val = token.Substring(eq + 1).Trim();\n\n            if (!double.TryParse(val, NumberStyles.Float, CultureInfo.InvariantCulture, out double v))\n                throw new ArgumentException(\n                    $\"Cannot parse mixture value '{val}' for component '{key}' as double.\");\n\n            dict[key] = v;\n        }\n        return dict;\n    }\n\n    private static double Get(Dictionary<string, double> dict, params string[] aliases)\n    {\n        foreach (string a in aliases)\n            if (dict.TryGetValue(a, out double v))\n                return v;\n        return 0.0;\n    }\n}
261 | user-repl/commands/RunManifest.cs | {4102 264 0.4539 3.8572} | 4217 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Text.Json;\n\nnamespace UserRepl.Commands;\n\npublic sealed record RunManifest(\n    string                    SchemaVersion,\n    DateTime                  CreatedUtc,\n    string                    Algorithm,        // \"spc\" | \"hdbscan\"\n    string                    CommandLine,\n    DatasetSpec               Dataset,\n    GraphSpec?                Graph,            // null for hdbscan\n    SweepSpec?                Sweep,            // null for hdbscan\n    HdbscanSpec?              Hdbscan,          // null for spc\n    OutputSpec                Output)\n{\n    public const string CurrentSchemaVersion = \"1.0\";\n    public const string FileName = \"manifest.json\";\n\n    public static string PathFor(string runDirectory) => Path.Combine(runDirectory, FileName);\n\n    public void WriteTo(string runDirectory)\n    {\n        string path = PathFor(runDirectory);\n        Directory.CreateDirectory(runDirectory);\n        File.WriteAllText(path, JsonSerializer.Serialize(this, JsonOptions));\n    }\n\n    public static RunManifest ReadFrom(string runDirectory)\n    {\n        string path = PathFor(runDirectory);\n        if (!File.Exists(path))\n            throw new FileNotFoundException($\"No manifest at {path}.\", path);\n        string json = File.ReadAllText(path);\n        return JsonSerializer.Deserialize<RunManifest>(json, JsonOptions)\n            ?? throw new InvalidDataException($\"Manifest at {path} deserialized to null.\");\n    }\n\n    public static readonly JsonSerializerOptions JsonOptions = new()\n    {\n        WriteIndented = true,\n        PropertyNamingPolicy = JsonNamingPolicy.SnakeCaseLower,\n    };\n}\n\npublic sealed record DatasetSpec(\n    string                              Source,                 // \"synthetic\" | \"csv\"\n    string?                             GeneratorName,\n    IReadOnlyDictionary<string, object?>? GeneratorParameters,\n    int?                                Seed,                   // synthetic-only\n    string?                             CsvPath,\n    string?                             LabelColumn,\n    bool?                               HasHeader,\n    string?                             Delimiter);             // serialized as string for readability\n\npublic sealed record GraphSpec(\n    string                              ProximityRule,          // enum name\n    int                                 K,\n    double                              Epsilon,\n    string                              KernelType,             // enum name\n    double                              Bandwidth,\n    string?                             DistanceMetric,         // spec string (\"euclidean\", \"minkowski:p=1.5\", ...)\n    bool                                EnsureConnected,\n    bool                                ApplyLocalMutualProximity,\n    IReadOnlyDictionary<string, double>? MixtureWeights,\n    IReadOnlyDictionary<string, double>? MixtureBandwidth);\n\npublic sealed record SweepSpec(\n    string                              Schedule,               // \"Adaptive\" | \"FixedGrid\"\n    string?                             TemperaturesSpec,       // raw --temperatures string for FixedGrid\n    int                                 Replicas,\n    int                                 BurnInCycles,\n    int                                 MeasureCycles,\n    int                                 FinalBurn,\n    int                                 FinalCycles,\n    int                                 Q,\n    string                              Analyzer,\n    string                              PartitionStrategy,\n    double                              Theta);\n\npublic sealed record HdbscanSpec(\n    int                                 MinPts,\n    int?                                MinClusterSize,\n    bool                                AllowSingleCluster,\n    string                              DistanceMetric,\n    double                              MinkowskiP);\n\npublic sealed record OutputSpec(\n    string                              RunDirectory,\n    string?                             CheckpointDirectory);
