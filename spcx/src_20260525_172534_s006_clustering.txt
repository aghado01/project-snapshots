idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
57 | clustering/graphical/spc/runtime/execution/sinks/NullFrameSink.cs | {312 33 0.1795 4.7717} | 324 | namespace Clustering.Graphical.SPC.Runtime.Execution.Sinks;\n\npublic sealed class NullFrameSink : IFrameSink\n{\n    public static NullFrameSink Instance { get; } = new();\n\n    public bool TaskAlreadyCompleted(SpcTaskSpec task) => false;\n\n    public void Accept(SpcTaskSpec task, SpcRunResult result)\n    {\n\n    }\n}
58 | clustering/graphical/spc/runtime/execution/sinks/SpcxDiskFrameSink.cs | {2895 197 0.2663 4.6284} | 2987 | using System;\nusing System.IO;\nusing Clustering.Graphical.SPC.Runtime.Sampler;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution.Sinks;\n\npublic sealed class SpcxDiskFrameSink : IFrameSink\n{\n    public static SpcxDiskFrameSink Instance { get; } = new();\n\n    public bool TaskAlreadyCompleted(SpcTaskSpec task)\n    {\n        if (task is null)\n            throw new ArgumentNullException(nameof(task));\n        if (task.CheckpointPath is null)\n            throw new InvalidOperationException(\n                $\"{nameof(SpcxDiskFrameSink)} requires {nameof(SpcTaskSpec.CheckpointPath)} to be non-null.\");\n\n        if (!File.Exists(task.CheckpointPath))\n            return false;\n\n        if (task.ObservableTier == PottsObservableTier.WithEdgeObservables)\n        {\n            string spcePath = Path.ChangeExtension(task.CheckpointPath, \".spce\");\n            if (!File.Exists(spcePath))\n                return false;\n        }\n\n        return true;\n    }\n\n    public void Accept(SpcTaskSpec task, SpcRunResult result)\n    {\n        if (task is null)\n            throw new ArgumentNullException(nameof(task));\n        if (result is null)\n            throw new ArgumentNullException(nameof(result));\n        if (task.CheckpointPath is null)\n            throw new InvalidOperationException(\n                $\"{nameof(SpcxDiskFrameSink)} requires {nameof(SpcTaskSpec.CheckpointPath)} to be non-null.\");\n\n        string? dir = Path.GetDirectoryName(task.CheckpointPath);\n        if (!string.IsNullOrEmpty(dir))\n            Directory.CreateDirectory(dir);\n\n        PottsCoreObservablesSerializer.Instance.WriteToFile(result.Frame, task.CheckpointPath);\n\n        if (result.EdgeObservables is not null)\n        {\n            string spcePath = Path.ChangeExtension(task.CheckpointPath, \".spce\");\n            PottsEdgeObservablesSerializer.Instance.WriteToFile(result.EdgeObservables, spcePath);\n        }\n    }\n\n    public SpcRunResult? TryLoad(SpcTaskSpec task, CsrGraph graph)\n    {\n        if (task is null)\n            throw new ArgumentNullException(nameof(task));\n\n        if (task.CheckpointPath is null || !File.Exists(task.CheckpointPath))\n            return null;\n\n        var frame = PottsCoreObservablesSerializer.Instance.ReadFromFile(task.CheckpointPath);\n\n        PottsModelEdgeObservables? edges = null;\n        if (task.ObservableTier == PottsObservableTier.WithEdgeObservables)\n        {\n            string spcePath = Path.ChangeExtension(task.CheckpointPath, \".spce\");\n            if (!File.Exists(spcePath))\n                return null;\n            edges = PottsEdgeObservablesSerializer.Instance.ReadFromFile(spcePath);\n        }\n\n        return new SpcRunResult\n        {\n            Graph           = graph,\n            Frame           = frame,\n            EdgeObservables = edges,\n        };\n    }\n}
59 | clustering/graphical/spc/runtime/execution/SpcBatchResult.cs | {516 60 0.1764 4.6402} | 535 | using System;\nusing System.Collections.Generic;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic sealed record SpcBatchResult\n{\n\n    public required IReadOnlyList<SpcRunResult> Runs { get; init; }\n\n    public required IReadOnlyList<SpcRunResult?> AlignedRuns { get; init; }\n\n    public required int RequestedTaskCount { get; init; }\n\n    public required int ExecutedTaskCount { get; init; }\n\n    public required int SkippedTaskCount { get; init; }\n\n    public required TimeSpan Elapsed { get; init; }\n}
60 | clustering/graphical/spc/runtime/execution/SpcExecutionOptions.cs | {375 42 0.1680 4.7542} | 389 | using Clustering.Graphical.SPC.Runtime.Execution.Sinks;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic sealed record SpcExecutionOptions\n{\n\n    public int MaxDegreeOfParallelism { get; init; } = -1;\n\n    public IFrameSink? FrameSink { get; init; }\n\n    public int? BaseSeed { get; init; }\n\n    public int TemperatureQuantizationFactor { get; init; } = 1000;\n}
61 | clustering/graphical/spc/runtime/execution/SpcExecutor.cs | {9551 721 0.3074 4.4898} | 9847 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.IO;\nusing System.Threading;\nusing System.Threading.Tasks;\nusing Clustering.Graphical.SPC.Runtime.Execution.Sinks;\nusing Clustering.Graphical.SPC.Runtime.Sampler;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Clustering.Graphical.SPC.Runtime.Scheduling;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic sealed class SpcExecutor : ISpcExecutor\n{\n\n    public List<SpcTaskSpec> BuildTaskList(\n        IReadOnlyList<double> temperatures,\n        int numReplicas,\n        int cyclesPerTask,\n        int q,\n        PottsObservableTier observableTier,\n        string checkpointDirectory,\n        int? baseSeed = null,\n        int burnInCyclesPerTask = 0)\n    {\n        if (temperatures is null || temperatures.Count == 0)\n            throw new ArgumentException(\"Temperatures must be non-empty.\", nameof(temperatures));\n        if (numReplicas <= 0)\n            throw new ArgumentOutOfRangeException(nameof(numReplicas), \"Replica count must be positive.\");\n        if (cyclesPerTask <= 0)\n            throw new ArgumentOutOfRangeException(nameof(cyclesPerTask), \"Cycles per task must be positive.\");\n        if (burnInCyclesPerTask < 0)\n            throw new ArgumentOutOfRangeException(nameof(burnInCyclesPerTask), \"Burn-in cycle count cannot be negative.\");\n        if (string.IsNullOrWhiteSpace(checkpointDirectory))\n            throw new ArgumentException(\"Checkpoint directory must be provided.\", nameof(checkpointDirectory));\n\n        var tasks = new List<SpcTaskSpec>(temperatures.Count * numReplicas);\n        foreach (double temperature in temperatures)\n        {\n            for (int replica = 0; replica < numReplicas; replica++)\n            {\n                tasks.Add(new SpcTaskSpec\n                {\n                    Temperature = temperature,\n                    ReplicaIndex = replica,\n                    Cycles = cyclesPerTask,\n                    Q = q,\n                    ObservableTier = observableTier,\n                    CheckpointPath = Path.Combine(checkpointDirectory, FileNameFor(temperature, replica)),\n                    BaseSeed = baseSeed,\n                    BurnInCycles = burnInCyclesPerTask,\n                });\n            }\n        }\n\n        return tasks;\n    }\n\n    public void Run(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (tasks is null)\n            throw new ArgumentNullException(nameof(tasks));\n        if (tasks.Count == 0)\n            return;\n\n        IFrameSink sink = executionOptions?.FrameSink ?? SpcxDiskFrameSink.Instance;\n\n        Parallel.ForEach(tasks, CreateParallelOptions(ct, executionOptions), task =>\n        {\n            if (sink.TaskAlreadyCompleted(task))\n                return;\n\n            var result = ExecuteTask(graph, task, executionOptions);\n            sink.Accept(task, result);\n        });\n    }\n\n    public SpcBatchResult RunBatch(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (tasks is null)\n            throw new ArgumentNullException(nameof(tasks));\n\n        IFrameSink sink = executionOptions?.FrameSink ?? SpcxDiskFrameSink.Instance;\n\n        var sw = System.Diagnostics.Stopwatch.StartNew();\n        var aligned = new SpcRunResult?[tasks.Count];\n\n        var executedThisCall = new bool[tasks.Count];\n        var executedCount = 0;\n        var skippedCount = 0;\n\n        Parallel.For(0, tasks.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            var task = tasks[i];\n\n            if (sink.TaskAlreadyCompleted(task))\n            {\n                SpcRunResult? cached = sink.TryLoad(task, graph);\n                if (cached is not null)\n                {\n                    aligned[i] = cached;\n                    Interlocked.Increment(ref skippedCount);\n                    return;\n                }\n            }\n\n            var result = ExecuteTask(graph, task, executionOptions);\n            sink.Accept(task, result);\n            aligned[i] = result;\n            executedThisCall[i] = true;\n            Interlocked.Increment(ref executedCount);\n        });\n\n        sw.Stop();\n\n        var executedRuns = new List<SpcRunResult>(executedCount);\n        for (int i = 0; i < aligned.Length; i++)\n        {\n            if (executedThisCall[i] && aligned[i] is not null)\n                executedRuns.Add(aligned[i]!);\n        }\n\n        return new SpcBatchResult\n        {\n            Runs = executedRuns,\n            AlignedRuns = aligned,\n            RequestedTaskCount = tasks.Count,\n            ExecutedTaskCount = executedCount,\n            SkippedTaskCount = skippedCount,\n            Elapsed = sw.Elapsed,\n        };\n    }\n\n    public IReadOnlyList<SpcRunResult> ExecuteAll(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (tasks is null)\n            throw new ArgumentNullException(nameof(tasks));\n\n        var results = new SpcRunResult[tasks.Count];\n        Parallel.For(0, tasks.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            results[i] = ExecuteTask(graph, tasks[i], executionOptions);\n        });\n\n        return results;\n    }\n\n    public IReadOnlyList<SpcRunResult> ExecuteAll(\n        IReadOnlyList<PottsRunSpec> specs,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (specs is null)\n            throw new ArgumentNullException(nameof(specs));\n\n        var results = new SpcRunResult[specs.Count];\n        Parallel.For(0, specs.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            results[i] = ExecuteTask(specs[i], executionOptions);\n        });\n\n        return results;\n    }\n\n    public SpcBatchResult ExecuteBatch(\n        IReadOnlyList<PottsRunSpec> specs,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null)\n    {\n        if (specs is null)\n            throw new ArgumentNullException(nameof(specs));\n\n        var sw = System.Diagnostics.Stopwatch.StartNew();\n        var results = new SpcRunResult[specs.Count];\n\n        Parallel.For(0, specs.Count, CreateParallelOptions(ct, executionOptions), i =>\n        {\n            results[i] = ExecuteTask(specs[i], executionOptions);\n        });\n\n        sw.Stop();\n        return new SpcBatchResult\n        {\n            Runs = results,\n\n            AlignedRuns = results,\n            RequestedTaskCount = specs.Count,\n            ExecutedTaskCount = specs.Count,\n            SkippedTaskCount = 0,\n            Elapsed = sw.Elapsed,\n        };\n    }\n\n    public SpcRunResult ExecuteTask(CsrGraph graph, SpcTaskSpec task, SpcExecutionOptions? executionOptions = null)\n    {\n        if (task is null)\n            throw new ArgumentNullException(nameof(task));\n\n        return ExecuteTask(new PottsRunSpec\n        {\n            Graph = graph,\n            Temperature = task.Temperature,\n            Q = task.Q,\n            ObservableTier = task.ObservableTier,\n            Seed = ResolveSeed(task.BaseSeed, task.ReplicaIndex, task.Temperature, executionOptions),\n            BurnInCycles = task.BurnInCycles,\n            Cycles = task.Cycles,\n            ReplicaIndex = task.ReplicaIndex,\n        }, executionOptions);\n    }\n\n    public SpcRunResult ExecuteTask(PottsRunSpec spec, SpcExecutionOptions? executionOptions = null)\n    {\n        if (spec is null)\n            throw new ArgumentNullException(nameof(spec));\n\n        var resolvedSpec = spec with\n        {\n            Seed = ResolveSeed(spec.Seed, spec.ReplicaIndex, spec.Temperature, executionOptions),\n        };\n\n        var pottsResult = PottsRunner.Run(resolvedSpec);\n        return new SpcRunResult\n        {\n            Graph = spec.Graph,\n            Frame = pottsResult.Frame,\n            EdgeObservables = pottsResult.EdgeObservables,\n        };\n    }\n\n    private static ParallelOptions CreateParallelOptions(\n        CancellationToken ct,\n        SpcExecutionOptions? executionOptions)\n    {\n        var maxDegreeOfParallelism = executionOptions?.MaxDegreeOfParallelism ?? -1;\n        return new ParallelOptions\n        {\n            CancellationToken = ct,\n            MaxDegreeOfParallelism = maxDegreeOfParallelism > 0\n                ? maxDegreeOfParallelism\n                : Environment.ProcessorCount,\n        };\n    }\n\n    private static int? ResolveSeed(\n        int? explicitSeed,\n        int replicaIndex,\n        double temperature,\n        SpcExecutionOptions? executionOptions)\n    {\n        if (explicitSeed.HasValue)\n            return explicitSeed;\n        if (executionOptions?.BaseSeed is not int baseSeed)\n            return null;\n\n        return SpcSeedHelper.Derive(\n            baseSeed,\n            temperature,\n            replica: replicaIndex,\n            round: 0,\n            quantizationFactor: executionOptions.TemperatureQuantizationFactor);\n    }\n\n    public static string GetCheckpointFileName(double temperature, int replicaIndex)\n        => $\"T_{temperature.ToString(\"F5\", CultureInfo.InvariantCulture)}_rep_{replicaIndex}.spcx\";\n\n    private static string FileNameFor(double temperature, int replicaIndex)\n        => GetCheckpointFileName(temperature, replicaIndex);\n}
62 | clustering/graphical/spc/runtime/execution/SpcRunResult.cs | {411 35 0.2457 4.4388} | 421 | using Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic sealed record SpcRunResult\n{\n    public required CsrGraph                   Graph           { get; init; }\n    public required PottsModelCoreObservables       Frame           { get; init; }\n    public          PottsModelEdgeObservables? EdgeObservables { get; init; }\n}
63 | clustering/graphical/spc/runtime/execution/SpcTaskSpec.cs | {657 73 0.2998 4.2313} | 674 | using Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic sealed record SpcTaskSpec\n{\n    public required double              Temperature    { get; init; }\n    public required int                 ReplicaIndex   { get; init; }\n    public required int                 Cycles         { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsObservableTier ObservableTier { get; init; }\n\n    public string? CheckpointPath { get; init; }\n\n    public int? BaseSeed { get; init; }\n    public int   BurnInCycles { get; init; }\n}
64 | clustering/graphical/spc/runtime/initialization/SpcGraphBuilder.cs | {3021 247 0.3548 4.3427} | 3119 | using System;\nusing Graphs;\nusing Graphs.Coupling;\nusing Graphs.Distance;\nusing Graphs.Primitives;\nusing Graphs.Proximity;\n\nnamespace Clustering.Graphical.SPC.Runtime.Initialization;\n\npublic static class SpcGraphBuilder\n{\n\n    public static GraphBuildResult BuildResult(double[][] features, SpcGraphConfig? config = null)\n    {\n        if (features is null)\n            throw new ArgumentNullException(nameof(features));\n\n        if (features.Length == 0)\n            throw new ArgumentException(\"Features must contain at least one observation.\", nameof(features));\n\n        int dimension = features[0]?.Length ?? throw new ArgumentException(\n            \"Feature vectors must not be null.\", nameof(features));\n        for (int i = 1; i < features.Length; i++)\n        {\n            if (features[i] is null)\n                throw new ArgumentException($\"Feature vector at index {i} is null.\", nameof(features));\n\n            if (features[i].Length != dimension)\n                throw new ArgumentException(\n                    $\"Feature vector at index {i} has dimension {features[i].Length}, expected {dimension}.\",\n                    nameof(features));\n        }\n\n        var cfg = config ?? new SpcGraphConfig();\n        int n = features.Length;\n        IDistanceMetric? metric = cfg.DistanceMetric;\n\n        metric?.Properties.ValidateDimension(dimension);\n\n        double Distance(int i, int j)\n        {\n            ReadOnlySpan<double> xi = features[i];\n            ReadOnlySpan<double> xj = features[j];\n            if (metric is not null)\n                return metric.Distance(xi, xj);\n\n            double sum = 0.0;\n            for (int d = 0; d < dimension; d++)\n            {\n                double diff = xi[d] - xj[d];\n                sum += diff * diff;\n            }\n            return Math.Sqrt(sum);\n        }\n\n        BandwidthStrategy bandwidthStrategy =\n            metric?.Properties.BandwidthStrategy ?? BandwidthStrategy.MadConsistencyFactor;\n\n        GraphBuildResult build = cfg.MixtureWeights is { } mixtureWeights\n            ? GraphBuilder.BuildWithMixtureResult(\n                n,\n                Distance,\n                mixtureWeights,\n                cfg.ProximityRule,\n                cfg.K,\n                cfg.Epsilon,\n                cfg.MixtureBandwidth,\n                cfg.EnsureConnected,\n                bandwidthStrategy)\n            : GraphBuilder.BuildResult(\n                n,\n                Distance,\n                cfg.ProximityRule,\n                cfg.K,\n                cfg.Epsilon,\n                cfg.KernelType,\n                cfg.Bandwidth,\n                cfg.EnsureConnected,\n                bandwidthStrategy);\n\n        if (cfg.ApplyLocalMutualProximity)\n        {\n            CsrGraph scaled = LocalMutualProximity.ApplyLocalScaling(build.Graph, weightsAreCouplings: true);\n            build = build with { Graph = scaled };\n        }\n\n        return metric is not null\n            ? build with { Metric = metric.Properties }\n            : build;\n    }\n}
65 | clustering/graphical/spc/runtime/initialization/SpcGraphConfig.cs | {824 108 0.1869 4.7012} | 845 | using Graphs;\nusing Graphs.Coupling;\nusing Graphs.Distance;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Initialization;\n\npublic sealed record SpcGraphConfig\n{\n    public ProximityRule ProximityRule { get; init; } = ProximityRule.Knn;\n    public int K { get; init; } = 10;\n    public double Epsilon { get; init; } = 0.0;\n    public KernelType KernelType { get; init; } = KernelType.Gaussian;\n    public double Bandwidth { get; init; } = 0.0;\n    public IDistanceMetric? DistanceMetric { get; init; }\n    public bool EnsureConnected { get; init; } = false;\n    public bool ApplyLocalMutualProximity { get; init; } = false;\n    public MixtureWeights? MixtureWeights { get; init; }\n    public MixtureBandwidth? MixtureBandwidth { get; init; }\n\n    public bool UseMixture => MixtureWeights is not null;\n}
66 | clustering/graphical/spc/runtime/sampler/potts/CoreObservables.cs | {1230 154 0.2626 4.3325} | 1262 | using System;\n\nnamespace Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\npublic sealed record PottsModelCoreObservables\n{\n\n    public required double              Temperature    { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsObservableTier ObservableTier { get; init; }\n\n    public int ReplicaIndex { get; init; }\n\n    public required int CycleCount { get; init; }\n\n    public required int[] Spins                { get; init; }\n    public required int[] ClusterSizeHistogram { get; init; }\n\n    public required ulong RngState0 { get; init; }\n    public required ulong RngState1 { get; init; }\n    public required ulong RngState2 { get; init; }\n    public required ulong RngState3 { get; init; }\n\n    public required double RunningSumSqClusterSizes     { get; init; }\n    public required double RunningSumSqClusterSizesExcl { get; init; }\n    public required double RunningSumEnergy             { get; init; }\n    public required double RunningSumEnergySq           { get; init; }\n\n    public required double RunningSumMag   { get; init; }\n    public required double RunningSumMagSq { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n}
67 | clustering/graphical/spc/runtime/sampler/potts/EdgeObservables.cs | {481 58 0.2266 4.5216} | 496 | using System;\n\nnamespace Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\npublic sealed record PottsModelEdgeObservables\n{\n\n    public required double Temperature  { get; init; }\n    public          int    ReplicaIndex { get; init; }\n    public required int    CycleCount   { get; init; }\n\n    public required int[]  BondFormedCount    { get; init; }\n    public required int[]  SpinAgreementCount { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n}
68 | clustering/graphical/spc/runtime/sampler/potts/IPottsConfig.cs | {444 49 0.1689 4.7008} | 468 | namespace Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\npublic interface IPottsConfig\n{\n\n    static abstract bool EdgeObservables { get; }\n}\n\npublic readonly struct StandardObservables : IPottsConfig\n{\n    public static bool EdgeObservables => false;\n}\n\npublic readonly struct WithEdgeObservables : IPottsConfig\n{\n    public static bool EdgeObservables => true;\n}\n\npublic enum PottsObservableTier\n{\n\n    Standard,\n\n    WithEdgeObservables,\n}
69 | clustering/graphical/spc/runtime/sampler/potts/IPottsModel.cs | {768 84 0.2109 4.7027} | 797 | using System;\n\nnamespace Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\ninternal interface IPottsModel\n{\n    void Cycle();\n    void Run(int cycleCount);\n    void BurnIn(int cycles);\n\n    int CycleCount { get; }\n    double Temperature { get; }\n    int Q { get; }\n    int N { get; }\n\n    ReadOnlySpan<int> Spins { get; }\n    ReadOnlySpan<int> ClusterSizeHistogram { get; }\n\n    double RunningSumSqClusterSizes { get; }\n    double RunningSumSqClusterSizesExcl { get; }\n    double RunningSumEnergy { get; }\n    double RunningSumEnergySq { get; }\n    double RunningSumMag { get; }\n    double RunningSumMagSq { get; }\n\n    PottsModelCoreObservables GetCheckpoint();\n    void Restore(PottsModelCoreObservables result);\n\n    PottsModelEdgeObservables? GetEdgeCheckpoint();\n}
