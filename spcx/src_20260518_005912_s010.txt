idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
59 | graphs/distance/euclidean/Euclidean.cs | {396 36 0.2323 4.5682} | 409 | using System;\nusing System.Numerics.Tensors;\n\nnamespace Graphs.Distance.Euclidean\n{\n    public static class Euclidean\n    {\n        /// <summary>Mismatched lengths throw inside <c>TensorPrimitives.Distance</c>; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            return TensorPrimitives.Distance<double>(a, b);\n        }\n    }\n} |
60 | graphs/distance/euclidean/Hamming.cs | {247 27 0.3036 4.3057} | 259 | using System;\nusing System.Numerics;\n\nnamespace Graphs.Distance.Euclidean\n{\n    public static class Hamming\n    {\n        public static double Distance(ulong a, ulong b)\n        {\n            return BitOperations.PopCount(a ^ b);\n        }\n    }\n} |
61 | graphs/distance/euclidean/Manhattan.cs | {445 54 0.3393 4.3496} | 461 | using System;\n\nnamespace Graphs.Distance.Euclidean\n{\n    public static class Manhattan\n    {\n        /// <summary>Mismatched lengths throw at <c>b[i]</c> via array indexing; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n                sum += Math.Abs(a[i] - b[i]);\n\n            return sum;\n        }\n    }\n} |
62 | graphs/distance/euclidean/Minkowski.cs | {1375 172 0.3622 4.3650} | 1411 | using System;\nusing System.Numerics.Tensors;\n\nnamespace Graphs.Distance.Euclidean\n{\n    public static class Minkowski\n    {\n        /// <summary>Mismatched lengths throw at <c>b[i]</c> via array indexing; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b, double p)\n        {\n            // Fast paths for common exponents.\n            // These assume well-behaved input magnitudes; the maxDiff overflow guard\n            // in the scalar fallback below does not apply here.\n            if (p == 1.0) return Manhattan.Distance(a, b);\n            if (p == 2.0) return TensorPrimitives.Distance<double>(a, b);\n\n            // Scalar fallback for arbitrary p.\n            // Factor out max |diff| to prevent |diff|^p overflow for large p.\n            // Lp = maxDiff * (sum((|diff| / maxDiff)^p))^(1/p)\n            double maxDiff = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double d = Math.Abs(a[i] - b[i]);\n                if (d > maxDiff) maxDiff = d;\n            }\n            if (maxDiff == 0) return 0.0;\n\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double scaled = Math.Abs(a[i] - b[i]) / maxDiff;\n                sum += Math.Pow(scaled, p);\n            }\n            return maxDiff * Math.Pow(sum, 1.0 / p);\n        }\n    }\n} |
63 | graphs/distance/geodesic/Cosine.cs | {672 66 0.2545 4.6580} | 692 | using System;\nusing System.Numerics.Tensors;\n\nnamespace Graphs.Distance.Geodesic\n{\n    public static class Cosine\n    {\n        /// <summary>Mismatched lengths throw inside <c>TensorPrimitives.CosineSimilarity</c>; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            double cosSim = TensorPrimitives.CosineSimilarity<double>(a, b);\n            if (double.IsNaN(cosSim)) return 1.0; // zero vector → max distance\n\n            // Clamp to [-1,1] for numerical safety before arccos\n            cosSim = Math.Max(-1.0, Math.Min(1.0, cosSim));\n            return Math.Acos(cosSim) / Math.PI;\n        }\n    }\n} |
64 | graphs/distance/geodesic/FisherRaoHalfPlane.cs | {3819 455 0.2192 4.8516} | 3919 | // ============================================================================\n// Metrics/FisherRaoHalfPlane.cs\n// ============================================================================\n// Fisher-Rao geodesic distance on the univariate Gaussian statistical manifold,\n// parameterised as (mu, log_sigma).\n//\n// The Fisher information matrix for N(mu, sigma) is diagonal:\n//\n//   I(mu, sigma) = diag(1/sigma^2, 2/sigma^2)\n//\n// This induces the Poincaré upper half-plane metric on the (mu, sigma) plane\n// (sigma > 0). The exact geodesic distance is:\n//\n//   d_FR(p, q) = sqrt(2) * arccosh(1 + [(mu1-mu2)^2 + 2*(sigma1-sigma2)^2]\n//                                       / (4 * sigma1 * sigma2))\n//\n// Reference: Costa et al., \"Fisher information distance: a geometrical reading\"\n// (2015); also derivable directly from the Poincaré half-plane metric.\n//\n// INPUT CONTRACT:\n//   Each point must be a 2-element double[] = [mu, log_sigma].\n//   log_sigma (not sigma) is used as the feature coordinate — common in\n//   GaussianManifold generator output where log_sigma prevents sigma ≤ 0.\n//   sigma is recovered via exp(log_sigma) internally.\n//\n// CONTRAST WITH FisherRaoSimplex:\n//   FisherRaoSimplex operates on discrete probability mass vectors (D-simplex).\n//   FisherRaoHalfPlane operates on continuous Gaussian parameters.\n//   Both are Fisher-Rao geodesics but on different statistical manifolds.\n//   Neither calls the other; they share no hot-path code.\n//\n// FUTURE — IStatisticalManifold:\n//   When multivariate Gaussian or Dirichlet support is added, a shared\n//   IStatisticalManifold interface (extending IRiemannianManifold) will\n//   surface FisherInformationAt(theta, fim) as a callable primitive for\n//   numerical geodesic integration. The closed-form classes (this one and\n//   FisherRaoSimplex) will implement that interface, overriding\n//   GeodesicDistance directly so the integrator is never invoked.\n// ============================================================================\nusing System;\nusing System.Runtime.CompilerServices;\n\nnamespace Graphs.Distance.Geodesic\n{\n    public static class FisherRaoHalfPlane\n    {\n        /// <summary>\n        /// Fisher-Rao geodesic distance between two univariate Gaussians,\n        /// each encoded as <c>[mu, log_sigma]</c>.\n        /// Explicit length guard: fixed 2-element contract; direct indexing via <c>p[0]</c>/<c>p[1]</c> would throw OOB for shorter inputs but silently read wrong values for longer ones.\n        /// </summary>\n        /// <param name=\"p\">2-element array <c>[mu1, log_sigma1]</c>.</param>\n        /// <param name=\"q\">2-element array <c>[mu2, log_sigma2]</c>.</param>\n        /// <returns>\n        /// The geodesic distance on the Poincaré upper half-plane.\n        /// Returns 0 for identical inputs; always non-negative.\n        /// </returns>\n        /// <exception cref=\"ArgumentException\">\n        /// Thrown if either array has length other than 2.\n        /// </exception>\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        public static double Distance(double[] p, double[] q)\n        {\n            if (p.Length != 2 || q.Length != 2)\n                throw new ArgumentException(\n                    \"FisherRaoHalfPlane requires 2-element [mu, log_sigma] vectors.\");\n\n            double dmu = p[0] - q[0];\n            double sig1 = Math.Exp(p[1]);\n            double sig2 = Math.Exp(q[1]);\n            double dsig = sig1 - sig2;\n\n            // z = 1 + [(mu1-mu2)^2 + 2*(sigma1-sigma2)^2] / (4*sigma1*sigma2)\n            // z >= 1 always (numerator >= 0, denominator > 0), so arccosh is real.\n            double z = 1.0 + (dmu * dmu + 2.0 * dsig * dsig) / (4.0 * sig1 * sig2);\n\n            return Sqrt2 * Math.Acosh(z);\n        }\n\n        private const double Sqrt2 = 1.4142135623730950488;\n    }\n} |
65 | graphs/distance/geodesic/FisherRaoSimplex.cs | {3819 426 0.2988 4.6204} | 3920 | using System;\nusing System.Buffers;\nusing System.Numerics.Tensors;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace Graphs.Distance.Geodesic\n{\n    /// <summary>\n    /// Fisher-Rao geodesic distance on the probability simplex (discrete\n    /// probability mass vectors). Uses the Bhattacharyya-angle form:\n    ///   d = 2 * arccos(sum_i sqrt(p_i * q_i))\n    /// which is the exact geodesic on the statistical manifold of categorical\n    /// distributions.\n    ///\n    /// INPUT CONTRACT: each input must be a non-negative D-vector summing to 1\n    /// (a probability mass function). Typical uses: topic model outputs, LDA\n    /// document-topic distributions, any discrete distribution embedding.\n    ///\n    /// CONTRAST WITH FisherRaoHalfPlane:\n    ///   FisherRaoHalfPlane is the exact geodesic for continuous univariate\n    ///   Gaussian parameters [mu, log_sigma] on the Poincaré upper half-plane.\n    ///   Use FisherRaoSimplex for discrete PMFs; use FisherRaoHalfPlane for\n    ///   Gaussian parameter spaces (e.g. GaussianManifold generator).\n    ///\n    /// LENGTH GUARD: explicit check for p.Length != q.Length — TensorPrimitives slices\n    ///   both spans to p.Length, so a shorter q would throw inside TensorPrimitives with\n    ///   an unhelpful bounds error rather than at the call boundary.\n    ///\n    /// FUTURE — IStatisticalManifold:\n    ///   Both classes will implement IStatisticalManifold once multivariate or\n    ///   Dirichlet families are added (those require numerical geodesic\n    ///   integration over the FIM and need a shared interface).\n    /// </summary>\n    public static class FisherRaoSimplex\n    {\n        // Single len-sized scratch — same three-tier pattern as Mahalanobis / JSD.\n        private static readonly ThreadLocal<double[]> _frScratch =\n            new(() => new double[512]);\n\n        public static double Distance(double[] p, double[] q)\n        {\n            int len = p.Length;\n            if (len == 0) return 0.0;\n            if (q.Length != len)\n                throw new ArgumentException(\n                    $\"FisherRaoSimplex requires equal-length PMF vectors (p={len}, q={q.Length}).\");\n\n            if (len <= 64)\n            {\n                Span<double> buf = stackalloc double[len];\n                return SimplexCore(p, q, buf, len);\n            }\n\n            if (len <= 512)\n            {\n                return SimplexCore(p, q, _frScratch.Value.AsSpan(0, len), len);\n            }\n\n            var pool = ArrayPool<double>.Shared;\n            double[] rented = pool.Rent(len);\n            try\n            {\n                return SimplexCore(p, q, rented.AsSpan(0, len), len);\n            }\n            finally\n            {\n                pool.Return(rented);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double SimplexCore(double[] p, double[] q, Span<double> buf, int len)\n        {\n            var pSpan = p.AsSpan(0, len);\n            var qSpan = q.AsSpan(0, len);\n\n            // bhat = sum(sqrt(p*q)). Max-clamp to 0 before sqrt guards against\n            // tiny negative values from FP noise that would otherwise yield NaN.\n            // Replaces the original `if (p[i] > 0 && q[i] > 0)` guard, which\n            // was redundant for proper probability inputs but blocked SIMD.\n            TensorPrimitives.Multiply<double>(pSpan, qSpan, buf);\n            TensorPrimitives.Max<double>(buf, 0.0, buf);\n            TensorPrimitives.Sqrt<double>(buf, buf);\n            double bhat = TensorPrimitives.Sum<double>(buf);\n\n            // Clamp before arccos for numerical safety (sum can drift just past 1).\n            if (bhat > 1.0) bhat = 1.0;\n            else if (bhat < -1.0) bhat = -1.0;\n            return 2.0 * Math.Acos(bhat);\n        }\n    }\n} |
66 | graphs/distance/geodesic/Poincare.cs | {1893 262 0.3687 4.3458} | 1949 | using System;\n\nnamespace Graphs.Distance.Geodesic\n{\n    public static class Poincare\n    {\n        /// <summary>\n        /// Poincare (hyperbolic) distance in the unit ball model.\n        /// d(u,v) = arcosh(1 + 2 * norm(u-v)^2 / ((1-norm(u)^2)(1-norm(v)^2))).\n        /// Assumes points lie inside the open unit ball.\n        /// Points on or outside the boundary are clipped to a large finite distance.\n        /// Explicit length guard: loop accumulates <c>normB2</c> over <c>a.Length</c> iterations — a shorter <c>b</c> would silently under-accumulate without throwing.\n        /// </summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            if (a == null || b == null || a.Length != b.Length)\n                throw new ArgumentException(\"Invalid feature vectors for Poincare distance.\");\n\n            int dim = a.Length;\n\n            double normA2 = 0.0, normB2 = 0.0, diff2 = 0.0;\n\n            for (int i = 0; i < dim; i++)\n            {\n                double ai = a[i], bi = b[i];\n                normA2 += ai * ai;\n                normB2 += bi * bi;\n                double d = ai - bi;\n                diff2 += d * d;\n            }\n\n            // Numerical stability: clip points near or outside the unit ball\n            const double eps = 1e-12;\n            if (normA2 >= 1.0 - eps || normB2 >= 1.0 - eps)\n                return 1e9; // large finite distance — prevents NaN / Inf in downstream kernels\n\n            double denom = (1.0 - normA2) * (1.0 - normB2);\n            if (denom < eps)\n                return 1e9;\n\n            double arg = 1.0 + 2.0 * diff2 / denom;\n\n            // arcosh(x) = log(x + sqrt(x*x - 1)) for x >= 1\n            if (arg < 1.0)\n                return 0.0; // identical points after clipping\n\n            double sqrtTerm = Math.Sqrt(arg * arg - 1.0);\n            return Math.Log(arg + sqrtTerm);\n        }\n    }\n} |
67 | graphs/distance/geodesic/Wasserstein1.cs | {1550 170 0.4232 4.1081} | 1598 | using System;\nusing System.Buffers;\n\nnamespace Graphs.Distance.Geodesic\n{\n    public static class Wasserstein\n    {\n        /// <summary>Mismatched lengths: <c>Array.Copy(b, sb, a.Length)</c> throws if <c>b</c> is shorter; no explicit guard needed.</summary>\n        public static double Distance1D(double[] a, double[] b)\n        {\n            // 1D EMD = integral of |CDF_a(x) - CDF_b(x)| dx\n            // Arrays must be sorted ascending for the CDF formulation to be correct.\n            //\n            // Sort scratch comes from ArrayPool — graph construction calls this\n            // O(N²) times. Two heap allocations per call (the previous Clone()\n            // pattern) dominated GC pressure for medium-large N.\n            int len = a.Length;\n            if (len == 0) return 0.0;\n\n            var pool = ArrayPool<double>.Shared;\n            double[] sa = pool.Rent(len);\n            double[] sb = pool.Rent(len);\n            try\n            {\n                Array.Copy(a, sa, len);\n                Array.Copy(b, sb, len);\n                Array.Sort(sa, 0, len);\n                Array.Sort(sb, 0, len);\n\n                double cumA = 0, cumB = 0, sum = 0;\n                for (int i = 0; i < len; i++)\n                {\n                    cumA += sa[i];\n                    cumB += sb[i];\n                    sum += Math.Abs(cumA - cumB);\n                }\n                return sum / len;\n            }\n            finally\n            {\n                pool.Return(sa);\n                pool.Return(sb);\n            }\n        }\n    }\n} |
68 | graphs/distance/other/Canberra.cs | {584 69 0.3784 4.2425} | 603 | using System;\n\nnamespace Graphs.Distance.Other\n{\n    public static class Canberra\n    {\n        /// <summary>Mismatched lengths throw at <c>b[i]</c> via array indexing; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double denom = Math.Abs(a[i]) + Math.Abs(b[i]);\n                if (denom < 1e-15) continue;\n                sum += Math.Abs(a[i] - b[i]) / denom;\n            }\n            return sum;\n        }\n    }\n} |
69 | graphs/distance/other/Ncd.cs | {3664 382 0.2959 4.4466} | 3756 | using System;\nusing System.IO;\nusing System.IO.Compression;\n\nnamespace Graphs.Distance.Other\n{\n    /// <summary>\n    /// Compressor selection for NCD. All options are System.IO.Compression\n    /// streaming compressors — no external dependencies.\n    /// Brotli is the default; designed for text and ships with a static\n    /// English/HTML/CSS dictionary that helps short-input compression.\n    /// Deflate / GZip / ZLib are the calibration-clean alternatives\n    /// (no static dictionary), differing only in stream framing overhead.\n    /// </summary>\n    public enum NcdCompressor { Brotli, Deflate, Gzip, ZLib }\n\n    public static class NormalizedCompressionDistance\n    {\n        /// <summary>\n        /// Pairwise NCD between two byte payloads.\n        /// Public entry point for non-graph consumers (e.g. per-segment text\n        /// scoring in downstream tools). Caller does not need to construct\n        /// an SpcBatchRequest.\n        /// Other metrics in this library keep their pairwise distance functions\n        /// private; NCD exposes its primitive because direct pairwise scoring\n        /// is its primary external use case.\n        /// </summary>\n        public static double Distance(\n            byte[] a, byte[] b, NcdCompressor compressor = NcdCompressor.Brotli)\n        {\n            if (a == null) throw new ArgumentNullException(nameof(a));\n            if (b == null) throw new ArgumentNullException(nameof(b));\n\n            int ca = CompressedSize(a, compressor);\n            int cb = CompressedSize(b, compressor);\n            int cab = CompressedSizeJoint(a, b, compressor);\n            return NcdRatio(ca, cb, cab);\n        }\n\n        private static double NcdRatio(int ca, int cb, int cab)\n        {\n            int min = Math.Min(ca, cb);\n            int max = Math.Max(ca, cb);\n            if (max == 0) return 0.0;\n            return (double)(cab - min) / max;\n        }\n\n        private static int CompressedSize(byte[] data, NcdCompressor compressor)\n        {\n            using var output = new MemoryStream();\n            using (Stream stream = CreateCompressor(output, compressor))\n            {\n                stream.Write(data, 0, data.Length);\n            }\n            return (int)output.Length;\n        }\n\n        // Joint compression via two sequential Write calls — avoids\n        // allocating a concatenated byte[]. All four BCL streaming\n        // compressors process successive Writes as one continuous input\n        // (no per-write flush by default), so the result is identical to\n        // C(a·b).\n        private static int CompressedSizeJoint(byte[] a, byte[] b, NcdCompressor compressor)\n        {\n            using var output = new MemoryStream();\n            using (Stream stream = CreateCompressor(output, compressor))\n            {\n                stream.Write(a, 0, a.Length);\n                stream.Write(b, 0, b.Length);\n            }\n            return (int)output.Length;\n        }\n\n        private static Stream CreateCompressor(Stream output, NcdCompressor compressor)\n        {\n            return compressor switch\n            {\n                NcdCompressor.Brotli => new BrotliStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                NcdCompressor.Deflate => new DeflateStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                NcdCompressor.Gzip => new GZipStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                NcdCompressor.ZLib => new ZLibStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                _ => throw new NotSupportedException($\"NcdCompressor {compressor} not implemented.\")\n            };\n        }\n    }\n} |
70 | graphs/distance/probability/JensenShannon.cs | {3717 358 0.3323 4.5085} | 3813 | using System;\nusing System.Buffers;\nusing System.Numerics.Tensors;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace Graphs.Distance.Probability\n{\n    public static class JensenShannon\n    {\n        // Three-tier scratch buffers — same pattern as Mahalanobis. JSD needs\n        // two len-sized buffers: one for m = (p+q)/2 (later repurposed for the\n        // q-side term) and one for the p-side term.\n        private static readonly ThreadLocal<(double[] a, double[] b)> _jsdScratch =\n            new(() => (new double[512], new double[512]));\n\n        /// <summary>Spans are sized from <c>p.Length</c>; a shorter <c>q</c> throws at <c>q.AsSpan(0, len)</c> with an unhelpful bounds error rather than at the call boundary.</summary>\n        public static double Distance(double[] p, double[] q)\n        {\n            int len = p.Length;\n            if (len == 0) return 0.0;\n\n            if (len <= 64)\n            {\n                Span<double> a = stackalloc double[len];\n                Span<double> b = stackalloc double[len];\n                return JensenShannonCore(p, q, a, b, len);\n            }\n\n            if (len <= 512)\n            {\n                var (aArr, bArr) = _jsdScratch.Value;\n                return JensenShannonCore(p, q,\n                    aArr.AsSpan(0, len),\n                    bArr.AsSpan(0, len), len);\n            }\n\n            var pool = ArrayPool<double>.Shared;\n            double[] rentedA = pool.Rent(len);\n            double[] rentedB = pool.Rent(len);\n            try\n            {\n                return JensenShannonCore(p, q,\n                    rentedA.AsSpan(0, len),\n                    rentedB.AsSpan(0, len), len);\n            }\n            finally\n            {\n                pool.Return(rentedA);\n                pool.Return(rentedB);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double JensenShannonCore(\n            double[] p, double[] q,\n            Span<double> mBuf, Span<double> termP, int len)\n        {\n            var pSpan = p.AsSpan(0, len);\n            var qSpan = q.AsSpan(0, len);\n\n            // m = (p+q)/2, clamped above 1e-15 to make division safe.\n            TensorPrimitives.Add<double>(pSpan, qSpan, mBuf);\n            TensorPrimitives.Multiply<double>(mBuf, 0.5, mBuf);\n            TensorPrimitives.Max<double>(mBuf, 1e-15, mBuf);\n\n            // termP = p * log(max(p/m, 1e-15))\n            // Clamping the ratio is mathematically safe: where p ≈ 0 the\n            // outer *p multiply collapses the term to 0, regardless of the\n            // (clamped) log value. This replaces the original `if p[i] > 1e-15`\n            // guard, which would otherwise kill SIMD vectorization.\n            TensorPrimitives.Divide<double>(pSpan, mBuf, termP);\n            TensorPrimitives.Max<double>(termP, 1e-15, termP);\n            TensorPrimitives.Log<double>(termP, termP);\n            TensorPrimitives.Multiply<double>(pSpan, termP, termP);\n            double sumP = TensorPrimitives.Sum<double>(termP);\n\n            // mBuf is repurposed for the q-side term. Order matters — m must\n            // remain valid through both Divide calls; q-side runs second so\n            // we can overwrite m in place.\n            TensorPrimitives.Divide<double>(qSpan, mBuf, mBuf);\n            TensorPrimitives.Max<double>(mBuf, 1e-15, mBuf);\n            TensorPrimitives.Log<double>(mBuf, mBuf);\n            TensorPrimitives.Multiply<double>(qSpan, mBuf, mBuf);\n            double sumQ = TensorPrimitives.Sum<double>(mBuf);\n\n            // JSD = 0.5 * (KL(p||m) + KL(q||m)); return sqrt for proper metric.\n            return Math.Sqrt(0.5 * (sumP + sumQ));\n        }\n    }\n} |
71 | graphs/distance/probability/Mahalanobis.cs | {5109 563 0.3269 4.5317} | 5272 | using System;\nusing System.Buffers;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace Graphs.Distance.Probability\n{\n    public static class Mahalanobis\n    {\n        // Three-tier scratch buffer allocation — avoids O(N²) heap allocations\n        // during graph construction while remaining thread-safe under Parallel.For.\n        //\n        // Tier 1 (dim ≤ 64):  stackalloc — zero heap cost, JIT-inlinable.\n        // Tier 2 (dim ≤ 512): ThreadLocal fixed arrays — one allocation per thread, ever.\n        // Tier 3 (dim > 512): ArrayPool rent/return per call.\n        //\n        // ThreadLocal note: _mahalScratch allocates once per thread that touches it.\n        // Primary caller is parallel graph construction (thread count ≤ ProcessorCount).\n        // Calling from many short-lived threads (e.g., test isolation) is correct but\n        // wastes memory. Write-before-read invariant: MahalanobisCore always writes\n        // diff[] before reading it and temp[i] before the final dot — scratch is safe.\n        private static readonly ThreadLocal<(double[] diff, double[] temp)> _mahalScratch =\n            new(() => (new double[512], new double[512]));\n\n        /// <summary>Explicit length guard: inner loop writes <c>diff[i] = a[i] - b[i]</c> over <c>a.Length</c> — a shorter <c>b</c> would throw inside the loop with no context on the mismatch.</summary>\n        public static double Distance(double[] a, double[] b, double[,] invCov)\n        {\n            if (a == null || b == null || invCov == null)\n                throw new ArgumentNullException(a == null ? nameof(a) : b == null ? nameof(b) : nameof(invCov));\n            if (a.Length != b.Length)\n                throw new ArgumentException(\"Feature vectors must have the same dimensionality.\");\n            return Distance(a, b, invCov, a.Length);\n        }\n\n        public static double Distance(double[] a, double[] b, double[,] invCov, int dim)\n            => Math.Sqrt(DispatchQuadraticForm(a, b, invCov, dim));\n\n        /// <summary>\n        /// Returns the squared Mahalanobis distance: (a−b)ᵀ Σ⁻¹ (a−b).\n        /// Avoids the sqrt of <see cref=\"Distance(double[],double[],double[,])\"/>;\n        /// prefer this when the squared form is all that is needed (e.g. log-pdf evaluation).\n        /// </summary>\n        public static double DistanceSquared(double[] a, double[] b, double[,] invCov)\n        {\n            if (a == null || b == null || invCov == null)\n                throw new ArgumentNullException(a == null ? nameof(a) : b == null ? nameof(b) : nameof(invCov));\n            if (a.Length != b.Length)\n                throw new ArgumentException(\"Feature vectors must have the same dimensionality.\");\n            return DispatchQuadraticForm(a, b, invCov, a.Length);\n        }\n\n        /// <summary>\n        /// Dimension-dispatched entry point. Selects the scratch-allocation tier and\n        /// delegates to <see cref=\"QuadraticFormCore\"/>.\n        /// </summary>\n        private static double DispatchQuadraticForm(double[] a, double[] b, double[,] invCov, int dim)\n        {\n            if (dim <= 64)\n            {\n                Span<double> diff = stackalloc double[dim];\n                Span<double> temp = stackalloc double[dim];\n                return QuadraticFormCore(a, b, invCov, diff, temp, dim);\n            }\n\n            if (dim <= 512)\n            {\n                var (diffArr, tempArr) = _mahalScratch.Value;\n                return QuadraticFormCore(a, b, invCov,\n                    diffArr.AsSpan(0, dim),\n                    tempArr.AsSpan(0, dim), dim);\n            }\n\n            // dim > 512 — pool rent, guaranteed return\n            double[] rentedDiff = ArrayPool<double>.Shared.Rent(dim);\n            double[] rentedTemp = ArrayPool<double>.Shared.Rent(dim);\n            try\n            {\n                return QuadraticFormCore(a, b, invCov,\n                    rentedDiff.AsSpan(0, dim),\n                    rentedTemp.AsSpan(0, dim), dim);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedDiff);\n                ArrayPool<double>.Shared.Return(rentedTemp);\n            }\n        }\n\n        /// <summary>\n        /// Inner kernel. Returns the raw quadratic form max(0, (a−b)ᵀ Σ⁻¹ (a−b)).\n        /// Callers apply sqrt for Euclidean-equivalent distance, or use directly for D².\n        /// </summary>\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double QuadraticFormCore(\n            double[] a, double[] b, double[,] invCov,\n            Span<double> diff, Span<double> temp, int dim)\n        {\n            for (int i = 0; i < dim; i++)\n                diff[i] = a[i] - b[i];\n\n            for (int i = 0; i < dim; i++)\n            {\n                double s = 0;\n                for (int j = 0; j < dim; j++)\n                    s += invCov[i, j] * diff[j];\n                temp[i] = s;\n            }\n\n            double quad = 0;\n            for (int i = 0; i < dim; i++)\n                quad += diff[i] * temp[i];\n\n            return Math.Max(0, quad);\n        }\n    }\n} |
72 | graphs/distance/sets/Jaccard.cs | {730 95 0.3767 4.1813} | 753 | using System;\n\nnamespace Graphs.Distance.Sets\n{\n    public static class Jaccard\n    {\n        /// <summary>Mismatched lengths throw at <c>b[i]</c> via array indexing; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b, double threshold = 0.5)\n        {\n            int intersection = 0, union = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                bool ai = a[i] > threshold;\n                bool bi = b[i] > threshold;\n                if (ai || bi) union++;\n                if (ai && bi) intersection++;\n            }\n            if (union == 0) return 0.0; // both empty → identical\n            return 1.0 - (double)intersection / union;\n        }\n    }\n} |
