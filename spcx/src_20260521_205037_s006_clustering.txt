idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
50 | clustering/spc/runtime/sampler/PottsRunnerIO.cs | {11021 804 0.3394 4.4261} | 11328 | using System;\nusing System.IO;\nusing System.Text;\nusing Clustering.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.SPC.Runtime.Sampler\n{\n\n    public interface IObservableSerializer<TObservable>\n    {\n        string DefaultFileExtension { get; }\n        void WriteTo(TObservable observable, Stream stream);\n        TObservable ReadFrom(Stream stream);\n    }\n\n    public abstract class ObservableSerializerBase<TObservable> : IObservableSerializer<TObservable>\n    {\n        public abstract string DefaultFileExtension { get; }\n\n        public abstract void WriteTo(TObservable observable, Stream stream);\n\n        public abstract TObservable ReadFrom(Stream stream);\n\n        public void WriteToFile(TObservable observable, string path)\n        {\n            if (observable is null)\n                throw new ArgumentNullException(nameof(observable));\n            if (string.IsNullOrWhiteSpace(path))\n                throw new ArgumentException(\"Path must be provided.\", nameof(path));\n\n            string tmpPath = path + \".tmp\";\n            using (var fs = File.Create(tmpPath))\n            {\n                WriteTo(observable, fs);\n                fs.Flush();\n            }\n\n            File.Move(tmpPath, path, overwrite: false);\n        }\n\n        public TObservable ReadFromFile(string path)\n        {\n            if (string.IsNullOrWhiteSpace(path))\n                throw new ArgumentException(\"Path must be provided.\", nameof(path));\n            if (!File.Exists(path))\n                throw new FileNotFoundException(\"Observable file not found.\", path);\n\n            using var fs = File.OpenRead(path);\n            return ReadFrom(fs);\n        }\n    }\n\n    public static class ObservableSerializer\n    {\n        public static void WriteToFile<TObservable>(\n            IObservableSerializer<TObservable> serializer,\n            TObservable observable,\n            string path)\n        {\n            if (serializer is null)\n                throw new ArgumentNullException(nameof(serializer));\n            if (observable is null)\n                throw new ArgumentNullException(nameof(observable));\n            if (string.IsNullOrWhiteSpace(path))\n                throw new ArgumentException(\"Path must be provided.\", nameof(path));\n\n            string tmpPath = path + \".tmp\";\n            using (var fs = File.Create(tmpPath))\n            {\n                serializer.WriteTo(observable, fs);\n                fs.Flush();\n            }\n\n            File.Move(tmpPath, path, overwrite: false);\n        }\n\n        public static TObservable ReadFromFile<TObservable>(\n            IObservableSerializer<TObservable> serializer,\n            string path)\n        {\n            if (serializer is null)\n                throw new ArgumentNullException(nameof(serializer));\n            if (string.IsNullOrWhiteSpace(path))\n                throw new ArgumentException(\"Path must be provided.\", nameof(path));\n            if (!File.Exists(path))\n                throw new FileNotFoundException(\"Observable file not found.\", path);\n\n            using var fs = File.OpenRead(path);\n            return serializer.ReadFrom(fs);\n        }\n    }\n\n    public sealed class PottsCoreObservablesSerializer : ObservableSerializerBase<PottsModelCoreObservables>\n    {\n        public static PottsCoreObservablesSerializer Instance { get; } = new();\n        public override string DefaultFileExtension => \".spcx\";\n\n        private const uint Magic = 0x58435053; // \"SPCX\"\n        private const int Version = 3;\n\n        public override void WriteTo(PottsModelCoreObservables observable, Stream stream)\n        {\n            if (observable is null)\n                throw new ArgumentNullException(nameof(observable));\n\n            using var w = new BinaryWriter(stream, Encoding.UTF8, leaveOpen: true);\n            w.Write(Magic);\n            w.Write(Version);\n            w.Write(observable.Temperature);\n            w.Write(observable.Q);\n            w.Write((int)observable.ObservableTier);\n            w.Write(observable.ReplicaIndex);\n            w.Write(observable.CycleCount);\n\n            w.Write(observable.Spins.Length);\n            foreach (int s in observable.Spins) w.Write(s);\n\n            w.Write(observable.ClusterSizeHistogram.Length);\n            foreach (int h in observable.ClusterSizeHistogram) w.Write(h);\n\n            w.Write(observable.RngState0);\n            w.Write(observable.RngState1);\n            w.Write(observable.RngState2);\n            w.Write(observable.RngState3);\n\n            w.Write(observable.RunningSumSqClusterSizes);\n            w.Write(observable.RunningSumSqClusterSizesExcl);\n            w.Write(observable.RunningSumEnergy);\n            w.Write(observable.RunningSumEnergySq);\n            w.Write(observable.RunningSumMag);\n            w.Write(observable.RunningSumMagSq);\n\n            w.Write(observable.TimestampUtc.Ticks);\n        }\n\n        public override PottsModelCoreObservables ReadFrom(Stream stream)\n        {\n            using var r = new BinaryReader(stream, Encoding.UTF8, leaveOpen: true);\n\n            uint magic = r.ReadUInt32();\n            if (magic != Magic)\n                throw new InvalidDataException(\n                    $\"Not an SPCX checkpoint (magic 0x{magic:X8}, expected 0x{Magic:X8}).\");\n            int version = r.ReadInt32();\n            if (version != Version)\n                throw new InvalidDataException(\n                    $\"Unsupported SPCX version {version} (this reader handles version {Version}).\");\n\n            double temperature  = r.ReadDouble();\n            int    q            = r.ReadInt32();\n            var    tier         = (PottsObservableTier)r.ReadInt32();\n            int    replicaIndex = r.ReadInt32();\n            int    cycleCount   = r.ReadInt32();\n\n            int spinCount = r.ReadInt32();\n            var spins = new int[spinCount];\n            for (int i = 0; i < spinCount; i++) spins[i] = r.ReadInt32();\n\n            int histBins = r.ReadInt32();\n            var hist = new int[histBins];\n            for (int i = 0; i < histBins; i++) hist[i] = r.ReadInt32();\n\n            ulong s0 = r.ReadUInt64();\n            ulong s1 = r.ReadUInt64();\n            ulong s2 = r.ReadUInt64();\n            ulong s3 = r.ReadUInt64();\n\n            double sumSqCluster     = r.ReadDouble();\n            double sumSqClusterExcl = r.ReadDouble();\n            double sumEnergy        = r.ReadDouble();\n            double sumEnergySq      = r.ReadDouble();\n            double sumMag           = r.ReadDouble();\n            double sumMagSq         = r.ReadDouble();\n\n            long ticks = r.ReadInt64();\n\n            return new PottsModelCoreObservables\n            {\n                Temperature              = temperature,\n                Q                        = q,\n                ObservableTier           = tier,\n                ReplicaIndex             = replicaIndex,\n                CycleCount               = cycleCount,\n                Spins                    = spins,\n                ClusterSizeHistogram     = hist,\n                RngState0                = s0,\n                RngState1                = s1,\n                RngState2                = s2,\n                RngState3                = s3,\n                RunningSumSqClusterSizes     = sumSqCluster,\n                RunningSumSqClusterSizesExcl = sumSqClusterExcl,\n                RunningSumEnergy             = sumEnergy,\n                RunningSumEnergySq           = sumEnergySq,\n                RunningSumMag                = sumMag,\n                RunningSumMagSq              = sumMagSq,\n                TimestampUtc             = new DateTime(ticks, DateTimeKind.Utc),\n            };\n        }\n\n        public new static void WriteToFile(PottsModelCoreObservables observable, string path)\n            => ((ObservableSerializerBase<PottsModelCoreObservables>)Instance).WriteToFile(observable, path);\n\n        public new static PottsModelCoreObservables ReadFromFile(string path)\n            => ((ObservableSerializerBase<PottsModelCoreObservables>)Instance).ReadFromFile(path);\n    }\n\n    public sealed class PottsEdgeObservablesSerializer : ObservableSerializerBase<PottsModelEdgeObservables>\n    {\n        public static PottsEdgeObservablesSerializer Instance { get; } = new();\n        public override string DefaultFileExtension => \".spce\";\n\n        private const uint Magic = 0x45435053; // \"SPCE\"\n        private const int Version = 1;\n\n        public override void WriteTo(PottsModelEdgeObservables observable, Stream stream)\n        {\n            if (observable is null)\n                throw new ArgumentNullException(nameof(observable));\n\n            using var w = new BinaryWriter(stream, Encoding.UTF8, leaveOpen: true);\n            w.Write(Magic);\n            w.Write(Version);\n            w.Write(observable.Temperature);\n            w.Write(observable.ReplicaIndex);\n            w.Write(observable.CycleCount);\n\n            w.Write(observable.BondFormedCount.Length);\n            foreach (int x in observable.BondFormedCount) w.Write(x);\n            foreach (int x in observable.SpinAgreementCount) w.Write(x);\n\n            w.Write(observable.TimestampUtc.Ticks);\n        }\n\n        public override PottsModelEdgeObservables ReadFrom(Stream stream)\n        {\n            using var r = new BinaryReader(stream, Encoding.UTF8, leaveOpen: true);\n\n            uint magic = r.ReadUInt32();\n            if (magic != Magic)\n                throw new InvalidDataException(\n                    $\"Not an SPCE checkpoint (magic 0x{magic:X8}, expected 0x{Magic:X8}).\");\n            int version = r.ReadInt32();\n            if (version != Version)\n                throw new InvalidDataException(\n                    $\"Unsupported SPCE version {version} (this reader handles version {Version}).\");\n\n            double temperature  = r.ReadDouble();\n            int    replicaIndex = r.ReadInt32();\n            int    cycleCount   = r.ReadInt32();\n\n            int edgeCount = r.ReadInt32();\n            var bond = new int[edgeCount];\n            for (int i = 0; i < edgeCount; i++) bond[i] = r.ReadInt32();\n            var spin = new int[edgeCount];\n            for (int i = 0; i < edgeCount; i++) spin[i] = r.ReadInt32();\n\n            long ticks = r.ReadInt64();\n\n            return new PottsModelEdgeObservables\n            {\n                Temperature        = temperature,\n                ReplicaIndex       = replicaIndex,\n                CycleCount         = cycleCount,\n                BondFormedCount    = bond,\n                SpinAgreementCount = spin,\n                TimestampUtc       = new DateTime(ticks, DateTimeKind.Utc),\n            };\n        }\n\n        public new static void WriteToFile(PottsModelEdgeObservables observable, string path)\n            => ((ObservableSerializerBase<PottsModelEdgeObservables>)Instance).WriteToFile(observable, path);\n\n        public new static PottsModelEdgeObservables ReadFromFile(string path)\n            => ((ObservableSerializerBase<PottsModelEdgeObservables>)Instance).ReadFromFile(path);\n    }\n}
51 | clustering/spc/runtime/sampler/PottsRunResult.cs | {394 34 0.1472 4.7823} | 407 | using Clustering.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.SPC.Runtime.Sampler;\n\npublic sealed record PottsRunResult\n{\n\n    public required PottsModelCoreObservables Frame { get; init; }\n\n    public PottsModelEdgeObservables? EdgeObservables { get; init; }\n\n    public double FkSusceptibility\n        => Frame.RunningSumSqClusterSizes / (Frame.CycleCount * (double)Frame.Spins.Length);\n}
52 | clustering/spc/runtime/sampler/PottsRunSpec.cs | {541 73 0.2107 4.4870} | 564 | using Clustering.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Sampler;\n\npublic sealed record PottsRunSpec\n{\n\n    public required CsrGraph Graph { get; init; }\n\n    public required double Temperature { get; init; }\n\n    public required int Q { get; init; }\n\n    public required PottsObservableTier ObservableTier { get; init; }\n\n    public int? Seed { get; init; }\n\n    public required int Cycles { get; init; }\n\n    public int BurnInCycles { get; init; }\n\n    public int ReplicaIndex { get; init; }\n}
53 | clustering/spc/runtime/scheduling/AdaptiveScheduler.cs | {11992 1061 0.3564 4.3637} | 12317 | using System;\nusing System.Collections.Generic;\nusing System.Diagnostics;\nusing System.Linq;\nusing System.Threading.Tasks;\nusing Clustering.SPC.Profiling;\nusing Clustering.SPC.Profiling.Signals;\nusing Clustering.SPC.Runtime.Execution;\nusing Clustering.SPC.Runtime.Sampler;\nusing Clustering.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Scheduling;\n\npublic sealed class AdaptiveSchedulerConfig\n{\n\n    public int    CoarseSteps        { get; init; } = 10;\n    public int    CoarseBurn         { get; init; } = 50;\n    public int    CoarseCycles       { get; init; } = 100;\n\n    public int    DenseSteps         { get; init; } = 12;\n    public int    DenseBurn          { get; init; } = 200;\n    public int    DenseCycles        { get; init; } = 400;\n\n    public int    DenseReplicas      { get; init; } = 3;\n\n    public int    DenseRounds        { get; init; } = 3;\n\n    public int    FinalBurn          { get; init; } = 1000;\n    public int    FinalCycles        { get; init; } = 5000;\n\n    public double? TempMin           { get; init; } = null;\n\n    public double? TempMax           { get; init; } = null;\n    public double ColdEndOvershoot   { get; init; } = 0.05;\n    public double HotEndOvershoot    { get; init; } = 5.0;\n\n    public int    Q                  { get; init; } = 20;\n\n    public double EarlyStopStability { get; init; } = 0.95;\n\n    public int?   BaseSeed           { get; init; } = null;\n\n    public int    MaxDegreeOfParallelism { get; init; } = -1;\n}\n\npublic sealed class AdaptiveScheduleSummary\n{\n    public required int      SubgraphNodes     { get; init; }\n    public required int      SubgraphEdges     { get; init; }\n    public          double   ChosenTemperature { get; init; }\n    public          double   StabilityScore    { get; init; }\n    public          int      TotalCyclesUsed   { get; init; }\n    public          bool     EarlyStopped      { get; init; }\n    public          TimeSpan Elapsed           { get; init; }\n\n    public SweepProfile Profile { get; init; } = SweepProfile.Empty;\n}\n\npublic sealed class AdaptiveScheduleResult\n{\n    public required AdaptiveScheduleSummary Summary { get; init; }\n    public required IReadOnlyList<SpcRunResult> SweepRuns { get; init; }\n    public required SpcRunResult FinalRun { get; init; }\n    public required ProfileCriteria ProfileCriteria { get; init; }\n    public          PottsModelEdgeObservables? EdgeObservables { get; init; }\n}\n\npublic static class AdaptiveScheduler\n{\npublic static AdaptiveScheduleResult Run(\n    CsrGraph graph,\n    AdaptiveSchedulerConfig? config = null,\n    ISignalAnalyzer? analyzer = null)\n{\n        var cfg = config ?? new AdaptiveSchedulerConfig();\n        var signalAnalyzer = analyzer ?? new ChiPeakSignalAnalyzer();\n        var sw = Stopwatch.StartNew();\n\n        double tempMin;\n        double tempMax;\n        if (cfg.TempMin.HasValue && cfg.TempMax.HasValue)\n        {\n            tempMin = cfg.TempMin.Value;\n            tempMax = cfg.TempMax.Value;\n        }\n        else\n        {\n            (double bracketMin, double bracketMax) = SpcScheduleHelpers.EstimateBracket(\n                graph,\n                cfg.Q,\n                cfg.ColdEndOvershoot,\n                cfg.HotEndOvershoot);\n            tempMin = cfg.TempMin ?? bracketMin;\n            tempMax = cfg.TempMax ?? bracketMax;\n        }\n\n        if (graph.NodeCount < 2)\n        {\n            var trivialProfile = SweepProfile.Empty;\n            return new AdaptiveScheduleResult\n            {\n                Summary = new AdaptiveScheduleSummary\n                {\n                    SubgraphNodes = graph.NodeCount,\n                    SubgraphEdges = 0,\n                    Elapsed       = sw.Elapsed,\n                    Profile       = trivialProfile,\n                },\n                SweepRuns = Array.Empty<SpcRunResult>(),\n                FinalRun = new SpcRunResult\n                {\n                    Graph = graph,\n                    Frame = new PottsModelCoreObservables\n                    {\n                        Temperature = 0.0,\n                        Q = cfg.Q,\n                        ObservableTier = PottsObservableTier.Standard,\n                        CycleCount = 0,\n                        Spins = Array.Empty<int>(),\n                        ClusterSizeHistogram = Array.Empty<int>(),\n                        RngState0 = 0UL,\n                        RngState1 = 0UL,\n                        RngState2 = 0UL,\n                        RngState3 = 0UL,\n                        RunningSumSqClusterSizes = 0.0,\n                        RunningSumSqClusterSizesExcl = 0.0,\n                        RunningSumEnergy = 0.0,\n                        RunningSumEnergySq = 0.0,\n                        RunningSumMag = 0.0,\n                        RunningSumMagSq = 0.0,\n                    },\n                    EdgeObservables = null,\n                },\n                ProfileCriteria = new ProfileCriteria(\n                    AnchorTemperature: 0.0,\n                    AnchorBand: (0.0, 0.0),\n                    RefinedTemperature: 0.0,\n                    CorroborationScore: 0.0,\n                    Enrichments: new Dictionary<string, double>()),\n                EdgeObservables = null,\n            };\n        }\n\n        int totalCycles = 0;\n\n        var coarseRuns = RunCoarseProbe(graph, cfg, tempMin, tempMax, ref totalCycles);\n        var coarseProfile = SweepProfile.From(coarseRuns);\n        double coarseStability = SpcProfileAnalysis.ComputeStability(coarseProfile);\n\n        if (coarseStability >= cfg.EarlyStopStability)\n        {\n            double earlyT = SpcProfileAnalysis.PickPeakTemperature(coarseProfile);\n            var earlyFinalRun = RunFinalEquilibrium(graph, earlyT, cfg, ref totalCycles);\n            ProfileCriteria earlyCriteria = signalAnalyzer.Analyze(coarseProfile);\n\n            return new AdaptiveScheduleResult\n            {\n                Summary = new AdaptiveScheduleSummary\n                {\n                    SubgraphNodes     = graph.NodeCount,\n                    SubgraphEdges     = graph.Targets.Length / 2,\n                    ChosenTemperature = earlyT,\n                    StabilityScore    = coarseStability,\n                    TotalCyclesUsed   = totalCycles,\n                    EarlyStopped      = true,\n                    Elapsed           = sw.Elapsed,\n                    Profile           = coarseProfile,\n                },\n                SweepRuns = coarseRuns,\n                FinalRun = earlyFinalRun,\n                ProfileCriteria = earlyCriteria,\n                EdgeObservables = earlyFinalRun.EdgeObservables,\n            };\n        }\n\n        var refinedRuns = RunDenseRefinement(graph, coarseRuns, cfg, ref totalCycles);\n        var allRuns = coarseRuns.Concat(refinedRuns).ToList();\n        var fullProfile = SweepProfile.From(allRuns);\n        double chosenT = SpcProfileAnalysis.PickPeakTemperature(fullProfile);\n        double stability = SpcProfileAnalysis.ComputeStability(fullProfile);\n\n        var finalRun = RunFinalEquilibrium(graph, chosenT, cfg, ref totalCycles);\n        ProfileCriteria criteria = signalAnalyzer.Analyze(fullProfile);\n\n        return new AdaptiveScheduleResult\n        {\n            Summary = new AdaptiveScheduleSummary\n            {\n                SubgraphNodes     = graph.NodeCount,\n                SubgraphEdges     = graph.Targets.Length / 2,\n                ChosenTemperature = chosenT,\n                StabilityScore    = stability,\n                TotalCyclesUsed   = totalCycles,\n                EarlyStopped      = false,\n                Elapsed           = sw.Elapsed,\n                Profile           = fullProfile,\n            },\n            SweepRuns = allRuns,\n            FinalRun = finalRun,\n            ProfileCriteria = criteria,\n            EdgeObservables = finalRun.EdgeObservables,\n        };\n    }\n\n    private static List<SpcRunResult> RunCoarseProbe(\n        CsrGraph graph,\n        AdaptiveSchedulerConfig cfg,\n        double tempMin,\n        double tempMax,\n        ref int totalCycles)\n    {\n        var grid = SpcScheduleHelpers.LogSpaceGrid(tempMin, tempMax, cfg.CoarseSteps);\n        var results = new SpcRunResult[grid.Length];\n\n        Parallel.For(0, grid.Length, ParallelOptions(cfg), idx =>\n        {\n            double T = grid[idx];\n            int? seed = SeedForTask(cfg.BaseSeed, T, round: 0, replica: 0);\n            results[idx] = RunProbePoint(graph, T, cfg.Q, cfg.CoarseBurn, cfg.CoarseCycles, seed);\n        });\n\n        totalCycles += grid.Length * (cfg.CoarseBurn + cfg.CoarseCycles);\n        return new List<SpcRunResult>(results);\n    }\n\n    private static List<SpcRunResult> RunDenseRefinement(\n        CsrGraph graph,\n        IReadOnlyList<SpcRunResult> coarseRuns,\n        AdaptiveSchedulerConfig cfg,\n        ref int totalCycles)\n    {\n        var coarseProfile = SweepProfile.From(coarseRuns);\n        var (lo, hi) = SpcProfileAnalysis.ComputeHalfMaximumBand(coarseProfile);\n        var grid = SpcScheduleHelpers.LogSpaceGrid(lo, hi, cfg.DenseSteps);\n\n        var tasks = new List<(int TempIdx, int Round, int Replica)>(\n            grid.Length * cfg.DenseReplicas * cfg.DenseRounds);\n        for (int round = 0; round < cfg.DenseRounds; round++)\n            for (int rep = 0; rep < cfg.DenseReplicas; rep++)\n                for (int t = 0; t < grid.Length; t++)\n                    tasks.Add((t, round, rep));\n\n        var results = new SpcRunResult[tasks.Count];\n        Parallel.For(0, tasks.Count, ParallelOptions(cfg), idx =>\n        {\n            var (tIdx, round, rep) = tasks[idx];\n            double T = grid[tIdx];\n            int? seed = SeedForTask(cfg.BaseSeed, T, round, rep);\n            results[idx] = RunProbePoint(graph, T, cfg.Q, cfg.DenseBurn, cfg.DenseCycles, seed);\n        });\n\n        totalCycles += tasks.Count * (cfg.DenseBurn + cfg.DenseCycles);\n        return new List<SpcRunResult>(results);\n    }\n\n    private static SpcRunResult RunFinalEquilibrium(\n        CsrGraph graph, double T, AdaptiveSchedulerConfig cfg, ref int totalCycles)\n    {\n        int? seed = SeedForTask(cfg.BaseSeed, T, round: -1, replica: 0);\n        var runResult = PottsRunner.Run(new PottsRunSpec\n        {\n            Graph = graph,\n            Temperature = T,\n            Q = cfg.Q,\n            ObservableTier = PottsObservableTier.WithEdgeObservables,\n            Seed = seed,\n            BurnInCycles = cfg.FinalBurn,\n            Cycles = cfg.FinalCycles,\n            ReplicaIndex = 0,\n        });\n\n        totalCycles += cfg.FinalBurn + cfg.FinalCycles;\n\n        return new SpcRunResult\n        {\n            Graph = graph,\n            Frame = runResult.Frame,\n            EdgeObservables = runResult.EdgeObservables\n                ?? throw new InvalidOperationException(\n                    \"WithEdgeObservables specialization did not produce edge observables — unexpected.\"),\n        };\n    }\n\n    private static SpcRunResult RunProbePoint(\n        CsrGraph graph, double T, int q, int burnCycles, int measureCycles, int? seed)\n    {\n        var runResult = PottsRunner.Run(new PottsRunSpec\n        {\n            Graph = graph,\n            Temperature = T,\n            Q = q,\n            ObservableTier = PottsObservableTier.Standard,\n            Seed = seed,\n            BurnInCycles = burnCycles,\n            Cycles = measureCycles,\n            ReplicaIndex = 0,\n        });\n\n        return new SpcRunResult\n        {\n            Graph = graph,\n            Frame = runResult.Frame,\n            EdgeObservables = null,\n        };\n    }\n\n    private static ParallelOptions ParallelOptions(AdaptiveSchedulerConfig cfg) => new()\n    {\n        MaxDegreeOfParallelism = cfg.MaxDegreeOfParallelism,\n    };\n\n    private static int? SeedForTask(int? baseSeed, double T, int round, int replica)\n    {\n        if (baseSeed is null) return null;\n\n        int tQuantized = (int)(T * 1000.0);\n        return HashCode.Combine(baseSeed.Value, tQuantized, round, replica);\n    }\n\n}
54 | clustering/spc/runtime/scheduling/SpcScheduleHelpers.cs | {1756 206 0.2830 4.6759} | 1811 | using System;\nusing Graphs.Diagnostics;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Runtime.Scheduling;\n\npublic static class SpcScheduleHelpers\n{\n    public static double[] LogSpaceGrid(double lo, double hi, int steps)\n    {\n        if (steps <= 1) return new[] { 0.5 * (lo + hi) };\n        if (lo <= 0.0 || hi <= 0.0)\n            throw new ArgumentException(\"LogSpaceGrid requires strictly positive endpoints.\");\n        if (hi <= lo) (lo, hi) = (hi, lo);\n\n        double logLo = Math.Log(lo);\n        double logHi = Math.Log(hi);\n        var grid = new double[steps];\n        for (int i = 0; i < steps; i++)\n            grid[i] = Math.Exp(logLo + i * (logHi - logLo) / (steps - 1));\n        return grid;\n    }\n\n    public static double Estimate(CsrGraph graph, int q)\n    {\n        if (graph.NodeCount == 0 || graph.Targets.Length == 0 || q < 2)\n            return 1.0;\n\n        EdgeWeightSummary weights = EdgeWeights.Summary(graph);\n        double meanWeightedDegree = 2.0 * weights.MeanWeight * weights.EdgeCount / graph.NodeCount;\n        double logQ = Math.Log(q);\n        if (logQ <= 0.0 || weights.MedianWeight <= 0.0)\n            return 1.0;\n\n        return weights.MedianWeight * meanWeightedDegree / logQ;\n    }\n\n    public static (double TempMin, double TempMax) EstimateBracket(\n        CsrGraph graph,\n        int q,\n        double coldOvershoot = 0.05,\n        double hotOvershoot = 5.0)\n    {\n        if (coldOvershoot <= 0.0)\n            throw new ArgumentOutOfRangeException(nameof(coldOvershoot));\n        if (hotOvershoot <= coldOvershoot)\n            throw new ArgumentException(\"hotOvershoot must exceed coldOvershoot.\");\n\n        double tc = Estimate(graph, q);\n        return (tc * coldOvershoot, tc * hotOvershoot);\n    }\n}
55 | clustering/spc/SpcClusteringSession.cs | {1744 136 0.2437 4.5731} | 1797 | using System;\nusing Clustering.SPC.Partitions;\nusing Clustering.SPC.Partitions.Strategies;\nusing Clustering.SPC.Profiling;\nusing Clustering.SPC.Profiling.Signals;\nusing Clustering.SPC.Runtime.Execution;\nusing Clustering.SPC.Runtime.Scheduling;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC;\n\npublic sealed record SpcSessionResult(\n    Partition Partition,\n    SweepProfile Profile,\n    ProfileCriteria ProfileCriteria,\n    AdaptiveScheduleSummary ScheduleSummary,\n    SpcRunResult FinalRun,\n    double? Purity);\n\npublic static class SpcClusteringSession\n{\n    public static SpcSessionResult Run(\n        CsrGraph graph,\n        AdaptiveSchedulerConfig? config = null,\n        IPartitionStrategy? partitionStrategy = null,\n        ISignalAnalyzer? analyzer = null,\n        Func<int[], int[], double>? purityEvaluator = null,\n        int[]? referenceLabels = null)\n    {\n        var adaptive = AdaptiveScheduler.Run(graph, config, analyzer);\n        var partition = (partitionStrategy ?? new ThresholdSpinAgreement { Theta = 0.5 })\n            .Apply(adaptive.FinalRun);\n\n        double? purity = null;\n        if (referenceLabels is not null)\n        {\n            if (purityEvaluator is null)\n                throw new ArgumentNullException(nameof(purityEvaluator),\n                    \"A purity evaluator is required when reference labels are supplied.\");\n\n            purity = purityEvaluator(partition.Labels, referenceLabels);\n        }\n\n        return new SpcSessionResult(\n            Partition: partition,\n            Profile: adaptive.Summary.Profile,\n            ProfileCriteria: adaptive.ProfileCriteria,\n            ScheduleSummary: adaptive.Summary,\n            FinalRun: adaptive.FinalRun,\n            Purity: purity);\n    }\n}
