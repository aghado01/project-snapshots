idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
7 | estimators/WeightedGeometricMedian.cs | {4728 427 0.3854 4.1413} | 4858 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Weighted Euclidean geometric median estimator using regularized Weiszfeld iterations.\n    /// </summary>\n    public static class LocationGeometricMedian\n    {\n        public static LocationEstimate WeightedEuclidean(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights,\n            Span<double> destination,\n            Span<double> scratch,\n            int maxIterations = 128,\n            double tolerance = 1e-8,\n            double epsilon = 1e-12)\n        {\n            if (maxIterations < 0)\n                throw new ArgumentOutOfRangeException(nameof(maxIterations), \"Max iterations must be non-negative.\");\n            if (!(tolerance > 0) || double.IsNaN(tolerance) || double.IsInfinity(tolerance))\n                throw new ArgumentOutOfRangeException(nameof(tolerance), \"Tolerance must be finite and positive.\");\n            if (!(epsilon > 0) || double.IsNaN(epsilon) || double.IsInfinity(epsilon))\n                throw new ArgumentOutOfRangeException(nameof(epsilon), \"Epsilon must be finite and positive.\");\n\n            LocationMean.ValidateInputs(points, indices, weights, destination, out int dim);\n            if (scratch.Length < dim)\n                throw new ArgumentException(\"Scratch span is shorter than the feature dimension.\", nameof(scratch));\n\n            LocationEstimate initial = LocationMean.WeightedEuclidean(points, indices, weights, destination);\n            if (maxIterations == 0)\n            {\n                double initialObjective = Objective(points, indices, weights, destination, dim);\n                return new LocationEstimate(false, 0, initialObjective, 0, initial.TotalWeight);\n            }\n\n            double shift = 0;\n            int iterations = 0;\n            bool converged = false;\n\n            for (int iteration = 0; iteration < maxIterations; iteration++)\n            {\n                iterations = iteration + 1;\n                scratch.Slice(0, dim).Clear();\n\n                double denominator = 0;\n                for (int i = 0; i < indices.Length; i++)\n                {\n                    double weight = weights[i];\n                    if (weight == 0) continue;\n\n                    double[] point = points[indices[i]];\n                    double distance = EuclideanDistance(point, destination, dim);\n                    double factor = weight / Math.Max(distance, epsilon);\n\n                    denominator += factor;\n                    for (int d = 0; d < dim; d++)\n                        scratch[d] += factor * point[d];\n                }\n\n                if (denominator <= 0)\n                    throw new ArgumentException(\"At least one weight must be positive.\", nameof(weights));\n\n                double inverseDenominator = 1.0 / denominator;\n                shift = 0;\n                double centerNorm = 0;\n\n                for (int d = 0; d < dim; d++)\n                {\n                    double next = scratch[d] * inverseDenominator;\n                    double diff = next - destination[d];\n                    shift += diff * diff;\n                    centerNorm += destination[d] * destination[d];\n                    scratch[d] = next;\n                }\n\n                shift = Math.Sqrt(shift);\n                double scale = 1.0 + Math.Sqrt(centerNorm);\n\n                scratch.Slice(0, dim).CopyTo(destination);\n                if (shift <= tolerance * scale)\n                {\n                    converged = true;\n                    break;\n                }\n            }\n\n            double objective = Objective(points, indices, weights, destination, dim);\n            return new LocationEstimate(converged, iterations, objective, shift, initial.TotalWeight);\n        }\n\n        private static double Objective(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights,\n            ReadOnlySpan<double> center,\n            int dim)\n        {\n            double objective = 0;\n            for (int i = 0; i < indices.Length; i++)\n            {\n                double weight = weights[i];\n                if (weight == 0) continue;\n                objective += weight * EuclideanDistance(points[indices[i]], center, dim);\n            }\n            return objective;\n        }\n\n        private static double EuclideanDistance(double[] point, ReadOnlySpan<double> center, int dim)\n        {\n            double squared = 0;\n            for (int d = 0; d < dim; d++)\n            {\n                double diff = point[d] - center[d];\n                squared += diff * diff;\n            }\n            return Math.Sqrt(squared);\n        }\n    }\n} |
8 | estimators/WeightedMean.cs | {4175 391 0.3660 4.2116} | 4298 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Weighted Euclidean Frechet mean estimators for dense feature vectors.\n    /// </summary>\n    public static class LocationMean\n    {\n        public static LocationEstimate WeightedEuclidean(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights,\n            Span<double> destination)\n        {\n            ValidateInputs(points, indices, weights, destination, out int dim);\n\n            destination.Clear();\n            double totalWeight = 0;\n\n            for (int i = 0; i < indices.Length; i++)\n            {\n                double weight = weights[i];\n                if (weight == 0) continue;\n\n                double[] point = points[indices[i]];\n                totalWeight += weight;\n                for (int d = 0; d < dim; d++)\n                    destination[d] += weight * point[d];\n            }\n\n            if (totalWeight <= 0)\n                throw new ArgumentException(\"At least one weight must be positive.\", nameof(weights));\n\n            double inverseWeight = 1.0 / totalWeight;\n            for (int d = 0; d < dim; d++)\n                destination[d] *= inverseWeight;\n\n            double objective = SquaredObjective(points, indices, weights, destination, dim);\n            return new LocationEstimate(true, 1, objective, 0, totalWeight);\n        }\n\n        internal static void ValidateInputs(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights,\n            Span<double> destination,\n            out int dim)\n        {\n            if (points == null)\n                throw new ArgumentNullException(nameof(points));\n            if (indices.Length == 0)\n                throw new ArgumentException(\"At least one point index is required.\", nameof(indices));\n            if (indices.Length != weights.Length)\n                throw new ArgumentException(\"Indices and weights must have the same length.\", nameof(weights));\n\n            int firstIndex = indices[0];\n            if ((uint)firstIndex >= (uint)points.Length || points[firstIndex] == null)\n                throw new ArgumentOutOfRangeException(nameof(indices), \"Point index is out of range.\");\n\n            dim = points[firstIndex].Length;\n            if (dim == 0)\n                throw new ArgumentException(\"Feature vectors must not be empty.\", nameof(points));\n            if (destination.Length < dim)\n                throw new ArgumentException(\"Destination span is shorter than the feature dimension.\", nameof(destination));\n\n            for (int i = 0; i < indices.Length; i++)\n            {\n                int pointIndex = indices[i];\n                if ((uint)pointIndex >= (uint)points.Length || points[pointIndex] == null)\n                    throw new ArgumentOutOfRangeException(nameof(indices), \"Point index is out of range.\");\n                if (points[pointIndex].Length != dim)\n                    throw new ArgumentException(\"All selected feature vectors must have the same dimension.\", nameof(points));\n\n                double weight = weights[i];\n                if (double.IsNaN(weight) || double.IsInfinity(weight) || weight < 0)\n                    throw new ArgumentOutOfRangeException(nameof(weights), \"Weights must be finite and non-negative.\");\n            }\n        }\n\n        internal static double SquaredObjective(\n            double[][] points,\n            ReadOnlySpan<int> indices,\n            ReadOnlySpan<double> weights,\n            ReadOnlySpan<double> center,\n            int dim)\n        {\n            double objective = 0;\n            for (int i = 0; i < indices.Length; i++)\n            {\n                double weight = weights[i];\n                if (weight == 0) continue;\n\n                double[] point = points[indices[i]];\n                double squared = 0;\n                for (int d = 0; d < dim; d++)\n                {\n                    double diff = point[d] - center[d];\n                    squared += diff * diff;\n                }\n                objective += weight * squared;\n            }\n            return objective;\n        }\n    }\n} |
9 | gmm/GaussianComponent.cs | {7102 740 0.2747 4.6827} | 8003 | using System;\nusing DistanceMetrics;\nusing LinearAlgebra;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// A single Gaussian component: weight π_k, mean μ_k, covariance Σ_k, and the\n    /// derived quantities needed for efficient E-Step evaluation.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// <strong>Zero-allocation M-Step.</strong>\n    /// Pre-allocated scratchpads (<see cref=\"ScratchMean\"/>, <see cref=\"ScratchCov\"/>)\n    /// are accumulated in place during the M-Step and committed via\n    /// <see cref=\"CommitScratch\"/>. No heap allocations occur during the EM loop.\n    /// </para>\n    /// <para>\n    /// <strong>Cholesky preservation.</strong>\n    /// The lower-triangular factor L (Σ = LLᵀ) is retained in a dedicated field after\n    /// <see cref=\"UpdateCache\"/> completes. Future sampling support can be added as\n    /// x = μ + L·z, z ~ N(0,I) without recomputing the decomposition.\n    /// </para>\n    /// </remarks>\n    public sealed class GaussianComponent\n    {\n        private readonly int _dim;\n\n        // ── Parameters ──────────────────────────────────────────────────────────\n\n        /// <summary>Mixture weight π_k.</summary>\n        public double Weight { get; set; }\n\n        /// <summary>Mean vector μ_k. Write elements in place; do not replace the array reference.</summary>\n        public double[] Mean { get; }\n\n        /// <summary>Covariance Σ_k. Write elements in place; do not replace the array reference.</summary>\n        public double[,] Covariance { get; }\n\n        // ── Cached quantities (updated by UpdateCache) ───────────────────────────\n\n        /// <summary>Σ⁻¹ — used in Mahalanobis distance computation during the E-Step.</summary>\n        public double[,] CovarianceInverse { get; }\n\n        /// <summary>\n        /// −½(D·ln(2π) + ln|Σ|). Cached to avoid recomputing the log-determinant on every\n        /// PDF evaluation. Set by <see cref=\"UpdateCache\"/>.\n        /// </summary>\n        public double LogNormalizationFactor { get; private set; }\n\n        // ── M-Step scratchpads (pre-allocated; internal to GaussianMixtureModel) ─\n\n        internal double[] ScratchMean { get; }\n        internal double[,] ScratchCov { get; }\n\n        // ── Cholesky work arrays ─────────────────────────────────────────────────\n\n        private readonly CholeskyDecomposition _chol;\n\n        public GaussianComponent(int dim)\n        {\n            _dim = dim;\n            Mean = new double[dim];\n            Covariance = new double[dim, dim];\n            CovarianceInverse = new double[dim, dim];\n            ScratchMean = new double[dim];\n            ScratchCov = new double[dim, dim];\n            _chol = new CholeskyDecomposition(dim);\n        }\n\n        // ── M-Step helpers ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Zeroes both scratchpad arrays. Call once per component per EM iteration\n        /// before accumulating weighted observations.\n        /// </summary>\n        internal void ResetScratch()\n        {\n            Array.Clear(ScratchMean, 0, ScratchMean.Length);\n            Array.Clear(ScratchCov, 0, ScratchCov.Length);\n        }\n\n        /// <summary>\n        /// Copies the normalised <see cref=\"ScratchMean\"/> into <see cref=\"Mean\"/>,\n        /// normalises <see cref=\"ScratchCov\"/> by <paramref name=\"effectiveCount\"/> into\n        /// <see cref=\"Covariance\"/> (with diagonal regularisation), updates\n        /// <see cref=\"Weight\"/>, and calls <see cref=\"UpdateCache\"/>.\n        /// </summary>\n        /// <param name=\"effectiveCount\">\n        /// Σ_i γ(z_{ik}) — the soft cluster population. <see cref=\"ScratchMean\"/> must\n        /// already be divided by this value before calling (done by the M-Step caller).\n        /// </param>\n        /// <param name=\"n\">Total data count N (used to compute the new weight).</param>\n        /// <param name=\"regularization\">Ridge term added to the diagonal of Σ to prevent singularity.</param>\n        internal void CommitScratch(double effectiveCount, int n, double regularization = 1e-6)\n        {\n            Weight = effectiveCount / n;\n\n            if (effectiveCount > 1e-12)\n            {\n                // ScratchMean has already been normalised by the caller; copy directly.\n                Array.Copy(ScratchMean, Mean, _dim);\n\n                double invCount = 1.0 / effectiveCount;\n                for (int d1 = 0; d1 < _dim; d1++)\n                    for (int d2 = 0; d2 < _dim; d2++)\n                    {\n                        Covariance[d1, d2] = ScratchCov[d1, d2] * invCount\n                                             + (d1 == d2 ? regularization : 0.0);\n                    }\n            }\n            // Dead cluster: leave Mean/Covariance at prior values.\n            // Weight ≈ 0 suppresses this component's contribution in future E-Steps.\n\n            UpdateCache();\n        }\n\n        // ── Cache update ─────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Decomposes <see cref=\"Covariance\"/> via <see cref=\"CholeskyDecomposition\"/>,\n        /// writes Σ⁻¹ into <see cref=\"CovarianceInverse\"/>, and caches\n        /// <see cref=\"LogNormalizationFactor\"/>. Call after writing new values directly\n        /// into <see cref=\"Covariance\"/> from external initialisation or the M-Step.\n        /// </summary>\n        public void UpdateCache()\n        {\n            _chol.Decompose(Covariance);\n            _chol.WriteInverseTo(CovarianceInverse);\n            LogNormalizationFactor = -0.5 * (_dim * Math.Log(2.0 * Math.PI) + _chol.LogDet);\n        }\n\n        // ── PDF evaluation ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Evaluates ln N(x | μ, Σ). Numerically safe in all dimensions; use in the E-Step.\n        /// </summary>\n        public double EvaluateLogPdf(double[] x) =>\n            LogNormalizationFactor - 0.5 * ComputeMahalanobisSquared(x);\n\n        /// <summary>\n        /// Evaluates N(x | μ, Σ) directly. May underflow to 0 in high dimensions;\n        /// prefer <see cref=\"EvaluateLogPdf\"/> for all EM arithmetic.\n        /// </summary>\n        public double EvaluatePdf(double[] x) =>\n            Math.Exp(EvaluateLogPdf(x));\n\n        /// <summary>\n        /// Returns the squared Mahalanobis distance from <paramref name=\"x\"/> to this component's mean.\n        /// D² = (x−μ)ᵀ Σ⁻¹ (x−μ). Exposed for use by <see cref=\"GaussianMixtureModel.Mahal\"/>.\n        /// Delegates to <see cref=\"Mahalanobis.DistanceSquared\"/> in <c>DistanceMetrics</c>.\n        /// </summary>\n        public double MahalanobisSquared(double[] x)\n            => Mahalanobis.DistanceSquared(x, Mean, CovarianceInverse);\n\n        /// <summary>\n        /// Draws a single sample via the <see cref=\"CholeskyDecomposition\"/> preserved\n        /// from the last <see cref=\"UpdateCache\"/> call.\n        /// </summary>\n        public double[] Sample(Random rng) => _chol.Sample(rng, Mean);\n\n        private double ComputeMahalanobisSquared(double[] x)\n            => Mahalanobis.DistanceSquared(x, Mean, CovarianceInverse);\n    }\n} |
