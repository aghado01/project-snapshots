idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
286 | user-repl/commands/HdbscanPreset.cs | {3649 289 0.3998 4.0471} | 3722 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Text.Json;\n\nnamespace UserRepl.Commands;\n\npublic sealed record HdbscanPreset\n{\n\n    public string?                              Dataset            { get; init; }\n    public Dictionary<string, object?>?         Params             { get; init; }\n    public string?                              DatasetFile        { get; init; }\n    public string?                              LabelColumn        { get; init; }\n    public bool?                                HasHeader          { get; init; }\n    public string?                              Delimiter          { get; init; }\n    public int?                                 Seed               { get; init; }\n\n    public string?                              BaseDir            { get; init; }\n    public string?                              RunName            { get; init; }\n    public bool?                                NoGuid             { get; init; }\n\n    public int?                                 MinPts             { get; init; }\n    public int?                                 MinClusterSize     { get; init; }\n    public bool?                                AllowSingleCluster { get; init; }\n    public string?                              DistanceMetric     { get; init; }\n    public double?                              MinkowskiP         { get; init; }\n\n    internal static HdbscanPreset Load(string path)\n    {\n        if (!File.Exists(path))\n            throw new FileNotFoundException($\"Preset file not found: {path}\", path);\n\n        string json = File.ReadAllText(path);\n        return JsonSerializer.Deserialize<HdbscanPreset>(json, JsonOptions)\n            ?? throw new InvalidDataException($\"Preset at {path} deserialized to null.\");\n    }\n\n    internal void ApplyTo(HdbscanCommand.Options options)\n    {\n\n        if (Dataset is not null)            options.DatasetKind        = HdbscanCommand.ParseDatasetKind(Dataset);\n        if (Params is not null)\n            foreach (var kvp in Params)     options.GeneratorParameters[kvp.Key] = kvp.Value;\n        if (DatasetFile is not null)        options.DatasetFile        = DatasetFile;\n        if (LabelColumn is not null)        options.LabelColumn        = LabelColumn;\n        if (HasHeader.HasValue)             options.HasHeader          = HasHeader.Value;\n        if (Delimiter is not null)          options.Delimiter          = HdbscanCommand.ParseDelimiter(Delimiter);\n        if (Seed.HasValue)                  options.Seed               = Seed.Value;\n\n        if (BaseDir is not null)            options.BaseDirectory      = BaseDir;\n        if (RunName is not null)            options.RunName            = RunName;\n        if (NoGuid.HasValue)                options.NoGuid             = NoGuid.Value;\n\n        if (MinPts.HasValue)                options.MinPts             = MinPts.Value;\n        if (MinClusterSize.HasValue)        options.MinClusterSize     = MinClusterSize.Value;\n        if (AllowSingleCluster.HasValue)    options.AllowSingleCluster = AllowSingleCluster.Value;\n        if (DistanceMetric is not null)     options.DistanceMetricSpec = DistanceMetric;\n        if (MinkowskiP.HasValue)            options.MinkowskiP         = MinkowskiP.Value;\n    }\n\n    private static readonly JsonSerializerOptions JsonOptions = new()\n    {\n        PropertyNamingPolicy        = JsonNamingPolicy.SnakeCaseLower,\n        ReadCommentHandling         = JsonCommentHandling.Skip,\n        AllowTrailingCommas         = true,\n        PropertyNameCaseInsensitive = true,\n        TypeInfoResolver            = UserReplJsonContext.Default,\n    };\n}
287 | user-repl/commands/ManifestMaterialization.cs | {4742 347 0.3089 4.5472} | 4939 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.Graphical.SPC.Runtime.Initialization;\nusing Graphs;\nusing Graphs.Distance;\nusing GraphsCouplingMixtureBandwidth = Graphs.Coupling.MixtureBandwidth;\nusing GraphsCouplingMixtureWeights  = Graphs.Coupling.MixtureWeights;\n\nnamespace UserRepl.Commands;\n\npublic static class ManifestMaterialization\n{\n\n    public static SpcUserDataset Materialize(this DatasetSpec spec)\n    {\n        ArgumentNullException.ThrowIfNull(spec);\n\n        if (string.Equals(spec.Source, \"csv\", StringComparison.OrdinalIgnoreCase))\n        {\n            string csvPath = spec.CsvPath\n                ?? throw new InvalidOperationException(\"DatasetSpec.Source='csv' requires CsvPath.\");\n            return SpcUserSession.FromCsv(\n                csvPath,\n                spec.LabelColumn,\n                spec.HasHeader ?? true,\n                ParseDelimiter(spec.Delimiter));\n        }\n\n        if (string.Equals(spec.Source, \"synthetic\", StringComparison.OrdinalIgnoreCase))\n        {\n            string generatorName = spec.GeneratorName\n                ?? throw new InvalidOperationException(\"DatasetSpec.Source='synthetic' requires GeneratorName.\");\n\n            var parameters = new Dictionary<string, object?>(StringComparer.OrdinalIgnoreCase);\n            if (spec.Seed.HasValue) parameters[\"seed\"] = spec.Seed.Value;\n            if (spec.GeneratorParameters is not null)\n                foreach (var kvp in spec.GeneratorParameters) parameters[kvp.Key] = kvp.Value;\n            return SpcUserSession.GenerateDataset(generatorName, parameters);\n        }\n\n        throw new InvalidOperationException(\n            $\"Unknown DatasetSpec.Source '{spec.Source}'. Expected 'synthetic' or 'csv'.\");\n    }\n\n    public static GraphCompilerConfig ToConfig(this GraphSpec spec)\n    {\n        ArgumentNullException.ThrowIfNull(spec);\n\n        var scaling = spec.KernelType.ToLowerInvariant() switch\n        {\n            \"gaussian\" => new Gaussian(spec.Bandwidth),\n            \"cauchy\" => new Cauchy(spec.Bandwidth),\n            \"laplacian\" => new Laplacian(spec.Bandwidth),\n            \"linear\" => new Linear(spec.Bandwidth),\n            \"mixture\" => new Mixture(\n                spec.MixtureWeights?.GetValueOrDefault(\"gaussian\") ?? 0.0,\n                spec.MixtureWeights?.GetValueOrDefault(\"cauchy\") ?? 0.0,\n                spec.MixtureWeights?.GetValueOrDefault(\"laplacian\") ?? 0.0),\n            _ => new Gaussian(spec.Bandwidth),\n        };\n\n        return new GraphCompilerConfig\n        {\n            Topology = new GraphTopologyConfig\n            {\n                Kind = spec.ProximityRule == \"epsilon\"\n                    ? TopologyKind.EpsilonBall\n                    : TopologyKind.Knn,\n                K = spec.K,\n                Epsilon = spec.Epsilon,\n            },\n            Filter = new GraphFilterConfig\n            {\n                Kind = spec.ProximityRule == \"mutualknn\"\n                    ? FilterKind.MutualKnn\n                    : FilterKind.OrRule,\n            },\n            Repair = new GraphRepairConfig\n            {\n                Kind = spec.EnsureConnected ? RepairKind.MstMin : RepairKind.NoRepair,\n            },\n            Scaling = new GraphScalingConfig\n            {\n                Kernel = scaling,\n                LmpRescale = spec.ApplyLocalMutualProximity,\n            },\n            Bandwidth = new GraphBandwidthConfig\n            {\n                StrategyOverride = null,\n                Timing = BandwidthTiming.PreRepair,\n            },\n        };\n    }\n\n    private static GraphsCouplingMixtureWeights BuildMixtureWeights(IReadOnlyDictionary<string, double> dict)\n        => new(\n            Gaussian:  Get(dict, \"gaussian\", \"gauss\", \"g\"),\n            Cauchy:    Get(dict, \"cauchy\",   \"c\"),\n            Laplacian: Get(dict, \"laplacian\", \"laplace\", \"l\"));\n\n    private static GraphsCouplingMixtureBandwidth BuildMixtureBandwidth(IReadOnlyDictionary<string, double> dict)\n        => new(\n            Gaussian:  Get(dict, \"gaussian\", \"gauss\", \"g\"),\n            Cauchy:    Get(dict, \"cauchy\",   \"c\"),\n            Laplacian: Get(dict, \"laplacian\", \"laplace\", \"l\"));\n\n    private static double Get(IReadOnlyDictionary<string, double> dict, params string[] aliases)\n    {\n        foreach (string a in aliases)\n            if (dict.TryGetValue(a, out double v)) return v;\n        return 0.0;\n    }\n\n    private static char ParseDelimiter(string? spec)\n    {\n        if (string.IsNullOrEmpty(spec)) return ',';\n        return spec switch\n        {\n            \"tab\" or \"\\\\t\" => '\\t',\n            var s when s.Length == 1 => s[0],\n            _ => throw new InvalidOperationException($\"Invalid delimiter spec '{spec}'.\"),\n        };\n    }\n}
288 | user-repl/commands/MixtureSpecParser.cs | {2136 191 0.2898 4.5979} | 2235 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.Linq;\nusing Graphs.Coupling;\n\nnamespace UserRepl.Commands;\n\npublic static class MixtureSpecParser\n{\n    public static MixtureWeights ParseWeights(string spec)\n    {\n        var dict = ParseKeyValuePairs(spec);\n        return new MixtureWeights(\n            Gaussian:  Get(dict, \"gauss\", \"gaussian\", \"g\"),\n            Cauchy:    Get(dict, \"cauchy\", \"c\"),\n            Laplacian: Get(dict, \"laplace\", \"laplacian\", \"l\"));\n    }\n\n    public static MixtureBandwidth ParseBandwidth(string spec)\n    {\n        var dict = ParseKeyValuePairs(spec);\n        return new MixtureBandwidth(\n            Gaussian:  Get(dict, \"gauss\", \"gaussian\", \"g\"),\n            Cauchy:    Get(dict, \"cauchy\", \"c\"),\n            Laplacian: Get(dict, \"laplace\", \"laplacian\", \"l\"));\n    }\n\n    private static Dictionary<string, double> ParseKeyValuePairs(string spec)\n    {\n        if (string.IsNullOrWhiteSpace(spec))\n            throw new ArgumentException(\"Mixture spec must be non-empty.\", nameof(spec));\n\n        var dict = new Dictionary<string, double>(StringComparer.OrdinalIgnoreCase);\n        foreach (string raw in spec.Split(',', StringSplitOptions.RemoveEmptyEntries))\n        {\n            string token = raw.Trim();\n            int eq = token.IndexOf('=');\n            if (eq <= 0)\n                throw new ArgumentException(\n                    $\"Mixture token '{token}' must be in the form name=value.\");\n\n            string key = token.Substring(0, eq).Trim();\n            string val = token.Substring(eq + 1).Trim();\n\n            if (!double.TryParse(val, NumberStyles.Float, CultureInfo.InvariantCulture, out double v))\n                throw new ArgumentException(\n                    $\"Cannot parse mixture value '{val}' for component '{key}' as double.\");\n\n            dict[key] = v;\n        }\n        return dict;\n    }\n\n    private static double Get(Dictionary<string, double> dict, params string[] aliases)\n    {\n        foreach (string a in aliases)\n            if (dict.TryGetValue(a, out double v))\n                return v;\n        return 0.0;\n    }\n}
289 | user-repl/commands/RunManifest.cs | {4158 267 0.4505 3.8762} | 4274 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Text.Json;\n\nnamespace UserRepl.Commands;\n\npublic sealed record RunManifest(\n    string                    SchemaVersion,\n    DateTime                  CreatedUtc,\n    string                    Algorithm,        // \"spc\" | \"hdbscan\"\n    string                    CommandLine,\n    DatasetSpec               Dataset,\n    GraphSpec?                Graph,            // null for hdbscan\n    SweepSpec?                Sweep,            // null for hdbscan\n    HdbscanSpec?              Hdbscan,          // null for spc\n    OutputSpec                Output)\n{\n    public const string CurrentSchemaVersion = \"1.0\";\n    public const string FileName = \"manifest.json\";\n\n    public static string PathFor(string runDirectory) => Path.Combine(runDirectory, FileName);\n\n    public void WriteTo(string runDirectory)\n    {\n        string path = PathFor(runDirectory);\n        Directory.CreateDirectory(runDirectory);\n        File.WriteAllText(path, JsonSerializer.Serialize(this, JsonOptions));\n    }\n\n    public static RunManifest ReadFrom(string runDirectory)\n    {\n        string path = PathFor(runDirectory);\n        if (!File.Exists(path))\n            throw new FileNotFoundException($\"No manifest at {path}.\", path);\n        string json = File.ReadAllText(path);\n        return JsonSerializer.Deserialize<RunManifest>(json, JsonOptions)\n            ?? throw new InvalidDataException($\"Manifest at {path} deserialized to null.\");\n    }\n\n    public static readonly JsonSerializerOptions JsonOptions = new()\n    {\n        WriteIndented = true,\n        PropertyNamingPolicy = JsonNamingPolicy.SnakeCaseLower,\n        TypeInfoResolver = UserReplJsonContext.Default,\n    };\n}\n\npublic sealed record DatasetSpec(\n    string                              Source,                 // \"synthetic\" | \"csv\"\n    string?                             GeneratorName,\n    IReadOnlyDictionary<string, object?>? GeneratorParameters,\n    int?                                Seed,                   // synthetic-only\n    string?                             CsvPath,\n    string?                             LabelColumn,\n    bool?                               HasHeader,\n    string?                             Delimiter);             // serialized as string for readability\n\npublic sealed record GraphSpec(\n    string                              ProximityRule,          // enum name\n    int                                 K,\n    double                              Epsilon,\n    string                              KernelType,             // enum name\n    double                              Bandwidth,\n    string?                             DistanceMetric,         // spec string (\"euclidean\", \"minkowski:p=1.5\", ...)\n    bool                                EnsureConnected,\n    bool                                ApplyLocalMutualProximity,\n    IReadOnlyDictionary<string, double>? MixtureWeights,\n    IReadOnlyDictionary<string, double>? MixtureBandwidth);\n\npublic sealed record SweepSpec(\n    string                              Schedule,               // \"Adaptive\" | \"FixedGrid\"\n    string?                             TemperaturesSpec,       // raw --temperatures string for FixedGrid\n    int                                 Replicas,\n    int                                 BurnInCycles,\n    int                                 MeasureCycles,\n    int                                 FinalBurn,\n    int                                 FinalCycles,\n    int                                 Q,\n    string                              Analyzer,\n    string                              PartitionStrategy,\n    double                              Theta);\n\npublic sealed record HdbscanSpec(\n    int                                 MinPts,\n    int?                                MinClusterSize,\n    bool                                AllowSingleCluster,\n    string                              DistanceMetric,\n    double                              MinkowskiP);\n\npublic sealed record OutputSpec(\n    string                              RunDirectory,\n    string?                             CheckpointDirectory);
