idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
9 | clustering/gmm/ModalMergeStrategy.cs | {4267 436 0.3916 4.1690} | 4708 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Mode-tree merging: components whose means ascend to the same local mode of\n    /// the mixture density are assigned to the same cluster. Geometry-only — does\n    /// not consume the responsibility matrix. See docs/gmm.md.\n    /// </summary>\n    public sealed class ModalMergeStrategy : IComponentMergeStrategy\n    {\n        /// <summary>\n        /// Euclidean distance within which two converged modes are treated as one.\n        /// <see cref=\"double.NaN\"/> (default) selects an adaptive threshold equal to\n        /// 5% of the mean pairwise distance between component means.\n        /// </summary>\n        public double ModeTolerance { get; }\n\n        /// <summary>Iteration cap forwarded to <c>ModeAscent.Ascend</c>.</summary>\n        public int MaxAscentSteps { get; }\n\n        /// <summary>Displacement tolerance forwarded to <c>ModeAscent.Ascend</c>.</summary>\n        public double AscentTol { get; }\n\n        public ModalMergeStrategy(\n            double modeTolerance = double.NaN,\n            int maxAscentSteps = 50,\n            double ascentTol = 1e-7)\n        {\n            ModeTolerance = modeTolerance;\n            MaxAscentSteps = maxAscentSteps;\n            AscentTol = ascentTol;\n        }\n\n        /// <inheritdoc />\n        public int[] Merge(GaussianComponent[] components, double[,]? responsibilities = null)\n        {\n            int k = components.Length;\n            if (k == 0) return Array.Empty<int>();\n            if (k == 1) return new[] { 0 };\n\n            // ── Ascend from each component mean (single shared scratch) ──────────\n            int d = components[0].Mean.Length;\n            var scratch = new ModeAscentScratch(d, k);\n            double[][] modes = new double[k][];\n            for (int i = 0; i < k; i++)\n            {\n                modes[i] = new double[d];\n                ModeAscent.Ascend(components[i].Mean, components, modes[i], scratch,\n                                  MaxAscentSteps, AscentTol);\n            }\n\n            // ── Resolve tolerance ─────────────────────────────────────────────────\n            double threshold = double.IsNaN(ModeTolerance)\n                ? ComputeAdaptiveTolerance(components, k)\n                : ModeTolerance;\n\n            // ── Sequential grouping by mode proximity ──────────────────────────────\n            // clusterModes[c] is the representative mode for cluster c.\n            // Allocation: at most k clusters (all components distinct).\n            int[] map = new int[k];\n            double[][] clusterModes = new double[k][];\n            int clusterCount = 0;\n\n            for (int i = 0; i < k; i++)\n            {\n                int assigned = -1;\n                for (int c = 0; c < clusterCount; c++)\n                {\n                    if (EuclideanDistance(modes[i], clusterModes[c]) < threshold)\n                    {\n                        assigned = c;\n                        break;\n                    }\n                }\n\n                if (assigned >= 0)\n                {\n                    map[i] = assigned;\n                }\n                else\n                {\n                    clusterModes[clusterCount] = modes[i];\n                    map[i] = clusterCount++;\n                }\n            }\n\n            return map;\n        }\n\n        // ── Internals ──────────────────────────────────────────────────────────────\n\n        // 5% of the mean pairwise distance between component means — scale-free.\n        private static double ComputeAdaptiveTolerance(GaussianComponent[] components, int k)\n        {\n            double sum = 0.0;\n            int pairs = 0;\n            for (int i = 0; i < k; i++)\n                for (int j = i + 1; j < k; j++)\n                {\n                    sum += EuclideanDistance(components[i].Mean, components[j].Mean);\n                    pairs++;\n                }\n            return sum / pairs * 0.05;\n        }\n\n        private static double EuclideanDistance(double[] a, double[] b)\n        {\n            double sum = 0.0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double d = a[i] - b[i];\n                sum += d * d;\n            }\n            return Math.Sqrt(sum);\n        }\n    }\n} |
10 | clustering/gmm/ModeAscent.cs | {6785 709 0.4168 4.1746} | 7250 | using System;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// Pre-allocated scratch for repeated <c>ModeAscent.Ascend</c> calls.\n    /// </summary>\n    public sealed class ModeAscentScratch\n    {\n        internal readonly double[] X;\n        internal readonly double[] XNew;\n        internal readonly double[] LogR;\n        internal readonly double[] R;\n        internal readonly double[] Grad;\n\n        public ModeAscentScratch(int dimension, int components)\n        {\n            X = new double[dimension];\n            XNew = new double[dimension];\n            LogR = new double[components];\n            R = new double[components];\n            Grad = new double[dimension];\n        }\n    }\n\n    /// <summary>\n    /// Gradient ascent on a Gaussian mixture log-density. Used by\n    /// <see cref=\"ModalMergeStrategy\"/> to map components to density basins.\n    /// Analytic gradient ∇ log p(x) = Σ_k r_k(x) · Σ_k⁻¹ (μ_k − x); backtracking\n    /// line search keeps the density monotone.\n    /// </summary>\n    public static class ModeAscent\n    {\n        // ── Public API ────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Ascends from <paramref name=\"start\"/> to the nearest local mode of the\n        /// mixture log-density. Components must have up-to-date caches.\n        /// </summary>\n        public static double[] Ascend(\n            double[] start,\n            GaussianComponent[] components,\n            int maxSteps = 50,\n            double tol = 1e-7)\n        {\n            var scratch = new ModeAscentScratch(start.Length, components.Length);\n            double[] result = new double[start.Length];\n            Ascend(start, components, result, scratch, maxSteps, tol);\n            return result;\n        }\n\n        /// <summary>\n        /// Allocation-free overload: caller supplies a result buffer and reusable\n        /// scratch. The result buffer receives the converged mode.\n        /// </summary>\n        public static void Ascend(\n            double[] start,\n            GaussianComponent[] components,\n            double[] result,\n            ModeAscentScratch scratch,\n            int maxSteps = 50,\n            double tol = 1e-7)\n        {\n            int d = start.Length;\n            int k = components.Length;\n\n            double[] x = scratch.X;\n            double[] xNew = scratch.XNew;\n            double[] logR = scratch.LogR;\n            double[] r = scratch.R;\n            double[] grad = scratch.Grad;\n\n            Array.Copy(start, x, d);\n\n            for (int step = 0; step < maxSteps; step++)\n            {\n                // Log-space responsibilities r_k(x) for stability at large Mahalanobis.\n                double logMax = double.NegativeInfinity;\n                for (int ki = 0; ki < k; ki++)\n                {\n                    logR[ki] = components[ki].LogWeight\n                               + components[ki].EvaluateLogPdf(x);\n                    if (logR[ki] > logMax) logMax = logR[ki];\n                }\n\n                double rSum = 0.0;\n                for (int ki = 0; ki < k; ki++)\n                {\n                    r[ki] = Math.Exp(logR[ki] - logMax);\n                    rSum += r[ki];\n                }\n                for (int ki = 0; ki < k; ki++) r[ki] /= rSum;\n\n                // ∇ log p(x) = Σ_k r_k · Σ_k⁻¹ (μ_k − x).\n                Array.Clear(grad, 0, d);\n                for (int ki = 0; ki < k; ki++)\n                {\n                    if (r[ki] < 1e-15) continue;\n                    double[] mu = components[ki].Mean;\n                    double[,] sigmaInv = components[ki].CovarianceInverse;\n                    for (int i = 0; i < d; i++)\n                    {\n                        double dot = 0.0;\n                        for (int j = 0; j < d; j++)\n                            dot += sigmaInv[i, j] * (mu[j] - x[j]);\n                        grad[i] += r[ki] * dot;\n                    }\n                }\n\n                // Backtracking line search keeps density monotone (up to 30 halvings).\n                double logP0 = LogMixtureDensity(x, components, logR);\n                double alpha = 1.0;\n                bool improved = false;\n\n                for (int ls = 0; ls < 30; ls++)\n                {\n                    for (int i = 0; i < d; i++) xNew[i] = x[i] + alpha * grad[i];\n                    if (LogMixtureDensity(xNew, components, logR) > logP0)\n                    {\n                        improved = true;\n                        break;\n                    }\n                    alpha *= 0.5;\n                }\n\n                if (!improved) break;  // already at a local mode\n\n                double disp = 0.0;\n                for (int i = 0; i < d; i++)\n                {\n                    double delta = xNew[i] - x[i];\n                    disp += delta * delta;\n                    x[i] = xNew[i];\n                }\n                if (Math.Sqrt(disp) < tol) break;\n            }\n\n            Array.Copy(x, result, d);\n        }\n\n        /// <summary>\n        /// Index of the component whose density basin contains <paramref name=\"point\"/>:\n        /// ascends to the local mode, then returns the component with the highest\n        /// weighted log-pdf at that mode.\n        /// </summary>\n        public static int GetBasin(\n            double[] point,\n            GaussianComponent[] components,\n            int maxSteps = 50,\n            double tol = 1e-7)\n        {\n            double[] mode = Ascend(point, components, maxSteps, tol);\n\n            int best = 0;\n            double bestScore = double.NegativeInfinity;\n            for (int ki = 0; ki < components.Length; ki++)\n            {\n                double score = components[ki].LogWeight\n                               + components[ki].EvaluateLogPdf(mode);\n                if (score > bestScore) { bestScore = score; best = ki; }\n            }\n            return best;\n        }\n\n        // ── Internals ─────────────────────────────────────────────────────────────\n\n        // Log mixture density at x via log-sum-exp. scratch length ≥ components.Length.\n        private static double LogMixtureDensity(\n            double[] x,\n            GaussianComponent[] components,\n            double[] scratch)\n        {\n            double logMax = double.NegativeInfinity;\n            for (int ki = 0; ki < components.Length; ki++)\n            {\n                scratch[ki] = components[ki].LogWeight\n                              + components[ki].EvaluateLogPdf(x);\n                if (scratch[ki] > logMax) logMax = scratch[ki];\n            }\n            double sum = 0.0;\n            for (int ki = 0; ki < components.Length; ki++)\n                sum += Math.Exp(scratch[ki] - logMax);\n            return logMax + Math.Log(sum);\n        }\n    }\n} |
11 | clustering/hdbscan/placeholder.cs | {0 0 0.0000 0.0000} | 0 |  |
12 | clustering/spc/batch.cs | {13863 1052 0.3470 4.2496} | 14177 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\nusing DistanceMetrics;\nusing ProximityGraphs;\n\nnamespace SpcCore\n{\n    // =========================================================================\n    // Metric Enum - dispatch key for selecting standalone metric primitives\n    // =========================================================================\n    public enum SpcMetric { Hamming, Euclidean, Canberra, Manhattan, Minkowski, Cosine, JensenShannon, Jaccard, Wasserstein, Mahalanobis, FisherRaoSimplex, FisherRaoHalfPlane, Poincare }\n\n    // =========================================================================\n    // Delta Estimator - bandwidth selection for coupling auto-delta\n    // =========================================================================\n    public enum DeltaEstimator { Mean, Median }\n\n    // =========================================================================\n    // Proximity - graph construction rule\n    // =========================================================================\n    public enum SpcProximity { Knn, MutualKnn, EpsilonBall, MstAugmented }\n\n    // =========================================================================\n    // Coupling Kernel - edge weight function J(d, delta)\n    // =========================================================================\n    public enum CouplingKernel { Gaussian, Cauchy, Laplacian, Linear }\n\n    // =========================================================================\n    // Batch Request / Result DTOs - public SPC run contract\n    // =========================================================================\n\n    public sealed class SpcBatchRequest\n    {\n        public SpcMetric Metric { get; set; } = SpcMetric.Hamming;\n\n        // Proximity rule: how to select which pairs become edges.\n        // Default Knn applies OR-symmetrization to produce an undirected graph.\n        public SpcProximity Proximity { get; set; } = SpcProximity.Knn;\n\n        // Epsilon radius for EpsilonBall proximity; ignored by other rules.\n        public double Epsilon { get; set; } = 0.0;\n\n        // Hamming path: 64-bit SimHash signatures produced upstream by Hashish.\n        public ulong[] SimHashes { get; set; }\n\n        // Feature-vector path for floating-point metrics.\n        public double[][] Features { get; set; }\n\n        // NCD path: raw byte payloads for pairwise normalized compression distance.\n        public byte[][] Documents { get; set; }\n\n        // Compressor selection for NCD.\n        public NcdCompressor NcdCompressor { get; set; } = NcdCompressor.Brotli;\n\n        public int K { get; set; }\n        public double[] Temperatures { get; set; } = Array.Empty<double>();\n        public int Steps { get; set; }\n        public int BaseSeed { get; set; } = -1;\n\n        // Coupling scale. If 0 or negative, auto-computed from 1-NN distances.\n        public double Delta { get; set; } = 0.0;\n\n        public DeltaEstimator Estimator { get; set; } = DeltaEstimator.Mean;\n        public CouplingKernel CouplingKernel { get; set; } = CouplingKernel.Gaussian;\n\n        public double MinkowskiP { get; set; } = 2.0;\n        public double JaccardThreshold { get; set; } = 0.5;\n        public double[,] CovarianceInverse { get; set; }\n\n        // In-memory checkpoint. Takes precedence over ResumeDirectory.\n        public SpcCheckpoint Checkpoint { get; set; }\n\n        // Opt-in disk resume: pass a prior SpcBatchResult.RunDirectory value\n        // together with CheckpointDirectory. Ignored if Checkpoint is provided.\n        public string ResumeDirectory { get; set; }\n\n        // Working root for state artifacts. Each run writes under\n        // CheckpointDirectory\\{RunDirectory}\\ with payload-type subdirectories.\n        public string CheckpointDirectory { get; set; }\n\n        // Greedy non-redundant default: JSON summaries plus compressed spin deltas.\n        public SpcCheckpointPersistenceOptions CheckpointPersistence { get; set; } = new SpcCheckpointPersistenceOptions();\n    }\n\n    public sealed class SpcBatchResult\n    {\n        public double[] Temperatures { get; set; } = Array.Empty<double>();\n        public Dictionary<double, int[]> FinalSpins { get; set; } = new Dictionary<double, int[]>();\n\n        // Fixed topology shared by all temperatures and used by post-hoc analysis.\n        public CsrGraph Graph { get; set; }\n\n        public string[] Warnings { get; set; } = Array.Empty<string>();\n\n        /// <summary>\n        /// The yyyyMMdd_HHmmss stamp used as this run's state directory name.\n        /// Pass as SpcBatchRequest.ResumeDirectory with CheckpointDirectory to resume.\n        /// </summary>\n        public string RunDirectory { get; set; }\n\n        /// <summary>\n        /// Full path to the run manifest when disk checkpointing is enabled.\n        /// </summary>\n        public string StateManifestPath { get; set; }\n\n        public SpcCheckpoint ToCheckpoint()\n        {\n            var checkpoint = new SpcCheckpoint();\n            for (int index = 0; index < Temperatures.Length; index++)\n            {\n                double temperature = Temperatures[index];\n                if (FinalSpins.TryGetValue(temperature, out var spins))\n                {\n                    checkpoint.Spins[temperature] = spins;\n                    checkpoint.TemperatureStates[temperature] = SpcBatch.CreateTemperatureState(\n                        string.Empty,\n                        temperature,\n                        0,\n                        new SimulationResult(temperature, spins));\n                }\n            }\n            return checkpoint;\n        }\n    }\n\n    // =========================================================================\n    // SpcCheckpoint - pause/resume contract and manifest carrier\n    // =========================================================================\n\n    public sealed class SpcCheckpoint\n    {\n        public Dictionary<double, int[]> Spins { get; set; } = new Dictionary<double, int[]>();\n        public Dictionary<double, SpcTemperatureState> TemperatureStates { get; set; } = new Dictionary<double, SpcTemperatureState>();\n        public SpcRunStateManifest Manifest { get; set; } = new SpcRunStateManifest();\n    }\n\n    // =========================================================================\n    // SpcBatch - public entry point and temperature-sweep orchestration\n    // =========================================================================\n\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Dispatches to the graph builder, then runs the shared Potts runner.\n        /// </summary>\n        public static SpcBatchResult Run(SpcBatchRequest request,\n            System.Threading.CancellationToken ct = default)\n        {\n            CsrGraph graph = BuildGraphFromMetric(request, out int n);\n            return RunSimulationCore(graph, n, request,\n                ValidateGraph(graph, n, request.Proximity), ct);\n        }\n\n        private static SpcBatchResult RunSimulationCore(\n            CsrGraph graph,\n            int n,\n            SpcBatchRequest request,\n            List<string> warnings,\n            System.Threading.CancellationToken ct = default)\n        {\n            var result = new SpcBatchResult\n            {\n                Temperatures = (double[])request.Temperatures.Clone(),\n                FinalSpins = new Dictionary<double, int[]>(request.Temperatures.Length),\n                Warnings = warnings.ToArray()\n            };\n\n            result.Graph = graph;\n\n            string runStamp = null;\n            if (!string.IsNullOrEmpty(request.CheckpointDirectory))\n            {\n                runStamp = DateTime.UtcNow.ToString(\"yyyyMMdd_HHmmss\");\n                result.RunDirectory = runStamp;\n                result.StateManifestPath = GetStateManifestPath(request.CheckpointDirectory, runStamp);\n                InitializeRunState(request.CheckpointDirectory, runStamp, request.ResumeDirectory);\n            }\n\n            SpcCheckpoint checkpoint = request.Checkpoint;\n            if (checkpoint == null && !string.IsNullOrEmpty(request.CheckpointDirectory))\n            {\n                string loadStamp = !string.IsNullOrEmpty(request.ResumeDirectory) ? request.ResumeDirectory : runStamp;\n                checkpoint = LoadCheckpoint(request.CheckpointDirectory, loadStamp);\n            }\n\n            var completed = new System.Collections.Concurrent.ConcurrentDictionary<double, SimulationResult>();\n            var options = new ParallelOptions { CancellationToken = ct };\n            int checkpointEpochSweeps = request.CheckpointPersistence == null\n                ? 0\n                : Math.Max(0, request.CheckpointPersistence.EpochSweepCount);\n\n            try\n            {\n                Parallel.For(0, request.Temperatures.Length, options, index =>\n                {\n                    double temperature = request.Temperatures[index];\n\n                    SpcTemperatureState checkpointState = null;\n                    if (checkpoint != null &&\n                        checkpoint.Spins.TryGetValue(temperature, out var checkpointSpins) &&\n                        checkpoint.TemperatureStates.TryGetValue(temperature, out checkpointState) &&\n                        checkpointState.IsComplete)\n                    {\n                        completed[temperature] = new SimulationResult(\n                            temperature,\n                            checkpointSpins,\n                            Math.Max(0, checkpointState.SweepCount),\n                            Math.Max(0, checkpointState.EpochCount),\n                            true);\n                        return;\n                    }\n\n                    int completedSweeps = 0;\n                    int completedEpochs = 0;\n\n                    if (checkpoint != null &&\n                        checkpoint.TemperatureStates.TryGetValue(temperature, out checkpointState))\n                    {\n                        completedSweeps = Math.Max(0, checkpointState.SweepCount);\n                        completedEpochs = Math.Max(0, checkpointState.EpochCount);\n                    }\n\n                    int seed = request.BaseSeed < 0\n                        ? -1\n                        : Math.Abs(request.BaseSeed ^ temperature.GetHashCode()) % int.MaxValue;\n\n                    var model = new PottsModel(n, q: 20, seed: seed);\n\n                    if (checkpoint != null && checkpoint.Spins.TryGetValue(temperature, out var warmSpins))\n                        model.SetSpins(warmSpins);\n\n                    model.LoadGraph(graph);\n\n                    int remainingSteps = Math.Max(0, request.Steps - completedSweeps);\n                    if (remainingSteps == 0)\n                    {\n                        int[] spins = Array.Empty<int>();\n                        if (checkpoint != null && checkpoint.Spins.TryGetValue(temperature, out var currentSpins))\n                            spins = currentSpins;\n                        else if (checkpointState != null)\n                            spins = checkpointState.CurrentSpins;\n\n                        completed[temperature] = new SimulationResult(\n                            temperature,\n                            spins,\n                            completedSweeps,\n                            completedEpochs,\n                            checkpointState != null && checkpointState.IsComplete);\n                        return;\n                    }\n\n                    Action<SimulationEpochResult> checkpointEpoch = null;\n                    if (!string.IsNullOrEmpty(request.CheckpointDirectory) &&\n                        checkpointEpochSweeps > 0 &&\n                        checkpointEpochSweeps < request.Steps)\n                    {\n                        checkpointEpoch = epoch => FlushCheckpoint(\n                            request.CheckpointDirectory,\n                            runStamp,\n                            temperature,\n                            epoch.EpochCount,\n                            epoch.SweepCount,\n                            new SimulationResult(\n                                temperature,\n                                epoch.Spins,\n                                epoch.SweepCount,\n                                epoch.EpochCount,\n                                false),\n                            false,\n                            request.CheckpointPersistence);\n                    }\n\n                    var simulation = model.RunSimulation(\n                        temperature,\n                        remainingSteps,\n                        checkpointEpoch,\n                        checkpointEpochSweeps,\n                        completedSweeps,\n                        completedEpochs,\n                        request.Steps);\n\n                    if (!string.IsNullOrEmpty(request.CheckpointDirectory))\n                        FlushCheckpoint(\n                            request.CheckpointDirectory,\n                            runStamp,\n                            temperature,\n                            simulation.EpochCount,\n                            simulation.SweepCount,\n                            simulation,\n                            true,\n                            request.CheckpointPersistence);\n\n                    completed[temperature] = simulation;\n                });\n            }\n            catch (OperationCanceledException) { }\n\n            for (int index = 0; index < request.Temperatures.Length; index++)\n            {\n                double temperature = request.Temperatures[index];\n                if (completed.TryGetValue(temperature, out var simulation))\n                {\n                    result.FinalSpins[temperature] = simulation.FinalSpins;\n                }\n            }\n\n            return result;\n        }\n    }\n} |
