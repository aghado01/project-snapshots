idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
124 | graphs/distance/other/Mahalanobis.cs | {3289 329 0.3697 4.3578} | 3382 | using System;\nusing System.Buffers;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace Graphs.Distance.Other\n{\n    public static class Mahalanobis\n    {\n\n        private static readonly ThreadLocal<(double[] diff, double[] temp)> _mahalScratch =\n            new(() => (new double[512], new double[512]));\n\n        public static double Distance(double[] a, double[] b, double[,] invCov)\n        {\n            if (a == null || b == null || invCov == null)\n                throw new ArgumentNullException(a == null ? nameof(a) : b == null ? nameof(b) : nameof(invCov));\n            if (a.Length != b.Length)\n                throw new ArgumentException(\"Feature vectors must have the same dimensionality.\");\n            return Distance(a, b, invCov, a.Length);\n        }\n\n        public static double Distance(double[] a, double[] b, double[,] invCov, int dim)\n            => Math.Sqrt(DispatchQuadraticForm(a, b, invCov, dim));\n\n        public static double DistanceSquared(double[] a, double[] b, double[,] invCov)\n        {\n            if (a == null || b == null || invCov == null)\n                throw new ArgumentNullException(a == null ? nameof(a) : b == null ? nameof(b) : nameof(invCov));\n            if (a.Length != b.Length)\n                throw new ArgumentException(\"Feature vectors must have the same dimensionality.\");\n            return DispatchQuadraticForm(a, b, invCov, a.Length);\n        }\n\n        private static double DispatchQuadraticForm(double[] a, double[] b, double[,] invCov, int dim)\n        {\n            if (dim <= 64)\n            {\n                Span<double> diff = stackalloc double[dim];\n                Span<double> temp = stackalloc double[dim];\n                return QuadraticFormCore(a, b, invCov, diff, temp, dim);\n            }\n\n            if (dim <= 512)\n            {\n                var (diffArr, tempArr) = _mahalScratch.Value;\n                return QuadraticFormCore(a, b, invCov,\n                    diffArr.AsSpan(0, dim),\n                    tempArr.AsSpan(0, dim), dim);\n            }\n\n            double[] rentedDiff = ArrayPool<double>.Shared.Rent(dim);\n            double[] rentedTemp = ArrayPool<double>.Shared.Rent(dim);\n            try\n            {\n                return QuadraticFormCore(a, b, invCov,\n                    rentedDiff.AsSpan(0, dim),\n                    rentedTemp.AsSpan(0, dim), dim);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedDiff);\n                ArrayPool<double>.Shared.Return(rentedTemp);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double QuadraticFormCore(\n            double[] a, double[] b, double[,] invCov,\n            Span<double> diff, Span<double> temp, int dim)\n        {\n            for (int i = 0; i < dim; i++)\n                diff[i] = a[i] - b[i];\n\n            for (int i = 0; i < dim; i++)\n            {\n                double s = 0;\n                for (int j = 0; j < dim; j++)\n                    s += invCov[i, j] * diff[j];\n                temp[i] = s;\n            }\n\n            double quad = 0;\n            for (int i = 0; i < dim; i++)\n                quad += diff[i] * temp[i];\n\n            return Math.Max(0, quad);\n        }\n    }\n}
125 | graphs/distance/other/Ncd.cs | {2414 217 0.3219 4.3574} | 2481 | using System;\nusing System.IO;\nusing System.IO.Compression;\n\nnamespace Graphs.Distance.Other\n{\n\n    public enum NcdCompressor { Brotli, Deflate, Gzip, ZLib }\n\n    public static class NormalizedCompressionDistance\n    {\n\n        public static double Distance(\n            byte[] a, byte[] b, NcdCompressor compressor = NcdCompressor.Brotli)\n        {\n            if (a == null) throw new ArgumentNullException(nameof(a));\n            if (b == null) throw new ArgumentNullException(nameof(b));\n\n            int ca = CompressedSize(a, compressor);\n            int cb = CompressedSize(b, compressor);\n            int cab = CompressedSizeJoint(a, b, compressor);\n            return NcdRatio(ca, cb, cab);\n        }\n\n        private static double NcdRatio(int ca, int cb, int cab)\n        {\n            int min = Math.Min(ca, cb);\n            int max = Math.Max(ca, cb);\n            if (max == 0) return 0.0;\n            return (double)(cab - min) / max;\n        }\n\n        private static int CompressedSize(byte[] data, NcdCompressor compressor)\n        {\n            using var output = new MemoryStream();\n            using (Stream stream = CreateCompressor(output, compressor))\n            {\n                stream.Write(data, 0, data.Length);\n            }\n            return (int)output.Length;\n        }\n\n        private static int CompressedSizeJoint(byte[] a, byte[] b, NcdCompressor compressor)\n        {\n            using var output = new MemoryStream();\n            using (Stream stream = CreateCompressor(output, compressor))\n            {\n                stream.Write(a, 0, a.Length);\n                stream.Write(b, 0, b.Length);\n            }\n            return (int)output.Length;\n        }\n\n        private static Stream CreateCompressor(Stream output, NcdCompressor compressor)\n        {\n            return compressor switch\n            {\n                NcdCompressor.Brotli => new BrotliStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                NcdCompressor.Deflate => new DeflateStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                NcdCompressor.Gzip => new GZipStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                NcdCompressor.ZLib => new ZLibStream(output, CompressionLevel.Optimal, leaveOpen: true),\n                _ => throw new NotSupportedException($\"NcdCompressor {compressor} not implemented.\")\n            };\n        }\n    }\n}
126 | graphs/GraphBuilder.cs | {8327 645 0.3569 4.3120} | 8547 | using System;\nusing System.Collections.Generic;\nusing Graphs.Coupling;\nusing Graphs.Distance;\nusing Graphs.Primitives;\nusing Graphs.Proximity;\n\nnamespace Graphs\n{\n\n    public enum ProximityRule { Knn, MutualKnn, EpsilonBall }\n\n    public enum KernelType { Gaussian, Cauchy, Laplacian, Linear }\n\n    public static class GraphBuilder\n    {\n\n        public static GraphBuildResult BuildResult(\n            int n,\n            Func<int, int, double> dist,\n            ProximityRule rule = ProximityRule.Knn,\n            int k = 10,\n            double epsilon = 0.0,\n            KernelType kernel = KernelType.Gaussian,\n            double bandwidth = 0.0,\n            bool ensureConnected = false,\n            BandwidthStrategy bandwidthStrategy = BandwidthStrategy.MadConsistencyFactor)\n        {\n\n            NeighborSelection directed = SelectNeighbors(n, dist, rule, k, epsilon);\n\n            double delta = bandwidth > 0.0\n                ? bandwidth\n                : EstimateBandwidth(kernel, directed.NearestNeighborDistances, bandwidthStrategy);\n\n            CsrGraph baseGraph = ToGraph(directed, kernel, delta);\n\n            CsrGraph graph;\n            CsrGraph? preRepair;\n            if (ensureConnected)\n            {\n                NeighborSelection repaired = ProximityGraph.EnsureConnected(directed, n, dist);\n                graph = ToGraph(repaired, kernel, delta);\n                preRepair = baseGraph;\n            }\n            else\n            {\n                graph = baseGraph;\n                preRepair = null;\n            }\n\n            return new GraphBuildResult(\n                Graph:                  graph,\n                DirectedSelection:      directed,\n                SingleBandwidth:        delta,\n                MixtureBandwidth:       null,\n                EnsureConnectedApplied: ensureConnected,\n                PreRepairGraph:         preRepair);\n        }\n\n        public static GraphBuildResult BuildWithMixtureResult(\n            int n,\n            Func<int, int, double> dist,\n            MixtureWeights weights,\n            ProximityRule rule = ProximityRule.Knn,\n            int k = 10,\n            double epsilon = 0.0,\n            MixtureBandwidth? bandwidth = null,\n            bool ensureConnected = false,\n            BandwidthStrategy bandwidthStrategy = BandwidthStrategy.MadConsistencyFactor)\n        {\n            NeighborSelection directed = SelectNeighbors(n, dist, rule, k, epsilon);\n\n            MixtureBandwidth resolvedBandwidth = bandwidth\n                ?? EstimateMixtureBandwidth(directed.NearestNeighborDistances, bandwidthStrategy);\n\n            CsrGraph baseGraph = ToGraphMixture(directed, resolvedBandwidth, weights);\n\n            CsrGraph graph;\n            CsrGraph? preRepair;\n            if (ensureConnected)\n            {\n                NeighborSelection repaired = ProximityGraph.EnsureConnected(directed, n, dist);\n                graph = ToGraphMixture(repaired, resolvedBandwidth, weights);\n                preRepair = baseGraph;\n            }\n            else\n            {\n                graph = baseGraph;\n                preRepair = null;\n            }\n\n            return new GraphBuildResult(\n                Graph:                  graph,\n                DirectedSelection:      directed,\n                SingleBandwidth:        null,\n                MixtureBandwidth:       resolvedBandwidth,\n                EnsureConnectedApplied: ensureConnected,\n                PreRepairGraph:         preRepair);\n        }\n\n        public static CsrGraph ToGraph(\n            NeighborSelection sel,\n            KernelType kernel = KernelType.Gaussian,\n            double bandwidth = 0.0)\n        {\n            Neighbor[][] allNeighbors = sel.AllNeighbors;\n            double[] nnDistances = sel.NearestNeighborDistances;\n            int n = allNeighbors.Length;\n\n            double delta = bandwidth > 0.0\n                ? bandwidth\n                : EstimateBandwidth(kernel, nnDistances);\n\n            Func<double, double, double> kernelFn = KernelFunction(kernel);\n\n            var edges = new List<Edge>(n * (allNeighbors.Length > 0 ? allNeighbors[0].Length : 0));\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in allNeighbors[i])\n                {\n                    if (i > nb.Index) continue;   // store each undirected edge once\n\n                    double coupling = kernelFn(nb.Distance, delta);\n                    if (coupling > 1e-10)\n                        edges.Add(new Edge(i, nb.Index, coupling));\n                }\n            }\n\n            return CsrGraph.FromEdges(edges.ToArray(), n);\n        }\n\n        public static CsrGraph ToGraphMixture(\n            NeighborSelection sel,\n            MixtureBandwidth bandwidth,\n            MixtureWeights weights)\n        {\n            Neighbor[][] allNeighbors = sel.AllNeighbors;\n            int n = allNeighbors.Length;\n\n            var edges = new List<Edge>(n * (allNeighbors.Length > 0 ? allNeighbors[0].Length : 0));\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in allNeighbors[i])\n                {\n                    if (i > nb.Index) continue;\n\n                    double coupling = Mixture.Evaluate(nb.Distance, bandwidth, weights);\n                    if (coupling > 1e-10)\n                        edges.Add(new Edge(i, nb.Index, coupling));\n                }\n            }\n\n            return CsrGraph.FromEdges(edges.ToArray(), n);\n        }\n\n        private static NeighborSelection SelectNeighbors(\n            int n, Func<int, int, double> dist,\n            ProximityRule rule, int k, double epsilon)\n        {\n            return rule switch\n            {\n                ProximityRule.Knn => ProximityGraph.SelectKnn(n, k, dist),\n                ProximityRule.MutualKnn => ProximityGraph.SelectMutualKnn(n, k, dist),\n                ProximityRule.EpsilonBall => ProximityGraph.SelectEpsilonBall(n, epsilon, dist),\n\n                _ => throw new NotSupportedException($\"ProximityRule {rule} not implemented.\")\n            };\n        }\n\n        private static double EstimateBandwidth(\n            KernelType kernel, double[] nnDistances,\n            BandwidthStrategy strategy = BandwidthStrategy.MadConsistencyFactor)\n        {\n            ReadOnlySpan<double> span = nnDistances;\n            Span<double> scratch = stackalloc double[nnDistances.Length <= 512 ? nnDistances.Length : 0];\n            double[]? scratchArr = scratch.Length == 0 ? new double[nnDistances.Length] : null;\n            Span<double> effectiveScratch = scratch.Length > 0 ? scratch : scratchArr!.AsSpan();\n\n            return kernel switch\n            {\n                KernelType.Gaussian  => BandwidthEstimation.ForGaussian(span, effectiveScratch, strategy),\n                KernelType.Laplacian => BandwidthEstimation.ForLaplacian(span, effectiveScratch, strategy),\n                KernelType.Cauchy    => BandwidthEstimation.ForCauchy(span, effectiveScratch, strategy),\n                KernelType.Linear    => BandwidthEstimation.ForLinear(span),\n                _ => throw new NotSupportedException($\"KernelType {kernel} not implemented.\")\n            };\n        }\n\n        private static MixtureBandwidth EstimateMixtureBandwidth(\n            double[] nnDistances,\n            BandwidthStrategy strategy = BandwidthStrategy.MadConsistencyFactor)\n        {\n            ReadOnlySpan<double> span = nnDistances;\n            Span<double> scratch = stackalloc double[nnDistances.Length <= 512 ? nnDistances.Length : 0];\n            double[]? scratchArr = scratch.Length == 0 ? new double[nnDistances.Length] : null;\n            Span<double> effectiveScratch = scratch.Length > 0 ? scratch : scratchArr!.AsSpan();\n            return BandwidthEstimation.ForMixture(span, effectiveScratch, strategy);\n        }\n\n        private static Func<double, double, double> KernelFunction(KernelType kernel)\n        {\n            return kernel switch\n            {\n                KernelType.Gaussian => Gaussian.Evaluate,\n                KernelType.Cauchy => Cauchy.Evaluate,\n                KernelType.Laplacian => Laplacian.Evaluate,\n                KernelType.Linear => Linear.Evaluate,\n                _ => throw new NotSupportedException($\"KernelType {kernel} not implemented.\")\n            };\n        }\n    }\n}
127 | graphs/GraphBuildResult.cs | {605 43 0.3455 4.2000} | 625 | using Graphs.Coupling;\nusing Graphs.Distance;\nusing Graphs.Primitives;\n\nnamespace Graphs\n{\n\n    public sealed record GraphBuildResult(\n        CsrGraph              Graph,\n        NeighborSelection     DirectedSelection,\n        double?               SingleBandwidth,\n        MixtureBandwidth?     MixtureBandwidth,\n        bool                  EnsureConnectedApplied,\n        CsrGraph?             PreRepairGraph = null,\n        MetricProperties?     Metric         = null)\n    {\n\n        public double RepresentativeBandwidth =>\n            SingleBandwidth ?? MixtureBandwidth?.Gaussian ?? 0.0;\n    }\n}
128 | graphs/GraphSelection.cs | {2887 281 0.3765 4.2449} | 2983 | using System;\n\nnamespace Graphs\n{\n    public readonly struct NeighborSelection\n    {\n        public NeighborSelection(Neighbor[][] allNeighbors, double[] nearestNeighborDistances)\n        {\n            AllNeighbors = allNeighbors ?? throw new ArgumentNullException(nameof(allNeighbors));\n            NearestNeighborDistances = nearestNeighborDistances ?? throw new ArgumentNullException(nameof(nearestNeighborDistances));\n        }\n\n        public Neighbor[][] AllNeighbors { get; }\n\n        public double[] NearestNeighborDistances { get; }\n\n        public void Deconstruct(out Neighbor[][] allNeighbors, out double[] nearestNeighborDistances)\n        {\n            allNeighbors = AllNeighbors;\n            nearestNeighborDistances = NearestNeighborDistances;\n        }\n    }\n\n    public struct Neighbor : IComparable<Neighbor>\n    {\n        public int Index;\n        public double Distance;\n\n        public int CompareTo(Neighbor other) => other.Distance.CompareTo(Distance);\n    }\n\n    internal sealed class BoundedMinHeap\n    {\n        private readonly Neighbor[] _heap;\n        private int _count;\n        private readonly int _capacity;\n\n        public BoundedMinHeap(int k)\n        {\n            if (k <= 0)\n                throw new ArgumentOutOfRangeException(nameof(k), \"K must be positive.\");\n\n            _capacity = k;\n            _heap = new Neighbor[k];\n        }\n\n        public void TryAdd(int index, double distance)\n        {\n            if (_count < _capacity)\n            {\n                _heap[_count] = new Neighbor { Index = index, Distance = distance };\n                _count++;\n                if (_count == _capacity) BuildHeap();\n            }\n            else if (distance < _heap[0].Distance)\n            {\n                _heap[0] = new Neighbor { Index = index, Distance = distance };\n                SiftDown(0);\n            }\n        }\n\n        public Neighbor[] GetSorted()\n        {\n            var result = new Neighbor[_count];\n            Array.Copy(_heap, result, _count);\n            Array.Sort(result, (a, b) => a.Distance.CompareTo(b.Distance));\n            return result;\n        }\n\n        private void BuildHeap()\n        {\n            for (int i = _count / 2 - 1; i >= 0; i--)\n                SiftDown(i);\n        }\n\n        private void SiftDown(int i)\n        {\n            while (true)\n            {\n                int largest = i;\n                int left = 2 * i + 1;\n                int right = 2 * i + 2;\n\n                if (left < _count && _heap[left].Distance > _heap[largest].Distance) largest = left;\n                if (right < _count && _heap[right].Distance > _heap[largest].Distance) largest = right;\n                if (largest == i) break;\n\n                var tmp = _heap[i];\n                _heap[i] = _heap[largest];\n                _heap[largest] = tmp;\n                i = largest;\n            }\n        }\n    }\n}
129 | graphs/primitives/BfsShells.cs | {4509 448 0.4431 4.0012} | 4657 | using System;\nusing System.Collections.Generic;\n\nnamespace Graphs.Primitives\n\n{\n\n    public sealed class BfsShellsResult\n    {\n\n        public int SeedNode { get; }\n\n        public int[] Distances { get; }\n\n        public int[][] Shells { get; }\n\n        public int MaxDepth { get; }\n\n        public int ReachableNodeCount { get; }\n\n        internal BfsShellsResult(int seedNode, int[] distances, int[][] shells, int reachableNodeCount)\n        {\n            SeedNode = seedNode;\n            Distances = distances;\n            Shells = shells;\n            MaxDepth = shells.Length - 1;\n            ReachableNodeCount = reachableNodeCount;\n        }\n    }\n\n    public static class BfsShells\n    {\n\n        public static BfsShellsResult Compute(CsrGraph graph, int seedNode, int? maxDepth = null)\n        {\n            int n = graph.NodeCount;\n            if (n == 0)\n                throw new ArgumentException(\"Graph is empty.\", nameof(graph));\n            if (seedNode < 0 || seedNode >= n)\n                throw new ArgumentOutOfRangeException(\n                    nameof(seedNode),\n                    $\"seedNode {seedNode} out of range for graph of size {n}.\");\n            if (maxDepth.HasValue && maxDepth.Value < 0)\n                throw new ArgumentOutOfRangeException(nameof(maxDepth), \"maxDepth must be >= 0 or null.\");\n\n            var distances = new int[n];\n            for (int i = 0; i < n; i++) distances[i] = -1;\n            distances[seedNode] = 0;\n\n            var shells = new List<int[]>(capacity: 8) { new[] { seedNode } };\n            int reachable = 1;\n\n            var current = new List<int>(8) { seedNode };\n            var next = new List<int>(16);\n            int depth = 0;\n\n            while (current.Count > 0)\n            {\n                if (maxDepth.HasValue && depth >= maxDepth.Value) break;\n\n                next.Clear();\n                foreach (int u in current)\n                {\n                    int rowStart = graph.RowPointers[u];\n                    int rowEnd = graph.RowPointers[u + 1];\n                    for (int e = rowStart; e < rowEnd; e++)\n                    {\n                        int v = graph.Targets[e];\n                        if (distances[v] == -1)\n                        {\n                            distances[v] = depth + 1;\n                            next.Add(v);\n                        }\n                    }\n                }\n\n                if (next.Count == 0) break;\n\n                shells.Add(next.ToArray());\n                reachable += next.Count;\n\n                (current, next) = (next, current);\n                depth++;\n            }\n\n            return new BfsShellsResult(seedNode, distances, shells.ToArray(), reachable);\n        }\n\n        public static int[] ComputeDistances(CsrGraph graph, int seedNode, int? maxDepth = null)\n        {\n            int n = graph.NodeCount;\n            if (n == 0)\n                throw new ArgumentException(\"Graph is empty.\", nameof(graph));\n            if (seedNode < 0 || seedNode >= n)\n                throw new ArgumentOutOfRangeException(\n                    nameof(seedNode),\n                    $\"seedNode {seedNode} out of range for graph of size {n}.\");\n            if (maxDepth.HasValue && maxDepth.Value < 0)\n                throw new ArgumentOutOfRangeException(nameof(maxDepth), \"maxDepth must be >= 0 or null.\");\n\n            var distances = new int[n];\n            for (int i = 0; i < n; i++) distances[i] = -1;\n            distances[seedNode] = 0;\n\n            var current = new List<int>(8) { seedNode };\n            var next = new List<int>(16);\n            int depth = 0;\n\n            while (current.Count > 0)\n            {\n                if (maxDepth.HasValue && depth >= maxDepth.Value) break;\n\n                next.Clear();\n                foreach (int u in current)\n                {\n                    int rowStart = graph.RowPointers[u];\n                    int rowEnd = graph.RowPointers[u + 1];\n                    for (int e = rowStart; e < rowEnd; e++)\n                    {\n                        int v = graph.Targets[e];\n                        if (distances[v] == -1)\n                        {\n                            distances[v] = depth + 1;\n                            next.Add(v);\n                        }\n                    }\n                }\n\n                if (next.Count == 0) break;\n                (current, next) = (next, current);\n                depth++;\n            }\n\n            return distances;\n        }\n    }\n}
130 | graphs/primitives/CsrGraph.cs | {4953 479 0.4450 3.9176} | 5111 | using System;\n\nnamespace Graphs.Primitives\n{\n\n    public struct Edge\n    {\n        public int Source;\n        public int Target;\n        public double J;\n\n        public Edge(int source, int target, double coupling)\n        {\n            Source = source;\n            Target = target;\n            J = coupling;\n        }\n    }\n\n    public struct CsrGraph\n    {\n        public int[] Targets;\n        public double[] Weights;\n        public int[] RowPointers;\n        public int NodeCount;\n\n        public static CsrGraph FromEdges(Edge[] edges, int nodeCount)\n        {\n            int[] degree = new int[nodeCount];\n            for (int e = 0; e < edges.Length; e++)\n            {\n                degree[edges[e].Source]++;\n                degree[edges[e].Target]++;\n            }\n\n            int[] rowPointers = new int[nodeCount + 1];\n            for (int node = 0; node < nodeCount; node++)\n                rowPointers[node + 1] = rowPointers[node] + degree[node];\n\n            int totalEntries = rowPointers[nodeCount];\n            int[] targets = new int[totalEntries];\n            double[] weights = new double[totalEntries];\n\n            int[] cursor = new int[nodeCount];\n            Array.Copy(rowPointers, cursor, nodeCount);\n\n            for (int e = 0; e < edges.Length; e++)\n            {\n                int src = edges[e].Source;\n                int tgt = edges[e].Target;\n                double w = edges[e].J;\n\n                targets[cursor[src]] = tgt;\n                weights[cursor[src]] = w;\n                cursor[src]++;\n\n                targets[cursor[tgt]] = src;\n                weights[cursor[tgt]] = w;\n                cursor[tgt]++;\n            }\n\n            return new CsrGraph\n            {\n                Targets = targets,\n                Weights = weights,\n                RowPointers = rowPointers,\n                NodeCount = nodeCount\n            };\n        }\n\n        public int Degree(int node) => RowPointers[node + 1] - RowPointers[node];\n\n        public double WeightedDegree(int node)\n        {\n            double sum = 0.0;\n            int start = RowPointers[node];\n            int end   = RowPointers[node + 1];\n            for (int e = start; e < end; e++) sum += Weights[e];\n            return sum;\n        }\n\n        public CsrGraph InducedSubgraph(\n            bool[] nodeMask,\n            out int[] newToOld,\n            out int[] oldToNew)\n        {\n            if (nodeMask is null)\n                throw new ArgumentNullException(nameof(nodeMask));\n            if (nodeMask.Length != NodeCount)\n                throw new ArgumentException(\n                    $\"nodeMask.Length ({nodeMask.Length}) must equal NodeCount ({NodeCount}).\",\n                    nameof(nodeMask));\n\n            oldToNew = new int[NodeCount];\n            int subN = 0;\n            for (int v = 0; v < NodeCount; v++)\n                oldToNew[v] = nodeMask[v] ? subN++ : -1;\n\n            newToOld = new int[subN];\n            for (int v = 0; v < NodeCount; v++)\n                if (nodeMask[v]) newToOld[oldToNew[v]] = v;\n\n            if (subN == 0)\n            {\n                return new CsrGraph\n                {\n                    Targets     = Array.Empty<int>(),\n                    Weights     = Array.Empty<double>(),\n                    RowPointers = new int[1],\n                    NodeCount   = 0,\n                };\n            }\n\n            int[] subDegree = new int[subN];\n            for (int u = 0; u < NodeCount; u++)\n            {\n                if (!nodeMask[u]) continue;\n                int newU = oldToNew[u];\n                int rowEnd = RowPointers[u + 1];\n                for (int e = RowPointers[u]; e < rowEnd; e++)\n                    if (nodeMask[Targets[e]]) subDegree[newU]++;\n            }\n\n            int[] subRowPointers = new int[subN + 1];\n            for (int i = 0; i < subN; i++)\n                subRowPointers[i + 1] = subRowPointers[i] + subDegree[i];\n\n            int totalEntries = subRowPointers[subN];\n            int[] subTargets  = new int[totalEntries];\n            double[] subWeights = new double[totalEntries];\n\n            int[] cursor = new int[subN];\n            for (int u = 0; u < NodeCount; u++)\n            {\n                if (!nodeMask[u]) continue;\n                int newU = oldToNew[u];\n                int rowEnd = RowPointers[u + 1];\n                for (int e = RowPointers[u]; e < rowEnd; e++)\n                {\n                    int v = Targets[e];\n                    if (!nodeMask[v]) continue;\n                    int pos = subRowPointers[newU] + cursor[newU]++;\n                    subTargets[pos] = oldToNew[v];\n                    subWeights[pos] = Weights[e];\n                }\n            }\n\n            return new CsrGraph\n            {\n                Targets     = subTargets,\n                Weights     = subWeights,\n                RowPointers = subRowPointers,\n                NodeCount   = subN,\n            };\n        }\n    }\n}
131 | graphs/primitives/Statistics.cs | {1800 243 0.4894 3.7975} | 1868 | using System;\n\nnamespace Graphs.Primitives\n{\n\n    public static class Statistics\n    {\n\n        public static double MedianOfSorted(ReadOnlySpan<double> sorted)\n        {\n            int n = sorted.Length;\n            if (n == 0) return 0.0;\n            int mid = n / 2;\n            return (n & 1) == 0\n                ? (sorted[mid - 1] + sorted[mid]) / 2.0\n                : sorted[mid];\n        }\n\n        public static double Skewness(ReadOnlySpan<double> values)\n        {\n            int n = values.Length;\n            if (n < 2) return 0.0;\n\n            double mean = 0.0;\n            for (int i = 0; i < n; i++) mean += values[i];\n            mean /= n;\n\n            double m2 = 0.0, m3 = 0.0;\n            for (int i = 0; i < n; i++)\n            {\n                double d = values[i] - mean;\n                double d2 = d * d;\n                m2 += d2;\n                m3 += d2 * d;\n            }\n            m2 /= n;\n            m3 /= n;\n\n            const double VarianceFloor = 1e-12;\n            if (m2 < VarianceFloor) return 0.0;\n            return m3 / Math.Pow(m2, 1.5);\n        }\n\n        public static double Skewness(ReadOnlySpan<int> values)\n        {\n            int n = values.Length;\n            if (n < 2) return 0.0;\n\n            double mean = 0.0;\n            for (int i = 0; i < n; i++) mean += values[i];\n            mean /= n;\n\n            double m2 = 0.0, m3 = 0.0;\n            for (int i = 0; i < n; i++)\n            {\n                double d = values[i] - mean;\n                double d2 = d * d;\n                m2 += d2;\n                m3 += d2 * d;\n            }\n            m2 /= n;\n            m3 /= n;\n\n            const double VarianceFloor = 1e-12;\n            if (m2 < VarianceFloor) return 0.0;\n            return m3 / Math.Pow(m2, 1.5);\n        }\n    }\n}
