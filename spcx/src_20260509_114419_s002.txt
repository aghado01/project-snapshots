idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
10 | estimators/ScatterAccumulator.cs | {2300 205 0.3887 4.0893} | 2363 | // ============================================================================\n// Estimators/Shared/ScatterAccumulator.cs   (internal primitive)\n// ============================================================================\n// Single implementation of the weighted outer-product scatter accumulation\n// used by both KarcherScatter and WeiszfeldScatter.\n//\n// Sigma = (consistencyFactor / sum_i w_i) * sum_i w_i * v_i * v_i^T\n// where v_i = log_mu(x_i).\n//\n// scatterDst is a caller-supplied row-major D×D flat buffer (length D²).\n// ============================================================================\nusing System;\nusing System.Buffers;\nusing Manifolds;\n\nnamespace Estimators.Shared\n{\n    internal static class ScatterAccumulator\n    {\n        public static void Accumulate<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            double[]               location,\n            ReadOnlySpan<double>   weights,\n            Span<double>           scatterDst,\n            double                 consistencyFactor)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int      dim    = manifold.Dimension;\n            double[] logArr = ArrayPool<double>.Shared.Rent(dim);\n            try\n            {\n                Span<double>         v       = logArr.AsSpan(0, dim);\n                ReadOnlySpan<double> locSpan = location;\n                scatterDst.Clear();\n                double sumW = 0;\n\n                for (int i = 0; i < data.Length; i++)\n                {\n                    double w = weights[i];\n                    if (w == 0) continue;\n                    sumW += w;\n                    manifold.LogMap(locSpan, data[i], v);\n                    for (int r = 0; r < dim; r++)\n                        for (int c = 0; c < dim; c++)\n                            scatterDst[r * dim + c] += w * v[r] * v[c];\n                }\n\n                if (sumW > 0)\n                {\n                    double scale = consistencyFactor / sumW;\n                    for (int k = 0; k < scatterDst.Length; k++)\n                        scatterDst[k] *= scale;\n                }\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(logArr);\n            }\n        }\n    }\n} |
11 | estimators/WeiszfeldScatter.cs | {3328 293 0.3317 4.3495} | 3417 | // ============================================================================\n// Estimators/Median/WeiszfeldScatter.cs\n// ============================================================================\n// Robust scatter companion to GeometricMedian.\n// Weights are the converged L1 IRLS weights w_i = 1 / d(mu, x_i), so points\n// far from the median contribute less to the scatter estimate — consistent with\n// the median's breakdown-point guarantees.\n//\n// Sigma = (cD / sum_i w_i) * sum_i w_i * v_i * v_i^T\n// where v_i = log_mu(x_i) and w_i are the L1 IRLS weights at convergence.\n//\n// Can also be called standalone with a fixed location estimate.\n// ============================================================================\nusing System;\nusing System.Buffers;\nusing Manifolds;\nusing Losses;\nusing Estimators.Shared;\n\nnamespace Estimators.Median\n{\n    public static class WeiszfeldScatter\n    {\n        /// <summary>\n        /// Accumulates scatter from converged IRLS weights into\n        /// <paramref name=\"scatterDst\"/> (row-major D×D, length D²).\n        /// Called internally by <see cref=\"GeometricMedian.ComputeWithScatter\"/>.\n        /// </summary>\n        internal static void Accumulate<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            double[]               location,\n            ReadOnlySpan<double>   finalIrlsWeights,\n            Span<double>           scatterDst,\n            double                 consistencyFactor)\n            where TManifold : struct, IRiemannianManifold\n            => ScatterAccumulator.Accumulate(\n                manifold, data, location,\n                finalIrlsWeights, scatterDst, consistencyFactor);\n\n        /// <summary>\n        /// Standalone scatter: computes robust Weiszfeld scatter around a fixed\n        /// <paramref name=\"location\"/> without re-running median estimation.\n        /// Runs one pass computing L1 weights from distances, then accumulates.\n        /// </summary>\n        public static void Compute<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            double[]               location,\n            Span<double>           scatterDestination,\n            ReadOnlySpan<double>   outerWeights = default,\n            double                 epsilon = 1e-10,\n            double                 consistencyFactor = 1.0)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int      n       = data.Length;\n            double[] rentedW = ArrayPool<double>.Shared.Rent(n);\n            try\n            {\n                Span<double>         w       = rentedW.AsSpan(0, n);\n                ReadOnlySpan<double> locSpan = location;\n\n                for (int i = 0; i < n; i++)\n                {\n                    double r  = manifold.Distance(locSpan, data[i]);\n                    double ow = outerWeights.IsEmpty ? 1.0 : outerWeights[i];\n                    w[i] = r > epsilon ? ow * L1Loss.Weight(r) : 0.0;\n                }\n\n                ScatterAccumulator.Accumulate(\n                    manifold, data, location, w,\n                    scatterDestination, consistencyFactor);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedW);\n            }\n        }\n    }\n} |
12 | gmm/GaussianComponent.cs | {7102 740 0.2747 4.6827} | 8003 | using System;\nusing DistanceMetrics;\nusing LinearAlgebra;\n\nnamespace StatisticalEstimators\n{\n    /// <summary>\n    /// A single Gaussian component: weight π_k, mean μ_k, covariance Σ_k, and the\n    /// derived quantities needed for efficient E-Step evaluation.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// <strong>Zero-allocation M-Step.</strong>\n    /// Pre-allocated scratchpads (<see cref=\"ScratchMean\"/>, <see cref=\"ScratchCov\"/>)\n    /// are accumulated in place during the M-Step and committed via\n    /// <see cref=\"CommitScratch\"/>. No heap allocations occur during the EM loop.\n    /// </para>\n    /// <para>\n    /// <strong>Cholesky preservation.</strong>\n    /// The lower-triangular factor L (Σ = LLᵀ) is retained in a dedicated field after\n    /// <see cref=\"UpdateCache\"/> completes. Future sampling support can be added as\n    /// x = μ + L·z, z ~ N(0,I) without recomputing the decomposition.\n    /// </para>\n    /// </remarks>\n    public sealed class GaussianComponent\n    {\n        private readonly int _dim;\n\n        // ── Parameters ──────────────────────────────────────────────────────────\n\n        /// <summary>Mixture weight π_k.</summary>\n        public double Weight { get; set; }\n\n        /// <summary>Mean vector μ_k. Write elements in place; do not replace the array reference.</summary>\n        public double[] Mean { get; }\n\n        /// <summary>Covariance Σ_k. Write elements in place; do not replace the array reference.</summary>\n        public double[,] Covariance { get; }\n\n        // ── Cached quantities (updated by UpdateCache) ───────────────────────────\n\n        /// <summary>Σ⁻¹ — used in Mahalanobis distance computation during the E-Step.</summary>\n        public double[,] CovarianceInverse { get; }\n\n        /// <summary>\n        /// −½(D·ln(2π) + ln|Σ|). Cached to avoid recomputing the log-determinant on every\n        /// PDF evaluation. Set by <see cref=\"UpdateCache\"/>.\n        /// </summary>\n        public double LogNormalizationFactor { get; private set; }\n\n        // ── M-Step scratchpads (pre-allocated; internal to GaussianMixtureModel) ─\n\n        internal double[] ScratchMean { get; }\n        internal double[,] ScratchCov { get; }\n\n        // ── Cholesky work arrays ─────────────────────────────────────────────────\n\n        private readonly CholeskyDecomposition _chol;\n\n        public GaussianComponent(int dim)\n        {\n            _dim = dim;\n            Mean = new double[dim];\n            Covariance = new double[dim, dim];\n            CovarianceInverse = new double[dim, dim];\n            ScratchMean = new double[dim];\n            ScratchCov = new double[dim, dim];\n            _chol = new CholeskyDecomposition(dim);\n        }\n\n        // ── M-Step helpers ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Zeroes both scratchpad arrays. Call once per component per EM iteration\n        /// before accumulating weighted observations.\n        /// </summary>\n        internal void ResetScratch()\n        {\n            Array.Clear(ScratchMean, 0, ScratchMean.Length);\n            Array.Clear(ScratchCov, 0, ScratchCov.Length);\n        }\n\n        /// <summary>\n        /// Copies the normalised <see cref=\"ScratchMean\"/> into <see cref=\"Mean\"/>,\n        /// normalises <see cref=\"ScratchCov\"/> by <paramref name=\"effectiveCount\"/> into\n        /// <see cref=\"Covariance\"/> (with diagonal regularisation), updates\n        /// <see cref=\"Weight\"/>, and calls <see cref=\"UpdateCache\"/>.\n        /// </summary>\n        /// <param name=\"effectiveCount\">\n        /// Σ_i γ(z_{ik}) — the soft cluster population. <see cref=\"ScratchMean\"/> must\n        /// already be divided by this value before calling (done by the M-Step caller).\n        /// </param>\n        /// <param name=\"n\">Total data count N (used to compute the new weight).</param>\n        /// <param name=\"regularization\">Ridge term added to the diagonal of Σ to prevent singularity.</param>\n        internal void CommitScratch(double effectiveCount, int n, double regularization = 1e-6)\n        {\n            Weight = effectiveCount / n;\n\n            if (effectiveCount > 1e-12)\n            {\n                // ScratchMean has already been normalised by the caller; copy directly.\n                Array.Copy(ScratchMean, Mean, _dim);\n\n                double invCount = 1.0 / effectiveCount;\n                for (int d1 = 0; d1 < _dim; d1++)\n                    for (int d2 = 0; d2 < _dim; d2++)\n                    {\n                        Covariance[d1, d2] = ScratchCov[d1, d2] * invCount\n                                             + (d1 == d2 ? regularization : 0.0);\n                    }\n            }\n            // Dead cluster: leave Mean/Covariance at prior values.\n            // Weight ≈ 0 suppresses this component's contribution in future E-Steps.\n\n            UpdateCache();\n        }\n\n        // ── Cache update ─────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Decomposes <see cref=\"Covariance\"/> via <see cref=\"CholeskyDecomposition\"/>,\n        /// writes Σ⁻¹ into <see cref=\"CovarianceInverse\"/>, and caches\n        /// <see cref=\"LogNormalizationFactor\"/>. Call after writing new values directly\n        /// into <see cref=\"Covariance\"/> from external initialisation or the M-Step.\n        /// </summary>\n        public void UpdateCache()\n        {\n            _chol.Decompose(Covariance);\n            _chol.WriteInverseTo(CovarianceInverse);\n            LogNormalizationFactor = -0.5 * (_dim * Math.Log(2.0 * Math.PI) + _chol.LogDet);\n        }\n\n        // ── PDF evaluation ───────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Evaluates ln N(x | μ, Σ). Numerically safe in all dimensions; use in the E-Step.\n        /// </summary>\n        public double EvaluateLogPdf(double[] x) =>\n            LogNormalizationFactor - 0.5 * ComputeMahalanobisSquared(x);\n\n        /// <summary>\n        /// Evaluates N(x | μ, Σ) directly. May underflow to 0 in high dimensions;\n        /// prefer <see cref=\"EvaluateLogPdf\"/> for all EM arithmetic.\n        /// </summary>\n        public double EvaluatePdf(double[] x) =>\n            Math.Exp(EvaluateLogPdf(x));\n\n        /// <summary>\n        /// Returns the squared Mahalanobis distance from <paramref name=\"x\"/> to this component's mean.\n        /// D² = (x−μ)ᵀ Σ⁻¹ (x−μ). Exposed for use by <see cref=\"GaussianMixtureModel.Mahal\"/>.\n        /// Delegates to <see cref=\"Mahalanobis.DistanceSquared\"/> in <c>DistanceMetrics</c>.\n        /// </summary>\n        public double MahalanobisSquared(double[] x)\n            => Mahalanobis.DistanceSquared(x, Mean, CovarianceInverse);\n\n        /// <summary>\n        /// Draws a single sample via the <see cref=\"CholeskyDecomposition\"/> preserved\n        /// from the last <see cref=\"UpdateCache\"/> call.\n        /// </summary>\n        public double[] Sample(Random rng) => _chol.Sample(rng, Mean);\n\n        private double ComputeMahalanobisSquared(double[] x)\n            => Mahalanobis.DistanceSquared(x, Mean, CovarianceInverse);\n    }\n} |
