idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
12 | clustering/spc/AdaptiveScheduler.cs | {18486 2102 0.2743 4.7262} | 20016 | using System;\nusing System.Collections.Generic;\nusing System.Diagnostics;\nusing System.Linq;\nusing System.Threading.Tasks;\nusing Clustering.SPC.Heuristics;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC;\n\n/// <summary>\n/// Configuration for <see cref=\"AdaptiveScheduler.Run\"/>. The defaults are\n/// tuned for typical MAPPER-SPC patches (50-150 nodes per induced subgraph)\n/// and produce a sharp χ(T) peak in roughly a second on a single core; the\n/// flat-task parallel pattern collapses that to well under a second on\n/// modern multicore.\n/// </summary>\npublic sealed class AdaptiveSchedulerConfig\n{\n    // ── Stage 1: coarse log-spaced probe ──────────────────────────────────\n    public int    CoarseSteps        { get; init; } = 10;\n    public int    CoarseBurn         { get; init; } = 50;\n    public int    CoarseCycles       { get; init; } = 100;\n\n    // ── Stage 2: dense band refinement with multi-replica averaging ──────\n    public int    DenseSteps         { get; init; } = 12;\n    public int    DenseBurn          { get; init; } = 200;\n    public int    DenseCycles        { get; init; } = 400;\n\n    /// <summary>\n    /// Independent replicas per (Temperature, Round) task. Each replica is its\n    /// own fresh PottsModel with a deterministic per-task seed (derived from\n    /// <see cref=\"BaseSeed\"/>); their χ measurements are averaged to drive\n    /// down variance at each grid point. Set together with\n    /// <see cref=\"DenseRounds\"/> for total measurements per T =\n    /// <c>DenseReplicas × DenseRounds</c>.\n    /// </summary>\n    public int    DenseReplicas      { get; init; } = 3;\n\n    /// <summary>\n    /// Number of refinement rounds. Each round runs DenseReplicas measurements\n    /// at every grid point and merges into a running average. Multiple rounds\n    /// let the χ profile sharpen further than DenseReplicas alone.\n    /// </summary>\n    public int    DenseRounds        { get; init; } = 3;\n\n    // ── Stage 3: final equilibrium at chosen T ───────────────────────────\n    public int    FinalBurn          { get; init; } = 1000;\n    public int    FinalCycles        { get; init; } = 5000;\n\n    // ── Schedule envelope ─────────────────────────────────────────────────\n    /// <summary>\n    /// Lower bound of the coarse temperature grid. Null derives a graph-aware\n    /// value from CriticalTemperatureEstimator.\n    /// </summary>\n    public double? TempMin           { get; init; } = null;\n    /// <summary>\n    /// Upper bound of the coarse temperature grid. Null derives a graph-aware\n    /// value from CriticalTemperatureEstimator.\n    /// </summary>\n    public double? TempMax           { get; init; } = null;\n    public double ColdEndOvershoot   { get; init; } = 0.05;\n    public double HotEndOvershoot    { get; init; } = 5.0;\n\n    /// <summary>Number of Potts colors (q) for all stages.</summary>\n    public int    Q                  { get; init; } = 20;\n\n    /// <summary>\n    /// If the coarse profile's stability score exceeds this, skip the dense\n    /// refinement stage and go directly to final equilibrium at the coarse\n    /// peak. Useful when the χ(T) curve is already sharp from the coarse\n    /// pass alone.\n    /// </summary>\n    public double EarlyStopStability { get; init; } = 0.95;\n\n    /// <summary>\n    /// Base seed for reproducibility. Per-task seeds are derived as\n    /// <c>HashCode.Combine(BaseSeed, T_quantized, round, replica)</c> so the\n    /// entire schedule is deterministic. <c>null</c> means each task draws\n    /// from OS entropy (non-reproducible but otherwise correct).\n    /// </summary>\n    public int?   BaseSeed           { get; init; } = null;\n\n    /// <summary>\n    /// Number of threads for the flat-task Parallel.For dispatch. Default\n    /// uses <see cref=\"Environment.ProcessorCount\"/>; lower it to leave\n    /// headroom when MAPPER itself dispatches patches in parallel.\n    /// </summary>\n    public int    MaxDegreeOfParallelism { get; init; } = -1;\n}\n\n/// <summary>\n/// Diagnostics from one <see cref=\"AdaptiveScheduler.Run\"/> call. Intended\n/// for both runtime introspection (debug output, profiling) and analytic\n/// post-processing (which patches needed which temperature regimes).\n/// </summary>\npublic sealed class AdaptivePatchDiagnostics\n{\n    public required int      SubgraphNodes     { get; init; }\n    public required int      SubgraphEdges     { get; init; }\n    public          double   ChosenTemperature { get; init; }\n    public          double   StabilityScore    { get; init; }\n    public          int      TotalCyclesUsed   { get; init; }\n    public          bool     EarlyStopped      { get; init; }\n    public          TimeSpan Elapsed           { get; init; }\n\n    /// <summary>\n    /// The full χ(T) profile assembled from coarse + (optional) dense stages.\n    /// Sorted ascending in T. Useful for plotting the per-patch susceptibility\n    /// curve.\n    /// </summary>\n    public IReadOnlyList<(double Temperature, double Chi)> Profile { get; init; }\n        = Array.Empty<(double, double)>();\n}\n\n/// <summary>\n/// Result of one adaptive schedule. <see cref=\"EdgeObservables\"/> is null\n/// only for trivial subgraphs (less than 2 nodes); callers should treat\n/// that case as \"every node is its own cluster\" downstream.\n/// </summary>\npublic sealed class AdaptiveResult\n{\n    public required AdaptivePatchDiagnostics  Diagnostics     { get; init; }\n    public          PottsModelEdgeObservables? EdgeObservables { get; init; }\n}\n\n/// <summary>\n/// Three-stage adaptive SPC schedule for a single (induced) subgraph.\n/// </summary>\n/// <remarks>\n/// <para><b>Stage 1 — Coarse probe.</b> Log-spaced grid over\n/// <c>[TempMin, TempMax]</c>; measures χ_FK at each grid point with a short\n/// burn-in + accumulation. Goal: locate the susceptibility peak region.</para>\n///\n/// <para><b>Stage 2 — Dense refinement with replicas.</b> Identify the\n/// half-maximum band around the coarse peak; build a log-spaced grid within\n/// that band; for each grid point, run <c>DenseReplicas × DenseRounds</c>\n/// independent measurements and average. The (T, replica, round) cartesian\n/// product is flattened to a single task list dispatched against the worker\n/// budget — same flat-parallel pattern as <see cref=\"SpcScheduler\"/>.</para>\n///\n/// <para><b>Stage 3 — Final equilibrium.</b> Construct a PottsModel at the\n/// chosen T with <see cref=\"PottsSusceptibility.FkAndEdges\"/>, run the full\n/// burn + accumulate cycle, and return the per-edge observables. The cut\n/// policy consumes these to produce cluster labels.</para>\n///\n/// <para><b>Early-stop.</b> If the coarse profile already has stability ≥\n/// <see cref=\"AdaptiveSchedulerConfig.EarlyStopStability\"/>, skip Stage 2\n/// and go directly to Stage 3 at the coarse peak. Reduces compute on\n/// patches with sharp transitions visible from the coarse sweep alone.</para>\n///\n/// <para><b>Susceptibility estimator.</b> χ_FK = ⟨Σ|c|²⟩ / (cycles · N) read\n/// directly from PottsModel's running accumulators. No need to track Var(S_max)\n/// or any new state — the existing FK accumulator IS what we want.</para>\n/// </remarks>\npublic static class AdaptiveScheduler\n{\n    public static AdaptiveResult Run(CsrGraph graph, AdaptiveSchedulerConfig? config = null)\n    {\n        var cfg = config ?? new AdaptiveSchedulerConfig();\n        var sw = Stopwatch.StartNew();\n\n        double tempMin;\n        double tempMax;\n        if (cfg.TempMin.HasValue && cfg.TempMax.HasValue)\n        {\n            tempMin = cfg.TempMin.Value;\n            tempMax = cfg.TempMax.Value;\n        }\n        else\n        {\n            (double bracketMin, double bracketMax) = CriticalTemperatureEstimator.EstimateBracket(\n                graph,\n                cfg.Q,\n                cfg.ColdEndOvershoot,\n                cfg.HotEndOvershoot);\n            tempMin = cfg.TempMin ?? bracketMin;\n            tempMax = cfg.TempMax ?? bracketMax;\n        }\n\n        // Trivial cases: empty or single-node subgraph. Nothing to schedule.\n        if (graph.NodeCount < 2)\n        {\n            return new AdaptiveResult\n            {\n                Diagnostics = new AdaptivePatchDiagnostics\n                {\n                    SubgraphNodes = graph.NodeCount,\n                    SubgraphEdges = 0,\n                    Elapsed       = sw.Elapsed,\n                },\n                EdgeObservables = null,\n            };\n        }\n\n        int totalCycles = 0;\n\n        // ── Stage 1 ───────────────────────────────────────────────────────\n        var coarseProfile = RunCoarseProbe(graph, cfg, tempMin, tempMax, ref totalCycles);\n        double coarseStability = ComputeStability(coarseProfile);\n\n        // Early-stop check: coarse curve already sharp?\n        if (coarseStability >= cfg.EarlyStopStability)\n        {\n            double earlyT = PickPeakTemperature(coarseProfile);\n            var earlyObs = RunFinalEquilibrium(graph, earlyT, cfg, ref totalCycles);\n            return new AdaptiveResult\n            {\n                Diagnostics = new AdaptivePatchDiagnostics\n                {\n                    SubgraphNodes     = graph.NodeCount,\n                    SubgraphEdges     = graph.Targets.Length / 2,\n                    ChosenTemperature = earlyT,\n                    StabilityScore    = coarseStability,\n                    TotalCyclesUsed   = totalCycles,\n                    EarlyStopped      = true,\n                    Elapsed           = sw.Elapsed,\n                    Profile           = coarseProfile,\n                },\n                EdgeObservables = earlyObs,\n            };\n        }\n\n        // ── Stage 2 ───────────────────────────────────────────────────────\n        var refinedProfile = RunDenseRefinement(graph, coarseProfile, cfg, ref totalCycles);\n        double chosenT = PickPeakTemperature(refinedProfile);\n        double stability = ComputeStability(refinedProfile);\n\n        // ── Stage 3 ───────────────────────────────────────────────────────\n        var finalObs = RunFinalEquilibrium(graph, chosenT, cfg, ref totalCycles);\n\n        var fullProfile = coarseProfile.Concat(refinedProfile)\n                                       .OrderBy(p => p.Temperature)\n                                       .ToList();\n\n        return new AdaptiveResult\n        {\n            Diagnostics = new AdaptivePatchDiagnostics\n            {\n                SubgraphNodes     = graph.NodeCount,\n                SubgraphEdges     = graph.Targets.Length / 2,\n                ChosenTemperature = chosenT,\n                StabilityScore    = stability,\n                TotalCyclesUsed   = totalCycles,\n                EarlyStopped      = false,\n                Elapsed           = sw.Elapsed,\n                Profile           = fullProfile,\n            },\n            EdgeObservables = finalObs,\n        };\n    }\n\n    // ── Stage 1: coarse probe ────────────────────────────────────────────\n    private static List<(double Temperature, double Chi)> RunCoarseProbe(\n        CsrGraph graph,\n        AdaptiveSchedulerConfig cfg,\n        double tempMin,\n        double tempMax,\n        ref int totalCycles)\n    {\n        var grid = LogSpaceGrid(tempMin, tempMax, cfg.CoarseSteps);\n        var results = new double[grid.Length];\n\n        Parallel.For(0, grid.Length, ParallelOptions(cfg), idx =>\n        {\n            double T = grid[idx];\n            int? seed = SeedForTask(cfg.BaseSeed, T, round: 0, replica: 0);\n            results[idx] = MeasureChi(graph, T, cfg.Q, cfg.CoarseBurn, cfg.CoarseCycles, seed);\n        });\n\n        totalCycles += grid.Length * (cfg.CoarseBurn + cfg.CoarseCycles);\n\n        var profile = new List<(double, double)>(grid.Length);\n        for (int i = 0; i < grid.Length; i++)\n            profile.Add((grid[i], results[i]));\n        return profile;\n    }\n\n    // ── Stage 2: dense band refinement ───────────────────────────────────\n    private static List<(double Temperature, double Chi)> RunDenseRefinement(\n        CsrGraph graph,\n        List<(double Temperature, double Chi)> coarseProfile,\n        AdaptiveSchedulerConfig cfg,\n        ref int totalCycles)\n    {\n        var (lo, hi) = ComputeHalfMaxBand(coarseProfile);\n        var grid = LogSpaceGrid(lo, hi, cfg.DenseSteps);\n\n        // Flatten the (T × Replica × Round) cartesian product into a single\n        // task list. This is the parallelization model the user wants: one\n        // pool of work units, dispatched against MaxDegreeOfParallelism.\n        var tasks = new List<(int TempIdx, int Round, int Replica)>(\n            grid.Length * cfg.DenseReplicas * cfg.DenseRounds);\n        for (int round = 0; round < cfg.DenseRounds; round++)\n            for (int rep = 0; rep < cfg.DenseReplicas; rep++)\n                for (int t = 0; t < grid.Length; t++)\n                    tasks.Add((t, round, rep));\n\n        var results = new double[tasks.Count];\n        Parallel.For(0, tasks.Count, ParallelOptions(cfg), idx =>\n        {\n            var (tIdx, round, rep) = tasks[idx];\n            double T = grid[tIdx];\n            int? seed = SeedForTask(cfg.BaseSeed, T, round, rep);\n            results[idx] = MeasureChi(graph, T, cfg.Q, cfg.DenseBurn, cfg.DenseCycles, seed);\n        });\n\n        totalCycles += tasks.Count * (cfg.DenseBurn + cfg.DenseCycles);\n\n        // Aggregate per T.\n        var sumPerT   = new double[grid.Length];\n        var countPerT = new int[grid.Length];\n        for (int idx = 0; idx < tasks.Count; idx++)\n        {\n            sumPerT[tasks[idx].TempIdx]   += results[idx];\n            countPerT[tasks[idx].TempIdx] += 1;\n        }\n\n        var refined = new List<(double, double)>(grid.Length);\n        for (int i = 0; i < grid.Length; i++)\n            refined.Add((grid[i], countPerT[i] > 0 ? sumPerT[i] / countPerT[i] : 0.0));\n        return refined;\n    }\n\n    // ── Stage 3: final equilibrium at chosen T ───────────────────────────\n    private static PottsModelEdgeObservables RunFinalEquilibrium(\n        CsrGraph graph, double T, AdaptiveSchedulerConfig cfg, ref int totalCycles)\n    {\n        int? seed = SeedForTask(cfg.BaseSeed, T, round: -1, replica: 0);\n        var model = new PottsModel(graph, T, cfg.Q,\n            PottsSusceptibility.FkAndEdges, seed);\n        if (cfg.FinalBurn > 0) model.BurnIn(cfg.FinalBurn);\n        model.Run(cfg.FinalCycles);\n\n        totalCycles += cfg.FinalBurn + cfg.FinalCycles;\n\n        var edges = model.GetEdgeCheckpoint()\n            ?? throw new InvalidOperationException(\n                \"FkAndEdges specialization did not produce edge observables — unexpected.\");\n        return edges;\n    }\n\n    // ── χ_FK measurement primitive ───────────────────────────────────────\n    /// <summary>\n    /// Construct a fresh PottsModel at temperature <paramref name=\"T\"/>, burn,\n    /// accumulate, and read χ_FK from the cluster-size accumulator. The model\n    /// is discarded on return — at our typical scale (~100 nodes per patch,\n    /// ~150-600 cycles per measurement) construction cost is negligible\n    /// compared to the SW dynamics.\n    /// </summary>\n    private static double MeasureChi(\n        CsrGraph graph, double T, int q, int burnCycles, int measureCycles, int? seed)\n    {\n        var model = new PottsModel(graph, T, q, PottsSusceptibility.FkOnly, seed);\n        if (burnCycles > 0) model.BurnIn(burnCycles);\n        model.Run(measureCycles);\n        // χ_FK(T) = ⟨Σ_c |c|²⟩ / (cycles · N)\n        return model.RunningSumSqClusterSizes / (measureCycles * (double)graph.NodeCount);\n    }\n\n    // ── Helpers ───────────────────────────────────────────────────────────\n    private static ParallelOptions ParallelOptions(AdaptiveSchedulerConfig cfg) => new()\n    {\n        MaxDegreeOfParallelism = cfg.MaxDegreeOfParallelism,\n    };\n\n    private static int? SeedForTask(int? baseSeed, double T, int round, int replica)\n    {\n        if (baseSeed is null) return null;\n        // Quantize T to ~3 significant digits so equal Ts produce equal seeds\n        // (modulo round/replica). HashCode.Combine is xxhash-flavored and\n        // adequate for de-correlating tasks.\n        int tQuantized = (int)(T * 1000.0);\n        return HashCode.Combine(baseSeed.Value, tQuantized, round, replica);\n    }\n\n    private static double[] LogSpaceGrid(double lo, double hi, int steps)\n    {\n        if (steps <= 1) return new[] { 0.5 * (lo + hi) };\n        if (lo <= 0.0 || hi <= 0.0)\n            throw new ArgumentException(\"LogSpaceGrid requires strictly positive endpoints.\");\n        if (hi <= lo) (lo, hi) = (hi, lo);\n\n        double logLo = Math.Log(lo);\n        double logHi = Math.Log(hi);\n        var grid = new double[steps];\n        for (int i = 0; i < steps; i++)\n            grid[i] = Math.Exp(logLo + i * (logHi - logLo) / (steps - 1));\n        return grid;\n    }\n\n    /// <summary>\n    /// Returns the (T_lo, T_hi) interval bracketing the half-maximum band\n    /// of the χ profile. Falls back to the full profile range if the curve\n    /// is flat (no clear peak).\n    /// </summary>\n    private static (double Lo, double Hi) ComputeHalfMaxBand(\n        IReadOnlyList<(double Temperature, double Chi)> profile)\n    {\n        double chiMax = profile.Max(p => p.Chi);\n        if (chiMax <= 0.0) return (profile[0].Temperature, profile[^1].Temperature);\n\n        double half = chiMax * 0.5;\n        double lo = profile[0].Temperature, hi = profile[^1].Temperature;\n\n        foreach (var (T, chi) in profile)\n            if (chi >= half) { lo = T; break; }\n        for (int i = profile.Count - 1; i >= 0; i--)\n            if (profile[i].Chi >= half) { hi = profile[i].Temperature; break; }\n\n        if (lo >= hi) (lo, hi) = (profile[0].Temperature, profile[^1].Temperature);\n        return (lo, hi);\n    }\n\n    private static double PickPeakTemperature(\n        IReadOnlyList<(double Temperature, double Chi)> profile)\n        => profile.OrderByDescending(p => p.Chi).First().Temperature;\n\n    /// <summary>\n    /// Stability score ∈ [0, 1]: 1 − (half-max bandwidth) / (full profile\n    /// range). A sharp peak gives a narrow band → score near 1; a flat\n    /// curve gives a wide band → score near 0.\n    /// </summary>\n    private static double ComputeStability(\n        IReadOnlyList<(double Temperature, double Chi)> profile)\n    {\n        if (profile.Count < 2) return 0.0;\n        var (lo, hi) = ComputeHalfMaxBand(profile);\n        double full = profile[^1].Temperature - profile[0].Temperature;\n        if (full <= 0.0) return 0.0;\n        return Math.Clamp(1.0 - (hi - lo) / full, 0.0, 1.0);\n    }\n} |
13 | clustering/spc/BlattCanonicalCut.cs | {6617 833 0.2497 4.7623} | 6837 | using System;\nusing System.Collections.Generic;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Cuts;\n\n/// <summary>\n/// Final partition produced by an <c>SPC</c> cut policy. Densely-labeled\n/// (labels live in <c>[0, ClusterCount)</c>) so downstream consumers can\n/// allocate per-cluster arrays without remapping.\n/// </summary>\npublic sealed record SpcCut\n{\n    public required int[]  Labels       { get; init; }\n    public required int    ClusterCount { get; init; }\n    public required double Theta        { get; init; }\n}\n\n/// <summary>\n/// Blatt 1996 canonical Friends-of-Friends cut policy.\n/// </summary>\n/// <remarks>\n/// <para><b>Algorithm.</b> For each undirected edge <c>(i,j)</c> in the\n/// graph, declare <c>(i,j)</c> a <i>friend</i> iff\n/// <c>⟨1_{s_i=s_j}⟩ = spinAgreementCount[e] / cycleCount &gt; θ</c>.\n/// Compute connected components of the friend-subgraph via union-find.\n/// Each component becomes one output cluster.</para>\n///\n/// <para><b>Why this works.</b> At equilibrium in the SP phase, pairs of\n/// nodes that belong to the same physical cluster spend most cycles in\n/// the same Potts color (so <c>⟨1_{s_i=s_j}⟩ ≈ 1</c>). Pairs that don't\n/// share a cluster are decoupled by the SW dynamics and align only by\n/// chance (so <c>⟨1_{s_i=s_j}⟩ ≈ 1/q</c>, the random-color baseline).\n/// A threshold <c>θ</c> between those two modes — Blatt finds the cut\n/// is insensitive to <c>θ</c> across <c>0.2 ≤ θ ≤ 0.9</c> — sharply\n/// separates the two regimes.</para>\n///\n/// <para><b>Index alignment.</b> <c>spinAgreementCount</c>\n/// must be the PottsRunner's per-cycle accumulator indexed by CSR entry\n/// — the same array that Phase B's <c>FkAndEdges</c> /\n/// <c>FkAndBlattAndEdges</c> specializations populate, accessed via the\n/// <c>.spce</c> sidecar's <see cref=\"Potts.PottsModelEdgeObservables.SpinAgreementCount\"/>\n/// field. The SW loop only increments the upper-triangular CSR slots\n/// (those where <c>j &gt; i</c>); this method walks the same pattern,\n/// so the index mapping is symmetric.</para>\n///\n/// <para><b>Not yet implemented.</b> The Landsat-style \"max-correlation\n/// neighbor\" augmentation (step (g)-addendum from the original paper)\n/// would also union each node with its best-correlated neighbor\n/// regardless of <c>θ</c>. Skipped for v1 because the j&lt;i lookup\n/// requires either symmetric scratch storage or a per-slot mirror index;\n/// both are doable but out of scope for a first cut. The <c>θ</c>-only\n/// path covers the canonical Iris / 2D-toy cases from the paper.</para>\n/// </remarks>\npublic static class BlattCanonicalCut\n{\n    public static SpcCut Apply(\n        CsrGraph graph,\n        int[] spinAgreementCount,\n        int cycleCount,\n        double theta = 0.5)\n    {\n        if (cycleCount <= 0)\n            throw new ArgumentOutOfRangeException(nameof(cycleCount), \"Cycle count must be positive.\");\n        if (graph.Targets is null || graph.RowPointers is null)\n            throw new ArgumentException(\"Graph CSR arrays are not initialized.\", nameof(graph));\n        if (spinAgreementCount.Length != graph.Targets.Length)\n            throw new ArgumentException(\n                $\"spinAgreementCount length ({spinAgreementCount.Length}) must match CSR slot count ({graph.Targets.Length}).\",\n                nameof(spinAgreementCount));\n        if (theta < 0.0 || theta > 1.0)\n            throw new ArgumentOutOfRangeException(nameof(theta), \"θ must lie in [0, 1].\");\n\n        int n = graph.NodeCount;\n        var uf = new UnionFind(n);\n        double cyclesD = cycleCount;\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;   // undirected: visit each edge once\n\n                double avgAgreement = spinAgreementCount[e] / cyclesD;\n                if (avgAgreement > theta)\n                    uf.Union(i, j);\n            }\n        }\n\n        // Densify labels: collapse the sparse UF root IDs to [0, K).\n        var labels = new int[n];\n        var labelMap = new Dictionary<int, int>();\n        int nextLabel = 0;\n        for (int i = 0; i < n; i++)\n        {\n            int root = uf.Find(i);\n            if (!labelMap.TryGetValue(root, out int dense))\n            {\n                dense = nextLabel++;\n                labelMap[root] = dense;\n            }\n            labels[i] = dense;\n        }\n\n        return new SpcCut\n        {\n            Labels       = labels,\n            ClusterCount = nextLabel,\n            Theta        = theta,\n        };\n    }\n}\n\n/// <summary>\n/// Cluster-purity helper for evaluating a cut against ground-truth labels.\n/// </summary>\n/// <remarks>\n/// <para>Lives alongside <see cref=\"BlattCanonicalCut\"/> for now — when\n/// the planned <c>src/clustering/qc/</c> project comes online, this\n/// belongs there, not under Cuts.</para>\n///\n/// <para><b>Definition.</b> Purity =\n/// <c>(1/N) · Σ_c max_k |c ∩ k|</c>, where the inner term is the count of\n/// points in both cut cluster <c>c</c> and ground-truth class <c>k</c>.\n/// Range <c>[0, 1]</c>; perfect agreement gives 1. Trivially achieves 1\n/// when each cut cluster contains exactly one ground-truth class — but\n/// also when there are many tiny cut clusters, so should be read\n/// alongside ClusterCount.</para>\n/// </remarks>\npublic static class Purity\n{\n    public static double Compute(int[] cutLabels, int[] groundTruthLabels)\n    {\n        if (cutLabels.Length != groundTruthLabels.Length)\n            throw new ArgumentException(\n                $\"cutLabels length ({cutLabels.Length}) does not match groundTruthLabels length ({groundTruthLabels.Length}).\");\n        int n = cutLabels.Length;\n        if (n == 0) return 0.0;\n\n        // Contingency: (cutCluster, trueClass) → count.\n        var counts = new Dictionary<(int Cut, int Truth), int>();\n        for (int i = 0; i < n; i++)\n        {\n            var key = (cutLabels[i], groundTruthLabels[i]);\n            counts[key] = counts.TryGetValue(key, out int c) ? c + 1 : 1;\n        }\n\n        // For each cut cluster, find the max count over true classes.\n        var maxPerCluster = new Dictionary<int, int>();\n        foreach (var (key, count) in counts)\n        {\n            if (!maxPerCluster.TryGetValue(key.Cut, out int current) || count > current)\n                maxPerCluster[key.Cut] = count;\n        }\n\n        long correctlyAssigned = 0;\n        foreach (var v in maxPerCluster.Values) correctlyAssigned += v;\n        return (double)correctlyAssigned / n;\n    }\n} |
14 | clustering/spc/diagnostics/BondFrequency.cs | {571 52 0.2294 4.6857} | 590 | using Clustering.SPC.Potts;\nusing Maths.Information;\n\nnamespace Clustering.SPC.Diagnostics;\n\npublic static class BondFrequency\n{\n    public static double EntropyNats(PottsModelEdgeObservables edges)\n    {\n        if (edges.CycleCount <= 0)\n            return 0.0;\n\n        double invCycles = 1.0 / edges.CycleCount;\n        var frequencies = new double[edges.BondFormedCount.Length];\n        for (int i = 0; i < edges.BondFormedCount.Length; i++)\n            frequencies[i] = edges.BondFormedCount[i] * invCycles;\n\n        return Shannon.EntropyNats(frequencies);\n    }\n} |
15 | clustering/spc/diagnostics/LabelEntropy.cs | {1689 134 0.2842 4.5537} | 1738 | using System;\nusing System.Collections.Generic;\nusing Clustering.SPC.Potts;\nusing Maths.Information;\n\nnamespace Clustering.SPC.Diagnostics;\n\npublic readonly record struct LabelEntropyCurve(\n    double[] Temperatures,\n    double[] Entropy);\n\npublic static class LabelEntropy\n{\n    public static LabelEntropyCurve From(IReadOnlyList<PottsModelStepResult> frames)\n    {\n        if (frames.Count == 0)\n            return new LabelEntropyCurve(Array.Empty<double>(), Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, int[]>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelStepResult frame = frames[i];\n            if (!grouped.TryGetValue(frame.Temperature, out int[]? histogram))\n            {\n                histogram = new int[frame.ClusterSizeHistogram.Length];\n                grouped[frame.Temperature] = histogram;\n            }\n            else if (histogram.Length < frame.ClusterSizeHistogram.Length)\n            {\n                Array.Resize(ref histogram, frame.ClusterSizeHistogram.Length);\n                grouped[frame.Temperature] = histogram;\n            }\n\n            for (int j = 0; j < frame.ClusterSizeHistogram.Length; j++)\n                histogram[j] += frame.ClusterSizeHistogram[j];\n        }\n\n        var temperatures = new double[grouped.Count];\n        var entropy = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, int[] histogram) in grouped)\n        {\n            temperatures[index] = temperature;\n            entropy[index] = Shannon.EntropyNats(histogram);\n            index++;\n        }\n\n        return new LabelEntropyCurve(temperatures, entropy);\n    }\n} |
16 | clustering/spc/diagnostics/SpecificHeat.cs | {1770 169 0.2932 4.5402} | 1818 | using System;\nusing System.Collections.Generic;\nusing Clustering.SPC.Potts;\n\nnamespace Clustering.SPC.Diagnostics;\n\npublic readonly record struct SpecificHeatCurve(\n    double[] Temperatures,\n    double[] Cv);\n\npublic static class SpecificHeat\n{\n    public static SpecificHeatCurve From(IReadOnlyList<PottsModelStepResult> frames)\n    {\n        if (frames.Count == 0)\n            return new SpecificHeatCurve(Array.Empty<double>(), Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double SumCv, int Count)>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelStepResult frame = frames[i];\n            double cv = 0.0;\n            if (frame.Temperature > 0.0 && frame.CycleCount > 0)\n            {\n                double meanEnergy = frame.RunningSumEnergy / frame.CycleCount;\n                double meanEnergySq = frame.RunningSumEnergySq / frame.CycleCount;\n                double variance = Math.Max(0.0, meanEnergySq - meanEnergy * meanEnergy);\n                cv = variance / (frame.Temperature * frame.Temperature);\n            }\n\n            if (grouped.TryGetValue(frame.Temperature, out (double SumCv, int Count) acc))\n                grouped[frame.Temperature] = (acc.SumCv + cv, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (cv, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var cvValues = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, (double sumCv, int count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            cvValues[index] = count > 0 ? sumCv / count : 0.0;\n            index++;\n        }\n\n        return new SpecificHeatCurve(temperatures, cvValues);\n    }\n} |
