idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
68 | synthetic/MobiusEllipsoid.cs | {21499 2211 0.3583 4.4054} | 22128 | using System;\nusing System.Collections.Generic;\n\nnamespace SyntheticDatasets\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // Enums & Geometry DTO\n        // =====================================================================\n\n        public enum TubeCrossSection\n        {\n            Ribbon,                // flat rectangular: uniform in N×B rectangle; sharpest topology signal\n            GaussianIsotropic,     // soft boundary, round cross-section\n            GaussianAnisotropic,   // elliptical cross-section; major axis twists with strip\n            UniformDisk,           // hard boundary, uniform density\n            Annular,               // hollow tube; maximally stresses global topology\n        }\n\n        public enum MobiusPlacement\n        {\n            /// <summary>Near θ≈0/2π seam — maximum Euclidean false-bridge stress.</summary>\n            NearSeam,\n            /// <summary>Orthogonal intersection at self-crossing zone (spine at θ≈π).</summary>\n            OrthogonalCenterCross,\n            /// <summary>Near the peripheral turning elbow (θ≈¾π).</summary>\n            PeripheralElbow,\n            /// <summary>Fully manual; use ellipsoidCenter and ellipsoidEulerXYZ directly.</summary>\n            Manual,\n        }\n\n        public sealed class MobiusTubeGeometry : ClusterGeometry\n        {\n            public double[][] SpineSamples { get; set; } = Array.Empty<double[]>();\n            /// <summary>Per-spine-sample [T, N, B] frame triplet. Shape: M × 3 × 3.</summary>\n            public double[][][] LocalFrames { get; set; } = Array.Empty<double[][]>();\n            public double SpineRadius { get; set; }\n            public double HalfWidth { get; set; }\n            public double HalfThickness { get; set; }\n            public double TwistCount { get; set; } = 1.0;\n            public TubeCrossSection CrossSection { get; set; }\n            public double RadialBias { get; set; } = 1.0;\n        }\n\n        // =====================================================================\n        // Generator\n        // =====================================================================\n\n        /// <summary>\n        /// Two clusters with fundamentally different topologies:\n        ///\n        ///   Cluster 0 — a solid Möbius tube: a thick twisted ribbon with non-orientable\n        ///     topology. In 3D it self-intersects at the gluing seam (θ=0/2π), creating\n        ///     genuine false-neighbor opportunities for Euclidean proximity graphs.\n        ///     The optional 4D embedding cleanly resolves the self-intersection using\n        ///     sin(halfTheta) as the 4th coordinate, which is maximal at θ=π (self-cross).\n        ///\n        ///   Cluster 1 — a compact anisotropic ellipsoid. Convex; a single full-covariance\n        ///     Gaussian fits it faithfully.\n        ///\n        /// Cross-section geometry:\n        ///   The N-direction (normal, half-twist) uses halfWidth as scale.\n        ///   The B-direction (binormal) uses halfThickness as scale.\n        ///   GaussianIsotropic with halfWidth == halfThickness produces a round tube.\n        ///   Both are sampled from the same TubeCrossSection distribution for consistency.\n        ///\n        /// Placement modes target geometrically meaningful stress points:\n        ///   NearSeam              — ellipsoid just outside spine at θ=0 (max false-bridge)\n        ///   OrthogonalCenterCross — ellipsoid at self-crossing zone (θ=π spine point)\n        ///   PeripheralElbow       — ellipsoid at a lateral turning region (θ≈¾π)\n        ///   Manual                — direct ellipsoidCenter + ellipsoidEulerXYZ override\n        ///\n        /// radialBias semantics:\n        ///   1.0 (default) = full tube extent (halfWidth / halfThickness as given).\n        ///   &lt; 1.0 = narrower tube, mass concentrated closer to spine.\n        ///   &gt; 1.0 = wider tube, more spread.\n        /// </summary>\n        public static SyntheticDataset GenerateMobiusAndEllipsoid(\n            // --- Möbius Tube ---\n            int mobiusPoints = 400,\n            double spineRadius = 2.5,\n            double halfWidth = 1.1,         // N-direction (normal) cross-section scale\n            double halfThickness = 0.12,    // B-direction (binormal); keep << halfWidth for ribbon shape\n            double noiseSigma = 0.06,\n            double twistCount = 1.0,        // 1.0 = classic Möbius half-twist; 2.0 = full twist\n            TubeCrossSection crossSection = TubeCrossSection.Ribbon,\n            double radialBias = 1.0,        // < 1.0 narrows tube toward spine; > 1.0 widens\n                                            // --- Ellipsoid ---\n            int ellipsoidPoints = 180,\n            double[]? ellipsoidAxes = null, // per-axis scale [a, b, c]; default [0.6, 1.0, 0.45]\n                                            // --- Placement ---\n            MobiusPlacement placement = MobiusPlacement.NearSeam,\n            double[]? ellipsoidCenter = null,    // 3-element; used when placement == Manual\n            double[]? ellipsoidEulerXYZ = null,  // Euler angles (radians); overrides auto-rotation\n            double intersectDepth = 0.0,         // Z-offset of ellipsoid center from spine plane\n            double intersectRadialShift = 0.0,   // shift along radial direction at placement point\n            double gapScale = 1.0,               // multiplier on inter-cluster gap\n                                                 // --- Ambient ---\n            int dimensions = 3,    // 3 or 4; 4D provides clean embedding via sin(halfTheta) lift\n            int seed = 42)\n        {\n            ellipsoidAxes ??= new[] { 3.0, 0.5, 0.5 };  // X is the long axis; all presets calibrated for this.\n            if (ellipsoidAxes.Length != 3)\n                throw new ArgumentException(\"ellipsoidAxes must have 3 elements.\");\n            if (dimensions < 3 || dimensions > 4)\n                throw new ArgumentException(\"dimensions must be 3 or 4.\");\n\n            var rng = new Random(seed);\n            int n = mobiusPoints + ellipsoidPoints;\n            var features = new double[n][];\n            var labels = new int[n];\n\n            // ====================== Möbius Tube ======================\n            var spineSamples = new List<double[]>();\n            var localFrames = new List<double[][]>();\n\n            // Clamp radialBias to avoid degenerate tube\n            double effBias = Math.Max(radialBias, 0.05);\n            const double kDensityMax = 1.4; // = 1 + 0.4 * |cos(0)|\n\n            int p = 0;\n            while (p < mobiusPoints)\n            {\n                double theta = 2.0 * Math.PI * rng.NextDouble();\n                // θ-density bias via rejection: concentrate toward seam (θ≈0) and apex (θ≈π)\n                // without distorting the cross-section geometry.\n                double thetaWeight = 1.0 + 0.4 * Math.Abs(Math.Cos(theta));\n                if (rng.NextDouble() * kDensityMax > thetaWeight) continue;\n                double halfTheta = theta * twistCount * 0.5;\n\n                double cx = spineRadius * Math.Cos(theta);\n                double cy = spineRadius * Math.Sin(theta);\n\n                double cht = Math.Cos(halfTheta);\n                double sht = Math.Sin(halfTheta);\n\n                // Möbius-twisted orthonormal frame.\n                // N rotates by halfTheta as θ increases, producing a half-twist per loop.\n                // At θ=0 and θ=2π: N has flipped — this is the Möbius monodromy.\n                //   T = unit tangent to spine circle\n                //   N = normal (half-twisted): [cos(halfTheta)·cos(θ), cos(halfTheta)·sin(θ), sin(halfTheta)]\n                //   B = binormal (completes right-hand frame): [-sin(halfTheta)·cos(θ), -sin(halfTheta)·sin(θ), cos(halfTheta)]\n                // Verification: T·N = T·B = N·B = 0; |T|=|N|=|B|=1 ✓\n                double[] T = { -Math.Sin(theta), Math.Cos(theta), 0.0 };\n                double[] N = { cht * Math.Cos(theta), cht * Math.Sin(theta), sht };\n                double[] B = { -sht * Math.Cos(theta), -sht * Math.Sin(theta), cht };\n\n                double px, py, pz, w4;\n                if (crossSection == TubeCrossSection.Ribbon)\n                {\n                    // Flat rectangular cross-section: direct N/B offsets (no phi mixing).\n                    // Uniform in [-halfWidth, halfWidth] × [-halfThickness, halfThickness].\n                    // Hard flat edges make the half-twist maximally visible from the side.\n                    double uN = (rng.NextDouble() * 2.0 - 1.0) * halfWidth * effBias;\n                    double uB = (rng.NextDouble() * 2.0 - 1.0) * halfThickness * effBias;\n                    px = cx + uN * N[0] + uB * B[0];\n                    py = cy + uN * N[1] + uB * B[1];\n                    pz = uN * N[2] + uB * B[2];   // cz = 0\n                    w4 = uN * sht * 1.2;\n                }\n                else\n                {\n                    // N and B extents sampled independently from the same distribution\n                    // using their respective scale parameters. This ensures:\n                    //   - GaussianIsotropic: round when halfWidth == halfThickness\n                    //   - GaussianAnisotropic: elliptical cross-section (halfWidth vs halfThickness ratio)\n                    //   - UniformDisk/Annular: geometrically consistent hard-boundary cross-section\n                    double rN = SampleMobiusCrossSection(rng, halfWidth * effBias, crossSection);\n                    double rB = SampleMobiusCrossSection(rng, halfThickness * effBias, crossSection);\n                    double phi = 2.0 * Math.PI * rng.NextDouble();\n                    double cosPhi = Math.Cos(phi);\n                    double sinPhi = Math.Sin(phi);\n                    px = cx + rN * cosPhi * N[0] + rB * sinPhi * B[0];\n                    py = cy + rN * cosPhi * N[1] + rB * sinPhi * B[1];\n                    pz = rN * cosPhi * N[2] + rB * sinPhi * B[2];   // cz = 0\n                    w4 = rN * sinPhi * sht * 1.2;\n                }\n\n                px += noiseSigma * SampleStandardNormal(rng);\n                py += noiseSigma * SampleStandardNormal(rng);\n                pz += noiseSigma * SampleStandardNormal(rng);\n\n                var point = new double[dimensions];\n                point[0] = px; point[1] = py; point[2] = pz;\n\n                if (dimensions == 4)\n                    // 4D lift: sht = sin(halfTheta).\n                    // sht=0 at θ=0 (seam) and sht=±1 at θ=π (self-crossing zone) —\n                    // precisely where separation is needed for a clean embedding.\n                    point[3] = w4;\n\n                features[p] = point;\n                labels[p] = 0;\n\n                p++;\n            }\n\n            // Generate spine samples on a uniform θ grid — independent of the random\n            // point cloud so the curve is ordered and arc-length uniform.\n            int spineRes = Math.Max(80, mobiusPoints / 5);\n            for (int s = 0; s < spineRes; s++)\n            {\n                double spTheta = 2.0 * Math.PI * s / spineRes;\n                double spHalf = spTheta * twistCount * 0.5;\n                double spCht = Math.Cos(spHalf), spSht = Math.Sin(spHalf);\n                spineSamples.Add(new[] { spineRadius * Math.Cos(spTheta), spineRadius * Math.Sin(spTheta), 0.0 });\n                localFrames.Add(new[] {\n                    new[] { -Math.Sin(spTheta), Math.Cos(spTheta), 0.0 },\n                    new[] { spCht * Math.Cos(spTheta), spCht * Math.Sin(spTheta), spSht },\n                    new[] { -spSht * Math.Cos(spTheta), -spSht * Math.Sin(spTheta), spCht },\n                });\n            }\n\n            // ====================== Ellipsoid ======================\n            double[] center = ResolveMobiusEllipsoidCenter(\n                placement, spineRadius, halfWidth,\n                intersectDepth, intersectRadialShift, gapScale, ellipsoidCenter, rng);\n\n            double[,] rot = ResolveMobiusEllipsoidRotation(placement, ellipsoidEulerXYZ, rng);\n            double[,] cov = BuildCovariance(rot, ellipsoidAxes);\n            double[,] L = CholeskyLower(cov, 3);\n\n            for (int ep = 0; ep < ellipsoidPoints; ep++)\n            {\n                double[] z = {\n                    SampleStandardNormal(rng),\n                    SampleStandardNormal(rng),\n                    SampleStandardNormal(rng)\n                };\n                double norm = Math.Sqrt(z[0] * z[0] + z[1] * z[1] + z[2] * z[2]) + 1e-12;\n                double rScale = Math.Pow(rng.NextDouble(), 1.0 / 3.0);\n                for (int d = 0; d < 3; d++) z[d] = z[d] / norm * rScale;\n\n                double[] pt = MultiplyMatrixVector(L, z);\n                var point = new double[dimensions];\n                for (int d = 0; d < 3; d++) point[d] = center[d] + pt[d];\n                if (dimensions == 4) point[3] = ellipsoidAxes[2] * SampleStandardNormal(rng);\n\n                features[mobiusPoints + ep] = point;\n                labels[mobiusPoints + ep] = 1;\n            }\n\n            // ====================== Metadata ======================\n            var mobiusGeom = new MobiusTubeGeometry\n            {\n                SpineSamples = spineSamples.ToArray(),\n                LocalFrames = localFrames.ToArray(),\n                SpineRadius = spineRadius,\n                HalfWidth = halfWidth,\n                HalfThickness = halfThickness,\n                TwistCount = twistCount,\n                CrossSection = crossSection,\n                RadialBias = radialBias,\n            };\n\n            var ellipsoidGeom = new EllipsoidGeometry { Center = center, Covariance = cov };\n\n            var parameters = new Dictionary<string, object>\n            {\n                [\"generator\"] = \"MobiusAndEllipsoid\",\n                [\"dimensions\"] = dimensions,\n                [\"mobiusPoints\"] = mobiusPoints,\n                [\"ellipsoidPoints\"] = ellipsoidPoints,\n                [\"spineRadius\"] = spineRadius,\n                [\"halfWidth\"] = halfWidth,\n                [\"halfThickness\"] = halfThickness,\n                [\"noiseSigma\"] = noiseSigma,\n                [\"twistCount\"] = twistCount,\n                [\"crossSection\"] = crossSection.ToString(),\n                [\"radialBias\"] = radialBias,\n                [\"placement\"] = placement.ToString(),\n                [\"intersectDepth\"] = intersectDepth,\n                [\"intersectRadialShift\"] = intersectRadialShift,\n                [\"gapScale\"] = gapScale,\n                [\"seed\"] = seed,\n            };\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = 2,\n                Parameters = parameters,\n                ClusterGeometries = new ClusterGeometry[] { mobiusGeom, ellipsoidGeom },\n                ClusterCovariances = new[] { ApproximateMobiusCovariance(spineRadius, halfWidth, halfThickness), cov },\n            };\n        }\n\n        // =====================================================================\n        // 4D Projection Helper\n        // =====================================================================\n\n        /// <summary>\n        /// Projects 4D points to 3D by rotating in the x₃–x₄ plane.\n        /// Sweep <paramref name=\"angle4\"/> from 0 → 2π for an animated 4D rotation.\n        /// Points with fewer than 4 dimensions are passed through unchanged.\n        /// </summary>\n        public static double[][] Project4DTo3D(double[][] pts4D, double angle4 = 0.0)\n        {\n            double c = Math.Cos(angle4), s = Math.Sin(angle4);\n            var proj = new double[pts4D.Length][];\n            for (int i = 0; i < pts4D.Length; i++)\n            {\n                var p = pts4D[i];\n                if (p.Length < 4) { proj[i] = (double[])p.Clone(); continue; }\n                proj[i] = new[] { p[0], p[1], p[2] * c + p[3] * s };\n            }\n            return proj;\n        }\n\n        // =====================================================================\n        // Placement Helpers\n        // =====================================================================\n\n        private static double[] ResolveMobiusEllipsoidCenter(\n            MobiusPlacement placement, double R, double w,\n            double intersectDepth, double intersectRadialShift, double gapScale,\n            double[]? manualCenter, Random rng)\n        {\n            if (placement == MobiusPlacement.Manual)\n            {\n                if (manualCenter is null || manualCenter.Length != 3)\n                    throw new ArgumentException(\n                        \"Manual placement requires a 3-element ellipsoidCenter.\");\n                return (double[])manualCenter.Clone();\n            }\n\n            switch (placement)\n            {\n                case MobiusPlacement.NearSeam:\n                    // Spine at θ=0 is at (R, 0, 0). The ribbon is horizontal here (N=X, B=Z).\n                    // Place the ellipsoid centred on the spine so it stabs through the ribbon.\n                    return new[] { R + intersectRadialShift, 0.0, intersectDepth };\n\n                case MobiusPlacement.OrthogonalCenterCross:\n                    // Spine at θ=π is at (-R, 0, 0). Place at the spine point itself so\n                    // the ellipsoid intersects the self-crossing zone of the tube.\n                    return new[] { -R + intersectRadialShift, 0.0, intersectDepth };\n\n                case MobiusPlacement.PeripheralElbow:\n                    // Turning region near θ=¾π — a lateral concave turning point.\n                    double alpha = Math.PI * 0.75;\n                    return new[] {\n                        R * Math.Cos(alpha) + intersectRadialShift,\n                        R * Math.Sin(alpha),\n                        intersectDepth * 0.8\n                    };\n\n                default:\n                    return new[] { R * 0.2, -R * 0.9, 0.8 };\n            }\n        }\n\n        private static double[,] ResolveMobiusEllipsoidRotation(\n            MobiusPlacement placement, double[]? eulerXYZ, Random rng)\n        {\n            if (eulerXYZ is not null)\n            {\n                if (eulerXYZ.Length != 3)\n                    throw new ArgumentException(\"ellipsoidEulerXYZ must have 3 elements.\");\n                return EulerToRotation(eulerXYZ[0], eulerXYZ[1], eulerXYZ[2]);\n            }\n\n            return placement switch\n            {\n                // X-major (3.0) → Z axis: Ry(π/2) maps local X → world -Z.\n                // At θ=0 the ribbon is horizontal; B=Z is the face normal — X pierces straight through.\n                MobiusPlacement.NearSeam\n                    => EulerToRotation(0.0, Math.PI / 2.0, 0.0),\n\n                // At θ=π the strip has twisted 90°. B=(1,0,0)=X is the face normal.\n                // X-major with identity rotation already points in X — no rotation needed.\n                MobiusPlacement.OrthogonalCenterCross\n                    => EulerToRotation(0.0, 0.0, 0.0),\n\n                // At θ=¾π the outward radial direction is (cos(¾π), sin(¾π), 0).\n                // Rz(¾π) maps local X → outward radial, giving a plunging angle through the arm.\n                MobiusPlacement.PeripheralElbow\n                    => EulerToRotation(0.0, 0.0, Math.PI * 0.75),\n\n                // Default: align X → Z (pierce through the Möbius plane).\n                _ => EulerToRotation(0.0, Math.PI / 2.0, 0.0),\n            };\n        }\n\n        // =====================================================================\n        // Cross-Section Sampler\n        // =====================================================================\n\n        private static double SampleMobiusCrossSection(\n            Random rng, double scale, TubeCrossSection mode)\n        {\n            return mode switch\n            {\n                TubeCrossSection.GaussianIsotropic or TubeCrossSection.GaussianAnisotropic\n                    => Math.Abs(SampleStandardNormal(rng)) * scale,\n                TubeCrossSection.UniformDisk\n                    => scale * Math.Sqrt(rng.NextDouble()),\n                TubeCrossSection.Annular\n                    => scale * (0.4 + 0.6 * rng.NextDouble()),\n                _ => scale * Math.Sqrt(rng.NextDouble()),\n            };\n        }\n\n        // =====================================================================\n        // Approximate covariance for best-fit-Gaussian diagnostic overlay\n        // =====================================================================\n\n        /// <summary>\n        /// Diagonal approximation of a Möbius tube's covariance for the viz best-fit\n        /// Gaussian overlay. Shows what a single-Gaussian model would report on this\n        /// non-orientable cluster.\n        ///\n        /// Derivation (θ uniform, φ uniform):\n        ///   Var[px] ≈ Var[R·cos(θ)] + Var[rN·cos(φ)·N₀ + rB·sin(φ)·B₀]\n        ///           ≈ R²/2 + (w²+t²)/4     (spine dominates in XY)\n        ///   Var[pz] ≈ (w²+t²)/4             (only tube cross-section contributes to Z)\n        /// </summary>\n        private static double[,] ApproximateMobiusCovariance(double R, double w, double t)\n        {\n            double varXY = R * R / 2.0 + (w * w + t * t) / 4.0;\n            double varZ = (w * w + t * t) / 4.0;\n            return new double[,] {\n                { varXY, 0.0,   0.0  },\n                { 0.0,   varXY, 0.0  },\n                { 0.0,   0.0,   varZ },\n            };\n        }\n    }\n} |
69 | synthetic/Simplex.cs | {3990 387 0.4386 3.9098} | 4109 | using System;\nusing System.Collections.Generic;\n\nnamespace SyntheticDatasets\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // Simplex — probability distributions with controllable support disjointness\n        // =====================================================================\n        /// <summary>\n        /// Each data point is a categorical probability vector drawn from a\n        /// cluster-specific Dirichlet. With disjointSupports = true, each\n        /// cluster's mass concentrates on a non-overlapping range of\n        /// categories — producing distribution pairs whose KL / JSD saturate\n        /// at the metric's maximum. Exposes the stress case for\n        /// information-geometric metrics and for proximity rules that depend\n        /// on non-degenerate distance rankings.\n        /// </summary>\n        public static SyntheticDataset GenerateSimplex(\n            int clusterCount = 5,\n            int pointsPerCluster = 40,\n            int categories = 20,\n            bool disjointSupports = true,\n            double concentration = 20.0,\n            int seed = 42)\n        {\n            var rng = new Random(seed);\n            int n = clusterCount * pointsPerCluster;\n            var features = new double[n][];\n            var labels = new int[n];\n            var modes = new double[clusterCount][];\n\n            if (disjointSupports)\n            {\n                int regionSize = categories / clusterCount;\n                if (regionSize < 2)\n                    throw new ArgumentException(\n                        $\"categories ({categories}) too small for {clusterCount} disjoint \" +\n                        $\"supports. Need at least 2 * clusterCount categories.\");\n                for (int c = 0; c < clusterCount; c++)\n                {\n                    var mode = new double[categories];\n                    int regionStart = c * regionSize;\n                    for (int k = 0; k < categories; k++)\n                        mode[k] = (k >= regionStart && k < regionStart + regionSize)\n                            ? 1.0 / regionSize\n                            : 1e-6;\n                    Normalize(mode);\n                    modes[c] = mode;\n                }\n            }\n            else\n            {\n                for (int c = 0; c < clusterCount; c++)\n                {\n                    var mode = new double[categories];\n                    double peakPos = (double)(c * categories) / clusterCount;\n                    for (int k = 0; k < categories; k++)\n                    {\n                        double dist = Math.Abs(k - peakPos);\n                        mode[k] = Math.Exp(-dist * dist / 4.0);\n                    }\n                    Normalize(mode);\n                    modes[c] = mode;\n                }\n            }\n\n            int idx = 0;\n            for (int c = 0; c < clusterCount; c++)\n            {\n                var alpha = new double[categories];\n                for (int k = 0; k < categories; k++)\n                    alpha[k] = concentration * modes[c][k] + 1e-3;\n                for (int p = 0; p < pointsPerCluster; p++)\n                {\n                    features[idx] = SampleDirichlet(alpha, rng);\n                    labels[idx] = c;\n                    idx++;\n                }\n            }\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = clusterCount,\n                Parameters = new Dictionary<string, object>\n                {\n                    [\"clusterCount\"] = clusterCount,\n                    [\"pointsPerCluster\"] = pointsPerCluster,\n                    [\"categories\"] = categories,\n                    [\"disjointSupports\"] = disjointSupports,\n                    [\"concentration\"] = concentration,\n                    [\"seed\"] = seed\n                }\n            };\n        }\n    }\n} |
70 | synthetic/SparseSupports.cs | {3024 289 0.3849 4.0995} | 3121 | using System;\nusing System.Collections.Generic;\n\nnamespace SyntheticDatasets\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // SparseSupports — high-D sparse vectors with configurable support overlap\n        // =====================================================================\n        /// <summary>\n        /// Each cluster lives on its own contiguous range of feature\n        /// dimensions (\"support\"), with configurable overlap between adjacent\n        /// clusters. Feature values are uniform [0.5, 1.5] inside the support\n        /// and exactly zero outside. Exposes the high-dimensional distance-\n        /// concentration regime; supportOverlap controls how ambiguous cluster\n        /// boundaries are.\n        /// </summary>\n        public static SyntheticDataset GenerateSparseSupports(\n            int clusterCount = 4,\n            int pointsPerCluster = 50,\n            int featureDim = 500,\n            int supportSize = 50,\n            int supportOverlap = 10,\n            int seed = 42)\n        {\n            if (supportOverlap >= supportSize)\n                throw new ArgumentException(\"supportOverlap must be less than supportSize.\");\n            int stride = supportSize - supportOverlap;\n            int maxStart = (clusterCount - 1) * stride + supportSize;\n            if (maxStart > featureDim)\n                throw new ArgumentException(\n                    $\"featureDim ({featureDim}) too small for {clusterCount} clusters \" +\n                    $\"with supportSize {supportSize} and overlap {supportOverlap}. \" +\n                    $\"Need at least {maxStart}.\");\n\n            var rng = new Random(seed);\n            int n = clusterCount * pointsPerCluster;\n            var features = new double[n][];\n            var labels = new int[n];\n\n            int idx = 0;\n            for (int c = 0; c < clusterCount; c++)\n            {\n                int supportStart = c * stride;\n                for (int p = 0; p < pointsPerCluster; p++)\n                {\n                    var point = new double[featureDim];\n                    for (int d = supportStart; d < supportStart + supportSize; d++)\n                        point[d] = 0.5 + rng.NextDouble();\n                    features[idx] = point;\n                    labels[idx] = c;\n                    idx++;\n                }\n            }\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = clusterCount,\n                Parameters = new Dictionary<string, object>\n                {\n                    [\"clusterCount\"] = clusterCount,\n                    [\"pointsPerCluster\"] = pointsPerCluster,\n                    [\"featureDim\"] = featureDim,\n                    [\"supportSize\"] = supportSize,\n                    [\"supportOverlap\"] = supportOverlap,\n                    [\"seed\"] = seed\n                }\n            };\n        }\n    }\n} |
71 | synthetic/SpatialBlobs.cs | {2329 216 0.3993 3.9655} | 2406 | using System;\nusing System.Collections.Generic;\n\nnamespace SyntheticDatasets\n{\n    public static partial class SyntheticData\n    {\n        // =====================================================================\n        // SpatialBlobs — Gaussian clusters with enforced separation\n        // =====================================================================\n        /// <summary>\n        /// Compact Gaussian clusters arranged on a hypersphere so inter-cluster\n        /// gaps exceed intra-cluster spread. Classic \"obvious blobs\" case:\n        /// clear density voids between clusters, no manifold structure,\n        /// isotropic covariance within each cluster.\n        /// </summary>\n        public static SyntheticDataset GenerateSpatialBlobs(\n            int clusterCount = 4,\n            int pointsPerCluster = 50,\n            int dimensions = 2,\n            double separation = 5.0,\n            double spread = 0.5,\n            int seed = 42)\n        {\n            var rng = new Random(seed);\n            int n = clusterCount * pointsPerCluster;\n            var features = new double[n][];\n            var labels = new int[n];\n            var centroids = PlaceCentroidsOnSphere(clusterCount, dimensions, separation, rng);\n\n            int idx = 0;\n            for (int c = 0; c < clusterCount; c++)\n            {\n                for (int p = 0; p < pointsPerCluster; p++)\n                {\n                    var point = new double[dimensions];\n                    for (int d = 0; d < dimensions; d++)\n                        point[d] = centroids[c][d] + spread * SampleStandardNormal(rng);\n                    features[idx] = point;\n                    labels[idx] = c;\n                    idx++;\n                }\n            }\n\n            return new SyntheticDataset\n            {\n                Features = features,\n                Labels = labels,\n                ClusterCount = clusterCount,\n                Parameters = new Dictionary<string, object>\n                {\n                    [\"clusterCount\"] = clusterCount,\n                    [\"pointsPerCluster\"] = pointsPerCluster,\n                    [\"dimensions\"] = dimensions,\n                    [\"separation\"] = separation,\n                    [\"spread\"] = spread,\n                    [\"seed\"] = seed\n                }\n            };\n        }\n    }\n} |
