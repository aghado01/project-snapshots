idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
168 | repo-audit/NuGetResolver.cs | {10517 772 0.3189 4.5320} | 10930 | using System;\nusing System.Collections.Generic;\nusing System.Diagnostics;\nusing System.IO;\nusing System.Linq;\nusing System.Xml.Linq;\n\nnamespace RepoAudit;\n\npublic static class NuGetResolver\n{\n\n    private static readonly string[] TfmChain =\n    [\n        \"net10.0\", \"net9.0\", \"net8.0\", \"net7.0\", \"net6.0\", \"net5.0\",\n        \"netcoreapp3.1\", \"netcoreapp3.0\", \"netcoreapp2.1\", \"netcoreapp2.0\",\n        \"netstandard2.1\", \"netstandard2.0\"\n    ];\n\n    public static string GetDefaultCacheDir()\n    {\n        if (TryResolveCacheDir(Environment.GetEnvironmentVariable(\"NUGET_PACKAGES\"), out var dir))\n            return dir;\n\n        if (TryResolveCacheDir(TryGetCacheDirFromDotnetCliHome(), out dir))\n            return dir;\n\n        if (TryResolveCacheDir(TryGetCacheDirFromDotnetRoot(), out dir))\n            return dir;\n\n        if (TryResolveCacheDir(Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), \".nuget\", \"packages\"), out dir))\n            return dir;\n\n        if (TryResolveCacheDir(TryGetCacheDirFromProcessPath(), out dir))\n            return dir;\n\n        return Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), \".nuget\", \"packages\");\n    }\n\n    private static bool TryResolveCacheDir(string? candidate, out string resolved)\n    {\n        if (!string.IsNullOrWhiteSpace(candidate) && Directory.Exists(candidate))\n        {\n            resolved = Path.GetFullPath(candidate);\n            return true;\n        }\n\n        resolved = string.Empty;\n        return false;\n    }\n\n    private static string? TryGetCacheDirFromDotnetCliHome()\n    {\n        string? cliHome = Environment.GetEnvironmentVariable(\"DOTNET_CLI_HOME\");\n        if (string.IsNullOrWhiteSpace(cliHome))\n            return null;\n        return Path.Combine(cliHome, \".nuget\", \"packages\");\n    }\n\n    private static string? TryGetCacheDirFromDotnetRoot()\n    {\n        string? dotnetRoot = TryGetDotnetRootFromEnvironment();\n        if (string.IsNullOrWhiteSpace(dotnetRoot))\n            return null;\n\n        var candidate = Path.GetFullPath(Path.Combine(dotnetRoot, \"..\", \"cli-home\", \".nuget\", \"packages\"));\n        return Directory.Exists(candidate) ? candidate : null;\n    }\n\n    private static string? TryGetDotnetRootFromEnvironment()\n    {\n        string? dotnetRoot = Environment.GetEnvironmentVariable(\"DOTNET_ROOT\");\n        if (!string.IsNullOrWhiteSpace(dotnetRoot) && Directory.Exists(dotnetRoot))\n            return dotnetRoot;\n\n        string? installDir = Environment.GetEnvironmentVariable(\"DOTNET_INSTALL_DIR\");\n        if (!string.IsNullOrWhiteSpace(installDir) && Directory.Exists(installDir))\n            return installDir;\n\n        return null;\n    }\n\n    private static string? TryGetCacheDirFromProcessPath()\n    {\n        try\n        {\n            string? mainModule = Process.GetCurrentProcess().MainModule?.FileName;\n            var seedPaths = new List<string?>\n            {\n                mainModule is null ? null : Path.GetDirectoryName(mainModule),\n                AppContext.BaseDirectory,\n                Environment.CurrentDirectory,\n            };\n\n            foreach (string? seed in seedPaths)\n            {\n                if (string.IsNullOrWhiteSpace(seed))\n                    continue;\n\n                var dir = new DirectoryInfo(seed);\n                while (dir != null)\n                {\n                    var candidate1 = Path.Combine(dir.FullName, \".nuget\", \"packages\");\n                    if (Directory.Exists(candidate1))\n                        return candidate1;\n\n                    var candidate2 = Path.Combine(dir.FullName, \"cli-home\", \".nuget\", \"packages\");\n                    if (Directory.Exists(candidate2))\n                        return candidate2;\n\n                    if (dir.Parent != null)\n                    {\n                        var candidate3 = Path.Combine(dir.Parent.FullName, \"dotnet\", \"cli-home\", \".nuget\", \"packages\");\n                        if (Directory.Exists(candidate3))\n                            return candidate3;\n                    }\n\n                    dir = dir.Parent;\n                }\n            }\n        }\n        catch\n        {\n\n        }\n\n        return null;\n    }\n\n    public static IReadOnlyList<string> Resolve(\n        PackageRef pkg, string cacheDir, string targetFramework = \"net10.0\")\n    {\n        var results = new List<string>();\n        ResolvePackage(pkg, cacheDir, targetFramework, results, new HashSet<string>(StringComparer.OrdinalIgnoreCase));\n        return results.AsReadOnly();\n    }\n\n    private static void ResolvePackage(\n        PackageRef pkg,\n        string cacheDir,\n        string targetFramework,\n        List<string> resolved,\n        HashSet<string> visited)\n    {\n        string pkgKey = $\"{pkg.Name.ToLowerInvariant()}|{pkg.Version}\";\n        if (!visited.Add(pkgKey))\n            return;\n\n        string pkgDir = Path.Combine(cacheDir, pkg.Name.ToLowerInvariant());\n        if (!Directory.Exists(pkgDir))\n            return;\n\n        string? versionDir = FindVersionDir(pkgDir, pkg.Version);\n        if (versionDir == null)\n            return;\n\n        string libDir = Path.Combine(versionDir, \"lib\");\n        if (Directory.Exists(libDir))\n        {\n            string? tfmDir = FindTfmDir(libDir, targetFramework);\n            if (tfmDir != null)\n            {\n                foreach (string dll in Directory.GetFiles(tfmDir, \"*.dll\")\n                    .Where(f => !f.EndsWith(\".resources.dll\", StringComparison.OrdinalIgnoreCase)))\n                {\n                    if (!resolved.Contains(dll, StringComparer.OrdinalIgnoreCase))\n                        resolved.Add(dll);\n                }\n            }\n        }\n\n        foreach (PackageRef dep in FindPackageDependencies(versionDir, targetFramework))\n            ResolvePackage(dep, cacheDir, targetFramework, resolved, visited);\n    }\n\n    private static IReadOnlyList<PackageRef> FindPackageDependencies(string versionDir, string targetFramework)\n    {\n        string? nuspecPath = Directory.GetFiles(versionDir, \"*.nuspec\").FirstOrDefault();\n        if (nuspecPath is null)\n            return Array.Empty<PackageRef>();\n\n        try\n        {\n            var doc = XDocument.Load(nuspecPath);\n            var metadata = doc.Root?.Element(\"metadata\");\n            if (metadata is null)\n                return Array.Empty<PackageRef>();\n\n            var dependencyGroups = metadata.Element(\"dependencies\")?.Elements(\"group\").ToArray();\n            IEnumerable<XElement> dependencyElements;\n\n            if (dependencyGroups != null && dependencyGroups.Length > 0)\n            {\n                XElement? group = dependencyGroups\n                    .FirstOrDefault(g => string.Equals(\n                        (string?)g.Attribute(\"targetFramework\"),\n                        targetFramework,\n                        StringComparison.OrdinalIgnoreCase));\n\n                if (group == null)\n                    group = dependencyGroups.FirstOrDefault(g => g.Attribute(\"targetFramework\") == null);\n\n                dependencyElements = group?.Elements(\"dependency\") ?? Enumerable.Empty<XElement>();\n            }\n            else\n            {\n                dependencyElements = metadata.Element(\"dependencies\")?.Elements(\"dependency\") ?? Enumerable.Empty<XElement>();\n            }\n\n            return dependencyElements\n                .Select(e => new PackageRef(\n                    (string?)e.Attribute(\"id\") ?? string.Empty,\n                    NormalizeVersionSpecifier((string?)e.Attribute(\"version\") ?? \"*\")))\n                .Where(pkg => !string.IsNullOrWhiteSpace(pkg.Name))\n                .ToArray();\n        }\n        catch\n        {\n            return Array.Empty<PackageRef>();\n        }\n    }\n\n    private static string NormalizeVersionSpecifier(string version)\n    {\n        string trimmed = version.Trim();\n        if (trimmed.StartsWith(\"[\") || trimmed.StartsWith(\"(\") || trimmed.StartsWith(\"*\"))\n        {\n            trimmed = trimmed.Trim('[', ']', '(', ')');\n            int commaIndex = trimmed.IndexOf(',');\n            if (commaIndex >= 0)\n                trimmed = trimmed.Substring(0, commaIndex).Trim();\n        }\n        return string.IsNullOrEmpty(trimmed) ? \"*\" : trimmed;\n    }\n\n    private static string? FindVersionDir(string pkgDir, string versionGlob)\n    {\n        if (!versionGlob.Contains('*') && !versionGlob.Contains('?'))\n        {\n            string exact = Path.Combine(pkgDir, versionGlob);\n            return Directory.Exists(exact) ? exact : null;\n        }\n\n        return Directory.EnumerateDirectories(pkgDir)\n            .Where(d => VersionMatchesGlob(Path.GetFileName(d), versionGlob))\n            .OrderByDescending(d => Path.GetFileName(d), VersionComparer.Instance)\n            .FirstOrDefault();\n    }\n\n    private static string? FindTfmDir(string libDir, string targetFramework)\n    {\n        var candidates = new List<string> { targetFramework };\n        foreach (string tfm in TfmChain)\n            if (!candidates.Contains(tfm, StringComparer.OrdinalIgnoreCase))\n                candidates.Add(tfm);\n\n        foreach (string tfm in candidates)\n        {\n            string dir = Path.Combine(libDir, tfm);\n            if (Directory.Exists(dir)) return dir;\n        }\n        return null;\n    }\n\n    private static bool VersionMatchesGlob(string version, string glob)\n    {\n        string[] gParts = glob.Split('.');\n        string[] vParts = version.Split('.');\n        for (int i = 0; i < gParts.Length; i++)\n        {\n            if (gParts[i] == \"*\") return true;\n            if (i >= vParts.Length) return false;\n            if (!gParts[i].Equals(vParts[i], StringComparison.OrdinalIgnoreCase)) return false;\n        }\n        return true;\n    }\n\n    private sealed class VersionComparer : IComparer<string>\n    {\n        public static readonly VersionComparer Instance = new();\n\n        public int Compare(string? x, string? y)\n        {\n            if (x == y) return 0;\n            int[] px = Parse(x), py = Parse(y);\n            int len = Math.Max(px.Length, py.Length);\n            for (int i = 0; i < len; i++)\n            {\n                int va = i < px.Length ? px[i] : 0;\n                int vb = i < py.Length ? py[i] : 0;\n                if (va != vb) return va.CompareTo(vb);\n            }\n            return 0;\n        }\n\n        private static int[] Parse(string? s) =>\n            s?.Split('.').Select(p => int.TryParse(p, out int n) ? n : 0).ToArray()\n            ?? Array.Empty<int>();\n    }\n}
169 | repo-audit/RunAudit.cs | {12824 1052 0.2706 4.7198} | 13371 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Linq;\n\nnamespace RepoAudit;\n\nint Main(string[] args)\n{\n    string repoRoot = Directory.GetCurrentDirectory();\n    string? impactNs = null;\n    bool validate = false;\n    bool useGit = true;\n    bool semantic = true;\n    bool link = false;\n\n    for (int i = 0; i < args.Length; i++)\n    {\n        switch (args[i])\n        {\n            case \"--impact\" when i + 1 < args.Length:\n                impactNs = args[++i];\n                break;\n            case \"--validate\":\n                validate = true;\n                break;\n            case \"--no-git\":\n                useGit = false;\n                break;\n            case \"--semantic\":\n                semantic = true;\n                break;\n            case \"--no-semantic\":\n                semantic = false;\n                break;\n            case \"--link\":\n                link = true;\n                break;\n            default:\n                if (!args[i].StartsWith(\"--\"))\n                    repoRoot = Path.GetFullPath(args[i]);\n                else\n                {\n                    Console.Error.WriteLine($\"Unknown option: {args[i]}\");\n                    return 2;\n                }\n                break;\n        }\n    }\n\n    string srcRoot = Path.Combine(repoRoot, \"src\");\n    string projectsRoot = Path.Combine(repoRoot, \"projects\");\n\n    if (!Directory.Exists(srcRoot))\n    {\n        Console.Error.WriteLine($\"src not found under {repoRoot}\");\n        return 1;\n    }\n\n    Console.WriteLine(\"Crawling \" + srcRoot);\n    var crawl = new FileSystemCrawler(srcRoot).Invoke();\n    Console.WriteLine($\"{crawl.FileCount} files in {crawl.DirectoryCount} directories\" +\n        (crawl.Skipped.Count > 0 ? $\", {crawl.Skipped.Count} skipped\" : \"\"));\n\n    string[] csprojFiles = Directory.Exists(projectsRoot)\n        ? Directory.GetFiles(projectsRoot, \"*.csproj\", SearchOption.AllDirectories)\n        : Array.Empty<string>();\n\n    Console.WriteLine($\"{csprojFiles.Length} project files\");\n\n    var allGlobs = csprojFiles.Select(CsprojParser.Parse).ToList();\n    foreach (var g in allGlobs)\n    {\n        Console.WriteLine($\"DEBUG Parsed csproj: {g.CsprojPath}\");\n        Console.WriteLine(g.ProjectReferences.Count > 0\n            ? $\"DEBUG ProjectReferences: {string.Join(\", \", g.ProjectReferences)}\"\n            : \"DEBUG ProjectReferences: none\");\n    }\n\n    var ownership = FileOwnership.Build(allGlobs, crawl);\n    var globsByPath = allGlobs.ToDictionary(g => g.CsprojPath, StringComparer.OrdinalIgnoreCase);\n\n    Console.WriteLine(\"syntax...\");\n    var analyses = ownership.Projects\n        .Where(p => globsByPath.ContainsKey(p.CsprojPath))\n        .Select(p => ProjectAnalyzer.Analyze(p, globsByPath[p.CsprojPath]))\n        .ToList();\n\n    var xref = CrossReference.Build(analyses);\n\n    if (useGit)\n    {\n        var renames = GitRenameDetector.TryGetRenames(repoRoot);\n        var renameGaps = GitRenameDetector.DetectRenameGaps(ownership, renames, repoRoot);\n        if (renameGaps.Count > 0)\n            xref = xref with { Violations = xref.Violations.Concat(renameGaps).ToList().AsReadOnly() };\n    }\n\n    var driftViolations = CrossReference.DetectNamespaceDrift(analyses, srcRoot);\n    if (driftViolations.Count > 0)\n        xref = xref with { Violations = xref.Violations.Concat(driftViolations).ToList().AsReadOnly() };\n\n    var orphanViolations = CrossReference.DetectPartialOrphans(analyses);\n    if (orphanViolations.Count > 0)\n        xref = xref with { Violations = xref.Violations.Concat(orphanViolations).ToList().AsReadOnly() };\n\n    if (impactNs != null)\n    {\n        RunImpact(impactNs, analyses, xref, repoRoot);\n        return 0;\n    }\n\n    Console.WriteLine(\"--- Type Ontology ---\");\n    foreach (var a in analyses.Where(a => a.Types.Count > 0).OrderBy(a => a.AssemblyName))\n    {\n        Console.WriteLine($\"\\n  {a.AssemblyName}  [{string.Join(\", \", a.DeclaredNamespaces)}]\");\n        foreach (var nsGroup in a.Types.GroupBy(t => t.ContainingNamespace).OrderBy(g => g.Key))\n        {\n            if (nsGroup.Key.Length > 0) Console.WriteLine($\"    {nsGroup.Key}\");\n            foreach (var t in nsGroup.OrderBy(t => t.Name))\n            {\n                string mods = string.Join(\" \", t.Modifiers.Where(m => m != \"partial\" && m != \"public\" && m != \"internal\"));\n                string arity = t.GenericArity > 0 ? $\"<T×{t.GenericArity}>\" : \"\";\n                string partial = t.IsPartial ? $\" partial({t.ContributingFiles.Count} files)\" : \"\";\n                string bases = t.BaseTypes.Count > 0 ? \" : \" + string.Join(\", \", t.BaseTypes) : \"\";\n                Console.WriteLine($\"      {(mods.Length > 0 ? mods + \" \" : \"\")}{t.Kind} {t.Name}{arity}{bases}{partial}\");\n            }\n        }\n    }\n\n    if (xref.Violations.Count > 0)\n    {\n        Console.WriteLine($\"\\n--- Violations ({xref.Violations.Count}) ---\");\n        foreach (var group in xref.Violations.GroupBy(v => v.Kind).OrderBy(g => g.Key.ToString()))\n        {\n            Console.WriteLine($\"\\n  {group.Key}\");\n            foreach (var v in group.OrderBy(v => v.Project))\n                Console.WriteLine($\"    {v.Project}: {v.Detail}\");\n        }\n        Console.WriteLine($\"\\n{xref.Violations.Count} cross-reference violations.\");\n    }\n\n    if (ownership.OrphanedFiles.Count > 0)\n    {\n        Console.WriteLine($\"\\n--- Orphaned files ({ownership.OrphanedFiles.Count}) ---\");\n        foreach (string f in ownership.OrphanedFiles.OrderBy(x => x))\n            Console.WriteLine($\"  {f}\");\n    }\n\n    string artifactsDir = Path.Combine(repoRoot, \"artifacts\");\n    var semanticOutcome = SemanticAnalysisOutcome.Disabled;\n    IReadOnlyList<ProjectDiagnostics>? semanticResults = null;\n    string? semanticFailurePath = null;\n\n    if (semantic)\n    {\n        var sorted = CrossReference.TopologicalSort(analyses);\n        Console.WriteLine($\"\\n--- Semantic Analysis ({sorted.Count} projects) ---\");\n        try\n        {\n            semanticResults = InProcessCompiler.Compile(sorted, progress: Console.WriteLine);\n            int totalErrors   = semanticResults.Sum(r => r.Errors.Count);\n            int totalWarnings = semanticResults.Sum(r => r.Warnings.Count);\n            int cleanProjects = semanticResults.Count(r => r.Errors.Count == 0 && r.Warnings.Count == 0);\n\n            Console.WriteLine($\"{cleanProjects}/{sorted.Count} clean\" +\n                (totalErrors   > 0 ? $\", {totalErrors} error{(totalErrors   != 1 ? \"s\" : \"\")}\" : \"\") +\n                (totalWarnings > 0 ? $\", {totalWarnings} warning{(totalWarnings != 1 ? \"s\" : \"\")}\" : \"\"));\n\n            foreach (var pd in semanticResults.Where(r => r.Errors.Count + r.Warnings.Count > 0))\n            {\n                foreach (var d in pd.Errors.Concat(pd.Warnings).OrderBy(d => d.FilePath).ThenBy(d => d.Line))\n                {\n                    string loc = d.FilePath != null\n                        ? $\"{Path.GetRelativePath(repoRoot, d.FilePath).Replace('\\\\', '/')}:{d.Line}:{d.Column}\"\n                        : \"no location\";\n                    Console.WriteLine($\"  {d.Id} {d.Severity,-7} {loc}\");\n                    Console.WriteLine($\"    {d.Message}\");\n                }\n            }\n\n            string semanticPath = ArtifactWriter.WriteSemantic(artifactsDir, repoRoot, semanticResults);\n            Console.WriteLine(Path.GetRelativePath(repoRoot, semanticPath).Replace('\\\\', '/'));\n\n            semanticOutcome = totalErrors == 0 && totalWarnings == 0\n                ? SemanticAnalysisOutcome.Succeeded\n                : SemanticAnalysisOutcome.CompletedWithIssues;\n        }\n        catch (Exception ex)\n        {\n            semanticOutcome = SemanticAnalysisOutcome.Failed;\n            semanticFailurePath = ArtifactWriter.WriteSemanticFailure(artifactsDir, repoRoot, ex);\n            Console.WriteLine($\"analysis failed: {ex.Message}\");\n            Console.WriteLine(Path.GetRelativePath(repoRoot, semanticFailurePath).Replace('\\\\', '/'));\n            Console.WriteLine(\"Open the failure trace to inspect exception details and captured output.\");\n            Console.WriteLine(\"After fixing the root cause, rerun with --validate to verify the fix.\");\n            if (validate) { Console.Error.WriteLine(\"FAILED: semantic analysis failure detected.\"); return 1; }\n        }\n    }\n\n    if (validate && semanticResults != null && semanticResults.Any(r => r.Errors.Count > 0))\n    {\n        int n = semanticResults.Sum(r => r.Errors.Count);\n        Console.Error.WriteLine($\"FAILED: {n} compilation error{(n != 1 ? \"s\" : \"\")} detected.\");\n        return 1;\n    }\n\n    var (jsonPath, healthPath, orientationPath) = ArtifactWriter.Write(\n        artifactsDir, repoRoot, analyses, ownership, xref);\n\n    Console.WriteLine(\"\\nwritten:\");\n    Console.WriteLine(\"  \" + Path.GetRelativePath(repoRoot, jsonPath).Replace('\\\\', '/'));\n    Console.WriteLine(\"  \" + Path.GetRelativePath(repoRoot, healthPath).Replace('\\\\', '/'));\n    Console.WriteLine(\"  \" + Path.GetRelativePath(repoRoot, orientationPath).Replace('\\\\', '/'));\n\n    Console.WriteLine(\"\\nNext steps:\");\n    Console.WriteLine(\"  1  Review artifacts/project-health.md for remediation guidance.\");\n    Console.WriteLine(\"  2  Inspect artifacts/semantic-diagnostics.json if semantic analysis ran.\");\n    Console.WriteLine(\"  3  Open artifacts/ASSISTANTORIENTATION.md to orient future analysis.\");\n    Console.WriteLine(\"\\nQuick read commands:\");\n    Console.WriteLine(\"  code artifacts/project-health.md\");\n    Console.WriteLine(\"  type artifacts\\\\project-health.md\");\n    Console.WriteLine(\"  pwsh -NoProfile -Command \\\"Get-Content artifacts/project-health.md | Select-Object -First 40\\\"\");\n\n    if (link)\n    {\n        var linkage = SymbolLinker.BuildLinkage(analyses);\n        Console.WriteLine(\"\\n--- Linking Analysis ---\");\n        if (linkage.Count == 0)\n        {\n            Console.WriteLine(\"no projects with project references found\");\n        }\n        else\n        {\n            foreach (var pl in linkage.OrderBy(l => l.Assembly, StringComparer.Ordinal))\n            {\n                Console.WriteLine(pl.Assembly);\n                foreach (var dep in pl.Dependencies.OrderBy(d => d.ReferencedAssembly, StringComparer.Ordinal))\n                {\n                    Console.WriteLine(dep.IsTypeEffective\n                        ? $\"  {dep.ReferencedAssembly,-40} {string.Join(\", \", dep.UsedTypes)}\"\n                        : $\"  {dep.ReferencedAssembly,-40} no type-level references detected\");\n                }\n            }\n        }\n\n        var unlinked = SymbolLinker.FindEffectivelyUnlinkedProjects(linkage);\n        if (unlinked.Count > 0)\n        {\n            Console.WriteLine(\"Projects with no type-level connections to any reference:\");\n            foreach (string name in unlinked) Console.WriteLine($\"  {name}\");\n        }\n    }\n\n    if (validate && xref.Violations.Count > 0)\n    {\n        Console.Error.WriteLine($\"FAILED: {xref.Violations.Count} violations detected.\");\n        return 1;\n    }\n\n    return 0;\n}\n\nvoid RunImpact(string ns, IReadOnlyList<ProjectAnalysis> analyses,\n               CrossReferenceIndex xref, string repoRoot)\n{\n    Console.WriteLine($\"Impact analysis for: {ns}\");\n\n    var declarers = analyses\n        .Where(a => a.DeclaredNamespaces.Contains(ns))\n        .OrderBy(a => a.AssemblyName)\n        .ToList();\n\n    if (declarers.Count == 0)\n    {\n        Console.WriteLine($\"Namespace {ns} is not declared in any project.\");\n        return;\n    }\n\n    Console.WriteLine(\"Declared in:\");\n    foreach (var a in declarers)\n    {\n        var files = a.FileAnalyses\n            .Where(f => f.DeclaredNamespaces.Contains(ns))\n            .Select(f => Path.GetRelativePath(repoRoot, f.FilePath).Replace('\\\\', '/'))\n            .OrderBy(f => f)\n            .ToList();\n        Console.WriteLine($\"  {a.AssemblyName} ({files.Count} file{(files.Count != 1 ? \"s\" : \"\")})\");\n        foreach (string f in files) Console.WriteLine($\"    {f}\");\n    }\n\n    if (!xref.ImportersOf.TryGetValue(ns, out var importerNames) || importerNames.Count == 0)\n    {\n        Console.WriteLine(\"Imported by: none\");\n        return;\n    }\n\n    var importerLookup = analyses.ToDictionary(a => a.AssemblyName, StringComparer.Ordinal);\n    Console.WriteLine(\"Imported by:\");\n    foreach (string name in importerNames.OrderBy(n => n))\n    {\n        if (!importerLookup.TryGetValue(name, out var a)) continue;\n        var files = a.FileAnalyses\n            .Where(f => f.ImportedNamespaces.Contains(ns))\n            .Select(f => Path.GetRelativePath(repoRoot, f.FilePath).Replace('\\\\', '/'))\n            .OrderBy(f => f)\n            .ToList();\n        Console.WriteLine($\"  {name} ({files.Count} file{(files.Count != 1 ? \"s\" : \"\")})\");\n        foreach (string f in files) Console.WriteLine($\"    {f}\");\n    }\n}\n\nreturn Main(args);
170 | repo-audit/SymbolLinker.cs | {2371 165 0.2855 4.5743} | 2438 | using System.Collections.Generic;\nusing System.Linq;\n\nnamespace RepoAudit;\n\npublic sealed record LinkedDependency(\n    string ReferencedAssembly,\n    IReadOnlyList<string> UsedTypes,\n    bool IsTypeEffective);   // true when UsedTypes.Count > 0\n\npublic sealed record ProjectLinkage(\n    string Assembly,\n    IReadOnlyList<LinkedDependency> Dependencies);\n\npublic static class SymbolLinker\n{\n\n    public static IReadOnlyList<ProjectLinkage> BuildLinkage(\n        IReadOnlyList<ProjectAnalysis> projects)\n    {\n\n        var typesByAssembly = projects.ToDictionary(\n            p => p.AssemblyName,\n            p => (IReadOnlySet<string>) new HashSet<string>(\n                p.Types.Select(t => t.Name), System.StringComparer.Ordinal),\n            System.StringComparer.Ordinal);\n\n        var result = new List<ProjectLinkage>();\n\n        foreach (ProjectAnalysis project in projects)\n        {\n            if (project.ProjectReferences.Count == 0) continue;\n\n            var allRefNames = new HashSet<string>(System.StringComparer.Ordinal);\n            foreach (FileAnalysis fa in project.FileAnalyses)\n                foreach (string name in fa.ReferencedTypeNames)\n                    allRefNames.Add(name);\n\n            var deps = new List<LinkedDependency>(project.ProjectReferences.Count);\n            foreach (string refAssembly in project.ProjectReferences)\n            {\n                if (!typesByAssembly.TryGetValue(refAssembly, out IReadOnlySet<string>? aTypes))\n                    continue;\n\n                var used = allRefNames\n                    .Where(n => aTypes.Contains(n))\n                    .OrderBy(n => n, System.StringComparer.Ordinal)\n                    .ToList();\n\n                deps.Add(new LinkedDependency(refAssembly, used.AsReadOnly(), used.Count > 0));\n            }\n\n            if (deps.Count > 0)\n                result.Add(new ProjectLinkage(project.AssemblyName, deps.AsReadOnly()));\n        }\n\n        return result.AsReadOnly();\n    }\n\n    public static IReadOnlyList<string> FindEffectivelyUnlinkedProjects(\n        IReadOnlyList<ProjectLinkage> linkage) =>\n        linkage\n            .Where(pl => pl.Dependencies.Count > 0 && pl.Dependencies.All(d => !d.IsTypeEffective))\n            .Select(pl => pl.Assembly)\n            .OrderBy(n => n, System.StringComparer.Ordinal)\n            .ToList()\n            .AsReadOnly();\n}
