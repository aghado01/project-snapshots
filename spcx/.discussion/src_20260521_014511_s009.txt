idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
55 | code-analysis/NuGetResolver.cs | {3765 336 0.2869 4.6664} | 3913 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Linq;\n\nnamespace CodeAnalysis;\n\npublic static class NuGetResolver\n{\n\n    private static readonly string[] TfmChain =\n    [\n        \"net10.0\", \"net9.0\", \"net8.0\", \"net7.0\", \"net6.0\", \"net5.0\",\n        \"netcoreapp3.1\", \"netcoreapp3.0\", \"netcoreapp2.1\", \"netcoreapp2.0\",\n        \"netstandard2.1\", \"netstandard2.0\"\n    ];\n\n    public static string GetDefaultCacheDir() =>\n        Environment.GetEnvironmentVariable(\"NUGET_PACKAGES\")\n        ?? Path.Combine(\n            Environment.GetFolderPath(Environment.SpecialFolder.UserProfile),\n            \".nuget\", \"packages\");\n\n    public static IReadOnlyList<string> Resolve(\n        PackageRef pkg, string cacheDir, string targetFramework = \"net10.0\")\n    {\n        string pkgDir = Path.Combine(cacheDir, pkg.Name.ToLowerInvariant());\n        if (!Directory.Exists(pkgDir)) return Array.Empty<string>();\n\n        string? versionDir = FindVersionDir(pkgDir, pkg.Version);\n        if (versionDir == null) return Array.Empty<string>();\n\n        string libDir = Path.Combine(versionDir, \"lib\");\n        if (!Directory.Exists(libDir)) return Array.Empty<string>();\n\n        string? tfmDir = FindTfmDir(libDir, targetFramework);\n        if (tfmDir == null) return Array.Empty<string>();\n\n        return Directory.GetFiles(tfmDir, \"*.dll\")\n            .Where(f => !f.EndsWith(\".resources.dll\", StringComparison.OrdinalIgnoreCase))\n            .ToList()\n            .AsReadOnly();\n    }\n\n    private static string? FindVersionDir(string pkgDir, string versionGlob)\n    {\n        if (!versionGlob.Contains('*') && !versionGlob.Contains('?'))\n        {\n            string exact = Path.Combine(pkgDir, versionGlob);\n            return Directory.Exists(exact) ? exact : null;\n        }\n\n        return Directory.EnumerateDirectories(pkgDir)\n            .Where(d => VersionMatchesGlob(Path.GetFileName(d), versionGlob))\n            .OrderByDescending(d => Path.GetFileName(d), VersionComparer.Instance)\n            .FirstOrDefault();\n    }\n\n    private static string? FindTfmDir(string libDir, string targetFramework)\n    {\n        var candidates = new List<string> { targetFramework };\n        foreach (string tfm in TfmChain)\n            if (!candidates.Contains(tfm, StringComparer.OrdinalIgnoreCase))\n                candidates.Add(tfm);\n\n        foreach (string tfm in candidates)\n        {\n            string dir = Path.Combine(libDir, tfm);\n            if (Directory.Exists(dir)) return dir;\n        }\n        return null;\n    }\n\n    private static bool VersionMatchesGlob(string version, string glob)\n    {\n        string[] gParts = glob.Split('.');\n        string[] vParts = version.Split('.');\n        for (int i = 0; i < gParts.Length; i++)\n        {\n            if (gParts[i] == \"*\") return true;\n            if (i >= vParts.Length) return false;\n            if (!gParts[i].Equals(vParts[i], StringComparison.OrdinalIgnoreCase)) return false;\n        }\n        return true;\n    }\n\n    private sealed class VersionComparer : IComparer<string>\n    {\n        public static readonly VersionComparer Instance = new();\n\n        public int Compare(string? x, string? y)\n        {\n            if (x == y) return 0;\n            int[] px = Parse(x), py = Parse(y);\n            int len = Math.Max(px.Length, py.Length);\n            for (int i = 0; i < len; i++)\n            {\n                int va = i < px.Length ? px[i] : 0;\n                int vb = i < py.Length ? py[i] : 0;\n                if (va != vb) return va.CompareTo(vb);\n            }\n            return 0;\n        }\n\n        private static int[] Parse(string? s) =>\n            s?.Split('.').Select(p => int.TryParse(p, out int n) ? n : 0).ToArray()\n            ?? Array.Empty<int>();\n    }\n} |
56 | code-analysis/Program.cs | {10735 916 0.2291 4.8836} | 11238 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Linq;\nusing CodeAnalysis;\n\nstring repoRoot    = Directory.GetCurrentDirectory();\nstring? impactNs   = null;\nbool    validate   = false;\nbool    useGit     = true;\nbool    semantic   = false;\nbool    link       = false;\n\nfor (int i = 0; i < args.Length; i++)\n{\n    switch (args[i])\n    {\n        case \"--impact\" when i + 1 < args.Length:\n            impactNs = args[++i];\n            break;\n        case \"--validate\":\n            validate = true;\n            break;\n        case \"--no-git\":\n            useGit = false;\n            break;\n        case \"--semantic\":\n            semantic = true;\n            break;\n        case \"--link\":\n            link = true;\n            break;\n        default:\n            if (!args[i].StartsWith(\"--\"))\n                repoRoot = Path.GetFullPath(args[i]);\n            else\n            {\n                Console.Error.WriteLine($\"Unknown option: {args[i]}\");\n                return 2;\n            }\n            break;\n    }\n}\n\nstring srcRoot      = Path.Combine(repoRoot, \"src\");\nstring projectsRoot = Path.Combine(repoRoot, \"projects\");\n\nif (!Directory.Exists(srcRoot))\n{\n    Console.Error.WriteLine($\"src/ not found under: {repoRoot}\");\n    return 1;\n}\n\nConsole.WriteLine($\"Crawling: {srcRoot}\");\nCrawlerResult crawl = new FileSystemCrawler(srcRoot).Invoke();\nConsole.WriteLine($\"  {crawl.FileCount} files in {crawl.DirectoryCount} directories\" +\n                  (crawl.Skipped.Count > 0 ? $\"  ({crawl.Skipped.Count} skipped)\" : \"\"));\n\nstring[] csprojFiles = Directory.Exists(projectsRoot)\n    ? Directory.GetFiles(projectsRoot, \"*.csproj\", SearchOption.AllDirectories)\n    : [];\n\nConsole.WriteLine($\"  {csprojFiles.Length} project files\");\nList<CsprojGlobs> allGlobs = csprojFiles.Select(CsprojParser.Parse).ToList();\nFileOwnershipResult ownership = FileOwnership.Build(allGlobs, crawl);\n\nvar globsByPath = allGlobs.ToDictionary(g => g.CsprojPath, StringComparer.OrdinalIgnoreCase);\n\nConsole.WriteLine(\"\\nAnalyzing syntax...\");\nList<ProjectAnalysis> analyses = ownership.Projects\n    .Where(p => globsByPath.ContainsKey(p.CsprojPath))\n    .Select(p => ProjectAnalyzer.Analyze(p, globsByPath[p.CsprojPath]))\n    .ToList();\n\nCrossReferenceIndex xref = CrossReference.Build(analyses);\n\nif (useGit)\n{\n    IReadOnlyList<GitRename> renames = GitRenameDetector.TryGetRenames(repoRoot);\n    IReadOnlyList<Violation> renameGaps = GitRenameDetector.DetectRenameGaps(ownership, renames, repoRoot);\n    if (renameGaps.Count > 0)\n        xref = xref with { Violations = xref.Violations.Concat(renameGaps).ToList().AsReadOnly() };\n}\n\n{\n    IReadOnlyList<Violation> driftViolations = CrossReference.DetectNamespaceDrift(analyses, srcRoot);\n    if (driftViolations.Count > 0)\n        xref = xref with { Violations = xref.Violations.Concat(driftViolations).ToList().AsReadOnly() };\n}\n\n{\n    IReadOnlyList<Violation> orphanViolations = CrossReference.DetectPartialOrphans(analyses);\n    if (orphanViolations.Count > 0)\n        xref = xref with { Violations = xref.Violations.Concat(orphanViolations).ToList().AsReadOnly() };\n}\n\nif (impactNs != null)\n{\n    RunImpact(impactNs, analyses, xref, repoRoot);\n    return 0;\n}\n\nConsole.WriteLine(\"\\n--- Type Ontology ---\");\nforeach (ProjectAnalysis a in analyses.Where(a => a.Types.Count > 0).OrderBy(a => a.AssemblyName))\n{\n    Console.WriteLine($\"\\n  {a.AssemblyName}  [{string.Join(\", \", a.DeclaredNamespaces)}]\");\n    foreach (IGrouping<string, TypeEntry> nsGroup in a.Types\n        .GroupBy(t => t.ContainingNamespace)\n        .OrderBy(g => g.Key))\n    {\n        if (nsGroup.Key.Length > 0) Console.WriteLine($\"    {nsGroup.Key}\");\n        foreach (TypeEntry t in nsGroup.OrderBy(t => t.Name))\n        {\n            string mods    = string.Join(\" \", t.Modifiers.Where(m => m != \"partial\" && m != \"public\" && m != \"internal\"));\n            string arity   = t.GenericArity > 0 ? $\"<T×{t.GenericArity}>\" : \"\";\n            string partial = t.IsPartial ? $\"  ← partial ({t.ContributingFiles.Count} files)\" : \"\";\n            string bases   = t.BaseTypes.Count > 0 ? $\" : {string.Join(\", \", t.BaseTypes)}\" : \"\";\n            Console.WriteLine($\"      {(mods.Length > 0 ? mods + \" \" : \"\")}{t.Kind} {t.Name}{arity}{bases}{partial}\");\n        }\n    }\n}\n\nif (xref.Violations.Count > 0)\n{\n    Console.WriteLine($\"\\n--- Violations ({xref.Violations.Count}) ---\");\n    foreach (IGrouping<ViolationKind, Violation> group in xref.Violations\n        .GroupBy(v => v.Kind)\n        .OrderBy(g => g.Key.ToString()))\n    {\n        Console.WriteLine($\"\\n  [{group.Key}]\");\n        foreach (Violation v in group.OrderBy(v => v.Project))\n            Console.WriteLine($\"    {v.Project}: {v.Detail}\");\n    }\n}\nelse\n{\n    Console.WriteLine(\"\\nNo cross-reference violations.\");\n}\n\nif (ownership.OrphanedFiles.Count > 0)\n{\n    Console.WriteLine($\"\\n--- Orphaned files ({ownership.OrphanedFiles.Count}) ---\");\n    foreach (string f in ownership.OrphanedFiles.OrderBy(x => x))\n        Console.WriteLine($\"  {f}\");\n}\n\nstring artifactsDir = Path.Combine(repoRoot, \"artifacts\");\nvar (jsonPath, healthPath, orientationPath) = ArtifactWriter.Write(artifactsDir, repoRoot, analyses, ownership, xref);\nConsole.WriteLine($\"\\nArtifacts written:\");\nConsole.WriteLine($\"  {Path.GetRelativePath(repoRoot, jsonPath).Replace('\\\\', '/')}\");\nConsole.WriteLine($\"  {Path.GetRelativePath(repoRoot, healthPath).Replace('\\\\', '/')}\");\nConsole.WriteLine($\"  {Path.GetRelativePath(repoRoot, orientationPath).Replace('\\\\', '/')}\");\n\nif (link)\n{\n    IReadOnlyList<ProjectLinkage> linkage = SymbolLinker.BuildLinkage(analyses);\n\n    Console.WriteLine(\"\\n--- Linking Analysis ---\");\n\n    if (linkage.Count == 0)\n    {\n        Console.WriteLine(\"  (no projects with project references found)\");\n    }\n    else\n    {\n        foreach (ProjectLinkage pl in linkage.OrderBy(l => l.Assembly, StringComparer.Ordinal))\n        {\n            Console.WriteLine($\"\\n  {pl.Assembly}\");\n            foreach (LinkedDependency dep in pl.Dependencies.OrderBy(d => d.ReferencedAssembly, StringComparer.Ordinal))\n            {\n                if (dep.IsTypeEffective)\n                    Console.WriteLine($\"    ← {dep.ReferencedAssembly,-40} [{string.Join(\", \", dep.UsedTypes)}]\");\n                else\n                    Console.WriteLine($\"    ← {dep.ReferencedAssembly,-40} (no type-level references detected)\");\n            }\n        }\n\n        IReadOnlyList<string> unlinked = SymbolLinker.FindEffectivelyUnlinkedProjects(linkage);\n        if (unlinked.Count > 0)\n        {\n            Console.WriteLine($\"\\n  Projects with no type-level connections to any reference:\");\n            foreach (string name in unlinked)\n                Console.WriteLine($\"    {name}\");\n        }\n    }\n}\n\nIReadOnlyList<ProjectDiagnostics>? semanticResults = null;\nif (semantic)\n{\n    IReadOnlyList<ProjectAnalysis> sorted = CrossReference.TopologicalSort(analyses);\n    Console.WriteLine($\"\\n--- Semantic Analysis ({sorted.Count} projects) ---\\n\");\n\n    semanticResults = InProcessCompiler.Compile(sorted, progress: Console.WriteLine);\n\n    int totalErrors   = semanticResults.Sum(r => r.Errors.Count);\n    int totalWarnings = semanticResults.Sum(r => r.Warnings.Count);\n    int cleanProjects = semanticResults.Count(r => r.Errors.Count == 0 && r.Warnings.Count == 0);\n\n    Console.WriteLine($\"\\n  {cleanProjects}/{sorted.Count} clean\" +\n        (totalErrors   > 0 ? $\"  {totalErrors} error{(totalErrors == 1 ? \"\" : \"s\")}\" : \"\") +\n        (totalWarnings > 0 ? $\"  {totalWarnings} warning{(totalWarnings == 1 ? \"\" : \"s\")}\" : \"\"));\n\n    foreach (ProjectDiagnostics pd in semanticResults.Where(r => r.Errors.Count + r.Warnings.Count > 0))\n    {\n        foreach (CompilerDiagnostic d in pd.Errors.Concat(pd.Warnings).OrderBy(d => d.FilePath).ThenBy(d => d.Line))\n        {\n            string loc = d.FilePath != null\n                ? $\"{Path.GetRelativePath(repoRoot, d.FilePath).Replace('\\\\', '/')}:{d.Line}:{d.Column}\"\n                : \"(no location)\";\n            Console.WriteLine($\"    [{d.Id}] {d.Severity,-7}  {loc}\");\n            Console.WriteLine($\"             {d.Message}\");\n        }\n    }\n\n    string semanticPath = ArtifactWriter.WriteSemantic(artifactsDir, repoRoot, semanticResults);\n    Console.WriteLine($\"\\n  {Path.GetRelativePath(repoRoot, semanticPath).Replace('\\\\', '/')}\");\n}\n\nif (validate)\n{\n    if (xref.Violations.Count > 0)\n    {\n        Console.Error.WriteLine($\"\\nValidation failed: {xref.Violations.Count} violation(s) detected.\");\n        return 1;\n    }\n    if (semantic && semanticResults != null && semanticResults.Any(r => r.Errors.Count > 0))\n    {\n        int n = semanticResults.Sum(r => r.Errors.Count);\n        Console.Error.WriteLine($\"\\nValidation failed: {n} compilation error{(n == 1 ? \"\" : \"s\")} detected.\");\n        return 1;\n    }\n}\n\nreturn 0;\n\nstatic void RunImpact(\n    string ns,\n    IReadOnlyList<ProjectAnalysis> analyses,\n    CrossReferenceIndex xref,\n    string repoRoot)\n{\n    Console.WriteLine($\"\\nImpact analysis for '{ns}'\");\n\n    var declarers = analyses\n        .Where(a => a.DeclaredNamespaces.Contains(ns))\n        .OrderBy(a => a.AssemblyName)\n        .ToList();\n\n    if (declarers.Count == 0)\n    {\n        Console.WriteLine($\"  Namespace '{ns}' is not declared in any project.\");\n        return;\n    }\n\n    Console.WriteLine(\"\\nDeclared in:\");\n    foreach (ProjectAnalysis a in declarers)\n    {\n        var files = a.FileAnalyses\n            .Where(f => f.DeclaredNamespaces.Contains(ns))\n            .Select(f => Path.GetRelativePath(repoRoot, f.FilePath).Replace('\\\\', '/'))\n            .OrderBy(f => f)\n            .ToList();\n        Console.WriteLine($\"  {a.AssemblyName} ({files.Count} file{(files.Count == 1 ? \"\" : \"s\")})\");\n        foreach (string f in files)\n            Console.WriteLine($\"    {f}\");\n    }\n\n    if (!xref.ImportersOf.TryGetValue(ns, out IReadOnlyList<string>? importerNames)\n        || importerNames.Count == 0)\n    {\n        Console.WriteLine(\"\\nImported by: (none)\");\n        return;\n    }\n\n    var importerLookup = analyses.ToDictionary(a => a.AssemblyName, StringComparer.Ordinal);\n    Console.WriteLine(\"\\nImported by:\");\n    foreach (string name in importerNames.OrderBy(n => n))\n    {\n        if (!importerLookup.TryGetValue(name, out ProjectAnalysis? a)) continue;\n        var files = a.FileAnalyses\n            .Where(f => f.ImportedNamespaces.Contains(ns))\n            .Select(f => Path.GetRelativePath(repoRoot, f.FilePath).Replace('\\\\', '/'))\n            .OrderBy(f => f)\n            .ToList();\n        Console.WriteLine($\"  {name} ({files.Count} file{(files.Count == 1 ? \"\" : \"s\")})\");\n        foreach (string f in files)\n            Console.WriteLine($\"    {f}\");\n    }\n} |
57 | code-analysis/SymbolLinker.cs | {2374 165 0.2852 4.5739} | 2441 | using System.Collections.Generic;\nusing System.Linq;\n\nnamespace CodeAnalysis;\n\npublic sealed record LinkedDependency(\n    string ReferencedAssembly,\n    IReadOnlyList<string> UsedTypes,\n    bool IsTypeEffective);   // true when UsedTypes.Count > 0\n\npublic sealed record ProjectLinkage(\n    string Assembly,\n    IReadOnlyList<LinkedDependency> Dependencies);\n\npublic static class SymbolLinker\n{\n\n    public static IReadOnlyList<ProjectLinkage> BuildLinkage(\n        IReadOnlyList<ProjectAnalysis> projects)\n    {\n\n        var typesByAssembly = projects.ToDictionary(\n            p => p.AssemblyName,\n            p => (IReadOnlySet<string>) new HashSet<string>(\n                p.Types.Select(t => t.Name), System.StringComparer.Ordinal),\n            System.StringComparer.Ordinal);\n\n        var result = new List<ProjectLinkage>();\n\n        foreach (ProjectAnalysis project in projects)\n        {\n            if (project.ProjectReferences.Count == 0) continue;\n\n            var allRefNames = new HashSet<string>(System.StringComparer.Ordinal);\n            foreach (FileAnalysis fa in project.FileAnalyses)\n                foreach (string name in fa.ReferencedTypeNames)\n                    allRefNames.Add(name);\n\n            var deps = new List<LinkedDependency>(project.ProjectReferences.Count);\n            foreach (string refAssembly in project.ProjectReferences)\n            {\n                if (!typesByAssembly.TryGetValue(refAssembly, out IReadOnlySet<string>? aTypes))\n                    continue;\n\n                var used = allRefNames\n                    .Where(n => aTypes.Contains(n))\n                    .OrderBy(n => n, System.StringComparer.Ordinal)\n                    .ToList();\n\n                deps.Add(new LinkedDependency(refAssembly, used.AsReadOnly(), used.Count > 0));\n            }\n\n            if (deps.Count > 0)\n                result.Add(new ProjectLinkage(project.AssemblyName, deps.AsReadOnly()));\n        }\n\n        return result.AsReadOnly();\n    }\n\n    public static IReadOnlyList<string> FindEffectivelyUnlinkedProjects(\n        IReadOnlyList<ProjectLinkage> linkage) =>\n        linkage\n            .Where(pl => pl.Dependencies.Count > 0 && pl.Dependencies.All(d => !d.IsTypeEffective))\n            .Select(pl => pl.Assembly)\n            .OrderBy(n => n, System.StringComparer.Ordinal)\n            .ToList()\n            .AsReadOnly();\n} |
58 | code-analysis/SyntaxWalker.cs | {10937 673 0.3517 4.3257} | 11220 | using System;\nusing System.Collections.Generic;\nusing System.IO;\nusing System.Linq;\nusing Microsoft.CodeAnalysis;\nusing Microsoft.CodeAnalysis.CSharp;\nusing Microsoft.CodeAnalysis.CSharp.Syntax;\n\nnamespace CodeAnalysis;\n\ninternal static class SyntaxWalker\n{\n    public static SourceFileAnalysis Analyze(string filePath)\n    {\n        string source;\n        try { source = File.ReadAllText(filePath); }\n        catch (Exception)\n        {\n            return new SourceFileAnalysis(filePath,\n                Array.Empty<string>(), Array.Empty<string>(), Array.Empty<RawTypeInfo>(),\n                Array.Empty<string>());\n        }\n\n        SyntaxTree tree = CSharpSyntaxTree.ParseText(source, path: filePath);\n        CompilationUnitSyntax root = tree.GetCompilationUnitRoot();\n\n        var importedNs = new HashSet<string>(StringComparer.Ordinal);\n        foreach (UsingDirectiveSyntax u in root.DescendantNodes().OfType<UsingDirectiveSyntax>())\n        {\n            if (u.StaticKeyword.IsKind(SyntaxKind.StaticKeyword)) continue;\n            if (u.Alias != null) continue;\n            string? name = u.Name?.ToString();\n            if (name != null) importedNs.Add(name);\n        }\n\n        var declaredNs = new HashSet<string>(StringComparer.Ordinal);\n        foreach (BaseNamespaceDeclarationSyntax ns in root.DescendantNodes().OfType<BaseNamespaceDeclarationSyntax>())\n            declaredNs.Add(ns.Name.ToString());\n\n        var rawTypes = new List<RawTypeInfo>();\n        foreach (MemberDeclarationSyntax member in root.DescendantNodes().OfType<MemberDeclarationSyntax>())\n        {\n            if (member.Parent is not (BaseNamespaceDeclarationSyntax or CompilationUnitSyntax)) continue;\n\n            RawTypeInfo? info = member switch\n            {\n                ClassDeclarationSyntax c    => Make(c.Identifier.ValueText, TypeKind.Class,\n                                                   c.Modifiers, c.BaseList, c.TypeParameterList, c, filePath),\n                InterfaceDeclarationSyntax i => Make(i.Identifier.ValueText, TypeKind.Interface,\n                                                   i.Modifiers, i.BaseList, i.TypeParameterList, i, filePath),\n                StructDeclarationSyntax s   => Make(s.Identifier.ValueText, TypeKind.Struct,\n                                                   s.Modifiers, s.BaseList, s.TypeParameterList, s, filePath),\n                RecordDeclarationSyntax r   => Make(r.Identifier.ValueText,\n                                                   r.ClassOrStructKeyword.IsKind(SyntaxKind.StructKeyword)\n                                                       ? TypeKind.RecordStruct : TypeKind.Record,\n                                                   r.Modifiers, r.BaseList, r.TypeParameterList, r, filePath),\n                EnumDeclarationSyntax e     => new RawTypeInfo(e.Identifier.ValueText, TypeKind.Enum,\n                                                   e.Modifiers.Select(m => m.ValueText).ToArray(),\n                                                   Array.Empty<string>(), 0,\n                                                   ContainingNamespace(e), filePath),\n                DelegateDeclarationSyntax d => new RawTypeInfo(d.Identifier.ValueText, TypeKind.Delegate,\n                                                   d.Modifiers.Select(m => m.ValueText).ToArray(),\n                                                   Array.Empty<string>(),\n                                                   d.TypeParameterList?.Parameters.Count ?? 0,\n                                                   ContainingNamespace(d), filePath),\n                _ => null\n            };\n\n            if (info != null) rawTypes.Add(info);\n        }\n\n        var referencedTypeNames = new HashSet<string>(StringComparer.Ordinal);\n        CollectTypeReferences(root, referencedTypeNames);\n\n        return new SourceFileAnalysis(\n            filePath,\n            declaredNs.ToArray(),\n            importedNs.ToArray(),\n            rawTypes.ToArray(),\n            referencedTypeNames.OrderBy(n => n).ToArray());\n    }\n\n    private static void CollectTypeReferences(SyntaxNode root, HashSet<string> names)\n    {\n        foreach (SyntaxNode node in root.DescendantNodes())\n        {\n            TypeSyntax? t = node switch\n            {\n                VariableDeclarationSyntax v       => v.Type,\n                ParameterSyntax p                 => p.Type,\n                MethodDeclarationSyntax m         => m.ReturnType,\n                PropertyDeclarationSyntax pr      => pr.Type,\n                FieldDeclarationSyntax f          => f.Declaration.Type,\n                SimpleBaseTypeSyntax b            => b.Type,\n                ObjectCreationExpressionSyntax o  => o.Type,\n                CastExpressionSyntax c            => c.Type,\n                TypeOfExpressionSyntax tof        => tof.Type,\n                TypePatternSyntax tp              => tp.Type,\n                _ => null\n            };\n            if (t != null) ExtractSimpleNames(t, names);\n        }\n\n        CollectDocCrefs(root, names);\n    }\n\n    private static void CollectDocCrefs(SyntaxNode root, HashSet<string> names)\n    {\n        foreach (SyntaxTrivia trivia in root.DescendantTrivia())\n        {\n            if (!trivia.HasStructure) continue;\n            if (trivia.GetStructure() is not DocumentationCommentTriviaSyntax docComment)\n                continue;\n\n            foreach (XmlCrefAttributeSyntax crefAttr in docComment.DescendantNodes()\n                         .OfType<XmlCrefAttributeSyntax>())\n                ExtractCrefNames(crefAttr.Cref, names);\n        }\n    }\n\n    private static void ExtractCrefNames(CrefSyntax cref, HashSet<string> names)\n    {\n        switch (cref)\n        {\n            case NameMemberCrefSyntax nm:\n\n                ExtractSimpleNames(nm.Name, names);\n\n                if (nm.Parameters != null)\n                    foreach (CrefParameterSyntax p in nm.Parameters.Parameters)\n                        ExtractSimpleNames(p.Type, names);\n                break;\n\n            case QualifiedCrefSyntax qc:\n\n                ExtractSimpleNames(qc.Container, names);\n                ExtractCrefNames(qc.Member, names);\n                break;\n\n            case TypeCrefSyntax tc:\n                ExtractSimpleNames(tc.Type, names);\n                break;\n\n        }\n    }\n\n    private static void ExtractSimpleNames(TypeSyntax typeSyntax, HashSet<string> names)\n    {\n        switch (typeSyntax)\n        {\n            case PredefinedTypeSyntax:\n                break; // int, string, bool, etc. — not project types\n\n            case IdentifierNameSyntax id:\n                string text = id.Identifier.ValueText;\n                if (text.Length > 0 && text != \"var\" && text != \"dynamic\")\n                    names.Add(text);\n                break;\n\n            case GenericNameSyntax gen:\n                names.Add(gen.Identifier.ValueText);\n                foreach (TypeSyntax arg in gen.TypeArgumentList.Arguments)\n                    ExtractSimpleNames(arg, names);\n                break;\n\n            case QualifiedNameSyntax qual:\n\n                ExtractSimpleNames(qual.Right, names);\n                break;\n\n            case NullableTypeSyntax nullable:\n                ExtractSimpleNames(nullable.ElementType, names);\n                break;\n\n            case ArrayTypeSyntax arr:\n                ExtractSimpleNames(arr.ElementType, names);\n                break;\n\n            case TupleTypeSyntax tuple:\n                foreach (TupleElementSyntax el in tuple.Elements)\n                    ExtractSimpleNames(el.Type, names);\n                break;\n        }\n    }\n\n    private static RawTypeInfo Make(\n        string name, TypeKind kind, SyntaxTokenList modifiers,\n        BaseListSyntax? baseList, TypeParameterListSyntax? typeParams,\n        MemberDeclarationSyntax node, string filePath) =>\n        new(name, kind,\n            modifiers.Select(m => m.ValueText).ToArray(),\n            baseList?.Types.Select(t => t.Type.ToString()).ToArray() ?? Array.Empty<string>(),\n            typeParams?.Parameters.Count ?? 0,\n            ContainingNamespace(node),\n            filePath);\n\n    private static string ContainingNamespace(SyntaxNode node)\n    {\n        var parts = new List<string>();\n        SyntaxNode? current = node.Parent;\n        while (current != null)\n        {\n            if (current is BaseNamespaceDeclarationSyntax ns)\n                parts.Insert(0, ns.Name.ToString());\n            current = current.Parent;\n        }\n        return string.Join(\".\", parts);\n    }\n}\n\npublic static class ProjectAnalyzer\n{\n    public static ProjectAnalysis Analyze(ProjectOwnership ownership, CsprojGlobs globs)\n    {\n        var fileAnalyses = ownership.OwnedFiles\n            .Where(f => f.EndsWith(\".cs\", StringComparison.OrdinalIgnoreCase))\n            .Select(SyntaxWalker.Analyze)\n            .ToList();\n\n        var declaredNs = new HashSet<string>(StringComparer.Ordinal);\n        var importedNs = new HashSet<string>(StringComparer.Ordinal);\n\n        foreach (SourceFileAnalysis fa in fileAnalyses)\n        {\n            foreach (string ns in fa.DeclaredNamespaces) declaredNs.Add(ns);\n            foreach (string ns in fa.ImportedNamespaces)  importedNs.Add(ns);\n        }\n\n        var fileAnalysesPublic = fileAnalyses\n            .Select(fa => new FileAnalysis(fa.FilePath, fa.DeclaredNamespaces, fa.ImportedNamespaces,\n                                           fa.ReferencedTypeNames))\n            .ToList();\n\n        return new ProjectAnalysis(\n            ownership.CsprojPath,\n            ownership.AssemblyName,\n            declaredNs,\n            importedNs,\n            globs.ProjectReferences,\n            GroupTypes(fileAnalyses),\n            fileAnalysesPublic,\n            globs.PackageReferences,\n            globs.CompilerSettings);\n    }\n\n    private static IReadOnlyList<TypeEntry> GroupTypes(IEnumerable<SourceFileAnalysis> analyses)\n    {\n        var result = new List<TypeEntry>();\n\n        foreach (var group in analyses\n            .SelectMany(fa => fa.RawTypes)\n            .GroupBy(t => (t.ContainingNamespace, t.Name, t.Kind)))\n        {\n            var members = group.ToList();\n            bool isPartial = members.Count > 1 ||\n                             members.Any(t => t.Modifiers.Contains(\"partial\", StringComparer.Ordinal));\n\n            result.Add(new TypeEntry(\n                group.Key.Name,\n                group.Key.Kind,\n                members.SelectMany(t => t.Modifiers).Distinct(StringComparer.Ordinal)\n                       .OrderBy(m => m).ToArray(),\n                isPartial,\n                members.Select(t => t.DeclaredInFile).Distinct().OrderBy(f => f).ToArray(),\n                members.SelectMany(t => t.BaseTypes).Distinct(StringComparer.Ordinal).ToArray(),\n                members[0].GenericArity,\n                group.Key.ContainingNamespace));\n        }\n\n        return result.AsReadOnly();\n    }\n} |
59 | code-analysis/TypeOntology.cs | {1481 102 0.1621 4.7344} | 1528 | using System.Collections.Generic;\n\nnamespace CodeAnalysis;\n\npublic enum TypeKind { Class, Interface, Struct, Record, RecordStruct, Enum, Delegate }\n\npublic sealed record TypeEntry(\n    string Name,\n    TypeKind Kind,\n    IReadOnlyList<string> Modifiers,\n    bool IsPartial,\n    IReadOnlyList<string> ContributingFiles,\n    IReadOnlyList<string> BaseTypes,\n    int GenericArity,\n    string ContainingNamespace);\n\npublic sealed record FileAnalysis(\n    string FilePath,\n    IReadOnlyList<string> DeclaredNamespaces,\n    IReadOnlyList<string> ImportedNamespaces,\n    IReadOnlyList<string> ReferencedTypeNames);\n\npublic sealed record ProjectAnalysis(\n    string CsprojPath,\n    string AssemblyName,\n    IReadOnlySet<string> DeclaredNamespaces,\n    IReadOnlySet<string> ImportedNamespaces,\n    IReadOnlyList<string> ProjectReferences,\n    IReadOnlyList<TypeEntry> Types,\n    IReadOnlyList<FileAnalysis> FileAnalyses,\n    IReadOnlyList<PackageRef> PackageReferences,\n    CsprojCompilerSettings CompilerSettings);\n\ninternal sealed record SourceFileAnalysis(\n    string FilePath,\n    IReadOnlyList<string> DeclaredNamespaces,\n    IReadOnlyList<string> ImportedNamespaces,\n    IReadOnlyList<RawTypeInfo> RawTypes,\n    IReadOnlyList<string> ReferencedTypeNames);\n\ninternal sealed record RawTypeInfo(\n    string Name,\n    TypeKind Kind,\n    IReadOnlyList<string> Modifiers,\n    IReadOnlyList<string> BaseTypes,\n    int GenericArity,\n    string ContainingNamespace,\n    string DeclaredInFile); |
