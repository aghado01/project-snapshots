idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
23 | clustering/spc/diagnostics/SpecificHeat.cs | {1780 169 0.2916 4.5459} | 1828 | using System;\nusing System.Collections.Generic;\nusing Clustering.SPC.Potts;\n\nnamespace Clustering.SPC.Diagnostics;\n\npublic readonly record struct SpecificHeatCurve(\n    double[] Temperatures,\n    double[] Cv);\n\npublic static class SpecificHeat\n{\n    public static SpecificHeatCurve From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames.Count == 0)\n            return new SpecificHeatCurve(Array.Empty<double>(), Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double SumCv, int Count)>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelCoreObservables frame = frames[i];\n            double cv = 0.0;\n            if (frame.Temperature > 0.0 && frame.CycleCount > 0)\n            {\n                double meanEnergy = frame.RunningSumEnergy / frame.CycleCount;\n                double meanEnergySq = frame.RunningSumEnergySq / frame.CycleCount;\n                double variance = Math.Max(0.0, meanEnergySq - meanEnergy * meanEnergy);\n                cv = variance / (frame.Temperature * frame.Temperature);\n            }\n\n            if (grouped.TryGetValue(frame.Temperature, out (double SumCv, int Count) acc))\n                grouped[frame.Temperature] = (acc.SumCv + cv, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (cv, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var cvValues = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, (double sumCv, int count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            cvValues[index] = count > 0 ? sumCv / count : 0.0;\n            index++;\n        }\n\n        return new SpecificHeatCurve(temperatures, cvValues);\n    }\n} |
24 | clustering/spc/diagnostics/Susceptibility.cs | {1549 143 0.2744 4.6017} | 1591 | using System.Collections.Generic;\nusing Clustering.SPC.Potts;\n\nnamespace Clustering.SPC.Diagnostics;\n\npublic readonly record struct SusceptibilityCurve(\n    double[] Temperatures,\n    double[] Chi);\n\npublic static class Susceptibility\n{\n    public static SusceptibilityCurve From(IReadOnlyList<PottsModelCoreObservables> frames)\n    {\n        if (frames.Count == 0)\n            return new SusceptibilityCurve(System.Array.Empty<double>(), System.Array.Empty<double>());\n\n        var grouped = new SortedDictionary<double, (double SumChi, int Count)>();\n        for (int i = 0; i < frames.Count; i++)\n        {\n            PottsModelCoreObservables frame = frames[i];\n            double chi = frame.CycleCount > 0 && frame.Spins.Length > 0\n                ? frame.RunningSumSqClusterSizes / (frame.CycleCount * (double)frame.Spins.Length)\n                : 0.0;\n\n            if (grouped.TryGetValue(frame.Temperature, out (double SumChi, int Count) acc))\n                grouped[frame.Temperature] = (acc.SumChi + chi, acc.Count + 1);\n            else\n                grouped[frame.Temperature] = (chi, 1);\n        }\n\n        var temperatures = new double[grouped.Count];\n        var chiValues = new double[grouped.Count];\n        int index = 0;\n        foreach ((double temperature, (double sumChi, int count)) in grouped)\n        {\n            temperatures[index] = temperature;\n            chiValues[index] = count > 0 ? sumChi / count : 0.0;\n            index++;\n        }\n\n        return new SusceptibilityCurve(temperatures, chiValues);\n    }\n} |
25 | clustering/spc/graphsignals/BondFrequencyBinaryEntropySum.cs | {1852 184 0.3143 4.5814} | 1912 | using System;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.GraphSignals;\n\npublic sealed class BondFrequencyBinaryEntropySum : IGraphSignal\n{\n    private static readonly double Ln2 = Math.Log(2.0);\n\n    public double[] Compute(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(BondFrequencyBinaryEntropySum)} requires Tier 1 edge observables.\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        if (edges.CycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {edges.CycleCount}.\");\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        double invCycles = 1.0 / edges.CycleCount;\n        var result = new double[n];\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                double p = edges.BondFormedCount[e] * invCycles;\n                double hBits = BinaryEntropyNats(p) / Ln2;\n                result[i] += hBits;\n                result[j] += hBits;\n            }\n        }\n        return result;\n    }\n\n    private static double BinaryEntropyNats(double p)\n    {\n        if (p <= 0.0 || p >= 1.0) return 0.0;\n        return -(p * Math.Log(p) + (1.0 - p) * Math.Log(1.0 - p));\n    }\n} |
26 | clustering/spc/graphsignals/BondFrequencyDegree.cs | {1652 157 0.3166 4.5249} | 1706 | using System;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.GraphSignals;\n\npublic sealed class BondFrequencyDegree : IGraphSignal\n{\n    public double[] Compute(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(BondFrequencyDegree)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null.\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        if (edges.CycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {edges.CycleCount}.\");\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        double invCycles = 1.0 / edges.CycleCount;\n        var result = new double[n];\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;   // walk only the upper triangle (where SW writes)\n                double freq = edges.BondFormedCount[e] * invCycles;\n                result[i] += freq;\n                result[j] += freq;\n            }\n        }\n        return result;\n    }\n} |
27 | clustering/spc/graphsignals/IGraphSignal.cs | {113 10 0.1239 4.7419} | 118 | namespace Clustering.SPC.GraphSignals;\n\npublic interface IGraphSignal\n{\n    double[] Compute(SpcRunResult run);\n} |
28 | clustering/spc/graphsignals/SpinCorrelationEigenvectorCentrality.cs | {2804 300 0.3146 4.5311} | 2895 | using System;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\nusing Maths.LinAlg;\n\nnamespace Clustering.SPC.GraphSignals;\n\npublic sealed class SpinCorrelationEigenvectorCentrality : IGraphSignal\n{\n\n    public int MaxSweeps { get; init; } = 256;\n\n    public double Tolerance { get; init; } = 1e-12;\n\n    public DenseEigenOptions EigenOptions { get; init; } = default;\n\n    public double[] Compute(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(SpinCorrelationEigenvectorCentrality)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null. Configure the runner to track edge \" +\n                \"observables (e.g. PottsObservableTier.WithEdgeObservables).\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        if (edges.CycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {edges.CycleCount}.\");\n        if (edges.SpinAgreementCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"SpinAgreementCount length ({edges.SpinAgreementCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        if (n == 0) return Array.Empty<double>();\n        if (n == 1) return new double[] { 1.0 };\n\n        var g = new double[n, n];\n        double invCycles = 1.0 / edges.CycleCount;\n\n        for (int i = 0; i < n; i++)\n            g[i, i] = 1.0;  // self-agreement (correlation-matrix convention)\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                double gij = edges.SpinAgreementCount[e] * invCycles;\n                g[i, j] = gij;\n                g[j, i] = gij;\n            }\n        }\n\n        EigenResult eig = DenseEigen.DecomposeSymmetric(g, MaxSweeps, Tolerance, EigenOptions);\n        double[] top = eig.Eigenvectors[0];\n        if (top.Length != n)\n            throw new InvalidOperationException(\n                $\"Eigenvector length ({top.Length}) does not match node count ({n}).\");\n\n        double signMass = 0.0;\n        for (int i = 0; i < n; i++) signMass += top[i];\n        if (signMass < 0.0)\n        {\n            var flipped = new double[n];\n            for (int i = 0; i < n; i++) flipped[i] = -top[i];\n            return flipped;\n        }\n\n        var result = new double[n];\n        Array.Copy(top, result, n);\n        return result;\n    }\n} |
29 | clustering/spc/heuristics/CriticalTemperatureEstimator.cs | {1191 121 0.2603 4.6972} | 1229 | using System;\nusing Graphs.Diagnostics;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Heuristics;\n\npublic static class CriticalTemperatureEstimator\n{\n    public static double Estimate(CsrGraph graph, int q)\n    {\n        if (graph.NodeCount == 0 || graph.Targets.Length == 0 || q < 2)\n            return 1.0;\n\n        EdgeWeightSummary weights = EdgeWeights.Summary(graph);\n        double meanWeightedDegree = 2.0 * weights.MeanWeight * weights.EdgeCount / graph.NodeCount;\n        double logQ = Math.Log(q);\n        if (logQ <= 0.0 || weights.MedianWeight <= 0.0)\n            return 1.0;\n\n        return weights.MedianWeight * meanWeightedDegree / logQ;\n    }\n\n    public static (double TempMin, double TempMax) EstimateBracket(\n        CsrGraph graph,\n        int q,\n        double coldOvershoot = 0.05,\n        double hotOvershoot = 5.0)\n    {\n        if (coldOvershoot <= 0.0)\n            throw new ArgumentOutOfRangeException(nameof(coldOvershoot));\n        if (hotOvershoot <= coldOvershoot)\n            throw new ArgumentException(\"hotOvershoot must exceed coldOvershoot.\");\n\n        double tc = Estimate(graph, q);\n        return (tc * coldOvershoot, tc * hotOvershoot);\n    }\n} |
30 | clustering/spc/partitions/IPartitionStrategy.cs | {116 10 0.1207 4.5614} | 121 | namespace Clustering.SPC.Partitions;\n\npublic interface IPartitionStrategy\n{\n    Partition Apply(SpcRunResult run);\n} |
31 | clustering/spc/partitions/Partition.cs | {180 24 0.2222 4.2888} | 186 | namespace Clustering.SPC.Partitions;\n\npublic sealed record Partition\n{\n    public required int[] Labels       { get; init; }\n    public required int   ClusterCount { get; init; }\n} |
32 | clustering/spc/partitions/strategies/ThresholdBondFrequency.cs | {1851 177 0.3133 4.5702} | 1914 | using System;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Partitions.Strategies;\n\npublic sealed class ThresholdBondFrequency : IPartitionStrategy\n{\n\n    public double Theta { get; init; } = 0.5;\n\n    public Partition Apply(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(ThresholdBondFrequency)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null.\");\n        if (Theta < 0.0 || Theta > 1.0)\n            throw new InvalidOperationException($\"Theta ({Theta}) must lie in [0, 1].\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        int cycleCount = edges.CycleCount;\n        if (cycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {cycleCount}.\");\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        var uf = new UnionFind(n);\n        double cyclesD = cycleCount;\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                double avgBondFreq = edges.BondFormedCount[e] / cyclesD;\n                if (avgBondFreq > Theta)\n                    uf.Union(i, j);\n            }\n        }\n\n        return UnionFindLabeler.Densify(uf, n);\n    }\n} |
33 | clustering/spc/partitions/strategies/ThresholdSpinAgreement.cs | {2553 236 0.2910 4.5857} | 2635 | using System;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Partitions.Strategies;\n\npublic sealed class ThresholdSpinAgreement : IPartitionStrategy\n{\n\n    public double Theta { get; init; } = 0.5;\n\n    public static Partition Apply(\n        CsrGraph graph,\n        int[] spinAgreementCount,\n        int cycleCount,\n        double theta = 0.5)\n    {\n        if (cycleCount <= 0)\n            throw new ArgumentOutOfRangeException(nameof(cycleCount), \"Cycle count must be positive.\");\n        if (graph.Targets is null || graph.RowPointers is null)\n            throw new ArgumentException(\"Graph CSR arrays are not initialized.\", nameof(graph));\n        if (spinAgreementCount.Length != graph.Targets.Length)\n            throw new ArgumentException(\n                $\"spinAgreementCount length ({spinAgreementCount.Length}) must match CSR slot count ({graph.Targets.Length}).\",\n                nameof(spinAgreementCount));\n        if (theta < 0.0 || theta > 1.0)\n            throw new ArgumentOutOfRangeException(nameof(theta), \"Theta must lie in [0, 1].\");\n\n        int n = graph.NodeCount;\n        var uf = new UnionFind(n);\n        double cyclesD = cycleCount;\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;   // undirected: visit each edge once\n\n                double avgAgreement = spinAgreementCount[e] / cyclesD;\n                if (avgAgreement > theta)\n                    uf.Union(i, j);\n            }\n        }\n\n        return UnionFindLabeler.Densify(uf, n);\n    }\n\n    public static Partition Apply(\n        CsrGraph graph,\n        PottsModelEdgeObservables edgeObservables,\n        double theta = 0.5)\n    {\n        ArgumentNullException.ThrowIfNull(edgeObservables);\n        return Apply(graph, edgeObservables.SpinAgreementCount, edgeObservables.CycleCount, theta);\n    }\n\n    public Partition Apply(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(ThresholdSpinAgreement)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null. Configure the runner to track \" +\n                \"edge observables (e.g. PottsObservableTier.WithEdgeObservables).\");\n        return Apply(run.Graph, run.EdgeObservables, Theta);\n    }\n} |
34 | clustering/spc/partitions/strategies/UnionFindLabeler.cs | {722 78 0.3338 4.2495} | 746 | using System.Collections.Generic;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC.Partitions.Strategies;\n\ninternal static class UnionFindLabeler\n{\n    internal static Partition Densify(UnionFind uf, int nodeCount)\n    {\n        var labels   = new int[nodeCount];\n        var labelMap = new Dictionary<int, int>();\n        int nextLabel = 0;\n        for (int i = 0; i < nodeCount; i++)\n        {\n            int root = uf.Find(i);\n            if (!labelMap.TryGetValue(root, out int dense))\n            {\n                dense = nextLabel++;\n                labelMap[root] = dense;\n            }\n            labels[i] = dense;\n        }\n        return new Partition { Labels = labels, ClusterCount = nextLabel };\n    }\n} |
35 | clustering/spc/potts/model/CoreObservables.cs | {1210 154 0.2669 4.3073} | 1242 | using System;\n\nnamespace Clustering.SPC.Potts.Model;\n\npublic sealed record PottsModelCoreObservables\n{\n\n    public required double              Temperature    { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsObservableTier ObservableTier { get; init; }\n\n    public int ReplicaIndex { get; init; }\n\n    public required int CycleCount { get; init; }\n\n    public required int[] Spins                { get; init; }\n    public required int[] ClusterSizeHistogram { get; init; }\n\n    public required ulong RngState0 { get; init; }\n    public required ulong RngState1 { get; init; }\n    public required ulong RngState2 { get; init; }\n    public required ulong RngState3 { get; init; }\n\n    public required double RunningSumSqClusterSizes     { get; init; }\n    public required double RunningSumSqClusterSizesExcl { get; init; }\n    public required double RunningSumEnergy             { get; init; }\n    public required double RunningSumEnergySq           { get; init; }\n\n    public required double RunningSumMag   { get; init; }\n    public required double RunningSumMagSq { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n} |
36 | clustering/spc/potts/model/EdgeObservables.cs | {461 58 0.2364 4.4751} | 476 | using System;\n\nnamespace Clustering.SPC.Potts.Model;\n\npublic sealed record PottsModelEdgeObservables\n{\n\n    public required double Temperature  { get; init; }\n    public          int    ReplicaIndex { get; init; }\n    public required int    CycleCount   { get; init; }\n\n    public required int[]  BondFormedCount    { get; init; }\n    public required int[]  SpinAgreementCount { get; init; }\n\n    public DateTime TimestampUtc { get; init; } = DateTime.UtcNow;\n} |
37 | clustering/spc/potts/model/IPottsConfig.cs | {424 49 0.1769 4.6577} | 448 | namespace Clustering.SPC.Potts.Model;\n\npublic interface IPottsConfig\n{\n\n    static abstract bool EdgeObservables { get; }\n}\n\npublic readonly struct StandardObservables : IPottsConfig\n{\n    public static bool EdgeObservables => false;\n}\n\npublic readonly struct WithEdgeObservables : IPottsConfig\n{\n    public static bool EdgeObservables => true;\n}\n\npublic enum PottsObservableTier\n{\n\n    Standard,\n\n    WithEdgeObservables,\n} |
38 | clustering/spc/potts/model/IPottsModel.cs | {748 84 0.2166 4.6744} | 777 | using System;\n\nnamespace Clustering.SPC.Potts.Model;\n\ninternal interface IPottsModel\n{\n    void Cycle();\n    void Run(int cycleCount);\n    void BurnIn(int cycles);\n\n    int CycleCount { get; }\n    double Temperature { get; }\n    int Q { get; }\n    int N { get; }\n\n    ReadOnlySpan<int> Spins { get; }\n    ReadOnlySpan<int> ClusterSizeHistogram { get; }\n\n    double RunningSumSqClusterSizes { get; }\n    double RunningSumSqClusterSizesExcl { get; }\n    double RunningSumEnergy { get; }\n    double RunningSumEnergySq { get; }\n    double RunningSumMag { get; }\n    double RunningSumMagSq { get; }\n\n    PottsModelCoreObservables GetCheckpoint();\n    void Restore(PottsModelCoreObservables result);\n\n    PottsModelEdgeObservables? GetEdgeCheckpoint();\n} |
