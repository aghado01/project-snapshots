idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
38 | clustering/graphical/spc/partitions/hierarchical/BlattPartitionStrategy.cs | {4965 461 0.2568 4.7765} | 5118 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing Clustering.Graphical.SPC.Partitions.Strategies;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Clustering.Graphical.SPC.Runtime.Scheduling;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Partitions.Hierarchical;\n\npublic sealed class BlattPartitionConfig\n{\n\n    public int Q { get; init; } = 20;\n\n    public int FinalBurn { get; init; } = 1000;\n\n    public int FinalCycles { get; init; } = 5000;\n\n    public PottsObservableTier FinalPassTier { get; init; } = PottsObservableTier.WithEdgeObservables;\n\n    public int? BaseSeed { get; init; }\n\n    public IPseudoTransitionDetector? Detector { get; init; }\n\n    public IPartitionStrategy? CutPolicy { get; init; }\n}\n\npublic sealed class BlattPartitionStrategy : IHierarchicalPartitionStrategy\n{\n    private readonly BlattPartitionConfig _config;\n\n    public BlattPartitionStrategy(BlattPartitionConfig? config = null)\n    {\n        _config = config ?? new BlattPartitionConfig();\n    }\n\n    public PartitionHierarchy Apply(SweepResult sweep, CsrGraph graph)\n    {\n        ArgumentNullException.ThrowIfNull(sweep);\n        ArgumentNullException.ThrowIfNull(graph);\n\n        var detector = _config.Detector ?? new MagnetizationPeakDetector();\n        var cut = _config.CutPolicy ?? new ThresholdSpinAgreement { Theta = 0.5 };\n\n        var profile = sweep.Summary.Profile;\n        if (profile.IsEmpty || profile.Temperatures.Count < 2)\n            return new PartitionHierarchy(Array.Empty<HierarchyLevel>(), NestingHolds: true);\n\n        double[] peakTemperatures = detector.Detect(profile);\n        double tMin = profile.Temperatures[0];\n        double tMax = profile.Temperatures[profile.Temperatures.Count - 1];\n\n        double[] phaseTs = ComputePhaseTemperatures(peakTemperatures, tMin, tMax);\n\n        var levels = new List<HierarchyLevel>(phaseTs.Length);\n        for (int phaseIdx = 0; phaseIdx < phaseTs.Length; phaseIdx++)\n        {\n            double T = phaseTs[phaseIdx];\n            int? seed = SpcSeedHelper.Derive(_config.BaseSeed, T, replica: phaseIdx, round: -1);\n            SpcRunResult phaseRun = SweepKernel.RunFinalEquilibrium(\n                graph, T, _config.Q,\n                _config.FinalBurn, _config.FinalCycles,\n                _config.FinalPassTier, seed);\n\n            Partition partition = cut.Apply(phaseRun);\n            levels.Add(new HierarchyLevel(\n                Temperature: T,\n                Partition:   partition,\n                Provenance:  BuildProvenance(phaseIdx, phaseTs.Length, peakTemperatures)));\n        }\n\n        bool nestingHolds = CheckNesting(levels);\n\n        return new PartitionHierarchy(levels, NestingHolds: nestingHolds);\n    }\n\n    private static double[] ComputePhaseTemperatures(\n        double[] peakTemperatures, double tMin, double tMax)\n    {\n        if (peakTemperatures.Length == 0)\n        {\n\n            return new[] { GeometricMidpoint(tMin, tMax) };\n        }\n\n        var sorted = (double[])peakTemperatures.Clone();\n        Array.Sort(sorted);\n\n        var phases = new double[sorted.Length + 1];\n        phases[0] = GeometricMidpoint(tMin, sorted[0]);\n        for (int i = 1; i < sorted.Length; i++)\n            phases[i] = GeometricMidpoint(sorted[i - 1], sorted[i]);\n        phases[sorted.Length] = GeometricMidpoint(sorted[sorted.Length - 1], tMax);\n        return phases;\n    }\n\n    private static double GeometricMidpoint(double a, double b)\n    {\n        if (a <= 0.0 || b <= 0.0) return 0.5 * (a + b);\n        return Math.Sqrt(a * b);\n    }\n\n    private static string BuildProvenance(int phaseIndex, int phaseCount, double[] peaks)\n    {\n\n        var ci = CultureInfo.InvariantCulture;\n        if (peaks.Length == 0) return \"no-peaks: single phase across full sweep range\";\n        if (phaseIndex == 0) return $\"cold of T_peak={peaks[0].ToString(\"G6\", ci)}\";\n        if (phaseIndex == phaseCount - 1) return $\"hot of T_peak={peaks[peaks.Length - 1].ToString(\"G6\", ci)}\";\n        return $\"between T_peak={peaks[phaseIndex - 1].ToString(\"G6\", ci)} and \" +\n               $\"T_peak={peaks[phaseIndex].ToString(\"G6\", ci)}\";\n    }\n\n    private static bool CheckNesting(IReadOnlyList<HierarchyLevel> levels)\n    {\n        if (levels.Count < 2) return true;\n        for (int i = 0; i < levels.Count - 1; i++)\n        {\n            int[] coldLabels = levels[i].Partition.Labels;\n            int[] hotLabels = levels[i + 1].Partition.Labels;\n            if (coldLabels.Length != hotLabels.Length) return false;\n\n            var hotToCold = new Dictionary<int, int>();\n            for (int k = 0; k < hotLabels.Length; k++)\n            {\n                int hot = hotLabels[k];\n                int cold = coldLabels[k];\n                if (!hotToCold.TryAdd(hot, cold) && hotToCold[hot] != cold)\n                    return false;\n            }\n        }\n        return true;\n    }\n}
39 | clustering/graphical/spc/partitions/hierarchical/IHierarchicalPartitionStrategy.cs | {255 16 0.0863 4.6764} | 264 | using Clustering.Graphical.SPC.Runtime.Scheduling;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Partitions.Hierarchical;\n\npublic interface IHierarchicalPartitionStrategy\n{\n\n    PartitionHierarchy Apply(SweepResult sweep, CsrGraph graph);\n}
40 | clustering/graphical/spc/partitions/hierarchical/IPseudoTransitionDetector.cs | {194 12 0.0928 4.7033} | 202 | using Clustering.Graphical.SPC.Profiling;\n\nnamespace Clustering.Graphical.SPC.Partitions.Hierarchical;\n\npublic interface IPseudoTransitionDetector\n{\n\n    double[] Detect(SweepProfile profile);\n}
41 | clustering/graphical/spc/partitions/hierarchical/MagnetizationPeakDetector.cs | {2157 223 0.2981 4.5733} | 2230 | using System;\nusing System.Collections.Generic;\nusing Clustering.Graphical.SPC.Profiling;\n\nnamespace Clustering.Graphical.SPC.Partitions.Hierarchical;\n\npublic sealed class MagnetizationPeakDetector : IPseudoTransitionDetector\n{\n\n    public string SignalChannelName { get; init; } = \"MagnetizationVariance\";\n\n    public double MinProminence { get; init; } = 0.1;\n\n    public double[] Detect(SweepProfile profile)\n    {\n        ArgumentNullException.ThrowIfNull(profile);\n        if (profile.IsEmpty) return Array.Empty<double>();\n\n        if (!profile.AdditionalChannels.TryGetValue(SignalChannelName, out var signal))\n            throw new InvalidOperationException(\n                $\"SweepProfile is missing channel '{SignalChannelName}'. \" +\n                \"Pass a profile built by SweepProfile.From(runs) or supply a custom \" +\n                \"channel name via SignalChannelName.\");\n\n        int n = signal.Count;\n        if (n != profile.Temperatures.Count)\n            throw new InvalidOperationException(\n                $\"Channel '{SignalChannelName}' has {n} samples but the profile has \" +\n                $\"{profile.Temperatures.Count} temperatures.\");\n        if (n < 3) return Array.Empty<double>();\n\n        double minVal = double.PositiveInfinity;\n        double maxVal = double.NegativeInfinity;\n        for (int i = 0; i < n; i++)\n        {\n            double v = signal[i];\n            if (v < minVal) minVal = v;\n            if (v > maxVal) maxVal = v;\n        }\n        double range = maxVal - minVal;\n        double absThreshold = range * MinProminence;\n        if (range == 0.0) return Array.Empty<double>();\n\n        var peaks = new List<double>();\n        for (int i = 1; i < n - 1; i++)\n        {\n            double prev = signal[i - 1];\n            double curr = signal[i];\n            double next = signal[i + 1];\n\n            if (curr <= prev || curr <= next) continue;\n\n            double higherSide = Math.Max(prev, next);\n            double prominence = curr - higherSide;\n            if (prominence < absThreshold) continue;\n\n            peaks.Add(profile.Temperatures[i]);\n        }\n\n        return peaks.ToArray();\n    }\n}
42 | clustering/graphical/spc/partitions/hierarchical/PartitionHierarchy.cs | {504 42 0.2599 4.5228} | 524 | using System.Collections.Generic;\n\nnamespace Clustering.Graphical.SPC.Partitions.Hierarchical;\n\npublic sealed record HierarchyLevel(\n    double    Temperature,\n    Partition Partition,\n    string?   Provenance = null);\n\npublic sealed record PartitionHierarchy(\n    IReadOnlyList<HierarchyLevel> Levels,\n    bool                          NestingHolds,\n    string                        TopologyAxis = \"temperature\")\n{\n\n    public int Count => Levels.Count;\n\n    public bool IsEmpty => Levels.Count == 0;\n}
43 | clustering/graphical/spc/partitions/IPartitionStrategy.cs | {233 14 0.0815 4.7140} | 241 | using Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Observables.Projections;\n\nnamespace Clustering.Graphical.SPC.Partitions;\n\npublic interface IPartitionStrategy\n{\n    Partition Apply(SpcRunResult run);\n}
44 | clustering/graphical/spc/partitions/Partition.cs | {190 24 0.2105 4.3595} | 196 | namespace Clustering.Graphical.SPC.Partitions;\n\npublic sealed record Partition\n{\n    public required int[] Labels       { get; init; }\n    public required int   ClusterCount { get; init; }\n}
45 | clustering/graphical/spc/partitions/strategies/ThresholdBondFrequency.cs | {1937 179 0.3005 4.6127} | 2001 | using System;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Partitions.Strategies;\n\npublic sealed class ThresholdBondFrequency : IPartitionStrategy\n{\n\n    public double Theta { get; init; } = 0.5;\n\n    public Partition Apply(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(ThresholdBondFrequency)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null.\");\n        if (Theta < 0.0 || Theta > 1.0)\n            throw new InvalidOperationException($\"Theta ({Theta}) must lie in [0, 1].\");\n\n        CsrGraph graph = run.Graph;\n        PottsModelEdgeObservables edges = run.EdgeObservables;\n        int cycleCount = edges.CycleCount;\n        if (cycleCount <= 0)\n            throw new InvalidOperationException(\n                $\"CycleCount must be positive; was {cycleCount}.\");\n        if (edges.BondFormedCount.Length != graph.Targets.Length)\n            throw new InvalidOperationException(\n                $\"BondFormedCount length ({edges.BondFormedCount.Length}) does not \" +\n                $\"match CSR slot count ({graph.Targets.Length}).\");\n\n        int n = graph.NodeCount;\n        var uf = new UnionFind(n);\n        double cyclesD = cycleCount;\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;\n                double avgBondFreq = edges.BondFormedCount[e] / cyclesD;\n                if (avgBondFreq > Theta)\n                    uf.Union(i, j);\n            }\n        }\n\n        return UnionFindLabeler.Densify(uf, n);\n    }\n}
46 | clustering/graphical/spc/partitions/strategies/ThresholdSpinAgreement.cs | {2639 238 0.2823 4.6133} | 2722 | using System;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Partitions.Strategies;\n\npublic sealed class ThresholdSpinAgreement : IPartitionStrategy\n{\n\n    public double Theta { get; init; } = 0.5;\n\n    public static Partition Apply(\n        CsrGraph graph,\n        int[] spinAgreementCount,\n        int cycleCount,\n        double theta = 0.5)\n    {\n        if (cycleCount <= 0)\n            throw new ArgumentOutOfRangeException(nameof(cycleCount), \"Cycle count must be positive.\");\n        if (graph.Targets is null || graph.RowPointers is null)\n            throw new ArgumentException(\"Graph CSR arrays are not initialized.\", nameof(graph));\n        if (spinAgreementCount.Length != graph.Targets.Length)\n            throw new ArgumentException(\n                $\"spinAgreementCount length ({spinAgreementCount.Length}) must match CSR slot count ({graph.Targets.Length}).\",\n                nameof(spinAgreementCount));\n        if (theta < 0.0 || theta > 1.0)\n            throw new ArgumentOutOfRangeException(nameof(theta), \"Theta must lie in [0, 1].\");\n\n        int n = graph.NodeCount;\n        var uf = new UnionFind(n);\n        double cyclesD = cycleCount;\n\n        for (int i = 0; i < n; i++)\n        {\n            int rowEnd = graph.RowPointers[i + 1];\n            for (int e = graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = graph.Targets[e];\n                if (j <= i) continue;   // undirected: visit each edge once\n\n                double avgAgreement = spinAgreementCount[e] / cyclesD;\n                if (avgAgreement > theta)\n                    uf.Union(i, j);\n            }\n        }\n\n        return UnionFindLabeler.Densify(uf, n);\n    }\n\n    public static Partition Apply(\n        CsrGraph graph,\n        PottsModelEdgeObservables edgeObservables,\n        double theta = 0.5)\n    {\n        ArgumentNullException.ThrowIfNull(edgeObservables);\n        return Apply(graph, edgeObservables.SpinAgreementCount, edgeObservables.CycleCount, theta);\n    }\n\n    public Partition Apply(SpcRunResult run)\n    {\n        ArgumentNullException.ThrowIfNull(run);\n        if (run.EdgeObservables is null)\n            throw new InvalidOperationException(\n                $\"{nameof(ThresholdSpinAgreement)} requires Tier 1 edge observables, \" +\n                \"but SpcRunResult.EdgeObservables is null. Configure the runner to track \" +\n                \"edge observables (e.g. PottsObservableTier.WithEdgeObservables).\");\n        return Apply(run.Graph, run.EdgeObservables, Theta);\n    }\n}
47 | clustering/graphical/spc/partitions/strategies/UnionFindLabeler.cs | {732 78 0.3292 4.2732} | 756 | using System.Collections.Generic;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Partitions.Strategies;\n\ninternal static class UnionFindLabeler\n{\n    internal static Partition Densify(UnionFind uf, int nodeCount)\n    {\n        var labels   = new int[nodeCount];\n        var labelMap = new Dictionary<int, int>();\n        int nextLabel = 0;\n        for (int i = 0; i < nodeCount; i++)\n        {\n            int root = uf.Find(i);\n            if (!labelMap.TryGetValue(root, out int dense))\n            {\n                dense = nextLabel++;\n                labelMap[root] = dense;\n            }\n            labels[i] = dense;\n        }\n        return new Partition { Labels = labels, ClusterCount = nextLabel };\n    }\n}
48 | clustering/graphical/spc/profiling/ProfileCriteria.cs | {304 22 0.1414 4.7061} | 313 | using System.Collections.Generic;\n\nnamespace Clustering.Graphical.SPC.Profiling;\n\npublic sealed record ProfileCriteria(\n    double AnchorTemperature,\n    (double Lo, double Hi) AnchorBand,\n    double RefinedTemperature,\n    double CorroborationScore,\n    IReadOnlyDictionary<string, double> Enrichments);
49 | clustering/graphical/spc/profiling/signals/ChiPeakSignalAnalyzer.cs | {1343 100 0.2740 4.6051} | 1390 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.Graphical.SPC.Profiling;\n\nnamespace Clustering.Graphical.SPC.Profiling.Signals;\n\npublic sealed class ChiPeakSignalAnalyzer : ISignalAnalyzer\n{\n    public ProfileCriteria Analyze(SweepProfile profile)\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n\n        if (profile.Count == 0)\n            throw new ArgumentException(\"SweepProfile must contain at least one point.\", nameof(profile));\n\n        double anchor = SpcProfileAnalysis.PickPeakTemperature(profile);\n        var band = SpcProfileAnalysis.ComputeHalfMaximumBand(profile);\n        double stability = SpcProfileAnalysis.ComputeStability(profile);\n        double peakChi = profile.Susceptibility.Count > 0\n            ? profile.Susceptibility.Max()\n            : 0.0;\n\n        return new ProfileCriteria(\n            AnchorTemperature: anchor,\n            AnchorBand: band,\n            RefinedTemperature: anchor,\n            CorroborationScore: stability,\n            Enrichments: new Dictionary<string, double>\n            {\n                [\"Stability\"] = stability,\n                [\"BandWidth\"] = band.Hi - band.Lo,\n                [\"PeakChi\"] = peakChi,\n                [\"BondEntropy\"] = profile.BondEntropy ?? double.NaN,\n            });\n    }\n}
50 | clustering/graphical/spc/profiling/signals/ISignalAnalyzer.cs | {185 12 0.0919 4.6214} | 192 | using Clustering.Graphical.SPC.Profiling;\n\nnamespace Clustering.Graphical.SPC.Profiling.Signals;\n\npublic interface ISignalAnalyzer\n{\n    ProfileCriteria Analyze(SweepProfile profile);\n}
51 | clustering/graphical/spc/profiling/signals/MultiSignalConsensusAnalyzer.cs | {4685 480 0.3358 4.5253} | 4850 | using System;\nusing System.Collections.Generic;\n\nnamespace Clustering.Graphical.SPC.Profiling.Signals;\n\npublic sealed class MultiSignalConsensusAnalyzer : ISignalAnalyzer\n{\n    public ProfileCriteria Analyze(SweepProfile profile)\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n        if (profile.Count == 0)\n            throw new ArgumentException(\"SweepProfile must contain at least one point.\", nameof(profile));\n\n        if (profile.Susceptibility.Count != profile.Count\n            || profile.SpecificHeat.Count != profile.Count\n            || profile.LabelEntropy.Count  != profile.Count)\n        {\n            throw new ArgumentException(\n                \"SweepProfile signal arrays must all match Temperatures length.\",\n                nameof(profile));\n        }\n\n        int chiIdx = ArgMax(profile.Susceptibility);\n        int cvIdx  = ArgMax(profile.SpecificHeat);\n        int hIdx   = ArgMaxAbsSlope(profile.LabelEntropy, profile.Temperatures);\n\n        double tcChi = profile.Temperatures[chiIdx];\n        double tcCv  = profile.Temperatures[cvIdx];\n        double tcH   = profile.Temperatures[hIdx];\n\n        double consensusT = MedianOfThree(tcChi, tcCv, tcH);\n\n        double tMin = profile.Temperatures[0];\n        double tMax = profile.Temperatures[profile.Count - 1];\n        if (tMax < tMin) (tMin, tMax) = (tMax, tMin);\n        double sweepRange = tMax - tMin;\n\n        double peakMin = Math.Min(tcChi, Math.Min(tcCv, tcH));\n        double peakMax = Math.Max(tcChi, Math.Max(tcCv, tcH));\n        double peakSpread = peakMax - peakMin;\n\n        double agreement = sweepRange > 0.0\n            ? Math.Clamp(1.0 - peakSpread / sweepRange, 0.0, 1.0)\n            : (peakSpread == 0.0 ? 1.0 : 0.0);\n\n        var band = SpcProfileAnalysis.ComputeHalfMaximumBand(profile);\n        double chiStability = SpcProfileAnalysis.ComputeStability(profile);\n\n        var enrichments = new Dictionary<string, double>\n        {\n\n            [\"Stability\"]   = chiStability,\n            [\"BandWidth\"]   = band.Hi - band.Lo,\n            [\"PeakChi\"]     = profile.Susceptibility[chiIdx],\n            [\"BondEntropy\"] = profile.BondEntropy ?? double.NaN,\n\n            [\"TcChi\"]       = tcChi,\n            [\"TcCv\"]        = tcCv,\n            [\"TcDhDt\"]      = tcH,\n            [\"PeakCv\"]      = profile.SpecificHeat[cvIdx],\n            [\"PeakDhDt\"]    = AbsSlopeAt(profile.LabelEntropy, profile.Temperatures, hIdx),\n            [\"Agreement\"]   = agreement,\n            [\"PeakSpread\"]  = peakSpread,\n        };\n\n        return new ProfileCriteria(\n            AnchorTemperature: tcChi,\n            AnchorBand: band,\n            RefinedTemperature: consensusT,\n            CorroborationScore: agreement,\n            Enrichments: enrichments);\n    }\n\n    private static int ArgMax(IReadOnlyList<double> values)\n    {\n        int bestIndex = 0;\n        double best = values[0];\n        for (int i = 1; i < values.Count; i++)\n        {\n            if (values[i] > best)\n            {\n                best = values[i];\n                bestIndex = i;\n            }\n        }\n        return bestIndex;\n    }\n\n    private static int ArgMaxAbsSlope(IReadOnlyList<double> values, IReadOnlyList<double> temperatures)\n    {\n        if (values.Count < 2) return 0;\n\n        int bestIndex = 0;\n        double best = double.NegativeInfinity;\n        for (int i = 0; i < values.Count; i++)\n        {\n            double slope = AbsSlopeAt(values, temperatures, i);\n            if (slope > best)\n            {\n                best = slope;\n                bestIndex = i;\n            }\n        }\n        return bestIndex;\n    }\n\n    private static double AbsSlopeAt(\n        IReadOnlyList<double> values,\n        IReadOnlyList<double> temperatures,\n        int index)\n    {\n        int n = values.Count;\n        if (n < 2) return 0.0;\n\n        if (index <= 0)\n        {\n            double dT = temperatures[1] - temperatures[0];\n            return dT == 0.0 ? 0.0 : Math.Abs((values[1] - values[0]) / dT);\n        }\n\n        if (index >= n - 1)\n        {\n            double dT = temperatures[n - 1] - temperatures[n - 2];\n            return dT == 0.0 ? 0.0 : Math.Abs((values[n - 1] - values[n - 2]) / dT);\n        }\n\n        double dTcentral = temperatures[index + 1] - temperatures[index - 1];\n        return dTcentral == 0.0\n            ? 0.0\n            : Math.Abs((values[index + 1] - values[index - 1]) / dTcentral);\n    }\n\n    private static double MedianOfThree(double a, double b, double c)\n    {\n        if ((a <= b && b <= c) || (c <= b && b <= a)) return b;\n        if ((b <= a && a <= c) || (c <= a && a <= b)) return a;\n        return c;\n    }\n}
52 | clustering/graphical/spc/profiling/SpcProfileAnalysis.cs | {3383 307 0.2903 4.5005} | 3493 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.Graphical.SPC.Runtime.Sampler;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.Graphical.SPC.Profiling;\n\npublic static class SpcProfileAnalysis\n{\n    public static (double Lo, double Hi) ComputeHalfMaximumBand(SweepProfile profile)\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n\n        if (profile.Temperatures.Count != profile.Susceptibility.Count)\n            throw new ArgumentException(\"SweepProfile temperatures and susceptibility values must have the same length.\", nameof(profile));\n\n        if (profile.Count == 0)\n            throw new ArgumentException(\"Profile must contain at least one point.\", nameof(profile));\n\n        double chiMax = profile.Susceptibility.Max();\n        if (chiMax <= 0.0) return (profile.Temperatures[0], profile.Temperatures[^1]);\n\n        double half = chiMax * 0.5;\n        double lo = profile.Temperatures[0];\n        double hi = profile.Temperatures[^1];\n\n        for (int i = 0; i < profile.Count; i++)\n        {\n            if (profile.Susceptibility[i] >= half)\n            {\n                lo = profile.Temperatures[i];\n                break;\n            }\n        }\n\n        for (int i = profile.Count - 1; i >= 0; i--)\n        {\n            if (profile.Susceptibility[i] >= half)\n            {\n                hi = profile.Temperatures[i];\n                break;\n            }\n        }\n\n        if (lo >= hi)\n            return (profile.Temperatures[0], profile.Temperatures[^1]);\n\n        return (lo, hi);\n    }\n\n    public static double PickPeakTemperature(SweepProfile profile)\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n\n        if (profile.Temperatures.Count != profile.Susceptibility.Count)\n            throw new ArgumentException(\"SweepProfile temperatures and susceptibility values must have the same length.\", nameof(profile));\n\n        if (profile.Count == 0)\n            throw new ArgumentException(\"Profile must contain at least one point.\", nameof(profile));\n\n        int bestIndex = 0;\n        double bestChi = profile.Susceptibility[0];\n        for (int i = 1; i < profile.Count; i++)\n        {\n            double chi = profile.Susceptibility[i];\n            if (chi > bestChi)\n            {\n                bestChi = chi;\n                bestIndex = i;\n            }\n        }\n\n        return profile.Temperatures[bestIndex];\n    }\n\n    public static double ComputeStability(SweepProfile profile)\n    {\n        if (profile is null)\n            throw new ArgumentNullException(nameof(profile));\n\n        if (profile.Temperatures.Count != profile.Susceptibility.Count)\n            throw new ArgumentException(\"SweepProfile temperatures and susceptibility values must have the same length.\", nameof(profile));\n\n        if (profile.Count < 2) return 0.0;\n        var (lo, hi) = ComputeHalfMaximumBand(profile);\n        double full = profile.Temperatures[^1] - profile.Temperatures[0];\n        if (full <= 0.0) return 0.0;\n        return Math.Clamp(1.0 - (hi - lo) / full, 0.0, 1.0);\n    }\n\n    public static double ComputeFkSusceptibility(PottsRunResult result)\n    {\n        if (result is null)\n            throw new ArgumentNullException(nameof(result));\n\n        return result.FkSusceptibility;\n    }\n}
53 | clustering/graphical/spc/profiling/SweepProfile.cs | {3157 218 0.2338 4.7872} | 3250 | using System;\nusing System.Collections.Generic;\nusing System.Linq;\nusing Clustering.Graphical.SPC.Observables.Reductions;\nusing Clustering.Graphical.SPC.Runtime.Execution;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\n\nnamespace Clustering.Graphical.SPC.Profiling;\n\npublic sealed record SweepProfile(\n    IReadOnlyList<double> Temperatures,\n    IReadOnlyList<double> Susceptibility,\n    IReadOnlyList<double> SpecificHeat,\n    IReadOnlyList<double> LabelEntropy,\n    double? BondEntropy,\n    IReadOnlyDictionary<string, IReadOnlyList<double>> AdditionalChannels)\n{\n    public static SweepProfile Empty { get; } = new(\n        Array.Empty<double>(),\n        Array.Empty<double>(),\n        Array.Empty<double>(),\n        Array.Empty<double>(),\n        null,\n        new Dictionary<string, IReadOnlyList<double>>());\n\n    public int Count => Temperatures.Count;\n\n    public bool IsEmpty => Count == 0;\n\n    public SweepProfile WithChannel(string name, IReadOnlyList<double> values)\n    {\n        if (name is null) throw new ArgumentNullException(nameof(name));\n        if (values is null) throw new ArgumentNullException(nameof(values));\n        if (values.Count != Count)\n            throw new ArgumentException(\"Channel values must match temperature length.\", nameof(values));\n\n        var channels = AdditionalChannels.ToDictionary(kvp => kvp.Key, kvp => kvp.Value);\n        channels[name] = values;\n        return this with { AdditionalChannels = channels };\n    }\n\n    public static SweepProfile From(IReadOnlyList<SpcRunResult> runs)\n    {\n        ArgumentNullException.ThrowIfNull(runs);\n        if (runs.Count == 0)\n            return Empty;\n\n        var frames = runs.Select(run => run.Frame).ToArray();\n\n        var susceptibility = Clustering.Graphical.SPC.Observables.Reductions.Susceptibility.From(frames);\n        var specificHeat = Clustering.Graphical.SPC.Observables.Reductions.SpecificHeat.From(frames);\n        var labelEntropy = Clustering.Graphical.SPC.Observables.Reductions.LabelEntropy.From(frames);\n        var meanEnergy = Clustering.Graphical.SPC.Observables.Reductions.MeanEnergy.From(frames);\n        var magnetization = Clustering.Graphical.SPC.Observables.Reductions.Magnetization.From(frames);\n\n        var channels = new Dictionary<string, IReadOnlyList<double>>(StringComparer.Ordinal)\n        {\n            [\"MeanEnergy\"] = meanEnergy.Energy,\n            [\"MeanMagnetization\"] = magnetization.Mean,\n            [\"MagnetizationSecondMoment\"] = magnetization.SecondMoment,\n            [\"MagnetizationVariance\"] = magnetization.Variance,\n        };\n\n        double? bondEntropy = null;\n        for (int i = runs.Count - 1; i >= 0; i--)\n        {\n            var edgeObservables = runs[i].EdgeObservables;\n            if (edgeObservables is not null)\n            {\n                bondEntropy = BondFrequencyReduction.EntropyNats(edgeObservables);\n                break;\n            }\n        }\n\n        return new SweepProfile(\n            susceptibility.Temperatures,\n            susceptibility.Chi,\n            specificHeat.Cv,\n            labelEntropy.Entropy,\n            bondEntropy,\n            channels);\n    }\n\n}
54 | clustering/graphical/spc/runtime/execution/IFrameSink.cs | {289 24 0.1384 4.8903} | 301 | using Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic interface IFrameSink\n{\n\n    bool TaskAlreadyCompleted(SpcTaskSpec task);\n\n    void Accept(SpcTaskSpec task, SpcRunResult result);\n\n    SpcRunResult? TryLoad(SpcTaskSpec task, CsrGraph graph) => null;\n}
55 | clustering/graphical/spc/runtime/execution/ISpcExecutor.cs | {1706 121 0.2169 4.6813} | 1758 | using System.Collections.Generic;\nusing System.Threading;\nusing Clustering.Graphical.SPC.Runtime.Sampler;\nusing Clustering.Graphical.SPC.Runtime.Sampler.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution;\n\npublic interface ISpcExecutor\n{\n\n    List<SpcTaskSpec> BuildTaskList(\n        IReadOnlyList<double> temperatures,\n        int numReplicas,\n        int cyclesPerTask,\n        int q,\n        PottsObservableTier observableTier,\n        string checkpointDirectory,\n        int? baseSeed = null,\n        int burnInCyclesPerTask = 0);\n\n    void Run(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null);\n\n    SpcBatchResult RunBatch(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null);\n\n    IReadOnlyList<SpcRunResult> ExecuteAll(\n        CsrGraph graph,\n        IReadOnlyList<SpcTaskSpec> tasks,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null);\n\n    IReadOnlyList<SpcRunResult> ExecuteAll(\n        IReadOnlyList<PottsRunSpec> specs,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null);\n\n    SpcBatchResult ExecuteBatch(\n        IReadOnlyList<PottsRunSpec> specs,\n        CancellationToken ct = default,\n        SpcExecutionOptions? executionOptions = null);\n\n    SpcRunResult ExecuteTask(CsrGraph graph, SpcTaskSpec task, SpcExecutionOptions? executionOptions = null);\n\n    SpcRunResult ExecuteTask(PottsRunSpec spec, SpcExecutionOptions? executionOptions = null);\n}
56 | clustering/graphical/spc/runtime/execution/sinks/InMemoryFrameSink.cs | {547 46 0.1444 4.8709} | 565 | using System.Collections.Concurrent;\nusing System.Collections.Generic;\nusing System.Linq;\n\nnamespace Clustering.Graphical.SPC.Runtime.Execution.Sinks;\n\npublic sealed class InMemoryFrameSink : IFrameSink\n{\n    private readonly ConcurrentBag<SpcRunResult> _results = new();\n\n    public bool TaskAlreadyCompleted(SpcTaskSpec task) => false;\n\n    public void Accept(SpcTaskSpec task, SpcRunResult result)\n        => _results.Add(result);\n\n    public IReadOnlyList<SpcRunResult> Results => _results.ToArray();\n\n    public int Count => _results.Count;\n}
