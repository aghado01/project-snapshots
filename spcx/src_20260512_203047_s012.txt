idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
64 | linalg/Cholesky.cs | {7045 874 0.3851 4.3637} | 7966 | using System;\n\nnamespace LinearAlgebra\n{\n    /// <summary>\n    /// In-place lower-triangular Cholesky decomposition Σ = LLᵀ with derived quantities\n    /// for Gaussian statistics: ln|Σ|, Σ⁻¹, and Cholesky-factor sampling.\n    /// </summary>\n    /// <remarks>\n    /// <para>\n    /// Allocate once per component with <see cref=\"CholeskyDecomposition(int)\"/>, then call\n    /// <see cref=\"Decompose\"/> each time the covariance changes. This avoids per-iteration\n    /// heap allocations in EM loops.\n    /// </para>\n    /// <para>\n    /// <strong>Diagonal floor.</strong> If a pivot drops to ≤ 1e-12 during decomposition\n    /// it is clamped, providing a soft guard against near-singular matrices without\n    /// throwing. Callers that require strict positive-definiteness should add a\n    /// regularisation term to the diagonal before calling <see cref=\"Decompose\"/>.\n    /// </para>\n    /// </remarks>\n    public sealed class CholeskyDecomposition\n    {\n        private readonly int _dim;\n\n        // Lower-triangular factor L  (Σ = L·Lᵀ).\n        // Upper triangle is always zero; only [i, j] for j ≤ i is written.\n        private readonly double[,] _l;\n\n        // L⁻¹ — lower-triangular intermediate produced during Decompose.\n        // Not meaningful to callers; used internally to build Σ⁻¹.\n        private readonly double[,] _lInv;\n\n        /// <summary>\n        /// ln|Σ| = 2 · Σ_i ln(L_{ii}). Valid after <see cref=\"Decompose\"/> has been called.\n        /// </summary>\n        public double LogDet { get; private set; }\n\n        /// <param name=\"dim\">Dimensionality of the square covariance matrix.</param>\n        public CholeskyDecomposition(int dim)\n        {\n            _dim = dim;\n            _l = new double[dim, dim];\n            _lInv = new double[dim, dim];\n        }\n\n        // ── Decompose ────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Decomposes <paramref name=\"sigma\"/> into L·Lᵀ, updating <see cref=\"LogDet\"/>\n        /// and preparing the internal L⁻¹ required by <see cref=\"WriteInverseTo\"/>\n        /// and <see cref=\"Sample\"/>.\n        /// </summary>\n        /// <param name=\"sigma\">\n        /// Square, symmetric, positive-semi-definite covariance matrix of size dim × dim.\n        /// Only the lower triangle is read; the upper triangle is ignored.\n        /// </param>\n        public void Decompose(double[,] sigma)\n        {\n            double logDetL = 0.0;\n\n            // ── Step 1: L via Cholesky-Banachiewicz ──────────────────────────────\n            for (int i = 0; i < _dim; i++)\n            {\n                for (int j = 0; j <= i; j++)\n                {\n                    double sum = sigma[i, j];\n                    for (int k = 0; k < j; k++)\n                        sum -= _l[i, k] * _l[j, k];\n\n                    if (i == j)\n                    {\n                        if (sum <= 1e-12) sum = 1e-12; // diagonal floor\n                        _l[i, i] = Math.Sqrt(sum);\n                        logDetL += Math.Log(_l[i, i]);\n                    }\n                    else\n                    {\n                        _l[i, j] = sum / _l[j, j];\n                        _l[j, i] = 0.0; // keep upper triangle zero\n                    }\n                }\n            }\n\n            LogDet = 2.0 * logDetL; // ln|Σ| = 2·ln|L|\n\n            // ── Step 2: L⁻¹ via forward substitution ────────────────────────────\n            for (int i = 0; i < _dim; i++)\n            {\n                _lInv[i, i] = 1.0 / _l[i, i];\n                for (int j = 0; j < i; j++)\n                {\n                    double sum = 0.0;\n                    for (int k = j; k < i; k++)\n                        sum -= _l[i, k] * _lInv[k, j];\n                    _lInv[i, j] = sum / _l[i, i];\n                    _lInv[j, i] = 0.0;\n                }\n            }\n        }\n\n        // ── Σ⁻¹ ─────────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Writes Σ⁻¹ = (L⁻¹)ᵀ · L⁻¹ into <paramref name=\"target\"/>.\n        /// Must be called after <see cref=\"Decompose\"/>.\n        /// </summary>\n        /// <param name=\"target\">Pre-allocated dim × dim matrix to receive Σ⁻¹.</param>\n        public void WriteInverseTo(double[,] target)\n        {\n            // L⁻¹ is lower-triangular: _lInv[k, i] = 0 for k < i.\n            // Sum therefore starts at k = j (the larger of i and j).\n            for (int i = 0; i < _dim; i++)\n            {\n                for (int j = i; j < _dim; j++)\n                {\n                    double sum = 0.0;\n                    for (int k = j; k < _dim; k++)\n                        sum += _lInv[k, i] * _lInv[k, j];\n                    target[i, j] = sum;\n                    target[j, i] = sum; // Σ⁻¹ is symmetric\n                }\n            }\n        }\n\n        // ── L read-out ───────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Writes the lower-triangular Cholesky factor L row-major into <paramref name=\"dst\"/>.\n        /// Length of <paramref name=\"dst\"/> must be dim×dim. Upper-triangle entries are written as 0.\n        /// Must be called after <see cref=\"Decompose\"/>.\n        /// </summary>\n        public void WriteLTo(Span<double> dst)\n        {\n            for (int i = 0; i < _dim; i++)\n                for (int j = 0; j < _dim; j++)\n                    dst[i * _dim + j] = _l[i, j];\n        }\n\n        // ── Sampling ─────────────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Returns a sample x = <paramref name=\"mean\"/> + L·z where z ~ N(0, I),\n        /// using the Box-Muller transform to generate standard normals.\n        /// Must be called after <see cref=\"Decompose\"/>.\n        /// </summary>\n        /// <param name=\"rng\">Random number generator.</param>\n        /// <param name=\"mean\">Mean vector of length dim.</param>\n        public double[] Sample(Random rng, double[] mean)\n        {\n            // Generate z ~ N(0, I) via Box-Muller pairs.\n            double[] z = new double[_dim];\n            for (int i = 0; i < _dim - 1; i += 2)\n            {\n                double u1 = 1.0 - rng.NextDouble(); // (0,1] avoids log(0)\n                double u2 = 1.0 - rng.NextDouble();\n                double mag = Math.Sqrt(-2.0 * Math.Log(u1));\n                z[i] = mag * Math.Cos(2.0 * Math.PI * u2);\n                z[i + 1] = mag * Math.Sin(2.0 * Math.PI * u2);\n            }\n            if (_dim % 2 == 1)\n            {\n                double u1 = 1.0 - rng.NextDouble();\n                double u2 = 1.0 - rng.NextDouble();\n                z[_dim - 1] = Math.Sqrt(-2.0 * Math.Log(u1)) * Math.Cos(2.0 * Math.PI * u2);\n            }\n\n            // x = μ + L·z  (L is lower-triangular: _l[i, j] = 0 for j > i)\n            double[] x = new double[_dim];\n            for (int i = 0; i < _dim; i++)\n            {\n                x[i] = mean[i];\n                for (int j = 0; j <= i; j++)\n                    x[i] += _l[i, j] * z[j];\n            }\n            return x;\n        }\n    }\n} |
65 | linalg/Frobenius.cs | {2462 310 0.3461 4.4362} | 2577 | using System;\n\nnamespace LinearAlgebra\n{\n    /// <summary>\n    /// Frobenius norm and related matrix-difference utilities.\n    /// </summary>\n    /// <remarks>\n    /// Primary consumers: coordinated expansion loop scatter-delta stopping signal\n    /// (‖ΔΣ_k‖_F) and NIW prior Ψ update convergence checks. All methods are\n    /// allocation-free and operate directly on 2D double arrays.\n    /// </remarks>\n    public static class MatrixNorms\n    {\n        /// <summary>\n        /// Frobenius norm: ‖A‖_F = √(Σᵢⱼ aᵢⱼ²).\n        /// </summary>\n        public static double FrobeniusNorm(double[,] a)\n        {\n            int rows = a.GetLength(0), cols = a.GetLength(1);\n            double sum = 0.0;\n            for (int i = 0; i < rows; i++)\n                for (int j = 0; j < cols; j++)\n                    sum += a[i, j] * a[i, j];\n            return Math.Sqrt(sum);\n        }\n\n        /// <summary>\n        /// Frobenius norm of the element-wise difference: ‖A − B‖_F.\n        /// </summary>\n        /// <remarks>\n        /// Used as the scatter-delta stopping signal in the coordinated expansion loop:\n        /// when ‖Σ_k(t) − Σ_k(t−1)‖_F &lt; ε, seed k has absorbed its natural territory\n        /// and its contribution to the loop should stop.\n        /// </remarks>\n        /// <param name=\"a\">Current matrix snapshot.</param>\n        /// <param name=\"b\">Previous matrix snapshot. Must match dimensions of <paramref name=\"a\"/>.</param>\n        public static double FrobeniusDelta(double[,] a, double[,] b)\n        {\n            int rows = a.GetLength(0), cols = a.GetLength(1);\n            double sum = 0.0;\n            for (int i = 0; i < rows; i++)\n                for (int j = 0; j < cols; j++)\n                {\n                    double d = a[i, j] - b[i, j];\n                    sum += d * d;\n                }\n            return Math.Sqrt(sum);\n        }\n\n        /// <summary>\n        /// Copies all elements of <paramref name=\"src\"/> into <paramref name=\"dst\"/> in place.\n        /// Convenience used alongside <see cref=\"FrobeniusDelta\"/> to snapshot a covariance\n        /// matrix before the M-step. Dimensions must match.\n        /// </summary>\n        public static void CopyTo(double[,] src, double[,] dst)\n        {\n            int rows = src.GetLength(0), cols = src.GetLength(1);\n            for (int i = 0; i < rows; i++)\n                for (int j = 0; j < cols; j++)\n                    dst[i, j] = src[i, j];\n        }\n    }\n} |
66 | losses/IRobustLoss.cs | {1189 144 0.1665 4.6289} | 1222 | // ============================================================================\n// Losses/IRobustLoss.cs\n// ============================================================================\nnamespace Losses;\n\npublic interface IRobustLoss\n{\n    /// <summary>L² returns true (single Karcher iteration suffices). Iterative losses return false.</summary>\n    static abstract bool IsClosedForm { get; }\n\n    /// <summary>L¹ returns true (loss is non-smooth at coincident data points). Smooth losses (L², Huber, Tukey on the bulk) return false.</summary>\n    static abstract bool IsSingularAtZero { get; }\n\n    /// <summary>IRLS weight w(r) = ψ(r)/r. Used for both the location iteration and the scatter accumulation.</summary>\n    static abstract double Weight(double r);\n}\n\npublic readonly struct L2Loss : IRobustLoss\n{\n    public static bool IsClosedForm => true;\n    public static bool IsSingularAtZero => false;\n    public static double Weight(double r) => 1.0;\n}\n\npublic readonly struct L1Loss : IRobustLoss\n{\n    public static bool IsClosedForm => false;\n    public static bool IsSingularAtZero => true;\n    public static double Weight(double r) => 1.0 / r;   // caller responsible for r > 0\n} |
67 | manifolds/EuclideanVectorManifold.cs | {1609 150 0.2175 4.5694} | 1648 | // ============================================================================\n// Manifolds/EuclideanVectorManifold.cs\n// ============================================================================\n// Flat Euclidean R^d.  Points are passed as ReadOnlySpan<double> / Span<double>\n// at all call sites.  The 1D case is EuclideanVectorManifold(dimension: 1).\n// ============================================================================\nusing System;\nusing System.Numerics.Tensors;\n\nnamespace Manifolds\n{\n    public readonly struct EuclideanVectorManifold : IRiemannianManifold\n    {\n        public static bool IsFlat => true;\n        public int Dimension { get; }\n\n        public EuclideanVectorManifold(int dimension) => Dimension = dimension;\n\n        public double Distance(ReadOnlySpan<double> p, ReadOnlySpan<double> q)\n            => TensorPrimitives.Distance<double>(p, q);\n\n        public void LogMap(ReadOnlySpan<double> p, ReadOnlySpan<double> q, Span<double> dst)\n            => TensorPrimitives.Subtract<double>(q, p, dst);\n\n        public void ExpMap(ReadOnlySpan<double> p, ReadOnlySpan<double> v, Span<double> dst)\n            => TensorPrimitives.Add<double>(p, v, dst);\n\n        public void AddScaled(Span<double> dst, ReadOnlySpan<double> v, double scalar)\n        {\n            for (int i = 0; i < Dimension; i++) dst[i] += v[i] * scalar;\n        }\n\n        public double Norm(ReadOnlySpan<double> p, ReadOnlySpan<double> v)\n        {\n            double sumSq = 0;\n            for (int i = 0; i < Dimension; i++) sumSq += v[i] * v[i];\n            return Math.Sqrt(sumSq);\n        }\n    }\n} |
68 | manifolds/geodesics/FisherRaoHalfPlane.cs | {3810 455 0.2197 4.8489} | 3910 | // ============================================================================\n// Metrics/FisherRaoHalfPlane.cs\n// ============================================================================\n// Fisher-Rao geodesic distance on the univariate Gaussian statistical manifold,\n// parameterised as (mu, log_sigma).\n//\n// The Fisher information matrix for N(mu, sigma) is diagonal:\n//\n//   I(mu, sigma) = diag(1/sigma^2, 2/sigma^2)\n//\n// This induces the Poincaré upper half-plane metric on the (mu, sigma) plane\n// (sigma > 0). The exact geodesic distance is:\n//\n//   d_FR(p, q) = sqrt(2) * arccosh(1 + [(mu1-mu2)^2 + 2*(sigma1-sigma2)^2]\n//                                       / (4 * sigma1 * sigma2))\n//\n// Reference: Costa et al., \"Fisher information distance: a geometrical reading\"\n// (2015); also derivable directly from the Poincaré half-plane metric.\n//\n// INPUT CONTRACT:\n//   Each point must be a 2-element double[] = [mu, log_sigma].\n//   log_sigma (not sigma) is used as the feature coordinate — common in\n//   GaussianManifold generator output where log_sigma prevents sigma ≤ 0.\n//   sigma is recovered via exp(log_sigma) internally.\n//\n// CONTRAST WITH FisherRaoSimplex:\n//   FisherRaoSimplex operates on discrete probability mass vectors (D-simplex).\n//   FisherRaoHalfPlane operates on continuous Gaussian parameters.\n//   Both are Fisher-Rao geodesics but on different statistical manifolds.\n//   Neither calls the other; they share no hot-path code.\n//\n// FUTURE — IStatisticalManifold:\n//   When multivariate Gaussian or Dirichlet support is added, a shared\n//   IStatisticalManifold interface (extending IRiemannianManifold) will\n//   surface FisherInformationAt(theta, fim) as a callable primitive for\n//   numerical geodesic integration. The closed-form classes (this one and\n//   FisherRaoSimplex) will implement that interface, overriding\n//   GeodesicDistance directly so the integrator is never invoked.\n// ============================================================================\nusing System;\nusing System.Runtime.CompilerServices;\n\nnamespace DistanceMetrics\n{\n    public static class FisherRaoHalfPlane\n    {\n        /// <summary>\n        /// Fisher-Rao geodesic distance between two univariate Gaussians,\n        /// each encoded as <c>[mu, log_sigma]</c>.\n        /// Explicit length guard: fixed 2-element contract; direct indexing via <c>p[0]</c>/<c>p[1]</c> would throw OOB for shorter inputs but silently read wrong values for longer ones.\n        /// </summary>\n        /// <param name=\"p\">2-element array <c>[mu1, log_sigma1]</c>.</param>\n        /// <param name=\"q\">2-element array <c>[mu2, log_sigma2]</c>.</param>\n        /// <returns>\n        /// The geodesic distance on the Poincaré upper half-plane.\n        /// Returns 0 for identical inputs; always non-negative.\n        /// </returns>\n        /// <exception cref=\"ArgumentException\">\n        /// Thrown if either array has length other than 2.\n        /// </exception>\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        public static double Distance(double[] p, double[] q)\n        {\n            if (p.Length != 2 || q.Length != 2)\n                throw new ArgumentException(\n                    \"FisherRaoHalfPlane requires 2-element [mu, log_sigma] vectors.\");\n\n            double dmu = p[0] - q[0];\n            double sig1 = Math.Exp(p[1]);\n            double sig2 = Math.Exp(q[1]);\n            double dsig = sig1 - sig2;\n\n            // z = 1 + [(mu1-mu2)^2 + 2*(sigma1-sigma2)^2] / (4*sigma1*sigma2)\n            // z >= 1 always (numerator >= 0, denominator > 0), so arccosh is real.\n            double z = 1.0 + (dmu * dmu + 2.0 * dsig * dsig) / (4.0 * sig1 * sig2);\n\n            return Sqrt2 * Math.Acosh(z);\n        }\n\n        private const double Sqrt2 = 1.4142135623730950488;\n    }\n} |
69 | manifolds/geodesics/FisherRaoSimplex.cs | {3810 426 0.2995 4.6171} | 3911 | using System;\nusing System.Buffers;\nusing System.Numerics.Tensors;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace DistanceMetrics\n{\n    /// <summary>\n    /// Fisher-Rao geodesic distance on the probability simplex (discrete\n    /// probability mass vectors). Uses the Bhattacharyya-angle form:\n    ///   d = 2 * arccos(sum_i sqrt(p_i * q_i))\n    /// which is the exact geodesic on the statistical manifold of categorical\n    /// distributions.\n    ///\n    /// INPUT CONTRACT: each input must be a non-negative D-vector summing to 1\n    /// (a probability mass function). Typical uses: topic model outputs, LDA\n    /// document-topic distributions, any discrete distribution embedding.\n    ///\n    /// CONTRAST WITH FisherRaoHalfPlane:\n    ///   FisherRaoHalfPlane is the exact geodesic for continuous univariate\n    ///   Gaussian parameters [mu, log_sigma] on the Poincaré upper half-plane.\n    ///   Use FisherRaoSimplex for discrete PMFs; use FisherRaoHalfPlane for\n    ///   Gaussian parameter spaces (e.g. GaussianManifold generator).\n    ///\n    /// LENGTH GUARD: explicit check for p.Length != q.Length — TensorPrimitives slices\n    ///   both spans to p.Length, so a shorter q would throw inside TensorPrimitives with\n    ///   an unhelpful bounds error rather than at the call boundary.\n    ///\n    /// FUTURE — IStatisticalManifold:\n    ///   Both classes will implement IStatisticalManifold once multivariate or\n    ///   Dirichlet families are added (those require numerical geodesic\n    ///   integration over the FIM and need a shared interface).\n    /// </summary>\n    public static class FisherRaoSimplex\n    {\n        // Single len-sized scratch — same three-tier pattern as Mahalanobis / JSD.\n        private static readonly ThreadLocal<double[]> _frScratch =\n            new(() => new double[512]);\n\n        public static double Distance(double[] p, double[] q)\n        {\n            int len = p.Length;\n            if (len == 0) return 0.0;\n            if (q.Length != len)\n                throw new ArgumentException(\n                    $\"FisherRaoSimplex requires equal-length PMF vectors (p={len}, q={q.Length}).\");\n\n            if (len <= 64)\n            {\n                Span<double> buf = stackalloc double[len];\n                return SimplexCore(p, q, buf, len);\n            }\n\n            if (len <= 512)\n            {\n                return SimplexCore(p, q, _frScratch.Value.AsSpan(0, len), len);\n            }\n\n            var pool = ArrayPool<double>.Shared;\n            double[] rented = pool.Rent(len);\n            try\n            {\n                return SimplexCore(p, q, rented.AsSpan(0, len), len);\n            }\n            finally\n            {\n                pool.Return(rented);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double SimplexCore(double[] p, double[] q, Span<double> buf, int len)\n        {\n            var pSpan = p.AsSpan(0, len);\n            var qSpan = q.AsSpan(0, len);\n\n            // bhat = sum(sqrt(p*q)). Max-clamp to 0 before sqrt guards against\n            // tiny negative values from FP noise that would otherwise yield NaN.\n            // Replaces the original `if (p[i] > 0 && q[i] > 0)` guard, which\n            // was redundant for proper probability inputs but blocked SIMD.\n            TensorPrimitives.Multiply<double>(pSpan, qSpan, buf);\n            TensorPrimitives.Max<double>(buf, 0.0, buf);\n            TensorPrimitives.Sqrt<double>(buf, buf);\n            double bhat = TensorPrimitives.Sum<double>(buf);\n\n            // Clamp before arccos for numerical safety (sum can drift just past 1).\n            if (bhat > 1.0) bhat = 1.0;\n            else if (bhat < -1.0) bhat = -1.0;\n            return 2.0 * Math.Acos(bhat);\n        }\n    }\n} |
70 | manifolds/geodesics/Poincare.cs | {1884 262 0.3705 4.3351} | 1940 | using System;\n\nnamespace DistanceMetrics\n{\n    public static class Poincare\n    {\n        /// <summary>\n        /// Poincare (hyperbolic) distance in the unit ball model.\n        /// d(u,v) = arcosh(1 + 2 * norm(u-v)^2 / ((1-norm(u)^2)(1-norm(v)^2))).\n        /// Assumes points lie inside the open unit ball.\n        /// Points on or outside the boundary are clipped to a large finite distance.\n        /// Explicit length guard: loop accumulates <c>normB2</c> over <c>a.Length</c> iterations — a shorter <c>b</c> would silently under-accumulate without throwing.\n        /// </summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            if (a == null || b == null || a.Length != b.Length)\n                throw new ArgumentException(\"Invalid feature vectors for Poincare distance.\");\n\n            int dim = a.Length;\n\n            double normA2 = 0.0, normB2 = 0.0, diff2 = 0.0;\n\n            for (int i = 0; i < dim; i++)\n            {\n                double ai = a[i], bi = b[i];\n                normA2 += ai * ai;\n                normB2 += bi * bi;\n                double d = ai - bi;\n                diff2 += d * d;\n            }\n\n            // Numerical stability: clip points near or outside the unit ball\n            const double eps = 1e-12;\n            if (normA2 >= 1.0 - eps || normB2 >= 1.0 - eps)\n                return 1e9; // large finite distance — prevents NaN / Inf in downstream kernels\n\n            double denom = (1.0 - normA2) * (1.0 - normB2);\n            if (denom < eps)\n                return 1e9;\n\n            double arg = 1.0 + 2.0 * diff2 / denom;\n\n            // arcosh(x) = log(x + sqrt(x*x - 1)) for x >= 1\n            if (arg < 1.0)\n                return 0.0; // identical points after clipping\n\n            double sqrtTerm = Math.Sqrt(arg * arg - 1.0);\n            return Math.Log(arg + sqrtTerm);\n        }\n    }\n} |
71 | manifolds/IRiemannianManifold.cs | {2093 202 0.2126 4.6392} | 2146 | // ============================================================================\n// Manifolds/IRiemannianManifold.cs\n// ============================================================================\n// Decision: non-generic, span-based throughout.\n// ProductManifold already implements this shape. EuclideanVectorManifold is\n// updated to match (see EuclideanVectorManifold.cs).\n// Points are represented as double[] (or ReadOnlySpan<double> at call sites).\n// ============================================================================\nusing System;\n\nnamespace Manifolds\n{\n    public interface IRiemannianManifold\n    {\n        /// <summary>\n        /// Compile-time flat-space flag. JIT erases curved-path dead branches\n        /// for EuclideanVectorManifold specialisations.\n        /// </summary>\n        static abstract bool IsFlat { get; }\n\n        /// <summary>\n        /// Tangent-space dimension D. For product manifolds, the sum of\n        /// per-factor dimensions.\n        /// </summary>\n        int Dimension { get; }\n\n        /// <summary>Geodesic distance d(p, q).</summary>\n        double Distance(ReadOnlySpan<double> p, ReadOnlySpan<double> q);\n\n        /// <summary>\n        /// Writes log_p(q) into <paramref name=\"dst\"/> (length = Dimension).\n        /// </summary>\n        void LogMap(ReadOnlySpan<double> p, ReadOnlySpan<double> q, Span<double> dst);\n\n        /// <summary>\n        /// Writes exp_p(v) into <paramref name=\"dst\"/> (length = Dimension).\n        /// </summary>\n        void ExpMap(ReadOnlySpan<double> p, ReadOnlySpan<double> v, Span<double> dst);\n\n        /// <summary>dst += scalar * v  (in-place tangent accumulation).</summary>\n        void AddScaled(Span<double> dst, ReadOnlySpan<double> v, double scalar);\n\n        /// <summary>\n        /// Riemannian norm ||v||_p. For flat manifolds this is just the\n        /// Euclidean norm of the tangent vector; for curved manifolds it equals\n        /// the geodesic length of the corresponding exp_p(v) path.\n        /// </summary>\n        double Norm(ReadOnlySpan<double> p, ReadOnlySpan<double> v);\n    }\n} |
72 | manifolds/RiemannianProductManifold.cs | {2084 196 0.2855 4.6097} | 2141 | // ============================================================================\n// Manifolds/ProductManifold.cs\n// ============================================================================\nusing System;\n\nnamespace Manifolds\n{\n    public readonly struct ProductManifold<TA, TB> : IRiemannianManifold\n        where TA : struct, IRiemannianManifold\n        where TB : struct, IRiemannianManifold\n    {\n        public static bool IsFlat => TA.IsFlat && TB.IsFlat;\n\n        private readonly TA _a;\n        private readonly TB _b;\n        private readonly int _dimA;\n        public int Dimension { get; }\n\n        public ProductManifold(TA a, TB b)\n        {\n            _a = a;\n            _b = b;\n            _dimA = a.Dimension;\n            Dimension = a.Dimension + b.Dimension;\n        }\n\n        public double Distance(ReadOnlySpan<double> p, ReadOnlySpan<double> q)\n        {\n            double dA = _a.Distance(p.Slice(0, _dimA), q.Slice(0, _dimA));\n            double dB = _b.Distance(p.Slice(_dimA), q.Slice(_dimA));\n            return Math.Sqrt(dA * dA + dB * dB);\n        }\n\n        public void LogMap(ReadOnlySpan<double> p, ReadOnlySpan<double> q, Span<double> dst)\n        {\n            _a.LogMap(p.Slice(0, _dimA), q.Slice(0, _dimA), dst.Slice(0, _dimA));\n            _b.LogMap(p.Slice(_dimA), q.Slice(_dimA), dst.Slice(_dimA));\n        }\n\n        public void ExpMap(ReadOnlySpan<double> p, ReadOnlySpan<double> v, Span<double> dst)\n        {\n            _a.ExpMap(p.Slice(0, _dimA), v.Slice(0, _dimA), dst.Slice(0, _dimA));\n            _b.ExpMap(p.Slice(_dimA), v.Slice(_dimA), dst.Slice(_dimA));\n        }\n\n        public void AddScaled(Span<double> dst, ReadOnlySpan<double> v, double scalar)\n        {\n            for (int i = 0; i < Dimension; i++) dst[i] += v[i] * scalar;\n        }\n\n        public double Norm(ReadOnlySpan<double> p, ReadOnlySpan<double> v)\n        {\n            double a = _a.Norm(p.Slice(0, _dimA), v.Slice(0, _dimA));\n            double b = _b.Norm(p.Slice(_dimA), v.Slice(_dimA));\n            return Math.Sqrt(a * a + b * b);\n        }\n    }\n} |
73 | metrics/Canberra.cs | {578 69 0.3824 4.2053} | 597 | using System;\n\nnamespace DistanceMetrics\n{\n    public static class Canberra\n    {\n        /// <summary>Mismatched lengths throw at <c>b[i]</c> via array indexing; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double denom = Math.Abs(a[i]) + Math.Abs(b[i]);\n                if (denom < 1e-15) continue;\n                sum += Math.Abs(a[i] - b[i]) / denom;\n            }\n            return sum;\n        }\n    }\n} |
74 | metrics/Cosine.cs | {663 66 0.2579 4.6362} | 683 | using System;\nusing System.Numerics.Tensors;\n\nnamespace DistanceMetrics\n{\n    public static class Cosine\n    {\n        /// <summary>Mismatched lengths throw inside <c>TensorPrimitives.CosineSimilarity</c>; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            double cosSim = TensorPrimitives.CosineSimilarity<double>(a, b);\n            if (double.IsNaN(cosSim)) return 1.0; // zero vector → max distance\n\n            // Clamp to [-1,1] for numerical safety before arccos\n            cosSim = Math.Max(-1.0, Math.Min(1.0, cosSim));\n            return Math.Acos(cosSim) / Math.PI;\n        }\n    }\n} |
75 | metrics/Euclidean.cs | {386 36 0.2383 4.5318} | 399 | using System;\nusing System.Numerics.Tensors;\n\nnamespace DistanceMetrics\n{\n    public static class Euclidean\n    {\n        /// <summary>Mismatched lengths throw inside <c>TensorPrimitives.Distance</c>; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b)\n        {\n            return TensorPrimitives.Distance<double>(a, b);\n        }\n    }\n} |
76 | metrics/Hamming.cs | {237 27 0.3165 4.2224} | 249 | using System;\nusing System.Numerics;\n\nnamespace DistanceMetrics\n{\n    public static class Hamming\n    {\n        public static double Distance(ulong a, ulong b)\n        {\n            return BitOperations.PopCount(a ^ b);\n        }\n    }\n} |
77 | metrics/Jaccard.cs | {725 95 0.3793 4.1570} | 748 | using System;\n\nnamespace DistanceMetrics\n{\n    public static class Jaccard\n    {\n        /// <summary>Mismatched lengths throw at <c>b[i]</c> via array indexing; no explicit guard needed.</summary>\n        public static double Distance(double[] a, double[] b, double threshold = 0.5)\n        {\n            int intersection = 0, union = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                bool ai = a[i] > threshold;\n                bool bi = b[i] > threshold;\n                if (ai || bi) union++;\n                if (ai && bi) intersection++;\n            }\n            if (union == 0) return 0.0; // both empty → identical\n            return 1.0 - (double)intersection / union;\n        }\n    }\n} |
