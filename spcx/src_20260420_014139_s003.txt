idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
17 | metrics/SpcBatch.FisherRao.cs | {3183 318 0.3255 4.5151} | 3279 | using System;\nusing System.Buffers;\nusing System.Numerics.Tensors;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Fisher-Rao proximity-graph builder.\n        /// Distance = 2·arccos(∑√(p_i·q_i)) — the geodesic on the statistical manifold\n        /// (Bhattacharyya angle). A proper metric; range [0, π].\n        /// Input features must be probability distributions (non-negative, sum to 1).\n        /// Captures geometry that JSD approximates linearly; use when distributional\n        /// curvature matters.\n        /// </summary>\n        private static Edge[] BuildFisherRaoGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Fisher-Rao metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                FisherRaoDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        // Single len-sized scratch — same three-tier pattern as Mahalanobis / JSD.\n        private static readonly ThreadLocal<double[]> _frScratch =\n            new(() => new double[512]);\n\n        private static double FisherRaoDistance(double[] p, double[] q)\n        {\n            int len = p.Length;\n            if (len == 0) return 0.0;\n\n            if (len <= 64)\n            {\n                Span<double> buf = stackalloc double[len];\n                return FisherRaoCore(p, q, buf, len);\n            }\n\n            if (len <= 512)\n            {\n                return FisherRaoCore(p, q, _frScratch.Value.AsSpan(0, len), len);\n            }\n\n            var pool = ArrayPool<double>.Shared;\n            double[] rented = pool.Rent(len);\n            try\n            {\n                return FisherRaoCore(p, q, rented.AsSpan(0, len), len);\n            }\n            finally\n            {\n                pool.Return(rented);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double FisherRaoCore(double[] p, double[] q, Span<double> buf, int len)\n        {\n            var pSpan = p.AsSpan(0, len);\n            var qSpan = q.AsSpan(0, len);\n\n            // bhat = sum(sqrt(p*q)). Max-clamp to 0 before sqrt guards against\n            // tiny negative values from FP noise that would otherwise yield NaN.\n            // Replaces the original `if (p[i] > 0 && q[i] > 0)` guard, which\n            // was redundant for proper probability inputs but blocked SIMD.\n            TensorPrimitives.Multiply<double>(pSpan, qSpan, buf);\n            TensorPrimitives.Max<double>(buf, 0.0, buf);\n            TensorPrimitives.Sqrt<double>(buf, buf);\n            double bhat = TensorPrimitives.Sum<double>(buf);\n\n            // Clamp before arccos for numerical safety (sum can drift just past 1).\n            if (bhat > 1.0) bhat = 1.0;\n            else if (bhat < -1.0) bhat = -1.0;\n            return 2.0 * Math.Acos(bhat);\n        }\n    }\n} |
18 | metrics/SpcBatch.Hamming.cs | {1264 126 0.2706 4.6149} | 1297 | using System;\nusing System.Numerics;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Hamming + SimHash proximity-graph builder.\n        /// Operates on pre-computed 64-bit SimHash signatures via hardware\n        /// POPCNT distance, then converts to Gaussian coupling\n        /// J = exp(-d^2 / 2*delta^2). Signatures are produced upstream by the\n        /// Hashish preprocessing layer; this kernel is a pure numeric consumer.\n        /// </summary>\n        private static Edge[] BuildHammingGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.SimHashes == null || request.SimHashes.Length == 0)\n                throw new ArgumentException(\"SimHashes must be provided for Hamming metric.\");\n\n            var hashes = request.SimHashes;\n            n = hashes.Length;\n\n            Func<int, int, double> pairDistance = (i, j) =>\n                (double)BitOperations.PopCount(hashes[i] ^ hashes[j]);\n\n            var (allNeighbors, nnDistances) = SelectNeighbors(\n                request.Proximity, n, request.K, request.Epsilon, pairDistance);\n\n            return ConvertToCoupling(allNeighbors, nnDistances, request.Delta, request.Estimator, request.CouplingKernel);\n        }\n    }\n} |
19 | metrics/SpcBatch.Jaccard.cs | {1633 183 0.3270 4.4077} | 1683 | using System;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Jaccard proximity-graph builder.\n        /// Distance = 1 - |A∩B| / |A∪B| over binary indicator features.\n        /// Values treated as binary: > 0.5 = present, &lt;= 0.5 = absent.\n        /// Ignores shared absences — only shared presences count as similarity.\n        /// Good for: market baskets, dependency graphs, categorical presence/absence.\n        /// </summary>\n        private static Edge[] BuildJaccardGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Jaccard metric.\");\n\n            n = request.Features.Length;\n            double threshold = request.JaccardThreshold;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                (a, b) => JaccardDistance(a, b, threshold), request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double JaccardDistance(double[] a, double[] b, double threshold)\n        {\n            int intersection = 0, union = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                bool ai = a[i] > threshold;\n                bool bi = b[i] > threshold;\n                if (ai || bi) union++;\n                if (ai && bi) intersection++;\n            }\n            if (union == 0) return 0.0; // both empty → identical\n            return 1.0 - (double)intersection / union;\n        }\n    }\n} |
20 | metrics/SpcBatch.JensenShannon.cs | {4574 435 0.3233 4.5266} | 4695 | using System;\nusing System.Buffers;\nusing System.Numerics.Tensors;\nusing System.Runtime.CompilerServices;\nusing System.Threading;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Jensen-Shannon Divergence proximity-graph builder.\n        /// JSD is a symmetric, bounded [0, ln2] divergence between probability distributions.\n        /// We return sqrt(JSD) which is a proper metric.\n        /// Input features must be probability distributions (non-negative, sum to 1).\n        /// Good for: gene expression profiles, topic distributions, compositional data.\n        /// Uses natural log — sqrt(JSD) ∈ [0, sqrt(ln2)].\n        /// </summary>\n        private static Edge[] BuildJensenShannonGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Jensen-Shannon metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                JensenShannonDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        // Three-tier scratch buffers — same pattern as Mahalanobis. JSD needs\n        // two len-sized buffers: one for m = (p+q)/2 (later repurposed for the\n        // q-side term) and one for the p-side term.\n        private static readonly ThreadLocal<(double[] a, double[] b)> _jsdScratch =\n            new(() => (new double[512], new double[512]));\n\n        private static double JensenShannonDistance(double[] p, double[] q)\n        {\n            int len = p.Length;\n            if (len == 0) return 0.0;\n\n            if (len <= 64)\n            {\n                Span<double> a = stackalloc double[len];\n                Span<double> b = stackalloc double[len];\n                return JensenShannonCore(p, q, a, b, len);\n            }\n\n            if (len <= 512)\n            {\n                var (aArr, bArr) = _jsdScratch.Value;\n                return JensenShannonCore(p, q,\n                    aArr.AsSpan(0, len),\n                    bArr.AsSpan(0, len), len);\n            }\n\n            var pool = ArrayPool<double>.Shared;\n            double[] rentedA = pool.Rent(len);\n            double[] rentedB = pool.Rent(len);\n            try\n            {\n                return JensenShannonCore(p, q,\n                    rentedA.AsSpan(0, len),\n                    rentedB.AsSpan(0, len), len);\n            }\n            finally\n            {\n                pool.Return(rentedA);\n                pool.Return(rentedB);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double JensenShannonCore(\n            double[] p, double[] q,\n            Span<double> mBuf, Span<double> termP, int len)\n        {\n            var pSpan = p.AsSpan(0, len);\n            var qSpan = q.AsSpan(0, len);\n\n            // m = (p+q)/2, clamped above 1e-15 to make division safe.\n            TensorPrimitives.Add<double>(pSpan, qSpan, mBuf);\n            TensorPrimitives.Multiply<double>(mBuf, 0.5, mBuf);\n            TensorPrimitives.Max<double>(mBuf, 1e-15, mBuf);\n\n            // termP = p * log(max(p/m, 1e-15))\n            // Clamping the ratio is mathematically safe: where p ≈ 0 the\n            // outer *p multiply collapses the term to 0, regardless of the\n            // (clamped) log value. This replaces the original `if p[i] > 1e-15`\n            // guard, which would otherwise kill SIMD vectorization.\n            TensorPrimitives.Divide<double>(pSpan, mBuf, termP);\n            TensorPrimitives.Max<double>(termP, 1e-15, termP);\n            TensorPrimitives.Log<double>(termP, termP);\n            TensorPrimitives.Multiply<double>(pSpan, termP, termP);\n            double sumP = TensorPrimitives.Sum<double>(termP);\n\n            // mBuf is repurposed for the q-side term. Order matters — m must\n            // remain valid through both Divide calls; q-side runs second so\n            // we can overwrite m in place.\n            TensorPrimitives.Divide<double>(qSpan, mBuf, mBuf);\n            TensorPrimitives.Max<double>(mBuf, 1e-15, mBuf);\n            TensorPrimitives.Log<double>(mBuf, mBuf);\n            TensorPrimitives.Multiply<double>(qSpan, mBuf, mBuf);\n            double sumQ = TensorPrimitives.Sum<double>(mBuf);\n\n            // JSD = 0.5 * (KL(p||m) + KL(q||m)); return sqrt for proper metric.\n            return Math.Sqrt(0.5 * (sumP + sumQ));\n        }\n    }\n} |
21 | metrics/SpcBatch.Mahalanobis.cs | {4732 479 0.3305 4.4967} | 4869 | using System;\nusing System.Buffers;\nusing System.Runtime.CompilerServices;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Mahalanobis proximity-graph builder.\n        /// Distance = sqrt((a-b)^T * Σ^{-1} * (a-b)) where Σ^{-1} is the\n        /// pre-computed inverse covariance matrix on SpcBatchRequest.CovarianceInverse.\n        /// \"Dewarps\" feature space by accounting for correlations between features.\n        /// Good for: correlated tabular data, Breast Cancer Wisconsin, physical measurements.\n        /// </summary>\n        private static Edge[] BuildMahalanobisGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Mahalanobis metric.\");\n            if (request.CovarianceInverse == null)\n                throw new ArgumentException(\"CovarianceInverse must be provided for Mahalanobis metric.\");\n\n            n = request.Features.Length;\n            int dim = request.Features[0].Length;\n            if (request.CovarianceInverse.GetLength(0) != dim || request.CovarianceInverse.GetLength(1) != dim)\n                throw new ArgumentException(\"CovarianceInverse dimensions must match feature dimensionality.\");\n\n            var invCov = request.CovarianceInverse;\n\n            Func<double[], double[], double> distFunc = (a, b) => MahalanobisDistance(a, b, invCov, dim);\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                distFunc, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        // Three-tier scratch buffer allocation — avoids O(N²) heap allocations\n        // during graph construction while remaining thread-safe under Parallel.For.\n        //\n        // Tier 1 (dim ≤ 64):  stackalloc — zero heap cost, JIT-inlinable.\n        // Tier 2 (dim ≤ 512): ThreadLocal fixed arrays — one allocation per thread, ever.\n        // Tier 3 (dim > 512): ArrayPool rent/return per call.\n        //\n        // ThreadLocal note: _mahalScratch allocates once per thread that touches it.\n        // Primary caller is parallel graph construction (thread count ≤ ProcessorCount).\n        // Calling from many short-lived threads (e.g., test isolation) is correct but\n        // wastes memory. Write-before-read invariant: MahalanobisCore always writes\n        // diff[] before reading it and temp[i] before the final dot — scratch is safe.\n        private static readonly ThreadLocal<(double[] diff, double[] temp)> _mahalScratch =\n            new(() => (new double[512], new double[512]));\n\n        private static double MahalanobisDistance(double[] a, double[] b, double[,] invCov, int dim)\n        {\n            if (dim <= 64)\n            {\n                Span<double> diff = stackalloc double[dim];\n                Span<double> temp = stackalloc double[dim];\n                return MahalanobisCore(a, b, invCov, diff, temp, dim);\n            }\n\n            if (dim <= 512)\n            {\n                var (diffArr, tempArr) = _mahalScratch.Value;\n                return MahalanobisCore(a, b, invCov,\n                    diffArr.AsSpan(0, dim),\n                    tempArr.AsSpan(0, dim), dim);\n            }\n\n            // dim > 512 — pool rent, guaranteed return\n            double[] rentedDiff = ArrayPool<double>.Shared.Rent(dim);\n            double[] rentedTemp = ArrayPool<double>.Shared.Rent(dim);\n            try\n            {\n                return MahalanobisCore(a, b, invCov,\n                    rentedDiff.AsSpan(0, dim),\n                    rentedTemp.AsSpan(0, dim), dim);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedDiff);\n                ArrayPool<double>.Shared.Return(rentedTemp);\n            }\n        }\n\n        [MethodImpl(MethodImplOptions.AggressiveInlining)]\n        private static double MahalanobisCore(\n            double[] a, double[] b, double[,] invCov,\n            Span<double> diff, Span<double> temp, int dim)\n        {\n            // diff = a - b\n            for (int i = 0; i < dim; i++)\n                diff[i] = a[i] - b[i];\n\n            // temp = invCov * diff\n            for (int i = 0; i < dim; i++)\n            {\n                double s = 0;\n                for (int j = 0; j < dim; j++)\n                    s += invCov[i, j] * diff[j];\n                temp[i] = s;\n            }\n\n            // result = sqrt(diff^T * temp)\n            double quad = 0;\n            for (int i = 0; i < dim; i++)\n                quad += diff[i] * temp[i];\n\n            return Math.Sqrt(Math.Max(0, quad));\n        }\n    }\n} |
22 | metrics/SpcBatch.Manhattan.cs | {1171 107 0.2681 4.5634} | 1205 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Manhattan (L1) proximity-graph builder.\n        /// Sum of absolute differences — preserves contrast in high-dimensional\n        /// sparse data where Euclidean (L2) suffers from curse of dimensionality.\n        /// Good for: scRNA-seq, sparse feature vectors, high-D categorical counts.\n        /// </summary>\n        private static Edge[] BuildManhattanGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Manhattan metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                ManhattanDistance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double ManhattanDistance(double[] a, double[] b)\n        {\n            return TensorPrimitives.SumOfAbsoluteDifferences<double>(a, b);\n        }\n    }\n} |
23 | metrics/SpcBatch.Minkowski.cs | {2167 240 0.3327 4.5120} | 2230 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Minkowski (Lp) proximity-graph builder — generalizes Manhattan (p=1), Euclidean (p=2),\n        /// and approaches Chebyshev (p→∞). The p exponent is taken from\n        /// SpcBatchRequest.MinkowskiP.\n        /// </summary>\n        private static Edge[] BuildMinkowskiGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Minkowski metric.\");\n\n            n = request.Features.Length;\n            double p = request.MinkowskiP;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                (a, b) => MinkowskiDistance(a, b, p), request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double MinkowskiDistance(double[] a, double[] b, double p)\n        {\n            // Fast paths for common exponents via TensorPrimitives.\n            // These assume well-behaved input magnitudes — the maxDiff overflow guard\n            // in the scalar fallback below does not apply here.\n            if (p == 1.0) return TensorPrimitives.SumOfAbsoluteDifferences<double>(a, b);\n            if (p == 2.0) return TensorPrimitives.Distance<double>(a, b);\n\n            // Scalar fallback for arbitrary p.\n            // Factor out max |diff| to prevent |diff|^p overflow for large p.\n            // Lp = maxDiff * (sum((|diff| / maxDiff)^p))^(1/p)\n            double maxDiff = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double d = Math.Abs(a[i] - b[i]);\n                if (d > maxDiff) maxDiff = d;\n            }\n            if (maxDiff == 0) return 0.0;\n\n            double sum = 0;\n            for (int i = 0; i < a.Length; i++)\n            {\n                double scaled = Math.Abs(a[i] - b[i]) / maxDiff;\n                sum += Math.Pow(scaled, p);\n            }\n            return maxDiff * Math.Pow(sum, 1.0 / p);\n        }\n    }\n} |
24 | metrics/SpcBatch.Wasserstein.cs | {2413 256 0.3734 4.2701} | 2483 | using System;\nusing System.Buffers;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// 1D Wasserstein (Earth Mover's Distance) proximity-graph builder.\n        /// Computes the integral of |CDF_a - CDF_b| — the cheapest EMD formulation.\n        /// Robust to phase shifts and translations that destroy Euclidean distance.\n        /// Input: feature vectors treated as 1D distributions (histograms or sorted samples).\n        /// Good for: ECG time series, pixel histograms, any 1D distribution comparison.\n        /// </summary>\n        private static Edge[] BuildWassersteinGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Wasserstein metric.\");\n\n            n = request.Features.Length;\n\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                Wasserstein1D, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static double Wasserstein1D(double[] a, double[] b)\n        {\n            // 1D EMD = integral of |CDF_a(x) - CDF_b(x)| dx\n            // Arrays must be sorted ascending for the CDF formulation to be correct.\n            //\n            // Sort scratch comes from ArrayPool — graph construction calls this\n            // O(N²) times. Two heap allocations per call (the previous Clone()\n            // pattern) dominated GC pressure for medium-large N.\n            int len = a.Length;\n            if (len == 0) return 0.0;\n\n            var pool = ArrayPool<double>.Shared;\n            double[] sa = pool.Rent(len);\n            double[] sb = pool.Rent(len);\n            try\n            {\n                Array.Copy(a, sa, len);\n                Array.Copy(b, sb, len);\n                Array.Sort(sa, 0, len);\n                Array.Sort(sb, 0, len);\n\n                double cumA = 0, cumB = 0, sum = 0;\n                for (int i = 0; i < len; i++)\n                {\n                    cumA += sa[i];\n                    cumB += sb[i];\n                    sum += Math.Abs(cumA - cumB);\n                }\n                return sum / len;\n            }\n            finally\n            {\n                pool.Return(sa);\n                pool.Return(sb);\n            }\n        }\n    }\n} |
25 | proximities/SpcBatch.EpsilonBall.cs | {2678 296 0.3719 4.2672} | 2756 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Epsilon-ball proximity.\n        /// Edge (i,j) exists if d(i,j) &lt; epsilon.\n        /// Inherently symmetric given a symmetric distance function.\n        /// K parameter is not used — all pairs within radius are included.\n        /// Produces a variable-degree graph: dense regions get more edges,\n        /// sparse regions get fewer. Can fail in high dimensions where\n        /// distance concentration makes epsilon selection fragile.\n        ///\n        /// Returns per-node neighbor lists and 1-NN distances for auto-delta.\n        /// </summary>\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighborsEpsilonBall(\n            int n, double epsilon, Func<int, int, double> pairDistance)\n        {\n            if (epsilon <= 0)\n                throw new ArgumentException(\"Epsilon must be positive for EpsilonBall proximity.\");\n\n            // Phase 1: parallel upper-triangle scan — each row i writes only to halfLists[i].\n            // No cross-row contention; j's symmetric entry is added in Phase 2.\n            var halfLists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                halfLists[i] = new List<Neighbor>();\n\n            Parallel.For(0, n, i =>\n            {\n                for (int j = i + 1; j < n; j++)\n                {\n                    double d = pairDistance(i, j);\n                    if (d < epsilon)\n                        halfLists[i].Add(new Neighbor { Index = j, Distance = d });\n                }\n            });\n\n            // Phase 2: sequential symmetrization — O(E), no contention.\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in halfLists[i])\n                {\n                    lists[i].Add(nb);\n                    lists[nb.Index].Add(new Neighbor { Index = i, Distance = nb.Distance });\n                }\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return (allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
26 | proximities/SpcBatch.Knn.cs | {3408 383 0.3794 4.2616} | 3495 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace SpcCore\n{\n    public static partial class SpcBatch\n    {\n        /// <summary>\n        /// Standard KNN proximity with OR-symmetrization.\n        /// For each node, selects K nearest neighbors by raw distance.\n        /// Then applies the OR rule: edge (i,j) exists if i is in j's KNN\n        /// OR j is in i's KNN. Produces an undirected neighbor set where\n        /// some nodes may have more than K neighbors after symmetrization.\n        ///\n        /// Returns per-node neighbor lists and 1-NN distances for auto-delta.\n        /// </summary>\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighborsKnn(\n            int n, int k, Func<int, int, double> pairDistance)\n        {\n            // Pass 1: directed KNN via bounded min-heap — parallel over rows.\n            // Each row i owns its own heap and writes only directedNeighbors[i].\n            var directedNeighbors = new Neighbor[n][];\n\n            Parallel.For(0, n, i =>\n            {\n                var heap = new BoundedMinHeap(k);\n                for (int j = 0; j < n; j++)\n                {\n                    if (i == j) continue;\n                    double d = pairDistance(i, j);\n                    heap.TryAdd(j, d);\n                }\n                directedNeighbors[i] = heap.GetSorted();\n            });\n\n            // Pass 2: OR-symmetrize\n            // Collect all directed edges into a set keyed by canonical pair (lo, hi).\n            // For each pair, keep the distance from whichever direction found it.\n            // If both directions found it, keep the smaller distance (stronger signal).\n            var pairMap = new Dictionary<long, double>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in directedNeighbors[i])\n                {\n                    int lo = Math.Min(i, nb.Index);\n                    int hi = Math.Max(i, nb.Index);\n                    long key = (long)lo * n + hi;\n\n                    if (!pairMap.TryGetValue(key, out double existing) || nb.Distance < existing)\n                        pairMap[key] = nb.Distance;\n                }\n            }\n\n            // Rebuild per-node neighbor lists from symmetrized pairs\n            var symmetricLists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                symmetricLists[i] = new List<Neighbor>();\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                double d = kvp.Value;\n                symmetricLists[lo].Add(new Neighbor { Index = hi, Distance = d });\n                symmetricLists[hi].Add(new Neighbor { Index = lo, Distance = d });\n            }\n\n            // Sort each node's neighbors by distance and extract 1-NN distances\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                symmetricLists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = symmetricLists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return (allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
