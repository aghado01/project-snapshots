idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
92 | regression/spline/BSpline.cs | {1672 258 0.3750 4.1911} | 1726 | using System;\n\nnamespace Maths.Regression.Spline;\n\npublic static class BSpline\n{\n\n    public static double[] EvaluateBasis(double t, double[] knots, int degree = 3)\n    {\n        int n = knots.Length - degree - 1;\n        double[] result = new double[n];\n\n        int span = degree;\n        while (span < n - 1 && t >= knots[span + 1])\n            span++;\n\n        double[] nLocal = new double[degree + 1];\n        double[] left  = new double[degree + 1];\n        double[] right = new double[degree + 1];\n        nLocal[0] = 1.0;\n\n        for (int j = 1; j <= degree; j++)\n        {\n            left[j]  = t - knots[span + 1 - j];\n            right[j] = knots[span + j] - t;\n            double saved = 0.0;\n\n            for (int r = 0; r < j; r++)\n            {\n                double denom = right[r + 1] + left[j - r];\n\n                double temp = denom > 0.0 ? nLocal[r] / denom : 0.0;\n                nLocal[r] = saved + right[r + 1] * temp;\n                saved = left[j - r] * temp;\n            }\n            nLocal[j] = saved;\n        }\n\n        for (int r = 0; r <= degree; r++)\n            result[span - degree + r] = nLocal[r];\n        return result;\n    }\n\n    public static double[] MakeClampedKnots(double[] interiorKnots, int degree = 3)\n    {\n        int nInterior = interiorKnots.Length;\n        int nControl  = nInterior + degree + 1;\n        double[] knots = new double[nControl + degree + 1];\n\n        for (int i = 0; i <= degree; i++) knots[i] = 0.0;\n        for (int i = 0; i < nInterior; i++) knots[degree + 1 + i] = interiorKnots[i];\n        for (int i = 0; i <= degree; i++) knots[degree + 1 + nInterior + i] = 1.0;\n        return knots;\n    }\n}
93 | regression/spline/IBasis.cs | {218 22 0.1697 4.7280} | 228 | namespace Maths.Regression.Spline;\n\npublic interface IBasis\n{\n\n    int Dimension(int knotCount);\n\n    double[,] Design(KnotConfig config, double[] x);\n\n    double Evaluate(KnotConfig config, double[] coef, double x);\n}
94 | regression/spline/KnotConfig.cs | {332 30 0.2108 4.6034} | 348 | using System;\n\nnamespace Maths.Regression.Spline;\n\npublic sealed class KnotConfig\n{\n\n    public double[] InteriorKnots { get; }\n\n    public int Count => InteriorKnots.Length;\n\n    public KnotConfig(double[] interiorKnots)\n    {\n        ArgumentNullException.ThrowIfNull(interiorKnots);\n        InteriorKnots = interiorKnots;\n    }\n}
95 | regression/spline/SplineBasis.cs | {1428 158 0.2780 4.7143} | 1475 | using System;\n\nnamespace Maths.Regression.Spline;\n\npublic sealed class SplineBasis : IBasis\n{\n    private readonly int _degree;\n\n    public SplineBasis(int degree = 3)\n    {\n        if (degree < 1) throw new ArgumentOutOfRangeException(nameof(degree), \"Degree must be at least 1.\");\n        _degree = degree;\n    }\n\n    public int Dimension(int knotCount) => knotCount + _degree + 1;\n\n    public int Degree => _degree;\n\n    public double Evaluate(KnotConfig config, double[] coef, double x)\n    {\n        ArgumentNullException.ThrowIfNull(config);\n        ArgumentNullException.ThrowIfNull(coef);\n        double[] knots = BSpline.MakeClampedKnots(config.InteriorKnots, _degree);\n        double[] row = BSpline.EvaluateBasis(x, knots, _degree);\n        double f = 0.0;\n        for (int j = 0; j < row.Length; j++) f += row[j] * coef[j];\n        return f;\n    }\n\n    public double[,] Design(KnotConfig config, double[] x)\n    {\n        ArgumentNullException.ThrowIfNull(config);\n        ArgumentNullException.ThrowIfNull(x);\n\n        double[] knots = BSpline.MakeClampedKnots(config.InteriorKnots, _degree);\n        int nu = knots.Length - _degree - 1;\n        var z = new double[x.Length, nu];\n        for (int i = 0; i < x.Length; i++)\n        {\n            double[] row = BSpline.EvaluateBasis(x[i], knots, _degree);\n            for (int j = 0; j < nu; j++)\n                z[i, j] = row[j];\n        }\n        return z;\n    }\n}
