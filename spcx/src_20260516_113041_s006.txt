idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
21 | clustering/spc/PottsRunner.cs | {17512 1879 0.2847 4.7312} | 18630 | using System;\nusing System.Numerics;\nusing System.Runtime.CompilerServices;\nusing Graphs.Primitives;\nusing Maths.Rng;\n\nnamespace Clustering.SPC.Potts;\n\n/// <summary>\n/// Single-temperature Swendsen-Wang sampler for the q-state Potts model.\n/// Generic over an <see cref=\"IPottsConfig\"/> struct so the JIT can specialize\n/// the hot path and dead-code-eliminate disabled accumulator branches.\n/// </summary>\n/// <remarks>\n/// <para><b>Hot-path discipline.</b> No per-cycle allocation. All scratch\n/// arrays are fields, sized at construction. Bond probabilities are precomputed\n/// once; the inner loop is a table lookup. The bond-formation and energy\n/// accumulation share a single edge walk because both decisions key off the\n/// same spin-equality test. The <c>j &lt;= i</c> skip handles symmetric CSR\n/// (each undirected edge stored as both (i,j) and (j,i)). The FK accumulator\n/// widens to <see langword=\"long\"/> on multiply to survive large N.</para>\n///\n/// <para><b>Color semantics.</b> <c>_spins[i]</c> is a Potts color in\n/// <c>[0, q)</c>, not a cluster ID. Bond formation reads the OLD spins (Pass 1)\n/// and the energy is evaluated against that same configuration. The cluster\n/// flip (Pass 2) overwrites <c>_spins</c> with NEW colors, one per UF root.\n/// Two distinct bond clusters may independently draw the same color; this is\n/// the \"color collision\" that distinguishes Blatt (which counts color buckets)\n/// from FK (which counts UF root sizes directly).</para>\n/// </remarks>\ninternal sealed class PottsRunner<TConfig> : IPottsRunner\n    where TConfig : struct, IPottsConfig\n{\n    // ── Inputs (read-only after construction) ─────────────────────────────\n    private readonly CsrGraph _graph;\n    private readonly double _temperature;\n    private readonly int _q;\n    private readonly int _n;\n\n    // ── State ─────────────────────────────────────────────────────────────\n    private readonly int[] _spins;             // [N] Potts color of each node, in [0, q)\n    private readonly UnionFind _uf;        // bond-cluster structure, rebuilt every cycle\n    private readonly double[] _bondTable;      // [E] p_e = 1 - exp(-J_e / T), precomputed once\n    private readonly Xoshiro256PlusPlus _rng;\n\n    // ── Scratch buffers (reused across cycles; never reallocated) ─────────\n    private readonly int[] _rootSizeScratch;   // [N] compacted UF root sizes after WriteRootSizesTo\n    private readonly int[] _clusterMap;        // [N] root index -> new color, sentinel -1\n    private readonly int[]? _colorBucketScratch; // [Q] color counts; null when Blatt disabled\n\n    // ── Per-edge accumulators (EdgeObservables specializations only) ──────\n    // Indexed by CSR entry. Symmetric CSR stores each undirected edge twice;\n    // only the j > i half is ever incremented (the SW loop's j ≤ i skip).\n    private readonly int[]? _bondFormedCount;     // [E] cycles where bond formed at edge e\n    private readonly int[]? _spinAgreementCount;  // [E] cycles where _spins[i] == _spins[j] at edge e\n\n    // ── Observables ───────────────────────────────────────────────────────\n    private readonly int[] _clusterSizeHistogram;  // log-binned; bin k = sizes [2^k, 2^(k+1))\n\n    private double _runningSumSqClusterSizes;       // Σ_cycles Σ_c |c|², the FK accumulator\n    private double _runningSumSqClusterSizesExcl;   // Σ_cycles (Σ_c |c|² − max_c|c|²), percolation diagnostic\n    private double _runningSumEnergy;               // Σ_cycles ⟨H⟩\n    private double _runningSumEnergySq;             // Σ_cycles ⟨H²⟩, for specific heat\n    private double _runningSumMag;                  // Σ_cycles m  (Blatt; zero when disabled)\n    private double _runningSumMagSq;                // Σ_cycles m² (Blatt; zero when disabled)\n\n    private int _cycleCount;\n\n    public PottsRunner(CsrGraph graph, double temperature, int q, int? seed)\n    {\n        if (graph.NodeCount <= 0) throw new ArgumentException(\"Graph must have at least one node.\", nameof(graph));\n        if (graph.Targets is null || graph.Weights is null || graph.RowPointers is null)\n            throw new ArgumentException(\"Graph CSR arrays are not initialized.\", nameof(graph));\n        if (temperature <= 0.0) throw new ArgumentOutOfRangeException(nameof(temperature), \"Temperature must be strictly positive.\");\n        if (q < 2) throw new ArgumentOutOfRangeException(nameof(q), \"Q must be at least 2.\");\n\n        _graph = graph;\n        _temperature = temperature;\n        _q = q;\n        _n = graph.NodeCount;\n\n        _spins = new int[_n];\n        _uf = new UnionFind(_n);\n        _bondTable = new double[graph.Targets.Length];\n        _rng = new Xoshiro256PlusPlus(seed);\n\n        // Precompute p_e = 1 - exp(-J_e / T). For symmetric CSR, (i,j) and (j,i)\n        // entries hold the same p; only one is touched per cycle (j <= i skip).\n        // The redundancy is a memory-vs-cache tradeoff: matching the CSR access\n        // pattern keeps the table walk linear and prefetcher-friendly.\n        double invT = 1.0 / _temperature;\n        for (int e = 0; e < _bondTable.Length; e++)\n        {\n            _bondTable[e] = 1.0 - Math.Exp(-graph.Weights[e] * invT);\n        }\n\n        _rootSizeScratch = new int[_n];\n        _clusterMap = new int[_n];\n\n        // Blatt-only allocation; folds away when TConfig.BlattMagnetization is false.\n        _colorBucketScratch = TConfig.BlattMagnetization ? new int[_q] : null;\n\n        // EdgeObservables-only allocation; both arrays fold away together\n        // when TConfig.EdgeObservables is false.\n        _bondFormedCount    = TConfig.EdgeObservables ? new int[_bondTable.Length] : null;\n        _spinAgreementCount = TConfig.EdgeObservables ? new int[_bondTable.Length] : null;\n\n        int maxBin = BitOperations.Log2((uint)Math.Max(1, _n)) + 1;\n        _clusterSizeHistogram = new int[maxBin];\n\n        // Random initial configuration (high-T disorder).\n        for (int i = 0; i < _n; i++)\n            _spins[i] = _rng.NextInt(_q);\n    }\n\n    // ── IPottsRunner read surface ─────────────────────────────────────────\n\n    public int CycleCount => _cycleCount;\n    public double Temperature => _temperature;\n    public int Q => _q;\n    public int N => _n;\n\n    public ReadOnlySpan<int> Spins => _spins;\n    public ReadOnlySpan<int> ClusterSizeHistogram => _clusterSizeHistogram;\n\n    public double RunningSumSqClusterSizes     => _runningSumSqClusterSizes;\n    public double RunningSumSqClusterSizesExcl => _runningSumSqClusterSizesExcl;\n    public double RunningSumEnergy             => _runningSumEnergy;\n    public double RunningSumEnergySq           => _runningSumEnergySq;\n    public double RunningSumMag                => _runningSumMag;\n    public double RunningSumMagSq              => _runningSumMagSq;\n\n    // ── Drivers ───────────────────────────────────────────────────────────\n\n    public void Run(int cycleCount)\n    {\n        if (cycleCount < 0) throw new ArgumentOutOfRangeException(nameof(cycleCount));\n        for (int i = 0; i < cycleCount; i++)\n            Cycle();\n    }\n\n    public void BurnIn(int cycles)\n    {\n        if (cycles < 0) throw new ArgumentOutOfRangeException(nameof(cycles));\n        for (int i = 0; i < cycles; i++)\n            Cycle();\n        ResetAccumulators();\n    }\n\n    /// <summary>\n    /// One Swendsen-Wang cycle: bond formation (with energy as a free byproduct),\n    /// cluster flip, and observable accumulation.\n    /// </summary>\n    public void Cycle()\n    {\n        _uf.Reset();\n\n        // ── Pass 1: single edge walk. Bond formation + energy in the same loop.\n        // For each undirected edge (i,j) where the CURRENT spins match:\n        //   * subtract J_ij from this cycle's energy (Potts H = -Σ J_ij δ),\n        //   * with probability bondTable[e], union i and j in the UF.\n        // Mismatched edges contribute neither energy nor a bond opportunity.\n        double cycleEnergy = 0.0;\n        for (int i = 0; i < _n; i++)\n        {\n            int rowEnd = _graph.RowPointers[i + 1];\n            for (int e = _graph.RowPointers[i]; e < rowEnd; e++)\n            {\n                int j = _graph.Targets[e];\n                if (j <= i) continue;  // undirected: visit each edge once\n\n                if (_spins[i] == _spins[j])\n                {\n                    if (TConfig.EdgeObservables) _spinAgreementCount![e]++;\n                    cycleEnergy -= _graph.Weights[e];\n                    if (_rng.NextDouble() < _bondTable[e])\n                    {\n                        if (TConfig.EdgeObservables) _bondFormedCount![e]++;\n                        _uf.Union(i, j);\n                    }\n                }\n            }\n        }\n\n        // ── Pass 2: cluster flip. Each UF root draws ONE color uniformly from\n        // [0, q); every member of that bond cluster inherits it. _clusterMap is\n        // reset with a -1 sentinel, then used as a root -> color cache so the\n        // RNG fires exactly once per cluster, not once per node.\n        _clusterMap.AsSpan().Fill(-1);\n        for (int i = 0; i < _n; i++)\n        {\n            int root = _uf.Find(i);\n            int newColor = _clusterMap[root];\n            if (newColor < 0)\n            {\n                newColor = _rng.NextInt(_q);\n                _clusterMap[root] = newColor;\n            }\n            _spins[i] = newColor;\n        }\n\n        // ── Pass 3a: FK susceptibility accumulator (always on).\n        // Σ_c |c|² over BOND CLUSTERS — read from the UF, not from spin buckets.\n        // This is the §10.1 fix: immune to coincidental color collisions because\n        // it never references _spins. long widening on the multiply lets us\n        // safely accumulate sums for N well past int range.\n        int rootCount = _uf.WriteRootSizesTo(_rootSizeScratch);\n        ReadOnlySpan<int> rootSizes = _rootSizeScratch.AsSpan(0, rootCount);\n        long sumSq = 0L;\n        long maxSize = 0L;\n        for (int r = 0; r < rootCount; r++)\n        {\n            long sz = rootSizes[r];\n            sumSq += sz * sz;\n            if (sz > maxSize) maxSize = sz;\n        }\n        _runningSumSqClusterSizes     += sumSq;\n        _runningSumSqClusterSizesExcl += sumSq - maxSize * maxSize;\n\n        // ── Pass 3b: Blatt 1996 magnetization estimator (opt-in via TConfig).\n        // m = (N_max · q / N - 1) / (q - 1), where N_max is the largest COLOR\n        // BUCKET in the current spin configuration. This block, the call to\n        // MaxColorBucket, and the dereference of _colorBucketScratch all\n        // disappear from PottsRunner<FkOnly>'s JITed IL — not a predicted\n        // branch, an absent one.\n        if (TConfig.BlattMagnetization)\n        {\n            int nMax = MaxColorBucket(_spins, _q, _colorBucketScratch!);\n            double m = ((double)nMax * _q / _n - 1.0) / (_q - 1.0);\n            _runningSumMag   += m;\n            _runningSumMagSq += m * m;\n        }\n\n        // ── Pass 3c: log-binned cluster size histogram scatter. Scalar writes\n        // (scatter doesn't vectorize), but the loop is short — rootCount is\n        // typically << N. BitOperations.Log2 is a single hardware instruction.\n        for (int r = 0; r < rootCount; r++)\n        {\n            int bin = BitOperations.Log2((uint)rootSizes[r]);\n            _clusterSizeHistogram[bin]++;\n        }\n\n        _runningSumEnergy   += cycleEnergy;\n        _runningSumEnergySq += cycleEnergy * cycleEnergy;\n        _cycleCount++;\n    }\n\n    // ── Checkpoint surface ────────────────────────────────────────────────\n\n    public PottsModelEdgeObservables? GetEdgeCheckpoint()\n    {\n        if (!TConfig.EdgeObservables) return null;\n        return new PottsModelEdgeObservables\n        {\n            Temperature        = _temperature,\n            CycleCount         = _cycleCount,\n            BondFormedCount    = (int[])_bondFormedCount!.Clone(),\n            SpinAgreementCount = (int[])_spinAgreementCount!.Clone(),\n        };\n    }\n\n    public PottsModelStepResult GetCheckpoint()\n    {\n        var (s0, s1, s2, s3) = _rng.SaveState();\n        var variant = (TConfig.BlattMagnetization, TConfig.EdgeObservables) switch\n        {\n            (false, false) => PottsSusceptibility.FkOnly,\n            (true,  false) => PottsSusceptibility.FkAndBlatt,\n            (false, true ) => PottsSusceptibility.FkAndEdges,\n            (true,  true ) => PottsSusceptibility.FkAndBlattAndEdges,\n        };\n\n        return new PottsModelStepResult\n        {\n            Temperature              = _temperature,\n            Q                        = _q,\n            Susceptibility           = variant,\n            CycleCount               = _cycleCount,\n            Spins                    = (int[])_spins.Clone(),\n            ClusterSizeHistogram     = (int[])_clusterSizeHistogram.Clone(),\n            RngState0                = s0,\n            RngState1                = s1,\n            RngState2                = s2,\n            RngState3                = s3,\n            RunningSumSqClusterSizes     = _runningSumSqClusterSizes,\n            RunningSumSqClusterSizesExcl = _runningSumSqClusterSizesExcl,\n            RunningSumEnergy             = _runningSumEnergy,\n            RunningSumEnergySq           = _runningSumEnergySq,\n            RunningSumMag                = _runningSumMag,\n            RunningSumMagSq              = _runningSumMagSq,\n        };\n    }\n\n    public void Restore(PottsModelStepResult result)\n    {\n        // Identifying-constant validation. We compare on the runner side\n        // (rather than on the public facade) so the TConfig specialization's\n        // own view of Blatt and EdgeObservables is the source of truth.\n        var thisVariant = (TConfig.BlattMagnetization, TConfig.EdgeObservables) switch\n        {\n            (false, false) => PottsSusceptibility.FkOnly,\n            (true,  false) => PottsSusceptibility.FkAndBlatt,\n            (false, true ) => PottsSusceptibility.FkAndEdges,\n            (true,  true ) => PottsSusceptibility.FkAndBlattAndEdges,\n        };\n\n        if (result.Susceptibility != thisVariant)\n            throw new InvalidOperationException(\n                $\"Snapshot susceptibility {result.Susceptibility} does not match model {thisVariant}.\");\n        if (result.Temperature != _temperature)\n            throw new InvalidOperationException(\n                $\"Snapshot temperature {result.Temperature} does not match model temperature {_temperature}.\");\n        if (result.Q != _q)\n            throw new InvalidOperationException(\n                $\"Snapshot Q {result.Q} does not match model Q {_q}.\");\n        if (result.Spins is null || result.Spins.Length != _n)\n            throw new InvalidOperationException(\n                $\"Snapshot spin count {result.Spins?.Length ?? 0} does not match model node count {_n}.\");\n        if (result.ClusterSizeHistogram is null || result.ClusterSizeHistogram.Length != _clusterSizeHistogram.Length)\n            throw new InvalidOperationException(\n                $\"Snapshot histogram bin count {result.ClusterSizeHistogram?.Length ?? 0} does not match model bin count {_clusterSizeHistogram.Length}.\");\n\n        // Copy mutable state. Array.Copy because we want elementwise copy,\n        // not reference assignment — the DTO keeps its arrays, the runner\n        // keeps its arrays, neither aliases the other.\n        Array.Copy(result.Spins, _spins, _n);\n        Array.Copy(result.ClusterSizeHistogram, _clusterSizeHistogram, _clusterSizeHistogram.Length);\n\n        _rng.LoadState(result.RngState0, result.RngState1, result.RngState2, result.RngState3);\n\n        _cycleCount                   = result.CycleCount;\n        _runningSumSqClusterSizes     = result.RunningSumSqClusterSizes;\n        _runningSumSqClusterSizesExcl = result.RunningSumSqClusterSizesExcl;\n        _runningSumEnergy             = result.RunningSumEnergy;\n        _runningSumEnergySq           = result.RunningSumEnergySq;\n        _runningSumMag                = result.RunningSumMag;\n        _runningSumMagSq              = result.RunningSumMagSq;\n    }\n\n    /// <summary>\n    /// Zero every running accumulator and reset the cycle counter. Called\n    /// at the tail of <see cref=\"BurnIn\"/>; the model continues to hold\n    /// post-burn-in spins and RNG state, but starts collecting statistics\n    /// fresh from this point.\n    /// </summary>\n    private void ResetAccumulators()\n    {\n        _runningSumSqClusterSizes     = 0.0;\n        _runningSumSqClusterSizesExcl = 0.0;\n        _runningSumEnergy             = 0.0;\n        _runningSumEnergySq           = 0.0;\n        _runningSumMag                = 0.0;\n        _runningSumMagSq              = 0.0;\n        _cycleCount                   = 0;\n        _clusterSizeHistogram.AsSpan().Clear();\n        if (TConfig.EdgeObservables)\n        {\n            _bondFormedCount!.AsSpan().Clear();\n            _spinAgreementCount!.AsSpan().Clear();\n        }\n    }\n\n    /// <summary>\n    /// Returns the size of the largest color bucket in <paramref name=\"spins\"/>.\n    /// O(N + Q) with Q small (typically ≤ 20). Inlined into <see cref=\"Cycle\"/>\n    /// when the Blatt specialization is active; absent from the IL otherwise.\n    /// </summary>\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static int MaxColorBucket(ReadOnlySpan<int> spins, int q, Span<int> scratch)\n    {\n        scratch.Clear();\n        for (int i = 0; i < spins.Length; i++)\n            scratch[spins[i]]++;\n\n        int max = 0;\n        for (int c = 0; c < q; c++)\n            if (scratch[c] > max) max = scratch[c];\n        return max;\n    }\n} |
22 | clustering/spc/SpcScheduler.cs | {8255 868 0.2821 4.5672} | 8504 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.IO;\nusing System.Threading;\nusing System.Threading.Tasks;\nusing Clustering.SPC.Potts;\nusing Graphs.Primitives;\n\nnamespace Clustering.SPC;\n\n/// <summary>\n/// One atomic unit of work for <see cref=\"SpcScheduler\"/>: a single\n/// <c>(Temperature, ReplicaIndex)</c> pair carrying all parameters needed\n/// to construct a <see cref=\"PottsModel\"/>, run a fixed number of SWCycles,\n/// and emit one checkpoint file.\n/// </summary>\npublic sealed record TaskSpec\n{\n    public required double              Temperature    { get; init; }\n    public required int                 ReplicaIndex   { get; init; }\n    public required int                 Cycles         { get; init; }\n    public required int                 Q              { get; init; }\n    public required PottsSusceptibility Susceptibility { get; init; }\n    public required string              CheckpointPath { get; init; }\n\n    /// <summary>\n    /// Optional base seed. Per-task seed is <c>BaseSeed + ReplicaIndex</c> so\n    /// distinct replicas get distinct initial conditions while the schedule\n    /// as a whole remains reproducible.\n    /// </summary>\n    public int? BaseSeed { get; init; }\n\n    /// <summary>\n    /// Cycles to advance the model before starting to accumulate observables.\n    /// The accumulators are reset to zero after this many cycles, so the\n    /// disordered random-spin head doesn't bias the running averages.\n    /// Zero (default) means no burn-in. 10-20% of <see cref=\"Cycles\"/> is the\n    /// conventional rule of thumb.\n    /// </summary>\n    public int BurnInCycles { get; init; }\n}\n\n/// <summary>\n/// Dump scheduler: takes a flat list of <see cref=\"TaskSpec\"/> and runs them\n/// in parallel, writing one checkpoint file per task. No in-memory\n/// aggregation, no dynamic supervision — the on-disk artifacts are the\n/// complete output of one schedule run, ready for offline analysis.\n/// </summary>\n/// <remarks>\n/// <para><b>Resumability.</b> Each task checks for an existing checkpoint\n/// file at its target path; if present, the task is skipped. Interrupted\n/// runs resume by re-invoking <see cref=\"Run\"/> with the same task list —\n/// completed tasks are detected by file presence, missing tasks are re-run.</para>\n///\n/// <para><b>Atomic writes.</b> Checkpoints are written to a <c>.tmp</c>\n/// path and renamed on completion, so a crashed write never leaves a\n/// half-written checkpoint that would be mistaken for a completed task.</para>\n///\n/// <para><b>No replica chaining.</b> Each task constructs a fresh\n/// <see cref=\"PottsModel\"/> with random initial spins (high-T disorder).\n/// There is no warm-start across temperatures or replicas. Top-down\n/// temperature ordering is a presentation convention here, not a physics\n/// one — when chained schedules become useful, a richer task graph will\n/// replace this flat list.</para>\n/// </remarks>\npublic static class SpcScheduler\n{\n    /// <summary>\n    /// Construct the flat task list as the Cartesian product\n    /// <c>Temperatures × Replicas</c>. Caller-controlled ordering — pass\n    /// temperatures top-down (T_max → T_min) by convention.\n    /// </summary>\n    public static List<TaskSpec> BuildTaskList(\n        IReadOnlyList<double> temperatures,\n        int numReplicas,\n        int cyclesPerTask,\n        int q,\n        PottsSusceptibility susceptibility,\n        string checkpointDirectory,\n        int? baseSeed = null,\n        int burnInCyclesPerTask = 0)\n    {\n        if (temperatures is null || temperatures.Count == 0)\n            throw new ArgumentException(\"Temperatures must be non-empty.\", nameof(temperatures));\n        if (numReplicas <= 0)\n            throw new ArgumentOutOfRangeException(nameof(numReplicas), \"Replica count must be positive.\");\n        if (cyclesPerTask <= 0)\n            throw new ArgumentOutOfRangeException(nameof(cyclesPerTask), \"Cycles per task must be positive.\");\n        if (burnInCyclesPerTask < 0)\n            throw new ArgumentOutOfRangeException(nameof(burnInCyclesPerTask), \"Burn-in cycle count cannot be negative.\");\n        if (string.IsNullOrWhiteSpace(checkpointDirectory))\n            throw new ArgumentException(\"Checkpoint directory must be provided.\", nameof(checkpointDirectory));\n\n        var tasks = new List<TaskSpec>(temperatures.Count * numReplicas);\n        foreach (double t in temperatures)\n        {\n            for (int r = 0; r < numReplicas; r++)\n            {\n                tasks.Add(new TaskSpec\n                {\n                    Temperature    = t,\n                    ReplicaIndex   = r,\n                    Cycles         = cyclesPerTask,\n                    Q              = q,\n                    Susceptibility = susceptibility,\n                    CheckpointPath = Path.Combine(checkpointDirectory, FileNameFor(t, r)),\n                    BaseSeed       = baseSeed,\n                    BurnInCycles   = burnInCyclesPerTask,\n                });\n            }\n        }\n        return tasks;\n    }\n\n    /// <summary>\n    /// Execute <paramref name=\"tasks\"/> in parallel against\n    /// <paramref name=\"graph\"/>. Each task runs in its own thread, constructs\n    /// a private <see cref=\"PottsModel\"/>, runs the configured number of\n    /// SWCycles, and writes one checkpoint file. Already-completed tasks\n    /// (target file present) are skipped.\n    /// </summary>\n    public static void Run(\n        CsrGraph graph,\n        IReadOnlyList<TaskSpec> tasks,\n        CancellationToken ct = default)\n    {\n        if (tasks is null) throw new ArgumentNullException(nameof(tasks));\n        if (tasks.Count == 0) return;\n\n        // Pre-create directories serially — Parallel.ForEach should not race\n        // on Directory.CreateDirectory for the same path.\n        var dirs = new HashSet<string>();\n        foreach (var t in tasks)\n        {\n            var dir = Path.GetDirectoryName(t.CheckpointPath);\n            if (!string.IsNullOrEmpty(dir) && dirs.Add(dir))\n                Directory.CreateDirectory(dir);\n        }\n\n        var opts = new ParallelOptions\n        {\n            CancellationToken      = ct,\n            MaxDegreeOfParallelism = Environment.ProcessorCount,\n        };\n\n        Parallel.ForEach(tasks, opts, task =>\n        {\n            if (File.Exists(task.CheckpointPath))\n                return;\n\n            int? seed = task.BaseSeed.HasValue\n                ? unchecked(task.BaseSeed.Value + task.ReplicaIndex)\n                : (int?)null;\n\n            var model = new PottsModel(\n                graph,\n                task.Temperature,\n                task.Q,\n                task.Susceptibility,\n                seed);\n\n            if (task.BurnInCycles > 0)\n                model.BurnIn(task.BurnInCycles);\n\n            model.Run(task.Cycles);\n\n            // Overlay scheduler-known metadata (PottsModel doesn't know its replica id).\n            var checkpoint = model.GetCheckpoint() with\n            {\n                ReplicaIndex = task.ReplicaIndex,\n            };\n\n            // Atomic write: .tmp → File.Move. A crashed write leaves a .tmp\n            // file that doesn't satisfy File.Exists(CheckpointPath), so the\n            // resume pass will redo the task.\n            string tmp = task.CheckpointPath + \".tmp\";\n            PottsModelStepResultIO.WriteToFile(checkpoint, tmp);\n            File.Move(tmp, task.CheckpointPath, overwrite: false);\n\n            // Sidecar: per-edge observables, present only when the\n            // susceptibility variant emits them. Same atomic-write pattern.\n            var edges = model.GetEdgeCheckpoint();\n            if (edges is not null)\n            {\n                var edgesWithReplica = edges with { ReplicaIndex = task.ReplicaIndex };\n                string spcePath = Path.ChangeExtension(task.CheckpointPath, \".spce\");\n                string spceTmp  = spcePath + \".tmp\";\n                PottsModelEdgeObservablesIO.WriteToFile(edgesWithReplica, spceTmp);\n                File.Move(spceTmp, spcePath, overwrite: false);\n            }\n        });\n    }\n\n    private static string FileNameFor(double temperature, int replicaIndex)\n        => $\"T_{temperature.ToString(\"F5\", CultureInfo.InvariantCulture)}_rep_{replicaIndex}.spcx\";\n} |
23 | dev/clustering/hdbscan/placeholder.cs | {0 0 0.0000 0.0000} | 0 |  |
24 | dev/clustering/init/kmedoids.cs | {61 7 0.0984 4.1373} | 61 | // placeholder for proper k-medoids clustering implementation |
25 | dev/clustering/qc/silhouette.cs | {46 6 0.1087 4.4123} | 46 | // PLACEHOLDER for a silhouette implementation |
26 | dev/graphs/Djikstra.cs | {0 0 0.0000 0.0000} | 0 |  |
27 | dev/graphs/proximity/Voronoi.cs | {95 12 0.1158 4.5702} | 97 | ///Placeholder\n///Voronoi diagram construction for proximity graphs (e.g. kNN, mutual kNN).\n/// |
28 | dev/tda/BSpline.cs | {2552 379 0.3143 4.4778} | 2623 | using System;\nusing System.Linq;\nusing MathNet.Numerics.LinearAlgebra;\n\npublic static class BSpline\n{\n    /// <summary>\n    /// Evaluates all non-zero B-spline basis functions of given degree at parameter t.\n    /// </summary>\n    /// <param name=\"t\">Parameter value.</param>\n    /// <param name=\"knots\">Extended knot vector (length = nControlPoints + degree + 1).</param>\n    /// <param name=\"degree\">Degree (3 for cubic).</param>\n    /// <returns>Vector of basis values, length = knots.Length - degree - 1.</returns>\n    public static Vector<double> EvaluateBasis(double t, double[] knots, int degree = 3)\n    {\n        int n = knots.Length - degree - 1; // number of control points\n        var N = Vector<double>.Build.Dense(n, 0.0);\n\n        // Find the knot span index such that knots[span] <= t < knots[span+1]\n        int span = degree;\n        while (span < knots.Length - 1 - degree && t >= knots[span + 1])\n            span++;\n\n        // Initialize zeroth-degree basis functions\n        double[] left = new double[degree + 1];\n        double[] right = new double[degree + 1];\n        N[span] = 1.0;\n\n        for (int j = 1; j <= degree; j++)\n        {\n            left[j] = t - knots[span + 1 - j];\n            right[j] = knots[span + j] - t;\n            double saved = 0.0;\n\n            for (int r = 0; r < j; r++)\n            {\n                double temp = N[span - j + 1 + r] / (right[r + 1] + left[j - r]);\n                N[span - j + 1 + r] = saved + right[r + 1] * temp;\n                saved = left[j - r] * temp;\n            }\n            N[span + 1] = saved;\n        }\n        return N;\n    }\n\n    /// <summary>\n    /// Creates an extended knot vector for a clamped cubic B-spline from interior knots.\n    /// </summary>\n    public static double[] MakeClampedKnots(double[] interiorKnots, int degree = 3)\n    {\n        int nInterior = interiorKnots.Length;\n        // Number of control points = nInterior + degree + 1  (for clamped)\n        int nControl = nInterior + degree + 1;\n        var knots = new double[nControl + degree + 1]; // length = nControl + degree + 1\n        double tMin = interiorKnots.First();\n        double tMax = interiorKnots.Last();\n\n        // Repeat first knot degree+1 times\n        for (int i = 0; i <= degree; i++) knots[i] = tMin;\n        // Interior knots\n        for (int i = 0; i < nInterior; i++) knots[degree + 1 + i] = interiorKnots[i];\n        // Repeat last knot degree+1 times\n        for (int i = 0; i <= degree; i++) knots[degree + 1 + nInterior + i] = tMax;\n        return knots;\n    }\n} |
29 | dev/tda/LaplacianSmooth.cs | {1721 253 0.3510 4.5046} | 1779 | public static Matrix<double> LaplacianSmooth(\n    Matrix<double> Xorig,          // N x 3\n    int[] rowPtr,                  // CSR row pointers (size N+1)\n    int[] colIdx,                  // column indices\n    double[] weights,              // edge weights (coupling strengths)\n    double lambda)\n{\n    int n = Xorig.RowCount;\n    // Build sparse Laplacian L = D - W\n    var L = SparseMatrix.Create(n, n, 0);\n    for (int i = 0; i < n; i++)\n    {\n        double diag = 0.0;\n        for (int j = rowPtr[i]; j < rowPtr[i+1]; j++)\n        {\n            int neigh = colIdx[j];\n            double w = weights[j];\n            L[i, neigh] -= w;\n            diag += w;\n        }\n        L[i, i] = diag;\n    }\n\n    var I = SparseMatrix.CreateIdentity(n);\n    var A = I + lambda * L; // SPD\n\n    // Solve for each coordinate\n    var Xsmooth = Matrix<double>.Build.Dense(n, 3);\n    for (int d = 0; d < 3; d++)\n    {\n        var b = Xorig.Column(d);\n        // Conjugate gradient\n        var x = ConjugateGradient(A, b, 300, 1e-6);\n        Xsmooth.SetColumn(d, x);\n    }\n    return Xsmooth;\n}\n\n// Simple Conjugate Gradient for sparse SPD systems\nprivate static Vector<double> ConjugateGradient(Matrix<double> A, Vector<double> b, int maxIter, double tol)\n{\n    var x = Vector<double>.Build.Dense(b.Count, 0.0);\n    var r = b - A * x;\n    var p = r.Clone();\n    double rsold = r.DotProduct(r);\n\n    for (int i = 0; i < maxIter; i++)\n    {\n        var Ap = A * p;\n        double alpha = rsold / p.DotProduct(Ap);\n        x = x + alpha * p;\n        r = r - alpha * Ap;\n        double rsnew = r.DotProduct(r);\n        if (Math.Sqrt(rsnew) < tol) break;\n        p = r + (rsnew / rsold) * p;\n        rsold = rsnew;\n    }\n    return x;\n} |
