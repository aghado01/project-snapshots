idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
13 | clustering/spc/checkpoint.cs | {58738 4299 0.3444 4.2984} | 60222 | using System;\nusing System.Collections.Generic;\nusing System.Globalization;\nusing System.IO;\nusing System.IO.Compression;\nusing System.Text;\nusing System.Text.Json;\nusing System.Text.Json.Serialization;\n\n// This implementation of checkpointing and state management is being deprecated and rewritten from scratch soon\n// see docs/StateManagement.md for design notes and rationale. The public API is expected to change significantly;\n\nnamespace SpcCore\n{\n    public enum SpcPipelineStage\n    {\n        Spc,\n        Handoff,\n        Gmm\n    }\n\n    public enum SpcStateArtifactKind\n    {\n        TemperatureCheckpoint,\n        TemperatureObservation,\n        HandoffReadiness,\n        GmmHandoffState,\n        GmmCheckpoint,\n        GmmResult\n    }\n\n    public enum SpcStateArtifactContainer\n    {\n        Json,\n        Binary\n    }\n\n    public enum SpcStateArtifactCompression\n    {\n        None,\n        Brotli,\n        GZip,\n        Deflate\n    }\n\n    public enum SpcStateArtifactEncoding\n    {\n        Snapshot,\n        Delta\n    }\n\n    public sealed class SpcRunStateManifest\n    {\n        public int SchemaVersion { get; set; } = 3;\n        public string RunStamp { get; set; } = string.Empty;\n        public string ParentRunStamp { get; set; } = string.Empty;\n        public string WorkingDirectory { get; set; } = string.Empty;\n        public string StateDirectory { get; set; } = string.Empty;\n        public DateTime CreatedUtc { get; set; } = DateTime.UtcNow;\n        public DateTime UpdatedUtc { get; set; } = DateTime.UtcNow;\n        public List<SpcStateArtifact> Artifacts { get; set; } = new List<SpcStateArtifact>();\n    }\n\n    public sealed class SpcStateArtifact\n    {\n        public string ArtifactId { get; set; } = string.Empty;\n        public SpcStateArtifactKind Kind { get; set; }\n        public SpcPipelineStage Stage { get; set; }\n        public string PayloadType { get; set; } = string.Empty;\n        public string RelativePath { get; set; } = string.Empty;\n        public string SourceArtifactId { get; set; } = string.Empty;\n        public string PreviousArtifactId { get; set; } = string.Empty;\n        public string BaseArtifactId { get; set; } = string.Empty;\n        public SpcStateArtifactContainer Container { get; set; } = SpcStateArtifactContainer.Json;\n        public SpcStateArtifactCompression Compression { get; set; } = SpcStateArtifactCompression.None;\n        public SpcStateArtifactEncoding Encoding { get; set; } = SpcStateArtifactEncoding.Snapshot;\n        public string TemperatureKey { get; set; } = string.Empty;\n        public double Temperature { get; set; } = double.NaN;\n        public int Sequence { get; set; }\n        public int EpochCount { get; set; }\n        public int SweepCount { get; set; }\n        public bool IsComplete { get; set; }\n        public bool IsIncremental { get; set; }\n        public long ByteLength { get; set; }\n        public DateTime CreatedUtc { get; set; } = DateTime.UtcNow;\n        public DateTime UpdatedUtc { get; set; } = DateTime.UtcNow;\n    }\n\n    public sealed class SpcCheckpointPersistenceOptions\n    {\n        public bool WriteTemperatureSummary { get; set; } = true;\n        public bool WriteSpinObservations { get; set; } = true;\n        public bool EmbedSpinsInTemperatureSummary { get; set; }\n        public int EpochSweepCount { get; set; }\n        public SpcStateArtifactEncoding SpinObservationEncoding { get; set; } = SpcStateArtifactEncoding.Delta;\n        public SpcStateArtifactCompression SpinObservationCompression { get; set; } = SpcStateArtifactCompression.Brotli;\n    }\n\n    public sealed class HandoffReadinessSnapshot\n    {\n        public int SchemaVersion { get; set; } = 3;\n        public string RunStamp { get; set; } = string.Empty;\n        public string SourceArtifactId { get; set; } = string.Empty;\n        public double Temperature { get; set; } = double.NaN;\n        public int ClusterCount { get; set; }\n        public double Coverage { get; set; } = double.NaN;\n        public double Separation { get; set; } = double.NaN;\n        public double Stability { get; set; } = double.NaN;\n        public double Surprise { get; set; } = double.NaN;\n        public double ReadinessScore { get; set; } = double.NaN;\n        public bool Ready { get; set; }\n        public DateTime ObservedUtc { get; set; } = DateTime.UtcNow;\n    }\n\n    public sealed class SpcTemperatureState\n    {\n        public int SchemaVersion { get; set; } = 3;\n        public string RunStamp { get; set; } = string.Empty;\n        public string ArtifactId { get; set; } = string.Empty;\n        public string TemperatureKey { get; set; } = string.Empty;\n        public double Temperature { get; set; }\n        public int EpochCount { get; set; }\n        public int SweepCount { get; set; }\n        public int ClusterCount { get; set; }\n        public bool IsComplete { get; set; }\n        public int ObservationSequence { get; set; }\n        public string CurrentSpinsArtifactId { get; set; } = string.Empty;\n        public string FinalSpinsArtifactId { get; set; } = string.Empty;\n        public int[] CurrentSpins { get; set; } = Array.Empty<int>();\n        public int[] FinalSpins { get; set; } = Array.Empty<int>();\n        public long[] SpinCounts { get; set; } = Array.Empty<long>();\n        public int SpinCountWidth { get; set; }\n        public double[] CentroidSnapshot { get; set; } = Array.Empty<double>();\n        public int LastSnapshotEpoch { get; set; }\n        public HandoffReadinessSnapshot Readiness { get; set; } = new HandoffReadinessSnapshot();\n        public DateTime CreatedUtc { get; set; } = DateTime.UtcNow;\n        public DateTime UpdatedUtc { get; set; } = DateTime.UtcNow;\n    }\n\n    public sealed class SpcTemperatureSpinFrame\n    {\n        public int SchemaVersion { get; set; } = 3;\n        public string RunStamp { get; set; } = string.Empty;\n        public string ArtifactId { get; set; } = string.Empty;\n        public string TemperatureKey { get; set; } = string.Empty;\n        public double Temperature { get; set; }\n        public int Sequence { get; set; }\n        public int EpochCount { get; set; }\n        public int SweepCount { get; set; }\n        public SpcStateArtifactEncoding Encoding { get; set; } = SpcStateArtifactEncoding.Delta;\n        public int SpinCount { get; set; }\n        public int[] ChangedIndices { get; set; } = Array.Empty<int>();\n        public int[] ChangedSpins { get; set; } = Array.Empty<int>();\n        [JsonIgnore]\n        public int[] Spins { get; set; } = Array.Empty<int>();\n    }\n\n    public sealed class GmmHandoffState\n    {\n        public int SchemaVersion { get; set; } = 3;\n        public string RunStamp { get; set; } = string.Empty;\n        public string SourceRunStamp { get; set; } = string.Empty;\n        public string SourceArtifactId { get; set; } = string.Empty;\n        public double Temperature { get; set; } = double.NaN;\n        public int K { get; set; }\n        public int Dimension { get; set; }\n        public double[] InitialMeans { get; set; } = Array.Empty<double>();\n        public double[] InitialCovariances { get; set; } = Array.Empty<double>();\n        public double[] InitialWeights { get; set; } = Array.Empty<double>();\n        public int[] CoreAssignments { get; set; } = Array.Empty<int>();\n        public double[] CoreStability { get; set; } = Array.Empty<double>();\n        public int[] CorePointIndices { get; set; } = Array.Empty<int>();\n        public HandoffReadinessSnapshot Readiness { get; set; } = new HandoffReadinessSnapshot();\n        public DateTime CreatedUtc { get; set; } = DateTime.UtcNow;\n    }\n\n    public static partial class SpcBatch\n    {\n        private const int StateSchemaVersion = 3;\n        private const int SpinFrameMagic = 0x53435046;\n        private static readonly object StateManifestLock = new object();\n        private static readonly JsonSerializerOptions StateJsonOptions = CreateStateJsonOptions();\n\n        public static string GetStateManifestPath(string directory, string runStamp)\n        {\n            return GetManifestPath(directory, runStamp);\n        }\n\n        public static string GetRunStateDirectory(string directory, string runStamp)\n        {\n            return Path.Combine(directory ?? string.Empty, runStamp ?? string.Empty);\n        }\n\n        public static SpcRunStateManifest LoadStateManifest(string directory, string runStamp)\n        {\n            return LoadStateManifestCore(directory, runStamp);\n        }\n\n        public static SpcCheckpoint LoadCheckpointState(string directory, string runStamp)\n        {\n            return LoadCheckpoint(directory, runStamp);\n        }\n\n        public static List<SpcTemperatureSpinFrame> LoadTemperatureSpinHistory(\n            string directory, string runStamp, double temperature)\n        {\n            var manifest = LoadStateManifestCore(directory, runStamp);\n            return LoadTemperatureSpinHistory(directory, manifest, TemperatureKey(temperature), string.Empty);\n        }\n\n        public static List<SpcTemperatureSpinFrame> LoadTemperatureSpinHistory(\n            string directory, string runStamp, double temperature, string stopArtifactId)\n        {\n            var manifest = LoadStateManifestCore(directory, runStamp);\n            return LoadTemperatureSpinHistory(directory, manifest, TemperatureKey(temperature), stopArtifactId ?? string.Empty);\n        }\n\n        public static int[] LoadTemperatureSpins(\n            string directory, string runStamp, double temperature, string stopArtifactId = null)\n        {\n            var history = LoadTemperatureSpinHistory(directory, runStamp, temperature, stopArtifactId ?? string.Empty);\n            if (history.Count == 0)\n                return Array.Empty<int>();\n\n            return (int[])history[history.Count - 1].Spins.Clone();\n        }\n\n        public static List<SpcStateArtifact> LoadTemperatureObservationArtifacts(\n            string directory, string runStamp, double temperature)\n        {\n            var manifest = LoadStateManifestCore(directory, runStamp);\n            return GetSpinFrameArtifacts(manifest, TemperatureKey(temperature));\n        }\n\n        public static string SaveHandoffReadiness(\n            string directory, string runStamp, HandoffReadinessSnapshot readiness,\n            string artifactId = null)\n        {\n            if (readiness == null) throw new ArgumentNullException(nameof(readiness));\n\n            string id = string.IsNullOrWhiteSpace(artifactId)\n                ? $\"handoff_readiness_{TemperatureKey(readiness.Temperature)}\"\n                : MakeArtifactId(artifactId);\n\n            readiness.SchemaVersion = StateSchemaVersion;\n            readiness.RunStamp = runStamp;\n\n            return SaveStateArtifact(\n                directory,\n                runStamp,\n                id,\n                SpcStateArtifactKind.HandoffReadiness,\n                SpcPipelineStage.Handoff,\n                readiness.Temperature,\n                0,\n                0,\n                readiness.Ready,\n                readiness,\n                readiness.SourceArtifactId);\n        }\n\n        public static string SaveGmmHandoffState(\n            string directory, string runStamp, GmmHandoffState handoff,\n            string artifactId = null)\n        {\n            if (handoff == null) throw new ArgumentNullException(nameof(handoff));\n\n            string id = string.IsNullOrWhiteSpace(artifactId)\n                ? $\"gmm_handoff_{TemperatureKey(handoff.Temperature)}\"\n                : MakeArtifactId(artifactId);\n\n            handoff.SchemaVersion = StateSchemaVersion;\n            handoff.RunStamp = runStamp;\n            if (string.IsNullOrEmpty(handoff.SourceRunStamp))\n                handoff.SourceRunStamp = runStamp;\n\n            return SaveStateArtifact(\n                directory,\n                runStamp,\n                id,\n                SpcStateArtifactKind.GmmHandoffState,\n                SpcPipelineStage.Handoff,\n                handoff.Temperature,\n                0,\n                0,\n                true,\n                handoff,\n                handoff.SourceArtifactId);\n        }\n\n        internal static void InitializeRunState(\n            string directory, string runStamp, string parentRunStamp = null)\n        {\n            string runDirectory = GetRunStateDirectory(directory, runStamp);\n            Directory.CreateDirectory(runDirectory);\n\n            lock (StateManifestLock)\n            {\n                var manifest = LoadStateManifestCore(directory, runStamp);\n                manifest.SchemaVersion = StateSchemaVersion;\n                manifest.RunStamp = runStamp;\n                manifest.WorkingDirectory = directory ?? string.Empty;\n                manifest.StateDirectory = runDirectory;\n                if (!string.IsNullOrEmpty(parentRunStamp) && parentRunStamp != runStamp)\n                    manifest.ParentRunStamp = parentRunStamp;\n                if (manifest.CreatedUtc == default(DateTime))\n                    manifest.CreatedUtc = DateTime.UtcNow;\n                manifest.UpdatedUtc = DateTime.UtcNow;\n                NormalizeManifest(manifest);\n                WriteJsonAtomic(GetManifestPath(directory, runStamp), manifest);\n            }\n        }\n\n        internal static void FlushCheckpoint(\n            string directory, string runStamp, double T, SimulationResult sr)\n        {\n            FlushCheckpoint(directory, runStamp, T, 0, sr, null);\n        }\n\n        internal static void FlushCheckpoint(\n            string directory, string runStamp, double T, int sweepCount, SimulationResult sr)\n        {\n            FlushCheckpoint(directory, runStamp, T, sweepCount, sr, null);\n        }\n\n        internal static void FlushCheckpoint(\n            string directory,\n            string runStamp,\n            double T,\n            int sweepCount,\n            SimulationResult sr,\n            SpcCheckpointPersistenceOptions persistence)\n        {\n            int epochCount = sr.EpochCount > 0 ? sr.EpochCount : 1;\n            FlushCheckpoint(directory, runStamp, T, epochCount, sweepCount, sr, true, persistence);\n        }\n\n        internal static void FlushCheckpoint(\n            string directory,\n            string runStamp,\n            double T,\n            int epochCount,\n            int sweepCount,\n            SimulationResult sr,\n            bool isComplete,\n            SpcCheckpointPersistenceOptions persistence)\n        {\n            Directory.CreateDirectory(GetRunStateDirectory(directory, runStamp));\n            var options = NormalizePersistenceOptions(persistence);\n\n            if (!options.WriteTemperatureSummary && !options.WriteSpinObservations)\n                return;\n\n            var state = CreateTemperatureState(runStamp, T, epochCount, sweepCount, sr, isComplete);\n\n            if (options.WriteSpinObservations)\n            {\n                var frame = WriteSpinFrameArtifact(\n                    directory,\n                    runStamp,\n                    T,\n                    state.EpochCount,\n                    sweepCount,\n                    sr.FinalSpins ?? Array.Empty<int>(),\n                    isComplete,\n                    options.SpinObservationEncoding,\n                    options.SpinObservationCompression);\n\n                state.ObservationSequence = frame.Sequence;\n                state.CurrentSpinsArtifactId = frame.ArtifactId;\n                if (isComplete)\n                    state.FinalSpinsArtifactId = frame.ArtifactId;\n            }\n\n            if (!options.EmbedSpinsInTemperatureSummary)\n            {\n                state.CurrentSpins = Array.Empty<int>();\n                state.FinalSpins = Array.Empty<int>();\n            }\n\n            if (!options.WriteTemperatureSummary)\n                return;\n\n            string ckptPath = GetTemperatureCheckpointPath(directory, runStamp, T);\n\n            WriteJsonAtomic(ckptPath, state);\n\n            UpsertArtifact(directory, runStamp, new SpcStateArtifact\n            {\n                ArtifactId = state.ArtifactId,\n                Kind = SpcStateArtifactKind.TemperatureCheckpoint,\n                Stage = SpcPipelineStage.Spc,\n                PayloadType = PayloadType(SpcStateArtifactKind.TemperatureCheckpoint),\n                RelativePath = GetArtifactRelativePath(SpcStateArtifactKind.TemperatureCheckpoint, Path.GetFileName(ckptPath)),\n                SourceArtifactId = isComplete ? state.FinalSpinsArtifactId : state.CurrentSpinsArtifactId,\n                Container = SpcStateArtifactContainer.Json,\n                Compression = SpcStateArtifactCompression.None,\n                Encoding = SpcStateArtifactEncoding.Snapshot,\n                TemperatureKey = state.TemperatureKey,\n                Temperature = T,\n                Sequence = state.ObservationSequence,\n                EpochCount = state.EpochCount,\n                SweepCount = state.SweepCount,\n                IsComplete = isComplete,\n                IsIncremental = false,\n                ByteLength = new FileInfo(ckptPath).Length,\n                UpdatedUtc = DateTime.UtcNow\n            });\n        }\n\n        internal static SpcCheckpoint LoadCheckpoint(string directory, string runStamp)\n        {\n            var cp = new SpcCheckpoint();\n            if (!Directory.Exists(directory))\n                return cp;\n\n            cp.Manifest = LoadStateManifestCore(directory, runStamp);\n\n            string pattern = $\"spc_{runStamp}_*.ckpt\";\n            foreach (string file in GetTemperatureCheckpointFiles(directory, runStamp, pattern))\n            {\n                try\n                {\n                    if (!TryLoadTemperatureState(file, runStamp, out var state))\n                        continue;\n\n                    int[] spins = state.FinalSpins.Length > 0\n                        ? state.FinalSpins\n                        : state.CurrentSpins;\n\n                    if (spins.Length == 0 &&\n                        TryLoadLatestSpinFrame(directory, cp.Manifest, state, out var frame))\n                    {\n                        spins = frame.Spins;\n                        state.CurrentSpins = frame.Spins;\n                        state.FinalSpins = state.IsComplete ? frame.Spins : Array.Empty<int>();\n                    }\n\n                    if (spins.Length == 0)\n                        continue;\n\n                    double T = state.Temperature;\n                    cp.Spins[T] = spins;\n                    cp.TemperatureStates[T] = state;\n                }\n                catch\n                {\n                    // Corrupt or partial file: skip silently; temperature will re-run.\n                }\n            }\n\n            RecoverCheckpointTemperaturesFromHistory(directory, runStamp, cp);\n\n            return cp;\n        }\n\n        private static JsonSerializerOptions CreateStateJsonOptions()\n        {\n            var options = new JsonSerializerOptions\n            {\n                WriteIndented = true,\n                NumberHandling = JsonNumberHandling.AllowNamedFloatingPointLiterals\n            };\n            options.Converters.Add(new JsonStringEnumConverter());\n            return options;\n        }\n\n        internal static SpcTemperatureState CreateTemperatureState(\n            string runStamp, double T, int sweepCount, SimulationResult sr)\n        {\n            int epochCount = sr.EpochCount > 0 ? sr.EpochCount : 1;\n            return CreateTemperatureState(runStamp, T, epochCount, sweepCount, sr, sr.IsComplete);\n        }\n\n        internal static SpcTemperatureState CreateTemperatureState(\n            string runStamp, double T, int epochCount, int sweepCount, SimulationResult sr, bool isComplete)\n        {\n            string temperatureKey = TemperatureKey(T);\n            int[] currentSpins = sr.FinalSpins == null\n                ? Array.Empty<int>()\n                : (int[])sr.FinalSpins.Clone();\n\n            return new SpcTemperatureState\n            {\n                SchemaVersion = StateSchemaVersion,\n                RunStamp = runStamp ?? string.Empty,\n                ArtifactId = $\"temperature_{temperatureKey}\",\n                TemperatureKey = temperatureKey,\n                Temperature = T,\n                EpochCount = epochCount,\n                SweepCount = sweepCount > 0 ? sweepCount : sr.SweepCount,\n                ClusterCount = CountDistinctLabels(currentSpins),\n                IsComplete = isComplete,\n                CurrentSpins = (int[])currentSpins.Clone(),\n                FinalSpins = isComplete ? currentSpins : Array.Empty<int>(),\n                UpdatedUtc = DateTime.UtcNow\n            };\n        }\n\n        private static SpcTemperatureSpinFrame WriteSpinFrameArtifact(\n            string directory,\n            string runStamp,\n            double T,\n            int epochCount,\n            int sweepCount,\n            int[] spins,\n            bool isComplete,\n            SpcStateArtifactEncoding encoding,\n            SpcStateArtifactCompression compression)\n        {\n            string temperatureKey = TemperatureKey(T);\n            var manifest = LoadStateManifestCore(directory, runStamp);\n            int sequence = NextSequenceForTemperature(manifest, temperatureKey);\n            string artifactId = $\"temperature_{temperatureKey}_spins_{sequence:D8}\";\n            string fileName = $\"spc_{runStamp}_{artifactId}.bin{CompressionExtension(compression)}\";\n            string relativePath = GetArtifactRelativePath(SpcStateArtifactKind.TemperatureObservation, fileName);\n            string path = GetArtifactPath(directory, runStamp, SpcStateArtifactKind.TemperatureObservation, fileName);\n            Directory.CreateDirectory(Path.GetDirectoryName(path));\n\n            int[] baselineSpins = Array.Empty<int>();\n            string previousArtifactId = string.Empty;\n            string baseArtifactId = artifactId;\n            if (encoding == SpcStateArtifactEncoding.Delta &&\n                TryLoadLatestSpinFrame(directory, manifest, temperatureKey, string.Empty, out var previousFrame))\n            {\n                baselineSpins = previousFrame.Spins;\n                previousArtifactId = previousFrame.ArtifactId;\n                baseArtifactId = GetBaseArtifactId(manifest, previousArtifactId);\n            }\n\n            var frame = CreateSpinObservationFrame(\n                runStamp,\n                artifactId,\n                temperatureKey,\n                T,\n                sequence,\n                epochCount,\n                sweepCount,\n                baselineSpins,\n                spins,\n                encoding);\n\n            WriteSpinFrameAtomic(path, frame, compression);\n\n            UpsertArtifact(directory, runStamp, new SpcStateArtifact\n            {\n                ArtifactId = artifactId,\n                Kind = SpcStateArtifactKind.TemperatureObservation,\n                Stage = SpcPipelineStage.Spc,\n                PayloadType = PayloadType(SpcStateArtifactKind.TemperatureObservation),\n                RelativePath = relativePath,\n                PreviousArtifactId = previousArtifactId,\n                BaseArtifactId = baseArtifactId,\n                Container = SpcStateArtifactContainer.Binary,\n                Compression = compression,\n                Encoding = encoding,\n                TemperatureKey = temperatureKey,\n                Temperature = T,\n                Sequence = sequence,\n                EpochCount = epochCount,\n                SweepCount = sweepCount,\n                IsComplete = isComplete,\n                IsIncremental = true,\n                ByteLength = new FileInfo(path).Length,\n                UpdatedUtc = DateTime.UtcNow\n            });\n\n            return frame;\n        }\n\n        private static SpcTemperatureSpinFrame CreateSpinObservationFrame(\n            string runStamp,\n            string artifactId,\n            string temperatureKey,\n            double temperature,\n            int sequence,\n            int epochCount,\n            int sweepCount,\n            int[] baselineSpins,\n            int[] currentSpins,\n            SpcStateArtifactEncoding encoding)\n        {\n            int[] baseline = baselineSpins ?? Array.Empty<int>();\n            int[] current = currentSpins ?? Array.Empty<int>();\n            var changedIndices = new List<int>();\n            var changedSpins = new List<int>();\n\n            for (int index = 0; index < current.Length; index++)\n            {\n                if (index >= baseline.Length || baseline[index] != current[index])\n                {\n                    changedIndices.Add(index);\n                    changedSpins.Add(current[index]);\n                }\n            }\n\n            return new SpcTemperatureSpinFrame\n            {\n                SchemaVersion = StateSchemaVersion,\n                RunStamp = runStamp,\n                ArtifactId = artifactId,\n                TemperatureKey = temperatureKey,\n                Temperature = temperature,\n                Sequence = sequence,\n                EpochCount = epochCount,\n                SweepCount = sweepCount,\n                Encoding = encoding,\n                SpinCount = current.Length,\n                ChangedIndices = changedIndices.ToArray(),\n                ChangedSpins = changedSpins.ToArray(),\n                Spins = (int[])current.Clone()\n            };\n        }\n\n        private static int NextSequenceForTemperature(SpcRunStateManifest manifest, string temperatureKey)\n        {\n            int max = 0;\n            for (int i = 0; i < manifest.Artifacts.Count; i++)\n            {\n                var artifact = manifest.Artifacts[i];\n                if (artifact.Kind == SpcStateArtifactKind.TemperatureObservation &&\n                    artifact.TemperatureKey == temperatureKey &&\n                    artifact.Sequence > max)\n                    max = artifact.Sequence;\n            }\n            return max + 1;\n        }\n\n        private static List<SpcTemperatureSpinFrame> LoadTemperatureSpinHistory(\n            string directory,\n            SpcRunStateManifest manifest,\n            string temperatureKey,\n            string stopArtifactId)\n        {\n            var artifacts = GetSpinFrameArtifacts(manifest, temperatureKey);\n            var history = new List<SpcTemperatureSpinFrame>(artifacts.Count);\n            int[] materializedSpins = Array.Empty<int>();\n\n            for (int i = 0; i < artifacts.Count; i++)\n            {\n                var artifact = artifacts[i];\n                bool isStopArtifact = !string.IsNullOrEmpty(stopArtifactId) && artifact.ArtifactId == stopArtifactId;\n                if (!TryReadSpinFrameArtifact(directory, manifest.RunStamp, artifact, out var frame))\n                    break;\n\n                materializedSpins = ApplySpinFrame(materializedSpins, frame);\n                history.Add(frame);\n\n                if (isStopArtifact)\n                    break;\n            }\n\n            return history;\n        }\n\n        private static bool TryLoadLatestSpinFrame(\n            string directory,\n            SpcRunStateManifest manifest,\n            string temperatureKey,\n            string stopArtifactId,\n            out SpcTemperatureSpinFrame frame)\n        {\n            frame = null;\n            var history = LoadTemperatureSpinHistory(directory, manifest, temperatureKey, stopArtifactId);\n            if (history.Count == 0)\n                return false;\n\n            frame = history[history.Count - 1];\n            return true;\n        }\n\n        private static bool TryLoadLatestSpinFrame(\n            string directory,\n            SpcRunStateManifest manifest,\n            SpcTemperatureState state,\n            out SpcTemperatureSpinFrame frame)\n        {\n            frame = null;\n\n            if (state == null)\n                return false;\n\n            if (!string.IsNullOrEmpty(state.FinalSpinsArtifactId))\n                return TryLoadLatestSpinFrame(directory, manifest, state.TemperatureKey, state.FinalSpinsArtifactId, out frame);\n\n            return TryLoadLatestSpinFrame(directory, manifest, state.TemperatureKey, string.Empty, out frame);\n        }\n\n        private static void RecoverCheckpointTemperaturesFromHistory(\n            string directory, string runStamp, SpcCheckpoint checkpoint)\n        {\n            if (checkpoint.Manifest == null || checkpoint.Manifest.Artifacts == null)\n                return;\n\n            var latestByTemperature = new Dictionary<string, SpcStateArtifact>();\n            for (int i = 0; i < checkpoint.Manifest.Artifacts.Count; i++)\n            {\n                var artifact = checkpoint.Manifest.Artifacts[i];\n                if (artifact.Kind != SpcStateArtifactKind.TemperatureObservation)\n                    continue;\n\n                if (!latestByTemperature.TryGetValue(artifact.TemperatureKey, out var existing) ||\n                    artifact.Sequence > existing.Sequence)\n                    latestByTemperature[artifact.TemperatureKey] = artifact;\n            }\n\n            foreach (var artifact in latestByTemperature.Values)\n            {\n                if (checkpoint.Spins.ContainsKey(artifact.Temperature))\n                    continue;\n\n                var history = LoadTemperatureSpinHistory(\n                    directory,\n                    checkpoint.Manifest,\n                    artifact.TemperatureKey,\n                    artifact.ArtifactId);\n                if (history.Count == 0)\n                    continue;\n\n                var frame = history[history.Count - 1];\n\n                checkpoint.Spins[frame.Temperature] = frame.Spins;\n                checkpoint.TemperatureStates[frame.Temperature] = new SpcTemperatureState\n                {\n                    SchemaVersion = StateSchemaVersion,\n                    RunStamp = runStamp,\n                    ArtifactId = $\"temperature_{frame.TemperatureKey}\",\n                    TemperatureKey = frame.TemperatureKey,\n                    Temperature = frame.Temperature,\n                    EpochCount = frame.EpochCount,\n                    SweepCount = frame.SweepCount,\n                    ObservationSequence = frame.Sequence,\n                    CurrentSpinsArtifactId = frame.ArtifactId,\n                    FinalSpinsArtifactId = artifact.IsComplete ? frame.ArtifactId : string.Empty,\n                    CurrentSpins = frame.Spins,\n                    FinalSpins = artifact.IsComplete ? frame.Spins : Array.Empty<int>(),\n                    ClusterCount = CountDistinctLabels(frame.Spins),\n                    IsComplete = artifact.IsComplete\n                };\n            }\n        }\n\n        private static List<SpcStateArtifact> GetSpinFrameArtifacts(\n            SpcRunStateManifest manifest, string temperatureKey)\n        {\n            var artifacts = new List<SpcStateArtifact>();\n            if (manifest == null || manifest.Artifacts == null)\n                return artifacts;\n\n            for (int i = 0; i < manifest.Artifacts.Count; i++)\n            {\n                var artifact = manifest.Artifacts[i];\n                if (artifact.Kind == SpcStateArtifactKind.TemperatureObservation &&\n                    artifact.TemperatureKey == temperatureKey)\n                    artifacts.Add(artifact);\n            }\n\n            artifacts.Sort((a, b) => a.Sequence.CompareTo(b.Sequence));\n            return artifacts;\n        }\n\n        private static string GetBaseArtifactId(SpcRunStateManifest manifest, string artifactId)\n        {\n            if (manifest == null || manifest.Artifacts == null || string.IsNullOrEmpty(artifactId))\n                return artifactId ?? string.Empty;\n\n            for (int i = 0; i < manifest.Artifacts.Count; i++)\n            {\n                var artifact = manifest.Artifacts[i];\n                if (artifact.ArtifactId == artifactId)\n                    return string.IsNullOrEmpty(artifact.BaseArtifactId) ? artifact.ArtifactId : artifact.BaseArtifactId;\n            }\n\n            return artifactId;\n        }\n\n        private static bool TryReadSpinFrameArtifact(\n            string directory, string runStamp, SpcStateArtifact artifact, out SpcTemperatureSpinFrame frame)\n        {\n            frame = null;\n            if (artifact == null || string.IsNullOrEmpty(artifact.RelativePath))\n                return false;\n\n            string path = ResolveArtifactPath(directory, runStamp, artifact);\n            if (!File.Exists(path))\n                return false;\n\n            try\n            {\n                frame = ReadSpinFrame(path, artifact.Compression);\n                if (string.IsNullOrEmpty(frame.ArtifactId)) frame.ArtifactId = artifact.ArtifactId;\n                if (string.IsNullOrEmpty(frame.TemperatureKey)) frame.TemperatureKey = artifact.TemperatureKey;\n                frame.Encoding = artifact.Encoding;\n                return true;\n            }\n            catch\n            {\n                frame = null;\n                return false;\n            }\n        }\n\n        private static int[] ApplySpinFrame(int[] baselineSpins, SpcTemperatureSpinFrame frame)\n        {\n            int spinCount = frame.SpinCount;\n            if (spinCount < 0)\n                throw new InvalidDataException(\"Invalid SPC spin-frame spin count.\");\n\n            var materialized = new int[spinCount];\n            if (frame.Encoding == SpcStateArtifactEncoding.Delta && baselineSpins != null && baselineSpins.Length > 0)\n                Array.Copy(baselineSpins, materialized, Math.Min(baselineSpins.Length, spinCount));\n\n            int[] changedIndices = frame.ChangedIndices ?? Array.Empty<int>();\n            int[] changedSpins = frame.ChangedSpins ?? Array.Empty<int>();\n            if (changedIndices.Length != changedSpins.Length)\n                throw new InvalidDataException(\"Invalid SPC spin-frame delta payload.\");\n\n            for (int change = 0; change < changedIndices.Length; change++)\n            {\n                int spinIndex = changedIndices[change];\n                if (spinIndex < 0 || spinIndex >= spinCount)\n                    throw new InvalidDataException(\"Invalid SPC spin-frame delta index.\");\n\n                materialized[spinIndex] = changedSpins[change];\n            }\n\n            frame.Spins = materialized;\n            return materialized;\n        }\n\n        private static void WriteSpinFrameAtomic(\n            string path, SpcTemperatureSpinFrame frame, SpcStateArtifactCompression compression)\n        {\n            int[] changedIndices = frame.ChangedIndices ?? Array.Empty<int>();\n            int[] changedSpins = frame.ChangedSpins ?? Array.Empty<int>();\n            if (changedIndices.Length != changedSpins.Length)\n                throw new InvalidDataException(\"Invalid SPC spin-frame delta payload.\");\n\n            string tmpPath = path + \".tmp\";\n            using (var file = new FileStream(tmpPath, FileMode.Create, FileAccess.Write, FileShare.None))\n            using (var compressed = CreateCompressionWriteStream(file, compression))\n            using (var writer = new BinaryWriter(compressed, Encoding.UTF8, leaveOpen: false))\n            {\n                writer.Write(SpinFrameMagic);\n                writer.Write(StateSchemaVersion);\n                writer.Write(frame.RunStamp ?? string.Empty);\n                writer.Write(frame.ArtifactId ?? string.Empty);\n                writer.Write(frame.TemperatureKey ?? string.Empty);\n                writer.Write(frame.Temperature);\n                writer.Write(frame.Sequence);\n                writer.Write(frame.EpochCount);\n                writer.Write(frame.SweepCount);\n                writer.Write(frame.SpinCount);\n                writer.Write(changedIndices.Length);\n                for (int change = 0; change < changedIndices.Length; change++)\n                {\n                    writer.Write(changedIndices[change]);\n                    writer.Write(changedSpins[change]);\n                }\n            }\n\n            File.Move(tmpPath, path, overwrite: true);\n        }\n\n        private static SpcTemperatureSpinFrame ReadSpinFrame(\n            string path, SpcStateArtifactCompression compression)\n        {\n            using (var file = new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.Read))\n            using (var compressed = CreateCompressionReadStream(file, compression))\n            using (var reader = new BinaryReader(compressed, Encoding.UTF8, leaveOpen: false))\n            {\n                int magic = reader.ReadInt32();\n                if (magic != SpinFrameMagic)\n                    throw new InvalidDataException(\"Invalid SPC spin-frame artifact.\");\n\n                int schemaVersion = reader.ReadInt32();\n                string runStamp = reader.ReadString();\n                string artifactId = reader.ReadString();\n                string temperatureKey = reader.ReadString();\n                double temperature = reader.ReadDouble();\n                int sequence = reader.ReadInt32();\n                int epochCount = reader.ReadInt32();\n                int sweepCount = reader.ReadInt32();\n\n                if (schemaVersion <= 2)\n                    return ReadLegacySnapshotSpinFrame(\n                        reader,\n                        schemaVersion,\n                        runStamp,\n                        artifactId,\n                        temperatureKey,\n                        temperature,\n                        sequence,\n                        epochCount,\n                        sweepCount);\n\n                int spinCount = reader.ReadInt32();\n                int changeCount = reader.ReadInt32();\n\n                if (spinCount < 0 || changeCount < 0)\n                    throw new InvalidDataException(\"Invalid SPC spin-frame delta length.\");\n\n                var changedIndices = new int[changeCount];\n                var changedSpins = new int[changeCount];\n                for (int change = 0; change < changeCount; change++)\n                {\n                    changedIndices[change] = reader.ReadInt32();\n                    changedSpins[change] = reader.ReadInt32();\n                }\n\n                return new SpcTemperatureSpinFrame\n                {\n                    SchemaVersion = schemaVersion,\n                    RunStamp = runStamp,\n                    ArtifactId = artifactId,\n                    TemperatureKey = temperatureKey,\n                    Temperature = temperature,\n                    Sequence = sequence,\n                    EpochCount = epochCount,\n                    SweepCount = sweepCount,\n                    SpinCount = spinCount,\n                    ChangedIndices = changedIndices,\n                    ChangedSpins = changedSpins\n                };\n            }\n        }\n\n        private static SpcTemperatureSpinFrame ReadLegacySnapshotSpinFrame(\n            BinaryReader reader,\n            int schemaVersion,\n            string runStamp,\n            string artifactId,\n            string temperatureKey,\n            double temperature,\n            int sequence,\n            int epochCount,\n            int sweepCount)\n        {\n            int length = reader.ReadInt32();\n\n            if (length < 0)\n                throw new InvalidDataException(\"Invalid SPC spin-frame length.\");\n\n            var changedIndices = new int[length];\n            var changedSpins = new int[length];\n            for (int index = 0; index < length; index++)\n            {\n                changedIndices[index] = index;\n                changedSpins[index] = reader.ReadInt32();\n            }\n\n            return new SpcTemperatureSpinFrame\n            {\n                SchemaVersion = schemaVersion,\n                RunStamp = runStamp,\n                ArtifactId = artifactId,\n                TemperatureKey = temperatureKey,\n                Temperature = temperature,\n                Sequence = sequence,\n                EpochCount = epochCount,\n                SweepCount = sweepCount,\n                Encoding = SpcStateArtifactEncoding.Snapshot,\n                SpinCount = length,\n                ChangedIndices = changedIndices,\n                ChangedSpins = changedSpins\n            };\n        }\n\n        private static Stream CreateCompressionWriteStream(\n            Stream stream, SpcStateArtifactCompression compression)\n        {\n            switch (compression)\n            {\n                case SpcStateArtifactCompression.None:\n                    return stream;\n                case SpcStateArtifactCompression.Brotli:\n                    return new BrotliStream(stream, CompressionLevel.SmallestSize, leaveOpen: false);\n                case SpcStateArtifactCompression.GZip:\n                    return new GZipStream(stream, CompressionLevel.SmallestSize, leaveOpen: false);\n                case SpcStateArtifactCompression.Deflate:\n                    return new DeflateStream(stream, CompressionLevel.SmallestSize, leaveOpen: false);\n                default:\n                    throw new NotSupportedException($\"Compression {compression} is not supported.\");\n            }\n        }\n\n        private static Stream CreateCompressionReadStream(\n            Stream stream, SpcStateArtifactCompression compression)\n        {\n            switch (compression)\n            {\n                case SpcStateArtifactCompression.None:\n                    return stream;\n                case SpcStateArtifactCompression.Brotli:\n                    return new BrotliStream(stream, CompressionMode.Decompress, leaveOpen: false);\n                case SpcStateArtifactCompression.GZip:\n                    return new GZipStream(stream, CompressionMode.Decompress, leaveOpen: false);\n                case SpcStateArtifactCompression.Deflate:\n                    return new DeflateStream(stream, CompressionMode.Decompress, leaveOpen: false);\n                default:\n                    throw new NotSupportedException($\"Compression {compression} is not supported.\");\n            }\n        }\n\n        private static string CompressionExtension(SpcStateArtifactCompression compression)\n        {\n            switch (compression)\n            {\n                case SpcStateArtifactCompression.None: return string.Empty;\n                case SpcStateArtifactCompression.Brotli: return \".br\";\n                case SpcStateArtifactCompression.GZip: return \".gz\";\n                case SpcStateArtifactCompression.Deflate: return \".deflate\";\n                default: return \".compressed\";\n            }\n        }\n\n        private static bool TryLoadTemperatureState(\n            string file, string runStamp, out SpcTemperatureState state)\n        {\n            state = null;\n\n            string text = File.ReadAllText(file);\n            string trimmed = text.TrimStart();\n            if (trimmed.Length == 0)\n                return false;\n\n            if (trimmed[0] == '{')\n            {\n                var parsed = JsonSerializer.Deserialize<SpcTemperatureState>(text, StateJsonOptions);\n                if (parsed == null)\n                    return false;\n\n                NormalizeTemperatureState(parsed, runStamp);\n                state = parsed;\n                return true;\n            }\n\n            string[] lines = text.Split(new[] { \"\\r\\n\", \"\\n\" }, StringSplitOptions.None);\n            if (lines.Length < 3)\n                return false;\n\n            double T = double.Parse(lines[0], CultureInfo.InvariantCulture);\n            double.Parse(lines[1], CultureInfo.InvariantCulture);\n\n            string[] parts = lines[2].Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);\n            var spins = new int[parts.Length];\n            for (int i = 0; i < parts.Length; i++)\n                spins[i] = int.Parse(parts[i], CultureInfo.InvariantCulture);\n\n            state = CreateTemperatureState(\n                runStamp,\n                T,\n                0,\n                new SimulationResult(T, spins));\n            return true;\n        }\n\n        private static void NormalizeTemperatureState(SpcTemperatureState state, string runStamp)\n        {\n            state.SchemaVersion = state.SchemaVersion == 0 ? StateSchemaVersion : state.SchemaVersion;\n            if (string.IsNullOrEmpty(state.RunStamp)) state.RunStamp = runStamp;\n            if (string.IsNullOrEmpty(state.TemperatureKey)) state.TemperatureKey = TemperatureKey(state.Temperature);\n            if (string.IsNullOrEmpty(state.ArtifactId)) state.ArtifactId = $\"temperature_{state.TemperatureKey}\";\n            if (string.IsNullOrEmpty(state.CurrentSpinsArtifactId)) state.CurrentSpinsArtifactId = string.Empty;\n            if (string.IsNullOrEmpty(state.FinalSpinsArtifactId)) state.FinalSpinsArtifactId = string.Empty;\n            if (state.CurrentSpins == null) state.CurrentSpins = Array.Empty<int>();\n            if (state.FinalSpins == null) state.FinalSpins = Array.Empty<int>();\n            if (!state.IsComplete &&\n                (!string.IsNullOrEmpty(state.FinalSpinsArtifactId) || state.FinalSpins.Length > 0))\n                state.IsComplete = true;\n            if (state.IsComplete && state.FinalSpins.Length == 0 && state.CurrentSpins.Length > 0)\n                state.FinalSpins = (int[])state.CurrentSpins.Clone();\n            if (state.CurrentSpins.Length == 0 && state.FinalSpins.Length > 0)\n                state.CurrentSpins = (int[])state.FinalSpins.Clone();\n            if (!state.IsComplete && state.FinalSpins.Length > 0)\n                state.FinalSpins = Array.Empty<int>();\n            if (state.SpinCounts == null) state.SpinCounts = Array.Empty<long>();\n            if (state.CentroidSnapshot == null) state.CentroidSnapshot = Array.Empty<double>();\n            if (state.Readiness == null) state.Readiness = new HandoffReadinessSnapshot();\n            if (state.ClusterCount == 0)\n                state.ClusterCount = CountDistinctLabels(\n                    state.CurrentSpins.Length > 0 ? state.CurrentSpins : state.FinalSpins);\n        }\n\n        private static SpcRunStateManifest LoadStateManifestCore(string directory, string runStamp)\n        {\n            string manifestPath = GetManifestPath(directory, runStamp);\n            if (!File.Exists(manifestPath))\n            {\n                string legacyManifestPath = Path.Combine(directory, $\"spc_{runStamp}.manifest.json\");\n                if (File.Exists(legacyManifestPath))\n                    manifestPath = legacyManifestPath;\n            }\n\n            SpcRunStateManifest manifest = null;\n\n            if (File.Exists(manifestPath))\n            {\n                try\n                {\n                    string text = File.ReadAllText(manifestPath);\n                    manifest = JsonSerializer.Deserialize<SpcRunStateManifest>(text, StateJsonOptions);\n                }\n                catch\n                {\n                    manifest = null;\n                }\n            }\n\n            if (manifest != null)\n            {\n                if (string.IsNullOrEmpty(manifest.WorkingDirectory))\n                    manifest.WorkingDirectory = directory ?? string.Empty;\n                if (string.IsNullOrEmpty(manifest.StateDirectory) || manifest.StateDirectory == directory)\n                    manifest.StateDirectory = GetRunStateDirectory(directory, runStamp);\n            }\n\n            if (manifest == null)\n            {\n                manifest = new SpcRunStateManifest\n                {\n                    SchemaVersion = StateSchemaVersion,\n                    RunStamp = runStamp ?? string.Empty,\n                    WorkingDirectory = directory ?? string.Empty,\n                    StateDirectory = GetRunStateDirectory(directory, runStamp),\n                    CreatedUtc = DateTime.UtcNow,\n                    UpdatedUtc = DateTime.UtcNow\n                };\n            }\n\n            NormalizeManifest(manifest);\n            return manifest;\n        }\n\n        private static void NormalizeManifest(SpcRunStateManifest manifest)\n        {\n            if (manifest.Artifacts == null)\n                manifest.Artifacts = new List<SpcStateArtifact>();\n            if (string.IsNullOrEmpty(manifest.RunStamp))\n                manifest.RunStamp = string.Empty;\n            if (string.IsNullOrEmpty(manifest.ParentRunStamp))\n                manifest.ParentRunStamp = string.Empty;\n            if (string.IsNullOrEmpty(manifest.WorkingDirectory))\n                manifest.WorkingDirectory = string.Empty;\n            if (string.IsNullOrEmpty(manifest.StateDirectory))\n                manifest.StateDirectory = string.Empty;\n            for (int i = 0; i < manifest.Artifacts.Count; i++)\n            {\n                var artifact = manifest.Artifacts[i];\n                if (string.IsNullOrEmpty(artifact.PayloadType)) artifact.PayloadType = PayloadType(artifact.Kind);\n                if (string.IsNullOrEmpty(artifact.SourceArtifactId)) artifact.SourceArtifactId = string.Empty;\n                if (string.IsNullOrEmpty(artifact.PreviousArtifactId)) artifact.PreviousArtifactId = string.Empty;\n                if (string.IsNullOrEmpty(artifact.BaseArtifactId)) artifact.BaseArtifactId = string.Empty;\n            }\n            if (manifest.CreatedUtc == default(DateTime))\n                manifest.CreatedUtc = DateTime.UtcNow;\n            if (manifest.UpdatedUtc == default(DateTime))\n                manifest.UpdatedUtc = DateTime.UtcNow;\n        }\n\n        private static SpcCheckpointPersistenceOptions NormalizePersistenceOptions(\n            SpcCheckpointPersistenceOptions persistence)\n        {\n            if (persistence == null)\n                return new SpcCheckpointPersistenceOptions();\n\n            if (persistence.EpochSweepCount < 0)\n                persistence.EpochSweepCount = 0;\n\n            if (persistence.SpinObservationEncoding != SpcStateArtifactEncoding.Delta &&\n                persistence.SpinObservationEncoding != SpcStateArtifactEncoding.Snapshot)\n                persistence.SpinObservationEncoding = SpcStateArtifactEncoding.Delta;\n\n            if (!Enum.IsDefined(typeof(SpcStateArtifactCompression), persistence.SpinObservationCompression))\n                persistence.SpinObservationCompression = SpcStateArtifactCompression.Brotli;\n\n            return persistence;\n        }\n\n        private static string SaveStateArtifact<T>(\n            string directory,\n            string runStamp,\n            string artifactId,\n            SpcStateArtifactKind kind,\n            SpcPipelineStage stage,\n            double temperature,\n            int epochCount,\n            int sweepCount,\n            bool isComplete,\n            T state,\n            string sourceArtifactId = null)\n        {\n            Directory.CreateDirectory(GetArtifactDirectory(directory, runStamp, kind));\n\n            string safeArtifactId = MakeArtifactId(artifactId);\n            string fileName = $\"spc_{runStamp}_{safeArtifactId}.json\";\n            string path = GetArtifactPath(directory, runStamp, kind, fileName);\n            WriteJsonAtomic(path, state);\n\n            UpsertArtifact(directory, runStamp, new SpcStateArtifact\n            {\n                ArtifactId = safeArtifactId,\n                Kind = kind,\n                Stage = stage,\n                PayloadType = PayloadType(kind),\n                RelativePath = GetArtifactRelativePath(kind, fileName),\n                SourceArtifactId = sourceArtifactId ?? string.Empty,\n                Container = SpcStateArtifactContainer.Json,\n                Compression = SpcStateArtifactCompression.None,\n                Encoding = SpcStateArtifactEncoding.Snapshot,\n                TemperatureKey = double.IsNaN(temperature) ? string.Empty : TemperatureKey(temperature),\n                Temperature = temperature,\n                EpochCount = epochCount,\n                SweepCount = sweepCount,\n                IsComplete = isComplete,\n                IsIncremental = false,\n                ByteLength = new FileInfo(path).Length,\n                UpdatedUtc = DateTime.UtcNow\n            });\n\n            return GetArtifactRelativePath(kind, fileName);\n        }\n\n        private static void UpsertArtifact(\n            string directory, string runStamp, SpcStateArtifact artifact)\n        {\n            lock (StateManifestLock)\n            {\n                var manifest = LoadStateManifestCore(directory, runStamp);\n                manifest.SchemaVersion = StateSchemaVersion;\n                manifest.RunStamp = runStamp;\n                manifest.WorkingDirectory = directory ?? string.Empty;\n                manifest.StateDirectory = GetRunStateDirectory(directory, runStamp);\n                manifest.UpdatedUtc = DateTime.UtcNow;\n\n                int existingIndex = -1;\n                for (int i = 0; i < manifest.Artifacts.Count; i++)\n                {\n                    var existing = manifest.Artifacts[i];\n                    if (existing.ArtifactId == artifact.ArtifactId && existing.Kind == artifact.Kind)\n                    {\n                        existingIndex = i;\n                        artifact.CreatedUtc = existing.CreatedUtc;\n                        break;\n                    }\n                }\n\n                if (artifact.CreatedUtc == default(DateTime))\n                    artifact.CreatedUtc = DateTime.UtcNow;\n                artifact.UpdatedUtc = DateTime.UtcNow;\n\n                if (existingIndex >= 0)\n                    manifest.Artifacts[existingIndex] = artifact;\n                else\n                    manifest.Artifacts.Add(artifact);\n\n                WriteJsonAtomic(GetManifestPath(directory, runStamp), manifest);\n            }\n        }\n\n        private static void WriteJsonAtomic<T>(string path, T value)\n        {\n            string parent = Path.GetDirectoryName(path);\n            if (!string.IsNullOrEmpty(parent))\n                Directory.CreateDirectory(parent);\n\n            string tmpPath = path + \".tmp\";\n            using (var stream = new FileStream(tmpPath, FileMode.Create, FileAccess.Write, FileShare.None))\n            {\n                JsonSerializer.Serialize(stream, value, StateJsonOptions);\n            }\n\n            File.Move(tmpPath, path, overwrite: true);\n        }\n\n        private static string GetManifestPath(string directory, string runStamp)\n        {\n            return Path.Combine(GetRunStateDirectory(directory, runStamp), $\"spc_{runStamp}.manifest.json\");\n        }\n\n        private static string GetTemperatureCheckpointPath(string directory, string runStamp, double T)\n        {\n            string key = $\"spc_{runStamp}_{TemperatureKey(T)}\";\n            return Path.Combine(GetArtifactDirectory(directory, runStamp, SpcStateArtifactKind.TemperatureCheckpoint), key + \".ckpt\");\n        }\n\n        private static string GetArtifactDirectory(\n            string directory, string runStamp, SpcStateArtifactKind kind)\n        {\n            return Path.Combine(GetRunStateDirectory(directory, runStamp), ArtifactTypeDirectory(kind));\n        }\n\n        private static string GetArtifactPath(\n            string directory, string runStamp, SpcStateArtifactKind kind, string fileName)\n        {\n            return Path.Combine(GetArtifactDirectory(directory, runStamp, kind), fileName);\n        }\n\n        private static string GetArtifactRelativePath(SpcStateArtifactKind kind, string fileName)\n        {\n            return Path.Combine(ArtifactTypeDirectory(kind), fileName);\n        }\n\n        private static string ArtifactTypeDirectory(SpcStateArtifactKind kind)\n        {\n            switch (kind)\n            {\n                case SpcStateArtifactKind.TemperatureCheckpoint:\n                    return \"temperature-checkpoints\";\n                case SpcStateArtifactKind.TemperatureObservation:\n                    return \"temperature-observations\";\n                case SpcStateArtifactKind.HandoffReadiness:\n                    return \"handoff-readiness\";\n                case SpcStateArtifactKind.GmmHandoffState:\n                    return \"gmm-handoff-state\";\n                case SpcStateArtifactKind.GmmCheckpoint:\n                    return \"gmm-checkpoints\";\n                case SpcStateArtifactKind.GmmResult:\n                    return \"gmm-results\";\n                default:\n                    return \"artifacts\";\n            }\n        }\n\n        private static string PayloadType(SpcStateArtifactKind kind)\n        {\n            return ArtifactTypeDirectory(kind);\n        }\n\n        private static string ResolveArtifactPath(\n            string directory, string runStamp, SpcStateArtifact artifact)\n        {\n            string runDirectory = GetRunStateDirectory(directory, runStamp);\n            string relativePath = artifact == null ? string.Empty : artifact.RelativePath;\n            if (!string.IsNullOrEmpty(relativePath))\n            {\n                string typedPath = Path.Combine(runDirectory, relativePath);\n                if (File.Exists(typedPath))\n                    return typedPath;\n\n                string legacyPath = Path.Combine(directory, relativePath);\n                if (File.Exists(legacyPath))\n                    return legacyPath;\n\n                return typedPath;\n            }\n\n            return string.Empty;\n        }\n\n        private static List<string> GetTemperatureCheckpointFiles(\n            string directory, string runStamp, string pattern)\n        {\n            var files = new List<string>();\n\n            string typedDirectory = GetArtifactDirectory(directory, runStamp, SpcStateArtifactKind.TemperatureCheckpoint);\n            if (Directory.Exists(typedDirectory))\n                files.AddRange(Directory.GetFiles(typedDirectory, pattern));\n\n            if (Directory.Exists(directory))\n            {\n                foreach (string legacyFile in Directory.GetFiles(directory, pattern))\n                {\n                    if (!files.Contains(legacyFile))\n                        files.Add(legacyFile);\n                }\n            }\n\n            return files;\n        }\n\n        private static string TemperatureKey(double T)\n        {\n            return BitConverter.DoubleToInt64Bits(T).ToString(\"X16\", CultureInfo.InvariantCulture);\n        }\n\n        private static string MakeArtifactId(string artifactId)\n        {\n            if (string.IsNullOrWhiteSpace(artifactId))\n                return \"artifact\";\n\n            var chars = new char[artifactId.Length];\n            for (int i = 0; i < artifactId.Length; i++)\n            {\n                char ch = artifactId[i];\n                chars[i] = char.IsLetterOrDigit(ch) || ch == '_' || ch == '-'\n                    ? ch\n                    : '_';\n            }\n            return new string(chars);\n        }\n\n        private static int CountDistinctLabels(int[] labels)\n        {\n            if (labels == null || labels.Length == 0)\n                return 0;\n\n            var seen = new HashSet<int>();\n            for (int i = 0; i < labels.Length; i++)\n                seen.Add(labels[i]);\n            return seen.Count;\n        }\n    }\n} |
