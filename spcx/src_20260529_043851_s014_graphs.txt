idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
135 | graphs/GraphLegacyPresets.cs | {3172 251 0.3897 4.0962} | 3255 | using System;\nusing Graphs.Coupling;\nusing Graphs.Distance;\n\nnamespace Graphs;\n\npublic static class GraphLegacyPresets\n{\n    public static GraphCompilerConfig ToCompilerConfig(\n        ProximityRule             rule,\n        int                       k,\n        double                    epsilon,\n        IKernelDescriptor         kernel,\n        bool                      ensureConnected,\n        RefinementKind            refinement         = RefinementKind.Auto,\n        bool?                     lmpRescale         = null,\n        BandwidthStrategy?        bandwidthStrategy  = null,\n        MutualBandwidthSource     mutualBandwidthSource = MutualBandwidthSource.DirectedKth)\n    {\n        var topology = rule switch\n        {\n            ProximityRule.EpsilonBall => new TopologyConfig\n            {\n                Kind = TopologyKind.EpsilonBall,\n                Epsilon = epsilon,\n            },\n            _ => new TopologyConfig { Kind = TopologyKind.Knn, K = k },\n        };\n\n        var filter = new FilterConfig\n        {\n            Kind = rule == ProximityRule.MutualKnn ? FilterKind.MutualKnn : FilterKind.OrRule,\n            MutualBandwidthSource = mutualBandwidthSource,\n        };\n\n        var bandwidth = new BandwidthConfig();\n        if (bandwidthStrategy is BandwidthStrategy strategy)\n            bandwidth = bandwidth with { StrategyOverride = strategy };\n\n        return new GraphCompilerConfig\n        {\n            Topology = topology,\n            Filter = filter,\n            Repair = new RepairConfig\n            {\n                Kind = ensureConnected ? RepairKind.MstMin : RepairKind.NoRepair,\n            },\n            Refinement = new RefinementConfig { Kind = refinement },\n            Scaling = new ScalingConfig { Kernel = kernel, LmpRescale = lmpRescale },\n            Bandwidth = bandwidth,\n        };\n    }\n\n    public static GraphCompilerConfig ToCompilerConfig(\n        ProximityRule         rule,\n        int                   k,\n        double                epsilon,\n        KernelType            kernelType,\n        double                bandwidth,\n        bool                  ensureConnected,\n        RefinementKind        refinement            = RefinementKind.Auto,\n        bool?                 lmpRescale            = null,\n        BandwidthStrategy?    bandwidthStrategy     = null,\n        MutualBandwidthSource mutualBandwidthSource = MutualBandwidthSource.DirectedKth,\n        Mixture?              mixture               = null)\n    {\n        IKernelDescriptor kernel = mixture is not null\n            ? mixture.Value\n            : kernelType switch\n            {\n                KernelType.Gaussian  => new Gaussian(bandwidth),\n                KernelType.Cauchy    => new Cauchy(bandwidth),\n                KernelType.Laplacian => new Laplacian(bandwidth),\n                KernelType.Linear    => new Linear(bandwidth),\n                _ => throw new NotSupportedException($\"KernelType {kernelType} is not supported.\"),\n            };\n\n        return ToCompilerConfig(\n            rule, k, epsilon, kernel, ensureConnected, refinement,\n            lmpRescale, bandwidthStrategy, mutualBandwidthSource);\n    }\n}
136 | graphs/GraphSelection.cs | {1726 122 0.3192 4.2320} | 1776 | using System;\n\nnamespace Graphs\n{\n    public readonly struct NeighborSelection\n    {\n        public NeighborSelection(\n            Neighbor[][] allNeighbors,\n            double[] nearestNeighborDistances,\n            double[] kthNeighborDistances)\n        {\n            AllNeighbors = allNeighbors ?? throw new ArgumentNullException(nameof(allNeighbors));\n            NearestNeighborDistances = nearestNeighborDistances ?? throw new ArgumentNullException(nameof(nearestNeighborDistances));\n            KthNeighborDistances = kthNeighborDistances ?? throw new ArgumentNullException(nameof(kthNeighborDistances));\n\n            int n = allNeighbors.Length;\n            if (nearestNeighborDistances.Length != n)\n            {\n                throw new ArgumentException(\n                    \"NearestNeighborDistances length must match AllNeighbors row count.\",\n                    nameof(nearestNeighborDistances));\n            }\n            if (kthNeighborDistances.Length != n)\n            {\n                throw new ArgumentException(\n                    \"KthNeighborDistances length must match AllNeighbors row count.\",\n                    nameof(kthNeighborDistances));\n            }\n        }\n\n        public Neighbor[][] AllNeighbors { get; }\n\n        public double[] NearestNeighborDistances { get; }\n\n        public double[] KthNeighborDistances { get; }\n\n        public void Deconstruct(\n            out Neighbor[][] allNeighbors,\n            out double[] nearestNeighborDistances,\n            out double[] kthNeighborDistances)\n        {\n            allNeighbors = AllNeighbors;\n            nearestNeighborDistances = NearestNeighborDistances;\n            kthNeighborDistances = KthNeighborDistances;\n        }\n    }\n}
137 | graphs/neighbors/DirectedKnn.cs | {1115 120 0.3390 4.4389} | 1152 | using System;\nusing System.Threading.Tasks;\n\nnamespace Graphs;\n\npublic static class DirectedKnn\n{\n    public static NeighborSelection Select(int n, int k, Func<int, int, double> dist)\n    {\n        if (k <= 0)\n            throw new ArgumentOutOfRangeException(nameof(k), \"K must be positive.\");\n        if (dist is null)\n            throw new ArgumentNullException(nameof(dist));\n\n        var directedNeighbors = new Neighbor[n][];\n        var kthDistances = new double[n];\n        var nnDistances  = new double[n];\n\n        Parallel.For(0, n, i =>\n        {\n            var heap = new BoundedMinHeap(k);\n            for (int j = 0; j < n; j++)\n            {\n                if (i == j) continue;\n                heap.TryAdd(j, dist(i, j));\n            }\n\n            Neighbor[] row = heap.GetSorted();\n            directedNeighbors[i] = row;\n            kthDistances[i] = row.Length > 0 ? row[row.Length - 1].Distance : 0.0;\n            nnDistances[i]  = row.Length > 0 ? row[0].Distance                : 0.0;\n        });\n\n        return new NeighborSelection(directedNeighbors, nnDistances, kthDistances);\n    }\n}
138 | graphs/neighbors/Symmetrization.cs | {4809 460 0.3489 4.3914} | 4950 | using System;\nusing System.Collections.Generic;\n\nnamespace Graphs;\n\npublic static class Symmetrization\n{\n\n    public static NeighborSelection OrUnion(NeighborSelection directed, int n)\n    {\n        if (directed.AllNeighbors is null)\n            throw new ArgumentNullException(nameof(directed));\n        if (n < 0)\n            throw new ArgumentOutOfRangeException(nameof(n));\n        if (directed.AllNeighbors.Length != n)\n            throw new ArgumentException(\"Directed row count must match node count.\", nameof(n));\n\n        var pairMap = BuildOrPairMap(directed.AllNeighbors, n);\n        return BuildSymmetricSelection(pairMap, n, directed.KthNeighborDistances);\n    }\n\n    public static NeighborSelection MutualIntersection(\n        NeighborSelection directed,\n        int n,\n        MutualBandwidthSource bandwidthSource = MutualBandwidthSource.DirectedKth)\n    {\n        if (directed.AllNeighbors is null)\n            throw new ArgumentNullException(nameof(directed));\n        if (n < 0)\n            throw new ArgumentOutOfRangeException(nameof(n));\n        if (directed.AllNeighbors.Length != n)\n            throw new ArgumentException(\"Directed row count must match node count.\", nameof(n));\n\n        var directedNeighbors = directed.AllNeighbors;\n        var directedSets = new HashSet<int>[n];\n        for (int i = 0; i < n; i++)\n        {\n            var set = new HashSet<int>(directedNeighbors[i].Length);\n            foreach (var nb in directedNeighbors[i])\n                set.Add(nb.Index);\n            directedSets[i] = set;\n        }\n\n        var pairMap = new Dictionary<long, double>();\n        bool wantsMutualKth = bandwidthSource == MutualBandwidthSource.MutualKth;\n        double[]? mutualRowMax = wantsMutualKth ? new double[n] : null;\n        bool[]? mutualRowHasNeighbor = wantsMutualKth ? new bool[n] : null;\n\n        for (int i = 0; i < n; i++)\n        {\n            foreach (var nb in directedNeighbors[i])\n            {\n                int j = nb.Index;\n                if (!directedSets[j].Contains(i)) continue;\n\n                if (mutualRowMax is not null)\n                {\n                    if (!mutualRowHasNeighbor![i] || nb.Distance > mutualRowMax[i])\n                        mutualRowMax[i] = nb.Distance;\n                    mutualRowHasNeighbor[i] = true;\n                }\n\n                int lo = Math.Min(i, j);\n                int hi = Math.Max(i, j);\n                long key = (long)lo * n + hi;\n                if (!pairMap.TryGetValue(key, out double existing) || nb.Distance < existing)\n                    pairMap[key] = nb.Distance;\n            }\n        }\n\n        var resolvedKth = new double[n];\n        if (mutualRowMax is not null)\n        {\n            for (int i = 0; i < n; i++)\n            {\n                resolvedKth[i] = mutualRowHasNeighbor![i]\n                    ? mutualRowMax[i]\n                    : double.PositiveInfinity;\n            }\n        }\n        else\n        {\n            Array.Copy(directed.KthNeighborDistances, resolvedKth, n);\n        }\n\n        return BuildSymmetricSelection(pairMap, n, resolvedKth);\n    }\n\n    private static Dictionary<long, double> BuildOrPairMap(Neighbor[][] directedNeighbors, int n)\n    {\n        var pairMap = new Dictionary<long, double>();\n        for (int i = 0; i < n; i++)\n        {\n            foreach (var nb in directedNeighbors[i])\n            {\n                int lo = Math.Min(i, nb.Index);\n                int hi = Math.Max(i, nb.Index);\n                long key = (long)lo * n + hi;\n                if (!pairMap.TryGetValue(key, out double existing) || nb.Distance < existing)\n                    pairMap[key] = nb.Distance;\n            }\n        }\n\n        return pairMap;\n    }\n\n    private static NeighborSelection BuildSymmetricSelection(\n        Dictionary<long, double> pairMap,\n        int n,\n        double[] kthDistances)\n    {\n        var lists = new List<Neighbor>[n];\n        for (int i = 0; i < n; i++)\n            lists[i] = new List<Neighbor>();\n\n        foreach (var kvp in pairMap)\n        {\n            int lo = (int)(kvp.Key / n);\n            int hi = (int)(kvp.Key % n);\n            double d = kvp.Value;\n            lists[lo].Add(new Neighbor { Index = hi, Distance = d });\n            lists[hi].Add(new Neighbor { Index = lo, Distance = d });\n        }\n\n        var allNeighbors = new Neighbor[n][];\n        var nnDistances  = new double[n];\n        for (int i = 0; i < n; i++)\n        {\n            lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n            allNeighbors[i] = lists[i].ToArray();\n            nnDistances[i] = allNeighbors[i].Length > 0\n                ? allNeighbors[i][0].Distance\n                : double.PositiveInfinity;\n        }\n\n        return new NeighborSelection(allNeighbors, nnDistances, kthDistances);\n    }\n}
139 | graphs/pipeline/filters/MutualKnnFilter.cs | {514 40 0.1537 4.7132} | 529 | using System;\n\nnamespace Graphs.Pipeline.Filters;\n\npublic sealed class MutualKnnFilter : ITopologyFilter\n{\n    private readonly MutualBandwidthSource _bandwidthSource;\n\n    public MutualKnnFilter(MutualBandwidthSource bandwidthSource = MutualBandwidthSource.DirectedKth)\n    {\n        _bandwidthSource = bandwidthSource;\n    }\n\n    public NeighborSelection Filter(NeighborSelection directed, int n, Func<int, int, double> pairDistance) =>\n        Symmetrization.MutualIntersection(directed, n, _bandwidthSource);\n}
140 | graphs/pipeline/filters/PassThroughFilter.cs | {781 75 0.3201 4.3853} | 809 | using System;\n\nnamespace Graphs.Pipeline.Filters;\n\npublic sealed class PassThroughFilter : ITopologyFilter\n{\n    public NeighborSelection Filter(NeighborSelection directed, int n, Func<int, int, double> pairDistance)\n    {\n        if (IsAlreadySymmetric(directed.AllNeighbors))\n            return directed;\n\n        return Symmetrization.OrUnion(directed, n);\n    }\n\n    private static bool IsAlreadySymmetric(Neighbor[][] neighbors)\n    {\n        for (int i = 0; i < neighbors.Length; i++)\n        {\n            if (neighbors[i].Length == 0) continue;\n            int j = neighbors[i][0].Index;\n            foreach (var nb in neighbors[j])\n            {\n                if (nb.Index == i) return true;\n            }\n            return false;\n        }\n        return true;\n    }\n}
141 | graphs/pipeline/generators/EpsilonBallGenerator.cs | {534 49 0.2022 4.6759} | 554 | using System;\nusing Graphs.Proximity;\n\nnamespace Graphs.Pipeline.Generators;\n\npublic sealed class EpsilonBallGenerator : ITopologyGenerator\n{\n    private readonly double _epsilon;\n\n    public EpsilonBallGenerator(double epsilon)\n    {\n        if (epsilon <= 0.0)\n            throw new ArgumentOutOfRangeException(nameof(epsilon), \"Epsilon must be positive.\");\n        _epsilon = epsilon;\n    }\n\n    public NeighborSelection Generate(int n, Func<int, int, double> dist)\n        => ProximityGraph.SelectEpsilonBall(n, _epsilon, dist);\n}
142 | graphs/pipeline/generators/KnnGenerator.cs | {424 47 0.2500 4.6562} | 443 | using System;\n\nnamespace Graphs.Pipeline.Generators;\n\npublic sealed class KnnGenerator : ITopologyGenerator\n{\n    private readonly int _k;\n\n    public KnnGenerator(int k)\n    {\n        if (k <= 0)\n            throw new ArgumentOutOfRangeException(nameof(k), \"K must be positive.\");\n        _k = k;\n    }\n\n    public NeighborSelection Generate(int n, Func<int, int, double> dist) =>\n        DirectedKnn.Select(n, _k, dist);\n}
143 | graphs/pipeline/IEdgeScaler.cs | {388 34 0.2139 4.6623} | 410 | using Graphs.Coupling;\nusing Graphs.Primitives;\n\nnamespace Graphs.Pipeline;\n\npublic interface IEdgeScaler\n{\n\n    ScalerResult Scale(NeighborSelection refined, int n);\n}\n\npublic readonly record struct ScalerResult(\n    CsrGraph          Graph,\n    double?           SingleBandwidth,\n    MixtureBandwidth? MixtureBandwidth);\n\npublic enum BandwidthTiming\n{\n\n    PreRepair,\n\n    PostRepair,\n}
144 | graphs/pipeline/IMetricRefiner.cs | {126 12 0.1349 4.4790} | 132 | namespace Graphs.Pipeline;\n\npublic interface IMetricRefiner\n{\n\n    NeighborSelection Refine(NeighborSelection input, int n);\n}
145 | graphs/pipeline/ITopologyFilter.cs | {182 18 0.1319 4.6524} | 190 | using System;\n\nnamespace Graphs.Pipeline;\n\npublic interface ITopologyFilter\n{\n\n    NeighborSelection Filter(NeighborSelection directed, int n, Func<int, int, double> pairDistance);\n}
146 | graphs/pipeline/ITopologyGenerator.cs | {151 16 0.1457 4.6570} | 159 | using System;\n\nnamespace Graphs.Pipeline;\n\npublic interface ITopologyGenerator\n{\n\n    NeighborSelection Generate(int n, Func<int, int, double> dist);\n}
147 | graphs/pipeline/ITopologyRepair.cs | {179 18 0.1341 4.6657} | 187 | using System;\n\nnamespace Graphs.Pipeline;\n\npublic interface ITopologyRepair\n{\n\n    NeighborSelection Repair(NeighborSelection input, int n, Func<int, int, double> pairDistance);\n}
148 | graphs/pipeline/refinement/EuclideanRefiner.cs | {174 18 0.1264 4.5681} | 179 | namespace Graphs.Pipeline.Refinement;\n\npublic sealed class EuclideanRefiner : IMetricRefiner\n{\n    public NeighborSelection Refine(NeighborSelection input, int n) => input;\n}
149 | graphs/pipeline/refinement/PathNeighborRefiner.cs | {6675 531 0.4132 4.1047} | 6870 | #nullable enable\nusing System;\nusing System.Buffers;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\nusing Graphs.Primitives;\n\nnamespace Graphs.Pipeline.Refinement;\n\npublic sealed class PathNeighborRefiner : IMetricRefiner\n{\n    private readonly double? _maxDistance;\n\n    public PathNeighborRefiner(double? maxDistance = null)\n    {\n        if (maxDistance.HasValue && maxDistance.Value <= 0.0)\n            throw new ArgumentOutOfRangeException(nameof(maxDistance), \"MaxDistance must be positive when specified.\");\n\n        _maxDistance = maxDistance;\n    }\n\n    public NeighborSelection Refine(NeighborSelection input, int n)\n    {\n        if (input.AllNeighbors is null)\n            throw new ArgumentNullException(nameof(input));\n        if (n < 0)\n            throw new ArgumentOutOfRangeException(nameof(n));\n        if (input.AllNeighbors.Length != n)\n            throw new ArgumentException(\"NeighborSelection row count must match node count.\", nameof(n));\n        if (n == 0)\n            return input;\n\n        CsrGraph graph = BuildWeightedGraph(input, n);\n        var refinedRows = new Neighbor[n][];\n        var nearestSample = new double[n];\n\n        Parallel.For(0, n,\n            () => new ThreadScratch(n),\n            (i, _, scratch) =>\n            {\n                var row = input.AllNeighbors[i];\n                int rowLength = row.Length;\n                if (rowLength == 0)\n                {\n                    refinedRows[i] = Array.Empty<Neighbor>();\n                    nearestSample[i] = double.PositiveInfinity;\n                    return scratch;\n                }\n\n                var refinedRow = new Neighbor[rowLength];\n                row.CopyTo(refinedRow, 0);\n                refinedRows[i] = refinedRow;\n\n                scratch.Reset(n, _maxDistance);\n                foreach (var neighbor in refinedRow)\n                    scratch.TargetMask[neighbor.Index] = true;\n\n                int pendingTargets = rowLength;\n                scratch.Distances[i] = 0.0;\n                scratch.Hops[i] = 0;\n                scratch.Queue.Enqueue(i, 0.0);\n\n                while (scratch.Queue.TryDequeue(out int u, out double currentDistance))\n                {\n                    if (currentDistance > scratch.Distances[u])\n                        continue;\n\nif (currentDistance > scratch.MaxDistance)\n                        continue;\n\n                    if (scratch.TargetMask[u])\n                    {\n                        bool updated = false;\n                        for (int index = 0; index < rowLength; index++)\n                        {\n                            if (refinedRow[index].Index != u)\n                                continue;\n\n                            double original = refinedRow[index].Distance;\n                            double refinedDistance = Math.Min(original, currentDistance);\n                            refinedRow[index].Distance = refinedDistance;\n                            updated = true;\n                            break;\n                        }\n\n                        if (updated)\n                        {\n                            pendingTargets--;\n                            if (pendingTargets == 0)\n                                break;\n                        }\n                    }\n\n                    int outgoing = graph.RowPointers[u];\n                    int end = graph.RowPointers[u + 1];\n                    int nextHop = scratch.Hops[u] + 1;\n                    for (int e = outgoing; e < end; e++)\n                    {\n                        int v = graph.Targets[e];\n                        double weight = graph.Weights[e];\n                        if (weight < 0.0) weight = 0.0;\n\n                        double altDistance = currentDistance + weight;\n                        if (altDistance >= scratch.Distances[v] || altDistance >= scratch.MaxDistance)\n                            continue;\n\n                        scratch.Distances[v] = altDistance;\n                        scratch.Hops[v] = nextHop;\n                        scratch.Queue.Enqueue(v, altDistance);\n                    }\n                }\n\n                double best = double.PositiveInfinity;\n                for (int j = 0; j < rowLength; j++)\n                {\n                    double d = refinedRow[j].Distance;\n                    if (d < best) best = d;\n                }\n                nearestSample[i] = best;\n                return scratch;\n            },\n            scratch => scratch.Dispose());\n\n        return new NeighborSelection(refinedRows, nearestSample, input.KthNeighborDistances);\n    }\n\n    private static CsrGraph BuildWeightedGraph(NeighborSelection selection, int n)\n    {\n        int edgeCount = 0;\n        for (int i = 0; i < n; i++)\n            edgeCount += selection.AllNeighbors[i].Length;\n\n        var rowPointers = new int[n + 1];\n        var targets = new int[edgeCount];\n        var weights = new double[edgeCount];\n\n        int pos = 0;\n        for (int i = 0; i < n; i++)\n        {\n            rowPointers[i] = pos;\n            foreach (var neighbor in selection.AllNeighbors[i])\n            {\n                targets[pos] = neighbor.Index;\n                weights[pos] = neighbor.Distance < 0.0 ? 0.0 : neighbor.Distance;\n                pos++;\n            }\n        }\n        rowPointers[n] = pos;\n\n        return new CsrGraph\n        {\n            NodeCount = n,\n            RowPointers = rowPointers,\n            Targets = targets,\n            Weights = weights,\n        };\n    }\n\n    private sealed class ThreadScratch : IDisposable\n    {\n        public readonly double[] Distances;\n        public readonly int[] Hops;\n        public readonly bool[] TargetMask;\n        public readonly PriorityQueue<int, double> Queue;\n        public double MaxDistance;\n\n        public ThreadScratch(int n)\n        {\n            Distances = ArrayPool<double>.Shared.Rent(n);\n            Hops = ArrayPool<int>.Shared.Rent(n);\n            TargetMask = ArrayPool<bool>.Shared.Rent(n);\n            Queue = new PriorityQueue<int, double>();\n            MaxDistance = double.PositiveInfinity;\n        }\n\n        public void Reset(int n, double? maxDistance)\n        {\n            MaxDistance = maxDistance ?? double.PositiveInfinity;\n            Array.Fill(Distances, double.PositiveInfinity, 0, n);\n            Array.Fill(Hops, int.MaxValue, 0, n);\n            Array.Fill(TargetMask, false, 0, n);\n            Queue.Clear();\n        }\n\n        public void Dispose()\n        {\n            ArrayPool<double>.Shared.Return(Distances);\n            ArrayPool<int>.Shared.Return(Hops);\n            ArrayPool<bool>.Shared.Return(TargetMask);\n        }\n    }\n}
150 | graphs/pipeline/repair/MstMinRepair.cs | {304 28 0.1349 4.7321} | 313 | using System;\nusing Graphs.Proximity;\n\nnamespace Graphs.Pipeline.Repair;\n\npublic sealed class MstMinRepair : ITopologyRepair\n{\n    public NeighborSelection Repair(NeighborSelection input, int n, Func<int, int, double> pairDistance)\n        => ConnectivityRepair.EnsureConnected(input, n, pairDistance);\n}
151 | graphs/pipeline/repair/NoRepair.cs | {223 24 0.1659 4.6253} | 231 | using System;\n\nnamespace Graphs.Pipeline.Repair;\n\npublic sealed class NoRepair : ITopologyRepair\n{\n    public NeighborSelection Repair(NeighborSelection input, int n, Func<int, int, double> pairDistance)\n        => input;\n}
152 | graphs/pipeline/scalers/AutoLmpScaler.cs | {2482 219 0.3030 4.5245} | 2571 | using System;\nusing Graphs;\nusing Graphs.Diagnostics;\nusing Graphs.Primitives;\nusing Graphs.Proximity;\n\nnamespace Graphs.Pipeline.Scalers;\n\ninternal sealed class AutoLmpScaler : IEdgeScaler\n{\n    private readonly IEdgeScaler _innerScaler;\n    private readonly DiagnosticsLog _log;\n    private readonly double _nearZeroEdgeRatioThreshold;\n\n    public AutoLmpScaler(\n        IEdgeScaler innerScaler,\n        DiagnosticsLog log,\n        double nearZeroEdgeRatioThreshold = 0.05)\n    {\n        _innerScaler = innerScaler ?? throw new ArgumentNullException(nameof(innerScaler));\n        _log = log ?? throw new ArgumentNullException(nameof(log));\n        if (nearZeroEdgeRatioThreshold < 0.0 || nearZeroEdgeRatioThreshold > 1.0)\n            throw new ArgumentOutOfRangeException(nameof(nearZeroEdgeRatioThreshold));\n        _nearZeroEdgeRatioThreshold = nearZeroEdgeRatioThreshold;\n    }\n\n    public ScalerResult Scale(NeighborSelection refined, int n)\n    {\n        ScalerResult tentative = _innerScaler.Scale(refined, n);\n        double ratio = ComputeNearZeroRatio(tentative.Graph);\n\n        if (ratio > _nearZeroEdgeRatioThreshold)\n        {\n            _log.Warning(\n                \"Scaling\",\n                $\"Auto-LMP enabled because tentative global scaling produced a near-zero edge ratio \" +\n                $\"{ratio:P1}, which exceeds the threshold {_nearZeroEdgeRatioThreshold:P1}.\");\n\n            CsrGraph rescaled = LocalMutualProximity.ApplyLocalScaling(\n                tentative.Graph,\n                weightsAreCouplings: true);\n            return tentative with { Graph = rescaled };\n        }\n\n        if (ratio > _nearZeroEdgeRatioThreshold * 0.5)\n        {\n            _log.Warning(\n                \"Scaling\",\n                $\"Tentative global scaling produced {ratio:P1} near-zero edges; graph may still be numerically fragile.\");\n        }\n        else\n        {\n            _log.Info(\n                \"Scaling\",\n                $\"Tentative near-zero edge ratio is {ratio:F4}; LMP is not required.\");\n        }\n\n        return tentative;\n    }\n\n    private static double ComputeNearZeroRatio(CsrGraph graph)\n    {\n        const double NearZero = 1e-8;\n        int count = 0;\n        double[] weights = graph.Weights;\n        for (int i = 0; i < weights.Length; i++)\n        {\n            if (weights[i] < NearZero)\n            {\n                count++;\n            }\n        }\n\n        return weights.Length == 0 ? 0.0 : (double)count / weights.Length;\n    }\n}
153 | graphs/pipeline/scalers/GlobalBandwidthScaler.cs | {6183 522 0.2908 4.6079} | 6344 | using System;\nusing System.Collections.Generic;\nusing Graphs;\nusing Graphs.Coupling;\nusing Graphs.Distance;\nusing Graphs.Primitives;\n\nnamespace Graphs.Pipeline.Scalers;\n\npublic sealed class GlobalBandwidthScaler : IEdgeScaler\n{\n    private readonly KernelType        _kernel;\n    private readonly double            _explicitBandwidth;\n    private readonly BandwidthStrategy _strategy;\n    private readonly BandwidthTiming   _timing;\n    private readonly MixtureWeights?   _mixtureWeights;\n    private readonly MixtureBandwidth? _explicitMixtureBandwidth;\n\n    public GlobalBandwidthScaler(\n        KernelType        kernel,\n        double            bandwidth = 0.0,\n        BandwidthStrategy strategy  = BandwidthStrategy.MadConsistencyFactor,\n        BandwidthTiming   timing    = BandwidthTiming.PreRepair)\n    {\n        _kernel = kernel;\n        _explicitBandwidth = bandwidth;\n        _strategy = strategy;\n        _timing = timing;\n        _mixtureWeights = null;\n        _explicitMixtureBandwidth = null;\n    }\n\n    public GlobalBandwidthScaler(\n        MixtureWeights    mixtureWeights,\n        MixtureBandwidth? mixtureBandwidth = null,\n        BandwidthStrategy strategy         = BandwidthStrategy.MadConsistencyFactor,\n        BandwidthTiming   timing           = BandwidthTiming.PreRepair)\n    {\n        _kernel = default;                       // unused on the mixture path\n        _explicitBandwidth = 0.0;\n        _strategy = strategy;\n        _timing = timing;\n        _mixtureWeights = mixtureWeights;\n        _explicitMixtureBandwidth = mixtureBandwidth;\n    }\n\n    public ScalerResult Scale(NeighborSelection refined, int n)\n    {\n        double[] bandwidthSample = _timing switch\n        {\n            BandwidthTiming.PreRepair  => refined.KthNeighborDistances,\n            BandwidthTiming.PostRepair => DerivePostRepairKthSample(refined),\n            _ => throw new ArgumentOutOfRangeException(),\n        };\n\n        return _mixtureWeights is { } weights\n            ? ScaleMixture(refined, n, weights, bandwidthSample)\n            : ScaleSingle(refined, n, bandwidthSample);\n    }\n\n    private ScalerResult ScaleSingle(NeighborSelection refined, int n, double[] bandwidthSample)\n    {\n        double delta = _explicitBandwidth > 0.0\n            ? _explicitBandwidth\n            : EstimateSingle(bandwidthSample);\n\n        Func<double, double, double> kernelFn = _kernel switch\n        {\n            KernelType.Gaussian  => GaussianKernel.Evaluate,\n            KernelType.Cauchy    => CauchyKernel.Evaluate,\n            KernelType.Laplacian => LaplacianKernel.Evaluate,\n            KernelType.Linear    => LinearKernel.Evaluate,\n            _ => throw new NotSupportedException($\"KernelType {_kernel} not implemented.\"),\n        };\n\n        Neighbor[][] allNeighbors = refined.AllNeighbors;\n        var edges = new List<Edge>(n * (allNeighbors.Length > 0 ? allNeighbors[0].Length : 0));\n\n        for (int i = 0; i < n; i++)\n        {\n            foreach (var nb in allNeighbors[i])\n            {\n                if (i > nb.Index) continue;          // store each undirected edge once\n                double coupling = kernelFn(nb.Distance, delta);\n                if (coupling > 1e-10)\n                    edges.Add(new Edge(i, nb.Index, coupling));\n            }\n        }\n\n        return new ScalerResult(\n            Graph:            CsrGraph.FromEdges(edges.ToArray(), n),\n            SingleBandwidth:  delta,\n            MixtureBandwidth: null);\n    }\n\n    private ScalerResult ScaleMixture(\n        NeighborSelection refined, int n, MixtureWeights weights, double[] bandwidthSample)\n    {\n        MixtureBandwidth resolved = _explicitMixtureBandwidth\n            ?? EstimateMixture(bandwidthSample);\n\n        Neighbor[][] allNeighbors = refined.AllNeighbors;\n        var edges = new List<Edge>(n * (allNeighbors.Length > 0 ? allNeighbors[0].Length : 0));\n\n        for (int i = 0; i < n; i++)\n        {\n            foreach (var nb in allNeighbors[i])\n            {\n                if (i > nb.Index) continue;\n                double coupling = MixtureKernel.Evaluate(nb.Distance, resolved, weights);\n                if (coupling > 1e-10)\n                    edges.Add(new Edge(i, nb.Index, coupling));\n            }\n        }\n\n        return new ScalerResult(\n            Graph:            CsrGraph.FromEdges(edges.ToArray(), n),\n            SingleBandwidth:  null,\n            MixtureBandwidth: resolved);\n    }\n\n    private double EstimateSingle(double[] sample)\n    {\n        ReadOnlySpan<double> span = sample;\n        Span<double> scratch = stackalloc double[sample.Length <= 512 ? sample.Length : 0];\n        double[]? scratchArr = scratch.Length == 0 ? new double[sample.Length] : null;\n        Span<double> effective = scratch.Length > 0 ? scratch : scratchArr!.AsSpan();\n        return _kernel switch\n        {\n            KernelType.Gaussian  => BandwidthEstimation.ForGaussian(span, effective, _strategy),\n            KernelType.Laplacian => BandwidthEstimation.ForLaplacian(span, effective, _strategy),\n            KernelType.Cauchy    => BandwidthEstimation.ForCauchy(span, effective, _strategy),\n            KernelType.Linear    => BandwidthEstimation.ForLinear(span),\n            _ => throw new NotSupportedException($\"KernelType {_kernel} not implemented.\"),\n        };\n    }\n\n    private MixtureBandwidth EstimateMixture(double[] sample)\n    {\n        ReadOnlySpan<double> span = sample;\n        Span<double> scratch = stackalloc double[sample.Length <= 512 ? sample.Length : 0];\n        double[]? scratchArr = scratch.Length == 0 ? new double[sample.Length] : null;\n        Span<double> effective = scratch.Length > 0 ? scratch : scratchArr!.AsSpan();\n        return BandwidthEstimation.ForMixture(span, effective, _strategy);\n    }\n\n    private static double[] DerivePostRepairKthSample(NeighborSelection refined)\n    {\n        Neighbor[][] rows = refined.AllNeighbors;\n        var sample = new List<double>(rows.Length);\n        for (int i = 0; i < rows.Length; i++)\n        {\n            if (rows[i].Length == 0) continue;\n            sample.Add(rows[i][rows[i].Length - 1].Distance);\n        }\n        return sample.ToArray();\n    }\n}
