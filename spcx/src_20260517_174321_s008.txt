idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
32 | estimators/manifolds/ManifoldMedian.cs | {3137 249 0.3105 4.4080} | 3430 | using System;\nusing System.Buffers;\nusing Maths.Geometry;\nusing Maths.Optimization;\n\nnamespace Estimators.Manifolds\n// ============================================================================\n// Estimators/Manifolds/ManifoldMedian.cs\n// ============================================================================\n// Hot-path geometric median via hybrid Weiszfeld / projected subgradient IRLS.\n// Convergence guaranteed for non-degenerate configurations; singularity at\n// coincident data points handled via OptimalityCheck or Regularise policy\n// (configured in IrlsOptions).\n//\n// Downstream callers (GMM init, SPC handoff, etc.) call this directly.\n// Zero heap allocation on the hot path — all scratch via ArrayPool.\n// ============================================================================\n\n{\n    public static class GeometricMedian\n    {\n        // ── Location only ─────────────────────────────────────────────────────\n\n        /// <summary>\n        /// Computes the geometric median in place.\n        /// <paramref name=\"destination\"/> is both the warm-start and the output.\n        /// </summary>\n        public static void Compute<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            ReadOnlySpan<double>   weights,\n            double[]               destination,\n            IrlsOptions            opts = default)\n            where TManifold : struct, IRiemannianManifold\n        {\n            Irls.Solve<TManifold, L1Loss>(\n                manifold, data, weights, destination, opts,\n                finalIrlsWeights: default);\n        }\n\n        // ── (Location, Scatter) ───────────────────────────────────────────────\n\n        /// <summary>\n        /// Computes the geometric median and the Weiszfeld (L1-weighted) scatter\n        /// in a single pass.\n        /// <para>\n        /// <paramref name=\"scatterDestination\"/> must be a caller-supplied\n        /// row-major D×D flat buffer of length D².\n        /// </para>\n        /// </summary>\n        public static void ComputeWithScatter<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            ReadOnlySpan<double>   weights,\n            double[]               locationDestination,\n            Span<double>           scatterDestination,\n            IrlsOptions            opts = default,\n            double                 consistencyFactor = 1.0)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int      n       = data.Length;\n            double[] rentedW = ArrayPool<double>.Shared.Rent(n);\n            try\n            {\n                Span<double> finalW = rentedW.AsSpan(0, n);\n                Irls.Solve<TManifold, L1Loss>(\n                    manifold, data, weights, locationDestination, opts, finalW);\n                WeiszfeldScatter.Accumulate(\n                    manifold, data, locationDestination, finalW,\n                    scatterDestination, consistencyFactor);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedW);\n            }\n        }\n    }\n} |
33 | estimators/manifolds/WeiszfeldScatter.cs | {3310 291 0.3329 4.3399} | 3398 | // ============================================================================\n// Estimators/Manifolds/WeiszfeldScatter.cs\n// ============================================================================\n// Robust scatter companion to GeometricMedian.\n// Weights are the converged L1 IRLS weights w_i = 1 / d(mu, x_i), so points\n// far from the median contribute less to the scatter estimate — consistent with\n// the median's breakdown-point guarantees.\n//\n// Sigma = (cD / sum_i w_i) * sum_i w_i * v_i * v_i^T\n// where v_i = log_mu(x_i) and w_i are the L1 IRLS weights at convergence.\n//\n// Can also be called standalone with a fixed location estimate.\n// ============================================================================\nusing System;\nusing System.Buffers;\nusing Maths.Geometry;\nusing Estimators.Shared;\n\nnamespace Estimators.Manifolds\n{\n    public static class WeiszfeldScatter\n    {\n        /// <summary>\n        /// Accumulates scatter from converged IRLS weights into\n        /// <paramref name=\"scatterDst\"/> (row-major D×D, length D²).\n        /// Called internally by <see cref=\"GeometricMedian.ComputeWithScatter\"/>.\n        /// </summary>\n        internal static void Accumulate<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            double[]               location,\n            ReadOnlySpan<double>   finalIrlsWeights,\n            Span<double>           scatterDst,\n            double                 consistencyFactor)\n            where TManifold : struct, IRiemannianManifold\n            => ScatterAccumulator.Accumulate(\n                manifold, data, location,\n                finalIrlsWeights, scatterDst, consistencyFactor);\n\n        /// <summary>\n        /// Standalone scatter: computes robust Weiszfeld scatter around a fixed\n        /// <paramref name=\"location\"/> without re-running median estimation.\n        /// Runs one pass computing L1 weights from distances, then accumulates.\n        /// </summary>\n        public static void Compute<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            double[]               location,\n            Span<double>           scatterDestination,\n            ReadOnlySpan<double>   outerWeights = default,\n            double                 epsilon = 1e-10,\n            double                 consistencyFactor = 1.0)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int      n       = data.Length;\n            double[] rentedW = ArrayPool<double>.Shared.Rent(n);\n            try\n            {\n                Span<double>         w       = rentedW.AsSpan(0, n);\n                ReadOnlySpan<double> locSpan = location;\n\n                for (int i = 0; i < n; i++)\n                {\n                    double r  = manifold.Distance(locSpan, data[i]);\n                    double ow = outerWeights.IsEmpty ? 1.0 : outerWeights[i];\n                    w[i] = r > epsilon ? ow / r : 0.0;\n                }\n\n                ScatterAccumulator.Accumulate(\n                    manifold, data, location, w,\n                    scatterDestination, consistencyFactor);\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(rentedW);\n            }\n        }\n    }\n} |
34 | estimators/shared/ConsistencyFactors.cs | {2479 311 0.2501 4.6252} | 2585 | // ============================================================================\n// Estimators/Shared/ConsistencyFactors.cs\n// ============================================================================\n// Calibration constants c_D for scatter estimators under reference distributions.\n// Independent of any specific estimator — pass the result as consistencyFactor\n// to GeometricMean.ComputeWithScatter or GeometricMedian.ComputeWithScatter.\n//\n// Usage:\n//   double cD = ConsistencyFactors.Gaussian(dim);\n//   GeometricMedian.ComputeWithScatter(manifold, data, weights,\n//       loc, scatter, opts, consistencyFactor: cD);\n//\n// Derivation (both cases):\n//   The scatter accumulator computes Σ̂ = (c_D / Σᵢwᵢ) · Σᵢ wᵢ vᵢvᵢᵀ.\n//   For consistency E[Σ̂] → I we need c_D = D · E[1/r] / E[r]\n//   where r = ||v|| is drawn from the distance distribution of the reference.\n// ============================================================================\nusing System;\n\nnamespace Estimators.Shared\n{\n    public static class ConsistencyFactors\n    {\n        /// <summary>\n        /// Consistency factor c_D for the Weiszfeld scatter under a Gaussian\n        /// reference distribution in D dimensions.\n        /// <para>\n        /// Derivation: r ~ χ_D, so E[r] = √2 · Γ((D+1)/2) / Γ(D/2) and\n        /// E[1/r] = Γ((D-1)/2) / (√2 · Γ(D/2)). The Γ terms cancel, giving\n        /// c_D = D · E[1/r] / E[r] = D/(D−1).\n        /// </para>\n        /// D=1 is undefined for this estimator; use MAD for the 1D case.\n        /// </summary>\n        public static double Gaussian(int dim)\n        {\n            if (dim <= 1) throw new ArgumentOutOfRangeException(nameof(dim),\n                \"Weiszfeld scatter is undefined in 1D; use MAD for the scalar case.\");\n            return (double)dim / (dim - 1);\n        }\n\n        /// <summary>\n        /// Consistency factor c_D for the Weiszfeld scatter under a spherical\n        /// Laplace reference distribution in D dimensions.\n        /// <para>\n        /// Derivation: r ~ Gamma(D,1), so E[r] = D and E[1/r] = 1/(D−1).\n        /// c_D = D · E[1/r] / E[r] = 1/(D−1).\n        /// </para>\n        /// D=1 is undefined; use MAD for the scalar case.\n        /// </summary>\n        public static double Laplace(int dim)\n        {\n            if (dim <= 1) throw new ArgumentOutOfRangeException(nameof(dim),\n                \"Weiszfeld scatter is undefined in 1D; use MAD for the scalar case.\");\n            return 1.0 / (dim - 1);\n        }\n    }\n} |
35 | estimators/shared/ScatterAccumulator.cs | {2305 205 0.3879 4.0966} | 2368 | // ============================================================================\n// Estimators/Shared/ScatterAccumulator.cs   (internal primitive)\n// ============================================================================\n// Single implementation of the weighted outer-product scatter accumulation\n// used by both KarcherScatter and WeiszfeldScatter.\n//\n// Sigma = (consistencyFactor / sum_i w_i) * sum_i w_i * v_i * v_i^T\n// where v_i = log_mu(x_i).\n//\n// scatterDst is a caller-supplied row-major D×D flat buffer (length D²).\n// ============================================================================\nusing System;\nusing System.Buffers;\nusing Maths.Geometry;\n\nnamespace Estimators.Shared\n{\n    internal static class ScatterAccumulator\n    {\n        public static void Accumulate<TManifold>(\n            TManifold              manifold,\n            ReadOnlySpan<double[]> data,\n            double[]               location,\n            ReadOnlySpan<double>   weights,\n            Span<double>           scatterDst,\n            double                 consistencyFactor)\n            where TManifold : struct, IRiemannianManifold\n        {\n            int      dim    = manifold.Dimension;\n            double[] logArr = ArrayPool<double>.Shared.Rent(dim);\n            try\n            {\n                Span<double>         v       = logArr.AsSpan(0, dim);\n                ReadOnlySpan<double> locSpan = location;\n                scatterDst.Clear();\n                double sumW = 0;\n\n                for (int i = 0; i < data.Length; i++)\n                {\n                    double w = weights[i];\n                    if (w == 0) continue;\n                    sumW += w;\n                    manifold.LogMap(locSpan, data[i], v);\n                    for (int r = 0; r < dim; r++)\n                        for (int c = 0; c < dim; c++)\n                            scatterDst[r * dim + c] += w * v[r] * v[c];\n                }\n\n                if (sumW > 0)\n                {\n                    double scale = consistencyFactor / sumW;\n                    for (int k = 0; k < scatterDst.Length; k++)\n                        scatterDst[k] *= scale;\n                }\n            }\n            finally\n            {\n                ArrayPool<double>.Shared.Return(logArr);\n            }\n        }\n    }\n} |
36 | ETL/adapters/half_plane.cs | {1122 138 0.4296 4.1186} | 1170 | namespace ETL.Adapters\n{\n    public sealed class HalfPlaneAdapter : IManifoldAdapter\n    {\n        public ProbeContract Embed(double[] raw, int n, int d, VizMetric metric, object? _ = null)\n        {\n            if (d < 2) throw new ArgumentException(\"HalfPlane requires ≥2D input.\");\n\n            double[] adapted = new double[n * 2];\n            for (int i = 0; i < n; i++)\n            {\n                int src = i * d;\n                int dst = i * 2;\n                adapted[dst] = raw[src];                    // μ ← first coord (or PC1, etc.)\n                double y = d > 1 ? raw[src + 1] : 0.0;\n                adapted[dst + 1] = y > 20 ? y : Math.Log(1.0 + Math.Exp(y)); // softplus σ\n            }\n\n            return new ProbeContract\n            {\n                Metric = metric,\n                AmbientDim = d,\n                AdaptedDim = 2,\n                Representation = \"GaussianHalfPlane\",\n                AmbientFeatures = raw,\n                AdaptedFeatures = adapted,\n                Notes = \"Projection: x1→μ, softplus(x2)→σ. Explicit dimensionality reduction.\"\n            };\n        }\n    }\n} |
37 | ETL/adapters/hyperbolic.cs | {1515 152 0.3927 4.1752} | 1564 | using System;\nusing Manifolds.Transforms;\n\nnamespace ETL.Adapters\n{\n    public sealed class PoincareAdapter : IManifoldAdapter\n    {\n        public ProbeContract Embed(double[] raw, int n, int d, ProbeMetric metric, object? probeParam = null)\n        {\n            if (n < 0) throw new ArgumentOutOfRangeException(nameof(n));\n            if (d < 1) throw new ArgumentOutOfRangeException(nameof(d));\n            if (raw.Length != n * d) throw new ArgumentException(\"Feature buffer must equal n * d.\", nameof(raw));\n\n            double tangentScale = probeParam is double t && t > 0.0 ? t : 1.0;\n            double[] adapted = new double[raw.Length];\n\n            for (int i = 0; i < n; i++)\n            {\n                int offset = i * d;\n                PoincareTransforms.ExpMapFromOrigin(\n                    raw.AsSpan(offset, d),\n                    adapted.AsSpan(offset, d),\n                    tangentScale: tangentScale,\n                    maxRadius: 0.95);\n            }\n\n            return new ProbeContract\n            {\n                Metric = metric,\n                SampleCount = n,\n                AmbientDim = d,\n                AdaptedDim = d,\n                Representation = \"PoincareBall\",\n                AmbientFeatures = raw,\n                AdaptedFeatures = adapted,\n                ProbeParam = tangentScale,\n                SupportsIntrinsicFlow = true,\n                Notes = \"Origin exp-map into the Poincare ball → hyperbolic core-periphery probe\"\n            };\n        }\n    }\n} |
38 | ETL/adapters/identity.cs | {822 86 0.3735 4.2129} | 850 | using System;\n\nnamespace ETL.Adapters\n{\n    public sealed class IdentityAdapter : IManifoldAdapter\n    {\n        public ProbeContract Embed(double[] raw, int n, int d, ProbeMetric metric, object? _ = null)\n        {\n            if (n < 0) throw new ArgumentOutOfRangeException(nameof(n));\n            if (d < 1) throw new ArgumentOutOfRangeException(nameof(d));\n            if (raw.Length != n * d) throw new ArgumentException(\"Feature buffer must equal n * d.\", nameof(raw));\n\n            return new ProbeContract\n            {\n                Metric = metric,\n                SampleCount = n,\n                AmbientDim = d,\n                AdaptedDim = d,\n                Representation = \"AmbientEuclidean\",\n                AmbientFeatures = raw,\n                AdaptedFeatures = raw,\n            };\n        }\n    }\n} |
39 | ETL/adapters/ManifoldAdapters.cs | {422 36 0.3697 4.1320} | 438 | using System;\n\nnamespace ETL.Adapters\n{\n    public static class ManifoldAdapters\n    {\n        public static IManifoldAdapter ForMetric(ProbeMetric metric)\n        {\n            return metric switch\n            {\n                ProbeMetric.Poincare => new PoincareAdapter(),\n                ProbeMetric.FisherRaoSimplex => new SimplexAdapter(),\n                _ => new IdentityAdapter(),\n            };\n        }\n    }\n} |
40 | ETL/adapters/simplex.cs | {2336 262 0.4131 4.1299} | 2410 | using System;\n\nnamespace ETL.Adapters\n{\n    public sealed class SimplexAdapter : IManifoldAdapter\n    {\n        public ProbeContract Embed(\n            double[] rawFeatures,\n            int n,\n            int d,\n            ProbeMetric metric,\n            object? probeParam = null)\n        {\n            if (n < 0) throw new ArgumentOutOfRangeException(nameof(n));\n            if (d < 1) throw new ArgumentException(\"Simplex needs at least 1 dimension.\");\n            if (rawFeatures.Length != n * d) throw new ArgumentException(\"Feature buffer must equal n * d.\", nameof(rawFeatures));\n\n            double tau = probeParam is double t && t > 0 ? t : 1.0; // temperature / sharpness\n            double[] adapted = new double[rawFeatures.Length];\n            Array.Copy(rawFeatures, adapted, rawFeatures.Length);\n\n            for (int i = 0; i < n; i++)\n            {\n                int offset = i * d;\n\n                // Stable softmax with temperature\n                double max = double.MinValue;\n                for (int j = 0; j < d; j++)\n                    max = Math.Max(max, adapted[offset + j]);\n\n                double sum = 0.0;\n                for (int j = 0; j < d; j++)\n                {\n                    double logit = (adapted[offset + j] - max) / tau;\n                    adapted[offset + j] = Math.Exp(logit);\n                    sum += adapted[offset + j];\n                }\n\n                // Guard against underflow/zero-sum (extremely rare with tau > 0)\n                if (sum < 1e-300)\n                    sum = 1.0;\n\n                for (int j = 0; j < d; j++)\n                    adapted[offset + j] /= sum;\n            }\n\n            return new ProbeContract\n            {\n                Metric = metric,\n                SampleCount = n,\n                AmbientDim = d,\n                AdaptedDim = d,\n                Representation = $\"SimplexPMF(τ={tau:F3})\",\n                AmbientFeatures = (double[])rawFeatures.Clone(),\n                AdaptedFeatures = adapted,\n                ProbeParam = tau,\n                SupportsIntrinsicFlow = false, // Fisher-Rao on simplex has rich geometry but no simple tangent flow yet\n                Notes = $\"Softmax(τ={tau}) embedding: ambient coords as logits → information geometry probe. Higher τ = flatter distributions.\"\n            };\n        }\n    }\n} |
41 | ETL/contracts/ContractValidator.cs | {3180 280 0.3792 4.1813} | 3279 | using System;\n\nnamespace ETL.Contracts\n{\n    public static class ContractValidator\n    {\n        public static void EnsureValidForGraph(ProbeContract contract)\n        {\n            ArgumentNullException.ThrowIfNull(contract);\n\n            if (contract.SampleCount < 0)\n                throw new InvalidOperationException(\"SampleCount must be non-negative.\");\n            if (contract.AmbientDim < 1 || contract.AdaptedDim < 1)\n                throw new InvalidOperationException(\"Contract dimensions must be positive.\");\n            if (contract.AmbientFeatures.Length != contract.SampleCount * contract.AmbientDim)\n                throw new InvalidOperationException(\"Ambient feature buffer does not match SampleCount × AmbientDim.\");\n            if (contract.AdaptedFeatures.Length != contract.SampleCount * contract.AdaptedDim)\n                throw new InvalidOperationException(\"Adapted feature buffer does not match SampleCount × AdaptedDim.\");\n\n            EnsureFinite(contract.AdaptedFeatures);\n\n            switch (contract.Metric)\n            {\n                case ProbeMetric.Poincare:\n                    EnsurePoincareDomain(contract);\n                    break;\n                case ProbeMetric.FisherRaoSimplex:\n                    EnsureSimplexDomain(contract);\n                    break;\n            }\n        }\n\n        private static void EnsureFinite(double[] values)\n        {\n            for (int i = 0; i < values.Length; i++)\n            {\n                if (!double.IsFinite(values[i]))\n                    throw new InvalidOperationException($\"Adapted feature buffer contains a non-finite value at index {i}.\");\n            }\n        }\n\n        private static void EnsurePoincareDomain(ProbeContract contract)\n        {\n            const double eps = 1e-12;\n\n            for (int i = 0; i < contract.SampleCount; i++)\n            {\n                int offset = i * contract.AdaptedDim;\n                double normSq = 0.0;\n                for (int j = 0; j < contract.AdaptedDim; j++)\n                {\n                    double value = contract.AdaptedFeatures[offset + j];\n                    normSq += value * value;\n                }\n\n                if (normSq >= 1.0 - eps)\n                    throw new InvalidOperationException($\"Row {i} lies on or outside the open Poincare ball.\");\n            }\n        }\n\n        private static void EnsureSimplexDomain(ProbeContract contract)\n        {\n            const double tolerance = 1e-8;\n\n            for (int i = 0; i < contract.SampleCount; i++)\n            {\n                int offset = i * contract.AdaptedDim;\n                double sum = 0.0;\n                for (int j = 0; j < contract.AdaptedDim; j++)\n                {\n                    double value = contract.AdaptedFeatures[offset + j];\n                    if (value < 0.0)\n                        throw new InvalidOperationException($\"Row {i} contains a negative simplex coordinate.\");\n                    sum += value;\n                }\n\n                if (Math.Abs(sum - 1.0) > tolerance)\n                    throw new InvalidOperationException($\"Row {i} does not sum to 1 within tolerance.\");\n            }\n        }\n    }\n} |
42 | ETL/contracts/ProbeContracts.cs | {1068 122 0.3287 4.2709} | 1105 | namespace ETL.Contracts\n{\n    public enum ProbeMetric\n    {\n        Euclidean,\n        Manhattan,\n        Minkowski,\n        Cosine,\n        Mahalanobis,\n        Poincare,\n        FisherRaoSimplex,\n        FisherRaoHalfPlane,\n        JensenShannon,\n        Wasserstein,\n        Hamming,\n        Jaccard,\n        Canberra,\n    }\n\n    public sealed class ProbeContract\n    {\n        public ProbeMetric Metric { get; init; }\n        public int SampleCount { get; init; }\n        public int AmbientDim { get; init; }\n        public int AdaptedDim { get; init; }\n        public string Representation { get; init; } = string.Empty;\n        public double[] AmbientFeatures { get; init; } = [];\n        public double[] AdaptedFeatures { get; init; } = [];\n        public object? ProbeParam { get; init; }\n        public bool SupportsIntrinsicFlow { get; init; }\n        public string? Notes { get; init; }\n    }\n\n    public interface IManifoldAdapter\n    {\n        ProbeContract Embed(double[] rawFeatures, int n, int d, ProbeMetric metric, object? probeParam = null);\n    }\n} |
43 | ETL/transforms/PoincareTransforms.cs | {6955 670 0.3435 4.3598} | 7141 | using System;\n\nnamespace ETL.Transforms\n{\n    /// <summary>\n    /// Helpers for mapping Euclidean vectors into the open unit ball so they can\n    /// be consumed by the Poincare distance and related hyperbolic workflows.\n    /// These transforms are preparation steps, not claims that the source data\n    /// is already a learned hyperbolic embedding.\n    /// </summary>\n    public static class PoincareTransforms\n    {\n        /// <summary>\n        /// Maps a Euclidean vector into the open unit ball by global linear\n        /// rescaling. Direction is preserved exactly and radial order is\n        /// preserved whenever <paramref name=\"sourceMaxNorm\"/> is shared across\n        /// the dataset.\n        /// </summary>\n        public static void ScaleToOpenBall(\n            ReadOnlySpan<double> source,\n            Span<double> destination,\n            double sourceMaxNorm,\n            double radius = 0.95,\n            double epsilon = 1e-12)\n        {\n            ValidateBuffers(source, destination);\n            ValidateRadius(radius);\n\n            if (!double.IsFinite(sourceMaxNorm) || sourceMaxNorm < 0.0)\n                throw new ArgumentOutOfRangeException(nameof(sourceMaxNorm));\n            if (!double.IsFinite(epsilon) || epsilon <= 0.0)\n                throw new ArgumentOutOfRangeException(nameof(epsilon));\n\n            double scale = radius / (sourceMaxNorm + epsilon);\n            for (int i = 0; i < source.Length; i++)\n                destination[i] = source[i] * scale;\n\n            EnsureInsideOpenBall(destination, radius, epsilon);\n        }\n\n        /// <summary>\n        /// Treats the source vector as a tangent vector at the origin and maps it\n        /// into the Poincare ball using the closed-form exponential map for the\n        /// unit-curvature ball. This is more principled than linear rescaling when\n        /// you want a true manifold map from tangent coordinates to ball points.\n        /// </summary>\n        public static void ExpMapFromOrigin(\n            ReadOnlySpan<double> tangent,\n            Span<double> destination,\n            double tangentScale = 1.0,\n            double maxRadius = 0.95,\n            double epsilon = 1e-12)\n        {\n            ValidateBuffers(tangent, destination);\n            ValidateRadius(maxRadius);\n\n            if (!double.IsFinite(tangentScale) || tangentScale < 0.0)\n                throw new ArgumentOutOfRangeException(nameof(tangentScale));\n            if (!double.IsFinite(epsilon) || epsilon <= 0.0)\n                throw new ArgumentOutOfRangeException(nameof(epsilon));\n\n            double tangentNorm = 0.0;\n            for (int i = 0; i < tangent.Length; i++)\n            {\n                double value = tangent[i] * tangentScale;\n                tangentNorm += value * value;\n            }\n\n            tangentNorm = Math.Sqrt(tangentNorm);\n            if (tangentNorm <= epsilon)\n            {\n                destination.Clear();\n                return;\n            }\n\n            double radial = Math.Min(Math.Tanh(tangentNorm), maxRadius);\n            double scale = radial / tangentNorm;\n            for (int i = 0; i < tangent.Length; i++)\n                destination[i] = tangent[i] * tangentScale * scale;\n\n            EnsureInsideOpenBall(destination, maxRadius, epsilon);\n        }\n\n        /// <summary>\n        /// Computes the maximum Euclidean norm across a dataset represented as a\n        /// jagged matrix. Useful for a shared linear ball transform.\n        /// </summary>\n        public static double ComputeMaxNorm(double[][] vectors)\n        {\n            ArgumentNullException.ThrowIfNull(vectors);\n\n            double maxNorm = 0.0;\n            foreach (double[] vector in vectors)\n            {\n                ArgumentNullException.ThrowIfNull(vector);\n                double norm = L2Norm(vector);\n                if (norm > maxNorm)\n                    maxNorm = norm;\n            }\n\n            return maxNorm;\n        }\n\n        /// <summary>\n        /// Applies a shared linear rescaling transform to every row in a jagged\n        /// matrix and returns a new matrix of ball coordinates.\n        /// </summary>\n        public static double[][] ScaleToOpenBall(\n            double[][] vectors,\n            double radius = 0.95,\n            double epsilon = 1e-12)\n        {\n            ArgumentNullException.ThrowIfNull(vectors);\n            double sourceMaxNorm = ComputeMaxNorm(vectors);\n            var transformed = new double[vectors.Length][];\n\n            for (int i = 0; i < vectors.Length; i++)\n            {\n                double[] source = vectors[i] ?? throw new ArgumentNullException(nameof(vectors));\n                var destination = new double[source.Length];\n                ScaleToOpenBall(source, destination, sourceMaxNorm, radius, epsilon);\n                transformed[i] = destination;\n            }\n\n            return transformed;\n        }\n\n        /// <summary>\n        /// Applies the origin exponential map to every row in a jagged matrix and\n        /// returns a new matrix of ball coordinates.\n        /// </summary>\n        public static double[][] ExpMapFromOrigin(\n            double[][] tangents,\n            double tangentScale = 1.0,\n            double maxRadius = 0.95,\n            double epsilon = 1e-12)\n        {\n            ArgumentNullException.ThrowIfNull(tangents);\n            var transformed = new double[tangents.Length][];\n\n            for (int i = 0; i < tangents.Length; i++)\n            {\n                double[] tangent = tangents[i] ?? throw new ArgumentNullException(nameof(tangents));\n                var destination = new double[tangent.Length];\n                ExpMapFromOrigin(tangent, destination, tangentScale, maxRadius, epsilon);\n                transformed[i] = destination;\n            }\n\n            return transformed;\n        }\n\n        private static void ValidateBuffers(ReadOnlySpan<double> source, Span<double> destination)\n        {\n            if (source.Length != destination.Length)\n                throw new ArgumentException(\"Source and destination must have equal length.\");\n        }\n\n        private static void ValidateRadius(double radius)\n        {\n            if (!double.IsFinite(radius) || radius <= 0.0 || radius >= 1.0)\n                throw new ArgumentOutOfRangeException(nameof(radius));\n        }\n\n        private static double L2Norm(ReadOnlySpan<double> vector)\n        {\n            double sumSq = 0.0;\n            for (int i = 0; i < vector.Length; i++)\n                sumSq += vector[i] * vector[i];\n            return Math.Sqrt(sumSq);\n        }\n\n        private static void EnsureInsideOpenBall(Span<double> vector, double maxRadius, double epsilon)\n        {\n            double norm = L2Norm(vector);\n            if (norm <= maxRadius)\n                return;\n\n            double scale = maxRadius / (norm + epsilon);\n            for (int i = 0; i < vector.Length; i++)\n                vector[i] *= scale;\n        }\n    }\n} |
