idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
54 | spc.graph.cs | {14073 1092 0.3364 4.3713} | 14458 | using System;\nusing System.Collections.Generic;\nusing CouplingKernels;\nusing DistanceMetrics;\nusing ProximityGraphs;\nusing StatisticalEstimators;\n\nnamespace SpcCore\n{\n    // =========================================================================\n    // Edge - weighted undirected connection between two data points\n    // =========================================================================\n\n    public struct Edge\n    {\n        public int Source;\n        public int Target;\n        public double J;\n\n        public Edge(int source, int target, double coupling)\n        {\n            Source = source;\n            Target = target;\n            J = coupling;\n        }\n    }\n\n    // =========================================================================\n    // CsrGraph - runtime topology for Potts simulation and post-hoc analysis\n    // =========================================================================\n\n    /// <summary>\n    /// Symmetric CSR adjacency: each undirected edge is stored in both endpoint rows.\n    /// The Swendsen-Wang loop skips target &lt;= source to process each edge once.\n    /// </summary>\n    public struct CsrGraph\n    {\n        public int[] Targets;\n        public double[] Weights;\n        public int[] RowPointers;\n        public int NodeCount;\n\n        /// <summary>\n        /// Build symmetric CSR from an undirected Edge[] where each edge appears once.\n        /// </summary>\n        public static CsrGraph FromEdges(Edge[] edges, int nodeCount)\n        {\n            int[] degree = new int[nodeCount];\n            for (int edge = 0; edge < edges.Length; edge++)\n            {\n                degree[edges[edge].Source]++;\n                degree[edges[edge].Target]++;\n            }\n\n            int[] rowPointers = new int[nodeCount + 1];\n            for (int node = 0; node < nodeCount; node++)\n                rowPointers[node + 1] = rowPointers[node] + degree[node];\n\n            int totalEntries = rowPointers[nodeCount];\n            int[] targets = new int[totalEntries];\n            double[] weights = new double[totalEntries];\n\n            int[] cursor = new int[nodeCount];\n            Array.Copy(rowPointers, cursor, nodeCount);\n\n            for (int edge = 0; edge < edges.Length; edge++)\n            {\n                int source = edges[edge].Source;\n                int target = edges[edge].Target;\n                double coupling = edges[edge].J;\n\n                targets[cursor[source]] = target;\n                weights[cursor[source]] = coupling;\n                cursor[source]++;\n\n                targets[cursor[target]] = source;\n                weights[cursor[target]] = coupling;\n                cursor[target]++;\n            }\n\n            return new CsrGraph\n            {\n                Targets = targets,\n                Weights = weights,\n                RowPointers = rowPointers,\n                NodeCount = nodeCount\n            };\n        }\n    }\n\n    // =========================================================================\n    // SPC graph initialization and diagnostics\n    // =========================================================================\n\n    public static partial class SpcBatch\n    {\n        internal static Edge[] BuildGraphFromMetric(SpcBatchRequest request, out int n)\n        {\n            switch (request.Metric)\n            {\n                case SpcMetric.Hamming:\n                    return BuildGraphFromSimHashes(request, out n);\n                case SpcMetric.Euclidean:\n                    return BuildFeatureMetricGraph(request, Euclidean.Distance, \"Euclidean\", out n);\n                case SpcMetric.Canberra:\n                    return BuildFeatureMetricGraph(request, Canberra.Distance, \"Canberra\", out n);\n                case SpcMetric.Manhattan:\n                    return BuildFeatureMetricGraph(request, Manhattan.Distance, \"Manhattan\", out n);\n                case SpcMetric.Minkowski:\n                    double p = request.MinkowskiP;\n                    return BuildFeatureMetricGraph(request, (a, b) => Minkowski.Distance(a, b, p), \"Minkowski\", out n);\n                case SpcMetric.Cosine:\n                    return BuildFeatureMetricGraph(request, Cosine.Distance, \"Cosine\", out n);\n                case SpcMetric.JensenShannon:\n                    return BuildFeatureMetricGraph(request, JensenShannon.Distance, \"Jensen-Shannon\", out n);\n                case SpcMetric.Jaccard:\n                    double threshold = request.JaccardThreshold;\n                    return BuildFeatureMetricGraph(request, (a, b) => Jaccard.Distance(a, b, threshold), \"Jaccard\", out n);\n                case SpcMetric.Wasserstein:\n                    return BuildFeatureMetricGraph(request, Wasserstein.Distance1D, \"Wasserstein\", out n);\n                case SpcMetric.Mahalanobis:\n                    return BuildMahalanobisMetricGraph(request, out n);\n                case SpcMetric.FisherRao:\n                    return BuildFeatureMetricGraph(request, FisherRao.Distance, \"Fisher-Rao\", out n);\n                case SpcMetric.Poincare:\n                    return BuildFeatureMetricGraph(request, Poincare.Distance, \"Poincare\", out n);\n                default:\n                    throw new NotSupportedException($\"Metric {request.Metric} not implemented.\");\n            }\n        }\n\n        internal static double EstimateDelta(DeltaEstimator estimator, double[] nnDistances)\n        {\n            switch (estimator)\n            {\n                case DeltaEstimator.Mean: return DeltaMean.Estimate(nnDistances);\n                case DeltaEstimator.Median: return DeltaMedian.Estimate(nnDistances);\n                default: throw new NotSupportedException($\"Estimator {estimator} not implemented.\");\n            }\n        }\n\n        internal static (Neighbor[][] allNeighbors, double[] nnDistances) SelectNeighbors(\n            SpcProximity proximity, int n, int k, double epsilon,\n            Func<int, int, double> pairDistance)\n        {\n            NeighborSelection selection;\n            switch (proximity)\n            {\n                case SpcProximity.Knn:\n                    selection = ProximityGraph.SelectKnn(n, k, pairDistance);\n                    break;\n                case SpcProximity.MutualKnn:\n                    selection = ProximityGraph.SelectMutualKnn(n, k, pairDistance);\n                    break;\n                case SpcProximity.EpsilonBall:\n                    selection = ProximityGraph.SelectEpsilonBall(n, epsilon, pairDistance);\n                    break;\n                case SpcProximity.MstAugmented:\n                    selection = ProximityGraph.SelectMstAugmented(n, k, pairDistance);\n                    break;\n                default:\n                    throw new NotSupportedException($\"Proximity {proximity} not implemented.\");\n            }\n\n            return (selection.AllNeighbors, selection.NearestNeighborDistances);\n        }\n\n        internal static Edge[] ConvertToCoupling(\n            Neighbor[][] allNeighbors, double[] nnDistances,\n            double delta, DeltaEstimator estimator,\n            CouplingKernel kernel = CouplingKernel.Gaussian)\n        {\n            double effectiveDelta = delta > 0 ? delta : EstimateDelta(estimator, nnDistances);\n\n            Func<double, double, double> kernelFunc;\n            switch (kernel)\n            {\n                case CouplingKernel.Gaussian: kernelFunc = Gaussian.Evaluate; break;\n                case CouplingKernel.Cauchy: kernelFunc = Cauchy.Evaluate; break;\n                case CouplingKernel.Laplacian: kernelFunc = Laplacian.Evaluate; break;\n                case CouplingKernel.Linear: kernelFunc = Linear.Evaluate; break;\n                default: throw new NotSupportedException($\"CouplingKernel {kernel} not implemented.\");\n            }\n\n            var edges = new List<Edge>();\n            int n = allNeighbors.Length;\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var neighbor in allNeighbors[i])\n                {\n                    if (i > neighbor.Index)\n                        continue;\n\n                    double coupling = kernelFunc(neighbor.Distance, effectiveDelta);\n                    if (coupling > 1e-10)\n                        edges.Add(new Edge(i, neighbor.Index, coupling));\n                }\n            }\n\n            return edges.ToArray();\n        }\n\n        internal static Edge[] BuildGraphFromFeatures(\n            double[][] features, int k, double delta, DeltaEstimator estimator,\n            Func<double[], double[], double> distanceFunc,\n            SpcProximity proximity = SpcProximity.Knn, double epsilon = 0.0,\n            CouplingKernel kernel = CouplingKernel.Gaussian)\n        {\n            int n = features.Length;\n            Func<int, int, double> pairDistance = (i, j) => distanceFunc(features[i], features[j]);\n\n            var (allNeighbors, nnDistances) = SelectNeighbors(proximity, n, k, epsilon, pairDistance);\n\n            return ConvertToCoupling(allNeighbors, nnDistances, delta, estimator, kernel);\n        }\n\n        internal static List<string> ValidateGraph(Edge[] edges, int n, SpcProximity proximity)\n        {\n            var warnings = new List<string>();\n\n            if (edges.Length == 0)\n            {\n                warnings.Add(\"Graph has zero edges. Simulation will produce no structure.\");\n                return warnings;\n            }\n\n            var components = new FastUnionFind(n);\n            for (int index = 0; index < edges.Length; index++)\n                components.Union(edges[index].Source, edges[index].Target);\n\n            var componentSizes = new Dictionary<int, int>();\n            for (int node = 0; node < n; node++)\n            {\n                int root = components.Find(node);\n                if (!componentSizes.ContainsKey(root))\n                    componentSizes[root] = 0;\n                componentSizes[root]++;\n            }\n\n            int componentCount = componentSizes.Count;\n            int largestComponent = 0;\n            foreach (var size in componentSizes.Values)\n                if (size > largestComponent)\n                    largestComponent = size;\n\n            double coverage = (double)largestComponent / n;\n            int isolatedNodes = 0;\n            foreach (var size in componentSizes.Values)\n                if (size == 1)\n                    isolatedNodes++;\n\n            if (componentCount > 1)\n            {\n                warnings.Add(string.Format(\n                    \"Graph is disconnected: {0} components. Largest covers {1:P1} of nodes ({2}/{3}). Proximity: {4}.\",\n                    componentCount, coverage, largestComponent, n, proximity));\n            }\n\n            if (coverage < 0.9)\n            {\n                warnings.Add(string.Format(\n                    \"Largest connected component covers only {0:P1} of nodes. \" +\n                    \"Potts phase transitions may not reflect global data structure. \" +\n                    \"Consider MstAugmented proximity or increasing K.\",\n                    coverage));\n            }\n\n            if (isolatedNodes > 0)\n            {\n                warnings.Add(string.Format(\n                    \"{0} isolated node(s) with no edges. These will never join any cluster.\",\n                    isolatedNodes));\n            }\n\n            return warnings;\n        }\n\n        private static Edge[] BuildFeatureMetricGraph(\n            SpcBatchRequest request,\n            Func<double[], double[], double> distance,\n            string metricName,\n            out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException($\"Features must be provided for {metricName} metric.\");\n\n            n = request.Features.Length;\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                distance, request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        private static Edge[] BuildGraphFromSimHashes(SpcBatchRequest request, out int n)\n        {\n            if (request.SimHashes == null || request.SimHashes.Length == 0)\n                throw new ArgumentException(\"SimHashes must be provided for Hamming metric.\");\n\n            var hashes = request.SimHashes;\n            n = hashes.Length;\n\n            Func<int, int, double> pairDistance = (i, j) => Hamming.Distance(hashes[i], hashes[j]);\n            var (allNeighbors, nnDistances) = SelectNeighbors(\n                request.Proximity, n, request.K, request.Epsilon, pairDistance);\n\n            return ConvertToCoupling(allNeighbors, nnDistances, request.Delta, request.Estimator, request.CouplingKernel);\n        }\n\n        private static Edge[] BuildMahalanobisMetricGraph(SpcBatchRequest request, out int n)\n        {\n            if (request.Features == null || request.Features.Length == 0)\n                throw new ArgumentException(\"Features must be provided for Mahalanobis metric.\");\n            if (request.CovarianceInverse == null)\n                throw new ArgumentException(\"CovarianceInverse must be provided for Mahalanobis metric.\");\n\n            n = request.Features.Length;\n            int dim = request.Features[0].Length;\n            if (request.CovarianceInverse.GetLength(0) != dim || request.CovarianceInverse.GetLength(1) != dim)\n                throw new ArgumentException(\"CovarianceInverse dimensions must match feature dimensionality.\");\n\n            var invCov = request.CovarianceInverse;\n            return BuildGraphFromFeatures(\n                request.Features, request.K, request.Delta, request.Estimator,\n                (a, b) => Mahalanobis.Distance(a, b, invCov, dim),\n                request.Proximity, request.Epsilon, request.CouplingKernel);\n        }\n\n        public static double NormalizedCompressionDistance(\n            byte[] a,\n            byte[] b,\n            NcdCompressor compressor = NcdCompressor.Brotli)\n        {\n            return global::DistanceMetrics.NormalizedCompressionDistance.Distance(a, b, compressor);\n        }\n    }\n} |
55 | spc.potts.cs | {8616 804 0.4221 4.0561} | 8900 | using System;\nusing System.Numerics.Tensors;\n\nnamespace SpcCore\n{\n\n    // =========================================================================\n    // PottsModel — Swendsen-Wang Monte Carlo on a proximity graph\n    // =========================================================================\n\n    public readonly struct SimulationResult\n    {\n        public readonly double Temperature;\n        public readonly int[] FinalSpins;\n        public readonly int SweepCount;\n        public readonly int EpochCount;\n        public readonly bool IsComplete;\n\n        public SimulationResult(double temperature, int[] finalSpins)\n            : this(\n                temperature,\n                finalSpins,\n                0,\n                0,\n                true)\n        {\n        }\n\n        public SimulationResult(\n            double temperature,\n            int[] finalSpins,\n            int sweepCount,\n            int epochCount,\n            bool isComplete)\n        {\n            Temperature = temperature;\n            FinalSpins = finalSpins;\n            SweepCount = sweepCount;\n            EpochCount = epochCount;\n            IsComplete = isComplete;\n        }\n    }\n\n    public readonly struct SimulationEpochResult\n    {\n        public readonly double Temperature;\n        public readonly int EpochCount;\n        public readonly int SweepCount;\n        public readonly int[] Spins;\n        public readonly bool IsComplete;\n\n        public SimulationEpochResult(\n            double temperature,\n            int epochCount,\n            int sweepCount,\n            int[] spins,\n            bool isComplete)\n        {\n            Temperature = temperature;\n            EpochCount = epochCount;\n            SweepCount = sweepCount;\n            Spins = spins;\n            IsComplete = isComplete;\n        }\n    }\n\n    // =========================================================================\n    // FastUnionFind - path-compressed union-by-size helper\n    // =========================================================================\n\n    public class FastUnionFind\n    {\n        private readonly int[] parent;\n        private readonly int[] size;\n\n        public FastUnionFind(int count)\n        {\n            parent = new int[count];\n            size = new int[count];\n            for (int index = 0; index < count; index++)\n            {\n                parent[index] = index;\n                size[index] = 1;\n            }\n        }\n\n        public int Find(int item)\n        {\n            int root = item;\n            while (root != parent[root])\n                root = parent[root];\n\n            while (item != root)\n            {\n                int next = parent[item];\n                parent[item] = root;\n                item = next;\n            }\n\n            return root;\n        }\n\n        public void Union(int left, int right)\n        {\n            int leftRoot = Find(left);\n            int rightRoot = Find(right);\n            if (leftRoot == rightRoot)\n                return;\n\n            if (size[leftRoot] < size[rightRoot])\n            {\n                parent[leftRoot] = rightRoot;\n                size[rightRoot] += size[leftRoot];\n            }\n            else\n            {\n                parent[rightRoot] = leftRoot;\n                size[leftRoot] += size[rightRoot];\n            }\n        }\n\n        public int[] GetLabels()\n        {\n            var labels = new int[parent.Length];\n            for (int index = 0; index < parent.Length; index++)\n                labels[index] = Find(index);\n            return labels;\n        }\n\n        public void Reset()\n        {\n            for (int index = 0; index < parent.Length; index++)\n            {\n                parent[index] = index;\n                size[index] = 1;\n            }\n        }\n    }\n\n    public class PottsModel\n    {\n        private int N;\n        private CsrGraph Graph;\n        private int[] Spins;\n        private Random Rng;\n        private int Q;\n\n        public PottsModel(int n, int q = 20, int seed = -1)\n        {\n            N = n;\n            Q = q;\n            Graph = default;\n            Spins = new int[N];\n            Rng = seed >= 0 ? new Random(seed) : new Random();\n            // Default random initialization\n            for (int i = 0; i < N; i++) Spins[i] = Rng.Next(Q);\n        }\n\n        /// <summary>\n        /// Inject spin state from a checkpoint (resume support).\n        /// </summary>\n        public void SetSpins(int[] savedSpins)\n        {\n            if (savedSpins.Length != N) throw new ArgumentException(\"Spin array size mismatch\");\n            Array.Copy(savedSpins, Spins, N);\n        }\n\n        /// <summary>\n        /// Load CSR graph topology for simulation.\n        /// </summary>\n        public void LoadGraph(CsrGraph graph)\n        {\n            Graph = graph;\n        }\n\n        /// <summary>\n        /// Run Swendsen-Wang Monte Carlo simulation at the given temperature.\n        /// Should support checkpointing by accepting completed sweep/epoch counts and returning intermediate results via callback.\n        /// </summary>\n        public SimulationResult RunSimulation(\n            double temperature,\n            int steps,\n            Action<SimulationEpochResult> checkpointEpoch = null,\n            int checkpointEpochSweeps = 0,\n            int completedSweeps = 0,\n            int completedEpochs = 0,\n            int totalSweeps = 0)\n        {\n            int entryCount = Graph.RowPointers[N];\n            var bondProb = new double[entryCount];\n\n            // Vectorised bond-probability init: bondProb[e] = 1 - exp(-J/T)\n            // Steps: multiply weights by -1/T → exp in-place → negate → add 1.0\n            TensorPrimitives.Multiply(Graph.Weights.AsSpan(0, entryCount), -1.0 / temperature, bondProb);\n            TensorPrimitives.Exp(bondProb, bondProb);\n            TensorPrimitives.Negate(bondProb, bondProb);\n            TensorPrimitives.Add(bondProb, 1.0, bondProb);\n\n            int totalSweepCount = totalSweeps > 0 ? totalSweeps : completedSweeps + steps;\n            int emittedEpochs = 0;\n\n            // Pre-allocate reusable structures outside the step loop (HPC 1+2)\n            var uf = new FastUnionFind(N);\n            int[] labels = new int[N];\n            int[] clusterMap = new int[N];   // root → assigned spin (-1 = unassigned)\n\n            for (int k = 0; k < steps; k++)\n            {\n                uf.Reset();\n\n                // Bond Formation — CSR row-major traversal (HPC 3)\n                // Sequential access through Targets/Weights arrays per row.\n                // Skip j <= i to process each undirected edge exactly once.\n                for (int i = 0; i < N; i++)\n                {\n                    int start = Graph.RowPointers[i];\n                    int end = Graph.RowPointers[i + 1];\n                    for (int e = start; e < end; e++)\n                    {\n                        int j = Graph.Targets[e];\n                        if (j <= i) continue;\n                        if (Spins[i] == Spins[j] && Rng.NextDouble() < bondProb[e])\n                            uf.Union(i, j);\n                    }\n                }\n\n                // Cluster Flipping — flat array replaces Dictionary<int,int>\n                for (int i = 0; i < N; i++)\n                {\n                    labels[i] = uf.Find(i);\n                    clusterMap[i] = -1;\n                }\n\n                for (int i = 0; i < N; i++)\n                {\n                    int root = labels[i];\n                    if (clusterMap[root] == -1)\n                        clusterMap[root] = Rng.Next(Q);\n                    Spins[i] = clusterMap[root];\n                }\n\n                int sweepCount = completedSweeps + k + 1;\n                if (checkpointEpoch != null &&\n                    checkpointEpochSweeps > 0 &&\n                    sweepCount < totalSweepCount &&\n                    sweepCount % checkpointEpochSweeps == 0)\n                {\n                    emittedEpochs++;\n                    checkpointEpoch(new SimulationEpochResult(\n                        temperature,\n                        completedEpochs + emittedEpochs,\n                        sweepCount,\n                        (int[])Spins.Clone(),\n                        false));\n                }\n            }\n\n            int finalEpochCount = completedEpochs + emittedEpochs + (steps > 0 ? 1 : 0);\n\n            return new SimulationResult(\n                temperature,\n                (int[])Spins.Clone(),\n                completedSweeps + steps,\n                finalEpochCount,\n                true\n            );\n        }\n    }\n} |
56 | spc.synthetic.cs | {2198 205 0.3503 4.2411} | 2257 | using System;\nusing SyntheticDatasets;\n\nnamespace SpcCore\n{\n    public static class SyntheticDatasetSpcExtensions\n    {\n        /// <summary>\n        /// Build an SpcBatchRequest from a generated dataset and a chosen\n        /// sweep configuration. Maps Features through directly; the rest\n        /// (metric, K, temperatures, steps, proximity, kernel, estimator)\n        /// is the harness's choice and must be supplied.\n        ///\n        /// Mahalanobis caveat: SpcBatchRequest.CovarianceInverse is left null.\n        /// dataset.ClusterCovariances exposes raw per-cluster covariance matrices,\n        /// not a pooled inverse — pooling and inversion is the caller's\n        /// responsibility (different harnesses pool differently).\n        /// </summary>\n        public static SpcBatchRequest ToSpcBatchRequest(\n            this SyntheticData.SyntheticDataset dataset,\n            SpcMetric metric,\n            int k,\n            double[] temperatures,\n            int steps,\n            SpcProximity proximity = SpcProximity.Knn,\n            CouplingKernel kernel = CouplingKernel.Gaussian,\n            DeltaEstimator estimator = DeltaEstimator.Mean,\n            double delta = 0.0,\n            double epsilon = 0.0,\n            double minkowskiP = 2.0,\n            double jaccardThreshold = 0.5,\n            int baseSeed = -1)\n        {\n            if (dataset == null) throw new ArgumentNullException(nameof(dataset));\n            if (dataset.Features == null || dataset.Features.Length == 0)\n                throw new ArgumentException(\"Dataset has no features.\", nameof(dataset));\n\n            return new SpcBatchRequest\n            {\n                Metric = metric,\n                Features = dataset.Features,\n                K = k,\n                Temperatures = temperatures ?? Array.Empty<double>(),\n                Steps = steps,\n                Proximity = proximity,\n                CouplingKernel = kernel,\n                Estimator = estimator,\n                Delta = delta,\n                Epsilon = epsilon,\n                MinkowskiP = minkowskiP,\n                JaccardThreshold = jaccardThreshold,\n                BaseSeed = baseSeed,\n            };\n        }\n    }\n} |
