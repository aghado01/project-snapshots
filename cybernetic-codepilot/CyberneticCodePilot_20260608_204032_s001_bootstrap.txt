idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | bootstrap/launch-generic.bat | {2523 306 0.1474 5.3911} | 3057 | @echo off\n:: ════════════════════════════════════════════════════════════════════════\n::  launch-generic.bat  —  SKETCH of the target generalized launcher\n::\n::  ONE file holding all shared portable bootstrap. Thin per-app shims\n::  (shims/launch-<app>.bat) set only the APP_* vars, then `call` this.\n::\n::  Generalizes the current launch-claude.bat / launch-antigravity.bat by\n::  replacing app-specific names ({app} pwsh, claude.exe, CLAUDE_*/ANTIGRAVITY_*)\n::  with generic APP_* parameters. The ~90-line invariant block was already\n::  byte-identical between the two launchers — this is where it lives once.\n:: ════════════════════════════════════════════════════════════════════════\nsetlocal EnableDelayedExpansion\n\n:: ── Contract: the calling shim must set these ───────────────────────────\n::   APP_NAME       e.g. claude | antigravity\n::   APP_EXE        full path to the app executable to start\n::   APP_PWSH_DIR   portable pwsh instance dir for this app\n::   APP_PWSH_EXE   %APP_PWSH_DIR%\\<binary>\n::   APP_USER       TERMINAL_USER value\n::   (shim may also export app-specific env: CLAUDE_CODE_*, ANTIGRAVITY_*, GEMINI_*)\nif not defined PORTABLE_ROOT ( echo [ERROR] PORTABLE_ROOT not set & exit /b 1 )\nif not defined APP_EXE       ( echo [ERROR] APP_EXE not set by shim & exit /b 1 )\nif not defined APP_PWSH_EXE  ( echo [ERROR] APP_PWSH_EXE not set by shim & exit /b 1 )\n\nset \"PDENV_BOOTSTRAP_DIR=%PORTABLE_ROOT%/UserGithub/PowerShellCore/ps.core.bootstrap\"\nset \"AGENT_BIN_DIR=%PDENV_BOOTSTRAP_DIR%/launchers/agent-bin\"\nset \"TERMINAL_USER=%APP_USER%\"\nset \"SHELL=%APP_PWSH_EXE%\"\n\n:: ── Shared portable invariants (identical across all apps) ──────────────\n::   Python/Pyenv · Dotnet · Git · JDK · Node/NPM · llama.cpp/CUDA · CLI-lib ·\n::   GTK · CMake · Go · Rust · Vim — all %PORTABLE_ROOT%-rooted.\n::   Factored out so there is exactly one copy (was duplicated per launcher):\ncall \"%PDENV_BOOTSTRAP_DIR%/launchers/portable-invariants.bat\"\n\n:: ── agent-bin powershell.cmd shim (the -NoProfile wrapper agents invoke) ─\nif not exist \"%AGENT_BIN_DIR:/=\\%\" mkdir \"%AGENT_BIN_DIR:/=\\%\"\nif not exist \"%AGENT_BIN_DIR:/=\\%\\powershell.cmd\" (\n    ( echo @echo off & echo \"%%SHELL:/=\\%%\" -NoProfile %%* ) > \"%AGENT_BIN_DIR:/=\\%\\powershell.cmd\"\n)\n\n:: ── PATH assembly + portable-first dedup (shared) ───────────────────────\ncall \"%PDENV_BOOTSTRAP_DIR%/launchers/portable-path.bat\"\n\necho [OK] PORTABLE_ROOT = %PORTABLE_ROOT%\necho [OK] %APP_USER% pwsh = %APP_PWSH_EXE%\necho Starting %APP_NAME%...\nstart \"\" \"%APP_EXE%\"\nendlocal
1 | bootstrap/shims/launch-claude.bat | {878 71 0.0843 5.4799} | 923 | @echo off\n:: SKETCH — thin app shim. Sets only what's claude-specific, then calls the\n:: generic launcher. This is the whole shim; everything shared lives upstream.\nsetlocal EnableDelayedExpansion\n\nset \"APP_NAME=claude\"\nset \"APP_PWSH_DIR=%PORTABLE_ROOT%/PowerShell/PowerShell-7.6.0-win-x64-claude\"\nset \"APP_PWSH_EXE=%APP_PWSH_DIR%/powershell.exe\"\nset \"APP_EXE=%LOCALAPPDATA%/AnthropicClaude/claude.exe\"\nset \"APP_USER=Claude\"\n\n:: app-specific env\nset \"CLAUDE_CODE_USE_POWERSHELL_TOOL=1\"\nset \"CLAUDE_CODE_SHELL=%APP_PWSH_EXE%\"\nset \"CLAUDE_CONFIG_DIR=%USERPROFILE%/.claude\"\nset \"CLAUDE_CLI_EXE=%USERPROFILE%/.local/bin\"\n\ncall \"%~dp0..\\launch-generic.bat\"\nendlocal\n\n:: launch-antigravity.bat is symmetric: APP_PWSH_DIR=...-antigravity-gemini,\n:: APP_EXE=.../Programs/Antigravity/antigravity.exe, APP_USER=Antigravity,\n:: plus ANTIGRAVITY_* / GEMINI_* vars in place of CLAUDE_CODE_*.
