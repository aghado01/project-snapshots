idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
1 | claude-jso-markdown.ps1 | {19101 1531 0.3510 4.4876} | 19743 | # claude-jso-markdown.ps1 — Markdown renderer for Claude Code thread exports\n#\n# Standalone — no dependencies. Reads the merged thread JSONL produced by\n# Export-ClaudeThread (claude-jso-jackson.ps1) and renders a single markdown document.\n#\n# Usage:\n#   . \"$env:CLAUDE_CONFIG_DIR\\tools\\claude-jso-markdown.ps1\"\n#   ConvertTo-ClaudeMarkdown -MergedJsonlPath $path -OutputPath thread.md\n#   ConvertTo-ClaudeMarkdown -MergedJsonlPath $path  # returns string\n#\n# FUNCTIONS\n# ---------\n#   ConvertTo-ClaudeMarkdown   Read merged JSONL, write or return markdown.\n#\n# FORMAT MODES\n# ------------\n#   Diarized   (default) Bold speaker headers (**Human**, **Claude · model**);\n#              turn separator ---\n#   Dialogue   Simple colon labels (User:, Claude:, Agent:);\n#              model attribution in frontmatter only; turn separator ---\n#   Structural Perplexity-style: human turns as # heading, Claude turns as\n#              bare body; exchange separator --- before each human turn (except first)\n#\n# COMPONENT TOKENS (for -Exclude)\n# ------------------------------------------\n#   thinking        assistant thinking blocks\n#   tool-calls      tool_use blocks in assistant content\n#   tool-results    user records that are purely tool_result carriers\n#   subagents       _islateral=true records (subagent turns)\n#   synthetic       records where message.model = <synthetic>\n#   timestamps      per-turn timestamp in speaker header\n#   session-markers <!-- session uuid depth:N --> HTML comments\n#\n# DEFAULT PROFILE: model-feeding\n#   Excludes thinking, tool-calls, tool-results, subagents, synthetic, timestamps,\n#   and session-markers. Produces a clean Human / Claude dialogue.\n#\n# FRONTMATTER\n#   All formats emit a YAML frontmatter block with: format, exported_at, turns,\n#   sessions (short UUIDs), models, and the active exclude list.\n# -----------------------------------------------------------------------\n\nfunction ConvertTo-ClaudeMarkdown\n{\n    <#\n    .SYNOPSIS\n        Render a merged Claude thread JSONL as a markdown document.\n    .DESCRIPTION\n        Reads a thread-*.jsonl produced by Export-ClaudeThread and renders it\n        as a single markdown file. YAML frontmatter is always emitted. Format\n        controls diarization style; -Exclude controls which content blocks appear.\n    .PARAMETER MergedJsonlPath\n        Path to a thread-*.jsonl produced by Export-ClaudeThread.\n    .PARAMETER OutputPath\n        Optional output .md file path. If omitted, returns the markdown string.\n    .PARAMETER Format\n        Diarized  (default) — bold speaker headers with --- turn separators.\n        Dialogue             — simple colon labels, model attribution in frontmatter.\n        Structural           — Perplexity-style # heading prompts + bare responses.\n    .PARAMETER Exclude\n        Array of component tokens to suppress. Defaults to the model-feeding\n        profile (thinking, tool-calls, tool-results, subagents, synthetic, timestamps,\n        session-markers). Pass an empty array to include everything.\n    .PARAMETER MaxToolInputLength\n        Truncate tool_use input JSON at this many characters. $null = no truncation.\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$MergedJsonlPath,\n\n        [string]$OutputPath,\n\n        [ValidateSet('Diarized', 'Dialogue', 'Structural')]\n        [string]$Format = 'Diarized',\n\n        [ValidateSet('thinking', 'tool-calls', 'tool-results', 'subagents',\n            'synthetic', 'timestamps', 'session-markers')]\n        [string[]]$Exclude = @('thinking', 'tool-calls', 'tool-results',\n            'subagents', 'synthetic', 'timestamps', 'session-markers'),\n\n        [AllowNull()]\n        [Nullable[int]]$MaxToolInputLength = 500\n    )\n\n    if (-not [System.IO.File]::Exists($MergedJsonlPath))\n    {\n        throw \"Merged JSONL not found: $MergedJsonlPath\"\n    }\n\n    $ex = [System.Collections.Generic.HashSet[string]]::new(\n        [string[]]$Exclude,\n        [System.StringComparer]::OrdinalIgnoreCase\n    )\n\n    # Metadata accumulated for frontmatter\n    $fmSessions = [System.Collections.Generic.HashSet[string]]::new()\n    $fmModels   = [System.Collections.Generic.HashSet[string]]::new()\n    $turnCount  = 0\n\n    $encoding = [System.Text.UTF8Encoding]::new($false)\n    $doc = [System.Text.StringBuilder]::new()\n    [int]$lastDepth = -1\n    [string]$lastSessionUuid = $null\n    [bool]$firstTurn = $true  # for Structural separator logic\n\n    $fs = [System.IO.FileStream]::new(\n        $MergedJsonlPath,\n        [System.IO.FileMode]::Open,\n        [System.IO.FileAccess]::Read,\n        [System.IO.FileShare]::Read\n    )\n    $sr = [System.IO.StreamReader]::new($fs, $encoding)\n\n    try\n    {\n        while ($null -ne ($rawLine = $sr.ReadLine()))\n        {\n            $trimmed = $rawLine.Trim()\n            if ($trimmed.Length -eq 0) { continue }\n\n            [System.Text.Json.JsonElement]$el = [System.Text.Json.JsonElement]::new()\n            try\n            {\n                $el = [System.Text.Json.JsonSerializer]::Deserialize(\n                    $trimmed, [System.Text.Json.JsonElement])\n            }\n            catch { continue }\n\n            # --- Annotation fields ---\n            $isLateral = $false\n            $agentType = $null\n            $agentDesc = $null\n            $sessionUuid = $null\n            [int]$depth = 0\n\n            try { $isLateral = $el.GetProperty('_islateral').GetBoolean() }   catch {}\n            try { $agentType = $el.GetProperty('_agenttype').GetString() }    catch {}\n            try { $agentDesc = $el.GetProperty('_agentdesc').GetString() }    catch {}\n            try { $sessionUuid = $el.GetProperty('_sessionuuid').GetString() } catch {}\n            try { $depth = $el.GetProperty('_sessiondepth').GetInt32() }      catch {}\n\n            # --- Record-level exclusions ---\n            if ($isLateral -and $ex.Contains('subagents')) { continue }\n\n            $model = $null\n            try { $model = $el.GetProperty('message').GetProperty('model').GetString() } catch {}\n            if ($model -eq '<synthetic>' -and $ex.Contains('synthetic')) { continue }\n\n            # --- Session marker on session/depth change (Diarized and Dialogue) ---\n            $sessionChanged = ($depth -ne $lastDepth) -or ($sessionUuid -ne $lastSessionUuid)\n            if ($Format -ne 'Structural' -and\n                -not $ex.Contains('session-markers') -and\n                $sessionChanged -and $doc.Length -gt 0)\n            {\n                $shortId = if ($sessionUuid -and $sessionUuid.Length -ge 8)\n                { $sessionUuid.Substring(0, 8) } else { $sessionUuid }\n                [void]$doc.Append(\"`n<!-- session $shortId depth:$depth -->`n\")\n            }\n            $lastDepth = $depth\n            $lastSessionUuid = $sessionUuid\n\n            # --- Content rendering ---\n            $contentMd = script:Get-ClaudeContentMarkdown $el $ex $MaxToolInputLength\n            if ($null -eq $contentMd -or $contentMd.Trim().Length -eq 0) { continue }\n\n            # --- Accumulate frontmatter metadata ---\n            if ($sessionUuid) { [void]$fmSessions.Add($sessionUuid) }\n            if ($model -and $model -ne '<synthetic>') { [void]$fmModels.Add($model) }\n            $turnCount++\n\n            # --- Timestamp ---\n            $timestamp = $null\n            try { $timestamp = $el.GetProperty('timestamp').GetString() } catch {}\n\n            # --- Record type ---\n            $recType = $null\n            try { $recType = $el.GetProperty('type').GetString() } catch {}\n            $isHumanTurn = ($recType -eq 'user' -and -not $isLateral)\n\n            # --- Speaker label (Diarized / Dialogue only) ---\n            $speakerLabel = script:Get-ClaudeSpeakerLabel $el $isLateral $agentType $agentDesc $model\n\n            # --- Emit turn ---\n            switch ($Format)\n            {\n                'Diarized'\n                {\n                    $speakerLine = if (-not $ex.Contains('timestamps') -and $timestamp)\n                    { \"**$speakerLabel** · $timestamp\" }\n                    else\n                    { \"**$speakerLabel**\" }\n\n                    [void]$doc.Append(\"---`n\")\n                    [void]$doc.Append(\"`n\")\n                    [void]$doc.Append(\"$speakerLine`n\")\n                    [void]$doc.Append(\"`n\")\n                    [void]$doc.Append(\"$($contentMd.TrimEnd())`n\")\n                    [void]$doc.Append(\"`n\")\n                }\n\n                'Dialogue'\n                {\n                    $dialogueLabel = script:Get-ClaudeDialogueLabel $isHumanTurn $isLateral $agentType\n                    $speakerLine = if (-not $ex.Contains('timestamps') -and $timestamp)\n                    { \"$dialogueLabel [$timestamp]:\" }\n                    else\n                    { \"${dialogueLabel}:\" }\n\n                    [void]$doc.Append(\"---`n\")\n                    [void]$doc.Append(\"`n\")\n                    [void]$doc.Append(\"$speakerLine`n\")\n                    [void]$doc.Append(\"`n\")\n                    [void]$doc.Append(\"$($contentMd.TrimEnd())`n\")\n                    [void]$doc.Append(\"`n\")\n                }\n\n                'Structural'\n                {\n                    if ($isHumanTurn)\n                    {\n                        # --- separator before each human turn except first\n                        if (-not $firstTurn)\n                        {\n                            [void]$doc.Append(\"---`n\")\n                            [void]$doc.Append(\"`n\")\n                        }\n                        # Human content as # heading block\n                        [void]$doc.Append(\"# $($contentMd.TrimEnd())`n\")\n                        [void]$doc.Append(\"`n\")\n                    }\n                    else\n                    {\n                        # Response (Claude, agent) as bare content\n                        if ($isLateral -and $agentType)\n                        {\n                            # Lightweight agent attribution in structural\n                            $attrParts = [System.Collections.Generic.List[string]]::new()\n                            $attrParts.Add($agentType)\n                            if ($agentDesc) { $attrParts.Add('\"' + $agentDesc + '\"') }\n                            [void]$doc.Append(\"*Agent ($($attrParts -join ' · '))*`n\")\n                            [void]$doc.Append(\"`n\")\n                        }\n                        [void]$doc.Append(\"$($contentMd.TrimEnd())`n\")\n                        [void]$doc.Append(\"`n\")\n                    }\n                }\n            }\n\n            $firstTurn = $false\n        }\n    }\n    finally\n    {\n        $sr.Dispose()\n        $fs.Dispose()\n    }\n\n    $frontmatter = script:Get-ClaudeMarkdownFrontmatter $Format $Exclude $fmSessions $fmModels $turnCount\n    $markdown = $frontmatter + $doc.ToString().TrimEnd() + \"`n\"\n\n    if ($OutputPath)\n    {\n        [System.IO.File]::WriteAllText($OutputPath, $markdown, $encoding)\n        return\n    }\n    return $markdown\n}\n\n# -----------------------------------------------------------------------\n\n# -----------------------------------------------------------------------\n\nfunction script:Get-ClaudeMarkdownFrontmatter\n{\n    param(\n        [string]$Format,\n        [string[]]$Exclude,\n        [System.Collections.Generic.HashSet[string]]$Sessions,\n        [System.Collections.Generic.HashSet[string]]$Models,\n        [int]$TurnCount\n    )\n\n    $sb = [System.Text.StringBuilder]::new()\n    [void]$sb.Append(\"---`n\")\n    [void]$sb.Append(\"format: $Format`n\")\n    [void]$sb.Append(\"exported_at: $([datetime]::UtcNow.ToString('o'))`n\")\n    [void]$sb.Append(\"turns: $TurnCount`n\")\n\n    if ($Sessions.Count -gt 0)\n    {\n        [void]$sb.Append(\"sessions:`n\")\n        foreach ($s in ($Sessions | Sort-Object))\n        {\n            $short = if ($s.Length -ge 8) { $s.Substring(0, 8) } else { $s }\n            [void]$sb.Append(\"  - $short`n\")\n        }\n    }\n\n    if ($Models.Count -gt 0)\n    {\n        [void]$sb.Append(\"models:`n\")\n        foreach ($m in ($Models | Sort-Object))\n        {\n            [void]$sb.Append(\"  - $m`n\")\n        }\n    }\n\n    if ($Exclude -and $Exclude.Count -gt 0)\n    {\n        [void]$sb.Append(\"exclude:`n\")\n        foreach ($e in $Exclude)\n        {\n            [void]$sb.Append(\"  - $e`n\")\n        }\n    }\n\n    [void]$sb.Append(\"---`n`n\")\n    return $sb.ToString()\n}\n\nfunction script:Get-ClaudeDialogueLabel\n{\n    param(\n        [bool]$isHumanTurn,\n        [bool]$isLateral,\n        [string]$agentType\n    )\n\n    if ($isHumanTurn) { return 'User' }\n    if ($isLateral)\n    {\n        if ($agentType) { return \"Agent ($agentType)\" }\n        return 'Agent'\n    }\n    return 'Claude'\n}\n\nfunction script:Get-ClaudeSpeakerLabel\n{\n    param(\n        [System.Text.Json.JsonElement]$el,\n        [bool]$isLateral,\n        [string]$agentType,\n        [string]$agentDesc,\n        [string]$model\n    )\n\n    if ($isLateral)\n    {\n        $parts = [System.Collections.Generic.List[string]]::new()\n        $parts.Add('Agent')\n        if ($agentType) { $parts.Add($agentType) }\n        if ($agentDesc) { $parts.Add('\"' + $agentDesc + '\"') }\n        return ($parts -join ' · ')\n    }\n\n    $recType = $null\n    try { $recType = $el.GetProperty('type').GetString() } catch {}\n    if ($recType -eq 'user') { return 'Human' }\n\n    if ($model -and $model -ne '<synthetic>') { return \"Claude · $model\" }\n    return 'Claude'\n}\n\nfunction script:Get-ClaudeContentMarkdown\n{\n    param(\n        [System.Text.Json.JsonElement]$el,\n        [System.Collections.Generic.HashSet[string]]$ex,\n        [Nullable[int]]$maxToolLen\n    )\n\n    $contentEl = [System.Text.Json.JsonElement]::new()\n    try { $contentEl = $el.GetProperty('message').GetProperty('content') }\n    catch { return $null }\n\n    $sb = [System.Text.StringBuilder]::new()\n\n    # --- String content (simple user text) ---\n    if ($contentEl.ValueKind -eq [System.Text.Json.JsonValueKind]::String)\n    {\n        $text = $contentEl.GetString()\n        if ($text.Trim().Length -gt 0) { [void]$sb.Append($text.TrimEnd()) }\n        $result = $sb.ToString()\n        if ($result.Trim().Length -gt 0) { return $result }\n        return $null\n    }\n\n    if ($contentEl.ValueKind -ne [System.Text.Json.JsonValueKind]::Array)\n    {\n        return $null\n    }\n\n    # --- Array of content blocks ---\n    foreach ($block in $contentEl.EnumerateArray())\n    {\n        $blockType = $null\n        try { $blockType = $block.GetProperty('type').GetString() } catch { continue }\n\n        switch ($blockType)\n        {\n            'text'\n            {\n                $text = $null\n                try { $text = $block.GetProperty('text').GetString() } catch {}\n                if ($text -and $text.Trim().Length -gt 0)\n                {\n                    if ($sb.Length -gt 0) { [void]$sb.Append(\"`n\") }\n                    [void]$sb.Append($text.TrimEnd())\n                }\n            }\n\n            'thinking'\n            {\n                if (-not $ex.Contains('thinking'))\n                {\n                    $text = $null\n                    try { $text = $block.GetProperty('thinking').GetString() } catch {}\n                    if ($text -and $text.Trim().Length -gt 0)\n                    {\n                        if ($sb.Length -gt 0) { [void]$sb.Append(\"`n\") }\n                        [void]$sb.Append(\"> **[thinking]**`n\")\n                        [void]$sb.Append(\">`n\")\n                        foreach ($tline in ($text.TrimEnd() -split \"`n\"))\n                        {\n                            [void]$sb.Append('> ' + $tline.TrimEnd() + \"`n\")\n                        }\n                    }\n                }\n            }\n\n            'tool_use'\n            {\n                if (-not $ex.Contains('tool-calls'))\n                {\n                    $name = $null\n                    $inputEl = [System.Text.Json.JsonElement]::new()\n                    try { $name = $block.GetProperty('name').GetString() } catch {}\n                    try { $inputEl = $block.GetProperty('input') }          catch {}\n\n                    $inputJson = ''\n                    if ($inputEl.ValueKind -ne [System.Text.Json.JsonValueKind]::Undefined -and\n                        $inputEl.ValueKind -ne [System.Text.Json.JsonValueKind]::Null)\n                    {\n                        $inputJson = [System.Text.Json.JsonSerializer]::Serialize(\n                            $inputEl, [System.Text.Json.JsonElement])\n                        if ($null -ne $maxToolLen -and $inputJson.Length -gt $maxToolLen)\n                        {\n                            $inputJson = $inputJson.Substring(0, $maxToolLen) + ' ... [truncated]'\n                        }\n                    }\n\n                    if ($sb.Length -gt 0) { [void]$sb.Append(\"`n\") }\n                    [void]$sb.Append(\"**[tool: $name]**`n\")\n                    [void]$sb.Append('```json' + \"`n\")\n                    [void]$sb.Append(\"$inputJson`n\")\n                    [void]$sb.Append('```')\n                }\n            }\n\n            'tool_result'\n            {\n                if (-not $ex.Contains('tool-results'))\n                {\n                    $toolUseId = $null\n                    $resultContent = [System.Text.Json.JsonElement]::new()\n                    try { $toolUseId = $block.GetProperty('tool_use_id').GetString() } catch {}\n                    try { $resultContent = $block.GetProperty('content') }              catch {}\n\n                    $resultText = ''\n                    if ($resultContent.ValueKind -eq [System.Text.Json.JsonValueKind]::String)\n                    {\n                        $resultText = $resultContent.GetString()\n                    }\n                    elseif ($resultContent.ValueKind -eq [System.Text.Json.JsonValueKind]::Array)\n                    {\n                        $parts = [System.Collections.Generic.List[string]]::new()\n                        foreach ($rb in $resultContent.EnumerateArray())\n                        {\n                            $rbType = $null\n                            try { $rbType = $rb.GetProperty('type').GetString() } catch {}\n                            if ($rbType -eq 'text')\n                            {\n                                $t = $null\n                                try { $t = $rb.GetProperty('text').GetString() } catch {}\n                                if ($t) { $parts.Add($t) }\n                            }\n                        }\n                        $resultText = $parts -join \"`n\"\n                    }\n\n                    $shortId = if ($toolUseId -and $toolUseId.Length -gt 16)\n                    { $toolUseId.Substring(0, 16) + '...' }\n                    else { $toolUseId }\n\n                    if ($sb.Length -gt 0) { [void]$sb.Append(\"`n\") }\n                    [void]$sb.Append(\"**[result: $shortId]**`n\")\n                    [void]$sb.Append('```' + \"`n\")\n                    [void]$sb.Append(\"$($resultText.TrimEnd())`n\")\n                    [void]$sb.Append('```')\n                }\n            }\n        }\n    }\n\n    $result = $sb.ToString()\n    if ($result.Trim().Length -gt 0) { return $result }\n    return $null\n} |
2 | jso-hash.ps1 | {5755 645 0.2810 4.7894} | 5946 | # jso-hash.ps1 — Hashing primitives for Claude Code sub-agents\n#\n# Dot-source this file to get the Phase 1 hashing surface:\n#\n#   . \"$env:USERPROFILE\\.claude\\tools\\jso-hash.ps1\"\n#\n# FUNCTIONS\n# ---------\n#   Find-StringPattern   Rabin-Karp exact substring search.\n#                        Use for QA anchor matching: locate a turn's first-8-words\n#                        anchor inside raw JSONL content without full scanning.\n#                        Returns array of 0-based match positions. Empty = no match.\n#\n#   Get-ContentHash      Rabin-Karp whole-content fingerprint (Int64).\n#                        Use for turn deduplication keys (Phase 2).\n#\n# CLASSES (available after dot-sourcing)\n# ----------------------------------------\n#   RabinKarpHash        Rolling hash engine. Base=257, Mod=1000000007.\n#                        AddChar / RemoveChar / RollWindow / Reset.\n#                        Used internally by Find-StringPattern and Get-ContentHash.\n#\n# DEPENDENCY\n# ----------\n#   None. Fully self-contained. No Import-Module, no dot-source chains.\n#\n# SOURCE\n# ------\n#   RabinKarpHash  — storage.psm1 (context-guardian), canonical bug-fixed version\n#   Find-StringPattern — rewritten against RabinKarpHash (replaces tooldig.readwrite\n#                        version which depended on hashing-primitives.psm1)\n# -----------------------------------------------------------------------\n\nclass RabinKarpHash\n{\n    [uint32] $Hash\n    [int]    $WindowSize\n    [uint32] $Base      = 257\n    [uint32] $Mod       = 1000000007\n    [uint32] $BasePower\n\n    RabinKarpHash([int]$windowSize)\n    {\n        $this.WindowSize = $windowSize\n        $this.Hash       = 0\n        $this.BasePower  = 1\n        for ($i = 0; $i -lt $windowSize; $i++)\n        {\n            # Cast to long before multiply — uint32*uint32 can exceed uint32 range\n            # at WindowSize > 5 (257^6 > 4.29B). Result fits back in uint32 after mod.\n            $this.BasePower = [uint32](([long]$this.BasePower * [long]$this.Base) % [long]$this.Mod)\n        }\n    }\n\n    [uint32] AddChar([char]$c)\n    {\n        $this.Hash = (($this.Hash * $this.Base) + [int]$c) % $this.Mod\n        return $this.Hash\n    }\n\n    # RollWindow: correct formula is hash = (hash*base - oldChar*base^m + newChar) mod mod\n    # BasePower = base^m (set in constructor), so this is a single composed operation.\n    # RemoveChar and AddChar are NOT composed here — composing them produces the wrong result\n    # because AddChar multiplies by base AFTER the removal, yielding base^(m+1) not base^m.\n    [uint32] RollWindow([char]$oldChar, [char]$newChar)\n    {\n        [long]$h        = ([long]$this.Hash * $this.Base) % $this.Mod\n        [long]$removal  = ([long][int]$oldChar * $this.BasePower) % $this.Mod\n        $h = ($h - $removal + [long]$this.Mod) % $this.Mod\n        $h = ($h + [int]$newChar) % $this.Mod\n        $this.Hash = [uint32]$h\n        return $this.Hash\n    }\n\n    [void] Reset() { $this.Hash = 0 }\n}\n\nfunction Get-ContentHash\n{\n    <#\n    .SYNOPSIS\n        Rabin-Karp whole-content fingerprint. Returns Int64.\n    .DESCRIPTION\n        Feeds every character through a RabinKarpHash roller and returns the\n        final hash value. Use as a fast deduplication key for turn content.\n    .PARAMETER Content\n        String to fingerprint.\n    .PARAMETER WindowSize\n        Rolling window size (default 32). Affects hash distribution, not correctness.\n    #>\n    [CmdletBinding()]\n    [OutputType([long])]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [AllowEmptyString()]\n        [string]$Content,\n\n        [int]$WindowSize = 32\n    )\n    process\n    {\n        if ([string]::IsNullOrWhiteSpace($Content)) { return 0L }\n        $roller = [RabinKarpHash]::new($WindowSize)\n        foreach ($char in $Content.ToCharArray()) { [void]$roller.AddChar($char) }\n        return [long]$roller.Hash\n    }\n}\n\nfunction Find-StringPattern\n{\n    <#\n    .SYNOPSIS\n        Rabin-Karp exact substring search. Returns 0-based match positions.\n    .DESCRIPTION\n        Searches Text for all occurrences of Pattern using rolling-hash comparison.\n        Returns an [int[]] of starting indices. Empty array = no match.\n\n        Primary use: QA anchor matching. Given an anchor string (first 8 words of a\n        turn), locate it inside a raw JSONL line without scanning full file content.\n\n        O(n + m) average case. Falls back to character comparison on hash collision\n        to eliminate false positives.\n    .PARAMETER Text\n        The string to search within.\n    .PARAMETER Pattern\n        The exact substring to find.\n    #>\n    [CmdletBinding()]\n    [OutputType([int[]])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Text,\n\n        [Parameter(Mandatory)]\n        [string]$Pattern\n    )\n\n    [int]$n = $Text.Length\n    [int]$m = $Pattern.Length\n    [int[]]$matches = @()\n\n    if ($m -eq 0 -or $m -gt $n) { return $matches }\n\n    $patternRoller = [RabinKarpHash]::new($m)\n    $textRoller    = [RabinKarpHash]::new($m)\n\n    # Build pattern hash and initial window hash.\n    for ($i = 0; $i -lt $m; $i++)\n    {\n        [void]$patternRoller.AddChar($Pattern[$i])\n        [void]$textRoller.AddChar($Text[$i])\n    }\n\n    [uint32]$patternHash = $patternRoller.Hash\n\n    for ($i = 0; $i -le $n - $m; $i++)\n    {\n        if ($textRoller.Hash -eq $patternHash)\n        {\n            # Hash match — verify character by character to rule out collision.\n            if ($Text.Substring($i, $m) -ceq $Pattern)\n            {\n                $matches += $i\n            }\n        }\n\n        # Roll window forward (skip on last iteration).\n        if ($i -lt $n - $m)\n        {\n            [void]$textRoller.RollWindow($Text[$i], $Text[$i + $m])\n        }\n    }\n\n    return $matches\n} |
