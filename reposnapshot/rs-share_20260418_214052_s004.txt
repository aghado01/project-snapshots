idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
7 | V3/rs.core.colonel-sanders.psm1 | {26245 2126 0.3439 4.4403} | 27014 | using namespace System\nusing namespace System.Collections.Concurrent\nusing namespace System.Management.Automation\nusing namespace System.Management.Automation.Runspaces\nusing namespace System.Threading\n\n#Requires -Version 7.6\nSet-StrictMode -Version Latest\n\nenum ResultMode\n{\n    Ordered     # results collected in item-submission order via pre-allocated index array\n    Unordered   # results collected as-completed into a ConcurrentBag; faster, no ordering guarantee\n    None        # no result collection; use for side-effect-only processors\n}\n\nenum IssPreset\n{\n    Bare     # CreateEmpty — bare PS engine, no built-ins; processor scripts are self-contained\n    Core     # CreateDefault2 — PS core cmdlets only; current default; good isolation/speed balance\n    Full     # CreateDefault — full module + provider set; use when processors need provider access\n}\n\nenum RunMode\n{\n    ApplyAll # broadcast one processor key to every item in the batch\n    KeyMatch # resolve processor key per item from a named property on each item object\n}\n\nclass RunspaceManager\n{\n\n    [hashtable]$Config\n    [nullable[int]]$WorkerCount\n    [int]$ReservedCores\n    [int]$WaitTimeoutMs\n    [ResultMode]$ResultMode\n    [double]$Tau  # time constant of dispatch cost curve; empirical critical point (items/worker at crossover) that determines the cutoff for serial only vs parallel execution; recalibrate if hardware or processor profile differs significantly\n\n    [double]$K # should have default empirical value derived before  # item count threshold per worker; defaults to Tau, user-overridable\n    [int]$InitThreads = 4  # max concurrent raw runspaces for parallel manifest bootstrap (PSOne-style)\n\n    [hashtable]$ProcessorManifest\n\n    [InitialSessionState]$Iss\n    [IssPreset]$IssPreset\n    [string[]]$IssModules\n\n    hidden [RunspacePool]$Pool\n    hidden [bool]$PoolIsOpen\n    hidden [ConcurrentBag[object]]$Collector\n    hidden $OrderedOutput  # untyped intentionally — PS unrolls [object[]] containing a single null on typed assignment\n\n    hidden [ConcurrentBag[string]]$Errors\n    hidden [ConcurrentBag[string]]$Warnings\n    hidden [ConcurrentBag[string]]$LifetimeErrors\n    hidden [ConcurrentBag[string]]$LifetimeWarnings\n    hidden [pscustomobject]$SystemCores  # null until first ResolveWorkerBudget call — lazy WMI\n    hidden [hashtable]$RunTiming\n\n    RunspaceManager([hashtable]$config = @{})\n    {\n        $this.Config = $config\n        $this.ProcessorManifest = @{}\n        $this.InitThreads = 4 # threads used for budgeting bootstrap and processor manifest loading; these are raw PS instances that bypass the pool because the pool's ISS construction is not thread-safe and would be a bottleneck in the bootstrap phase; once the manager is initialized and the manifest is loaded, the configured InitThreads is not relevant to runtime operation of the manager and can be set to null or any value without impact\n        $this.WorkerCount = $null # resolved in budget resolution; null means 'auto' based on system cores and item count, probably shoudln't be null\n        $this.ReservedCores = 2 # used in budget resolution\n        $this.WaitTimeoutMs = 60000\n        $this.ResultMode = [ResultMode]::Ordered\n        $this.Tau = 4.0 # tau is probably a misnomer, but is a critical point in the size of the batch\n        $this.K = $this.Tau #\n        $this.Iss = $null\n        $this.IssPreset = [IssPreset]::Core\n\n        $this.Errors = [ConcurrentBag[string]]::new()\n        $this.Warnings = [ConcurrentBag[string]]::new()\n        $this.LifetimeErrors = [ConcurrentBag[string]]::new()\n        $this.LifetimeWarnings = [ConcurrentBag[string]]::new()\n\n        $this.PoolIsOpen = $false\n        $this.RunTiming = @{}\n    }\n\n    hidden [void] ResetRunDiagnostics()\n    {\n        $this.Errors = [ConcurrentBag[string]]::new()\n        $this.Warnings = [ConcurrentBag[string]]::new()\n        $this.RunTiming = @{}\n    }\n\n    hidden [void] AddError([string]$message)\n    {\n        if ([string]::IsNullOrWhiteSpace($message)) { return }\n        $this.Errors.Add($message)\n        $this.LifetimeErrors.Add($message)\n    }\n\n    hidden [void] AddWarning([string]$message)\n    {\n        if ([string]::IsNullOrWhiteSpace($message)) { return }\n        $this.Warnings.Add($message)\n        $this.LifetimeWarnings.Add($message)\n    }\n\n    [RunspaceManager] SetWorkerCount([nullable[int]]$count)\n    {\n        if ($null -ne $count -and $count -lt 1) { throw 'WorkerCount must be >= 1 or null.' }\n        $this.WorkerCount = $count\n        return $this\n    }\n\n    [RunspaceManager] SetReservedCores([int]$count)\n    {\n        if ($count -lt 0) { throw 'ReservedCores must be >= 0.' }\n        $this.ReservedCores = $count\n        return $this\n    }\n\n    [RunspaceManager] SetWaitTimeoutMs([int]$ms)\n    {\n        if ($ms -lt 1) { throw 'WaitTimeoutMs must be >= 1.' }\n        $this.WaitTimeoutMs = $ms\n        return $this\n    }\n\n    [RunspaceManager] SetTau([double]$tau)\n    {\n        if ($tau -le 0) { throw 'Tau must be > 0.' }\n        $this.Tau = $tau\n        return $this\n    }\n\n    [RunspaceManager] SetK([double]$k)\n    {\n        if ($k -lt 0) { throw 'K must be >= 0.' }\n        $this.K = $k\n        return $this\n    }\n\n    [RunspaceManager] SetInitThreads([int]$n)\n    {\n        if ($n -lt 1) { throw 'InitThreads must be >= 1.' }\n        $this.InitThreads = $n\n        return $this\n    }\n\n    [RunspaceManager] SetIssPreset([IssPreset]$preset)\n    {\n        $this.IssPreset = $preset\n        return $this\n    }\n\n    [RunspaceManager] SetIssModules([string[]]$modules)\n    {\n        $this.IssModules = $modules ?? @()\n        return $this\n    }\n\n    [RunspaceManager] SetResultMode([ResultMode]$mode)\n    {\n        $this.ResultMode = $mode\n        return $this\n    }\n\n    [RunspaceManager] SetProcessorManifest([hashtable]$manifest)\n    {\n        $this.ProcessorManifest = @{} + ($manifest ?? @{})\n        return $this\n    }\n\n    [RunspaceManager] Initialize([hashtable]$processorPaths)\n    {\n        $null = $this.LoadProcessorsFromManifest($processorPaths)\n        return $this\n    }\n\n    hidden [InitialSessionState] BuildIss()\n    {\n\n        $issState = switch ($this.IssPreset)\n        {\n            ([IssPreset]::Bare) { [InitialSessionState]::CreateEmpty() }\n            ([IssPreset]::Core) { [InitialSessionState]::CreateDefault2() }\n            ([IssPreset]::Full) { [InitialSessionState]::CreateDefault() }\n            default { [InitialSessionState]::CreateDefault2() }\n        }\n        foreach ($mod in $this.IssModules)\n        {\n            if (-not [string]::IsNullOrWhiteSpace($mod))\n            {\n                $issState.ImportPSModule($mod)\n            }\n        }\n        return $issState\n    }\n\n    [RunspaceManager] LoadProcessorsFromManifest([hashtable]$filePaths)\n    {\n        if ($null -eq $filePaths -or $filePaths.Count -eq 0)\n        {\n            $this.AddWarning('Processor manifest was empty; no processors were loaded.')\n            return $this\n        }\n\n        $readScript = {\n            param([string]$key, [string]$path)\n            $result = @{ Key = $key; Fn = \"Invoke-$key\"; Body = $null; Error = $null }\n            try\n            {\n                if ([string]::IsNullOrWhiteSpace($key)) { $result.Error = 'Processor key cannot be empty.'; return $result }\n                if ([string]::IsNullOrWhiteSpace($path)) { $result.Error = 'Processor path cannot be empty.'; return $result }\n                if (-not (Test-Path -LiteralPath $path)) { $result.Error = \"Processor script not found: $path\"; return $result }\n\n                $body = Get-Content -LiteralPath $path -Raw\n\n                if ($body -match '(?m)^\\s*#\\s*Requires\\b')\n                {\n                    $result.Error = 'Processor scripts loaded into ISS must not declare #Requires.'\n                    return $result\n                }\n                if ($body -match '(?m)^\\s*function\\s+[A-Za-z0-9_-]+\\b')\n                {\n                    $result.Error = 'Processor scripts loaded into ISS must contain a function body with (Item, Config) parameters, not an outer function declaration.'\n                    return $result\n                }\n\n                $result.Body = $body\n            }\n            catch\n            {\n                $result.Error = $_.Exception.Message\n            }\n            return $result\n        }\n\n        $entries = @($filePaths.GetEnumerator())\n\n        $batches = [System.Collections.Generic.List[object[]]]::new()\n        for ($i = 0; $i -lt $entries.Count; $i += $this.InitThreads)\n        {\n            $end = [Math]::Min($i + $this.InitThreads, $entries.Count) - 1\n            $batches.Add($entries[$i..$end])\n        }\n\n        $loadedBodies = [System.Collections.Generic.List[hashtable]]::new($entries.Count)\n\n        foreach ($batch in $batches)\n        {\n            $wave = [System.Collections.Generic.List[hashtable]]::new($batch.Count)\n            foreach ($entry in $batch)\n            {\n                $ps = [PowerShell]::Create()\n                $null = $ps.AddScript($readScript).AddArgument([string]$entry.Key).AddArgument([string]$entry.Value)\n                $async = $ps.BeginInvoke()\n                $wave.Add(@{ PS = $ps; Async = $async })\n            }\n            foreach ($reader in $wave)\n            {\n                try\n                {\n                    $resultList = $reader.PS.EndInvoke($reader.Async)\n                    if ($resultList -and $resultList.Count -gt 0) { $loadedBodies.Add($resultList[0]) }\n                }\n                catch\n                {\n                    $this.AddWarning(\"Bootstrap reader failed: $($_.Exception.Message)\")\n                }\n                finally\n                {\n                    $reader.PS.Dispose()\n                }\n            }\n        }\n\n        $sessionState = $this.BuildIss()\n        $fnMap = @{}\n\n        foreach ($loaded in $loadedBodies)\n        {\n            if ($loaded.Error)\n            {\n                $this.AddWarning(\"Processor '$($loaded.Key)' was skipped: $($loaded.Error)\")\n                continue\n            }\n            $sessionState.Commands.Add([SessionStateFunctionEntry]::new($loaded.Fn, $loaded.Body))\n            $fnMap[$loaded.Key] = $loaded.Fn\n        }\n\n        if ($fnMap.Count -eq 0)\n        {\n            $this.AddWarning('No processors were successfully loaded from the manifest.')\n        }\n\n        $this.Iss = $sessionState\n        $this.ProcessorManifest = $fnMap\n        return $this\n    }\n\n    [pscustomobject] Run([object[]]$items, [string]$processorKey)\n    {\n        return $this.Run($items, [RunMode]::ApplyAll, $processorKey)\n    }\n\n    [pscustomobject] Run([object[]]$items, [RunMode]$mode, [string]$key)\n    {\n        $swTotal = [System.Diagnostics.Stopwatch]::StartNew()\n        try\n        {\n            $this.ResetRunDiagnostics()\n            if ($null -eq $items) { $items = @() }\n\n            $budget = $this.ResolveWorkerBudget($items.Count)\n\n            $this.OrderedOutput = $null\n            if ($this.ResultMode -eq [ResultMode]::Ordered) { $this.OrderedOutput = [object[]]::new($items.Count) }\n            if ($this.ResultMode -eq [ResultMode]::Unordered) { $this.Collector = [ConcurrentBag[object]]::new() }\n\n            $configForRun = $this.Config?.Clone() ?? @{}\n            $this.RunCore($items, $mode, $key, $budget.Threads, $configForRun)\n\n            $results = $null\n            if ($this.ResultMode -eq [ResultMode]::Ordered)\n            {\n                $results = $this.OrderedOutput\n            }\n            elseif ($this.ResultMode -eq [ResultMode]::Unordered)\n            {\n                $results = if ($this.Collector) { $this.Collector.ToArray() } else { @() }\n            }\n\n            $this.RunTiming['TotalMs'] = $swTotal.ElapsedMilliseconds\n\n            $processorField = $key\n\n            return [pscustomobject]@{\n                DispatchMode     = $mode\n                Processor        = $processorField\n                Budget           = $budget\n                Results          = $results\n                Timing           = [pscustomobject]$this.RunTiming\n                Warnings         = @($this.Warnings.ToArray())\n                Errors           = @($this.Errors.ToArray())\n                LifetimeWarnings = @($this.LifetimeWarnings.ToArray())\n                LifetimeErrors   = @($this.LifetimeErrors.ToArray())\n            }\n        }\n        finally\n        {\n            $this.ClosePool()\n        }\n    }\n\n    hidden [void] RunCore([object[]]$items, [RunMode]$mode, [string]$selectorProperty, [int]$threads, [hashtable]$config)\n    {\n        $count = $items.Count\n        if ($count -eq 0) { return }\n\n        $fixedFn = $null\n        if ($mode -eq [RunMode]::ApplyAll)\n        {\n            if (-not ($this.ProcessorManifest -and $this.ProcessorManifest.ContainsKey($selectorProperty)))\n            {\n                throw \"Processor key '$selectorProperty' not found in ProcessorManifest.\"\n            }\n            $fixedFn = [string]$this.ProcessorManifest[$selectorProperty]\n        }\n\n        if ($threads -eq 1)\n        {\n            $this.RunCoreSerial($items, $mode, $selectorProperty, $fixedFn, $config)\n        }\n        else\n        {\n            $this.RunCoreParallel($items, $mode, $selectorProperty, $fixedFn, $threads, $config)\n        }\n    }\n\n    hidden [void] RunCoreSerial([object[]]$items, [RunMode]$mode, [string]$selectorProperty, [string]$fixedFn, [hashtable]$config)\n    {\n        $count = $items.Count\n\n        $issToUse = if ($this.Iss) { $this.Iss } else { $this.BuildIss() }\n        $rs = [RunspaceFactory]::CreateRunspace($issToUse)\n        $rs.ThreadOptions = [PSThreadOptions]::UseNewThread\n        $rs.ApartmentState = [ApartmentState]::MTA\n\n        $swOpen = [System.Diagnostics.Stopwatch]::StartNew()\n        $rs.Open()\n        $this.RunTiming['SerialRunspaceOpenMs'] = $swOpen.ElapsedMilliseconds\n\n        $ps = [PowerShell]::Create()\n        $ps.Runspace = $rs\n\n        $fnMap = $this.ProcessorManifest\n\n        $swProcess = [System.Diagnostics.Stopwatch]::StartNew()\n        try\n        {\n            for ($i = 0; $i -lt $count; $i++)\n            {\n                $ps.Commands.Clear()\n\n                if ($mode -eq [RunMode]::ApplyAll)\n                {\n                    $null = $ps.AddScript({\n                            param($fn, $item, $cfg)\n                            & $fn $item $cfg\n                        }).AddArgument($fixedFn).AddArgument($items[$i]).AddArgument($config)\n                }\n                else\n                {\n\n                    $resolvedFn = $fnMap[$items[$i].$selectorProperty]\n                    if (-not $resolvedFn)\n                    {\n                        $this.AddError(\"Item [$i]: no processor resolved for key property '$selectorProperty' = '$($items[$i].$selectorProperty)'\")\n                        if ($this.ResultMode -eq [ResultMode]::Ordered) { $this.OrderedOutput[$i] = $null }\n                        continue\n                    }\n                    $null = $ps.AddScript({\n                            param($fn, $item, $cfg)\n                            & $fn $item $cfg\n                        }).AddArgument($resolvedFn).AddArgument($items[$i]).AddArgument($config)\n                }\n\n                try\n                {\n                    $resultList = $ps.Invoke()\n                    $r = if ($resultList -and $resultList.Count -gt 0) { $resultList[0] } else { $null }\n\n                    if ($this.ResultMode -eq [ResultMode]::Ordered) { $this.OrderedOutput[$i] = $r }\n                    elseif ($this.ResultMode -eq [ResultMode]::Unordered -and $null -ne $r) { $this.Collector.Add($r) }\n\n                    if ($ps.HadErrors)\n                    {\n                        $msgs = @($ps.Streams.Error | ForEach-Object { $_.ToString() }) -join '; '\n                        $this.AddError(\"Item [$i]: $msgs\")\n                    }\n                    $ps.Streams.ClearStreams()\n                }\n                catch\n                {\n                    $this.AddError(\"Item [$i]: $($_.Exception.Message)\")\n                }\n            }\n        }\n        finally\n        {\n            $this.RunTiming['SerialProcessMs'] = $swProcess.ElapsedMilliseconds\n            $ps.Dispose()\n            $rs.Close()\n            $rs.Dispose()\n        }\n    }\n\n    hidden [void] RunCoreParallel([object[]]$items, [RunMode]$mode, [string]$selectorProperty, [string]$fixedFn, [int]$threads, [hashtable]$config)\n    {\n        $count = $items.Count\n\n        $swPool = [System.Diagnostics.Stopwatch]::StartNew()\n        $this.OpenPool($threads)\n        $this.RunTiming['PoolOpenMs'] = $swPool.ElapsedMilliseconds\n\n        $sliceLists = [System.Collections.Generic.List[object][]]::new($threads)\n        $sliceIndices = [System.Collections.Generic.List[int][]]::new($threads)\n        for ($t = 0; $t -lt $threads; $t++)\n        {\n            $sliceLists[$t] = [System.Collections.Generic.List[object]]::new()\n            $sliceIndices[$t] = [System.Collections.Generic.List[int]]::new()\n        }\n        for ($i = 0; $i -lt $count; $i++)\n        {\n            $slot = $i % $threads\n            $sliceLists[$slot].Add($items[$i])\n            $sliceIndices[$slot].Add($i)\n        }\n\n        $workers = [System.Collections.Generic.List[object]]::new()\n        $handles = [System.Collections.Generic.List[WaitHandle]]::new()\n\n        $orderedRef = $this.OrderedOutput   # may be null if ResultMode != Ordered\n        $errorBagRef = $this.Errors\n        $collRef = $this.Collector       # may be null if ResultMode != Unordered\n\n        $swDispatch = [System.Diagnostics.Stopwatch]::StartNew()\n        for ($w = 0; $w -lt $threads; $w++)\n        {\n            $slice = $sliceLists[$w].ToArray()\n            if (-not $slice -or $slice.Length -eq 0) { continue }\n            $idxs = $sliceIndices[$w].ToArray()\n\n            $ps = [PowerShell]::Create()\n\n            $null = $ps.AddScript({\n                    param(\n                        [string]$fixedFn,\n                        [string]$keyProp,\n                        [hashtable]$fnMap,\n                        [object[]]$mySlice,\n                        [int[]]$myIdxs,\n                        [hashtable]$cfg,\n                        [object[]]$orderedOut,\n                        [System.Collections.Concurrent.ConcurrentBag[string]]$errBag,\n                        $bag\n                    )\n                    for ($i = 0; $i -lt $mySlice.Length; $i++)\n                    {\n                        try\n                        {\n\n                            $fn = if ($keyProp) { $fnMap[$mySlice[$i].$keyProp] } else { $fixedFn }\n                            if (-not $fn)\n                            {\n                                $errBag.Add(\"Item [$($myIdxs[$i])]: no processor resolved for key property '$keyProp' = '$($mySlice[$i].$keyProp)'\")\n                                continue\n                            }\n\n                            $Error.Clear()\n                            $r = & $fn $mySlice[$i] $cfg\n                            if ($null -ne $orderedOut) { $orderedOut[$myIdxs[$i]] = $r }\n                            if ($null -ne $bag -and $null -ne $r) { $bag.Add($r) }\n                            if ($Error.Count -gt 0)\n                            {\n                                $msgs = ($Error | ForEach-Object { $_.ToString() }) -join '; '\n                                $errBag.Add(\"Item [$($myIdxs[$i])]: $msgs\")\n                                $Error.Clear()\n                            }\n                        }\n                        catch\n                        {\n                            $errBag.Add(\"Item [$($myIdxs[$i])]: $($_.Exception.Message)\")\n                        }\n                    }\n                }).AddArgument($fixedFn).\n            AddArgument($(if ($mode -eq [RunMode]::KeyMatch) { $selectorProperty }         else { $null })).\n            AddArgument($(if ($mode -eq [RunMode]::KeyMatch) { $this.ProcessorManifest } else { $null })).\n            AddArgument($slice).\n            AddArgument($idxs).\n            AddArgument($config).\n            AddArgument($orderedRef).\n            AddArgument($errorBagRef).\n            AddArgument($collRef)\n\n            $ps.RunspacePool = $this.Pool\n            $async = $ps.BeginInvoke()\n            $handles.Add($async.AsyncWaitHandle)\n            $ps | Add-Member -NotePropertyName _async -NotePropertyValue $async\n            $workers.Add($ps)\n        }\n        $this.RunTiming['DispatchMs'] = $swDispatch.ElapsedMilliseconds\n\n        $swWait = [System.Diagnostics.Stopwatch]::StartNew()\n        if ($handles.Count -gt 0)\n        {\n            $ok = $this.WaitAllHandles($handles.ToArray(), $this.WaitTimeoutMs)\n            if (-not $ok) { $this.AddError('Timeout waiting for one or more workers.') }\n        }\n        $this.RunTiming['WaitMs'] = $swWait.ElapsedMilliseconds\n\n        $swCollect = [System.Diagnostics.Stopwatch]::StartNew()\n        foreach ($ps in $workers)\n        {\n            try\n            {\n                if ($ps._async.IsCompleted)\n                {\n                    $null = $ps.EndInvoke($ps._async)\n                    if ($ps.HadErrors)\n                    {\n                        $messages = @($ps.Streams.Error | ForEach-Object { $_.ToString() }) -join '; '\n                        $this.AddError(\"Worker error: $messages\")\n                    }\n                }\n                else\n                {\n                    try { $ps.Stop() } catch {}\n                    $this.AddError('Worker did not complete before timeout; stopped.')\n                }\n            }\n            catch\n            {\n                $this.AddError($_.ToString())\n            }\n            finally\n            {\n                $ps.Dispose()\n            }\n        }\n        $this.RunTiming['CollectMs'] = $swCollect.ElapsedMilliseconds\n    }\n\n    hidden [pscustomobject] ResolveWorkerBudget([int]$itemCount)\n    {\n        if ($null -eq $this.SystemCores) { $this.SystemCores = $this.GetSystemCores() }\n        $logical = [int][Math]::Max(1, $this.SystemCores.LogicalCores)\n        $effectiveReserved = $this.ReservedCores\n\n        if ($effectiveReserved -ge $logical)\n        {\n            $clampedReserved = [Math]::Max(0, $logical - 1)\n            $this.AddWarning(\n                \"ReservedCores ($effectiveReserved) leaves no worker capacity for $logical logical cores; clamping reserve to $clampedReserved.\")\n            $effectiveReserved = $clampedReserved\n        }\n\n        $threads = if ($null -ne $this.WorkerCount)\n        {\n            [int]$this.WorkerCount\n        }\n        else\n        {\n            [Math]::Max(1, $logical - $effectiveReserved)\n        }\n\n        if ($null -ne $this.WorkerCount -and $this.WorkerCount -gt $logical)\n        {\n            $this.AddWarning(\n                \"WorkerCount ($($this.WorkerCount)) exceeds logical core count ($logical); clamping to $logical.\")\n        }\n\n        $threads = [Math]::Max(1, $threads)\n        $threads = [Math]::Min($threads, $logical)\n\n        if ($itemCount -gt 0)\n        {\n            $threads = [Math]::Min($threads, $itemCount)\n        }\n\n        if ($itemCount -gt 0 -and $this.K -gt 0)\n        {\n            $graded = [Math]::Max(1, [int][Math]::Ceiling($itemCount / $this.K))\n            $threads = [Math]::Min($threads, $graded)\n        }\n\n        return [pscustomobject]@{\n            Threads       = $threads\n            ItemCount     = $itemCount\n            Tau           = $this.Tau\n            K             = $this.K\n            ReservedCores = $effectiveReserved\n            LogicalCores  = $logical\n            PhysicalCores = [int]$this.SystemCores.PhysicalCores\n            Policy        = if ($null -ne $this.WorkerCount) { 'ExplicitWorkerCount' } else { 'LogicalMinusReservedCores' }\n        }\n    }\n\n    hidden [pscustomobject] GetSystemCores()\n    {\n        try\n        {\n            $cpu = Get-CimInstance -ClassName Win32_Processor -ErrorAction Stop\n            $physical = ($cpu | Measure-Object -Property NumberOfCores -Sum).Sum\n            $logical = ($cpu | Measure-Object -Property NumberOfLogicalProcessors -Sum).Sum\n        }\n        catch\n        {\n            $logical = [int]($env:NUMBER_OF_PROCESSORS ?? [Environment]::ProcessorCount)\n            $physical = $logical\n            $this.AddWarning('Fell back to environment processor count because Win32_Processor query failed.')\n        }\n        if (-not $physical -or $physical -le 0) { $physical = $logical }\n        if (-not $logical -or $logical -le 0) { $logical = $physical }\n        return [pscustomobject]@{\n            PhysicalCores = [int]$physical\n            LogicalCores  = [int]$logical\n        }\n    }\n\n    hidden [void] OpenPool([int]$threads)\n    {\n        if ($this.PoolIsOpen) { return }\n        $issToUse = if ($this.Iss) { $this.Iss } else { $this.BuildIss() }\n        $this.Pool = [RunspaceFactory]::CreateRunspacePool($issToUse)\n        $null = $this.Pool.SetMinRunspaces(1)\n        $null = $this.Pool.SetMaxRunspaces($threads)\n        $this.Pool.ThreadOptions = [PSThreadOptions]::UseNewThread\n        $this.Pool.ApartmentState = [ApartmentState]::MTA\n        $this.Pool.Open()\n        $this.PoolIsOpen = $true\n    }\n\n    hidden [void] ClosePool()\n    {\n        if (-not $this.PoolIsOpen -or -not $this.Pool) { return }\n        try\n        {\n            $this.Pool.Close()\n            $this.Pool.Dispose()\n        }\n        catch\n        {\n            $this.AddError(\"Pool cleanup error: $($_.ToString())\")\n        }\n        finally\n        {\n            $this.Pool = $null\n            $this.PoolIsOpen = $false\n        }\n    }\n\n    hidden [bool] WaitAllHandles([WaitHandle[]]$handles, [int]$timeoutMs)\n    {\n        $allOk = $true\n        $sw = [System.Diagnostics.Stopwatch]::StartNew()\n        foreach ($h in $handles)\n        {\n            $remaining = [Math]::Max(50, $timeoutMs - [int]$sw.ElapsedMilliseconds)\n            if (-not $h.WaitOne($remaining)) { $allOk = $false }\n        }\n        return $allOk\n    }\n\n    [void] Dispose() { $this.ClosePool() }\n}\n\nfunction New-RunspaceManager\n{\n    param([hashtable]$Config = @{})\n    [RunspaceManager]::new($Config)\n}\n\nExport-ModuleMember -Function @(\n    'New-RunspaceManager'\n) |
