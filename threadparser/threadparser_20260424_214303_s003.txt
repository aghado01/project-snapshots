idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
2 | v1/threadparser.psm1 | {44265 3330 0.3259 4.6210} | 45689 | using namespace System.Text\nusing namespace System.Text.RegularExpressions\nusing namespace System.Collections.Generic\nusing namespace System.IO\n\nusing module .\\threadparser-primitives.psm1\n\n#Requires -Version 7.5\nSet-StrictMode -Version Latest\n\nclass PreProcessOp {\n    [string]$Name\n    [string]$Description\n    [regex]$Pattern          # precompiled regex (null for non-regex ops)\n    [scriptblock]$Transform  # $_ = input text, returns transformed text\n\n    PreProcessOp([string]$name, [string]$desc, [regex]$pattern, [scriptblock]$transform) {\n        $this.Name = $name\n        $this.Description = $desc\n        $this.Pattern = $pattern\n        $this.Transform = $transform\n    }\n\n    PreProcessOp([string]$name, [string]$desc, [scriptblock]$transform) {\n        $this.Name = $name\n        $this.Description = $desc\n        $this.Pattern = $null\n        $this.Transform = $transform\n    }\n}\n\nclass PreProcessor {\n\n    static [Dictionary[string, PreProcessOp]]$Registry\n\n    static PreProcessor() {\n        [PreProcessor]::Registry = [Dictionary[string, PreProcessOp]]::new(\n            [StringComparer]::OrdinalIgnoreCase)\n\n        [PreProcessor]::Register('NormalizeLineEndings',\n            'CRLF/CR to LF for consistent regex matching',\n            { param($t) $t -replace \"`r`n\", \"`n\" -replace \"`r\", \"`n\" })\n\n        [PreProcessor]::Register('StripBOM',\n            'Remove UTF-8 BOM that browser exports leave',\n            { param($t) if ($t.Length -gt 0 -and [int]$t[0] -eq 0xFEFF) { $t.Substring(1) } else { $t } })\n\n        [PreProcessor]::Register('NormalizeUnicode',\n            'Unicode NFC normalization for consistent matching',\n            { param($t) try { $t.Normalize([NormalizationForm]::FormC) } catch { $t } })\n\n        [PreProcessor]::Register('TrimTrailingWhitespace',\n            'Per-line trailing whitespace removal for reliable boundary detection',\n            [regex]::new('[ \\t]+$', [RegexOptions]::Compiled -bor [RegexOptions]::Multiline),\n            { param($t) [PreProcessor]::Registry['TrimTrailingWhitespace'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('CollapseExcessiveBlanks',\n            'Collapse 3+ blank lines to 2 — preserves single and double empty lines',\n            [regex]::new('\\n{3,}', [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['CollapseExcessiveBlanks'].Pattern.Replace($t, \"`n`n\") })\n\n        [PreProcessor]::Register('StripHtmlComments',\n            'Remove <!-- --> blocks that pollute turn boundary detection',\n            [regex]::new('(?s)<!--.*?-->', [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['StripHtmlComments'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('StripScriptStyle',\n            'Remove <script> and <style> blocks',\n            [regex]::new('(?is)<(script|style)\\b[^>]*>.*?</\\1>', [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['StripScriptStyle'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('StripHiddenSpans',\n            'Remove display:none spans (hidden citation containers)',\n            [regex]::new(\n                '(?is)<span\\b[^>]*\\bstyle\\s*=\\s*([\"\\x27])(?:(?!\\1).)*\\bdisplay\\s*:\\s*none\\b(?:(?!\\1).)*\\1[^>]*>.*?</span>',\n                [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['StripHiddenSpans'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('StripImages',\n            'Remove <img> tags (logos, embedded images)',\n            [regex]::new('(?is)<img\\b[^>]*/?>', [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['StripImages'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('ConvertBrToNewline',\n            'Convert <br> tags to newlines',\n            [regex]::new('(?i)<br\\s*/?>', [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['ConvertBrToNewline'].Pattern.Replace($t, \"`n\") })\n\n        [PreProcessor]::Register('StripRemainingHtml',\n            'Remove any remaining HTML tags (last resort, after targeted stripping)',\n            [regex]::new('<[^>]+>', [RegexOptions]::Compiled),\n            { param($t) [PreProcessor]::Registry['StripRemainingHtml'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('TrimDocument',\n            'Remove leading/trailing whitespace from entire document',\n            { param($t) $t.Trim() })\n\n        [PreProcessor]::Register('StripTimingLines',\n            'Remove Grok-style NNNms timing lines at end of responses',\n            [regex]::new('(?m)^\\d+ms\\s*$', [RegexOptions]::Compiled -bor [RegexOptions]::Multiline),\n            { param($t) [PreProcessor]::Registry['StripTimingLines'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('StripCopilotToolUse',\n            'Remove Copilot VS Code tool-use interleaving lines (Searched for, Read [...], Ran terminal)',\n            [regex]::new(\n                '(?m)^(?:Searched\\s+for\\s+.+|Read\\s+\\[.+\\]|Ran\\s+terminal\\s+command(?:\\s*:.*)?)[ \\t]*$',\n                [RegexOptions]::Compiled -bor [RegexOptions]::Multiline),\n            { param($t) [PreProcessor]::Registry['StripCopilotToolUse'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('StripCopilotUsedTools',\n            'Remove Copilot Used N tools lines',\n            [regex]::new('(?m)^Used\\s+\\d+\\s+tools?\\s*$', [RegexOptions]::Compiled -bor [RegexOptions]::Multiline),\n            { param($t) [PreProcessor]::Registry['StripCopilotUsedTools'].Pattern.Replace($t, '') })\n\n        [PreProcessor]::Register('StripRoleMarkers',\n            'Remove explicit role prefix markers (User:, GitHub Copilot:, Assistant:, etc.)',\n            [regex]::new(\n                '(?m)^(?:User|GitHub\\s+Copilot|Assistant|Human|AI):\\s*',\n                [RegexOptions]::Compiled -bor [RegexOptions]::Multiline),\n            { param($t) [PreProcessor]::Registry['StripRoleMarkers'].Pattern.Replace($t, '') })\n    }\n\n    static [void] Register([string]$name, [string]$desc, [scriptblock]$transform) {\n        [PreProcessor]::Registry[$name] = [PreProcessOp]::new($name, $desc, $transform)\n    }\n\n    static [void] Register([string]$name, [string]$desc, [regex]$pattern, [scriptblock]$transform) {\n        [PreProcessor]::Registry[$name] = [PreProcessOp]::new($name, $desc, $pattern, $transform)\n    }\n\n    static [string] Apply([string]$text, [string[]]$ops) {\n        foreach ($opName in $ops) {\n            if (-not [PreProcessor]::Registry.ContainsKey($opName)) {\n                Write-Warning \"PreProcessor: unknown op '$opName' — skipping\"\n                continue\n            }\n            $op = [PreProcessor]::Registry[$opName]\n            $text = & $op.Transform $text\n        }\n        return $text\n    }\n\n    static [string[]] ListOps() {\n        return @([PreProcessor]::Registry.Keys | Sort-Object)\n    }\n}\n\nclass ContentFilter {\n\n    [regex]$IncludeRegex    # $null = accept all\n    [regex]$ExcludeRegex    # $null = exclude none\n\n    ContentFilter([string[]]$Include, [string[]]$Exclude) {\n\n        $includeNormalized = [HashSet[string]]::new([StringComparer]::OrdinalIgnoreCase)\n        $excludeNormalized = [HashSet[string]]::new([StringComparer]::OrdinalIgnoreCase)\n\n        if ($Include) {\n            foreach ($raw in $Include) {\n                $n = $this.NormalizeGlob($raw)\n                if ($null -ne $n) { [void]$includeNormalized.Add($n) }\n            }\n        }\n\n        if ($Exclude) {\n            foreach ($raw in $Exclude) {\n                $n = $this.NormalizeGlob($raw)\n                if ($null -ne $n) { [void]$excludeNormalized.Add($n) }\n            }\n        }\n\n        $annihilated = @($includeNormalized | Where-Object { $excludeNormalized.Contains($_) })\n        foreach ($a in $annihilated) {\n            [void]$includeNormalized.Remove($a)\n            [void]$excludeNormalized.Remove($a)\n        }\n\n        $this.IncludeRegex = $this.CompileGlobs(@($includeNormalized))\n        $this.ExcludeRegex = $this.CompileGlobs(@($excludeNormalized))\n    }\n\n    [bool] Test([string]$fileName) {\n\n        if ($null -ne $this.IncludeRegex) {\n            if (-not $this.IncludeRegex.IsMatch($fileName)) {\n                return $false\n            }\n        }\n\n        if ($null -ne $this.ExcludeRegex) {\n            if ($this.ExcludeRegex.IsMatch($fileName)) {\n                return $false\n            }\n        }\n\n        return $true\n    }\n\n    hidden [string] NormalizeGlob([string]$raw) {\n        $p = $raw.Trim()\n        if ([string]::IsNullOrWhiteSpace($p)) { return $null }\n\n        if ($p.StartsWith('#')) { return $null }\n\n        $p = [regex]::Replace($p, '[/\\\\]+', '/')\n\n        if ([string]::IsNullOrWhiteSpace($p) -or $p -eq '/') {\n            Write-Warning \"ContentFilter: discarding degenerate pattern '$raw'\"\n            return $null\n        }\n\n        return $p\n    }\n\n    hidden [string] TranslateGlob([string]$glob) {\n\n        $chars = $glob.ToCharArray()\n        $len = $chars.Count\n        $sb = [System.Text.StringBuilder]::new($len * 2)\n        $i = 0\n\n        while ($i -lt $len) {\n            $c = $chars[$i]\n            switch ($c) {\n                '*' {\n                    if (($i + 1) -lt $len -and $chars[$i + 1] -eq '*') {\n                        $i += 2\n                        if ($i -lt $len -and $chars[$i] -eq '/') {\n                            $i++\n                            [void]$sb.Append('(.+/)?')\n                        }\n                        elseif ($i -eq $len) {\n                            [void]$sb.Append('.*')\n                        }\n                        else {\n                            [void]$sb.Append('.*')\n                        }\n                        continue\n                    }\n                    [void]$sb.Append('[^/]*')\n                    $i++\n                }\n                '?' {\n                    [void]$sb.Append('[^/]')\n                    $i++\n                }\n                '[' {\n                    [void]$sb.Append('[')\n                    $i++\n                    if ($i -lt $len -and $chars[$i] -eq '!') {\n                        [void]$sb.Append('^')\n                        $i++\n                    }\n                    while ($i -lt $len -and $chars[$i] -ne ']') {\n                        if ($chars[$i] -eq '\\' -and ($i + 1) -lt $len) {\n                            [void]$sb.Append('\\')\n                            $i++\n                            [void]$sb.Append($chars[$i])\n                            $i++\n                        }\n                        else {\n                            [void]$sb.Append($chars[$i])\n                            $i++\n                        }\n                    }\n                    if ($i -lt $len) {\n                        [void]$sb.Append(']')\n                        $i++\n                    }\n                }\n                '\\' {\n                    $i++\n                    if ($i -lt $len) {\n                        [void]$sb.Append([regex]::Escape([string]$chars[$i]))\n                    }\n                    $i++\n                }\n                '.' { [void]$sb.Append('\\.'); $i++ }\n                '+' { [void]$sb.Append('\\+'); $i++ }\n                '^' { [void]$sb.Append('\\^'); $i++ }\n                '$' { [void]$sb.Append('\\$'); $i++ }\n                '{' { [void]$sb.Append('\\{'); $i++ }\n                '}' { [void]$sb.Append('\\}'); $i++ }\n                '(' { [void]$sb.Append('\\('); $i++ }\n                ')' { [void]$sb.Append('\\)'); $i++ }\n                '|' { [void]$sb.Append('\\|'); $i++ }\n                '/' { [void]$sb.Append('/'); $i++ }\n                default {\n                    [void]$sb.Append($c)\n                    $i++\n                }\n            }\n        }\n\n        return \"^$($sb.ToString())$\"\n    }\n\n    hidden [regex] CompileGlobs([string[]]$globs) {\n        if (-not $globs -or $globs.Count -eq 0) { return $null }\n\n        $fragments = foreach ($g in $globs) {\n            $this.TranslateGlob($g)\n        }\n\n        $combined = '(' + ($fragments -join '|') + ')'\n        return [regex]::new($combined,\n            [RegexOptions]::Compiled -bor [RegexOptions]::IgnoreCase)\n    }\n}\n\nclass ThreadFormat {\n    [string]$Name\n    [string]$Description\n\n    [regex]$FormatDetector        # matches raw input to identify this format\n    [regex]$TurnSplitter          # splits document into turn sections\n    [regex]$PromptExtractor       # extracts prompt text from a turn section\n    [regex]$ResponsePrefix        # optional: stripped from start of response body (e.g. \"GitHub Copilot:\")\n    [regex]$DividerPattern        # provider-specific divider markup\n    [string]$DividerReplacement   # canonical replacement token\n    [regex]$ReferenceDetector     # detects start of reference/citation block\n    [regex]$FootnotePattern       # matches individual footnote entries\n    [regex]$InlineCitationPattern # matches inline citation pointers like [^1_1]\n    [regex]$CodeFencePattern      # matches fenced code blocks\n\n    [string[]]$DefaultOps\n}\n\nclass FormatRegistry {\n    static [Dictionary[string, ThreadFormat]]$Formats\n\n    static FormatRegistry() {\n        [FormatRegistry]::Formats = [Dictionary[string, ThreadFormat]]::new(\n            [StringComparer]::OrdinalIgnoreCase)\n\n        [FormatRegistry]::RegisterPerplexity()\n        [FormatRegistry]::RegisterCopilot()\n        [FormatRegistry]::RegisterGrok()\n        [FormatRegistry]::RegisterGeneric()\n    }\n\n    static [void] RegisterPerplexity() {\n        $g = [ThreadFormat]::new()\n        $g.Name = 'Perplexity'\n        $g.Description = 'Perplexity AI thread exports with ⁂ dividers and footnote citations'\n\n        $g.FormatDetector = [regex]::new(\n            '(?i)(pplx-full-logo|perplexity\\.ai|<div[^>]*align\\s*=\\s*[\"\\x27]?center[\"\\x27]?[^>]*>\\s*⁂)',\n            [RegexOptions]::Compiled)\n\n        $g.TurnSplitter = [regex]::new(\n            '(?m)^[ \\t]*---[ \\t]*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.PromptExtractor = [regex]::new(\n            '(?m)^\\s*#\\s+(?<prompt>.+?)\\s*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DividerPattern = [regex]::new(\n            '(?is)<div\\b[^>]*\\balign\\s*=\\s*([\"\\x27]?)center\\1[^>]*>\\s*⁂\\s*</div>',\n            [RegexOptions]::Compiled)\n        $g.DividerReplacement = \"`n`n[[THREAD_DIVIDER]]`n`n\"\n\n        $g.ReferenceDetector = [regex]::new(\n            '(?m)^\\[\\^[^\\]]+\\]:',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.FootnotePattern = [regex]::new(\n            '(?m)^\\s*(\\[\\^[^\\]]+\\]:\\s*.*)$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.InlineCitationPattern = [regex]::new(\n            '(?:[ \\t\\r\\n,;:]*\\[\\^[^\\]]+\\])+',\n            [RegexOptions]::Compiled)\n\n        $g.CodeFencePattern = [regex]::new(\n            '(?m)^```(?<lang>\\w+)?\\s*$(?<code>[\\s\\S]*?)^```\\s*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DefaultOps = @(\n            'NormalizeLineEndings'\n            'StripBOM'\n            'NormalizeUnicode'\n            'StripHtmlComments'\n            'StripScriptStyle'\n            'StripHiddenSpans'\n            'StripImages'\n            'ConvertBrToNewline'\n            'TrimTrailingWhitespace'\n            'CollapseExcessiveBlanks'\n            'TrimDocument'\n        )\n\n        [FormatRegistry]::Formats['Perplexity'] = $g\n    }\n\n    static [void] RegisterCopilot() {\n        $g = [ThreadFormat]::new()\n        $g.Name = 'Copilot'\n        $g.Description = 'VS Code GitHub Copilot \"Copy All\" thread exports with User:/GitHub Copilot: role prefixes'\n\n        $g.FormatDetector = [regex]::new(\n            '(?m)^GitHub\\s+Copilot:',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.TurnSplitter = [regex]::new(\n            '(?m)(?=^User:\\s)',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.PromptExtractor = [regex]::new(\n            '(?m)^User:\\s+(?<prompt>.+?)$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.ResponsePrefix = [regex]::new(\n            '^\\s*GitHub\\s+Copilot:\\s*',\n            [RegexOptions]::Compiled)\n\n        $g.DividerPattern = $null\n        $g.DividerReplacement = $null\n\n        $g.ReferenceDetector = $null\n        $g.FootnotePattern = $null\n        $g.InlineCitationPattern = $null\n\n        $g.CodeFencePattern = [regex]::new(\n            '(?m)^```(?<lang>\\w+)?\\s*$(?<code>[\\s\\S]*?)^```\\s*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DefaultOps = @(\n            'NormalizeLineEndings'\n            'StripBOM'\n            'NormalizeUnicode'\n            'StripCopilotToolUse'\n            'StripCopilotUsedTools'\n            'TrimTrailingWhitespace'\n            'CollapseExcessiveBlanks'\n            'TrimDocument'\n        )\n\n        [FormatRegistry]::Formats['Copilot'] = $g\n    }\n\n    static [void] RegisterGrok() {\n        $g = [ThreadFormat]::new()\n        $g.Name = 'Grok'\n        $g.Description = 'Grok AI tab utility markdown exports — fused blocks with NNNms timing lines'\n\n        $g.FormatDetector = [regex]::new(\n            '(?m)^\\d+ms\\s*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.TurnSplitter = [regex]::new(\n            '(?m)(?:\\n\\s*\\n){2,}(?=.{1,200}\\n\\n)',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.PromptExtractor = [regex]::new(\n            '(?m)^(?<prompt>[^\\n]+?)$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DividerPattern = $null\n        $g.DividerReplacement = $null\n\n        $g.ReferenceDetector = $null\n        $g.FootnotePattern = $null\n        $g.InlineCitationPattern = $null\n\n        $g.CodeFencePattern = [regex]::new(\n            '(?m)^```(?<lang>\\w+)?\\s*$(?<code>[\\s\\S]*?)^```\\s*$', # not all code fences have language marker. sometimes its {language}\\n```\\n, or ```{block}... normalization is important here\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DefaultOps = @(\n            'NormalizeLineEndings'\n            'StripBOM'\n            'NormalizeUnicode'\n            'StripTimingLines'\n            'TrimTrailingWhitespace'\n            'CollapseExcessiveBlanks'\n            'TrimDocument'\n        )\n\n        [FormatRegistry]::Formats['Grok'] = $g\n    }\n\n    static [void] RegisterGeneric() {\n        $g = [ThreadFormat]::new()\n        $g.Name = 'Generic'\n        $g.Description = 'Fallback format for unrecognized thread exports — HR-delimited sections'\n\n        $g.FormatDetector = [regex]::new('^', [RegexOptions]::Compiled)\n\n        $g.TurnSplitter = [regex]::new(\n            '(?m)^[ \\t]*---[ \\t]*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.PromptExtractor = [regex]::new(\n            '(?m)^\\s*#{1,2}\\s+(?<prompt>.+?)\\s*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DividerPattern = $null\n        $g.DividerReplacement = $null\n\n        $g.ReferenceDetector = [regex]::new(\n            '(?m)^\\[\\^[^\\]]+\\]:',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.FootnotePattern = [regex]::new(\n            '(?m)^\\s*(\\[\\^[^\\]]+\\]:\\s*.*)$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.InlineCitationPattern = [regex]::new(\n            '(?:[ \\t\\r\\n,;:]*\\[\\^[^\\]]+\\])+',\n            [RegexOptions]::Compiled)\n\n        $g.CodeFencePattern = [regex]::new(\n            '(?m)^```(?<lang>\\w+)?\\s*$(?<code>[\\s\\S]*?)^```\\s*$',\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n\n        $g.DefaultOps = @(\n            'NormalizeLineEndings'\n            'StripBOM'\n            'NormalizeUnicode'\n            'TrimTrailingWhitespace'\n            'CollapseExcessiveBlanks'\n            'TrimDocument'\n        )\n\n        [FormatRegistry]::Formats['Generic'] = $g\n    }\n\n    static [ThreadFormat] Detect([string]$rawContent) {\n\n        foreach ($name in @([FormatRegistry]::Formats.Keys | Where-Object { $_ -ne 'Generic' })) {\n            $g = [FormatRegistry]::Formats[$name]\n            if ($g.FormatDetector.IsMatch($rawContent)) {\n                return $g\n            }\n        }\n        return [FormatRegistry]::Formats['Generic']\n    }\n\n    static [ThreadFormat] Get([string]$name) {\n        if ([FormatRegistry]::Formats.ContainsKey($name)) {\n            return [FormatRegistry]::Formats[$name]\n        }\n        throw [System.ArgumentException]::new(\"Unknown format: '$name'. Available: $([FormatRegistry]::Formats.Keys -join ', ')\")\n    }\n\n    static [string[]] ListFormats() {\n        return @([FormatRegistry]::Formats.Keys | Sort-Object)\n    }\n}\n\nclass ThreadParser {\n    \n\n    [string]$RawContent\n    [string]$ProcessedContent\n    [ThreadFormat]$Format\n    [string[]]$Ops\n    [bool]$IncludeReferences\n    [bool]$StripInlineCitations\n    [bool]$ComputeHashes\n    [bool]$Raw\n    [bool]$SaveDiff\n    [List[hashtable]]$Turns\n    [string]$SourceFile\n    [string]$DetectedFormat\n    [List[string]]$DiffLog        # audit log of what preprocessing removed\n\n    hidden [bool]$HasRun\n\n    ThreadParser([string]$content, [hashtable]$config) {\n        $this.RawContent = $content\n        $this.HasRun = $false\n        $this.Turns = [List[hashtable]]::new()\n        $this.DiffLog = [List[string]]::new()\n        $this.SourceFile = $config.SourceFile ?? ''\n        $this.IncludeReferences = $config.IncludeReferences ?? $false\n        $this.StripInlineCitations = -not $this.IncludeReferences\n        $this.ComputeHashes = $config.ComputeHashes ?? $false\n        $this.Raw = $config.Raw ?? $false\n        $this.SaveDiff = $config.SaveDiff ?? $false\n\n        if ($config.Format) {\n            $this.Format = [FormatRegistry]::Get($config.Format)\n        }\n        elseif ($config.Grammar) {\n\n            $this.Format = [FormatRegistry]::Get($config.Grammar)\n        }\n        else {\n            $this.Format = [FormatRegistry]::Detect($content)\n        }\n        $this.DetectedFormat = $this.Format.Name\n\n        $this.Ops = if ($config.Ops) { $config.Ops } else { $this.Format.DefaultOps }\n    }\n\n    [List[hashtable]] Invoke() {\n        if ($this.HasRun) {\n            throw [System.InvalidOperationException]::new(\n                'ThreadParser.Invoke() already called. Create a new instance.')\n        }\n        $this.HasRun = $true\n\n        $this.PreProcess()\n        $this.NormalizeDividers()\n        $this.SplitAndExtract()\n\n        return $this.Turns\n    }\n\n    hidden [void] PreProcess() {\n\n        if ($this.Raw) {\n            $this.ProcessedContent = $this.RawContent\n            return\n        }\n\n        if ($this.SaveDiff) {\n\n            $text = $this.RawContent\n            foreach ($opName in $this.Ops) {\n                if (-not [PreProcessor]::Registry.ContainsKey($opName)) {\n                    Write-Warning \"PreProcessor: unknown op '$opName' — skipping\"\n                    continue\n                }\n                $before = $text\n                $op = [PreProcessor]::Registry[$opName]\n                $text = & $op.Transform $text\n                if ($text -ne $before) {\n                    $this.DiffLog.Add(\"### op: $opName\")\n\n                    $beforeLines = $before -split \"`n\"\n                    $afterLines = $text -split \"`n\"\n                    $afterSet = [HashSet[string]]::new([StringComparer]::Ordinal)\n                    foreach ($l in $afterLines) { [void]$afterSet.Add($l) }\n                    foreach ($l in $beforeLines) {\n                        if (-not $afterSet.Contains($l)) {\n                            $this.DiffLog.Add(\"- $l\")\n                        }\n                    }\n\n                }\n            }\n            $this.ProcessedContent = $text\n        } else {\n            $this.ProcessedContent = [PreProcessor]::Apply($this.RawContent, $this.Ops)\n        }\n    }\n\n    hidden [void] NormalizeDividers() {\n        if ($this.Format.DividerPattern) {\n            $this.ProcessedContent = $this.Format.DividerPattern.Replace(\n                $this.ProcessedContent,\n                $this.Format.DividerReplacement)\n        }\n    }\n\n    hidden [void] SplitAndExtract() {\n        $sections = $this.Format.TurnSplitter.Split($this.ProcessedContent)\n        $turnIdx = 0\n\n        foreach ($raw in $sections) {\n            $section = $raw.Trim()\n            if ([string]::IsNullOrWhiteSpace($section)) { continue }\n\n            $m = $this.Format.PromptExtractor.Match($section)\n            if (-not $m.Success) { continue }\n\n            $turnIdx++\n            $prompt = $m.Groups['prompt'].Value.Trim()\n            $body = $section.Substring($m.Index + $m.Length).Trim()\n\n            if ($this.Format.ResponsePrefix) {\n                $body = $this.Format.ResponsePrefix.Replace($body, '', 1).Trim()\n            }\n\n            $response = ''\n            $references = ''\n\n            if ($body -match '\\[\\[THREAD_DIVIDER\\]\\]') {\n                $parts = $body -split '\\[\\[THREAD_DIVIDER\\]\\]', 2\n                $response = $parts[0].Trim()\n                $references = $parts[1].Trim()\n            } elseif ($this.Format.ReferenceDetector) {\n                $refMatch = $this.Format.ReferenceDetector.Match($body)\n                if ($refMatch.Success) {\n                    $response = $body.Substring(0, $refMatch.Index).Trim()\n                    $references = $body.Substring($refMatch.Index).Trim()\n                } else {\n                    $response = $body\n                }\n            } else {\n                $response = $body\n            }\n\n            $refArray = @()\n            if ($this.IncludeReferences -and $references -and $this.Format.FootnotePattern) {\n                $footMatches = $this.Format.FootnotePattern.Matches($references)\n                if ($footMatches.Count -gt 0) {\n                    foreach ($fm in $footMatches) { $refArray += $fm.Groups[1].Value.Trim() }\n                } else {\n                    $refArray = @($references -split \"`n\" | ForEach-Object { $_.Trim() } | Where-Object { $_ })\n                }\n            }\n\n            if ($this.StripInlineCitations -and $this.Format.InlineCitationPattern) {\n                $response = $this.Format.InlineCitationPattern.Replace($response, '')\n                $response = [regex]::Replace($response, '\\s{2,}', ' ').Trim()\n            }\n\n            $codeBlocks = @()\n            if ($this.Format.CodeFencePattern) {\n                $codeMatches = $this.Format.CodeFencePattern.Matches($response)\n                $blockIdx = 0\n                foreach ($cm in $codeMatches) {\n                    $lang = $cm.Groups['lang'].Value\n                    $code = $cm.Groups['code'].Value.Trim()\n                    if ($code) {\n                        $codeBlocks += @{\n                            index    = $blockIdx\n                            language = if ($lang) { $lang.ToLower() } else { 'text' }\n                            content  = $code\n                            chars    = $code.Length\n                        }\n                        $blockIdx++\n                    }\n                }\n            }\n\n            $turn = [ordered]@{\n                turn_idx    = $turnIdx\n                prompt      = $prompt\n                response    = $response\n                char_count  = $response.Length\n                code_blocks = $codeBlocks.Count\n            }\n\n            if ($this.ComputeHashes) {\n                $turn['content_hash'] = [SHA256Hash]::Compute($response)\n                $turn['prompt_hash']  = [SHA256Hash]::Compute($prompt)\n                $turn['simhash']      = [SimHash]::Compute($response)\n            }\n\n            if ($codeBlocks.Count -gt 0) {\n                $turn['code_block_details'] = $codeBlocks\n                $turn['languages'] = @($codeBlocks | ForEach-Object { $_.language } | Select-Object -Unique | Sort-Object)\n            }\n\n            if ($this.IncludeReferences -and $refArray.Count -gt 0) {\n                $turn['references'] = $refArray\n            }\n\n            $this.Turns.Add($turn)\n        }\n    }\n}\n\nfunction Write-JsonlOutput {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [List[hashtable]]$Turns,\n        [Parameter(Mandatory)] [string]$OutputPath,\n        [Parameter(Mandatory)] [string]$SourceFileName,\n        [int]$MinIdxWidth = 3,\n        [switch]$NoManifest\n    )\n\n    $idxWidth = [Math]::Max($MinIdxWidth, ($Turns.Count.ToString().Length))\n\n    $metadata = @{\n        TotalRecords = 0\n        MinBytes     = [int]::MaxValue\n        MaxBytes     = 0\n    }\n\n    $sw = [StreamWriter]::new($OutputPath, $false, [UTF8Encoding]::new($false))\n    try {\n        foreach ($turn in $Turns) {\n            $byteOffset = $sw.BaseStream.Position\n            $turnIdxStr = $turn.turn_idx.ToString().PadLeft($idxWidth, '0')\n\n            $jsonObject = [ordered]@{\n                byte_offset = [int]$byteOffset\n                turn_idx    = $turnIdxStr\n                prompt      = $turn.prompt\n                response    = $turn.response\n            }\n\n            if ($turn.ContainsKey('references') -and $turn.references.Count -gt 0) {\n                $jsonObject['references'] = $turn.references\n            }\n\n            $jsonLine = $jsonObject | ConvertTo-Json -Compress\n            $sw.WriteLine($jsonLine)\n\n            $metadata.TotalRecords++\n            $lineBytes = [Encoding]::UTF8.GetByteCount($jsonLine) + 1\n            if ($lineBytes -lt $metadata.MinBytes) { $metadata.MinBytes = $lineBytes }\n            if ($lineBytes -gt $metadata.MaxBytes) { $metadata.MaxBytes = $lineBytes }\n        }\n    }\n    finally {\n        $sw.Dispose()\n    }\n\n    $metadata.FileTotalBytes = (Get-Item -LiteralPath $OutputPath).Length\n    $metadata.FileName = Split-Path -Leaf $OutputPath\n\n    if (-not $NoManifest) {\n        $manifestPath = Join-Path (Split-Path $OutputPath) \"manifest_$(Split-Path -LeafBase $OutputPath).json\"\n        Write-ThreadManifest -Metadata @($metadata) -OutputPath $manifestPath\n    }\n\n    return $metadata\n}\n\nfunction Write-DirectoryOutput {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [List[hashtable]]$Turns,\n        [Parameter(Mandatory)] [string]$OutputDirectory,\n        [int]$MinIdxWidth = 3\n    )\n\n    $idxWidth = [Math]::Max($MinIdxWidth, ($Turns.Count.ToString().Length))\n\n    if (-not (Test-Path $OutputDirectory)) {\n        New-Item -Path $OutputDirectory -ItemType Directory -Force | Out-Null\n    }\n\n    foreach ($turn in $Turns) {\n        $turnIdxStr = $turn.turn_idx.ToString().PadLeft($idxWidth, '0')\n        $turnDir = Join-Path $OutputDirectory \"turn_$turnIdxStr\"\n        New-Item -Path $turnDir -ItemType Directory -Force | Out-Null\n\n        $promptSafe = ($turn.prompt -replace '[<>:\"/\\\\| ?*]', '_')\n        if ($promptSafe.Length -gt 60) { $promptSafe = $promptSafe.Substring(0, 60) }\n\n        $mdContent = \"# $($turn.prompt)`n`n$($turn.response)\"\n        $mdPath = Join-Path $turnDir \"turn_${turnIdxStr}_conversation.md\"\n        [File]::WriteAllText($mdPath, $mdContent, [UTF8Encoding]::new($false))\n\n        if ($turn.ContainsKey('code_block_details') -and $turn.code_block_details.Count -gt 0) {\n            $byLang = @{}\n            foreach ($block in $turn.code_block_details) {\n                $lang = $block.language\n                if (-not $byLang.ContainsKey($lang)) { $byLang[$lang] = [List[string]]::new() }\n                $byLang[$lang].Add($block.content)\n            }\n\n            $langExtMap = @{\n                powershell = '.ps1'; javascript = '.js'; typescript = '.ts'\n                python = '.py'; csharp = '.cs'; java = '.java'\n                bash = '.sh'; shell = '.sh'; json = '.json'\n                xml = '.xml'; yaml = '.yml'; html = '.html'\n                css = '.css'; sql = '.sql'; rust = '.rs'\n                go = '.go'; ruby = '.rb'; cpp = '.cpp'; c = '.c'\n                text = '.txt'\n            }\n\n            foreach ($lang in $byLang.Keys) {\n                $ext = if ($langExtMap.ContainsKey($lang)) { $langExtMap[$lang] } else { '.txt' }\n                $codeContent = $byLang[$lang] -join \"`n`n# --- block boundary ---`n`n\"\n                $codePath = Join-Path $turnDir \"turn_${turnIdxStr}_code${ext}\"\n                [File]::WriteAllText($codePath, $codeContent, [UTF8Encoding]::new($false))\n            }\n        }\n    }\n\n    return @{ TurnCount = $Turns.Count; OutputDirectory = $OutputDirectory }\n}\n\nfunction Write-ThreadManifest {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [array]$Metadata,\n        [Parameter(Mandatory)] [string]$OutputPath\n    )\n\n    $filesJson = ($Metadata | ForEach-Object {\n        $entry = [ordered]@{\n            file_name            = $_.FileName\n            file_total_bytes     = $_.FileTotalBytes\n            num_records          = $_.TotalRecords\n            record_byte_size_range = [ordered]@{\n                min_bytes = $_.MinBytes\n                max_kb    = [Math]::Round($_.MaxBytes / 1024.0, 2)\n            }\n        }\n        if ($_.ContainsKey('SourceHash') -and $_.SourceHash) {\n            $entry['source_hash'] = $_.SourceHash\n        }\n        $entry\n    })\n\n    $manifest = [ordered]@{\n        purpose              = 'provide operational and metadata description of shard file contents and layout to enhance access efficiency and ergonomics'\n        guidance             = [ordered]@{\n            truncation_warning = 'Verify the per-file record count provided in this manifest (below in TOC) by counting the total lines you see in the file against the stated record count. If there is a mismatch, it indicates that the file has been truncated for previewing and you may need to manually read in the file with a generic read_file tool. Otherwise report back to user that you are unable to access the data as provided.'\n            usage_guide        = 'Treat the payload as a seekable, searchable virtual database (jsonl-like records lacking file extension/container formality that retain access benefits without need for format-specific utilities or tools). Use the TOC below to plan and fetch relevant rows of the shard file and use byte_offset prefixes provided to selectively access records (metadata, content) in the shard file as needed. Treat this file as an interactive ephemeral database for performing your assigned tasks based on the data contents.'\n        }\n        payload_description  = [ordered]@{\n            type          = 'Diarized user-assistant message threads parsed and chunked by prompt-response in natural sequential order packaged in a jsonl-like format for LLM consumption'\n            file_scope    = 'One file per chat thread diarized markdown chat thread export'\n            file_type     = 'jsonl-like'\n            file_extension = 'none'\n            file_encoding = 'utf-8'\n            record_format = 'jsonl-like lines containing serialized, compacted json objects with address prefixes, metadata and content fields relevant to the document type'\n        }\n        table_of_contents    = [ordered]@{\n            properties = [ordered]@{\n                num_files              = $Metadata.Count\n                comment                = 'record fields described below are ordered as they appear in each line of the shard file'\n                record_field_definitions = [ordered]@{\n                    byte_offset = 'integer offset in bytes from start of file to start of record line'\n                    turn_idx    = 'document-global sequential integer index of the prompt-response turn contained in the record, starting at 1'\n                    prompt      = 'string containing the user prompt text for this turn'\n                    response    = 'string containing the assistant response text for this turn'\n                    references  = '[optional] a new line-delimited block of text containing reference URLs or citations if included in response'\n                }\n            }\n            files = $filesJson\n        }\n    }\n\n    $manifest | ConvertTo-Json -Depth 10 | Set-Content -LiteralPath $OutputPath -Encoding UTF8\n}\n\nfunction Invoke-ThreadParser {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0, ValueFromPipeline, ValueFromPipelineByPropertyName)]\n        [string[]]$FilePath,\n\n        [Parameter(Position = 1)]\n        [string]$OutputDirectory,\n\n        [ValidateSet('Jsonl', 'Directory')]\n        [string]$OutputProfile = 'Jsonl',\n\n        [Alias('Grammar')]\n        [string]$Format,\n\n        [string[]]$Ops,\n\n        [switch]$IncludeReferences,\n\n        [string[]]$Include,\n\n        [string[]]$Exclude,\n\n        [switch]$ComputeHashes,\n\n        [switch]$Raw,\n\n        [switch]$SaveDiff,\n\n        [switch]$NoManifest,\n\n        [int]$MinIdxWidth = 3\n    )\n\n    begin {\n        $allFiles = [List[string]]::new()\n    }\n\n    process {\n        foreach ($p in $FilePath) {\n            $allFiles.Add($p)\n        }\n    }\n\n    end {\n\n        $resolvedInputs = [List[hashtable]]::new()\n        foreach ($path in $allFiles) {\n            try {\n                $resolved = (Resolve-Path -LiteralPath $path -ErrorAction Stop).ProviderPath\n                if (Test-Path -LiteralPath $resolved -PathType Container) {\n                    $mdFiles = Get-ChildItem -LiteralPath $resolved -Filter '*.md' -File\n                    if ($mdFiles.Count -eq 0) {\n                        Write-Warning \"No .md files in directory: $path\"\n                    }\n                    foreach ($f in $mdFiles) {\n                        $resolvedInputs.Add(@{ FullPath = $f.FullName })\n                    }\n                } else {\n                    $resolvedInputs.Add(@{ FullPath = $resolved })\n                }\n            } catch {\n                Write-Warning \"Could not resolve '$path': $($_.Exception.Message)\"\n            }\n        }\n\n        if ($resolvedInputs.Count -eq 0) {\n            Write-Warning 'No valid input files'\n            return\n        }\n\n        if ($Include -or $Exclude) {\n            $filter = [ContentFilter]::new($Include, $Exclude)\n            $before = $resolvedInputs.Count\n            $resolvedInputs = [List[hashtable]]::new(\n                @($resolvedInputs | Where-Object { $filter.Test((Split-Path $_.FullPath -Leaf)) }))\n            $dropped = $before - $resolvedInputs.Count\n            if ($dropped -gt 0) {\n                Write-Host \"  Filtered: $dropped file(s) excluded by include/exclude globs\" -ForegroundColor DarkGray\n            }\n            if ($resolvedInputs.Count -eq 0) {\n                Write-Warning 'No files remaining after include/exclude filtering'\n                return\n            }\n        }\n\n        $timestamp = (Get-Date).ToString('yyyyMMddTHHmmss')\n        $firstDir = Split-Path $resolvedInputs[0].FullPath -Parent\n        $baseOut = if ($OutputDirectory) {\n            if ([Path]::IsPathRooted($OutputDirectory)) { $OutputDirectory }\n            else { Join-Path $firstDir $OutputDirectory }\n        } else {\n            Join-Path $firstDir '.threadparsed'\n        }\n\n        $batchDir = Join-Path $baseOut \"batch_${timestamp}_parsed\"\n\n        $allResults = [List[hashtable]]::new()\n        $allMetadata = [List[hashtable]]::new()\n        $errors = [List[string]]::new()\n\n        foreach ($input in $resolvedInputs) {\n            $inputName = Split-Path $input.FullPath -Leaf\n            $baseName = [Path]::GetFileNameWithoutExtension($inputName)\n            $sanitized = $baseName -replace '[<>:\"/\\\\|?*\\x00-\\x1F]', '_'\n\n            Write-Host \"Parsing: $inputName\" -ForegroundColor Cyan\n\n            try {\n                $content = Get-Content -LiteralPath $input.FullPath -Raw -Encoding UTF8\n                $config = @{\n                    SourceFile        = $input.FullPath\n                    IncludeReferences = $IncludeReferences.IsPresent\n                    ComputeHashes     = $ComputeHashes.IsPresent\n                    Raw               = $Raw.IsPresent\n                    SaveDiff          = $SaveDiff.IsPresent -and -not $Raw.IsPresent\n                }\n                if ($Format) { $config.Format = $Format }\n                if ($Ops) { $config.Ops = $Ops }\n\n                $parser = [ThreadParser]::new($content, $config)\n                $turns = $parser.Invoke()\n\n                Write-Host \"  Detected: $($parser.DetectedFormat) | Turns: $($turns.Count)\" -ForegroundColor Gray\n\n                $resultEntry = @{\n                    BaseName       = $sanitized\n                    Turns          = $turns\n                    Format         = $parser.DetectedFormat\n                    SourceFile     = $input.FullPath\n                    DiffLog        = $parser.DiffLog\n                }\n\n                if ($ComputeHashes.IsPresent) {\n                    $resultEntry['SourceHash'] = [SHA256Hash]::Compute($content)\n                }\n\n                $allResults.Add($resultEntry)\n            } catch {\n                $msg = \"  ERROR parsing ${inputName}: $($_.Exception.Message)\"\n                Write-Warning $msg\n                $errors.Add($msg)\n            }\n        }\n\n        if ($allResults.Count -eq 0) {\n            Write-Warning \"No files parsed successfully ($($errors.Count) error(s))\"\n            return\n        }\n\n        New-Item -Path $batchDir -ItemType Directory -Force | Out-Null\n\n        foreach ($result in $allResults) {\n            switch ($OutputProfile) {\n                'Jsonl' {\n                    $corpusDir = Join-Path $batchDir \"corpus_files_${timestamp}\"\n                    New-Item -Path $corpusDir -ItemType Directory -Force | Out-Null\n\n                    $outFile = Join-Path $corpusDir \"$($result.BaseName)_${timestamp}_parsed\"\n                    $meta = Write-JsonlOutput -Turns $result.Turns -OutputPath $outFile `\n                        -SourceFileName $result.BaseName -MinIdxWidth $MinIdxWidth `\n                        -NoManifest:$NoManifest\n\n                    if ($result.ContainsKey('SourceHash')) {\n                        $meta['SourceHash'] = $result.SourceHash\n                    }\n\n                    $allMetadata.Add($meta)\n                    Write-Host \"  -> $($meta.FileName) ($($meta.TotalRecords) records)\" -ForegroundColor Green\n                }\n                'Directory' {\n                    $dirOut = Join-Path $batchDir \"$($result.BaseName)_${timestamp}\"\n                    $meta = Write-DirectoryOutput -Turns $result.Turns -OutputDirectory $dirOut `\n                        -MinIdxWidth $MinIdxWidth\n\n                    Write-Host \"  -> $($meta.OutputDirectory) ($($meta.TurnCount) turns)\" -ForegroundColor Green\n                }\n            }\n\n            if ($SaveDiff.IsPresent -and -not $Raw.IsPresent -and $result.DiffLog -and $result.DiffLog.Count -gt 0) {\n                $diffPath = Join-Path $batchDir \"$($result.BaseName)_stripped.diff.md\"\n                $diffContent = \"# Preprocessing Diff: $($result.BaseName)`n`n\"\n                $diffContent += \"Format: $($result.Format)`n`n\"\n                $diffContent += ($result.DiffLog -join \"`n\")\n                [File]::WriteAllText($diffPath, $diffContent, [UTF8Encoding]::new($false))\n                Write-Host \"  -> diff sidecar: $(Split-Path $diffPath -Leaf)\" -ForegroundColor DarkGray\n            }\n        }\n\n        if ($OutputProfile -eq 'Jsonl' -and -not $NoManifest -and $allMetadata.Count -gt 0) {\n            $batchManifest = Join-Path $batchDir \"manifest_${timestamp}.json\"\n            Write-ThreadManifest -Metadata $allMetadata -OutputPath $batchManifest\n            Write-Host \"`nManifest: $batchManifest\" -ForegroundColor Green\n        }\n\n        $totalTurns = ($allResults | ForEach-Object { $_.Turns.Count } | Measure-Object -Sum).Sum\n        Write-Host \"`nBatch complete: $($allResults.Count) file(s), $totalTurns total turns\" -ForegroundColor Cyan\n        if ($errors.Count -gt 0) {\n            Write-Warning \"$($errors.Count) file(s) had errors\"\n        }\n\n        return @{\n            BatchDirectory = $batchDir\n            FilesProcessed = $allResults.Count\n            TotalTurns     = $totalTurns\n            Errors         = $errors.Count\n            OutputProfile  = $OutputProfile\n        }\n    }\n}\n\nExport-ModuleMember -Function @(\n    'Invoke-ThreadParser'\n    'Write-DirectoryOutput'\n    'Write-JsonlOutput'\n    'Write-ThreadManifest'\n) |
