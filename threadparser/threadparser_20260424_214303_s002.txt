idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
1 | v1-batch/threadparser-batch.psm1 | {15149 1255 0.3460 4.5101} | 15740 | using namespace System.Text\n\n#requires -Version 7.5\n\nfunction Remove-ThreadHtmlBoilerplate {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Text\n    )\n\n    $Text = [regex]::Replace(\n        $Text,\n        '(?is)<div\\b[^>]*\\balign\\s*=\\s*([\"'']?)center\\1[^>]*>\\s*⁂\\s*</div>',\n        \"`n`n[[THREAD_DIVIDER]]`n`n\"\n    )\n\n    $Text = [regex]::Replace($Text, '(?s)<!--.*?-->', '')\n\n    $Text = [regex]::Replace($Text, '(?is)<(script|style)\\b[^>]*>.*?</\\1>', '')\n\n    $Text = [regex]::Replace(\n        $Text,\n        '(?is)<span\\b[^>]*\\bstyle\\s*=\\s*([\"''])(?:(?!\\1).)*\\bdisplay\\s*:\\s*none\\b(?:(?!\\1).)*\\1[^>]*>.*?</span>',\n        ''\n    )\n\n    $Text = [regex]::Replace($Text, '(?is)<img\\b[^>]*>', '')\n\n    $Text = [regex]::Replace($Text, '(?i)<br\\s*/?>', \"`n\")\n\n    $Text = $Text -replace \"`r\", ''\n    $Text = [regex]::Replace($Text, \"[ \\t]+\\n\", \"`n\")\n    $Text = [regex]::Replace($Text, \"\\n{3,}\", \"`n`n\")\n\n    return $Text.Trim()\n}\n\nfunction ParseThreadToJsonl {\n    [CmdletBinding()]\n    param (\n        [Parameter(Mandatory = $true, Position = 0, ValueFromPipeline = $true, ValueFromPipelineByPropertyName = $true)]\n        [string[]]$FilePath,\n\n        [Parameter(Position = 1)]\n        [string]$OutputDirectory,\n\n        [int]$MinIdxWidth = 3,\n\n        [switch]$DoNotOverwrite,\n\n        [switch]$IncludeReferences,\n\n        [switch]$NoManifest\n    )\n\n    begin {\n\n        $allInputFiles = @()\n\n        $script:allShardMetadata = @()\n        $script:sharedOutputDir = $null\n\n        $script:currentBatchDir = $null\n        $script:corpusDir = $null\n    }\n\n    process {\n\n        $allInputFiles += $FilePath\n    }\n\n    end {\n        if ($allInputFiles.Count -eq 0) {\n            Write-Warning \"No input files provided\"\n            return\n        }\n\n        $resolvedInputs = @()\n        foreach ($path in $allInputFiles) {\n            try {\n\n                $resolved = Resolve-Path -LiteralPath $path -ErrorAction Stop\n                $itemPath = $resolved.ProviderPath\n\n                if (Test-Path -LiteralPath $itemPath -PathType Container) {\n\n                    $mdFiles = Get-ChildItem -LiteralPath $itemPath -Filter \"*.md\" -File -ErrorAction Stop\n                    if ($mdFiles.Count -eq 0) {\n                        Write-Warning \"No .md files found in directory: $path\"\n                    }\n                    else {\n                        Write-Host \"Expanding directory '$path' -> $($mdFiles.Count) .md file(s)\"\n                        foreach ($mdFile in $mdFiles) {\n                            $resolvedInputs += @{\n                                OriginalPath = $path\n                                FullPath     = $mdFile.FullName\n                            }\n                        }\n                    }\n                }\n                else {\n\n                    $resolvedInputs += @{\n                        OriginalPath = $path\n                        FullPath     = $itemPath\n                    }\n                }\n            }\n            catch {\n                Write-Warning \"Could not resolve input path '$path': $($_.Exception.Message)\"\n            }\n        }\n\n        if ($resolvedInputs.Count -eq 0) {\n            Write-Warning \"No valid input files could be resolved\"\n            return\n        }\n\n        $timestamp = (Get-Date).ToString('yyyyMMddTHHmmss')\n\n        $firstInputDir = Split-Path -Path $resolvedInputs[0].FullPath -Parent\n        if ($OutputDirectory) {\n            try {\n                $resolvedOut = Resolve-Path -LiteralPath $OutputDirectory -ErrorAction Stop\n                $script:sharedOutputDir = $resolvedOut.ProviderPath\n            }\n            catch {\n                $script:sharedOutputDir = Join-Path -Path $firstInputDir -ChildPath $OutputDirectory\n                $null = New-Item -Path $script:sharedOutputDir -ItemType Directory -Force\n            }\n        }\n        else {\n            $script:sharedOutputDir = Join-Path -Path $firstInputDir -ChildPath \".threadparsed\"\n            $null = New-Item -Path $script:sharedOutputDir -ItemType Directory -Force\n        }\n\n        / { target_dir }_ { base_name }_ { timestamp }_parsed  # one\n        / { base_name }_manifest_ { timestamp }.json\n\n        $script:currentBatchDir = Join-Path -Path $script:sharedOutputDir -ChildPath (\"batch_${timestamp}_parsed\")\n        $null = New-Item -Path $script:currentBatchDir -ItemType Directory -Force\n\n        $script:corpusDir = Join-Path -Path $script:currentBatchDir -ChildPath (\"corpus_files_${timestamp}\")\n        $null = New-Item -Path $script:corpusDir -ItemType Directory -Force\n\n        foreach ($inputFile in $resolvedInputs) {\n            $fullInputPath = $inputFile.FullPath\n            $inputName = Split-Path -Path $fullInputPath -Leaf\n            $baseName = [System.IO.Path]::GetFileNameWithoutExtension($inputName)\n\n            $sanitizedBase = $baseName -replace '[<>:\"/\\\\|?*\\x00-\\x1F]', '_'\n            $shardFileName = \"${sanitizedBase}_${timestamp}_parsed\"\n\n            $outputFile = Join-Path -Path $script:corpusDir -ChildPath $shardFileName\n\n            if ($DoNotOverwrite.IsPresent -and (Test-Path -LiteralPath $outputFile)) {\n                Write-Host \"Output file exists and -DoNotOverwrite specified; skipping: $outputFile\"\n                continue\n            }\n\n            Write-Host \"Processing: $inputName -> $shardFileName\"\n\n            $content = Get-Content -LiteralPath $fullInputPath -Raw\n            $content = Remove-ThreadHtmlBoilerplate -Text $content\n\n            $sections = [regex]::Split($content, '(?m)^[ \\t]*---[ \\t]*$')\n\n            $promptHeaderRegex = [regex]::new('(?m)^\\s*#\\s+(?<prompt>.+?)\\s*$', 'Multiline')\n            $validCount = 0\n            foreach ($s in $sections) {\n                if ($promptHeaderRegex.IsMatch($s)) { $validCount++ }\n            }\n            $idxWidth = [Math]::Max($MinIdxWidth, ($validCount.ToString().Length))\n\n            $shardMetadata = @{\n                FileName     = $shardFileName\n                TotalRecords = 0\n                MinBytes     = [int]::MaxValue\n                MaxBytes     = 0\n            }\n\n            try {\n                $sw = [System.IO.StreamWriter]::new($outputFile, $false, [System.Text.UTF8Encoding]::new($false))\n            }\n            catch {\n                Write-Warning \"Failed to open output file '$outputFile' for writing: $($_.Exception.Message)\"\n                continue\n            }\n\n            try {\n                $turnIdx = 0\n\n                foreach ($raw in $sections) {\n                    $section = $raw.Trim()\n                    if ([string]::IsNullOrWhiteSpace($section)) { continue }\n\n                    $m = $promptHeaderRegex.Match($section)\n                    if (-not $m.Success) { continue }\n\n                    $turnIdx++\n                    $prompt = $m.Groups['prompt'].Value.Trim()\n\n                    $body = $section.Substring($m.Index + $m.Length).Trim()\n\n                    $mainResponse = ''\n                    $references = ''\n\n                    if ($body -match '\\[\\[THREAD_DIVIDER\\]\\]') {\n                        $parts = $body -split '\\[\\[THREAD_DIVIDER\\]\\]', 2\n                        $mainResponse = $parts[0].Trim()\n                        $references = $parts[1].Trim()\n                    }\n                    else {\n\n                        $refMatch = [regex]::Match($body, '(?m)^\\[\\^[^\\]]+\\]:')\n                        if ($refMatch.Success) {\n                            $mainResponse = $body.Substring(0, $refMatch.Index).Trim()\n                            $references = $body.Substring($refMatch.Index).Trim()\n                        }\n                        else {\n                            $mainResponse = $body.Trim()\n                            $references = \"\"\n                        }\n                    }\n\n                    $refArray = @()\n                    if ($IncludeReferences.IsPresent -and -not [string]::IsNullOrWhiteSpace($references)) {\n                        $footMatches = [regex]::Matches($references, '(?m)^\\s*(\\[\\^[^\\]]+\\]:\\s*.*)$')\n                        if ($footMatches.Count -gt 0) {\n                            foreach ($fm in $footMatches) { $refArray += $fm.Groups[1].Value.Trim() }\n                        }\n                        else {\n                            $lines = $references -split \"`n\"\n                            foreach ($l in $lines) { $t = $l.Trim(); if ($t -ne '') { $refArray += $t } }\n                        }\n                    }\n\n                    if (-not $IncludeReferences.IsPresent) {\n                        try {\n                            $marker = '[[PTR_MARKER]]'\n                            $mainResponse = [regex]::Replace($mainResponse, '(?m)(?:[ \\t\\r\\n,;:]*\\[\\^[^\\]]+\\])+', $marker)\n                            $mainResponse = [regex]::Replace($mainResponse, '\\s*[,;:\\-]+\\s*' + [regex]::Escape($marker) + '\\s*', ' ')\n                            $mainResponse = [regex]::Replace($mainResponse, '\\s*' + [regex]::Escape($marker) + '\\s*[,;:\\-]+\\s*', ' ')\n                            $mainResponse = [regex]::Replace($mainResponse, '(?:' + [regex]::Escape($marker) + ')+', ' ')\n                            $mainResponse = [regex]::Replace($mainResponse, '\\s+,', ',')\n                            while ([regex]::IsMatch($mainResponse, ',\\s*,', 'None')) {\n                                $mainResponse = [regex]::Replace($mainResponse, ',\\s*,', ',')\n                            }\n                            $mainResponse = [regex]::Replace($mainResponse, ',{2,}', ',')\n                            $mainResponse = [regex]::Replace($mainResponse, ',\\s*,+', ' ')\n                            $mainResponse = [regex]::Replace($mainResponse, ',\\s*([\\\\.;:!\\?])', '$1')\n                            $mainResponse = [regex]::Replace($mainResponse, ',\\s+(and|or)\\b', ' $1')\n                            $mainResponse = [regex]::Replace($mainResponse, '\\s{2,}', ' ')\n                            $mainResponse = $mainResponse.Trim()\n                            $mainResponse = $mainResponse.Trim(' ', ',', ';', ':', '-')\n                        }\n                        catch {\n\n                        }\n                    }\n\n                    $byteOffset = $sw.BaseStream.Position\n\n                    $turnIdxStr = $turnIdx.ToString().PadLeft($idxWidth, '0')\n\n                    $jsonObject = [ordered]@{\n                        byte_offset = [int]$byteOffset\n                        turn_idx    = $turnIdxStr\n                        prompt      = $prompt\n                        response    = $mainResponse\n                    }\n\n                    if ($IncludeReferences.IsPresent -and $refArray.Count -gt 0) {\n                        $jsonObject['references'] = $refArray\n                    }\n\n                    $jsonLine = ($jsonObject | ConvertTo-Json -Compress)\n                    $sw.WriteLine($jsonLine)\n\n                    $shardMetadata.TotalRecords++\n                    $lineBytes = [System.Text.Encoding]::UTF8.GetByteCount($jsonLine) + 1  # +1 for newline\n                    if ($lineBytes -lt $shardMetadata.MinBytes) { $shardMetadata.MinBytes = $lineBytes }\n                    if ($lineBytes -gt $shardMetadata.MaxBytes) { $shardMetadata.MaxBytes = $lineBytes }\n                }\n            }\n            finally {\n                if ($sw) { $sw.Dispose() }\n            }\n\n            $shardMetadata.FileTotalBytes = (Get-Item -LiteralPath $outputFile).Length\n\n            $script:allShardMetadata += $shardMetadata\n\n            Write-Host \"  -> Completed: $shardFileName ($($shardMetadata.TotalRecords) records)\"\n        }\n\n        if (-not $NoManifest.IsPresent -and $script:allShardMetadata.Count -gt 0) {\n\n            $manifestPath = Join-Path -Path $script:currentBatchDir -ChildPath \"manifest_${timestamp}.json\"\n\n            $filesJson = ($script:allShardMetadata | ForEach-Object {\n                    @\"\n      {\n        \"file_name\": \"$($_.FileName)\",\n        \"file_total_bytes\": $($_.FileTotalBytes),\n        \"num_records\": $($_.TotalRecords),\n        \"record_byte_size_range\": {\n          \"min_bytes\": $($_.MinBytes),\n          \"max_kb\": $([Math]::Round($_.MaxBytes / 1024.0, 2))\n        }\n      }\n\"@\n                }) -join \",`n\"\n\n            $manifestTemplate = @\"\n{\n  \"purpose\": \"provide operational and metadata description of shard file contents and layout to enhance access efficiency and ergonomics\",\n  \"guidance\": {\n    \"truncation_warning\": \"Verify the per-file record count provided in this manifest (below in TOC) by counting the total lines you see in the file against the stated record count. If there is a mismatch, it indicates that the file has been truncated for previwing and you may need to manually read in the file with a generic read_file tool. Otherwise report back to user that you are unable to access the data as provided.\",\n    \"usage_guide\": \"Treat the payload as a seekable, searchable virtual database (jsonl-like records lacking file extension/container formality that retain access benefits without need for format-specific utilities or tools). Use the TOC below to plan and fetch relevant rows of the shard file and use byte_offset prefixes provided to selectively access records (metadata, content) in the shard file as needed. Treat this file as an interactive ephemeral database for performing your assigned tasks based on the data contents.\"\n  },\n  \"payload_description\": {\n    \"type\": \"Diarized user-assistant message threads parsed and chunked by prompt-response in natural sequential order packaged in a jsonl-like format for LLM consumption\",\n    \"file_scope\": \"One file per chat thread diarized markdown chat thread export\",\n    \"file_type\": \"jsonl-like\",\n    \"file_extension\": \"none\",\n    \"file_encoding\": \"utf-8\",\n    \"record_format\": \"jsonl-like lines containing serialized, compacted json objects with address prefixes, metadata and content fields relevant to the document type\"\n  },\n  \"table_of_contents\": {\n    \"properties\": {\n      \"num_files\": $($script:allShardMetadata.Count),\n      \"comment\": \"record fields described below are ordered as they appear in each line of the shard file\",\n      \"record_field_definitions\": {\n        \"byte_offset\": \"integer offset in bytes from start of file to start of record line\",\n        \"turn_idx\": \"document-global sequential integer index of the prompt-response turn contained in the record, starting at 1\",\n        \"prompt\": \"string containing the user prompt text for this turn\",\n        \"response\": \"string containing the assistant response text for this turn\",\n        \"references\": \"[optional] a new line-delimited block of text containing reference URLs or citations if included in response\"\n      }\n    },\n    \"files\": [\n$filesJson\n    ]\n  }\n}\n\"@\n\n            $manifestTemplate | Set-Content -LiteralPath $manifestPath -Encoding UTF8\n            Write-Host \"`nManifest generated: $manifestPath\"\n            Write-Host \"  Covers $($script:allShardMetadata.Count) shard file(s)\"\n        }\n\n        Write-Host \"`nBatch processing complete. $($script:allShardMetadata.Count) file(s) processed.\"\n    }\n}\n\nexport-ModuleMember -Function @(\n    'ParseThreadToJsonl'\n) |
