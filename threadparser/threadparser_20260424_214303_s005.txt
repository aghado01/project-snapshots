idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
5 | v2/threadparser-v2.psm1 | {44463 3578 0.3411 4.5175} | 45828 | using namespace System.Text\nusing namespace System.Text.RegularExpressions\nusing namespace System.Collections.Generic\nusing namespace System.IO\n\nusing module .\\threadparser_v2-primitives.psm1\n\n#Requires -Version 7.5\nSet-StrictMode -Version Latest\n\nclass ConfigLoader {\n    static [hashtable] $Config\n    static [bool] $Loaded = $false\n\n    static [void] Load([string]$configPath) {\n        if (-not (Test-Path -LiteralPath $configPath)) {\n            throw \"ThreadParser config not found: $configPath\"\n        }\n        $raw = Get-Content -LiteralPath $configPath -Raw -Encoding UTF8\n        [ConfigLoader]::Config = $raw | ConvertFrom-Json -AsHashtable\n        [ConfigLoader]::Loaded = $true\n    }\n\n    static [hashtable] GetConfig() {\n        if (-not [ConfigLoader]::Loaded) {\n            $defaultPath = Join-Path $PSScriptRoot 'threadparser_v2-formats.json'\n            [ConfigLoader]::Load($defaultPath)\n        }\n        return [ConfigLoader]::Config\n    }\n\n    static [regex] CompilePattern([hashtable]$spec) {\n        if ($null -eq $spec) { return $null }\n        $opts = [RegexOptions]::None\n        if ($spec.ContainsKey('options')) {\n            foreach ($o in $spec['options']) {\n                $opts = $opts -bor [RegexOptions]$o\n            }\n        }\n        return [regex]::new($spec['pattern'], $opts)\n    }\n}\n\nclass PreProcessOp {\n    [string]$Name\n    [string]$Description\n    [regex]$Pattern\n    [string]$Replacement\n    [scriptblock]$Transform\n\n    PreProcessOp([string]$name, [string]$desc, [regex]$pattern, [string]$replacement) {\n        $this.Name = $name\n        $this.Description = $desc\n        $this.Pattern = $pattern\n        $this.Replacement = $replacement\n\n        $p = $pattern\n        $r = $replacement\n        $this.Transform = { param($t) $p.Replace($t, $r) }.GetNewClosure()\n    }\n\n    PreProcessOp([string]$name, [string]$desc, [scriptblock]$transform) {\n        $this.Name = $name\n        $this.Description = $desc\n        $this.Transform = $transform\n    }\n}\n\nclass PreProcessor {\n    static [Dictionary[string, PreProcessOp]]$Registry\n    static [bool]$Initialized = $false\n\n    static [void] Initialize() {\n        if ([PreProcessor]::Initialized) { return }\n        [PreProcessor]::Registry = [Dictionary[string, PreProcessOp]]::new(\n            [StringComparer]::OrdinalIgnoreCase)\n\n        $config = [ConfigLoader]::GetConfig()\n        $ops = $config['ops']\n\n        foreach ($name in $ops.Keys) {\n            $opDef = $ops[$name]\n            $desc = $opDef['description']\n\n            switch ($opDef['type']) {\n                'regex' {\n                    $opts = [RegexOptions]::None\n                    if ($opDef.ContainsKey('options')) {\n                        foreach ($o in $opDef['options']) {\n                            $opts = $opts -bor [RegexOptions]$o\n                        }\n                    }\n                    $pattern = [regex]::new($opDef['pattern'], $opts)\n\n                    $replacement = $opDef['replacement'] -replace '\\\\n', \"`n\" -replace '\\\\r', \"`r\" -replace '\\\\t', \"`t\"\n                    [PreProcessor]::Registry[$name] = [PreProcessOp]::new($name, $desc, $pattern, $replacement)\n                }\n                'builtin' {\n                    $transform = [PreProcessor]::GetBuiltinTransform($name)\n                    [PreProcessor]::Registry[$name] = [PreProcessOp]::new($name, $desc, $transform)\n                }\n            }\n        }\n\n        [PreProcessor]::Initialized = $true\n    }\n\n    static [scriptblock] GetBuiltinTransform([string]$name) {\n        $result = switch ($name) {\n            'StripBOM' {\n                { param($t) if ($t.Length -gt 0 -and [int]$t[0] -eq 0xFEFF) { $t.Substring(1) } else { $t } }\n            }\n            'NormalizeUnicode' {\n                { param($t) try { $t.Normalize([NormalizationForm]::FormC) } catch { $t } }\n            }\n            'TrimDocument' {\n                { param($t) $t.Trim() }\n            }\n            default {\n                throw \"No builtin transform for op: $name\"\n            }\n        }\n        return $result\n    }\n\n    static [string] Apply([string]$text, [string[]]$ops) {\n        [PreProcessor]::Initialize()\n        foreach ($opName in $ops) {\n            if (-not [PreProcessor]::Registry.ContainsKey($opName)) {\n                Write-Warning \"PreProcessor: unknown op '$opName' — skipping\"\n                continue\n            }\n            $op = [PreProcessor]::Registry[$opName]\n            $text = & $op.Transform $text\n        }\n        return $text\n    }\n\n    static [string] ApplyWithDiff([string]$text, [string[]]$ops, [List[string]]$diffLog) {\n        [PreProcessor]::Initialize()\n        foreach ($opName in $ops) {\n            if (-not [PreProcessor]::Registry.ContainsKey($opName)) {\n                Write-Warning \"PreProcessor: unknown op '$opName' — skipping\"\n                continue\n            }\n            $before = $text\n            $op = [PreProcessor]::Registry[$opName]\n            $text = & $op.Transform $text\n            if ($text -ne $before) {\n                $diffLog.Add(\"### op: $opName\")\n                $diffLog.Add('```diff')\n                $beforeLines = $before -split \"`n\"\n                $afterLines = $text -split \"`n\"\n                $afterSet = [HashSet[string]]::new([StringComparer]::Ordinal)\n                foreach ($l in $afterLines) { [void]$afterSet.Add($l) }\n                foreach ($l in $beforeLines) {\n                    if (-not $afterSet.Contains($l)) { $diffLog.Add(\"- $l\") }\n                }\n                $diffLog.Add('```')\n                $diffLog.Add('')\n            }\n        }\n        return $text\n    }\n\n    static [string[]] ListOps() {\n        [PreProcessor]::Initialize()\n        return @([PreProcessor]::Registry.Keys | Sort-Object)\n    }\n}\n\nclass ContentFilter {\n    [regex]$IncludeRegex\n    [regex]$ExcludeRegex\n\n    ContentFilter([string[]]$include, [string[]]$exclude) {\n\n        if ($include -and $exclude) {\n            $common = $include | Where-Object { $_ -in $exclude }\n            if ($common) {\n                $include = $include | Where-Object { $_ -notin $common }\n                $exclude = $exclude | Where-Object { $_ -notin $common }\n            }\n        }\n\n        if ($include -and $include.Count -gt 0) {\n            $rxParts = $include | ForEach-Object { [ContentFilter]::TranslateGlob($_) }\n            $this.IncludeRegex = [regex]::new('^(?:' + ($rxParts -join '|') + ')$',\n                [RegexOptions]::Compiled -bor [RegexOptions]::IgnoreCase)\n        }\n        if ($exclude -and $exclude.Count -gt 0) {\n            $rxParts = $exclude | ForEach-Object { [ContentFilter]::TranslateGlob($_) }\n            $this.ExcludeRegex = [regex]::new('^(?:' + ($rxParts -join '|') + ')$',\n                [RegexOptions]::Compiled -bor [RegexOptions]::IgnoreCase)\n        }\n    }\n\n    [bool] Test([string]$leafName) {\n        if ($this.IncludeRegex -and -not $this.IncludeRegex.IsMatch($leafName)) {\n            return $false\n        }\n        if ($this.ExcludeRegex -and $this.ExcludeRegex.IsMatch($leafName)) {\n            return $false\n        }\n        return $true\n    }\n\n    static [string] TranslateGlob([string]$glob) {\n        $sb = [StringBuilder]::new($glob.Length * 2)\n        for ($i = 0; $i -lt $glob.Length; $i++) {\n            $c = $glob[$i]\n            switch ($c) {\n                '*' {\n                    if ($i + 1 -lt $glob.Length -and $glob[$i + 1] -eq '*') {\n                        [void]$sb.Append('.*')\n                        $i++\n                    }\n                    else { [void]$sb.Append('[^/\\\\]*') }\n                }\n                '?' { [void]$sb.Append('[^/\\\\]') }\n                '.' { [void]$sb.Append('\\.') }\n                '[' { [void]$sb.Append('[') }\n                ']' { [void]$sb.Append(']') }\n                default {\n                    if ('.+^${}()|[]\\/'.Contains($c)) {\n                        [void]$sb.Append('\\')\n                    }\n                    [void]$sb.Append($c)\n                }\n            }\n        }\n        return $sb.ToString()\n    }\n}\n\nclass FormatProfile {\n    [string]$Name\n    [string]$Description\n    [int]$Priority\n\n    [regex]$FormatDetector\n    [regex]$TurnSplitter\n    [regex]$PromptExtractor\n    [regex]$ResponsePrefix\n    [regex]$DividerPattern\n    [string]$DividerReplacement\n    [regex]$ReferenceDetector\n    [regex]$FootnotePattern\n    [regex]$InlineCitationPattern\n    [regex]$CodeFencePattern\n\n    [string[]]$DefaultOps\n}\n\nclass FormatRegistry {\n    static [Dictionary[string, FormatProfile]]$Formats\n    static [bool]$Initialized = $false\n\n    static [void] Initialize() {\n        if ([FormatRegistry]::Initialized) { return }\n        [FormatRegistry]::Formats = [Dictionary[string, FormatProfile]]::new(\n            [StringComparer]::OrdinalIgnoreCase)\n\n        $config = [ConfigLoader]::GetConfig()\n        $fmtDefs = $config['formats']\n\n        foreach ($name in $fmtDefs.Keys) {\n            $def = $fmtDefs[$name]\n            $fp = [FormatProfile]::new()\n            $fp.Name = $name\n            $fp.Description = $def['description']\n            $fp.Priority = if ($def.ContainsKey('priority')) { $def['priority'] } else { 100 }\n\n            $fp.FormatDetector = [ConfigLoader]::CompilePattern($def['detector'])\n            $fp.TurnSplitter = [ConfigLoader]::CompilePattern($def['turn_splitter'])\n            $fp.PromptExtractor = [ConfigLoader]::CompilePattern($def['prompt_extractor'])\n            $fp.ResponsePrefix = if ($def['response_prefix']) { [ConfigLoader]::CompilePattern($def['response_prefix']) } else { $null }\n\n            if ($def['divider']) {\n                $fp.DividerPattern = [ConfigLoader]::CompilePattern($def['divider'])\n                $fp.DividerReplacement = ($def['divider']['replacement'] ?? '') -replace '\\\\n', \"`n\"\n            }\n\n            $fp.ReferenceDetector = if ($def['reference_detector']) { [ConfigLoader]::CompilePattern($def['reference_detector']) } else { $null }\n            $fp.FootnotePattern = if ($def['footnote_pattern']) { [ConfigLoader]::CompilePattern($def['footnote_pattern']) } else { $null }\n            $fp.InlineCitationPattern = if ($def['inline_citation_pattern']) { [ConfigLoader]::CompilePattern($def['inline_citation_pattern']) } else { $null }\n            $fp.CodeFencePattern = if ($def['code_fence_pattern']) { [ConfigLoader]::CompilePattern($def['code_fence_pattern']) } else { $null }\n            $fp.DefaultOps = $def['default_ops']\n\n            [FormatRegistry]::Formats[$name] = $fp\n        }\n\n        [FormatRegistry]::Initialized = $true\n    }\n\n    static [FormatProfile] Detect([string]$rawContent) {\n        [FormatRegistry]::Initialize()\n\n        $ordered = [FormatRegistry]::Formats.Values |\n            Where-Object { $_.Name -ne 'Generic' } |\n            Sort-Object Priority\n        foreach ($fp in $ordered) {\n            if ($fp.FormatDetector.IsMatch($rawContent)) { return $fp }\n        }\n        return [FormatRegistry]::Formats['Generic']\n    }\n\n    static [FormatProfile] Get([string]$name) {\n        [FormatRegistry]::Initialize()\n        if ([FormatRegistry]::Formats.ContainsKey($name)) {\n            return [FormatRegistry]::Formats[$name]\n        }\n        throw [ArgumentException]::new(\n            \"Unknown format: '$name'. Available: $([FormatRegistry]::Formats.Keys -join ', ')\")\n    }\n\n    static [string[]] ListFormats() {\n        [FormatRegistry]::Initialize()\n        return @([FormatRegistry]::Formats.Keys | Sort-Object)\n    }\n}\n\nclass RegexEngine {\n    [string]$RawContent\n    [string]$ProcessedContent\n    [FormatProfile]$Format\n    [string[]]$Ops\n    [bool]$IncludeReferences\n    [bool]$StripInlineCitations\n    [bool]$ComputeHashes\n    [bool]$Raw\n    [bool]$SaveDiff\n    [List[hashtable]]$Turns\n    [string]$SourceFile\n    [string]$DetectedFormat\n    [List[string]]$DiffLog\n    [string]$Status             # Parsed, ParsedWithWarnings, Malformed\n    [List[string]]$Warnings\n\n    hidden [bool]$HasRun\n\n    RegexEngine([string]$content, [hashtable]$config) {\n        $this.RawContent = $content\n        $this.HasRun = $false\n        $this.Turns = [List[hashtable]]::new()\n        $this.DiffLog = [List[string]]::new()\n        $this.Warnings = [List[string]]::new()\n        $this.Status = 'Parsed'\n        $this.SourceFile = if ($config.ContainsKey('SourceFile')) { $config['SourceFile'] } else { '' }\n        $this.IncludeReferences = if ($config.ContainsKey('IncludeReferences')) { $config['IncludeReferences'] } else { $false }\n        $this.StripInlineCitations = -not $this.IncludeReferences\n        $this.ComputeHashes = if ($config.ContainsKey('ComputeHashes')) { $config['ComputeHashes'] } else { $false }\n        $this.Raw = if ($config.ContainsKey('Raw')) { $config['Raw'] } else { $false }\n        $this.SaveDiff = if ($config.ContainsKey('SaveDiff')) { $config['SaveDiff'] } else { $false }\n\n        if ($config.ContainsKey('Format') -and $config['Format']) {\n            $this.Format = [FormatRegistry]::Get($config['Format'])\n        } else {\n            $this.Format = [FormatRegistry]::Detect($content)\n        }\n        $this.DetectedFormat = $this.Format.Name\n        $this.Ops = if ($config.ContainsKey('Ops') -and $config['Ops']) { $config['Ops'] } else { $this.Format.DefaultOps }\n    }\n\n    [List[hashtable]] Invoke() {\n        if ($this.HasRun) {\n            throw [InvalidOperationException]::new(\n                'RegexEngine.Invoke() already called. Create a new instance.')\n        }\n        $this.HasRun = $true\n\n        $this.PreProcess()\n        $this.NormalizeDividers()\n        $this.SplitAndExtract()\n        $this.Classify()\n\n        return $this.Turns\n    }\n\n    hidden [void] PreProcess() {\n        if ($this.Raw) {\n            $this.ProcessedContent = $this.RawContent\n            return\n        }\n        if ($this.SaveDiff) {\n            $this.ProcessedContent = [PreProcessor]::ApplyWithDiff(\n                $this.RawContent, $this.Ops, $this.DiffLog)\n        } else {\n            $this.ProcessedContent = [PreProcessor]::Apply($this.RawContent, $this.Ops)\n        }\n    }\n\n    hidden [void] NormalizeDividers() {\n        if ($this.Format.DividerPattern) {\n            $this.ProcessedContent = $this.Format.DividerPattern.Replace(\n                $this.ProcessedContent, $this.Format.DividerReplacement)\n        }\n    }\n\n    hidden [void] SplitAndExtract() {\n        $sections = $this.Format.TurnSplitter.Split($this.ProcessedContent)\n        $turnIdx = 0\n\n        foreach ($raw in $sections) {\n            $section = $raw.Trim()\n            if ([string]::IsNullOrWhiteSpace($section)) { continue }\n\n            $m = $this.Format.PromptExtractor.Match($section)\n            if (-not $m.Success) { continue }\n\n            $turnIdx++\n            $prompt = $m.Groups['prompt'].Value.Trim()\n            $body = $section.Substring($m.Index + $m.Length).Trim()\n\n            if ($this.Format.ResponsePrefix) {\n                $body = $this.Format.ResponsePrefix.Replace($body, '', 1).Trim()\n            }\n\n            $response = ''\n            $references = ''\n\n            if ($body -match '\\[\\[THREAD_DIVIDER\\]\\]') {\n                $parts = $body -split '\\[\\[THREAD_DIVIDER\\]\\]', 2\n                $response = $parts[0].Trim()\n                $references = $parts[1].Trim()\n            } elseif ($this.Format.ReferenceDetector) {\n                $refMatch = $this.Format.ReferenceDetector.Match($body)\n                if ($refMatch.Success) {\n                    $response = $body.Substring(0, $refMatch.Index).Trim()\n                    $references = $body.Substring($refMatch.Index).Trim()\n                } else {\n                    $response = $body\n                }\n            } else {\n                $response = $body\n            }\n\n            $refArray = @()\n            if ($this.IncludeReferences -and $references -and $this.Format.FootnotePattern) {\n                $footMatches = $this.Format.FootnotePattern.Matches($references)\n                if ($footMatches.Count -gt 0) {\n                    foreach ($fm in $footMatches) { $refArray += $fm.Groups[1].Value.Trim() }\n                } else {\n                    $refArray = @($references -split \"`n\" |\n                        ForEach-Object { $_.Trim() } | Where-Object { $_ })\n                }\n            }\n\n            if ($this.StripInlineCitations -and $this.Format.InlineCitationPattern) {\n                $response = $this.Format.InlineCitationPattern.Replace($response, '')\n                $response = [regex]::Replace($response, '\\s{2,}', ' ').Trim()\n            }\n\n            $codeBlocks = @()\n            if ($this.Format.CodeFencePattern) {\n                $codeMatches = $this.Format.CodeFencePattern.Matches($response)\n                $blockIdx = 0\n                foreach ($cm in $codeMatches) {\n                    $lang = $cm.Groups['lang'].Value\n                    $code = $cm.Groups['code'].Value.Trim()\n                    if ($code) {\n                        $codeBlocks += @{\n                            index    = $blockIdx\n                            language = if ($lang) { $lang.ToLower() } else { 'text' }\n                            content  = $code\n                            chars    = $code.Length\n                        }\n                        $blockIdx++\n                    }\n                }\n            }\n\n            $turn = [ordered]@{\n                turn_idx    = $turnIdx\n                prompt      = $prompt\n                response    = $response\n                char_count  = $response.Length\n                code_blocks = $codeBlocks.Count\n            }\n\n            if ($this.ComputeHashes) {\n                $turn['content_hash'] = [SHA256Hash]::Compute($response)\n                $turn['prompt_hash']  = [SHA256Hash]::Compute($prompt)\n                $turn['simhash']      = [SimHash]::Compute($response)\n            }\n\n            if ($codeBlocks.Count -gt 0) {\n                $turn['code_block_details'] = $codeBlocks\n                $turn['languages'] = @($codeBlocks | ForEach-Object { $_.language } |\n                    Select-Object -Unique | Sort-Object)\n            }\n\n            if ($this.IncludeReferences -and $refArray.Count -gt 0) {\n                $turn['references'] = $refArray\n            }\n\n            $this.Turns.Add($turn)\n        }\n    }\n\n    hidden [void] Classify() {\n        if ($this.Turns.Count -eq 0) {\n            $this.Status = 'Malformed'\n            $this.Warnings.Add('No turns extracted — file may not be a supported thread format')\n            return\n        }\n\n        $emptyCount = @($this.Turns | Where-Object { [string]::IsNullOrWhiteSpace($_.response) }).Count\n        if ($emptyCount -gt 0) {\n            $this.Status = 'ParsedWithWarnings'\n            $this.Warnings.Add(\"$emptyCount turn(s) have empty responses\")\n        }\n\n        if ($this.DetectedFormat -eq 'Generic') {\n            if ($this.Status -ne 'Malformed') { $this.Status = 'ParsedWithWarnings' }\n            $this.Warnings.Add('Format auto-detected as Generic — structure may not be reliable')\n        }\n    }\n}\n\nfunction Write-FilteredMarkdown {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string]$Content,\n        [Parameter(Mandatory)] [string]$OutputPath\n    )\n\n    $dir = Split-Path $OutputPath -Parent\n    if (-not (Test-Path -LiteralPath $dir)) {\n        New-Item -Path $dir -ItemType Directory -Force | Out-Null\n    }\n    [File]::WriteAllText($OutputPath, $Content, [UTF8Encoding]::new($false))\n}\n\nfunction Write-TurnFiles {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [List[hashtable]]$Turns,\n        [Parameter(Mandatory)] [string]$OutputDirectory,\n        [int]$MinIdxWidth = 3\n    )\n\n    $config = [ConfigLoader]::GetConfig()\n    $langExtMap = $config['pagination']['language_extensions']\n\n    $idxWidth = [Math]::Max($MinIdxWidth, ($Turns.Count.ToString().Length))\n\n    if (-not (Test-Path $OutputDirectory)) {\n        New-Item -Path $OutputDirectory -ItemType Directory -Force | Out-Null\n    }\n\n    foreach ($turn in $Turns) {\n        $turnIdxStr = $turn.turn_idx.ToString().PadLeft($idxWidth, '0')\n        $turnDir = Join-Path $OutputDirectory \"turn_$turnIdxStr\"\n        New-Item -Path $turnDir -ItemType Directory -Force | Out-Null\n\n        $mdContent = \"# $($turn.prompt)`n`n$($turn.response)\"\n        $mdPath = Join-Path $turnDir \"turn_${turnIdxStr}_conversation.md\"\n        [File]::WriteAllText($mdPath, $mdContent, [UTF8Encoding]::new($false))\n\n        if ($turn.ContainsKey('code_block_details') -and $turn.code_block_details.Count -gt 0) {\n            $byLang = @{}\n            foreach ($block in $turn.code_block_details) {\n                $lang = $block.language\n                if (-not $byLang.ContainsKey($lang)) { $byLang[$lang] = [List[string]]::new() }\n                $byLang[$lang].Add($block.content)\n            }\n\n            foreach ($lang in $byLang.Keys) {\n                $ext = if ($langExtMap -and $langExtMap.ContainsKey($lang)) { $langExtMap[$lang] } else { '.txt' }\n                $codeContent = $byLang[$lang] -join \"`n`n# --- block boundary ---`n`n\"\n                $codePath = Join-Path $turnDir \"turn_${turnIdxStr}_code${ext}\"\n                [File]::WriteAllText($codePath, $codeContent, [UTF8Encoding]::new($false))\n            }\n        }\n    }\n\n    return @{ TurnCount = $Turns.Count; OutputDirectory = $OutputDirectory }\n}\n\nfunction Write-JsonlTurns {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [List[hashtable]]$Turns,\n        [Parameter(Mandatory)] [string]$OutputPath,\n        [Parameter(Mandatory)] [string]$SourceFileName,\n        [int]$MinIdxWidth = 3,\n        [switch]$NoManifest\n    )\n\n    $idxWidth = [Math]::Max($MinIdxWidth, ($Turns.Count.ToString().Length))\n\n    $metadata = @{\n        TotalRecords = 0\n        MinBytes     = [int]::MaxValue\n        MaxBytes     = 0\n    }\n\n    $dir = Split-Path $OutputPath -Parent\n    if (-not (Test-Path -LiteralPath $dir)) {\n        New-Item -Path $dir -ItemType Directory -Force | Out-Null\n    }\n\n    $sw = [StreamWriter]::new($OutputPath, $false, [UTF8Encoding]::new($false))\n    try {\n        foreach ($turn in $Turns) {\n            $byteOffset = $sw.BaseStream.Position\n            $turnIdxStr = $turn.turn_idx.ToString().PadLeft($idxWidth, '0')\n\n            $jsonObject = [ordered]@{\n                byte_offset = [int]$byteOffset\n                turn_idx    = $turnIdxStr\n                prompt      = $turn.prompt\n                response    = $turn.response\n            }\n\n            if ($turn.ContainsKey('references') -and $turn.references.Count -gt 0) {\n                $jsonObject['references'] = $turn.references\n            }\n\n            $jsonLine = $jsonObject | ConvertTo-Json -Compress\n            $sw.WriteLine($jsonLine)\n\n            $metadata.TotalRecords++\n            $lineBytes = [Encoding]::UTF8.GetByteCount($jsonLine) + 1\n            if ($lineBytes -lt $metadata.MinBytes) { $metadata.MinBytes = $lineBytes }\n            if ($lineBytes -gt $metadata.MaxBytes) { $metadata.MaxBytes = $lineBytes }\n        }\n    }\n    finally {\n        $sw.Dispose()\n    }\n\n    $metadata.FileTotalBytes = (Get-Item -LiteralPath $OutputPath).Length\n    $metadata.FileName = Split-Path -Leaf $OutputPath\n\n    return $metadata\n}\n\nfunction Split-ContentToPages {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string]$Content,\n        [string]$Language = 'text',\n        [double]$TargetPageKB = 3,\n        [double]$MaxPageKB = 8\n    )\n\n    $config = [ConfigLoader]::GetConfig()\n    $pagination = $config['pagination']\n    $aliases = $pagination['language_aliases']\n    $boundaries = $pagination['boundary_patterns']\n\n    $lang = $Language.ToLower()\n    if ($aliases -and $aliases.ContainsKey($lang)) { $lang = $aliases[$lang] }\n\n    $lines = $Content -split \"`n\"\n    $pages = [List[hashtable]]::new()\n\n    $boundaryPattern = $null\n    if ($boundaries -and $boundaries.ContainsKey($lang)) {\n        $boundaryPattern = [regex]::new($boundaries[$lang],\n            [RegexOptions]::Compiled -bor [RegexOptions]::Multiline)\n    }\n\n    if ($boundaryPattern) {\n\n        $boundaryLines = [List[int]]::new()\n        for ($i = 0; $i -lt $lines.Length; $i++) {\n            if ($boundaryPattern.IsMatch($lines[$i])) {\n                $boundaryLines.Add($i)\n            }\n        }\n\n        if ($boundaryLines.Count -gt 0) {\n\n            for ($b = 0; $b -lt $boundaryLines.Count; $b++) {\n                $startLine = $boundaryLines[$b]\n                $endLine = if ($b + 1 -lt $boundaryLines.Count) {\n                    $boundaryLines[$b + 1] - 1\n                } else {\n                    $lines.Length - 1\n                }\n\n                if ($b -eq 0 -and $startLine -gt 0) {\n                    $preamble = ($lines[0..($startLine - 1)] -join \"`n\").TrimEnd()\n                    if (-not [string]::IsNullOrWhiteSpace($preamble)) {\n                        $pages.Add(@{\n                            unit       = 'preamble'\n                            name       = $null\n                            language   = $lang\n                            lines      = @(1, $startLine)\n                            content    = $preamble\n                        })\n                    }\n                }\n\n                $chunk = ($lines[$startLine..$endLine] -join \"`n\").TrimEnd()\n                $chunkKB = [Encoding]::UTF8.GetByteCount($chunk) / 1024.0\n\n                $nameMatch = $boundaryPattern.Match($lines[$startLine])\n                $unitName = if ($nameMatch.Success) {\n                    ($lines[$startLine] -replace '^\\s+', '' -replace '\\s*[{(].*', '').Trim()\n                } else { $null }\n\n                if ($chunkKB -le $MaxPageKB) {\n\n                    $pages.Add(@{\n                        unit       = 'function'\n                        name       = $unitName\n                        language   = $lang\n                        lines      = @($startLine + 1, $endLine + 1)\n                        content    = $chunk\n                    })\n                } else {\n\n                    $fragments = Split-AtBlankLines -Lines $lines -Start $startLine -End $endLine -MaxKB $MaxPageKB\n                    $fragIdx = 0\n                    foreach ($frag in $fragments) {\n                        $pages.Add(@{\n                            unit         = 'function-fragment'\n                            name         = $unitName\n                            fragment_idx = $fragIdx\n                            language     = $lang\n                            lines        = @($frag.Start + 1, $frag.End + 1)\n                            content      = $frag.Content\n                        })\n                        $fragIdx++\n                    }\n                }\n            }\n\n            return $pages\n        }\n    }\n\n    $targetBytes = $TargetPageKB * 1024\n    $currentStart = 0\n    $currentBytes = 0\n    $lastBlank = -1\n\n    for ($i = 0; $i -lt $lines.Length; $i++) {\n        $lineBytes = [Encoding]::UTF8.GetByteCount($lines[$i]) + 1\n        $currentBytes += $lineBytes\n\n        if ([string]::IsNullOrWhiteSpace($lines[$i])) { $lastBlank = $i }\n\n        if ($currentBytes -ge $targetBytes -and $lastBlank -gt $currentStart) {\n            $chunk = ($lines[$currentStart..$lastBlank] -join \"`n\").TrimEnd()\n            if (-not [string]::IsNullOrWhiteSpace($chunk)) {\n                $pages.Add(@{\n                    unit     = 'block'\n                    name     = $null\n                    language = $lang\n                    lines    = @($currentStart + 1, $lastBlank + 1)\n                    content  = $chunk\n                })\n            }\n            $currentStart = $lastBlank + 1\n            $currentBytes = 0\n            $lastBlank = -1\n        }\n    }\n\n    if ($currentStart -lt $lines.Length) {\n        $chunk = ($lines[$currentStart..($lines.Length - 1)] -join \"`n\").TrimEnd()\n        if (-not [string]::IsNullOrWhiteSpace($chunk)) {\n            $pages.Add(@{\n                unit     = 'block'\n                name     = $null\n                language = $lang\n                lines    = @($currentStart + 1, $lines.Length)\n                content  = $chunk\n            })\n        }\n    }\n\n    return $pages\n}\n\nfunction Split-AtBlankLines {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string[]]$Lines,\n        [int]$Start,\n        [int]$End,\n        [double]$MaxKB\n    )\n\n    $maxBytes = $MaxKB * 1024\n    $fragments = [List[hashtable]]::new()\n    $fragStart = $Start\n    $currentBytes = 0\n\n    for ($i = $Start; $i -le $End; $i++) {\n        $lineBytes = [Encoding]::UTF8.GetByteCount($Lines[$i]) + 1\n        $currentBytes += $lineBytes\n\n        if ($currentBytes -ge $maxBytes -and [string]::IsNullOrWhiteSpace($Lines[$i])) {\n            $content = ($Lines[$fragStart..($i - 1)] -join \"`n\").TrimEnd()\n            if ($content) {\n                $fragments.Add(@{ Start = $fragStart; End = $i - 1; Content = $content })\n            }\n            $fragStart = $i + 1\n            $currentBytes = 0\n        }\n    }\n\n    if ($fragStart -le $End) {\n        $content = ($Lines[$fragStart..$End] -join \"`n\").TrimEnd()\n        if ($content) {\n            $fragments.Add(@{ Start = $fragStart; End = $End; Content = $content })\n        }\n    }\n\n    return $fragments\n}\n\nfunction Write-ThreadManifest {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [array]$FileResults,\n        [Parameter(Mandatory)] [string]$OutputPath,\n        [array]$ErrorRecords = @(),\n        [string[]]$EnabledOutputs,\n        [string]$ParseMode = 'Regex'\n    )\n\n    $filesJson = @($FileResults) + @($ErrorRecords | ForEach-Object {\n        @{\n            SourceFile = $_.File\n            Format     = $null\n            Status     = 'Error'\n            TurnCount  = 0\n            Warnings   = @()\n            _Error     = $_\n        }\n    }) | ForEach-Object {\n        $entry = [ordered]@{\n            source_file  = $_.SourceFile\n            format       = $_.Format\n            parse_mode   = $ParseMode\n            status       = $_.Status\n            turn_count   = $_.TurnCount\n        }\n        if ($_.Warnings -and $_.Warnings.Count -gt 0) {\n            $entry['warnings'] = $_.Warnings\n        }\n        if ($_.ContainsKey('ShardMeta') -and $_.ShardMeta) {\n            $entry['shard'] = [ordered]@{\n                file_name        = $_.ShardMeta.FileName\n                file_total_bytes = $_.ShardMeta.FileTotalBytes\n                num_records      = $_.ShardMeta.TotalRecords\n                record_byte_size_range = [ordered]@{\n                    min_bytes = $_.ShardMeta.MinBytes\n                    max_kb    = [Math]::Round($_.ShardMeta.MaxBytes / 1024.0, 2)\n                }\n            }\n        }\n        if ($_.ContainsKey('SourceHash') -and $_.SourceHash) {\n            $entry['source_hash'] = $_.SourceHash\n        }\n        if ($_.ContainsKey('_Error') -and $_._Error) {\n            $e = $_._Error\n            $entry['error'] = [ordered]@{\n                message    = $e.Message\n                type       = $e.Type\n                stack      = $e.StackTrace\n                line       = $e.Line\n                source     = $e.Source\n            }\n        }\n        $entry\n    }\n\n    $manifest = [ordered]@{\n        purpose     = 'provide operational and metadata description of shard file contents and layout to enhance access efficiency and ergonomics'\n        version     = '3.0.0'\n        parse_mode  = $ParseMode\n        outputs     = $EnabledOutputs\n        guidance    = [ordered]@{\n            truncation_warning = 'Verify per-file record counts by counting lines against stated num_records. Mismatch = truncated context window — use read_file to fetch missing rows.'\n            usage_guide        = 'Treat JSONL shards as seekable virtual databases. Use the TOC to plan targeted reads via byte_offset. Each line is independently parseable JSON.'\n        }\n        payload_description = [ordered]@{\n            type           = 'Diarized user-assistant threads parsed by prompt-response turn in sequential order'\n            file_type      = 'jsonl-like'\n            file_extension = 'none'\n            file_encoding  = 'utf-8'\n            record_format  = 'compacted JSON objects with byte_offset addressing, metadata, and content fields'\n        }\n        table_of_contents = [ordered]@{\n            num_files = $FileResults.Count\n            record_field_definitions = [ordered]@{\n                byte_offset = 'integer offset in bytes from file start to record line start'\n                turn_idx    = 'document-global sequential turn index, 1-based'\n                prompt      = 'user prompt text for this turn'\n                response    = 'assistant response text for this turn'\n                references  = '[optional] footnote reference entries'\n            }\n            files = @($filesJson)\n        }\n    }\n\n    $dir = Split-Path $OutputPath -Parent\n    if (-not (Test-Path -LiteralPath $dir)) {\n        New-Item -Path $dir -ItemType Directory -Force | Out-Null\n    }\n    $manifest | ConvertTo-Json -Depth 10 | Set-Content -LiteralPath $OutputPath -Encoding UTF8\n}\n\nfunction Invoke-ThreadParser {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0, ValueFromPipeline, ValueFromPipelineByPropertyName)]\n        [string[]]$FilePath,\n\n        [ValidateSet('Auto', 'Regex', 'Ast')]\n        [string]$ParseMode = 'Auto',\n\n        [ValidateSet('file_filtered', 'file_turns', 'jsonl_turns', 'ast_jsonl')]\n        [string[]]$Outputs = @('jsonl_turns'),\n\n        [Parameter(Position = 1)]\n        [string]$OutputDirectory,\n\n        [string]$Format,\n\n        [string[]]$Ops,\n\n        [string[]]$Include,\n\n        [string[]]$Exclude,\n\n        [switch]$IncludeReferences,\n\n        [switch]$ComputeHashes,\n\n        [switch]$Raw,\n\n        [switch]$SaveDiff,\n\n        [switch]$NoManifest,\n\n        [string]$ConfigPath,\n\n        [int]$MinIdxWidth = 3\n    )\n\n    begin {\n\n        if ($ConfigPath) { [ConfigLoader]::Load($ConfigPath) }\n\n        $allFiles = [List[string]]::new()\n    }\n\n    process {\n        foreach ($p in $FilePath) { $allFiles.Add($p) }\n    }\n\n    end {\n\n        $resolvedInputs = [List[hashtable]]::new()\n        foreach ($path in $allFiles) {\n            try {\n                $resolved = (Resolve-Path -LiteralPath $path -ErrorAction Stop).ProviderPath\n                if (Test-Path -LiteralPath $resolved -PathType Container) {\n                    $mdFiles = Get-ChildItem -LiteralPath $resolved -Filter '*.md' -File\n                    if ($mdFiles.Count -eq 0) { Write-Warning \"No .md files in: $path\" }\n                    foreach ($f in $mdFiles) { $resolvedInputs.Add(@{ FullPath = $f.FullName }) }\n                } else {\n                    $resolvedInputs.Add(@{ FullPath = $resolved })\n                }\n            } catch {\n                Write-Warning \"Could not resolve '$path': $($_.Exception.Message)\"\n            }\n        }\n\n        if ($resolvedInputs.Count -eq 0) {\n            Write-Warning 'No valid input files'\n            return\n        }\n\n        if ($Include -or $Exclude) {\n            $filter = [ContentFilter]::new($Include, $Exclude)\n            $before = $resolvedInputs.Count\n            $resolvedInputs = [List[hashtable]]::new(\n                @($resolvedInputs | Where-Object { $filter.Test((Split-Path $_.FullPath -Leaf)) }))\n            $dropped = $before - $resolvedInputs.Count\n            if ($dropped -gt 0) {\n                Write-Host \"  Filtered: $dropped file(s) excluded\" -ForegroundColor DarkGray\n            }\n            if ($resolvedInputs.Count -eq 0) {\n                Write-Warning 'No files remaining after filtering'\n                return\n            }\n        }\n\n        $timestamp = (Get-Date).ToString('yyyyMMddTHHmmss')\n        $firstDir = Split-Path $resolvedInputs[0].FullPath -Parent\n        $baseOut = if ($OutputDirectory) {\n            if ([Path]::IsPathRooted($OutputDirectory)) { $OutputDirectory }\n            else { Join-Path $firstDir $OutputDirectory }\n        } else {\n            Join-Path $firstDir '.threadparsed'\n        }\n\n        $batchDir = Join-Path $baseOut \"batch_${timestamp}_parsed\"\n\n        $allResults = [List[hashtable]]::new()\n        $errors = [List[hashtable]]::new()\n\n        foreach ($input in $resolvedInputs) {\n            $inputName = Split-Path $input.FullPath -Leaf\n            $baseName = [Path]::GetFileNameWithoutExtension($inputName)\n            $sanitized = $baseName -replace '[<>:\"/\\\\|?*\\x00-\\x1F]', '_'\n\n            Write-Host \"Parsing: $inputName\" -ForegroundColor Cyan\n\n            try {\n                $content = Get-Content -LiteralPath $input.FullPath -Raw -Encoding UTF8\n\n                $effectiveMode = $ParseMode\n                if ($effectiveMode -eq 'Auto') {\n\n                    $effectiveMode = 'Regex'\n                }\n                if ($effectiveMode -eq 'Ast') {\n                    Write-Warning \"AST engine not yet available for '$inputName' — falling back to Regex\"\n                    $effectiveMode = 'Regex'\n                }\n\n                $config = @{\n                    SourceFile        = $input.FullPath\n                    IncludeReferences = $IncludeReferences.IsPresent\n                    ComputeHashes     = $ComputeHashes.IsPresent\n                    Raw               = $Raw.IsPresent\n                    SaveDiff          = $SaveDiff.IsPresent -and -not $Raw.IsPresent\n                }\n                if ($Format) { $config.Format = $Format }\n                if ($Ops) { $config.Ops = $Ops }\n\n                $engine = [RegexEngine]::new($content, $config)\n                $turns = $engine.Invoke()\n\n                Write-Host \"  Detected: $($engine.DetectedFormat) | Turns: $($turns.Count) | Status: $($engine.Status)\" -ForegroundColor Gray\n                if ($engine.Warnings.Count -gt 0) {\n                    foreach ($w in $engine.Warnings) {\n                        Write-Host \"  Warning: $w\" -ForegroundColor Yellow\n                    }\n                }\n\n                $resultEntry = @{\n                    BaseName        = $sanitized\n                    Turns           = $turns\n                    Format          = $engine.DetectedFormat\n                    SourceFile      = $input.FullPath\n                    Status          = $engine.Status\n                    Warnings        = @($engine.Warnings)\n                    ProcessedContent = $engine.ProcessedContent\n                    DiffLog         = $engine.DiffLog\n                    TurnCount       = $turns.Count\n                }\n\n                if ($ComputeHashes.IsPresent) {\n                    $resultEntry['SourceHash'] = [SHA256Hash]::Compute($content)\n                }\n\n                $allResults.Add($resultEntry)\n            } catch {\n                $errRecord = @{\n                    File       = $inputName\n                    Message    = $_.Exception.Message\n                    Type       = $_.Exception.GetType().Name\n                    StackTrace = $_.ScriptStackTrace\n                    Line       = $_.InvocationInfo.ScriptLineNumber\n                    Source     = $_.InvocationInfo.ScriptName\n                }\n                Write-Warning \"  ERROR parsing ${inputName}: $($_.Exception.Message)\"\n                Write-Verbose \"  [$($errRecord.Type)] at line $($errRecord.Line) in $(Split-Path $errRecord.Source -Leaf)\"\n                Write-Verbose \"  Stack:`n$($errRecord.StackTrace)\"\n                $errors.Add($errRecord)\n            }\n        }\n\n        if ($allResults.Count -eq 0) {\n            Write-Warning \"No files parsed successfully ($($errors.Count) error(s))\"\n            return\n        }\n\n        New-Item -Path $batchDir -ItemType Directory -Force | Out-Null\n\n        foreach ($result in $allResults) {\n\n            if ('file_filtered' -in $Outputs -and $result.ProcessedContent) {\n                $filteredDir = Join-Path $batchDir 'filtered'\n                $filteredPath = Join-Path $filteredDir \"$($result.BaseName).clean.md\"\n                Write-FilteredMarkdown -Content $result.ProcessedContent -OutputPath $filteredPath\n                Write-Host \"  -> filtered/$($result.BaseName).clean.md\" -ForegroundColor Green\n            }\n\n            if ('file_turns' -in $Outputs -and $result.Turns.Count -gt 0) {\n                $turnsDir = Join-Path $batchDir \"turns/$($result.BaseName)\"\n                Write-TurnFiles -Turns $result.Turns -OutputDirectory $turnsDir -MinIdxWidth $MinIdxWidth\n                Write-Host \"  -> turns/$($result.BaseName)/ ($($result.TurnCount) turns)\" -ForegroundColor Green\n            }\n\n            if ('jsonl_turns' -in $Outputs -and $result.Turns.Count -gt 0) {\n                $shardsDir = Join-Path $batchDir 'shards'\n                $shardPath = Join-Path $shardsDir \"$($result.BaseName)_${timestamp}_parsed\"\n                $shardMeta = Write-JsonlTurns -Turns $result.Turns -OutputPath $shardPath `\n                    -SourceFileName $result.BaseName -MinIdxWidth $MinIdxWidth\n                $result['ShardMeta'] = $shardMeta\n                Write-Host \"  -> shards/$($shardMeta.FileName) ($($shardMeta.TotalRecords) records)\" -ForegroundColor Green\n            }\n\n            if ('ast_jsonl' -in $Outputs) {\n                Write-Host \"  -> ast_jsonl: not yet implemented (requires AST engine)\" -ForegroundColor DarkYellow\n            }\n\n            if ($SaveDiff.IsPresent -and -not $Raw.IsPresent -and $result.DiffLog -and $result.DiffLog.Count -gt 0) {\n                $diffPath = Join-Path $batchDir \"$($result.BaseName)_stripped.diff.md\"\n                $diffContent = \"# Preprocessing Diff: $($result.BaseName)`n`nFormat: $($result.Format)`n`n\"\n                $diffContent += ($result.DiffLog -join \"`n\")\n                [File]::WriteAllText($diffPath, $diffContent, [UTF8Encoding]::new($false))\n                Write-Host \"  -> $($result.BaseName)_stripped.diff.md\" -ForegroundColor DarkGray\n            }\n        }\n\n        if (-not $NoManifest) {\n            $manifestPath = Join-Path $batchDir \"manifest_${timestamp}.json\"\n            Write-ThreadManifest -FileResults $allResults -ErrorRecords $errors -OutputPath $manifestPath `\n                -EnabledOutputs $Outputs -ParseMode $ParseMode\n            Write-Host \"`nManifest: $(Split-Path $manifestPath -Leaf)\" -ForegroundColor Green\n        }\n\n        $totalTurns = ($allResults | ForEach-Object { $_.TurnCount } | Measure-Object -Sum).Sum\n        $malformed = @($allResults | Where-Object { $_.Status -eq 'Malformed' }).Count\n        $warned = @($allResults | Where-Object { $_.Status -eq 'ParsedWithWarnings' }).Count\n\n        Write-Host \"`nBatch: $($allResults.Count) file(s), $totalTurns total turns\" -ForegroundColor Cyan\n        Write-Host \"  Outputs: $($Outputs -join ', ')\" -ForegroundColor Gray\n        if ($malformed -gt 0) { Write-Host \"  Malformed: $malformed file(s)\" -ForegroundColor Red }\n        if ($warned -gt 0) { Write-Host \"  Warnings: $warned file(s)\" -ForegroundColor Yellow }\n        if ($errors.Count -gt 0) { Write-Host \"  Errors: $($errors.Count) file(s)\" -ForegroundColor Red }\n\n        return @{\n            BatchDirectory = $batchDir\n            FilesProcessed = $allResults.Count\n            TotalTurns     = $totalTurns\n            Errors         = $errors.Count\n            ErrorDetails   = @($errors)\n            Malformed      = $malformed\n            Warnings       = $warned\n            Outputs        = $Outputs\n            ParseMode      = $ParseMode\n        }\n    }\n}\n\nfunction Get-ThreadFormats {\n    \n    [CmdletBinding()]\n    param()\n\n    [FormatRegistry]::Initialize()\n    [FormatRegistry]::Formats.Values | Sort-Object Priority | ForEach-Object {\n        [PSCustomObject]@{\n            Name        = $_.Name\n            Priority    = $_.Priority\n            Description = $_.Description\n            DefaultOps  = $_.DefaultOps -join ', '\n        }\n    }\n}\n\nfunction Get-ThreadPreProcessOps {\n    \n    [CmdletBinding()]\n    param()\n\n    [PreProcessor]::Initialize()\n    [PreProcessor]::Registry.Values | Sort-Object Name | ForEach-Object {\n        [PSCustomObject]@{\n            Name        = $_.Name\n            Description = $_.Description\n            HasPattern  = $null -ne $_.Pattern\n        }\n    }\n}\n\nExport-ModuleMember -Function @(\n    'Invoke-ThreadParser'\n    'Get-ThreadFormats'\n    'Get-ThreadPreProcessOps'\n    'Write-FilteredMarkdown'\n    'Write-TurnFiles'\n    'Write-JsonlTurns'\n    'Write-ThreadManifest'\n    'Split-ContentToPages'\n) |
