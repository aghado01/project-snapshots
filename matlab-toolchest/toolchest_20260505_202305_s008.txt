idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
57 | clustering/+clustering/+evaluation/GapEvaluation.m | {30689 2681 0.3415 4.4153} | 31405 |  classdef   GapEvaluation < clustering.evaluation.ClusterCriterion\n%GapEvaluation Evaluation object based on the gap values\n%   A GapEvaluation object computes the gap criterion values which can be\n%   used for estimating the number of clusters. It's created using\n%   the following syntax:\n%\n%   EVA = EVALCLUSTERS(X,CLUST,'GAP')\n%\n%   GapEvaluation properties:\n%      X                      - Data used to create this object.\n%      Distance               - The distance metric.\n%      NumObservations        - Number of observations in X.\n%      OptimalK               - The optimal number of clusters suggested.\n%      OptimalY               - The optimal clustering solution.\n%      InspectedK             - List of the number of clusters inspected.\n%      CriterionName          - Name of the criterion.\n%      CriterionValues        - The gap values for each number of clusters.\n%      ClusteringFunction     - Clustering algorithm used.\n%      Missing                - A vector indicating which observations are missing.\n%      B                      - Number of reference data sets.\n%      ReferenceDistribution  - Method used to generate reference data.\n%      SearchMethod           - Method used to find the optimal clustering solution.\n%      LogW                   - Natural logarithm of W on the input data.\n%      ExpectedLogW           - Expectation of the natural logarithm of W on the reference data.\n%      SE                     - Standard error of the expected LogW for each number of clusters on the reference data.\n%      StdLogW                - Standard deviation of the natural logarithm of W on the reference data.\n%\n%   GapEvaluation methods:\n%      addK             - Add a new list of number of clusters for evaluation.\n%      compact          - Compact this object.\n%      increaseB        - Add more simulation data sets.\n%      plot             - Plot the gap values.\n%\n%   See also EVALCLUSTERS,clustering.evaluation.SilhouetteEvaluation\n%            clustering.evaluation.CalinskiHarabaszEvaluation,\n%            clustering.evaluation.DaviesBouldinEvaluation.\n\n% Reference:\n%    Tibshirani, R., Walther, G. and Hastie, T. (2001) Estimating the\n%    number of data clusters via the Gap statistic. Journal of the Royal\n%    Statistical Society B, 63, p411-423\n\nproperties(GetAccess=public,SetAccess=protected)\n%Distance The distance metric.\n%   The Distance property specifies the distance metric that used. It's a\n%   string representing a built-in distance measure, or a function handle.\n    Distance = 'sqEuclidean';\n\n%B The number of reference data sets.\n%   The B property is an integer representing the number of reference data\n%   sets used for computing gap criterion values.\n    B = 100;\n\n%ReferenceDistribution The method used to generate the reference data\n%   The ReferenceDistribution property is a string indicating how reference\n%   data are generated. It is 'uniform' or 'PCA'. The 'uniform' value means\n%   generating each reference feature uniformly over the range of each\n%   feature of the X property. 'PCA' means  generating the reference\n%   feature from a uniform distribution over a box aligned with the\n%   principal components of the X property.\n    ReferenceDistribution = 'PCA'\n\n%LogW Natural logarithm of W on the input data.\n%   The LogW property is a numeric vector containing the natural logarithm\n%   of W on the input data for each number of clusters in the InspectedK\n%   property. W is the sum of the within-cluster average pairwise distance\n%   values, summed over all the clusters.  If the distance is the squared\n%   Euclidean distance, then W is also equal to the sum of within-cluster\n%   sums of point-to-centroid squared Euclidean distances.\n    LogW;\n\n%ExpectedLogW Expectation of the natural logarithm of W on the reference data.\n%   The ExpectedLogW property is a numeric vector containing the estimate\n%   of expectation of the natural logarithm of W on the reference data for\n%   each number of clusters in the InspectedK property. W is the sum of the\n%   within-cluster average pairwise distances, summed over all the clusters.\n%   If the distance is the squared Euclidean distance, then W is also the\n%   sum of within-cluster sum of point-to-centroid squared Euclidean\n%   distances.\n    ExpectedLogW;\n\n%StdLogW Standard deviation of the natural logarithm of W on the reference data.\n%   The StdLogW property is a numeric vector containing the  standard\n%   deviation of the natural logarithm of W on reference data for each\n%   number of clusters. W is the sum of the within-cluster average pairwise\n%   distance values, summed over all the clusters. If the distance is the\n%   squared Euclidean distance, then W is also the sum of within-cluster\n%   sums of point-to-centroid squared Euclidean distances.\n    StdLogW;\n\n%SE Standard error of the natural logarithm of W on the reference data.\n%   The SE property is a numeric vector containing the standard error of\n%   the natural logarithm of W on reference data for each number of\n%   clusters in the InspectedK property. W is the sum of the within-cluster\n%   average pairwise distance values, summed over all the clusters. If the\n%   distance is the squared Euclidean distance, then W is also the sum of\n%   within-cluster sum of point-to-centroid squared Euclidean distances.\n    SE;\n\n%SearchMethod Method used to find the optimal clustering solution.\n%   The SearchMethod property is a string indicating how the optimal number\n%   of clusters is estimated. It's either 'FirstMaxSe' or 'GlobalMaxSe'.\n    SearchMethod;\n end\n\n    properties(GetAccess=protected,SetAccess=protected)\n        VTransform % the transfer of V, [U,S,V]=svd(X0)\n        minX2;  % The minimal value of X2, X2= X0*V; it will be empty if PCA is used\n        maxX2;  % The maximal value of X2, X2= X0*V; it will be empty if PCA is used\n        refDistLoc; %1:PCA reference distribution, 2: uniform reference distribution\n        SearchMethodLoc; %1-FirstMaxSe 2 -GlobalMaxSE\n        NXCols; % The number of columns in X\n        minX;\n        maxX;\n        needSort = false; %True means the input klist are not sorted\n        distanceLoc;\n        distLoc;\n        ValidResult;\n    end\n\n    methods\n        function this = addK(this,klist)\n\n            this = addK@clustering.evaluation.ClusterCriterion(this,klist);\n            klist = this.NewKList;\n            NumNewK = numel(klist);\n            logW = nan(1,NumNewK);\n            criterionValues = nan(1,NumNewK);\n            goodResult = true(1,NumNewK);\n            [refLogW,goodResult] = getRefLogW(this,this.B,klist,goodResult);\n            [expLogW,stdLogWRef,SERef] = getRefStats(this,refLogW);\n\n            NumOldK = numel(this.CriterionValues);\n            isNewK = [false(1,NumOldK) true(1,NumNewK)];\n\n            this.CriterionValues = [this.CriterionValues, criterionValues];\n            this.StdLogW =[this.StdLogW, stdLogWRef];\n            this.ExpectedLogW = [this.ExpectedLogW, expLogW];\n            this.SE = [this.SE, SERef];\n            this.LogW = [this.LogW logW];\n            this.ValidResult = [this.ValidResult goodResult];\n            %Sort the K values if needed\n            [sortedKlist,sortIdx] = sort(this.InspectedK);\n\n            if this.needSort || any(sortedKlist ~= this.InspectedK)\n                this.needSort = true;\n                this = sortLists(this,sortedKlist,sortIdx);\n                isNewK = isNewK(sortIdx);\n            end\n\n            %Clustering input data using the K values in the new KList,\n            p = numel(this.InspectedK);\n            oldOptimalK = this.OptimalK;\n            oldOptimalY = this.OptimalY;\n            this.OptimalK = NaN;\n            this.OptimalY = [];\n            if this.SearchMethodLoc == 1 %FirstMaxSE\n                for j = p:-1:1\n                    NC = this.InspectedK(j);\n                    IDX = [];\n                    if isNewK(j)\n                        %If this K value is new, evaluate this value\n                        [this,IDX] = getGapValue(this,j);\n                    end\n                    if ~this.ValidResult(j)\n                           continue;\n                    end\n                    %find the next valid clustering result\n                    nextValid = find(this.ValidResult(j+1:end),1);\n\n                    if ( isempty(nextValid) ... %no next valid clustering result\n                           || this.CriterionValues(j) >=...\n                                (this.CriterionValues(j+nextValid)-this.SE(j+nextValid)))\n                        this = updateOptimal(this,isNewK(j),NC,oldOptimalY,IDX,NC==oldOptimalK);\n                    end\n                end\n\n            else%GlobalMaxSE\n                maxGap= -inf;\n                th = -inf;\n\n                for j=p:-1:1\n                    NC = this.InspectedK(j);\n                    IDX = [];\n                    if isNewK(j) %this is a new K value\n                        [this,IDX] = getGapValue(this,j);\n                    end\n                    if this.CriterionValues(j) >= maxGap\n                        maxGap = this.CriterionValues(j);\n                        th = maxGap - this.SE(j);\n                    end\n                    if  this.CriterionValues(j) >= th\n                        this = updateOptimal(this,isNewK(j),NC,oldOptimalY,IDX,NC==oldOptimalK);\n                    end\n                end\n            end\n\n            %Fill the OptimalY property if it's empty\n            if ~isnan(this.OptimalK) && isempty(this.OptimalY)\n                if ~(ischar(this.ClusteringFunction) && this.FunLoc == 2)\n                    warning(message('stats:clustering:evaluation:GapEvaluation:NonOriginalOptimalY'));\n                end\n                this.OptimalY = this.evalFun(this.OptimalK);\n           end\n\n            %Re-sort some properties based on the original order of klist\n            this = backToOriginalList(this,sortIdx);\n            this = postProcess(this);\n            this.NewKList = [];\n\n      end % addK\n\n      function this = increaseB(this, B2)\n%INCREASEB increase the number of reference data sets\n%   E = INCREASEB(E, B) generates B more reference data sets and updates\n%   the gap criterion values and other related properties in the\n%   GapEvaluation object E.\n\n          checkB(B2); %Sanity check on B2\n          if isempty(this.X)\n            error(message('stats:clustering:evaluation:ClusterCriterion:EmptyX'));\n          end\n          if any(this.Missing)\n              this.PrivX = this.X(~this.Missing,:);\n          else\n              this.PrivX = this.X;\n          end\n\n          [sortedKlist,sortIdx]= sort(this.InspectedK);\n          if this.needSort\n              this = sortLists(this,sortedKlist,sortIdx);\n          end\n          this.B = this.B+B2;\n          %Clustering B simulation data sets and compute all the statistics\n\n          [refLogW, this.ValidResult] = getRefLogW(this,B2,this.InspectedK, this.ValidResult);\n          this = IncreUpdateRefStats(this,refLogW);\n          %Clustering input data set and find the optimal solution.\n          this = findOptimal(this);\n          %Resort klist and CriterionValues based on the original order\n          this = backToOriginalList(this,sortIdx);\n\n          this = postProcess(this);\n      end % increaseB\n\n        function hout = plot(this)\n%PLOT  Plot the gap values.\n%  PLOT(E) plots the gap values with one standard error bars versus the\n%  number of clusters.\n%\n%  H = PLOT(E) returns the handle to the line in the plot.\n\n            if this.needSort\n                [sortedKList, IDX] = sort(this.InspectedK);\n                h = errorbar(sortedKList,this.CriterionValues(IDX),this.SE(IDX),'-ob');\n            else\n                h = errorbar(this.InspectedK,this.CriterionValues,this.SE,'-ob');\n            end\n\n            if ~isnan(this.OptimalK)\n                optimalVal = this.CriterionValues(this.InspectedK == this.OptimalK);\n                line(this.OptimalK,optimalVal,'Marker','.','MarkerSize',10);\n            end\n            xlabel(getString(message('stats:clustering:evaluation:ClusterCriterion:PlotXlabel')));\n            YString = getString(message('stats:clustering:evaluation:ClusterCriterion:PlotYlabel',this.CriterionName));\n            ylabel(YString);\n            if nargout > 0\n                hout = h;\n            end\n        end\n    end\n\n   methods (Hidden)\n        function   this = GapEvaluation(X,Y,varargin)\n            narginchk(2,inf);\n            [basePnames, baseDflts ]= clustering.evaluation.ClusterCriterion.getAllArgs();\n\n            [klist,~,args] = ...\n                internal.stats.parseArgs(basePnames, baseDflts, varargin{:});\n\n            if isempty(klist) %klist must be provided for the gap criterion\n                error(message('stats:clustering:evaluation:GapEvaluation:EmptyKList'));\n            end\n\n            this = this@clustering.evaluation.ClusterCriterion(X,Y,klist);\n            this.CriterionName = 'Gap';\n\n            defaultSearch ='GlobalMaxSE';\n\n            pnames = {'Distance'     'ReferenceDistribution'  'B' 'SearchMethod'};\n            dflts =  {'sqeuclidean'  'PCA'                    100  defaultSearch };\n\n            [distance, refDist,BNum, searchMethod]=...\n                internal.stats.parseArgs(pnames, dflts, args{:});\n\n            %Sanity check for distance\n            %Distance must be a string or a function handle\n            distList = {'sqEuclidean' 'Euclidean' 'cityblock'  'cosine' 'correlation' 'Hamming', 'Jaccard'};\n            if ischar(distance)\n                [this.Distance,this.distLoc] = internal.stats.getParamVal(...\n                    distance,distList,'distance');\n            elseif isa(distance,'function_handle')\n                this.distLoc = 0;\n                this.Distance = distance;\n            else\n                error(message('stats:clustering:evaluation:GapEvaluation:BadDist'));\n            end\n\n            if this.distLoc ~= 1 && ischar(Y)\n                %if Y is a built-in function and the distance is not the sqEuclidean\n                %distance\n                if this.FunLoc == 1\n                    this.ClustFuncHandle = getKmeansFunc(this.Distance);\n                    %The above line is the same as the following code:\n                    %this.ClustFuncHandle = ...\n                    %   @(X,NC)(kmeans(X,NC,'rep',5,'EmptyAction','singleton','distance',this.Distance));\n                    %We assign the function handle inside the sub-function\n                    %to make sure the onCleanup object destructor will be\n                    %called when needed.\n\n                elseif this.FunLoc == 2 %linkage\n                    if this.distLoc ~= 2 %the distance is neither 'euclidean' nor 'sqeuclidean'\n                        this.LinkageFuncHandle = getLinkageFuncHandle(this.Distance);\n                        %The above line is same as the following code.\n                        %We assign the function handle inside the sub-function\n                        %to make sure the onCleanup object destructor will be\n                        %called when needed.\n                        % this.LinkageFuncHandle = ...\n                        %   @(X)(linkage(X,'linkage','ave',this.Distance);\n                     end\n\n                elseif this.FunLoc == 3 %gmdistribution\n                    error(message('stats:clustering:evaluation:GapEvaluation:InvalidDistForGM'));\n                end\n            end\n\n            %Sanity check for ReferenceDistribution\n            [this.ReferenceDistribution,this.refDistLoc]=internal.stats.getParamVal(...\n                refDist,{'PCA','Uniform'},'ReferenceDistribution');\n\n            if this.refDistLoc == 1; %PCA reference distribution\n                this = getSVD(this);\n            else\n                this.maxX = max(this.PrivX,[],1);\n                this.minX = min(this.PrivX,[],1);\n            end\n\n            checkB(BNum);\n            this.B = BNum;\n\n            [this.SearchMethod,this.SearchMethodLoc] = ...\n                internal.stats.getParamVal(...\n                searchMethod,{'FirstMaxSE','GlobalMaxSE'},'SearchMethod');\n\n            this.NXCols = size(X,2);\n\n           [sortedKlist,sortIdx] = sort(this.InspectedK);\n\n            if any(sortedKlist ~= this.InspectedK)\n                this.needSort = true;\n                this.InspectedK = sortedKlist;\n            end\n\n            %Initialize the properties:\n            p = numel(this.InspectedK);\n            this.LogW = nan(1,p);\n            this.CriterionValues = nan(1,p);\n            this.ExpectedLogW = nan(1,p);\n            this.StdLogW = nan(1,p);\n            this.SE = nan(1,p);\n            this.ValidResult = true(1,p);\n\n            %Clustering B simulation data sets and compute all the\n            %statistics on the reference data sets.\n            [refLogW,this.ValidResult] = getRefLogW(this,this.B,this.InspectedK, this.ValidResult );\n            [this.ExpectedLogW,this.StdLogW,this.SE] = getRefStats(this,refLogW);\n            %Pre-compute the Z matrix for input data if the linkage option is requested\n            if ~isempty(this.FunLoc) && this.FunLoc == 2\n                    this.LinkageZ = this.LinkageFuncHandle(this.PrivX);\n            end\n\n            %Clustering input data after the clustering on reference data\n            %is done. Find the optimal number of clusters.\n            this = findOptimal(this);\n\n            %Re-sort some properties based on the original order of input klist\n            this = backToOriginalList(this,sortIdx);\n\n            this = postProcess(this);\n        end %Constructor\n\n   end\n\n   methods (Access = protected)\n       function IDX = evalFunXref(this,NC,Xref)\n           %clustering the reference data using the original reference data\n           try\n               IDX = this.ClustFuncHandle(Xref,NC);\n           catch ME\n               if strcmp('MATLAB:UndefinedFunction', ME.identifier) ...\n                       && ~isempty(strfind(ME.message, func2str(this.ClustFuncHandle)))\n                   error(message('stats:clustering:evaluation:ClusterCriterion:FunNotFound', func2str(this.ClustFuncHandle )));\n               else\n                   warning(message('stats:clustering:evaluation:GapEvaluation:FunErrorOnRef',NC, func2str(this.ClustFuncHandle), ME.message));\n                   IDX = [];\n                   return;\n               end\n           end\n           if isa(this.ClusteringFunction,'function_handle')\n               IDX = checkFuncHandle(this,IDX,NC,false);\n           end\n\n       end\n\n        function IDX = evalFunZref(this,NC,Zref)\n            %clustering the reference data using the Z matrix generated from the original reference data\n            try\n                IDX = this.ZToClustFuncHandle(Zref,NC);\n            catch ME\n                if strcmp('MATLAB:UndefinedFunction', ME.identifier) ...\n                        && ~isempty(strfind(ME.message, func2str(this.ZToClustFuncHandle)))\n                    error(message('stats:clustering:evaluation:ClusterCriterion:FunNotFound', func2str(this.ZToClustFuncHandle)));\n                else\n                    warning(message('stats:clustering:evaluation:GapEvaluation:FunErrorOnRef',NC, func2str(this.ZToClustFuncHandle), ME.message));\n                    IDX = [];\n                end\n            end\n        end\n\n        function this = findOptimal(this)\n            %Run clustering algorithm with the number specified in\n            %this.InspectedK. Update the corresponding properties and\n            %update the OptimalY and the OptimalK properties.\n            p = numel(this.InspectedK);\n            this.OptimalK = NaN;\n            this.OptimalY = [];\n            if this.SearchMethodLoc == 1 %FirstMaxSE\n                   for j = p:-1:1\n                       NC = this.InspectedK(j);\n                       [this,IDX] = getGapValue(this,j);\n\n                       if ~this.ValidResult(j)\n                           continue;\n                       end\n                       nextValid = find(this.ValidResult(j+1:end),1);\n                       if ( isempty(nextValid) ...\n                           || this.CriterionValues(j) >=...\n                                (this.CriterionValues(j+nextValid)-this.SE(j+nextValid)))\n                            this.OptimalK = NC;\n                            this.OptimalY = IDX;\n                       end\n\n                    end\n\n                else%GlobalMaxSE\n                    maxGap= -inf;\n                    th = -inf;\n\n                    for j=p:-1:1\n                        NC = this.InspectedK(j);\n                        [this,IDX] = getGapValue(this,j);\n                        if this.CriterionValues(j) >= maxGap\n                            maxGap = this.CriterionValues(j);\n                            th = maxGap - this.SE(j);\n                        end\n                        if  this.CriterionValues(j) >= th\n                            this.OptimalK = NC;\n                            this.OptimalY = IDX;\n                        end\n                    end\n            end\n        end\n\n        function [this,IDX] = getGapValue(this,j)\n            %Update the gap value and LogW value for the jth value in the\n            %sorted this.InspectedK\n              if ~this.ValidResult(j)\n                %no need to clustering X with this particular number of\n                %clusters because clustering on the reference data has been\n                %failed at least once.\n                 IDX = [];\n                 this.CriterionValues(j) = nan;\n                 this.LogW(j) = nan;\n                 return;\n              end\n\n            NC = this.InspectedK(j);\n            if ischar(this.ClusteringFunction) && this.FunLoc==1 && this.distLoc==1\n                [IDX, ~, SUMD] = kmeans(this.PrivX,NC,'rep',5,'empty','singleton');\n                this.LogW(j) = log(sum(SUMD,1));\n            else\n                IDX = this.evalFun(NC);\n                if ~isempty(IDX)\n                    this.LogW(j) = getLogW(this.PrivX,IDX,this.Distance,this.distLoc==1);\n                else\n                    this.LogW(j)= nan;\n                    this.ValidResult(j) = false;\n                end\n            end\n\n            %Compute the gap value\n            this.CriterionValues(j) = this.ExpectedLogW(j)-this.LogW(j);\n        end\n\n        function [ExpLogW,StdLogW,SE] = getRefStats(this,refLogW)\n            %Get the Expected value,STD, SE and the variance of logW after\n            %refLogW are computed\n            %Any columns that has NaN values will make the corresponding\n            %entries in the following row vector as NaN.\n            ExpLogW = mean(refLogW,1);\n            StdLogW = sqrt(var(refLogW,1,1));\n            SE = StdLogW*sqrt(1/this.B+1); %The standard error\n        end\n\n        function [refLogW, validResult] = getRefLogW(this,B,klist, validResult)\n            %validResult is a vector recording which entries are valid\n            if ischar(this.ClusteringFunction) && this.FunLoc==1 && this.distLoc == 1\n                 warnCell={ 'stats:kmeans:EmptyClusterInBatchUpdate',...\n                'stats:kmeans:FailedToConvergeRep',...\n                'stats:kmeans:EmptyClusterRep'};\n            else\n                 warnCell={'stats:gmdistribution:FailedToConverge',...\n                'stats:gmdistribution:FailedToConvergeReps',...\n                'stats:gmdistribution:IllCondCov',...\n                'stats:kmeans:EmptyClusterInBatchUpdate',...\n                'stats:kmeans:EmptyCluster',...\n                'stats:kmeans:EmptyClusterRep',...\n                'stats:kmeans:FailedToConverge',...\n                'stats:kmeans:FailedToConvergeRep'};\n            end\n\n            p = numel(klist);\n            refLogW = nan(B,p);\n\n            for whichID = 1:numel(warnCell)\n                status(whichID) = warning('off', warnCell{whichID});\n            end\n            restoreWS = onCleanup(@() warning(status));\n\n            for b = 1:B\n                %Generate reference data B times\n                XRef = getReferenceData(this);\n                ZRef = [];\n                if ~isempty(this.FunLoc) && this.FunLoc == 2\n                    ZRef = this.LinkageFuncHandle(XRef);\n                end\n                for j = 1:p\n                    if ~validResult(j)\n                         %If clustering reference data into NC clusters has\n                         %failed before, there is no need to clustering on other\n                         %reference data sets with the same NC\n                         continue;\n                    end\n                    NC = klist(j);\n                    if ischar(this.ClusteringFunction) && this.FunLoc==1 && this.distLoc == 1\n                        [~, ~,SUMD_ref] = kmeans(XRef,NC,'rep',5,'empty','singleton');\n                        refLogW(b,j) = log(sum(SUMD_ref,1));\n                    else% other clustering except kmeans\n                        if isempty(ZRef)\n                            IDX = this.evalFunXref(NC,XRef);\n                        else\n                            IDX = this.evalFunZref(NC,ZRef);\n                        end\n                        if ~isempty(IDX)\n                            refLogW(b,j) = getLogW(XRef,IDX,this.Distance,this.distLoc==1);\n                        else\n                            validResult(j) = false;  % refLogW(b,j) will remain as NaN values\n                        end\n                    end\n                end\n            end\n            warning(status);\n        end\n\n        function  this = IncreUpdateRefStats(this,newRefLogW)\n            %Incrementally update the ExpectedLogW the StdLogW\n            %and the SE property after getting the logW matrix on new reference data\n             B2 = size(newRefLogW,1);\n             newSumLogW = sum(newRefLogW,1);\n             newSSELogW = var(newRefLogW,1,1)*B2;\n             B1 = this.B-B2;\n             oldSumLogW = this.ExpectedLogW*B1;\n             oldSSELogW = (this.StdLogW.^2)*B1;\n             SSELogW = oldSSELogW+newSSELogW+(B1/((this.B)*B2))*(((B2/B1)*oldSumLogW-newSumLogW).^2);\n             this.ExpectedLogW = (newSumLogW+ oldSumLogW)/(this.B);\n             this.StdLogW = sqrt(SSELogW/this.B);\n             this.SE = this.StdLogW*sqrt(1/this.B+1);\n\n        end\n\n        %Generate reference data\n        function Xref = getReferenceData(this)\n            if this.refDistLoc == 1%PCA reference\n                Xref = getUniformRef(this.minX2,this.maxX2,...\n                    this.NumObservations,this.NXCols);\n                Xref = Xref*this.VTransform;\n                %The mean of Xref has been shifted, but it's OK because\n                %shifting the mean doesn't affect the separation of the\n                %data.\n            else % uniform reference\n                Xref = getUniformRef(this.minX,this.maxX, this.NumObservations,...\n                    this.NXCols);\n            end\n        end\n\n        function this = getSVD(this)\n            meanX=mean(this.PrivX);\n            X0= bsxfun(@minus,this.PrivX,meanX);\n            [~,~,V] = svd(X0);\n            X2=X0*V;\n            this.minX2 = min(X2,[],1);\n            this.maxX2 = max(X2,[],1);\n            this.VTransform = V';\n        end\n\n        function this = backToOriginalList(this,sortIdx)\n            if this.needSort\n                this.InspectedK(sortIdx) = this.InspectedK;\n                this.CriterionValues(sortIdx) = this.CriterionValues;\n                this.LogW(sortIdx) = this.LogW;\n                this.StdLogW(sortIdx) = this.StdLogW;\n                this.ExpectedLogW(sortIdx) = this.ExpectedLogW;\n                this.SE(sortIdx) = this.SE;\n                this.ValidResult(sortIdx) = this.ValidResult;\n            end\n            this.CriterionValues(~this.ValidResult) = nan;\n            this.LogW(~this.ValidResult) = nan;\n            this.StdLogW(~this.ValidResult) = nan;\n            this.ExpectedLogW(~this.ValidResult) = nan;\n            this.SE(~this.ValidResult) = nan;\n\n        end\n\n        % Sort some properties based on sortIdx\n        function this = sortLists(this,sortedKlist,sortIdx)\n            this.InspectedK = sortedKlist;\n            this.CriterionValues =this.CriterionValues(sortIdx);\n            this.StdLogW = this.StdLogW(sortIdx);\n            this.ExpectedLogW = this.ExpectedLogW(sortIdx);\n            this.SE = this.SE(sortIdx);\n            this.LogW = this.LogW(sortIdx);\n            this.ValidResult = this.ValidResult(sortIdx);\n        end\n\n        %Update OptimalK and OptimalY for addK method\n        function this = updateOptimal(this,isNewK,NC,oldOptimalY,IDX,isOldOptimalK)\n            this.OptimalK = NC;\n            this.OptimalY = IDX;\n            if ~isNewK && isOldOptimalK\n                 this.OptimalY = oldOptimalY;\n            end\n        end\n    end%protected methods\n\n  end%class\n\nfunction  logW = getLogW(X,index,dist,isSqEuclidean)\n% Get log(W), where W is explained in formula 2 in Reference\np = size(X,2);\nclusts = unique(index);\nnum = numel(clusts);\ncentroids = nan(num,p);\nsumD = nan(num,1);\n\nif isSqEuclidean\n    %get sum of squared Euclidean distance of point-to-centroid distances\n    %if the distance is the squared Euclidean distance\n    for i = 1:num\n        members = (index == clusts(i));\n        if any(members)\n            ni = sum(members);\n            centroids(i,:) = sum(X(members,:),1) / ni;\n            sumD(i)= sum(pdist2(X(members,:),centroids(i,:)).^2);\n\n        end\n    end\nelse %other distance rather than squared Euclidean distance\n    %Get the sum of pairwise distances divided the number of points\n    for i = 1:num\n        members = (index == clusts(i));\n        if any(members)\n            ni = sum(members);\n            sumD(i) = sum(pdist(X(members,:),dist))/ni;\n        end\n    end\nend\n\nlogW = log(sum(sumD));\nend\n\n%Generate uniform data between on the interval from minX and maxX\nfunction Xref = getUniformRef(minX,maxX,nXRows,nXCols)\n  Xref = bsxfun(@plus,minX,bsxfun(@times,(maxX-minX),rand(nXRows,nXCols)));\nend\n\n%Sanity check on the number of simulations\nfunction checkB(B)\n     if ~isscalar(B)||~isnumeric(B) || B <= 0 || round(B) ~= B || isinf(B) ||  isnan(B)\n          error(message('stats:clustering:evaluation:GapEvaluation:InvalidB'));\n     end\nend\n\nfunction fh = getKmeansFunc(distMetric)\n%fh = @(X,NC)(kmeans(X,NC,'rep',5,'EmptyAction','singleton','distance',this.Distance));\n     fh = @nested;\n     function out = nested(X, NC)\n        out=kmeans(X,NC, 'rep',5,'EmptyAction','singleton','distance',distMetric);\n      end\nend\n\nfunction fh = getLinkageFuncHandle(distMetric)\n% fh = @(X,NC)(clusterdata(X,'linkage','ave',this.Distance));\n     fh = @nested;\n     function out = nested(X)\n        out= linkage(X,'ave',distMetric);\n      end\nend |
