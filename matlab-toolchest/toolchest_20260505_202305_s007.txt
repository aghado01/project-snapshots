idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
54 | clustering/+clustering/+evaluation/CalinskiHarabaszEvaluation.m | {6015 559 0.2953 4.5565} | 6173 |  classdef   CalinskiHarabaszEvaluation  < clustering.evaluation.ClusterCriterion\n % CalinskiHarabaszEvaluation Evaluation based on CalinskiHarabasz criterion.\n%     A CalinskiHarabaszEvaluation object computes the CalinskiHarabasz\n%     criterion values which can be used for estimating the number of\n%     clusters. It's created using the following syntax:\n%\n%     EVA = EVALCLUSTERS(X,CLUST,'CalinskiHarabasz')\n%\n%   CalinskiHarabaszEvaluation properties:\n%      X                     - Data used to create this object.\n%      NumObservations       - Number of observations in X.\n%      OptimalK              - The optimal number of clusters suggested.\n%      OptimalY              - The optimal clustering solution.\n%      InspectedK            - List of the number of clusters inspected.\n%      CriterionName         - Name of the criterion.\n%      CriterionValues       - The criterion values for each number of clusters.\n%      ClusteringFunction    - Clustering algorithm used.\n%      Missing               - A vector indicating which observations are missing.\n%\n%   CalinskiHarabaszEvaluation methods:\n%      addK                  - Add a new list of number of clusters for evaluation.\n%      compact               - Compact this object.\n%      plot                  - Plot the CalinskiHarabasz values.\n%\n%   See also EVALCLUSTERS, clustering.evaluation.GapEvaluation,\n%            clustering.evaluation.SilhouetteEvaluation,\n%            clustering.evaluation.DaviesBouldinEvaluation.\n\n  properties(GetAccess=protected, SetAccess=protected)\n      GlobalMean =[];\n end\n\n  methods %Public\n      function this = addK(this,klist)\n          this = addK@clustering.evaluation.ClusterCriterion(this,klist);\n\n          [this,newCriterionValues] = evalklist(this, this.NewKList);\n\n          this.CriterionValues = [this.CriterionValues, newCriterionValues];\n          this.NewKList = [];\n\n      end\n  end % methods Public\n\n  methods (Hidden)\n    function   this = CalinskiHarabaszEvaluation(X,Y,varargin)\n      narginchk(2,inf);\n      [basePnames, baseDflts ]= clustering.evaluation.ClusterCriterion.getAllArgs();\n\n      klist = ...\n      internal.stats.parseArgs(basePnames, baseDflts, varargin{:});\n\n      this = this@clustering.evaluation.ClusterCriterion(X,Y,klist);\n      this.CriterionName = 'CalinskiHarabasz';\n      this.GlobalMean = mean(this.PrivX,1);\n\n      if iscell(Y) %Convert the soft score to hard clustering solutions\n          Y = this.soft2hard(Y);\n      end\n\n      if isnumeric(Y) % Y is hard solution\n          Y(this.Missing,:)=[]; %Removing missing values in the clustering solutions\n\n          for j=1:numel(this.InspectedK)\n              NC = this.InspectedK(j);\n              [centroids, Ni, SUMD] = getCandSUMD(this, Y(:,j));\n              this.CriterionValues(j)= getCH(this,centroids, Ni, SUMD,NC);\n          end\n\n          [optimalV,optimalKLoc] = max(this.CriterionValues);\n          if ~isnan(optimalV)\n              this.OptimalK = this.InspectedK(optimalKLoc);\n          end\n          this.PrivX = [];\n\n      else %Y is a string or a function handle\n           if ischar(this.ClusteringFunction) && this.FunLoc == 2\n            %For linkage clustering, build the Z matrix just once\n               this.LinkageZ = this.LinkageFuncHandle(this.PrivX);\n           end\n           [this,this.CriterionValues] = evalklist(this, this.InspectedK);\n      end\n\n    end\n\n  end %end public methods\n\n  methods (Access = protected)\n     function [this, criterionValues]= evalklist(this, klist)\n         %Evaluate a klist and then update the object\n         %It can only be called when Y is a string or a function handle\n         %optimalCritv is the initial optimal criterion value.\n         %klist is the list of K values to be evaluated\n         criterionValues = nan(1,numel(klist));\n\n         if ~isnan(this.OptimalK)\n              optimalCritV = max(this.CriterionValues);\n         else\n              optimalCritV = -inf;\n         end\n\n         iskmeans = ischar(this.ClusteringFunction) && this.FunLoc==1;\n\n         for j = 1:numel(klist)\n             NC = klist(j);\n             if iskmeans %if kmeans clustering is requested\n                 [IDX,centroids,SUMD]=kmeans(this.PrivX,NC,'rep',5,'emptyaction','singleton');\n                 Ni= accumarray(IDX,ones(this.NumObservations,1));\n             else\n                 IDX = this.evalFun(NC);\n                 if isempty(IDX)\n                     continue;\n                 end\n                 [centroids, Ni, SUMD] = getCandSUMD(this, IDX);\n             end\n             criterionValues(j)= getCH(this,centroids, Ni, SUMD,NC);\n             if  criterionValues(j) > optimalCritV\n                 optimalCritV = criterionValues(j);\n                 this.OptimalK = klist(j);\n                 this.OptimalY = IDX;\n             end\n         end\n\n        this = postProcess(this);\n\n    end % evalklist\n\n  function [centroids, counts, sumD] = getCandSUMD(this, index)\n      %Get centroids, number of observations and sum of Squared Euclidean\n      %distance for each cluster based on cluster index. Index doesn't need\n      %to be integers between 1:number of clusters\n      p = size(this.PrivX,2);\n      clusts = unique(index);\n\n      num = length(clusts);\n      centroids = NaN(num,p);\n      counts = zeros(num,1);\n      sumD = zeros(num,1);\n      for i = 1:num\n          members = (index == clusts(i));\n          if any(members)\n              counts(i) = sum(members);\n              centroids(i,:) = sum(this.PrivX(members,:),1) / counts(i);\n              sumD(i)= sum(pdist2(this.PrivX(members,:),centroids(i,:)).^2);\n          end\n      end\n  end\n\n  function CH = getCH(this,centroids, Ni, SUMD,NC)\n     %Get CalinskiHarabasz value based on cluster centroids, The number of\n     %points in each cluster, sum of Squared Euclidean and the number of\n     %clusters\n      SSW = sum(SUMD,1);\n      SSB = (pdist2(centroids,this.GlobalMean)).^2;\n      SSB = sum(Ni.*SSB);\n      CH =(SSB/(NC-1))/(SSW/(this.NumObservations-NC));\n  end\nend\nend |
55 | clustering/+clustering/+evaluation/ClusterCriterion.m | {19585 1796 0.2853 4.5565} | 20100 |  classdef (Abstract) ClusterCriterion < classreg.learning.internal.DisallowVectorOps...\n                           & matlab.mixin.CustomDisplay\n% ClusterCriterion Clustering evaluation object.\n%   ClusterCriterion is an abstract class used for clustering evaluation.\n%   You cannot create an instance of this class directly. Create an\n%   instance of a derived class such as SilhouetteEvaluation,\n%   GapEvaluation, CalinskiHarabaszEvaluation or DaviesBouldinEvaluation by\n%   calling the function EVALCLUSTERS\n%\n%   ClusterCriterion properties:\n%      X                     - Data used to create this object.\n%      NumObservations       - Number of observations in X.\n%      OptimalK              - The optimal number of clusters suggested.\n%      OptimalY              - The optimal clustering solution.\n%      InspectedK            - List of the number of clusters inspected.\n%      CriterionName         - Name of the criterion.\n%      CriterionValues       - The criterion values for each number of clusters.\n%      ClusteringFunction    - Clustering algorithm used.\n%      Missing               - A vector indicating which observations are missing.\n%\n%   ClusterCriterion methods\n%      compact               - Compact this object.\n%      plot                  - Plot the criterion values.\n%      addK                  - Add a new list of number of clusters for\n%                              evaluation.\n\nproperties(GetAccess=public, SetAccess=protected)\n%OptimalY The suggested clustering solution.\n%   The OptimalY property is a column vector saving the clustering solution\n%   corresponding to the value of OptimalK. It will be empty if the\n%   clustering solutions are provided in the input.\n    OptimalY = [];\n\n%X matrix of data used to create this evaluation object.\n%   The X property is a numeric matrix of data with one row per observation\n%   and one column per variable.\n    X;\n\n%InspectedK The list of number of clusters that have been inspected.\n    %   The InspectedK property is a vector of integers indicating the\n    %   number of clusters inspected.\n    InspectedK;\n\n%CriterionValues The criterion values.\n%   The CriterionValues property saves the criterion values corresponding\n%   to each number of clusters in the InspectedK property.\n    CriterionValues;\n\n%CriterionName Name of the criterion.\n%   The CriterionName property is a string indicating the name of the\n%   criterion requested for evaluation.\n    CriterionName;\n\n%OptimalK The suggested number of clusters.\n%   The OptimalK property is an integer indicating the suggested number\n%   of clusters after comparing the values of the requested criterion.\n%   The value of OptimalK is estimated based on the following:\n%   'CalinskiHarabasz'    The number of clusters giving the\n%                         maximum CalinskiHarabasz value.\n%   'silhouette'          The number of clusters giving the maximum\n%                         silhouette value.\n%   'gap'                 The number of clusters giving the first local\n%                         maximum gap value or the global maximum gap\n%                         value within some tolerance.\n%   'DaviesBouldin'       The number of clusters giving the minimum\n%                         DaviesBouldin value.\n    OptimalK = nan;\n\n%ClusteringFunction Clustering algorithm used.\n%   The ClusteringFunction property is empty if the clustering solutions\n%   are provided in the input. Otherwise it's a string or a function handle\n%   representing the clustering algorithm requested.\n    ClusteringFunction;\n\n%NumObservations Number of observations.\n%   The NumObservations property gives the number of observations used\n%   when creating the object. This is the number of observations\n%   supplied in the data, minus the number of rows with missing (NaN)\n%   values.\n    NumObservations;\n\n%Missing A vector indicating which observations are missing.\n%   The Missing property is a column vector of logical values, with one\n%   value for each row in the input data. The value is TRUE if there are\n%   missing (NaN) values in the corresponding row of the data matrix or\n%   clustering solutions.\n    Missing;\n\nend\n\nproperties(GetAccess=protected, SetAccess=protected)\n   FunLoc=[];\n    % An integer saving the location of builtin clustering algorithm\n    % in  {'kmeans', 'linkage', 'gmdistribution'} if Y is a string;\n    % Otherwise it's empty.\n    % 1 - represents \"kmeans\".\n    % 2 - represents \"linkage\"\n    % 3 - represents \"gmdistribution\"\n\n   ClustFuncHandle = [];\n   % Either a function handle passed from the input, or a function handle\n   % based on the string representing the clustering algorithm\n   % ClusteFuncHandle will be empty if \"linkage\" clustering option is\n   % requested or a pre-computed clustering solution is provided.\n\n   LinkageFuncHandle = [];\n   % If \"linkage\" clustering option is requested. LinkageFuncHandle will be\n   % \"linkage\"; otherwise it will be empty\n\n   ZToClustFuncHandle = []\n   % If \"linkage\" clustering option is requested. ZToClustFuncHandle will be\n   % \"cluster\"; otherwise it will be empty\n\n   LinkageZ = [];\n   % LinkageZ will save the Z output from LinkageFuncHandle if \"linkage\"\n   % option is requested. Otherwise, LinkageZ will be empty.\n\n   PrivX;\n    % This property contains the input X after removing the rows contains\n    % NaN values.\n\n   NewKList = [];\n    %This property contains the new klist after removing the repeated\n    %values appeared in both InspectedK property and new klist\n\nend\n\nmethods (Access = protected,Static);%(Hidden,Static)\n    function  [args, dflts] =  getAllArgs()\n        args  ={'klist'};\n        dflts = {[]};\n    end\nend\n\n methods (Hidden, Static)\n       function a = empty(varargin)\n            error(message('stats:clustering:evaluation:ClusterCriterion:NoEmptyAllowed'));\n      end\n end\n\nmethods (Access = public)\n\n    function this = addK(this,klist)\n% ADDK Add additional number of clusters to be evaluated.\n%    E = ADDK(E, KLIST) evaluates the number of clusters provided in KLIST\n%    and then updates the related properties in clustering evaluation\n%    object E. It can only be called when E.ClusteringFunction is not\n%    empty. KLIST is a vector of positive integers containing the number of\n%    clusters to be evaluated.\n\n        if isempty(this.ClusteringFunction)\n            error(message('stats:clustering:evaluation:ClusterCriterion:AddkforEmptyClusteringFunction'));\n        end\n\n        if isempty(this.X)\n            error(message('stats:clustering:evaluation:ClusterCriterion:EmptyX'));\n        end\n\n        klist = checkKList(this,klist);\n\n        % new check whether there is any overlapped between the old klist\n        % and the new list. Remove the repeated K values if any\n         unique_klist = unique([this.InspectedK klist],'stable');\n\n         if numel(unique_klist) ~= numel([this.InspectedK klist]);\n               unique_klist(1:numel(this.InspectedK))=[];\n               if isempty(unique_klist)\n                    warning(message('stats:clustering:evaluation:ClusterCriterion:AllKHaveInspected'));\n                    return;\n               end\n               warning(message('stats:clustering:evaluation:ClusterCriterion:RepeatedK'));\n               this.NewKList = unique_klist;\n         else\n               this.NewKList = klist;\n         end\n\n      this.InspectedK =[this.InspectedK this.NewKList];\n\n      if any(this.Missing)\n          this.PrivX = this.X(~this.Missing,:);\n      else\n          this.PrivX = this.X;\n      end\n\n    end %addK\n\n  function hout = plot(this)\n%PLOT  Plot the requested criterion values\n%  PLOT(E) plots the requested criterion values versus the number of\n%  clusters.\n%\n%  H = PLOT(E) returns the handle to the line in the plot.\n\n        [sortedKList, IDX] = sort(this.InspectedK);\n        CritVals = this.CriterionValues;\n        critVals = CritVals(IDX);\n        h = plot(sortedKList, critVals ,'-ob');\n        if ~isnan(this.OptimalK)\n            optimalVal = this.CriterionValues(this.InspectedK == this.OptimalK);\n            line(this.OptimalK,optimalVal,'Marker','.','MarkerSize',10);\n        end\n        xlabel(getString(message('stats:clustering:evaluation:ClusterCriterion:PlotXlabel')));\n        YString = getString(message('stats:clustering:evaluation:ClusterCriterion:PlotYlabel',this.CriterionName));\n        ylabel(YString);\n        if nargout>=1\n            hout = h;\n        end\n end\n\n  function this = compact(this)\n%COMPACT Compact this object.\n%   C = COMPACT(E) sets E.X, E.OptimalY and E.Missing in clustering\n%   evaluation object E to be empty.\n\n      this.X = [];\n      this.OptimalY = [];\n      this.Missing = [];\n  end\nend\n\nmethods (Access = protected)\n    function klist = checkKList(this,klist)\n        %Sanity check for  klist\n        if ~(isnumeric(klist) && isvector(klist) && all(klist > 0) ...\n                && all(round(klist)==klist)) || any(isinf(klist)) || any(isnan(klist))\n            error(message('stats:clustering:evaluation:ClusterCriterion:BadKList'));\n        end\n\n        badKs = find(klist>sum(~this.Missing,1));\n        if ~isempty(badKs)\n            klist(badKs) = [];\n            if isempty(klist)\n                 error(message('stats:clustering:evaluation:ClusterCriterion:EmptyKLISTAfterRemoveBigKs'));\n            end\n            warning(message('stats:clustering:evaluation:ClusterCriterion:RemoveTooBigK'));\n        end\n        klist =klist(:)';\n    end\nfunction group = getPropertyGroups(obj)\n\n  plist1 = struct('NumObservations',   obj.NumObservations,...\n                  'InspectedK', obj.InspectedK,...\n                  'CriterionValues',obj.CriterionValues,...\n                  'OptimalK', obj.OptimalK);\n\n  group(1) = matlab.mixin.util.PropertyGroup(plist1);\n\nend\n\nfunction this = postProcess(this)\n    %add NaNs back to the rows in OptimalY if there is missing values in\n    %X or original clustering solution\n    this.PrivX = [];\n    if any(this.Missing) && ~isempty(this.OptimalY) && ...\n            size(this.OptimalY,1) == this.NumObservations\n        this.OptimalY = internal.stats.insertnan(this.Missing,this.OptimalY);\n    end\nend\n\nfunction this = getKfromY(this, Y)\n    %Y must be a matrix containing hard solutions\n    nYCols = size(Y,2);\n    for j = 1:nYCols\n        this.InspectedK(j) = numel(unique(Y(~this.Missing,j)));\n        %Warn if the number of clusters changes due to\n        %removing rows with missing values\n        oldK = sum(~isnan(unique(Y(:,j))));\n        if oldK ~= this.InspectedK(j)\n            warning(message('stats:clustering:evaluation:ClusterCriterion:KChangeInY',j,this.InspectedK(j)));\n        end\n    end\nend\n\nfunction IDX = soft2hard(this, softY)\n%Convert cell array of matrix (soft solution) to matrix (hard solution)\n    p = numel(this.InspectedK);\n    IDX = zeros(size(this.X,1),p);\n\n    for j = 1:p\n        [~, IDX(:,j)] = max(softY{j},[],2);\n        tempK = numel(unique(IDX(~this.Missing,j)));\n        if tempK ~= this.InspectedK(j)\n             warning(message('stats:clustering:evaluation:ClusterCriterion:Soft2hardBadNC',j,tempK));\n             this.InspectedK(j) = tempK;\n        end\n\n    end\nend\n\nfunction IDX = evalFun(this,NC)\n% Clustering PrivX into NC clustering using the requested clustering\n% (either built-in clustering algorithm or a function handle)\n       IDX = [];\n       funcHandle = this.ClustFuncHandle;\n       try\n           if  ischar(this.ClusteringFunction) && this.FunLoc == 2% linkage clustering\n               %this.LinkageZ must have already been computed before call this\n               %method\n               funcHandle = this.ZToClustFuncHandle;\n               IDX = this.ZToClustFuncHandle(this.LinkageZ,NC);\n            else\n               IDX = this.ClustFuncHandle(this.PrivX,NC);\n            end\n       catch ME\n           if strcmp('MATLAB:UndefinedFunction', ME.identifier) ...\n                   && ~isempty(strfind(ME.message, func2str(funcHandle)))\n               error(message('stats:clustering:evaluation:ClusterCriterion:FunNotFound', func2str(funcHandle)));\n           else\n               warning(message('stats:clustering:evaluation:ClusterCriterion:FunError', NC, func2str(funcHandle), ME.message));\n               return;\n           end\n       end\n\n       if isa(this.ClusteringFunction,'function_handle')\n           %Sanity check on results from a function handle\n           IDX = checkFuncHandle(this,IDX,NC,true);\n       end\nend\n\n%Check whether the size if valid, convert a soft clustering solution\n%into a hard clustering solution.\nfunction funcOut = checkFuncHandle(this, funcOut,nc,isOriginal)\n    if ~isnumeric(funcOut)\n        error(message('stats:clustering:evaluation:ClusterCriterion:FuncOutNonNumeric'));\n    end\n\n    [nRows, nCols]=size(funcOut);\n    if size(this.PrivX,1) ~= nRows\n        error(message('stats:clustering:evaluation:ClusterCriterion:FuncOutSizeMismatch'));\n    end\n    if  any(any(isnan(funcOut)))\n        error(message('stats:clustering:evaluation:ClusterCriterion:FuncOutNaN'));\n    end\n    if nc == 1\n        if ~isvector(funcOut)\n            error(message('stats:clustering:evaluation:ClusterCriterion:FuncOutBad'));\n        end\n        if numel(unique(funcOut)) == 1 && all(round(funcOut) == funcOut) && all(funcOut > 0)\n            isHard = true;   %view funcOut as a hard clustering solution\n        else\n            isHard = false;  %view funcOut as a hard clustering solution even it may come from a soft clustering solution.\n        end\n\n    else % nc ~= 1\n        if  nCols == 1  %vector\n            if any(round(funcOut)~= funcOut) || any(funcOut <= 0)\n                error(message('stats:clustering:evaluation:ClusterCriterion:FuncOutBad'));\n            end\n            actualNC =  numel(unique(funcOut));\n            if actualNC ~= nc\n                if isOriginal\n                    warning(message('stats:clustering:evaluation:ClusterCriterion:FuncOutNCBadOnX',nc, actualNC));\n                else\n                    warning(message('stats:clustering:evaluation:ClusterCriterion:FuncOutNCBadOnRef',nc, actualNC));\n                end\n                funcOut = [];\n                return;\n            end\n            isHard = true; %hard clustering solution\n        else\n            if nCols ~= nc\n                error(message('stats:clustering:evaluation:ClusterCriterion:FuncOutBad'));\n            end\n            isHard = false; %soft score\n        end\n    end\n\n    if ~isHard %convert soft solution to hard solution\n        [~, funcOut] = max(funcOut,[],2);\n        actualNC =  numel(unique(funcOut));\n        if actualNC ~= nc\n            if isOriginal\n                warning(message('stats:clustering:evaluation:ClusterCriterion:SoftFuncOutNCBadOnX',nc,actualNC));\n            else\n                warning(message('stats:clustering:evaluation:ClusterCriterion:SoftFuncOutNCBadOnRef',nc, actualNC));\n            end\n\n            funcOut = [];\n        end\n    end\n\nend\n\n    %Base Class Constructor\n    function this = ClusterCriterion(X,Y,klist)\n\n         %Performs some basic sanity checking on X, Y, KList, also checks\n         %missing values, finds the number of clusters to be evaluated, and\n         %set the function handle for built-in algorithms\n\n         if ~isfloat(X) || ~ismatrix(X)\n             error(message('stats:clustering:evaluation:ClusterCriterion:XBad'));\n         end\n         if isempty(X)\n             error(message('stats:clustering:evaluation:ClusterCriterion:XEmpty'));\n         end\n\n         this.X = X;\n         this.Missing = any(isnan(this.X),2); %check missing values in X\n\n         if isempty(klist)\n             %Y must be precomputed clustering solutions if klist is empty\n             if isnumeric(Y) && ismatrix(Y) %If Y contains hard clustering solution\n                 [nYRows, nYCols] = size(Y);\n                 if size(X,1) ~= nYRows;\n                     error(message('stats:clustering:evaluation:ClusterCriterion:XHardYSizeMismatch'));\n                 end\n\n                 this.Missing = this.Missing | any(isnan(Y),2);\n                 this.InspectedK = zeros(1,nYCols);\n\n                 if ~(all(all(Y(~this.Missing,:) > 0)) && all(all(round(Y(~this.Missing,:))==Y(~this.Missing,:))))\n                       error(message('stats:clustering:evaluation:ClusterCriterion:BadY'));\n                 end\n                 this = getKfromY(this, Y);\n\n             elseif iscell(Y) %Soft clustering solution\n                 p = numel(Y);\n                 this.InspectedK=zeros(1,p);\n                 for j=1:p\n                     if ~ismatrix(Y{j}) || ~isnumeric(Y{j})\n                          error(message('stats:clustering:evaluation:ClusterCriterion:BadY'));\n                     end\n                     [ny,K] = size(Y{j});\n                     if ny ~= size(this.X,1)\n                           error(message('stats:clustering:evaluation:ClusterCriterion:XSoftYSizeMismatch'));\n                     end\n                     this.Missing = this.Missing| any(isnan(Y{j}),2);\n                     this.InspectedK(j) = K;\n                 end\n\n             else\n                 error(message('stats:clustering:evaluation:ClusterCriterion:BadY'));\n             end\n\n             %We don't allow repeated K values if Y contains clustering\n             %solutions.\n             if numel(unique(this.InspectedK)) ~= numel(this.InspectedK)\n                 error(message('stats:clustering:evaluation:ClusterCriterion:SameK'));\n             end\n\n         else  % If KLIST is provided, then Y must be a string or a function handle\n             klist = checkKList(this,klist);\n\n             unique_klist = unique(klist,'stable');\n             if numel(unique_klist) ~= numel(klist) %Gives warning for repeated K values\n                 warning(message('stats:clustering:evaluation:ClusterCriterion:RepeatedKinKList'));\n             end\n             this.InspectedK = unique_klist;\n\n             if ischar(Y) %Y represents a built-in clustering algorithm\n                 ClustFuncList = {'kmeans', 'linkage', 'gmdistribution'};\n\n                 [Y,this.FunLoc]= internal.stats.getParamVal(Y,ClustFuncList,'CLUST');\n\n                 if this.FunLoc == 1 %kmeans\n                     this.ClustFuncHandle = ...\n                         @(X,NC)(kmeans(X,NC, 'EmptyAction','singleton','rep',5));\n\n                 elseif this.FunLoc == 2\n                     %Ward linkage with default Euclidean distance will be\n                     %used. ClustFunctionHandle will not be used\n                     this.LinkageFuncHandle =  @(X)(linkage(X,'ward'));\n                     this.ZToClustFuncHandle = ...\n                         @(Z,NC)(cluster(Z,'maxclust',NC));\n\n                 elseif this.FunLoc == 3\n                     this.ClustFuncHandle = ...\n                         @(X, NC)(cluster(fitgmdist(X,NC,'rep',5,'sharedcov',true),X));\n                 end\n\n             elseif isa(Y, 'function_handle')\n                 this.ClustFuncHandle = Y;\n             else\n                 error(message('stats:clustering:evaluation:ClusterCriterion:BadFun'));\n             end\n\n             this.ClusteringFunction = Y; %A string or a function handle\n\n         end %if isempty(klist)\n\n         this.NumObservations = sum(~this.Missing,1);\n\n         if this.NumObservations == 0\n             error(message('stats:clustering:evaluation:ClusterCriterion:NObservationsZero'));\n         end\n\n         if any(this.Missing)\n             this.PrivX = this.X(~this.Missing,:);\n         else\n             this.PrivX = this.X;\n         end\n\n         this.CriterionValues = nan(1,numel(this.InspectedK));\n    end\nend % protected method\n\nend% classdef |
56 | clustering/+clustering/+evaluation/DaviesBouldinEvaluation.m | {5314 486 0.3246 4.4456} | 5462 |  classdef   DaviesBouldinEvaluation  < clustering.evaluation.ClusterCriterion\n%DaviesBouldinEvaluation Evaluation object based on DaviesBouldin criterion\n%   A DaviesBouldinEvaluation object computes the DaviesBouldin criterion\n%   values which can be used for estimating the number of clusters.\n%   It's created using the following syntax:\n%\n%   EVA = EVALCLUSTERS(X,CLUST,'DaviesBouldin')\n%\n%   DaviesBouldinEvaluation properties:\n%      X                     - Data used to create this object.\n%      NumObservations       - Number of observations in X.\n%      OptimalK              - The optimal number of clusters suggested.\n%      OptimalY              - The optimal clustering solution.\n%      InspectedK            - List of the number of clusters inspected.\n%      CriterionName         - Name of the criterion.\n%      CriterionValues       - The DaviesBouldin values for each number of clusters.\n%      ClusteringFunction    - Clustering algorithm used.\n%      Missing               - A vector indicating which observations are missing.\n%\n%   DaviesBouldinEvaluation methods:\n%      addK                  - Add a new list of number of clusters for evaluation.\n%      compact               - Compact this object.\n%      plot                  - Plot the criterion values.\n%\n%  See also EVALCLUSTERS, clustering.evaluation.GapEvaluation,\n%           clustering.evaluation.SilhouetteEvaluation,\n%           clustering.evaluation.CalinskiHarabaszEvaluation,\n\n% References:\n%    Davies, D.L., Bouldin, D.W. (1979), A cluster separation measure, IEEE\n%    Transactions on Pattern Analysis and Machine Intelligence, Volume 1,\n%    Issue 2  p224-227\n\n  methods\n\n       function this = addK(this,klist)\n          this = addK@clustering.evaluation.ClusterCriterion(this,klist);\n          [this,newCriterionValues] = evalklist(this, this.NewKList);\n          this.CriterionValues = [this.CriterionValues, newCriterionValues];\n          this.NewKList = [];\n      end\n  end\n methods (Hidden)\n\n  function   this = DaviesBouldinEvaluation(X,Y,varargin)\n      narginchk(2,inf);\n      [basePnames, baseDflts]= clustering.evaluation.ClusterCriterion.getAllArgs();\n\n      klist = ...\n      internal.stats.parseArgs(basePnames, baseDflts, varargin{:});\n\n      this = this@clustering.evaluation.ClusterCriterion(X,Y,klist);\n      this.CriterionName = 'DaviesBouldin';\n\n      %Convert soft score to hard clustering solutions\n      if iscell(Y)\n          Y = this.soft2hard(Y);\n      end\n\n      if isnumeric(Y) % Y is hard solution\n          Y(this.Missing,:) = [];\n          for j=1:numel(this.InspectedK);\n              this.CriterionValues(j)= getDB(this,Y(:,j));\n          end\n\n          [minVal,optimalKLoc] = min(this.CriterionValues);\n          if ~isnan(minVal)\n             this.OptimalK=this.InspectedK(optimalKLoc);\n          end\n\n         this.PrivX = [];\n      else %Y is a string or a function handle\n          if ischar(this.ClusteringFunction) && this.FunLoc == 2 && ...\n                  isempty(this.LinkageZ)\n               this.LinkageZ = this.LinkageFuncHandle(this.PrivX);\n           end\n\n          [this,this.CriterionValues] = evalklist(this,this.InspectedK);\n      end\n\n  end\n\n  end %end public methods\n\n  methods (Access = protected)\n      function [this, criterionValues]= evalklist(this, klist)\n          %evaluate a klist and update the object\n          %it can only be applied when Y is a string or a function handle\n          %optimalCritv is the initial optimal criterion value.\n          %klist is the list of K values to be evaluated\n\n          criterionValues = nan(1,numel(klist));\n\n          if ~isnan(this.OptimalK)\n               optimalCritV = min(this.CriterionValues);\n          else\n              optimalCritV = inf;\n          end\n\n          for j = 1:numel(klist)\n              NC = klist(j);\n              IDX  = this.evalFun(NC);\n              if isempty(IDX)\n                    continue;\n              end\n              criterionValues(j) = getDB(this,IDX);\n              if  criterionValues(j) < optimalCritV\n                  optimalCritV = criterionValues(j);\n                  this.OptimalK = klist(j);\n                  this.OptimalY = IDX;\n              end\n          end\n          this = postProcess(this);\n      end\n\n      function DB = getDB(this,index)\n         clusts = unique(index);\n          num = length(clusts);\n          if num == 1\n              DB = nan;\n              return;\n          end\n          centroids = NaN(num,size(this.PrivX,2));\n\n          aveWithinD= zeros(num,1);\n          for i = 1:num\n              members = (index == clusts(i));\n              if any(members)\n                  centroids(i,:) = mean(this.PrivX(members,:),1) ;\n                  %average Euclidean distance of each observation to the centroids\n                  aveWithinD(i)= mean(pdist2(this.PrivX(members,:),centroids(i,:)));\n              end\n          end\n\n          interD = pdist(centroids,'euclidean'); %Euclidean distance\n          R = zeros(num);\n          for i = 1:num\n              for j=i+1:num %j>i\n                  R(i,j)= (aveWithinD(i)+aveWithinD(j))/ interD((i-1)*(num-i/2)+j-i);%d((I-1)*(M-I/2)+J-I)\n              end\n          end\n          R=R+R';\n\n          RI = max(R,[],1);\n          DB = mean(RI);\n      end\n  end %end protected method\n\n  end %class |
