idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
19 | scripts/start_background_run.ps1 | {3319 329 0.2118 4.8990} | 3428 | [CmdletBinding(PositionalBinding = $false)]\nparam(\n    [Parameter(Mandatory = $true)]\n    [string]$Name,\n\n    [Parameter(Mandatory = $true)]\n    [string]$ScriptPath,\n\n    [Parameter(ValueFromRemainingArguments = $true)]\n    [Alias('Args')]\n    [string[]]$ScriptArgs = @(),\n\n    [string]$RunRoot = 'reports/runs'\n)\n\n$ErrorActionPreference = 'Stop'\n\n$repoRoot = Split-Path -Parent $PSScriptRoot\n$scriptFullPath = if ([System.IO.Path]::IsPathRooted($ScriptPath))\n{\n    (Resolve-Path -LiteralPath $ScriptPath).Path\n}\nelse\n{\n    (Resolve-Path -LiteralPath (Join-Path $repoRoot $ScriptPath)).Path\n}\n\n$safeName = ($Name -replace '[^A-Za-z0-9_.-]', '-').Trim('-')\nif (-not $safeName) { $safeName = 'run' }\n$runId = '{0}-{1}' -f (Get-Date -Format 'yyyyMMdd-HHmmss'), $safeName\n\n$runRootPath = if ([System.IO.Path]::IsPathRooted($RunRoot)) { $RunRoot } else { Join-Path $repoRoot $RunRoot }\n$runDir = Join-Path $runRootPath $runId\nNew-Item -ItemType Directory -Path $runDir -Force | Out-Null\n\n$stdoutLog = Join-Path $runDir 'stdout.log'\n$stderrLog = Join-Path $runDir 'stderr.log'\n$argumentsPath = Join-Path $runDir 'arguments.json'\n$metadataPath = Join-Path $runDir 'metadata.json'\n$statusPath = Join-Path $runDir 'status.json'\n$createdUtc = (Get-Date).ToUniversalTime().ToString('o')\n\n[ordered]@{\n    arguments = @($ScriptArgs)\n} | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $argumentsPath -Encoding UTF8\n\n$metadata = [ordered]@{\n    schema_version = 1\n    run_id         = $runId\n    name           = $Name\n    repo_root      = $repoRoot\n    script_path    = $scriptFullPath\n    arguments      = @($ScriptArgs)\n    run_dir        = $runDir\n    stdout_log     = $stdoutLog\n    stderr_log     = $stderrLog\n    created_utc    = $createdUtc\n}\n$metadata | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $metadataPath -Encoding UTF8\n\n[ordered]@{\n    schema_version = 1\n    run_id         = $runId\n    name           = $Name\n    state          = 'launching'\n    pid            = $null\n    exit_code      = $null\n    created_utc    = $createdUtc\n    started_utc    = $null\n    finished_utc   = $null\n    status_utc     = (Get-Date).ToUniversalTime().ToString('o')\n    run_dir        = $runDir\n    stdout_log     = $stdoutLog\n    stderr_log     = $stderrLog\n    error          = $null\n} | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $statusPath -Encoding UTF8\n\n$workerPath = Join-Path $PSScriptRoot 'background_run_worker.ps1'\n$processArgs = @(\n    '-NoProfile',\n    '-ExecutionPolicy', 'Bypass',\n    '-File', $workerPath,\n    '-RunDir', $runDir,\n    '-ScriptPath', $scriptFullPath,\n    '-ArgumentsPath', $argumentsPath\n)\n\n$process = Start-Process `\n    -FilePath 'pwsh' `\n    -ArgumentList $processArgs `\n    -WorkingDirectory $repoRoot `\n    -RedirectStandardOutput $stdoutLog `\n    -RedirectStandardError $stderrLog `\n    -WindowStyle Hidden `\n    -PassThru\n\n$status = Get-Content -LiteralPath $statusPath -Raw | ConvertFrom-Json\n$status.state = 'launched'\n$status.pid = $process.Id\n$status.status_utc = (Get-Date).ToUniversalTime().ToString('o')\n$status | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $statusPath -Encoding UTF8\n\n[pscustomobject]@{\n    RunId     = $runId\n    Pid       = $process.Id\n    RunDir    = $runDir\n    Status    = $statusPath\n    StdOutLog = $stdoutLog\n    StdErrLog = $stderrLog\n} |
20 | scripts/update_baseline_alias.ps1 | {914 102 0.1521 4.7881} | 953 | param(\n    [Parameter(Mandatory = $true)]\n    [string]$BaselineDir\n)\n\n$ErrorActionPreference = 'Stop'\n\nif (-not (Test-Path -LiteralPath $BaselineDir -PathType Container))\n{\n    throw \"Baseline directory does not exist: $BaselineDir\"\n}\n\n$reportsDir = Join-Path (Get-Location) 'reports'\nif (-not (Test-Path -LiteralPath $reportsDir -PathType Container))\n{\n    New-Item -ItemType Directory -Path $reportsDir | Out-Null\n}\n\n$aliasPath = Join-Path $reportsDir 'baseline-latest'\nif (Test-Path -LiteralPath $aliasPath)\n{\n    Remove-Item -LiteralPath $aliasPath -Recurse -Force\n}\n\n$target = (Resolve-Path -LiteralPath $BaselineDir).Path\ntry\n{\n    New-Item -ItemType Junction -Path $aliasPath -Target $target | Out-Null\n}\ncatch\n{\n    New-Item -ItemType Directory -Path $aliasPath | Out-Null\n    Copy-Item -LiteralPath (Join-Path $target '*') -Destination $aliasPath -Recurse -Force\n}\n\nWrite-Host \"baseline-latest -> $target\" |
21 | src/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
22 | src/client/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
23 | src/client/providers.py | {7984 851 0.1617 5.0619} | 8831 | \"\"\"Provider abstraction layer (Phase 1).\n\nGoals:\n- Uniform interface for generating model responses.\n- Deterministic seeding passthrough when provider supports it.\n- Retry, basic backoff, structured trace metadata.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any, Dict, Iterable, Optional\nimport os, time, json, random\nfrom urllib import error, request\n\nDEFAULT_MAX_RETRIES = 3\nBACKOFF_SECONDS = 1.5\n\n@dataclass\nclass ProviderRequest:\n\tprompt: str\n\tmodel: str\n\ttemperature: float = 0.2\n\tmax_tokens: int = 512\n\tseed: Optional[int] = None\n\tmetadata: Optional[Dict[str, Any]] = None\n\n@dataclass\nclass ProviderResponse:\n\ttext: str\n\tmodel: str\n\tlatency_ms: float\n\tinput_tokens: int | None = None\n\toutput_tokens: int | None = None\n\tcost_usd: float | None = None\n\terror: str | None = None\n\traw: Dict[str, Any] | None = None\n\nclass ProviderClient:\n\tname: str = \"base\"\n\n\tdef __init__(self, model: str, **kwargs):\n\t\tself.model = model\n\t\tself.kwargs = kwargs\n\n\t# Phase 1: synchronous single prompt generate\n\tdef generate(self, req: ProviderRequest) -> ProviderResponse:  # pragma: no cover - abstract\n\t\traise NotImplementedError\n\n\t# Shared retry wrapper\n\tdef safe_generate(self, req: ProviderRequest, max_retries: int = DEFAULT_MAX_RETRIES) -> ProviderResponse:\n\t\tattempt = 0\n\t\tlast_err: str | None = None\n\t\twhile attempt <= max_retries:\n\t\t\ttry:\n\t\t\t\tstart = time.perf_counter()\n\t\t\t\tresp = self.generate(req)\n\t\t\t\telapsed = (time.perf_counter() - start) * 1000\n\t\t\t\tresp.latency_ms = elapsed\n\t\t\t\treturn resp\n\t\t\texcept Exception as e:  # noqa: BLE001\n\t\t\t\tlast_err = str(e)\n\t\t\t\tif attempt == max_retries:\n\t\t\t\t\treturn ProviderResponse(\n\t\t\t\t\t\ttext=\"\",\n\t\t\t\t\t\tmodel=self.model,\n\t\t\t\t\t\tlatency_ms=0.0,\n\t\t\t\t\t\terror=last_err,\n\t\t\t\t\t\traw={\"attempts\": attempt + 1},\n\t\t\t\t\t)\n\t\t\t\ttime.sleep(BACKOFF_SECONDS * (attempt + 1))\n\t\t\t\tattempt += 1\n\t\treturn ProviderResponse(\n\t\t\ttext=\"\",\n\t\t\tmodel=self.model,\n\t\t\tlatency_ms=0.0,\n\t\t\terror=last_err or \"unknown provider error\",\n\t\t\traw={\"attempts\": attempt},\n\t\t)\n\nclass EchoDeterministicProvider(ProviderClient):\n\t\"\"\"Deterministic echo provider for baseline runs.\n\n\tUseful for CI / tests before wiring real APIs. Simulates token counts & latency.\n\t\"\"\"\n\n\tname = \"echo\"\n\n\tdef generate(self, req: ProviderRequest) -> ProviderResponse:\n\t\tseed = req.seed if req.seed is not None else 0\n\t\trnd = random.Random(seed)\n\t\tsimulated_latency = 25 + rnd.random() * 15  # ms\n\t\ttime.sleep(simulated_latency / 1000.0)\n\t\t# Basic synthetic expansion to emulate model behaviour\n\t\treply = f\"[echo:{self.model}] \" + req.prompt.strip()[:200]\n\t\tinput_tokens = len(req.prompt.split())\n\t\toutput_tokens = len(reply.split())\n\t\treturn ProviderResponse(\n\t\t\ttext=reply,\n\t\t\tmodel=self.model,\n\t\t\tlatency_ms=simulated_latency,\n\t\t\tinput_tokens=input_tokens,\n\t\t\toutput_tokens=output_tokens,\n\t\t\tcost_usd=0.0,\n\t\t\traw={\"provider\": self.name},\n\t\t)\n\nclass OpenAICompatibleProvider(ProviderClient):\n\t\"\"\"OpenAI-compatible chat-completions adapter.\n\n\tWorks with local servers such as llama.cpp, plus hosted APIs that\n\texpose `/v1/chat/completions` semantics. It intentionally avoids an SDK so the\n\techo provider remains the only hard dependency for CI.\n\t\"\"\"\n\n\tname = \"openai_compatible\"\n\n\tdef __init__(\n\t\tself,\n\t\tmodel: str,\n\t\tbase_url: str,\n\t\tprovider_name: str | None = None,\n\t\tapi_key: str | None = None,\n\t\tapi_key_env: str | None = None,\n\t\ttimeout_s: int | float = 120,\n\t\tchat_path: str = \"/chat/completions\",\n\t\theaders: dict[str, str] | None = None,\n\t\t**kwargs,\n\t):\n\t\tsuper().__init__(model=model, **kwargs)\n\t\tself.name = provider_name or self.name\n\t\tself.base_url = base_url.rstrip(\"/\")\n\t\tself.chat_path = chat_path if chat_path.startswith(\"/\") else f\"/{chat_path}\"\n\t\tself.timeout_s = float(timeout_s)\n\t\tself.api_key = api_key or (os.environ.get(api_key_env) if api_key_env else None)\n\t\tself.headers = headers or {}\n\n\tdef generate(self, req: ProviderRequest) -> ProviderResponse:\n\t\tmessages: list[dict[str, str]] = []\n\t\tif req.metadata and isinstance(req.metadata.get(\"messages\"), list):\n\t\t\tmessages = [\n\t\t\t\t{\"role\": str(msg.get(\"role\", \"user\")), \"content\": str(msg.get(\"content\", \"\"))}\n\t\t\t\tfor msg in req.metadata[\"messages\"]\n\t\t\t\tif isinstance(msg, dict) and msg.get(\"content\") is not None\n\t\t\t]\n\t\telse:\n\t\t\tif req.metadata and req.metadata.get(\"system\"):\n\t\t\t\tmessages.append({\"role\": \"system\", \"content\": str(req.metadata[\"system\"])})\n\t\t\tmessages.append({\"role\": \"user\", \"content\": req.prompt})\n\n\t\tpayload: dict[str, Any] = {\n\t\t\t\"model\": req.model or self.model,\n\t\t\t\"messages\": messages,\n\t\t\t\"temperature\": req.temperature,\n\t\t\t\"max_tokens\": req.max_tokens,\n\t\t}\n\t\tif req.seed is not None:\n\t\t\tpayload[\"seed\"] = req.seed\n\t\tif req.metadata and isinstance(req.metadata.get(\"extra_body\"), dict):\n\t\t\tpayload.update(req.metadata[\"extra_body\"])\n\n\t\tbody = json.dumps(payload).encode(\"utf-8\")\n\t\theaders = {\"Content-Type\": \"application/json\", **self.headers}\n\t\tif self.api_key:\n\t\t\theaders.setdefault(\"Authorization\", f\"Bearer {self.api_key}\")\n\n\t\turl = f\"{self.base_url}{self.chat_path}\"\n\t\treq_obj = request.Request(url, data=body, headers=headers, method=\"POST\")\n\t\ttry:\n\t\t\twith request.urlopen(req_obj, timeout=self.timeout_s) as resp:\n\t\t\t\traw = json.loads(resp.read().decode(\"utf-8\"))\n\t\texcept error.HTTPError as exc:\n\t\t\tdetail = exc.read().decode(\"utf-8\", errors=\"replace\")\n\t\t\traise RuntimeError(f\"{self.name} HTTP {exc.code}: {detail}\") from exc\n\t\texcept error.URLError as exc:\n\t\t\traise RuntimeError(f\"{self.name} request failed: {exc.reason}\") from exc\n\n\t\tchoices = raw.get(\"choices\") or []\n\t\tchoice = choices[0] if choices else {}\n\t\tmessage = choice.get(\"message\") or {}\n\t\ttext = message.get(\"content\") or choice.get(\"text\") or \"\"\n\t\tusage = raw.get(\"usage\") or {}\n\t\treturn ProviderResponse(\n\t\t\ttext=text,\n\t\t\tmodel=raw.get(\"model\") or self.model,\n\t\t\tlatency_ms=0.0,\n\t\t\tinput_tokens=usage.get(\"prompt_tokens\"),\n\t\t\toutput_tokens=usage.get(\"completion_tokens\"),\n\t\t\tcost_usd=None,\n\t\t\traw={\"provider\": self.name, \"base_url\": self.base_url},\n\t\t)\n\n_REGISTRY: Dict[str, type[ProviderClient]] = {\n\tEchoDeterministicProvider.name: EchoDeterministicProvider,\n\tOpenAICompatibleProvider.name: OpenAICompatibleProvider,\n}\n\ndef create_provider(name: str, model: str, **kwargs) -> ProviderClient:\n\tif name not in _REGISTRY:\n\t\traise ValueError(f\"Unknown provider adapter '{name}'. Registered: {list(_REGISTRY)}\")\n\treturn _REGISTRY[name](model=model, **kwargs)\n\ndef load_provider_from_config(cfg: dict, override_name: str | None = None, override_model: str | None = None) -> ProviderClient:\n\tproviders: Iterable[dict] = cfg.get(\"providers\", [])\n\tdefault_model = cfg.get(\"default_model\")\n\tsel_name = override_name\n\tsel_model = override_model or default_model\n\tif sel_name is None:\n\t\t# first provider entry if not specified\n\t\tfirst = next(iter(providers), None)\n\t\tif first is None:\n\t\t\traise ValueError(\"providers.yaml missing providers list\")\n\t\tsel_name = first.get(\"name\")\n\t\tsel_model = sel_model or first.get(\"model\")\n\t# find matching provider row\n\tmatch = None\n\tfor p in providers:\n\t\tif p.get(\"name\") == sel_name:\n\t\t\tmatch = p\n\t\t\tbreak\n\tif match is None:\n\t\traise ValueError(f\"Provider '{sel_name}' not found in config\")\n\tsel_model = override_model or match.get(\"model\") or sel_model\n\tif not sel_name or not sel_model:\n\t\traise ValueError(\"Provider config must resolve both provider name and model\")\n\tadapter_name = str(match.get(\"type\") or sel_name)\n\tprovider_kwargs = {k: v for k, v in match.items() if k not in {\"name\", \"model\", \"type\"}}\n\tprovider_kwargs.setdefault(\"provider_name\", str(sel_name))\n\treturn create_provider(adapter_name, model=str(sel_model), **provider_kwargs)\n\ndef save_trace_line(path: str, record: dict) -> None:\n\tos.makedirs(os.path.dirname(path), exist_ok=True)\n\twith open(path, \"a\", encoding=\"utf-8\") as f:\n\t\tf.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n\n__all__ = [\n\t\"ProviderClient\",\n\t\"ProviderRequest\",\n\t\"ProviderResponse\",\n\t\"EchoDeterministicProvider\",\n\t\"OpenAICompatibleProvider\",\n\t\"create_provider\",\n\t\"load_provider_from_config\",\n\t\"save_trace_line\",\n] |
24 | src/metrics/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
25 | src/metrics/agreement.py | {1212 141 0.2038 4.6405} | 1265 | \"\"\"Inter-rater and inter-run agreement metrics.\"\"\"\n\nfrom __future__ import annotations\n\nfrom collections import Counter\nfrom typing import Sequence, Hashable\n\ndef percent_agreement(labels_a: Sequence[Hashable], labels_b: Sequence[Hashable]) -> float:\n    if len(labels_a) != len(labels_b):\n        raise ValueError(\"label sequences must have the same length\")\n    if not labels_a:\n        return 0.0\n    matches = sum(1 for a, b in zip(labels_a, labels_b) if a == b)\n    return round(matches / len(labels_a), 6)\n\ndef cohen_kappa(labels_a: Sequence[Hashable], labels_b: Sequence[Hashable]) -> float:\n    \"\"\"Cohen's kappa for two categorical raters.\"\"\"\n\n    if len(labels_a) != len(labels_b):\n        raise ValueError(\"label sequences must have the same length\")\n    n = len(labels_a)\n    if n == 0:\n        return 0.0\n\n    observed = percent_agreement(labels_a, labels_b)\n    counts_a = Counter(labels_a)\n    counts_b = Counter(labels_b)\n    categories = set(counts_a) | set(counts_b)\n    expected = sum((counts_a[c] / n) * (counts_b[c] / n) for c in categories)\n    if expected == 1.0:\n        return 1.0\n    return round((observed - expected) / (1 - expected), 6)\n\n__all__ = [\"percent_agreement\", \"cohen_kappa\"] |
26 | src/metrics/calibration.py | {2224 252 0.2127 4.7032} | 2323 | \"\"\"Calibration and score-distribution helpers.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Iterable, Sequence, Any\nimport math\nimport statistics\n\nfrom .significance import percentile\n\ndef _clean(values: Iterable[float | int | None]) -> list[float]:\n    return [float(v) for v in values if v is not None]\n\ndef brier_score(probabilities: Sequence[float], outcomes: Sequence[int | bool]) -> float:\n    if len(probabilities) != len(outcomes):\n        raise ValueError(\"probabilities and outcomes must have the same length\")\n    if not probabilities:\n        return 0.0\n    total = 0.0\n    for probability, outcome in zip(probabilities, outcomes):\n        total += (float(probability) - float(bool(outcome))) ** 2\n    return round(total / len(probabilities), 6)\n\ndef distribution_summary(values: Iterable[float | int | None]) -> dict[str, Any]:\n    data = _clean(values)\n    if not data:\n        return {\"n\": 0}\n    return {\n        \"n\": len(data),\n        \"mean\": round(statistics.fmean(data), 6),\n        \"std\": round(statistics.pstdev(data), 6) if len(data) > 1 else 0.0,\n        \"min\": round(min(data), 6),\n        \"p25\": round(percentile(data, 25), 6),\n        \"p50\": round(percentile(data, 50), 6),\n        \"p75\": round(percentile(data, 75), 6),\n        \"p95\": round(percentile(data, 95), 6),\n        \"max\": round(max(data), 6),\n    }\n\ndef cronbach_alpha(rows: Sequence[Sequence[float | int]]) -> float | None:\n    \"\"\"Cronbach's alpha for examples x rubric-items matrices.\"\"\"\n\n    if not rows or len(rows[0]) < 2:\n        return None\n    item_count = len(rows[0])\n    if any(len(row) != item_count for row in rows):\n        raise ValueError(\"all rows must have the same number of items\")\n\n    columns = [[float(row[idx]) for row in rows] for idx in range(item_count)]\n    item_variance_sum = sum(statistics.pvariance(column) for column in columns)\n    totals = [sum(float(value) for value in row) for row in rows]\n    total_variance = statistics.pvariance(totals)\n    if math.isclose(total_variance, 0.0):\n        return None\n    alpha = (item_count / (item_count - 1)) * (1 - item_variance_sum / total_variance)\n    return round(alpha, 6)\n\n__all__ = [\"brier_score\", \"distribution_summary\", \"cronbach_alpha\"] |
27 | src/metrics/citations.py | {2931 375 0.1679 4.8724} | 3163 | \"\"\"Citations metric (Phase 2).\n\nExtract inline numeric citations in the form [1], [2], etc. Optionally\ncompare against an expected set of reference sources (domain list) if provided.\n\nHeuristic:\n1. Extract numbers inside square brackets, allowing repeats.\n2. Unique count of produced citations = produced_set size.\n3. If expected_sources provided, we treat them as the target citation range 1..N.\n   - expected_ids = {1..len(expected_sources)}\n   - precision = matched / produced_unique (0 if none produced)\n   - recall    = matched / expected_count (0 if expected_count>0 and none matched)\n   - f1 = 2PR/(P+R) if P+R>0 else 0\n4. If no expected sources: we emit density features only (produced count) and f1=None.\n\nReturns dict with keys: produced_unique, produced_total, expected_count,\nmatched, precision, recall, f1.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import List, Dict, Any, Optional\nimport re\n\n_CIT_RE = re.compile(r\"\\[(\\d+)\\]\")\n\ndef detect_citations(answer: str) -> List[int]:\n\t\"\"\"Return ordered list of numeric citation ids like [1], [2].\"\"\"\n\tif not answer:\n\t\treturn []\n\treturn [int(m.group(1)) for m in _CIT_RE.finditer(answer)]\n\ndef compute_faithfulness(answer: str, references: Optional[List[str]] = None) -> float:\n\t\"\"\"Thin wrapper returning citation F1 if references given else 0.\n\n\tThis mirrors score_citations (expected count = len(references)) and returns\n\tits F1 for convenience in tests / future composite metrics.\n\t\"\"\"\n\tif not references:\n\t\treturn 0.0\n\tr = score_citations(answer, expected_sources=references)\n\treturn float(r.get(\"f1\") or 0.0)\n\ndef score_citations(answer: str, expected_sources: Optional[List[str]] = None) -> Dict[str, Any]:\n\tif not answer:\n\t\treturn {\n\t\t\t\"produced_unique\": 0,\n\t\t\t\"produced_total\": 0,\n\t\t\t\"expected_count\": len(expected_sources) if expected_sources else None,\n\t\t\t\"matched\": 0,\n\t\t\t\"precision\": 0.0,\n\t\t\t\"recall\": 0.0,\n\t\t\t\"f1\": 0.0 if expected_sources else None,\n\t\t}\n\n\tnums = [int(m.group(1)) for m in _CIT_RE.finditer(answer)]\n\tproduced_total = len(nums)\n\tproduced_set = set(nums)\n\texpected_count = len(expected_sources) if expected_sources else None\n\n\tif expected_sources and expected_count is not None:\n\t\texpected_ids = set(range(1, expected_count + 1))  # sequential expectation\n\t\tmatched = len(produced_set & expected_ids)\n\t\tprecision = matched / len(produced_set) if produced_set else 0.0\n\t\trecall = matched / expected_count if expected_count else 0.0\n\t\tif precision + recall > 0:\n\t\t\tf1 = 2 * precision * recall / (precision + recall)\n\t\telse:\n\t\t\tf1 = 0.0\n\telse:\n\t\tmatched = 0\n\t\tprecision = 0.0\n\t\trecall = 0.0\n\t\tf1 = None\n\n\treturn {\n\t\t\"produced_unique\": len(produced_set),\n\t\t\"produced_total\": produced_total,\n\t\t\"expected_count\": expected_count,\n\t\t\"matched\": matched,\n\t\t\"precision\": round(precision, 4),\n\t\t\"recall\": round(recall, 4),\n\t\t\"f1\": round(f1, 4) if f1 is not None else None,\n\t}\n\n__all__ = [\"score_citations\", \"detect_citations\", \"compute_faithfulness\"] |
28 | src/metrics/consistency.py | {2896 296 0.2462 4.5479} | 3040 | \"\"\"Response consistency and variance metrics.\"\"\"\n\nfrom __future__ import annotations\n\nfrom collections import Counter\nfrom itertools import combinations\nfrom math import sqrt\nfrom typing import Any\nimport statistics\nimport re\n\nfrom ..client.providers import ProviderClient, ProviderRequest\n\n_TOKEN_RE = re.compile(r\"[a-z0-9]+(?:'[a-z0-9]+)?\", re.I)\n\ndef _counts(text: str) -> Counter[str]:\n    return Counter(m.group(0).lower() for m in _TOKEN_RE.finditer(text or \"\"))\n\ndef _cosine(left: Counter[str], right: Counter[str]) -> float:\n    if not left or not right:\n        return 0.0\n    keys = set(left) | set(right)\n    dot = sum(left[k] * right[k] for k in keys)\n    left_norm = sqrt(sum(v * v for v in left.values()))\n    right_norm = sqrt(sum(v * v for v in right.values()))\n    if not left_norm or not right_norm:\n        return 0.0\n    return dot / (left_norm * right_norm)\n\ndef score_response_consistency(responses: list[str]) -> dict[str, Any]:\n    \"\"\"Compute pairwise lexical similarity across repeated generations.\"\"\"\n\n    if len(responses) < 2:\n        return {\n            \"n\": len(responses),\n            \"pair_count\": 0,\n            \"mean_similarity\": None,\n            \"variance\": None,\n            \"min_similarity\": None,\n            \"max_similarity\": None,\n        }\n\n    counts = [_counts(response) for response in responses]\n    similarities = [_cosine(a, b) for a, b in combinations(counts, 2)]\n    mean_similarity = statistics.fmean(similarities) if similarities else 0.0\n    variance = statistics.pvariance(similarities) if len(similarities) > 1 else 0.0\n    return {\n        \"n\": len(responses),\n        \"pair_count\": len(similarities),\n        \"mean_similarity\": round(mean_similarity, 4),\n        \"variance\": round(variance, 6),\n        \"min_similarity\": round(min(similarities), 4),\n        \"max_similarity\": round(max(similarities), 4),\n    }\n\ndef run_consistency(\n    prompt: str,\n    provider: ProviderClient,\n    *,\n    n_runs: int = 5,\n    temperature: float = 0.8,\n    base_seed: int | None = None,\n    max_tokens: int = 512,\n) -> dict[str, Any]:\n    \"\"\"Generate repeated responses and score their variance.\n\n    This is opt-in because it multiplies provider calls.\n    \"\"\"\n\n    responses: list[str] = []\n    errors: list[str] = []\n    for idx in range(max(0, n_runs)):\n        seed = None if base_seed is None else base_seed + idx\n        req = ProviderRequest(\n            prompt=prompt,\n            model=provider.model,\n            temperature=temperature,\n            max_tokens=max_tokens,\n            seed=seed,\n        )\n        resp = provider.safe_generate(req)\n        responses.append(resp.text)\n        if resp.error:\n            errors.append(resp.error)\n\n    result = score_response_consistency(responses)\n    result[\"responses\"] = responses\n    result[\"errors\"] = errors\n    return result\n\n__all__ = [\"score_response_consistency\", \"run_consistency\"] |
29 | src/metrics/grounding.py | {3051 351 0.2481 4.6579} | 3202 | \"\"\"Deterministic grounding heuristics.\n\nThis is intentionally lightweight: each response sentence is treated as a claim\nand compared against the supplied context with n-gram overlap. It is not a full\nhallucination detector, but it gives the harness a reproducible grounding signal\nthat can later be replaced or augmented by retrieval-aware and LLM-judge methods.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom collections.abc import Iterable\nfrom typing import Any\nimport re\n\n_TOKEN_RE = re.compile(r\"[a-z0-9]+(?:'[a-z0-9]+)?\", re.I)\n_SENTENCE_RE = re.compile(r\"(?<=[.!?])\\s+\")\n\ndef _as_text(context: str | Iterable[str] | None) -> str:\n    if context is None:\n        return \"\"\n    if isinstance(context, str):\n        return context\n    return \"\\n\".join(str(part) for part in context if part)\n\ndef _tokens(text: str) -> list[str]:\n    return [m.group(0).lower() for m in _TOKEN_RE.finditer(text or \"\")]\n\ndef _sentences(text: str) -> list[str]:\n    return [s.strip() for s in _SENTENCE_RE.split(text.strip()) if s.strip()]\n\ndef _ngrams(tokens: list[str], n: int) -> set[tuple[str, ...]]:\n    if not tokens:\n        return set()\n    n = max(1, min(n, len(tokens)))\n    return {tuple(tokens[i : i + n]) for i in range(0, len(tokens) - n + 1)}\n\ndef _jaccard(left: set[tuple[str, ...]], right: set[tuple[str, ...]]) -> float:\n    if not left or not right:\n        return 0.0\n    return len(left & right) / len(left | right)\n\ndef score_grounding(\n    response: str,\n    context: str | Iterable[str] | None,\n    *,\n    ngram_size: int = 3,\n    support_threshold: float = 0.08,\n) -> dict[str, Any]:\n    \"\"\"Score how many response claims are lexically supported by context.\n\n    Returns a dict with `score` in [0, 1] when context is available, or `None`\n    when the metric is not applicable.\n    \"\"\"\n\n    context_text = _as_text(context)\n    if not response.strip() or not context_text.strip():\n        return {\n            \"score\": None,\n            \"reason\": \"missing_response_or_context\",\n            \"total_claims\": 0,\n            \"grounded_claims\": 0,\n            \"avg_overlap\": 0.0,\n            \"claims\": [],\n        }\n\n    context_tokens = _tokens(context_text)\n    context_grams = _ngrams(context_tokens, ngram_size)\n    claims = []\n    for sentence in _sentences(response):\n        claim_tokens = _tokens(sentence)\n        grams = _ngrams(claim_tokens, ngram_size)\n        overlap = _jaccard(grams, context_grams)\n        claims.append(\n            {\n                \"claim\": sentence,\n                \"overlap\": round(overlap, 4),\n                \"supported\": overlap >= support_threshold,\n            }\n        )\n\n    total = len(claims)\n    grounded = sum(1 for claim in claims if claim[\"supported\"])\n    avg_overlap = sum(float(claim[\"overlap\"]) for claim in claims) / total if total else 0.0\n    return {\n        \"score\": round(grounded / total, 4) if total else 0.0,\n        \"total_claims\": total,\n        \"grounded_claims\": grounded,\n        \"avg_overlap\": round(avg_overlap, 4),\n        \"claims\": claims,\n    }\n\n__all__ = [\"score_grounding\"] |
30 | src/metrics/mercury.py | {321 47 0.1433 4.3917} | 324 | '''\nPlaceholder for Mercury metrics integration.\nMercury is a tool for evaluating and comparing LLMs, with a focus on interpretability and detailed analysis. Integrating Mercury metrics into Prompt Harness would allow users to leverage its capabilities for more in-depth evaluation of their prompts and model outputs.\n''' |
31 | src/metrics/significance.py | {2711 314 0.2324 4.6383} | 2856 | \"\"\"Uncertainty-aware comparison helpers.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Iterable, Any\nimport random\nimport statistics\n\ndef _clean(values: Iterable[float | int | None]) -> list[float]:\n    return [float(v) for v in values if v is not None]\n\ndef percentile(values: list[float], pct: float) -> float:\n    if not values:\n        return 0.0\n    ordered = sorted(values)\n    k = (len(ordered) - 1) * pct / 100.0\n    lower = int(k)\n    upper = min(lower + 1, len(ordered) - 1)\n    frac = k - lower\n    return ordered[lower] * (1 - frac) + ordered[upper] * frac\n\ndef bootstrap_mean_ci(\n    values: Iterable[float | int | None],\n    *,\n    confidence: float = 0.95,\n    n_resamples: int = 2000,\n    seed: int = 13,\n) -> dict[str, Any]:\n    \"\"\"Return a non-parametric bootstrap confidence interval for the mean.\"\"\"\n\n    data = _clean(values)\n    if not data:\n        return {\"n\": 0, \"mean\": None, \"ci_low\": None, \"ci_high\": None, \"confidence\": confidence}\n\n    rng = random.Random(seed)\n    means = []\n    for _ in range(max(1, n_resamples)):\n        sample = [data[rng.randrange(len(data))] for _ in data]\n        means.append(statistics.fmean(sample))\n\n    alpha = (1.0 - confidence) / 2.0\n    return {\n        \"n\": len(data),\n        \"mean\": round(statistics.fmean(data), 6),\n        \"ci_low\": round(percentile(means, alpha * 100), 6),\n        \"ci_high\": round(percentile(means, (1 - alpha) * 100), 6),\n        \"confidence\": confidence,\n    }\n\ndef compare_mean_delta(\n    baseline: Iterable[float | int | None],\n    candidate: Iterable[float | int | None],\n    *,\n    confidence: float = 0.95,\n    n_resamples: int = 2000,\n    seed: int = 13,\n) -> dict[str, Any]:\n    \"\"\"Bootstrap the candidate-baseline mean delta.\"\"\"\n\n    base = _clean(baseline)\n    cand = _clean(candidate)\n    if not base or not cand:\n        return {\"delta\": None, \"ci_low\": None, \"ci_high\": None, \"confidence\": confidence}\n\n    rng = random.Random(seed)\n    deltas = []\n    for _ in range(max(1, n_resamples)):\n        base_sample = [base[rng.randrange(len(base))] for _ in base]\n        cand_sample = [cand[rng.randrange(len(cand))] for _ in cand]\n        deltas.append(statistics.fmean(cand_sample) - statistics.fmean(base_sample))\n\n    alpha = (1.0 - confidence) / 2.0\n    return {\n        \"baseline_mean\": round(statistics.fmean(base), 6),\n        \"candidate_mean\": round(statistics.fmean(cand), 6),\n        \"delta\": round(statistics.fmean(cand) - statistics.fmean(base), 6),\n        \"ci_low\": round(percentile(deltas, alpha * 100), 6),\n        \"ci_high\": round(percentile(deltas, (1 - alpha) * 100), 6),\n        \"confidence\": confidence,\n    }\n\n__all__ = [\"bootstrap_mean_ci\", \"compare_mean_delta\", \"percentile\"] |
32 | src/probes/__init__.py | {662 38 0.1616 4.4865} | 712 | \"\"\"Context-window and tokenizer probe generation utilities.\"\"\"\n\nfrom .payloads import (\n    PayloadLine,\n    build_integer_lines,\n    build_integer_payload,\n    build_whitespace_lines,\n    build_whitespace_payload,\n    render_payload_json,\n    render_payload_template,\n)\nfrom .unicode import build_bpe_merge_probe, build_token_boundary_probe, build_unicode_probe\n\n__all__ = [\n    \"PayloadLine\",\n    \"build_bpe_merge_probe\",\n    \"build_integer_lines\",\n    \"build_integer_payload\",\n    \"build_token_boundary_probe\",\n    \"build_unicode_probe\",\n    \"build_whitespace_lines\",\n    \"build_whitespace_payload\",\n    \"render_payload_json\",\n    \"render_payload_template\",\n] |
33 | src/probes/adapters.py | {1511 141 0.2574 4.5588} | 1606 | \"\"\"Adapters from raw probe artifacts to prompt-harness JSONL rows.\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nfrom pathlib import Path\nfrom typing import Any, Iterable\n\ndef build_probe_prompt(serialized_payload: str, probe_type: str) -> str:\n    return \"\\n\".join(\n        [\n            \"You are evaluating a context-window/tokenizer probe payload.\",\n            f\"Probe type: {probe_type}\",\n            \"Describe observable structure, likely tokenizer stress points, and any ambiguity. Do not transform the payload.\",\n            \"\",\n            \"Payload:\",\n            serialized_payload,\n        ]\n    )\n\ndef make_prompt_record(\n    *,\n    record_id: str,\n    serialized_payload: str,\n    suite: str,\n    probe_type: str,\n    payload_path: str,\n    extra_slices: Iterable[str] = (),\n) -> dict[str, Any]:\n    slices = [\"context_probe\", suite, probe_type, *extra_slices]\n    return {\n        \"id\": record_id,\n        \"prompt\": build_probe_prompt(serialized_payload, probe_type),\n        \"slices\": slices,\n        \"probe\": {\n            \"suite\": suite,\n            \"probe_type\": probe_type,\n            \"payload_path\": payload_path,\n        },\n    }\n\ndef write_jsonl(path: str | Path, rows: Iterable[dict[str, Any]]) -> None:\n    output_path = Path(path)\n    output_path.parent.mkdir(parents=True, exist_ok=True)\n    with output_path.open(\"w\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n        for row in rows:\n            handle.write(json.dumps(row, ensure_ascii=False, separators=(\",\", \":\")) + \"\\n\") |
