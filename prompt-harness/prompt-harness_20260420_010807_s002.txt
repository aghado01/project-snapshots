idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
19 | src/render/report.py | {5016 387 0.2969 4.4664} | 5326 | \n\nfrom __future__ import annotations\n\nimport datetime\nimport json\nimport logging\nimport os\n\nfrom jinja2 import TemplateNotFound\n\nfrom . import get_environment, get_template_engine\n\ndef _read_json(path: str) -> dict:\n    if not os.path.exists(path):\n        return {}\n    try:\n        with open(path, encoding=\"utf-8\") as f:\n            return json.load(f)\n    except (FileNotFoundError, json.JSONDecodeError):\n        return {}\n\ndef build_report_with_engine(run_dir: str) -> str:\n    \n    stats: dict = _read_json(os.path.join(run_dir, \"run_stats.json\"))\n    slices: dict = _read_json(os.path.join(run_dir, \"slices.json\"))\n    report_data: dict = {\n        \"report_title\": f\"Run Report: {os.path.basename(run_dir)}\",\n        \"timestamp\": datetime.datetime.now().isoformat(),\n        \"baseline_name\": os.path.basename(run_dir),\n        \"summary\": stats if stats else {},\n        \"slices\": [],\n    }\n    if isinstance(report_data[\"slices\"], list) and slices:\n        for name, row in sorted(slices.items()):\n            report_data[\"slices\"].append(\n                {\n                    \"name\": name,\n                    \"count\": row.get(\"count\", \"\"),\n                    \"avg_latency_ms\": row.get(\"avg_latency_ms\", \"\"),\n                    \"avg_cit_f1\": row.get(\"avg_cit_f1\", \"\"),\n                    \"avg_instruction_format_score\": row.get(\n                        \"avg_instruction_format_score\", \"\"\n                    ),\n                }\n            )\n\n    fallback_template = \n\n    engine = get_template_engine()\n    try:\n        return engine.render_template_safe(\n            \"reports/report.md.j2\",\n            fallback_template=fallback_template,\n            **report_data\n        )\n    except Exception as exc:  # pragma: no cover\n        logging.warning(\"Template rendering failed (%s); using simple fallback\", exc)\n\n        lines: list[str] = [report_data[\"report_title\"]]\n        if stats:\n            lines.append(\"## Global Stats\")\n            for k, v in stats.items():\n                lines.append(f\"- **{k}**: {v}\")\n        if isinstance(report_data[\"slices\"], list) and report_data[\"slices\"]:\n            lines.append(\"\")\n            lines.append(\"## Slices\")\n            lines.append(\"slice | count | avg latency ms | cit f1 | instr fmt score\")\n            lines.append(\"---|---:|---:|---:|---:\")\n            for s in report_data[\"slices\"]:\n                lines.append(\n                    f\"{s['name']} | {s['count']} | {s['avg_latency_ms']} | \"\n                    f\"{s['avg_cit_f1']} | {s['avg_instruction_format_score']}\"\n                )\n        return \"\\n\".join(lines) + \"\\n\"\n\ndef build_report(run_dir: str) -> str:\n    stats: dict = _read_json(os.path.join(run_dir, \"run_stats.json\"))\n    slices: dict = _read_json(os.path.join(run_dir, \"slices.json\"))\n    report_data: dict = {\n        \"report_title\": f\"Run Report: {os.path.basename(run_dir)}\",\n        \"timestamp\": datetime.datetime.now().isoformat(),\n        \"baseline_name\": os.path.basename(run_dir),\n        \"summary\": stats if stats else {},\n        \"slices\": [],\n    }\n    if isinstance(report_data[\"slices\"], list) and slices:\n        for name, row in sorted(slices.items()):\n            report_data[\"slices\"].append(\n                {\n                    \"name\": name,\n                    \"count\": row.get(\"count\", \"\"),\n                    \"avg_latency_ms\": row.get(\"avg_latency_ms\", \"\"),\n                    \"avg_cit_f1\": row.get(\"avg_cit_f1\", \"\"),\n                    \"avg_instruction_format_score\": row.get(\n                        \"avg_instruction_format_score\", \"\"\n                    ),\n                }\n            )\n\n    try:\n        env = get_environment()\n        template = env.get_template(\"reports/report.md.j2\")\n        return template.render(report_data)\n    except TemplateNotFound:\n\n        logging.debug(\"Report template not found; using inline markdown fallback\")\n    except (OSError, RuntimeError) as exc:  # pragma: no cover\n        logging.warning(\"Report template render failed (%s); falling back\", exc)\n\n    lines: list[str] = [report_data[\"report_title\"]]\n    if stats:\n        lines.append(\"## Global Stats\")\n        for k, v in stats.items():\n            lines.append(f\"- **{k}**: {v}\")\n    if isinstance(report_data[\"slices\"], list) and report_data[\"slices\"]:\n        lines.append(\"\")\n        lines.append(\"## Slices\")\n        lines.append(\"slice | count | avg latency ms | cit f1 | instr fmt score\")\n        lines.append(\"---|---:|---:|---:|---:\")\n        for s in report_data[\"slices\"]:\n            lines.append(\n                f\"{s['name']} | {s['count']} | {s['avg_latency_ms']} | \"\n                f\"{s['avg_cit_f1']} | {s['avg_instruction_format_score']}\"\n            )\n    return \"\\n\".join(lines) + \"\\n\"\n\ndef write_report(run_dir: str) -> str:\n    md = build_report(run_dir)\n    out_path = os.path.join(run_dir, \"report.md\")\n    with open(out_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(md)\n    return out_path\n\n__all__ = [\"build_report\", \"build_report_with_engine\", \"write_report\"] |
20 | src/render/slice_delta.py | {1684 182 0.2215 4.8099} | 1767 | \nfrom __future__ import annotations\nimport json, os\nfrom typing import Dict\n\ndef _load(path: str):\n    if not os.path.exists(path):\n        return {}\n    with open(path, 'r', encoding='utf-8') as f:\n        return json.load(f)\n\ndef _fmt_delta(curr: float | None, base: float | None, pct: bool=False, higher_is_better=True) -> str:\n    if curr is None:\n        return \"?\"\n    if base is None:\n        return f\"{curr}\"\n    delta = curr - base\n    arrow = \"→\"\n    if abs(delta) < 1e-6:\n        arrow = \"→\"\n    else:\n        improved = (delta > 0) if higher_is_better else (delta < 0)\n        arrow = \"↑\" if improved else \"↓\"\n    if pct:\n        return f\"{curr:.2%} {arrow}{delta:+.2%}\".replace('+','+')\n    return f\"{curr:.3f} {arrow}{delta:+.3f}\"\n\ndef build_slice_delta_table(baseline_dir: str, candidate_dir: str) -> str:\n    b = _load(os.path.join(baseline_dir, 'slices.json'))\n    c = _load(os.path.join(candidate_dir, 'slices.json'))\n    if not c:\n        return \"(no slice data)\"\n    lines = [\"| Slice | Cit F1 (Δ) | Instr (Δ) | Avg Lat ms (Δ) |\", \"|-------|------------|-----------|----------------|\"]\n    all_slices = sorted(set(b.keys()) | set(c.keys()))\n    for s in all_slices:\n        cb = b.get(s, {})\n        cc = c.get(s, {})\n        cit = _fmt_delta(cc.get('avg_cit_f1'), cb.get('avg_cit_f1'), higher_is_better=True)\n        inst = _fmt_delta(cc.get('avg_instruction_format_score'), cb.get('avg_instruction_format_score'), higher_is_better=True)\n        lat = _fmt_delta(cc.get('avg_latency_ms'), cb.get('avg_latency_ms'), higher_is_better=False)\n        lines.append(f\"| {s} | {cit} | {inst} | {lat} |\")\n    return \"\\n\".join(lines)\n\n__all__ = [\"build_slice_delta_table\"] |
21 | src/render/templates/reports/report.md.j2 | {998 167 0.1754 4.6096} | 1018 | # {{ (report_title if report_title is defined and report_title else\n       (report_data.report_title if report_data is defined and report_data.report_title is defined else\n\t('Run Report: ' ~ (run_name if run_name is defined else '')))) }}\nGenerated: {{ (timestamp if timestamp is defined else (report_data.timestamp if report_data is defined else '')) }}\n\n{% set _summary = (summary if summary is defined else (stats if stats is defined else (report_data.summary if report_data is defined else {}))) %}\n{% if _summary %}## Global Stats\n{% for k,v in _summary.items() -%}\n- **{{ k }}**: {{ v }}\n{% endfor %}\n{% endif %}\n{% set _slices = slices if slices is defined else (report_data.slices if report_data is defined else []) %}\n{% if _slices %}\n## Slices\nslice | count | avg latency ms | cit f1 | instr fmt score\n---|---:|---:|---:|---:\n{% for s in _slices -%}\n{{ s.name }} | {{ s.count }} | {{ s.avg_latency_ms }} | {{ s.avg_cit_f1 }} | {{ s.avg_instruction_format_score }}\n{% endfor %}\n{% endif %} |
22 | src/rubrics/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
23 | src/rubrics/engine.py | {1566 177 0.3129 4.3946} | 1639 | \n\nfrom __future__ import annotations\n\nimport re\nfrom typing import Any\n\ndef evaluate_instruction_format(\n    prompt: str, answer: str, rubric_cfg: dict[str, Any]\n) -> dict[str, Any]:\n    criteria_cfg = rubric_cfg.get(\"criteria\", {})\n\n    if isinstance(criteria_cfg, list):  # unnamed specs; derive a key\n        criteria_items = [\n            (spec.get(\"name\") or f\"c{i}\", spec) for i, spec in enumerate(criteria_cfg)\n        ]\n    else:\n        criteria_items = list(criteria_cfg.items())\n    weights = rubric_cfg.get(\"weights\", {})\n    results: dict[str, int] = {}\n    total = 0.0\n    weight_sum = 0.0\n    for name, spec in criteria_items:\n        passed = 0\n        ctype = spec.get(\"type\")\n        if ctype == \"regex\":\n            pat = spec.get(\"pattern\")\n            if pat and re.search(pat, answer, flags=re.MULTILINE):\n                passed = 1\n        elif ctype == \"keyword_presence\":\n            min_kw = spec.get(\"min_keywords\", 1)\n            prompt_tokens = {\n                w.strip(\",.!?\").lower() for w in prompt.split()[:20] if len(w) > 3\n            }\n            ans_lower = answer.lower()\n            hits = sum(1 for t in prompt_tokens if t and t in ans_lower)\n            if hits >= min_kw:\n                passed = 1\n        else:\n            passed = 0\n        results[name] = passed\n        w = float(weights.get(name, 0.0))\n        weight_sum += w\n        total += w * passed\n    overall = (total / weight_sum) if weight_sum else 0.0\n    return {\"criteria\": results, \"score\": round(overall, 4)}\n\n__all__ = [\"evaluate_instruction_format\"] |
24 | src/runners/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
25 | src/runners/batch.py | {6732 678 0.1595 5.0538} | 7550 | \n\nfrom __future__ import annotations\n\nimport argparse, os, sys, json, csv, time, math, glob\nfrom pathlib import Path\nfrom typing import List, Dict, Any\n\nimport yaml  # type: ignore\n\nfrom ..client.providers import (\n\tload_provider_from_config,\n\tProviderRequest,\n\tsave_trace_line,\n)\nfrom ..metrics.citations import score_citations\nfrom ..slices.label import label as slice_label\nfrom ..render.report import write_report  # type: ignore\nfrom ..rubrics.engine import evaluate_instruction_format\n\ndef _read_yaml(path: str) -> dict:\n\twith open(path, \"r\", encoding=\"utf-8\") as f:\n\t\treturn yaml.safe_load(f) or {}\n\n_RUBRIC_CACHE: dict | None = None\n\ndef _load_rubric_cfg() -> dict:\n\tglobal _RUBRIC_CACHE\n\tif _RUBRIC_CACHE is not None:\n\t\treturn _RUBRIC_CACHE\n\tpath = \"configs/rubrics.yaml\"\n\tif not os.path.exists(path):\n\t\t_RUBRIC_CACHE = {}\n\t\treturn _RUBRIC_CACHE\n\traw = _read_yaml(path)\n\n\tout = {}\n\tfor item in raw.get(\"rubrics\", []) or []:\n\t\tnm = item.get(\"name\")\n\t\tif nm:\n\t\t\tout[nm] = item\n\t_RUBRIC_CACHE = out\n\treturn out\n\ndef _load_prompts(root: str) -> List[Dict[str, Any]]:\n\t\n\tpaths = sorted(glob.glob(os.path.join(root, \"*.jsonl\")))\n\tprompts: List[Dict[str, Any]] = []\n\tfor p in paths:\n\t\twith open(p, \"r\", encoding=\"utf-8\") as f:\n\t\t\tfor line in f:\n\t\t\t\tline = line.strip()\n\t\t\t\tif not line:\n\t\t\t\t\tcontinue\n\t\t\t\tobj = json.loads(line)\n\t\t\t\tif \"id\" not in obj:\n\t\t\t\t\tobj[\"id\"] = f\"{Path(p).stem}:{len(prompts)}\"\n\n\t\t\t\tif not obj.get(\"prompt\") and obj.get(\"text\"):\n\t\t\t\t\tobj[\"prompt\"] = obj[\"text\"]\n\t\t\t\tprompts.append(obj)\n\treturn prompts\n\ndef _ensure_run_dir(out_dir: str) -> str:\n\tos.makedirs(out_dir, exist_ok=True)\n\treturn out_dir\n\ndef _percentile(values: List[float], pct: float) -> float:\n\tif not values:\n\t\treturn 0.0\n\tk = (len(values) - 1) * (pct / 100.0)\n\tf = math.floor(k)\n\tc = math.ceil(k)\n\tif f == c:\n\t\treturn values[int(k)]\n\treturn values[f] * (c - k) + values[c] * (k - f)\n\ndef run_batch(args: argparse.Namespace) -> int:\n\tcfg = _read_yaml(args.config)\n\tprovider = load_provider_from_config(cfg, override_name=args.provider, override_model=args.model)\n\n\tprompts = _load_prompts(args.prompts)\n\tif not prompts:\n\t\tprint(\"No prompts found\", file=sys.stderr)\n\t\treturn 1\n\n\trun_dir = _ensure_run_dir(args.out)\n\ttraces_path = os.path.join(run_dir, \"traces.jsonl\")\n\tcsv_path = os.path.join(run_dir, \"summary.csv\")\n\n\tlatencies: List[float] = []\n\ttotal_in_tokens = 0\n\ttotal_out_tokens = 0\n\ttotal_cit_f1 = 0.0\n\ttotal_rubric_score = 0.0\n\n\tslice_aggs: dict[str, dict[str, float | int]] = {}\n\n\twith open(csv_path, \"w\", newline=\"\", encoding=\"utf-8\") as csvfile:\n\t\twriter = csv.DictWriter(csvfile, fieldnames=[\"id\", \"model\", \"latency_ms\", \"input_tokens\", \"output_tokens\", \"cit_f1\", \"error\"])\n\t\twriter.writeheader()\n\t\tfor idx, ex in enumerate(prompts):\n\t\t\tseed = args.seed + idx\n\t\t\treq = ProviderRequest(prompt=ex.get(\"prompt\", \"\"), model=provider.model, seed=seed)\n\t\t\tresp = provider.safe_generate(req)\n\t\t\tlatencies.append(resp.latency_ms)\n\t\t\ttotal_in_tokens += resp.input_tokens or 0\n\t\t\ttotal_out_tokens += resp.output_tokens or 0\n\n\t\t\texpected_sources = ex.get(\"reference_sources\") if isinstance(ex, dict) else None\n\t\t\tcit = score_citations(resp.text, expected_sources=expected_sources)\n\t\t\ttotal_cit_f1 += (cit.get(\"f1\") or 0.0)\n\t\t\texplicit_slices = ex.get(\"slices\") or ex.get(\"slice_tags\")\n\t\t\tslices = slice_label(ex.get(\"prompt\", \"\"), explicit_slices)\n\n\t\t\trubric_cfg = _load_rubric_cfg().get(\"instruction_format\")\n\t\t\tif rubric_cfg:\n\t\t\t\trubric_eval = evaluate_instruction_format(ex[\"prompt\"], resp.text, rubric_cfg)\n\t\t\t\trubric_score = rubric_eval[\"score\"]\n\t\t\telse:\n\t\t\t\trubric_eval = {\"criteria\": {}, \"score\": 0.0}\n\t\t\t\trubric_score = 0.0\n\t\t\trubric_scores = {\"instruction_format\": rubric_eval}\n\t\t\trecord = {\n\t\t\t\t\"id\": ex[\"id\"],\n\t\t\t\t\"prompt\": ex[\"prompt\"],\n\t\t\t\t\"response\": resp.text,\n\t\t\t\t\"model\": resp.model,\n\t\t\t\t\"latency_ms\": resp.latency_ms,\n\t\t\t\t\"input_tokens\": resp.input_tokens,\n\t\t\t\t\"output_tokens\": resp.output_tokens,\n\t\t\t\t\"metrics\": {\"citations\": cit},\n\t\t\t\t\"slices\": slices,\n\t\t\t\t\"rubric_scores\": rubric_scores,\n\t\t\t\t\"cost_usd\": resp.cost_usd,\n\t\t\t\t\"error\": resp.error,\n\t\t\t\t\"seed\": seed,\n\t\t\t\t\"ts\": time.time(),\n\t\t\t}\n\t\t\tsave_trace_line(traces_path, record)\n\t\t\twriter.writerow({\n\t\t\t\t\"id\": record[\"id\"],\n\t\t\t\t\"model\": record[\"model\"],\n\t\t\t\t\"latency_ms\": record[\"latency_ms\"],\n\t\t\t\t\"input_tokens\": record[\"input_tokens\"],\n\t\t\t\t\"output_tokens\": record[\"output_tokens\"],\n\t\t\t\t\"cit_f1\": cit[\"f1\"],\n\t\t\t\t\"error\": record[\"error\"],\n\t\t\t})\n\n\t\t\tfor s in slices:\n\t\t\t\tagg = slice_aggs.setdefault(s, {\"count\": 0, \"total_latency\": 0.0, \"total_cit_f1\": 0.0, \"total_rubric_score\": 0.0})\n\t\t\t\tagg[\"count\"] += 1\n\t\t\t\tagg[\"total_latency\"] += resp.latency_ms\n\t\t\t\tagg[\"total_cit_f1\"] += cit[\"f1\"] or 0.0\n\t\t\t\tagg[\"total_rubric_score\"] += rubric_score\n\t\t\ttotal_rubric_score += rubric_score\n\n\tlat_sorted = sorted(latencies)\n\tstats = {\n\t\t\"count\": len(latencies),\n\t\t\"latency_p50_ms\": _percentile(lat_sorted, 50),\n\t\t\"latency_p95_ms\": _percentile(lat_sorted, 95),\n\t\t\"avg_input_tokens\": (total_in_tokens / len(prompts)) if prompts else 0,\n\t\t\"avg_output_tokens\": (total_out_tokens / len(prompts)) if prompts else 0,\n\t\t\"model\": provider.model,\n\t\t\"provider\": getattr(provider, \"name\", \"unknown\"),\n\t\t\"citations_f1_mean\": round((total_cit_f1 / len(prompts)) if prompts else 0.0, 4),\n\t\t\"instruction_format_mean\": round((total_rubric_score / len(prompts)) if prompts else 0.0, 4),\n\t}\n\twith open(os.path.join(run_dir, \"run_stats.json\"), \"w\", encoding=\"utf-8\") as f:\n\t\tjson.dump(stats, f, indent=2)\n\n\tslice_out = {}\n\tfor s, a in slice_aggs.items():\n\t\tc = a[\"count\"] or 1\n\t\tslice_out[s] = {\n\t\t\t\"count\": a[\"count\"],\n\t\t\t\"avg_latency_ms\": round(a[\"total_latency\"] / c, 2),\n\t\t\t\"avg_cit_f1\": round(a[\"total_cit_f1\"] / c, 4),\n\t\t\t\"avg_instruction_format_score\": round(a[\"total_rubric_score\"] / c, 4),\n\t\t}\n\twith open(os.path.join(run_dir, \"slices.json\"), \"w\", encoding=\"utf-8\") as f:\n\t\tjson.dump(slice_out, f, indent=2)\n\n\ttry:\n\t\twrite_report(run_dir)\n\texcept Exception as e:  # pragma: no cover - non critical\n\t\tprint(f\"[warn] report generation failed: {e}\", file=sys.stderr)\n\tprint(f\"Run complete -> {run_dir}\")\n\treturn 0\n\ndef build_arg_parser() -> argparse.ArgumentParser:\n\tp = argparse.ArgumentParser(description=\"Batch prompt runner (Phase 1)\")\n\tp.add_argument(\"--config\", default=\"configs/providers.yaml\")\n\tp.add_argument(\"--prompts\", default=\"data/prompts\")\n\tp.add_argument(\"--out\", required=True, help=\"Output directory (run folder)\")\n\tp.add_argument(\"--provider\", help=\"Override provider name\")\n\tp.add_argument(\"--model\", help=\"Override model id\")\n\tp.add_argument(\"--seed\", type=int, default=13)\n\treturn p\n\ndef main(argv: List[str] | None = None) -> int:\n\targs = build_arg_parser().parse_args(argv)\n\treturn run_batch(args)\n\nif __name__ == \"__main__\":  # pragma: no cover\n\traise SystemExit(main()) |
26 | src/runners/compare.py | {4378 401 0.2677 4.4738} | 4569 | \nfrom __future__ import annotations\nimport argparse, json, os, yaml\n\ndef _read_json(path: str):\n    with open(path, \"r\", encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef _read_yaml(path: str):\n    with open(path, \"r\", encoding=\"utf-8\") as f:\n        return yaml.safe_load(f) or {}\n\ndef evaluate_gates(gates_cfg: dict, baseline_dir: str, candidate_dir: str) -> dict:\n    gates = gates_cfg.get(\"gates\", []) or []\n    candidate_stats = _read_json(os.path.join(candidate_dir, \"run_stats.json\"))\n    baseline_stats = {}\n    if baseline_dir and os.path.exists(os.path.join(baseline_dir, 'run_stats.json')):\n        baseline_stats = _read_json(os.path.join(baseline_dir, 'run_stats.json'))\n    candidate_slices = {}\n    slices_path = os.path.join(candidate_dir, \"slices.json\")\n    if os.path.exists(slices_path):\n        candidate_slices = _read_json(slices_path)\n\n    global_metrics = {\"latency_p95_ms\": candidate_stats.get(\"latency_p95_ms\")}\n    if candidate_slices:\n        cit_vals = [s.get(\"avg_cit_f1\") for s in candidate_slices.values() if s.get(\"avg_cit_f1\") is not None]\n        rubric_vals = [s.get(\"avg_instruction_format_score\") for s in candidate_slices.values() if s.get(\"avg_instruction_format_score\") is not None]\n        if cit_vals:\n            global_metrics[\"citations_f1_mean\"] = sum(cit_vals) / len(cit_vals)\n        if rubric_vals:\n            global_metrics[\"instruction_format_mean\"] = sum(rubric_vals) / len(rubric_vals)\n\n    flattened_slice = {}\n    for sname, data in candidate_slices.items():\n        for k, v in data.items():\n            flattened_slice[f\"slice.{sname}.{k}\"] = v\n\n    results = []\n    overall_pass = True\n    for gate in gates:\n        metric_path = gate.get(\"metric\") or gate.get(\"name\")\n        min_v = gate.get(\"min\")\n        max_v = gate.get(\"max\")\n        max_drop = gate.get(\"max_drop\")  # relative drop allowed (for higher-is-better metrics)\n        max_increase = gate.get(\"max_increase\")  # relative increase allowed (for lower-is-better metrics e.g. latency)\n        value = None\n        if metric_path.startswith(\"global.\"):\n            value = global_metrics.get(metric_path.split('.',1)[1])\n        elif metric_path.startswith(\"slice.\"):\n            value = flattened_slice.get(metric_path)\n        else:\n            value = global_metrics.get(metric_path)\n        passed = True\n        if min_v is not None and (value is None or value < min_v):\n            passed = False\n        if max_v is not None and (value is None or value > max_v):\n            passed = False\n\n        if (max_drop is not None or max_increase is not None) and baseline_stats:\n\n            base_val = None\n            if metric_path.startswith('global.'):\n                base_key = metric_path.split('.',1)[1]\n                base_val = baseline_stats.get(base_key)\n            if base_val is not None and value is not None:\n                if max_drop is not None and base_val != 0:\n                    rel = (base_val - value) / base_val  # positive if drop\n                    if rel > max_drop:\n                        passed = False\n                if max_increase is not None and base_val != 0:\n                    rel_inc = (value - base_val) / base_val  # positive if increase\n                    if rel_inc > max_increase:\n                        passed = False\n        if not passed:\n            overall_pass = False\n        results.append({\n            \"gate\": gate.get(\"name\"),\n            \"metric\": metric_path,\n            \"value\": value,\n            \"min\": min_v,\n            \"max\": max_v,\n            \"max_drop\": max_drop,\n            \"max_increase\": max_increase,\n            \"pass\": passed,\n        })\n\n    report = {\"overall_pass\": overall_pass, \"gates\": results}\n    with open(os.path.join(candidate_dir, \"gates_report.json\"), \"w\", encoding=\"utf-8\") as f:\n        json.dump(report, f, indent=2)\n    return report\n\ndef main(argv=None):\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"--baseline\", required=True)\n    ap.add_argument(\"--candidate\", required=True)\n    ap.add_argument(\"--gates\", default=\"configs/gates.yaml\")\n    args = ap.parse_args(argv)\n    gates_cfg = _read_yaml(args.gates)\n    rep = evaluate_gates(gates_cfg, args.baseline, args.candidate)\n    print(json.dumps(rep, indent=2))\n    return 0 if rep[\"overall_pass\"] else 2\n\nif __name__ == \"__main__\":  # pragma: no cover\n    raise SystemExit(main()) |
27 | src/slices/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
28 | src/slices/label.py | {1001 101 0.1518 5.1766} | 1116 | \n\nfrom __future__ import annotations\n\nimport re\nfrom typing import List, Iterable, Optional\n\n_RE_UNDERSPEC = re.compile(r\"\\b(help|make|write|improve|design|plan|calculate|create)\\b\", re.I)\n_RE_CIT = re.compile(r\"\\b(cite|citation|sources?)\\b\", re.I)\n_RE_CONSTRAINT = re.compile(r\"\\b(exactly|no more than|under \\d+ words|at least \\d+)\\b\", re.I)\n\ndef label(prompt: str, explicit: Optional[Iterable[str]] = None) -> List[str]:\n\ttags: List[str] = []\n\tif explicit:\n\t\tfor t in explicit:\n\t\t\tif t and t not in tags:\n\t\t\t\ttags.append(str(t))\n\tif not prompt:\n\t\treturn tags\n\ttxt = prompt.strip()\n\tL = len(txt)\n\tif L < 80:\n\t\ttags.append(\"length_short\")\n\telif L < 180:\n\t\ttags.append(\"length_medium\")\n\telse:\n\t\ttags.append(\"length_long\")\n\n\tif _RE_UNDERSPEC.search(txt):\n\t\tif L < 45:\n\t\t\ttags.append(\"extremely_vague\")\n\t\telse:\n\t\t\ttags.append(\"underspecified\")\n\n\tif _RE_CIT.search(txt):\n\t\ttags.append(\"needs_citations\")\n\n\tif _RE_CONSTRAINT.search(txt):\n\t\ttags.append(\"constraint_heavy\")\n\n\treturn tags\n\n__all__ = [\"label\"] |
29 | templates/table_specs/slice_summary.py | {209 19 0.1244 4.8610} | 221 | \nfrom __future__ import annotations\n\nEXAMPLE_TABLE = .strip()\n\ndef build_slice_gate_table(run_data=None):  # run_data placeholder\n    return EXAMPLE_TABLE\n\n__all__ = [\"build_slice_gate_table\", \"EXAMPLE_TABLE\"] |
