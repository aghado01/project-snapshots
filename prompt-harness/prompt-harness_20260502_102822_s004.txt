idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
38 | src/render/engine.py | {10833 946 0.3133 4.2992} | 11247 | \"\"\"General-purpose Jinja2 rendering engine.\n\nThis module provides a flexible Jinja2 rendering engine that doesn't assume\nspecific project structure or template locations. It can be configured with\ncustom template directories, loaders, and rendering options.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom pathlib import Path\nfrom typing import Any, Dict, List, Optional, Union, cast\n\nfrom jinja2 import (\n    BaseLoader,\n    ChoiceLoader,\n    DictLoader,\n    Environment,\n    FileSystemBytecodeCache,\n    FileSystemLoader,\n    Template,\n    TemplateNotFound,\n    select_autoescape,\n)\n\nclass TemplateEngine:\n    \"\"\"A configurable Jinja2 template rendering engine.\n\n    This engine can work with various template sources (filesystem, dictionary,\n    custom loaders) and provides flexible configuration options.\n    \"\"\"\n\n    def __init__(\n        self,\n        template_dirs: Optional[Union[str, Path, List[Union[str, Path]]]] = None,\n        template_dict: Optional[Dict[str, str]] = None,\n        loader: Optional[BaseLoader] = None,\n        cache_dir: Optional[Union[str, Path]] = None,\n        autoescape_extensions: Optional[List[str]] = None,\n        no_autoescape_extensions: Optional[List[str]] = None,\n        autoescape_default: bool = False,\n        **env_kwargs: Any,\n    ):\n        \"\"\"Initialize the template engine.\n\n        Args:\n            template_dirs: Directory or list of directories containing templates.\n                          If None, no filesystem templates will be available.\n            template_dict: Dictionary mapping template names to template strings.\n                          Useful for inline templates or testing.\n            loader: Custom Jinja2 loader. If provided, takes precedence over\n                   template_dirs and template_dict.\n            cache_dir: Directory for bytecode cache. If None, no caching is used.\n            autoescape_extensions: List of file extensions that should have\n                                 autoescaping enabled (e.g., ['html', 'xml']).\n                                 Defaults to ['html', 'xml'].\n            no_autoescape_extensions: List of file extensions that should have\n                                    autoescaping disabled (e.g., ['md', 'txt']).\n                                    Defaults to ['md', 'txt'].\n            autoescape_default: Default autoescaping behavior for templates\n                              without recognized extensions.\n            **env_kwargs: Additional keyword arguments passed to Jinja2 Environment.\n        \"\"\"\n        self._env = self._create_environment(\n            template_dirs=template_dirs,\n            template_dict=template_dict,\n            loader=loader,\n            cache_dir=cache_dir,\n            autoescape_extensions=autoescape_extensions or [\"html\", \"xml\"],\n            no_autoescape_extensions=no_autoescape_extensions or [\"md\", \"txt\"],\n            autoescape_default=autoescape_default,\n            **env_kwargs,\n        )\n\n    def _create_environment(\n        self,\n        template_dirs: Optional[Union[str, Path, List[Union[str, Path]]]],\n        template_dict: Optional[Dict[str, str]],\n        loader: Optional[BaseLoader],\n        cache_dir: Optional[Union[str, Path]],\n        autoescape_extensions: List[str],\n        no_autoescape_extensions: List[str],\n        autoescape_default: bool,\n        **env_kwargs: Any,\n    ) -> Environment:\n        \"\"\"Create and configure the Jinja2 environment.\"\"\"\n\n        # Set up loader\n        if loader is not None:\n            env_loader = loader\n        elif template_dirs is not None or template_dict is not None:\n            loaders = []\n\n            # Add filesystem loader if template directories provided\n            if template_dirs is not None:\n                if isinstance(template_dirs, (str, Path)):\n                    template_dirs = [template_dirs]\n                str_dirs = [str(d) for d in template_dirs]\n                loaders.append(FileSystemLoader(str_dirs))\n\n            # Add dictionary loader if template dict provided\n            if template_dict is not None:\n                loaders.append(DictLoader(template_dict))\n\n            # Use single loader or choice loader\n            if len(loaders) == 1:\n                env_loader = loaders[0]\n            else:\n                env_loader = ChoiceLoader(loaders)\n        else:\n            # No templates configured - create empty dict loader\n            env_loader = DictLoader({})\n\n        # Set up bytecode cache\n        bytecode_cache = None\n        if cache_dir is not None:\n            cache_path = Path(cache_dir)\n            cache_path.mkdir(parents=True, exist_ok=True)\n            bytecode_cache = FileSystemBytecodeCache(directory=str(cache_path))\n\n        # Set up autoescaping\n        selected_autoescape = select_autoescape(\n            enabled_extensions=autoescape_extensions,\n            disabled_extensions=no_autoescape_extensions,\n            default_for_string=autoescape_default,\n        )\n\n        enabled_names = {ext.lower().lstrip(\".\") for ext in autoescape_extensions}\n        disabled_names = {ext.lower().lstrip(\".\") for ext in no_autoescape_extensions}\n\n        def autoescape(template_name: Optional[str]) -> bool:\n            if template_name:\n                suffix = template_name.rsplit(\".\", 1)[-1].lower()\n                if suffix in enabled_names:\n                    return True\n                if suffix in disabled_names:\n                    return False\n            return bool(selected_autoescape(template_name))\n\n        # Create environment\n        env = Environment(\n            loader=env_loader,\n            autoescape=autoescape,\n            bytecode_cache=bytecode_cache,\n            **env_kwargs,\n        )\n\n        return env\n\n    @property\n    def environment(self) -> Environment:\n        \"\"\"Get the underlying Jinja2 environment.\"\"\"\n        return self._env\n\n    def render_template(self, template_name: str, **context: Any) -> str:\n        \"\"\"Render a template by name with the given context.\n\n        Args:\n            template_name: Name of the template to render.\n            **context: Template context variables.\n\n        Returns:\n            Rendered template as string.\n\n        Raises:\n            TemplateNotFound: If the template doesn't exist.\n        \"\"\"\n        template = self._env.get_template(template_name)\n        return template.render(**context)\n\n    def render_string(self, template_string: str, **context: Any) -> str:\n        \"\"\"Render a template string with the given context.\n\n        Args:\n            template_string: Template content as string.\n            **context: Template context variables.\n\n        Returns:\n            Rendered template as string.\n        \"\"\"\n        template = self._env.from_string(template_string)\n        return template.render(**context)\n\n    def render_template_safe(\n        self,\n        template_name: str,\n        fallback_template: Optional[str] = None,\n        **context: Any,\n    ) -> str:\n        \"\"\"Render a template with fallback handling.\n\n        Args:\n            template_name: Name of the template to render.\n            fallback_template: Optional fallback template string if the\n                             named template is not found.\n            **context: Template context variables.\n\n        Returns:\n            Rendered template as string, or fallback if template not found.\n\n        Raises:\n            TemplateNotFound: If both the named template and fallback are unavailable.\n        \"\"\"\n        try:\n            return self.render_template(template_name, **context)\n        except TemplateNotFound:\n            if fallback_template is not None:\n                return self.render_string(fallback_template, **context)\n            raise\n\n    def add_template(self, name: str, content: str) -> None:\n        \"\"\"Add a template to the engine's dictionary loader.\n\n        Note: This only works if the engine was configured with a DictLoader\n        as part of its loader chain.\n\n        Args:\n            name: Template name.\n            content: Template content.\n        \"\"\"\n        # This is a limitation - we can't easily add templates to an existing\n        # FileSystemLoader. For runtime template addition, users should use\n        # DictLoader or implement a custom loader.\n        loader = self._env.loader\n\n        # Direct DictLoader\n        if isinstance(loader, DictLoader):\n            mapping = cast(Dict[str, str], loader.mapping)\n            mapping[name] = content\n            return\n\n        if isinstance(loader, ChoiceLoader):\n            for sub in loader.loaders:\n                if isinstance(sub, DictLoader):\n                    mapping = cast(Dict[str, str], sub.mapping)\n                    mapping[name] = content\n                    return\n\n        raise NotImplementedError(\n            \"Adding templates at runtime is only supported with DictLoader. \"\n            \"Initialize the engine with template_dict or include a DictLoader in the loader chain.\"\n        )\n\n    def template_exists(self, template_name: str) -> bool:\n        \"\"\"Check if a template exists.\n\n        Args:\n            template_name: Name of the template to check.\n\n        Returns:\n            True if template exists, False otherwise.\n        \"\"\"\n        try:\n            self._env.get_template(template_name)\n            return True\n        except TemplateNotFound:\n            return False\n\n    def list_templates(self) -> List[str]:\n        \"\"\"List all available template names.\n\n        Returns:\n            List of template names.\n        \"\"\"\n        return self._env.list_templates()\n\ndef create_simple_engine(\n    template_dir: Optional[Union[str, Path]] = None,\n    cache_dir: Optional[Union[str, Path]] = None,\n) -> TemplateEngine:\n    \"\"\"Create a simple template engine with common defaults.\n\n    Args:\n        template_dir: Directory containing templates. If None, only string\n                     templates can be rendered.\n        cache_dir: Directory for bytecode cache. If None, no caching is used.\n\n    Returns:\n        Configured TemplateEngine instance.\n    \"\"\"\n    return TemplateEngine(\n        template_dirs=template_dir,\n        cache_dir=cache_dir,\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n\ndef create_string_engine(templates: Optional[Dict[str, str]] = None) -> TemplateEngine:\n    \"\"\"Create a template engine that works only with string templates.\n\n    Args:\n        templates: Optional dictionary of template name -> template content.\n\n    Returns:\n        Configured TemplateEngine instance.\n    \"\"\"\n    return TemplateEngine(\n        template_dict=templates or {},\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    ) |
39 | src/render/examples.py | {8101 714 0.2837 4.4622} | 8661 | \"\"\"Examples demonstrating the general-purpose Jinja2 TemplateEngine.\n\nThis module shows various ways to use the TemplateEngine for different\nscenarios without assuming project structure.\n\"\"\"\n\nfrom pathlib import Path\nimport tempfile\nfrom typing import Dict, Any\n\nfrom .engine import TemplateEngine, create_simple_engine, create_string_engine\n\ndef example_string_templates():\n    \"\"\"Example: Using templates defined as strings.\"\"\"\n\n    # Define templates as strings\n    templates = {\n        \"greeting\": \"Hello, {{ name }}! Welcome to {{ app_name }}.\",\n        \"report\": \"\"\"\n# {{ title }}\nGenerated: {{ timestamp }}\n\n## Summary\n{% for key, value in data.items() -%}\n- **{{ key }}**: {{ value }}\n{% endfor %}\n\"\"\".strip(),\n        \"email\": \"\"\"\nDear {{ recipient }},\n\n{{ message }}\n\nBest regards,\n{{ sender }}\n\"\"\".strip()\n    }\n\n    # Create engine with string templates\n    engine = create_string_engine(templates)\n\n    # Render various templates\n    greeting = engine.render_template(\"greeting\",\n                                    name=\"Alice\",\n                                    app_name=\"Quality Lab\")\n\n    report = engine.render_template(\"report\",\n                                  title=\"Daily Report\",\n                                  timestamp=\"2025-09-07\",\n                                  data={\"tests_run\": 42, \"success_rate\": \"95%\"})\n\n    email = engine.render_template(\"email\",\n                                 recipient=\"Bob\",\n                                 message=\"The report is ready for review.\",\n                                 sender=\"System\")\n\n    return {\n        \"greeting\": greeting,\n        \"report\": report,\n        \"email\": email\n    }\n\ndef example_filesystem_templates():\n    \"\"\"Example: Using templates from filesystem.\"\"\"\n\n    # Create a temporary directory with templates\n    with tempfile.TemporaryDirectory() as temp_dir:\n        temp_path = Path(temp_dir)\n\n        # Create template files\n        (temp_path / \"hello.txt\").write_text(\"Hello, {{ name }}!\")\n        (temp_path / \"config.yaml\").write_text(\"\"\"\n# Configuration for {{ app_name }}\nversion: {{ version }}\ndebug: {{ debug }}\nfeatures:\n{% for feature in features -%}\n  - {{ feature }}\n{% endfor %}\n\"\"\".strip())\n\n        # Create engine with filesystem templates\n        engine = create_simple_engine(template_dir=temp_dir)\n\n        # Render templates\n        hello = engine.render_template(\"hello.txt\", name=\"World\")\n\n        config = engine.render_template(\"config.yaml\",\n                                      app_name=\"MyApp\",\n                                      version=\"1.0.0\",\n                                      debug=True,\n                                      features=[\"auth\", \"logging\", \"metrics\"])\n\n        return {\n            \"hello\": hello,\n            \"config\": config\n        }\n\ndef example_mixed_templates():\n    \"\"\"Example: Using both filesystem and string templates.\"\"\"\n\n    with tempfile.TemporaryDirectory() as temp_dir:\n        temp_path = Path(temp_dir)\n\n        # Create a file template\n        (temp_path / \"base.html\").write_text(\"\"\"\n<!DOCTYPE html>\n<html>\n<head><title>{{ title }}</title></head>\n<body>{{ content }}</body>\n</html>\n\"\"\".strip())\n\n        # Define string templates\n        string_templates = {\n            \"content\": \"<h1>{{ heading }}</h1><p>{{ text }}</p>\"\n        }\n\n        # Create engine with both sources\n        engine = TemplateEngine(\n            template_dirs=temp_dir,\n            template_dict=string_templates\n        )\n\n        # Render content template first\n        content = engine.render_template(\"content\",\n                                       heading=\"Welcome\",\n                                       text=\"This is a mixed template example.\")\n\n        # Then render the full page\n        page = engine.render_template(\"base.html\",\n                                    title=\"Example Page\",\n                                    content=content)\n\n        return page\n\ndef example_safe_rendering():\n    \"\"\"Example: Safe rendering with fallbacks.\"\"\"\n\n    # Create engine with some templates\n    templates = {\n        \"exists\": \"This template exists: {{ message }}\"\n    }\n    engine = create_string_engine(templates)\n\n    # This will work\n    result1 = engine.render_template(\"exists\", message=\"Hello!\")\n\n    # This will use the fallback\n    fallback = \"Fallback template: {{ message }}\"\n    result2 = engine.render_template_safe(\"missing\",\n                                        fallback_template=fallback,\n                                        message=\"Using fallback\")\n\n    # Check if templates exist\n    exists_check = engine.template_exists(\"exists\")  # True\n    missing_check = engine.template_exists(\"missing\")  # False\n\n    return {\n        \"result1\": result1,\n        \"result2\": result2,\n        \"exists_check\": exists_check,\n        \"missing_check\": missing_check\n    }\n\ndef example_custom_configuration():\n    \"\"\"Example: Custom engine configuration.\"\"\"\n\n    templates = {\n        \"html\": \"<p>{{ content }}</p>\",  # Will be autoescaped\n        \"text\": \"{{ content }}\",         # Won't be autoescaped\n    }\n\n    # Create engine with custom autoescaping\n    engine = TemplateEngine(\n        template_dict=templates,\n        autoescape_extensions=[\"html\"],\n        no_autoescape_extensions=[\"txt\", \"text\"],\n        autoescape_default=True,  # Default to escaping\n    )\n\n    # Test autoescaping\n    unsafe_content = \"<script>alert('xss')</script>\"\n\n    html_result = engine.render_template(\"html\", content=unsafe_content)\n    text_result = engine.render_template(\"text\", content=unsafe_content)\n\n    return {\n        \"html_escaped\": html_result,  # Should be escaped\n        \"text_raw\": text_result,      # Should be raw\n    }\n\ndef example_runtime_templates():\n    \"\"\"Example: Adding templates at runtime.\"\"\"\n\n    # Start with empty engine\n    engine = create_string_engine()\n\n    # Add templates dynamically\n    engine.add_template(\"dynamic1\", \"Hello, {{ name }}!\")\n    engine.add_template(\"dynamic2\", \"Count: {{ count }}\")\n\n    # Render the dynamic templates\n    result1 = engine.render_template(\"dynamic1\", name=\"Dynamic\")\n    result2 = engine.render_template(\"dynamic2\", count=42)\n\n    # List all templates\n    templates_list = engine.list_templates()\n\n    return {\n        \"result1\": result1,\n        \"result2\": result2,\n        \"all_templates\": templates_list\n    }\n\ndef example_render_string():\n    \"\"\"Example: Rendering strings directly without named templates.\"\"\"\n\n    engine = create_string_engine()\n\n    # Render templates directly from strings\n    template1 = \"Hello, {{ name }}!\"\n    result1 = engine.render_string(template1, name=\"Direct\")\n\n    template2 = \"\"\"\nItems:\n{% for item in items -%}\n- {{ item }}\n{% endfor %}\n\"\"\".strip()\n    result2 = engine.render_string(template2, items=[\"apple\", \"banana\", \"cherry\"])\n\n    return {\n        \"greeting\": result1,\n        \"list\": result2\n    }\n\nif __name__ == \"__main__\":\n    # Run all examples\n    print(\"=== String Templates ===\")\n    string_results = example_string_templates()\n    for name, content in string_results.items():\n        print(f\"\\n{name}:\\n{content}\")\n\n    print(\"\\n=== Filesystem Templates ===\")\n    fs_results = example_filesystem_templates()\n    for name, content in fs_results.items():\n        print(f\"\\n{name}:\\n{content}\")\n\n    print(\"\\n=== Mixed Templates ===\")\n    mixed_result = example_mixed_templates()\n    print(f\"\\n{mixed_result}\")\n\n    print(\"\\n=== Safe Rendering ===\")\n    safe_results = example_safe_rendering()\n    for name, content in safe_results.items():\n        print(f\"\\n{name}: {content}\")\n\n    print(\"\\n=== Custom Configuration ===\")\n    custom_results = example_custom_configuration()\n    for name, content in custom_results.items():\n        print(f\"\\n{name}: {content}\")\n\n    print(\"\\n=== Runtime Templates ===\")\n    runtime_results = example_runtime_templates()\n    for name, content in runtime_results.items():\n        print(f\"\\n{name}: {content}\")\n\n    print(\"\\n=== Render String ===\")\n    string_render_results = example_render_string()\n    for name, content in string_render_results.items():\n        print(f\"\\n{name}:\\n{content}\") |
40 | src/render/report.py | {5956 588 0.2465 4.6494} | 6300 | \"\"\"Markdown report generation (Phase 2 minimal).\"\"\"\n\nfrom __future__ import annotations\n\nimport datetime\nimport json\nimport logging\nimport os\n\nfrom jinja2 import TemplateNotFound\n\nfrom . import get_environment, get_template_engine\n\ndef _read_json(path: str) -> dict:\n    if not os.path.exists(path):\n        return {}\n    try:\n        with open(path, encoding=\"utf-8\") as f:\n            return json.load(f)\n    except (FileNotFoundError, json.JSONDecodeError):\n        return {}\n\ndef build_report_with_engine(run_dir: str) -> str:\n    \"\"\"Alternative implementation using the new TemplateEngine.\n\n    This demonstrates how to use the more flexible template engine\n    with fallback template handling.\n    \"\"\"\n    stats: dict = _read_json(os.path.join(run_dir, \"run_stats.json\"))\n    slices: dict = _read_json(os.path.join(run_dir, \"slices.json\"))\n    report_data: dict = {\n        \"report_title\": f\"Run Report: {os.path.basename(run_dir)}\",\n        \"timestamp\": datetime.datetime.now().isoformat(),\n        \"baseline_name\": os.path.basename(run_dir),\n        \"summary\": stats if stats else {},\n        \"slices\": [],\n        \"slice_columns\": [],\n    }\n    slice_columns: set[str] = set()\n    if isinstance(report_data[\"slices\"], list) and slices:\n        for name, row in sorted(slices.items()):\n            rendered_row = {\"name\": name, \"count\": row.get(\"count\", \"\")}\n            for key, value in row.items():\n                if key.startswith(\"avg_\"):\n                    slice_columns.add(key)\n                    rendered_row[key] = value\n            report_data[\"slices\"].append(rendered_row)\n    report_data[\"slice_columns\"] = sorted(slice_columns)\n\n    # Define a fallback template as a string\n    fallback_template = \"\"\"# {{ report_title }}\nGenerated: {{ timestamp }}\n\n{% if summary %}## Global Stats\n{% for k,v in summary.items() -%}\n- **{{ k }}**: {{ v }}\n{% endfor %}\n{% endif %}\n{% if slices %}\n## Slices\nslice | count{% for c in slice_columns %} | {{ c|replace('_', ' ') }}{% endfor %}\n---|---:{% for c in slice_columns %}|---:{% endfor %}\n{% for s in slices -%}\n{{ s.name }} | {{ s.count }}{% for c in slice_columns %} | {{ s.get(c, '') }}{% endfor %}\n{% endfor %}\n{% endif %}\n\"\"\"\n\n    # Use the new template engine with safe fallback\n    engine = get_template_engine()\n    try:\n        return engine.render_template_safe(\n            \"reports/report.md.j2\",\n            fallback_template=fallback_template,\n            **report_data\n        )\n    except Exception as exc:  # pragma: no cover\n        logging.warning(\"Template rendering failed (%s); using simple fallback\", exc)\n        # Ultimate fallback to old markdown generation\n        lines: list[str] = [report_data[\"report_title\"]]\n        if stats:\n            lines.append(\"## Global Stats\")\n            for k, v in stats.items():\n                lines.append(f\"- **{k}**: {v}\")\n        if isinstance(report_data[\"slices\"], list) and report_data[\"slices\"]:\n            lines.append(\"\")\n            lines.append(\"## Slices\")\n            columns = report_data.get(\"slice_columns\", [])\n            lines.append(\"slice | count\" + \"\".join(f\" | {c}\" for c in columns))\n            lines.append(\"---|---:\" + \"\".join(\"|---:\" for _ in columns))\n            for s in report_data[\"slices\"]:\n                lines.append(f\"{s['name']} | {s['count']}\" + \"\".join(f\" | {s.get(c, '')}\" for c in columns))\n        return \"\\n\".join(lines) + \"\\n\"\n\ndef build_report(run_dir: str) -> str:\n    stats: dict = _read_json(os.path.join(run_dir, \"run_stats.json\"))\n    slices: dict = _read_json(os.path.join(run_dir, \"slices.json\"))\n    report_data: dict = {\n        \"report_title\": f\"Run Report: {os.path.basename(run_dir)}\",\n        \"timestamp\": datetime.datetime.now().isoformat(),\n        \"baseline_name\": os.path.basename(run_dir),\n        \"summary\": stats if stats else {},\n        \"slices\": [],\n        \"slice_columns\": [],\n    }\n    slice_columns: set[str] = set()\n    if isinstance(report_data[\"slices\"], list) and slices:\n        for name, row in sorted(slices.items()):\n            rendered_row = {\"name\": name, \"count\": row.get(\"count\", \"\")}\n            for key, value in row.items():\n                if key.startswith(\"avg_\"):\n                    slice_columns.add(key)\n                    rendered_row[key] = value\n            report_data[\"slices\"].append(rendered_row)\n    report_data[\"slice_columns\"] = sorted(slice_columns)\n\n    # Jinja2 template rendering (prefer template, fallback to inlined markdown)\n    try:\n        env = get_environment()\n        template = env.get_template(\"reports/report.md.j2\")\n        return template.render(report_data)\n    except TemplateNotFound:\n        # fall back to inline template if missing\n        logging.debug(\"Report template not found; using inline markdown fallback\")\n    except (OSError, RuntimeError) as exc:  # pragma: no cover\n        logging.warning(\"Report template render failed (%s); falling back\", exc)\n\n    # fallback to old markdown if template missing\n    lines: list[str] = [report_data[\"report_title\"]]\n    if stats:\n        lines.append(\"## Global Stats\")\n        for k, v in stats.items():\n            lines.append(f\"- **{k}**: {v}\")\n    if isinstance(report_data[\"slices\"], list) and report_data[\"slices\"]:\n        lines.append(\"\")\n        lines.append(\"## Slices\")\n        columns = report_data.get(\"slice_columns\", [])\n        lines.append(\"slice | count\" + \"\".join(f\" | {c}\" for c in columns))\n        lines.append(\"---|---:\" + \"\".join(\"|---:\" for _ in columns))\n        for s in report_data[\"slices\"]:\n            lines.append(f\"{s['name']} | {s['count']}\" + \"\".join(f\" | {s.get(c, '')}\" for c in columns))\n    return \"\\n\".join(lines) + \"\\n\"\n\ndef write_report(run_dir: str) -> str:\n    md = build_report(run_dir)\n    out_path = os.path.join(run_dir, \"report.md\")\n    with open(out_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(md)\n    return out_path\n\n__all__ = [\"build_report\", \"build_report_with_engine\", \"write_report\"] |
41 | src/render/slice_delta.py | {1753 190 0.2173 4.8102} | 1842 | \"\"\"Slice delta table generation between baseline & candidate runs.\"\"\"\nfrom __future__ import annotations\nimport json, os\nfrom typing import Dict\n\ndef _load(path: str):\n    if not os.path.exists(path):\n        return {}\n    with open(path, 'r', encoding='utf-8') as f:\n        return json.load(f)\n\ndef _fmt_delta(curr: float | None, base: float | None, pct: bool=False, higher_is_better=True) -> str:\n    if curr is None:\n        return \"?\"\n    if base is None:\n        return f\"{curr}\"\n    delta = curr - base\n    arrow = \"→\"\n    if abs(delta) < 1e-6:\n        arrow = \"→\"\n    else:\n        improved = (delta > 0) if higher_is_better else (delta < 0)\n        arrow = \"↑\" if improved else \"↓\"\n    if pct:\n        return f\"{curr:.2%} {arrow}{delta:+.2%}\".replace('+','+')\n    return f\"{curr:.3f} {arrow}{delta:+.3f}\"\n\ndef build_slice_delta_table(baseline_dir: str, candidate_dir: str) -> str:\n    b = _load(os.path.join(baseline_dir, 'slices.json'))\n    c = _load(os.path.join(candidate_dir, 'slices.json'))\n    if not c:\n        return \"(no slice data)\"\n    lines = [\"| Slice | Cit F1 (Δ) | Instr (Δ) | Avg Lat ms (Δ) |\", \"|-------|------------|-----------|----------------|\"]\n    all_slices = sorted(set(b.keys()) | set(c.keys()))\n    for s in all_slices:\n        cb = b.get(s, {})\n        cc = c.get(s, {})\n        cit = _fmt_delta(cc.get('avg_cit_f1'), cb.get('avg_cit_f1'), higher_is_better=True)\n        inst = _fmt_delta(cc.get('avg_instruction_format_score'), cb.get('avg_instruction_format_score'), higher_is_better=True)\n        lat = _fmt_delta(cc.get('avg_latency_ms'), cb.get('avg_latency_ms'), higher_is_better=False)\n        lines.append(f\"| {s} | {cit} | {inst} | {lat} |\")\n    return \"\\n\".join(lines)\n\n__all__ = [\"build_slice_delta_table\"] |
42 | src/rubrics/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
