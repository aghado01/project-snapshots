idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | configs/gates_mercury.yaml | {349 20 0.1633 4.3412} | 360 | mercury_gates:\n  rubric_format:\n    - all_criteria_true_false_descriptive\n    - minimum_3_custom_criteria\n    - no_command_language\n  conversation:\n    - no_system_user_contradiction\n    - 5_to_20_turns\n    - target_instruction_following_not_knowledge\n  submission:\n    - golden_passes_all_rubric_criteria\n    - daily_limit_3_until_first_acceptances |
1 | configs/gates.yaml | {650 66 0.1969 4.4553} | 668 | gates:\n  - name: citations_f1\n    metric: global.citations_f1_mean\n    min: 0.40  # relaxed initial threshold for echo baseline\n  - name: instruction_format_score\n    metric: global.instruction_format_mean\n    min: 0.50\n  - name: latency_p95\n    metric: global.latency_p95_ms\n    max: 1500\n  - name: slice_citations_needed\n    metric: slice.needs_citations.avg_cit_f1\n    min: 0.30\n  - name: citations_no_large_drop\n    metric: global.citations_f1_mean\n    max_drop: 0.15  # allow at most 15% relative drop vs baseline\n  - name: latency_no_large_increase\n    metric: global.latency_p95_ms\n    max_increase: 0.20  # allow at most 20% relative increase |
2 | configs/providers.yaml | {157 23 0.2293 4.2377} | 163 | default_model: echo-small\nproviders:\n  - name: echo\n    model: echo-small\n  # Real providers can be added later, e.g.:\n  # - name: openai\n  #   model: gpt-4o |
3 | configs/rubrics.yaml | {584 54 0.2586 4.5716} | 613 | rubrics:\n  - name: instruction_format\n    weights:\n      follows_format: 0.6\n      answers_question: 0.4\n    criteria:\n      follows_format:\n        type: regex\n        pattern: '^(?:[-*]|\\d+\\.)\\s'\n      answers_question:\n        type: keyword_presence\n        min_keywords: 1\n\nmercury_rubrics:\n  inference_memory:\n    base_criteria: *base_criteria  # Reference existing\n    custom_patterns:\n      - \"Contains exactly {count} {items}\"\n      - \"Mentions {entity} at least once\"\n      - \"Does not contain {forbidden_content}\"\n  # Templates for all 5 error modes with TRUE/FALSE patterns |
4 | eval/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
5 | init.ps1 | {3213 357 0.1575 5.0685} | 3549 | Param(\n  [string]$RepoName = \"conversational-quality-lab\"\n)\n\n$here = (Get-Location).Path\n$root = Join-Path $here $RepoName\n\nNew-Item -ItemType Directory -Path $root -Force | Out-Null\n\n$dirs = @(\n  \"configs\",\n  \"data/prompts\",\n  \"data/corpus\",\n  \"src\",\n  \"src/client\",\n  \"src/runners\",\n  \"src/metrics\",\n  \"src/rubrics\",\n  \"src/slices\",\n  \"src/render\",\n  \"eval\",\n  \"reports\",\n  \"templates/table_specs\",\n  \"scripts\",\n  \"tests/unit\"\n) | ForEach-Object { Join-Path $root $_ }\n\nforeach ($d in $dirs) {\n  New-Item -ItemType Directory -Path $d -Force | Out-Null\n}\n\n$files = @{\n  (Join-Path $root \".gitignore\") = @\"\n.env\n__pycache__/\n*.pyc\n.reports_tmp/\nreports/\n.run/\n\"@;\n\n  (Join-Path $root \"README.md\") = @\"\n\nEvaluation-first skeleton with batched runs, slice metrics, and regression gates.\n\"@;\n\n  (Join-Path $root \".env.example\") = @\"\n\nPROVIDER_API_KEY=sk-xxx\n\"@;\n\n  (Join-Path $root \"requirements.txt\") = @\"\npydantic>=2\npython-dotenv\n\"@;\n\n  (Join-Path $root \"configs\\providers.yaml\") = @\"\ndefault_model: gpt-4o\nproviders:\n  - name: openai\n    model: gpt-4o\n\"@;\n\n  (Join-Path $root \"configs\\rubrics.yaml\") = @\"\nrubrics: []\n\"@;\n\n  (Join-Path $root \"configs\\gates.yaml\") = @\"\ngates:\n  - name: citations_f1\n    threshold: 0.70\n\"@;\n\n  (Join-Path $root \"data\\prompts\\README.md\") = \"Prompt sets with slice tags.\";\n  (Join-Path $root \"data\\prompts\\citations.jsonl\") = \"\";\n  (Join-Path $root \"data\\prompts\\instructions.jsonl\") = \"\";\n  (Join-Path $root \"data\\prompts\\ambiguity.jsonl\") = \"\";\n  (Join-Path $root \"data\\corpus\\README.md\") = \"Tiny retrieval corpus.\";\n\n  (Join-Path $root \"src\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\client\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\client\\providers.py\") = @\"\n\nclass ProviderClient: ...\n\"@;\n\n  (Join-Path $root \"src\\runners\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\runners\\batch.py\") = @\"\n\ndef main(): ...\nif __name__ == '__main__': main()\n\"@;\n\n  (Join-Path $root \"src\\metrics\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\metrics\\citations.py\") = @\"\n\ndef score_citations(ans): return 0.0\n\"@;\n\n  (Join-Path $root \"src\\rubrics\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\slices\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\slices\\label.py\") = @\"\n\ndef label(prompt): return []\n\"@;\n\n  (Join-Path $root \"src\\render\\__init__.py\") = \"\";\n\n  (Join-Path $root \"eval\\__init__.py\") = \"\";\n\n  (Join-Path $root \"scripts\\run_baseline.ps1\") = @\"\npython -m src.runners.batch --config configs\\providers.yaml --prompts data\\prompts --out reports\\baseline\n\"@;\n\n  (Join-Path $root \"scripts\\compare_to_baseline.ps1\") = @\"\npython -m src.runners.batch --compare reports\\baseline --out reports\\candidate\n\"@;\n\n  (Join-Path $root \"templates\\table_specs\\README.md\") = \"Specs for dense QA tables.\";\n\n  (Join-Path $root \"tests\\unit\\test_placeholders.py\") = @\"\ndef test_placeholder():\n    assert True\n\"@;\n}\n\nforeach ($kvp in $files.GetEnumerator()) {\n  $path = $kvp.Key\n  $content = $kvp.Value\n  New-Item -ItemType File -Path $path -Force | Out-Null\n  if ($null -ne $content -and $content -ne \"\") {\n    Set-Content -Path $path -Value $content -Encoding UTF8\n  }\n}\n\nWrite-Host \"\"\nWrite-Host \"Created repo at: $root\"\nWrite-Host \"Top-level structure:\"\nGet-ChildItem -Path $root -Recurse | Select-Object FullName |
6 | scripts/compare_gates.ps1 | {420 48 0.1595 4.8108} | 439 | param(\n    [string]$Baseline = \"reports/baseline-latest\",\n    [string]$Candidate,\n    [string]$Gates = \"configs/gates.yaml\"\n)\nif (-not $Candidate) { throw \"--Candidate run directory required\" }\npython -m src.runners.compare --baseline $Baseline --candidate $Candidate --gates $Gates\n$status = $LASTEXITCODE\nif ($status -ne 0) {\n    Write-Host \"Gate check failed with code $status\" -ForegroundColor Red\n    exit $status\n} |
7 | scripts/compare_to_baseline.ps1 | {78 7 0.0769 4.1736} | 80 | python -m src.runners.batch --compare reports\\baseline --out reports\\candidate |
8 | scripts/run_baseline.ps1 | {348 39 0.1092 5.0130} | 359 | $ts = Get-Date -Format 'yyyyMMdd-HHmmss'\n$out = \"reports/baseline-$ts\"\npython -m src.runners.batch --config configs\\providers.yaml --prompts data\\prompts --out $out\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\npwsh scripts/update_baseline_alias.ps1 -BaselineDir $out\nWrite-Host \"Baseline run saved to $out and aliased at reports/baseline-latest\" |
9 | scripts/update_baseline_alias.ps1 | {0 0 0.0000 0.0000} | 0 |  |
10 | src/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
11 | src/client/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
12 | src/client/providers.py | {3714 410 0.1707 5.0763} | 4099 | \n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any, Dict, Iterable, Optional\nimport os, time, json, random\n\nDEFAULT_MAX_RETRIES = 3\nBACKOFF_SECONDS = 1.5\n\n@dataclass\nclass ProviderRequest:\n\tprompt: str\n\tmodel: str\n\ttemperature: float = 0.2\n\tmax_tokens: int = 512\n\tseed: Optional[int] = None\n\tmetadata: Optional[Dict[str, Any]] = None\n\n@dataclass\nclass ProviderResponse:\n\ttext: str\n\tmodel: str\n\tlatency_ms: float\n\tinput_tokens: int | None = None\n\toutput_tokens: int | None = None\n\tcost_usd: float | None = None\n\terror: str | None = None\n\traw: Dict[str, Any] | None = None\n\nclass ProviderClient:\n\tname: str = \"base\"\n\n\tdef __init__(self, model: str, **kwargs):\n\t\tself.model = model\n\t\tself.kwargs = kwargs\n\n\tdef generate(self, req: ProviderRequest) -> ProviderResponse:  # pragma: no cover - abstract\n\t\traise NotImplementedError\n\n\tdef safe_generate(self, req: ProviderRequest, max_retries: int = DEFAULT_MAX_RETRIES) -> ProviderResponse:\n\t\tattempt = 0\n\t\tlast_err: str | None = None\n\t\twhile attempt <= max_retries:\n\t\t\ttry:\n\t\t\t\tstart = time.perf_counter()\n\t\t\t\tresp = self.generate(req)\n\t\t\t\telapsed = (time.perf_counter() - start) * 1000\n\t\t\t\tresp.latency_ms = elapsed\n\t\t\t\treturn resp\n\t\t\texcept Exception as e:  # noqa: BLE001\n\t\t\t\tlast_err = str(e)\n\t\t\t\tif attempt == max_retries:\n\t\t\t\t\treturn ProviderResponse(\n\t\t\t\t\t\ttext=\"\",\n\t\t\t\t\t\tmodel=self.model,\n\t\t\t\t\t\tlatency_ms=0.0,\n\t\t\t\t\t\terror=last_err,\n\t\t\t\t\t\traw={\"attempts\": attempt + 1},\n\t\t\t\t\t)\n\t\t\t\ttime.sleep(BACKOFF_SECONDS * (attempt + 1))\n\t\t\t\tattempt += 1\n\nclass EchoDeterministicProvider(ProviderClient):\n\t\n\n\tname = \"echo\"\n\n\tdef generate(self, req: ProviderRequest) -> ProviderResponse:\n\t\tseed = req.seed if req.seed is not None else 0\n\t\trnd = random.Random(seed)\n\t\tsimulated_latency = 25 + rnd.random() * 15  # ms\n\t\ttime.sleep(simulated_latency / 1000.0)\n\n\t\treply = f\"[echo:{self.model}] \" + req.prompt.strip()[:200]\n\t\tinput_tokens = len(req.prompt.split())\n\t\toutput_tokens = len(reply.split())\n\t\treturn ProviderResponse(\n\t\t\ttext=reply,\n\t\t\tmodel=self.model,\n\t\t\tlatency_ms=simulated_latency,\n\t\t\tinput_tokens=input_tokens,\n\t\t\toutput_tokens=output_tokens,\n\t\t\tcost_usd=0.0,\n\t\t\traw={\"provider\": self.name},\n\t\t)\n\n_REGISTRY: Dict[str, type[ProviderClient]] = {\n\tEchoDeterministicProvider.name: EchoDeterministicProvider,\n}\n\ndef create_provider(name: str, model: str, **kwargs) -> ProviderClient:\n\tif name not in _REGISTRY:\n\t\traise ValueError(f\"Unknown provider '{name}'. Registered: {list(_REGISTRY)}\")\n\treturn _REGISTRY[name](model=model, **kwargs)\n\ndef load_provider_from_config(cfg: dict, override_name: str | None = None, override_model: str | None = None) -> ProviderClient:\n\tproviders: Iterable[dict] = cfg.get(\"providers\", [])\n\tdefault_model = cfg.get(\"default_model\")\n\tsel_name = override_name\n\tsel_model = override_model or default_model\n\tif sel_name is None:\n\n\t\tfirst = next(iter(providers), None)\n\t\tif first is None:\n\t\t\traise ValueError(\"providers.yaml missing providers list\")\n\t\tsel_name = first.get(\"name\")\n\t\tsel_model = sel_model or first.get(\"model\")\n\n\tmatch = None\n\tfor p in providers:\n\t\tif p.get(\"name\") == sel_name:\n\t\t\tmatch = p\n\t\t\tbreak\n\tif match is None:\n\t\traise ValueError(f\"Provider '{sel_name}' not found in config\")\n\tsel_model = override_model or match.get(\"model\") or sel_model\n\treturn create_provider(sel_name, model=sel_model)\n\ndef save_trace_line(path: str, record: dict) -> None:\n\tos.makedirs(os.path.dirname(path), exist_ok=True)\n\twith open(path, \"a\", encoding=\"utf-8\") as f:\n\t\tf.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n\n__all__ = [\n\t\"ProviderClient\",\n\t\"ProviderRequest\",\n\t\"ProviderResponse\",\n\t\"EchoDeterministicProvider\",\n\t\"create_provider\",\n\t\"load_provider_from_config\",\n\t\"save_trace_line\",\n] |
13 | src/metrics/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
14 | src/metrics/citations.py | {1835 218 0.1717 4.8796} | 2025 | \n\nfrom __future__ import annotations\n\nfrom typing import List, Dict, Any, Optional\nimport re\n\n_CIT_RE = re.compile(r\"\\[(\\d+)\\]\")\n\ndef detect_citations(answer: str) -> List[int]:\n\t\n\tif not answer:\n\t\treturn []\n\treturn [int(m.group(1)) for m in _CIT_RE.finditer(answer)]\n\ndef compute_faithfulness(answer: str, references: Optional[List[str]] = None) -> float:\n\t\n\tif not references:\n\t\treturn 0.0\n\tr = score_citations(answer, expected_sources=references)\n\treturn float(r.get(\"f1\") or 0.0)\n\ndef score_citations(answer: str, expected_sources: Optional[List[str]] = None) -> Dict[str, Any]:\n\tif not answer:\n\t\treturn {\n\t\t\t\"produced_unique\": 0,\n\t\t\t\"produced_total\": 0,\n\t\t\t\"expected_count\": len(expected_sources) if expected_sources else None,\n\t\t\t\"matched\": 0,\n\t\t\t\"precision\": 0.0,\n\t\t\t\"recall\": 0.0,\n\t\t\t\"f1\": 0.0 if expected_sources else None,\n\t\t}\n\n\tnums = [int(m.group(1)) for m in _CIT_RE.finditer(answer)]\n\tproduced_total = len(nums)\n\tproduced_set = set(nums)\n\texpected_count = len(expected_sources) if expected_sources else None\n\n\tif expected_sources and expected_count is not None:\n\t\texpected_ids = set(range(1, expected_count + 1))  # sequential expectation\n\t\tmatched = len(produced_set & expected_ids)\n\t\tprecision = matched / len(produced_set) if produced_set else 0.0\n\t\trecall = matched / expected_count if expected_count else 0.0\n\t\tif precision + recall > 0:\n\t\t\tf1 = 2 * precision * recall / (precision + recall)\n\t\telse:\n\t\t\tf1 = 0.0\n\telse:\n\t\tmatched = 0\n\t\tprecision = 0.0\n\t\trecall = 0.0\n\t\tf1 = None\n\n\treturn {\n\t\t\"produced_unique\": len(produced_set),\n\t\t\"produced_total\": produced_total,\n\t\t\"expected_count\": expected_count,\n\t\t\"matched\": matched,\n\t\t\"precision\": round(precision, 4),\n\t\t\"recall\": round(recall, 4),\n\t\t\"f1\": round(f1, 4) if f1 is not None else None,\n\t}\n\n__all__ = [\"score_citations\", \"detect_citations\", \"compute_faithfulness\"] |
15 | src/metrics/mercury.py | {0 0 0.0000 0.0000} | 0 |  |
16 | src/render/__init__.py | {1265 89 0.1992 4.6437} | 1342 | \n\nfrom __future__ import annotations\n\nimport os\nfrom functools import lru_cache\n\nfrom jinja2 import Environment\n\nfrom .engine import TemplateEngine\nfrom .report import build_report, write_report  # type: ignore F401\n\ndef _templates_path() -> str:\n    return os.path.join(os.path.dirname(__file__), \"templates\")\n\ndef _bytecode_cache_path() -> str:\n    root = os.path.join(\n        os.path.dirname(os.path.dirname(__file__)),\n        \".cache\",\n        \"jinja2\",\n    )\n    os.makedirs(root, exist_ok=True)\n    return root\n\n@lru_cache(maxsize=1)\ndef get_environment() -> Environment:\n    \n    engine = TemplateEngine(\n        template_dirs=_templates_path(),\n        cache_dir=_bytecode_cache_path(),\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n    return engine.environment\n\n@lru_cache(maxsize=1)\ndef get_template_engine() -> TemplateEngine:\n    \n    return TemplateEngine(\n        template_dirs=_templates_path(),\n        cache_dir=_bytecode_cache_path(),\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n\n__all__ = [\"get_environment\", \"get_template_engine\", \"write_report\", \"build_report\"] |
17 | src/render/engine.py | {5665 409 0.3041 4.3385} | 5874 | \n\nfrom __future__ import annotations\n\nimport os\nfrom pathlib import Path\nfrom typing import Any, Dict, List, Optional, Union\n\nfrom jinja2 import (\n    BaseLoader,\n    DictLoader,\n    Environment,\n    FileSystemBytecodeCache,\n    FileSystemLoader,\n    Template,\n    TemplateNotFound,\n    select_autoescape,\n)\n\nclass TemplateEngine:\n    \n\n    def __init__(\n        self,\n        template_dirs: Optional[Union[str, Path, List[Union[str, Path]]]] = None,\n        template_dict: Optional[Dict[str, str]] = None,\n        loader: Optional[BaseLoader] = None,\n        cache_dir: Optional[Union[str, Path]] = None,\n        autoescape_extensions: Optional[List[str]] = None,\n        no_autoescape_extensions: Optional[List[str]] = None,\n        autoescape_default: bool = False,\n        **env_kwargs: Any,\n    ):\n        \n        self._env = self._create_environment(\n            template_dirs=template_dirs,\n            template_dict=template_dict,\n            loader=loader,\n            cache_dir=cache_dir,\n            autoescape_extensions=autoescape_extensions or [\"html\", \"xml\"],\n            no_autoescape_extensions=no_autoescape_extensions or [\"md\", \"txt\"],\n            autoescape_default=autoescape_default,\n            **env_kwargs,\n        )\n\n    def _create_environment(\n        self,\n        template_dirs: Optional[Union[str, Path, List[Union[str, Path]]]],\n        template_dict: Optional[Dict[str, str]],\n        loader: Optional[BaseLoader],\n        cache_dir: Optional[Union[str, Path]],\n        autoescape_extensions: List[str],\n        no_autoescape_extensions: List[str],\n        autoescape_default: bool,\n        **env_kwargs: Any,\n    ) -> Environment:\n\n        if loader is not None:\n            env_loader = loader\n        elif template_dirs is not None or template_dict is not None:\n            loaders = []\n\n            if template_dirs is not None:\n                if isinstance(template_dirs, (str, Path)):\n                    template_dirs = [template_dirs]\n                str_dirs = [str(d) for d in template_dirs]\n                loaders.append(FileSystemLoader(str_dirs))\n\n            if template_dict is not None:\n                loaders.append(DictLoader(template_dict))\n\n            if len(loaders) == 1:\n                env_loader = loaders[0]\n            else:\n                from jinja2 import ChoiceLoader\n                env_loader = ChoiceLoader(loaders)\n        else:\n\n            env_loader = DictLoader({})\n\n        bytecode_cache = None\n        if cache_dir is not None:\n            cache_path = Path(cache_dir)\n            cache_path.mkdir(parents=True, exist_ok=True)\n            bytecode_cache = FileSystemBytecodeCache(directory=str(cache_path))\n\n        autoescape = select_autoescape(\n            enabled_extensions=autoescape_extensions,\n            disabled_extensions=no_autoescape_extensions,\n            default_for_string=autoescape_default,\n        )\n\n        env = Environment(\n            loader=env_loader,\n            autoescape=autoescape,\n            bytecode_cache=bytecode_cache,\n            **env_kwargs,\n        )\n\n        return env\n\n    @property\n    def environment(self) -> Environment:\n        \n        return self._env\n\n    def render_template(self, template_name: str, **context: Any) -> str:\n        \n        template = self._env.get_template(template_name)\n        return template.render(**context)\n\n    def render_string(self, template_string: str, **context: Any) -> str:\n        \n        template = self._env.from_string(template_string)\n        return template.render(**context)\n\n    def render_template_safe(\n        self,\n        template_name: str,\n        fallback_template: Optional[str] = None,\n        **context: Any,\n    ) -> str:\n        \n        try:\n            return self.render_template(template_name, **context)\n        except TemplateNotFound:\n            if fallback_template is not None:\n                return self.render_string(fallback_template, **context)\n            raise\n\n    def add_template(self, name: str, content: str) -> None:\n\n        loader = self._env.loader\n\n        if hasattr(loader, 'mapping'):\n            loader.mapping[name] = content\n            return\n\n        if hasattr(loader, 'loaders') and isinstance(getattr(loader, 'loaders'), (list, tuple)):\n            for sub in loader.loaders:  # type: ignore[attr-defined]\n                if hasattr(sub, 'mapping'):\n                    sub.mapping[name] = content\n                    return\n\n        raise NotImplementedError(\n            \"Adding templates at runtime is only supported with DictLoader. \"\n            \"Initialize the engine with template_dict or include a DictLoader in the loader chain.\"\n        )\n\n    def template_exists(self, template_name: str) -> bool:\n        \n        try:\n            self._env.get_template(template_name)\n            return True\n        except TemplateNotFound:\n            return False\n\n    def list_templates(self) -> List[str]:\n        \n        return self._env.list_templates()\n\ndef create_simple_engine(\n    template_dir: Optional[Union[str, Path]] = None,\n    cache_dir: Optional[Union[str, Path]] = None,\n) -> TemplateEngine:\n    \n    return TemplateEngine(\n        template_dirs=template_dir,\n        cache_dir=cache_dir,\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n\ndef create_string_engine(templates: Optional[Dict[str, str]] = None) -> TemplateEngine:\n    \n    return TemplateEngine(\n        template_dict=templates or {},\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    ) |
18 | src/render/examples.py | {6167 438 0.3042 4.3434} | 6584 | \n\nfrom pathlib import Path\nimport tempfile\nfrom typing import Dict, Any\n\nfrom .engine import TemplateEngine, create_simple_engine, create_string_engine\n\ndef example_string_templates():\n\n    templates = {\n        \"greeting\": \"Hello, {{ name }}! Welcome to {{ app_name }}.\",\n        \"report\": .strip(),\n        \"email\": .strip()\n    }\n\n    engine = create_string_engine(templates)\n\n    greeting = engine.render_template(\"greeting\",\n                                    name=\"Alice\",\n                                    app_name=\"Quality Lab\")\n\n    report = engine.render_template(\"report\",\n                                  title=\"Daily Report\",\n                                  timestamp=\"2025-09-07\",\n                                  data={\"tests_run\": 42, \"success_rate\": \"95%\"})\n\n    email = engine.render_template(\"email\",\n                                 recipient=\"Bob\",\n                                 message=\"The report is ready for review.\",\n                                 sender=\"System\")\n\n    return {\n        \"greeting\": greeting,\n        \"report\": report,\n        \"email\": email\n    }\n\ndef example_filesystem_templates():\n\n    with tempfile.TemporaryDirectory() as temp_dir:\n        temp_path = Path(temp_dir)\n\n        (temp_path / \"hello.txt\").write_text(\"Hello, {{ name }}!\")\n        (temp_path / \"config.yaml\").write_text(.strip())\n\n        engine = create_simple_engine(template_dir=temp_dir)\n\n        hello = engine.render_template(\"hello.txt\", name=\"World\")\n\n        config = engine.render_template(\"config.yaml\",\n                                      app_name=\"MyApp\",\n                                      version=\"1.0.0\",\n                                      debug=True,\n                                      features=[\"auth\", \"logging\", \"metrics\"])\n\n        return {\n            \"hello\": hello,\n            \"config\": config\n        }\n\ndef example_mixed_templates():\n    \n\n    with tempfile.TemporaryDirectory() as temp_dir:\n        temp_path = Path(temp_dir)\n\n        (temp_path / \"base.html\").write_text(.strip())\n\n        string_templates = {\n            \"content\": \"<h1>{{ heading }}</h1><p>{{ text }}</p>\"\n        }\n\n        engine = TemplateEngine(\n            template_dirs=temp_dir,\n            template_dict=string_templates\n        )\n\n        content = engine.render_template(\"content\",\n                                       heading=\"Welcome\",\n                                       text=\"This is a mixed template example.\")\n\n        page = engine.render_template(\"base.html\",\n                                    title=\"Example Page\",\n                                    content=content)\n\n        return page\n\ndef example_safe_rendering():\n\n    templates = {\n        \"exists\": \"This template exists: {{ message }}\"\n    }\n    engine = create_string_engine(templates)\n\n    result1 = engine.render_template(\"exists\", message=\"Hello!\")\n\n    fallback = \"Fallback template: {{ message }}\"\n    result2 = engine.render_template_safe(\"missing\",\n                                        fallback_template=fallback,\n                                        message=\"Using fallback\")\n\n    exists_check = engine.template_exists(\"exists\")  # True\n    missing_check = engine.template_exists(\"missing\")  # False\n\n    return {\n        \"result1\": result1,\n        \"result2\": result2,\n        \"exists_check\": exists_check,\n        \"missing_check\": missing_check\n    }\n\ndef example_custom_configuration():\n    \n\n    templates = {\n        \"html\": \"<p>{{ content }}</p>\",  # Will be autoescaped\n        \"text\": \"{{ content }}\",         # Won't be autoescaped\n    }\n\n    engine = TemplateEngine(\n        template_dict=templates,\n        autoescape_extensions=[\"html\"],\n        no_autoescape_extensions=[\"txt\", \"text\"],\n        autoescape_default=True,  # Default to escaping\n    )\n\n    unsafe_content = \"<script>alert('xss')</script>\"\n\n    html_result = engine.render_template(\"html\", content=unsafe_content)\n    text_result = engine.render_template(\"text\", content=unsafe_content)\n\n    return {\n        \"html_escaped\": html_result,  # Should be escaped\n        \"text_raw\": text_result,      # Should be raw\n    }\n\ndef example_runtime_templates():\n\n    engine = create_string_engine()\n\n    engine.add_template(\"dynamic1\", \"Hello, {{ name }}!\")\n    engine.add_template(\"dynamic2\", \"Count: {{ count }}\")\n\n    result1 = engine.render_template(\"dynamic1\", name=\"Dynamic\")\n    result2 = engine.render_template(\"dynamic2\", count=42)\n\n    templates_list = engine.list_templates()\n\n    return {\n        \"result1\": result1,\n        \"result2\": result2,\n        \"all_templates\": templates_list\n    }\n\ndef example_render_string():\n    \n\n    engine = create_string_engine()\n\n    template1 = \"Hello, {{ name }}!\"\n    result1 = engine.render_string(template1, name=\"Direct\")\n\n    template2 = .strip()\n    result2 = engine.render_string(template2, items=[\"apple\", \"banana\", \"cherry\"])\n\n    return {\n        \"greeting\": result1,\n        \"list\": result2\n    }\n\nif __name__ == \"__main__\":\n\n    print(\"=== String Templates ===\")\n    string_results = example_string_templates()\n    for name, content in string_results.items():\n        print(f\"\\n{name}:\\n{content}\")\n\n    print(\"\\n=== Filesystem Templates ===\")\n    fs_results = example_filesystem_templates()\n    for name, content in fs_results.items():\n        print(f\"\\n{name}:\\n{content}\")\n\n    print(\"\\n=== Mixed Templates ===\")\n    mixed_result = example_mixed_templates()\n    print(f\"\\n{mixed_result}\")\n\n    print(\"\\n=== Safe Rendering ===\")\n    safe_results = example_safe_rendering()\n    for name, content in safe_results.items():\n        print(f\"\\n{name}: {content}\")\n\n    print(\"\\n=== Custom Configuration ===\")\n    custom_results = example_custom_configuration()\n    for name, content in custom_results.items():\n        print(f\"\\n{name}: {content}\")\n\n    print(\"\\n=== Runtime Templates ===\")\n    runtime_results = example_runtime_templates()\n    for name, content in runtime_results.items():\n        print(f\"\\n{name}: {content}\")\n\n    print(\"\\n=== Render String ===\")\n    string_render_results = example_render_string()\n    for name, content in string_render_results.items():\n        print(f\"\\n{name}:\\n{content}\") |
