idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
34 | src/probes/cli.py | {7745 483 0.2167 4.6229} | 8130 | \"\"\"Command-line entrypoint for generating prompt-harness probe fixtures.\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nfrom pathlib import Path\nfrom typing import Any\n\nfrom .adapters import make_prompt_record, write_jsonl\nfrom .payloads import (\n    analyze_payload,\n    analyze_payload_lines,\n    build_integer_lines,\n    build_integer_payload,\n    build_whitespace_lines,\n    build_whitespace_payload,\n    lines_to_payload,\n    render_payload_json,\n    render_payload_template,\n    validate_line_indexed_payload,\n)\nfrom .unicode import (\n    build_bpe_merge_probe,\n    build_minimal_pair_probe,\n    build_token_boundary_probe,\n    build_unicode_probe,\n)\n\ndef _write_json(path: Path, data: Any, *, compact: bool = False) -> str:\n    path.parent.mkdir(parents=True, exist_ok=True)\n    if compact:\n        text = json.dumps(data, ensure_ascii=False, separators=(\",\", \":\"))\n    else:\n        text = json.dumps(data, ensure_ascii=False, indent=2)\n    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n    return text\n\ndef _generate_tokenizer_baseline(args: argparse.Namespace) -> list[dict[str, Any]]:\n    output_dir = Path(args.payload_out) / \"tokenizer_baseline\"\n    rows: list[dict[str, Any]] = []\n\n    whitespace_lines = build_whitespace_lines(\n        args.line_count,\n        args.start_width,\n        args.increment,\n        args.max_width,\n    )\n    whitespace = lines_to_payload(whitespace_lines)\n    if args.render_mode == \"template\":\n        whitespace_json = render_payload_template(\"simple-whitespace.j2\", whitespace_lines)\n        whitespace_analysis = analyze_payload_lines(\n            \"whitespace\",\n            whitespace_lines,\n            whitespace_json,\n        ).as_dict()\n    else:\n        whitespace = build_whitespace_payload(\n            args.line_count,\n            args.start_width,\n            args.increment,\n            args.max_width,\n        )\n        whitespace_json = render_payload_json(whitespace, ensure_ascii=False, compact=args.compact)\n        whitespace_analysis = analyze_payload(\"whitespace\", whitespace, whitespace_json).as_dict()\n    whitespace_path = output_dir / \"whitespace-payload.json\"\n    whitespace_path.parent.mkdir(parents=True, exist_ok=True)\n    whitespace_path.write_text(whitespace_json, encoding=\"utf-8\", newline=\"\\n\")\n    whitespace_analysis[\"validation_errors\"] = validate_line_indexed_payload(whitespace, str)\n    whitespace_analysis[\"render_mode\"] = args.render_mode\n    _write_json(output_dir / \"whitespace-payload-analysis.json\", whitespace_analysis)\n    rows.append(\n        make_prompt_record(\n            record_id=\"tokenizer_baseline:whitespace\",\n            serialized_payload=whitespace_json,\n            suite=\"tokenizer_baseline\",\n            probe_type=\"whitespace\",\n            payload_path=str(whitespace_path),\n        )\n    )\n\n    integer_lines = build_integer_lines(args.line_count, args.start_value, args.increment)\n    integer = lines_to_payload(integer_lines)\n    if args.render_mode == \"template\":\n        integer_json = render_payload_template(\"simple-integer.j2\", integer_lines)\n        integer_analysis = analyze_payload_lines(\n            \"integer\",\n            integer_lines,\n            integer_json,\n        ).as_dict()\n    else:\n        integer = build_integer_payload(args.line_count, args.start_value, args.increment)\n        integer_json = render_payload_json(integer, ensure_ascii=False, compact=args.compact)\n        integer_analysis = analyze_payload(\"integer\", integer, integer_json).as_dict()\n    integer_path = output_dir / \"integer-payload.json\"\n    integer_path.write_text(integer_json, encoding=\"utf-8\", newline=\"\\n\")\n    integer_analysis[\"validation_errors\"] = validate_line_indexed_payload(integer, int)\n    integer_analysis[\"render_mode\"] = args.render_mode\n    _write_json(output_dir / \"integer-payload-analysis.json\", integer_analysis)\n    rows.append(\n        make_prompt_record(\n            record_id=\"tokenizer_baseline:integer\",\n            serialized_payload=integer_json,\n            suite=\"tokenizer_baseline\",\n            probe_type=\"integer\",\n            payload_path=str(integer_path),\n        )\n    )\n    return rows\n\ndef _generate_unicode_recon(args: argparse.Namespace) -> list[dict[str, Any]]:\n    output_dir = Path(args.payload_out) / \"unicode_recon\"\n    specs = {\n        \"unicode\": (\"unicode-systematic-probe.json\", build_unicode_probe()),\n        \"bpe\": (\"bpe-merge-probe.json\", build_bpe_merge_probe()),\n        \"boundary\": (\"token-boundary-probe.json\", build_token_boundary_probe()),\n    }\n    rows: list[dict[str, Any]] = []\n    for probe_type, (file_name, payload) in specs.items():\n        path = output_dir / file_name\n        text = _write_json(path, payload, compact=args.compact)\n        rows.append(\n            make_prompt_record(\n                record_id=f\"unicode_recon:{probe_type}\",\n                serialized_payload=text,\n                suite=\"unicode_recon\",\n                probe_type=probe_type,\n                payload_path=str(path),\n                extra_slices=[\"unicode_probe\"],\n            )\n        )\n\n    for char in [\"a\", \" \", \"中\", \"α\", \"А\", \"1\", \"!\", \"😀\"]:\n        payload = build_minimal_pair_probe(char, max_length=args.minimal_pair_max_length)\n        codepoint = f\"U+{ord(char):04X}\"\n        path = output_dir / f\"minimal-pair-{codepoint}.json\"\n        text = _write_json(path, payload, compact=args.compact)\n        rows.append(\n            make_prompt_record(\n                record_id=f\"unicode_recon:minimal_pair:{codepoint}\",\n                serialized_payload=text,\n                suite=\"unicode_recon\",\n                probe_type=\"minimal_pair\",\n                payload_path=str(path),\n                extra_slices=[\"unicode_probe\"],\n            )\n        )\n    return rows\n\ndef build_arg_parser() -> argparse.ArgumentParser:\n    parser = argparse.ArgumentParser(description=\"Generate context probe payloads and prompt fixtures\")\n    parser.add_argument(\"--suite\", choices=[\"all\", \"tokenizer_baseline\", \"unicode_recon\"], default=\"all\")\n    parser.add_argument(\"--payload-out\", default=\"data/probe_payloads\")\n    parser.add_argument(\"--prompts-out\", default=\"data/prompts/context_probes\")\n    parser.add_argument(\"--line-count\", type=int, default=50)\n    parser.add_argument(\"--start-width\", type=int, default=1)\n    parser.add_argument(\"--start-value\", type=int, default=1)\n    parser.add_argument(\"--increment\", type=int, default=1)\n    parser.add_argument(\"--max-width\", type=int, default=100)\n    parser.add_argument(\"--minimal-pair-max-length\", type=int, default=10)\n    parser.add_argument(\n        \"--render-mode\",\n        choices=[\"template\", \"structured\"],\n        default=\"template\",\n        help=\"line-indexed payload rendering mode\",\n    )\n    parser.add_argument(\"--compact\", action=\"store_true\", help=\"write compact JSON payloads\")\n    return parser\n\ndef main(argv: list[str] | None = None) -> int:\n    args = build_arg_parser().parse_args(argv)\n    suites = [\"tokenizer_baseline\", \"unicode_recon\"] if args.suite == \"all\" else [args.suite]\n\n    generated: dict[str, list[dict[str, Any]]] = {}\n    for suite in suites:\n        if suite == \"tokenizer_baseline\":\n            rows = _generate_tokenizer_baseline(args)\n        elif suite == \"unicode_recon\":\n            rows = _generate_unicode_recon(args)\n        else:  # pragma: no cover\n            raise ValueError(f\"unknown suite: {suite}\")\n        generated[suite] = rows\n        write_jsonl(Path(args.prompts_out) / f\"{suite}.jsonl\", rows)\n\n    print(f\"Generated probe suites: {', '.join(generated)}\")\n    print(f\"Payloads: {args.payload_out}\")\n    print(f\"Prompt fixtures: {args.prompts_out}\")\n    return 0\n\nif __name__ == \"__main__\":  # pragma: no cover\n    raise SystemExit(main()) |
35 | src/probes/payloads.py | {6299 572 0.2288 4.6335} | 6555 | \"\"\"Line-indexed payload generators for context-window probes.\n\nThese helpers intentionally keep structured payload construction separate from\nrendering. Some probes need valid JSON, while others deliberately need raw\ntemplate rendering so malformed strings and escaping boundaries can be tested.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Any, Literal\n\nfrom src.render.engine import TemplateEngine\n\nPayloadKind = Literal[\"integer\", \"whitespace\"]\n\n@dataclass(frozen=True)\nclass PayloadAnalysis:\n    payload_type: PayloadKind\n    line_count: int\n    total_json_bytes: int\n    payload_only_bytes: int\n    json_overhead_bytes: int\n\n    @property\n    def overhead_ratio(self) -> float:\n        if self.total_json_bytes == 0:\n            return 0.0\n        return self.json_overhead_bytes / self.total_json_bytes\n\n    @property\n    def overhead_percentage(self) -> float:\n        return round(self.overhead_ratio * 100, 2)\n\n    @property\n    def payload_efficiency(self) -> float:\n        return round((1 - self.overhead_ratio) * 100, 2)\n\n    def as_dict(self) -> dict[str, Any]:\n        return {\n            \"payload_type\": self.payload_type,\n            \"line_count\": self.line_count,\n            \"total_json_bytes\": self.total_json_bytes,\n            \"payload_only_bytes\": self.payload_only_bytes,\n            \"json_overhead_bytes\": self.json_overhead_bytes,\n            \"overhead_ratio\": self.overhead_ratio,\n            \"overhead_percentage\": self.overhead_percentage,\n            \"payload_efficiency\": self.payload_efficiency,\n        }\n\n@dataclass(frozen=True)\nclass PayloadLine:\n    number: int\n    content: Any\n    content_bytes: int\n\ndef _templates_path() -> Path:\n    return Path(__file__).resolve().parents[2] / \"templates\" / \"probes\"\n\ndef build_whitespace_lines(\n    line_count: int,\n    start_width: int = 1,\n    increment: int = 1,\n    max_width: int = 100,\n) -> list[PayloadLine]:\n    if line_count < 1:\n        raise ValueError(\"line_count must be >= 1\")\n    if start_width < 0 or increment < 0 or max_width < 0:\n        raise ValueError(\"width parameters must be >= 0\")\n\n    lines: list[PayloadLine] = []\n    for index in range(1, line_count + 1):\n        width = min(start_width + ((index - 1) * increment), max_width)\n        content = \" \" * width\n        lines.append(\n            PayloadLine(\n                number=index,\n                content=content,\n                content_bytes=len(content.encode(\"utf-8\")),\n            )\n        )\n    return lines\n\ndef build_integer_lines(\n    line_count: int,\n    start_value: int = 1,\n    increment: int = 1,\n) -> list[PayloadLine]:\n    if line_count < 1:\n        raise ValueError(\"line_count must be >= 1\")\n\n    lines: list[PayloadLine] = []\n    for index in range(1, line_count + 1):\n        content = start_value + ((index - 1) * increment)\n        lines.append(\n            PayloadLine(\n                number=index,\n                content=content,\n                content_bytes=len(str(content).encode(\"utf-8\")),\n            )\n        )\n    return lines\n\ndef lines_to_payload(lines: list[PayloadLine]) -> dict[str, Any]:\n    return {str(line.number): line.content for line in lines}\n\ndef build_whitespace_payload(\n    line_count: int,\n    start_width: int = 1,\n    increment: int = 1,\n    max_width: int = 100,\n) -> dict[str, str]:\n    payload = lines_to_payload(\n        build_whitespace_lines(line_count, start_width, increment, max_width)\n    )\n    return {key: str(value) for key, value in payload.items()}\n\ndef build_integer_payload(\n    line_count: int,\n    start_value: int = 1,\n    increment: int = 1,\n) -> dict[str, int]:\n    payload = lines_to_payload(build_integer_lines(line_count, start_value, increment))\n    return {key: int(value) for key, value in payload.items()}\n\ndef render_payload_template(\n    template_name: str,\n    payload_lines: list[PayloadLine],\n    *,\n    template_dir: str | Path | None = None,\n) -> str:\n    \"\"\"Render a payload with Jinja without JSON escaping.\n\n    This is intentionally not equivalent to ``json.dumps``. Probe templates are\n    allowed to emit invalid JSON, ambiguous quoting, trailing delimiters, or raw\n    Unicode depending on the experiment being designed.\n    \"\"\"\n\n    engine = TemplateEngine(template_dirs=template_dir or _templates_path())\n    return engine.render_template(template_name, payload_lines=payload_lines)\n\ndef render_payload_json(\n    payload: dict[str, Any],\n    *,\n    ensure_ascii: bool = False,\n    compact: bool = False,\n) -> str:\n    if compact:\n        return json.dumps(payload, ensure_ascii=ensure_ascii, separators=(\",\", \":\"))\n    return json.dumps(payload, ensure_ascii=ensure_ascii, indent=2)\n\ndef analyze_payload(\n    payload_type: PayloadKind,\n    payload: dict[str, Any],\n    rendered_json: str,\n) -> PayloadAnalysis:\n    payload_only_bytes = sum(len(str(value).encode(\"utf-8\")) for value in payload.values())\n    total_json_bytes = len(rendered_json.encode(\"utf-8\"))\n    return PayloadAnalysis(\n        payload_type=payload_type,\n        line_count=len(payload),\n        total_json_bytes=total_json_bytes,\n        payload_only_bytes=payload_only_bytes,\n        json_overhead_bytes=total_json_bytes - payload_only_bytes,\n    )\n\ndef analyze_payload_lines(\n    payload_type: PayloadKind,\n    payload_lines: list[PayloadLine],\n    rendered_payload: str,\n) -> PayloadAnalysis:\n    payload_only_bytes = sum(line.content_bytes for line in payload_lines)\n    total_json_bytes = len(rendered_payload.encode(\"utf-8\"))\n    return PayloadAnalysis(\n        payload_type=payload_type,\n        line_count=len(payload_lines),\n        total_json_bytes=total_json_bytes,\n        payload_only_bytes=payload_only_bytes,\n        json_overhead_bytes=total_json_bytes - payload_only_bytes,\n    )\n\ndef validate_line_indexed_payload(payload: dict[str, Any], expected_type: type) -> list[str]:\n    errors: list[str] = []\n    if not payload:\n        errors.append(\"payload is empty\")\n    for key, value in payload.items():\n        if not key.isdigit():\n            errors.append(f\"invalid key {key!r}; expected numeric string\")\n        if not isinstance(value, expected_type):\n            errors.append(f\"invalid value for {key!r}; expected {expected_type.__name__}\")\n    return errors |
36 | src/probes/unicode.py | {6844 611 0.2725 4.6259} | 7401 | \"\"\"Unicode and tokenizer-boundary probe builders.\"\"\"\n\nfrom __future__ import annotations\n\nimport unicodedata\nfrom typing import Any\n\ndef _chars(start: int, end: int) -> list[str]:\n    return [chr(codepoint) for codepoint in range(start, end + 1)]\n\ndef character_categories() -> dict[str, list[str]]:\n    return {\n        \"ascii_printable\": _chars(32, 126),\n        \"latin_extended\": _chars(192, 255),\n        \"greek\": _chars(913, 937) + _chars(945, 969),\n        \"cyrillic\": _chars(1040, 1071) + _chars(1072, 1103),\n        \"arabic_sample\": [chr(cp) for cp in (1575, 1576, 1578, 1579, 1580, 1582, 1583, 1584, 1585, 1586)],\n        \"chinese_common\": [chr(cp) for cp in (20013, 22269, 26159, 29992, 30340, 31616, 32463, 35821, 36825, 39564)],\n        \"emoji_basic\": [chr(cp) for cp in range(0x1F600, 0x1F606)],\n        \"mathematical\": [chr(cp) for cp in (8704, 8706, 8707, 8709, 8711, 8712, 8713, 8715, 8716, 8719)],\n        \"punctuation\": list(\"!@#$%^&*()-_+=[]{}|\\\\:;\\\"'<>,.?/~`\"),\n        \"whitespace\": [\" \", \"\\t\", \"\\n\", \"\\r\", chr(0x00A0), chr(0x2000), chr(0x2001), chr(0x2002), chr(0x2003)],\n        \"zero_width\": [chr(0x200B), chr(0x200C), chr(0x200D), chr(0xFEFF)],\n    }\n\ndef describe_character(char: str) -> dict[str, Any]:\n    if len(char) != 1:\n        raise ValueError(\"expected a single Unicode character\")\n    return {\n        \"char\": char,\n        \"codepoint\": f\"U+{ord(char):04X}\",\n        \"unicode_category\": unicodedata.category(char),\n        \"unicode_name\": unicodedata.name(char, \"<unassigned>\"),\n        \"utf8_bytes\": len(char.encode(\"utf-8\")),\n        \"utf16_bytes\": len(char.encode(\"utf-16-le\")),\n    }\n\ndef build_minimal_pair_probe(base_char: str, max_length: int = 10) -> dict[str, Any]:\n    if len(base_char) != 1:\n        raise ValueError(\"base_char must be a single Unicode character\")\n    if max_length < 1:\n        raise ValueError(\"max_length must be >= 1\")\n\n    pairs: list[dict[str, Any]] = [\n        {\"type\": \"single\", \"base_char\": base_char, \"test\": base_char, \"length\": 1},\n    ]\n    for length in range(2, max_length + 1):\n        pairs.append(\n            {\n                \"type\": \"repetition\",\n                \"base_char\": base_char,\n                \"test\": base_char * length,\n                \"length\": length,\n                \"expected_pattern\": \"may merge at tokenizer-specific lengths\",\n            }\n        )\n    pairs.extend(\n        [\n            {\"type\": \"space_prefix\", \"base_char\": base_char, \"test\": f\" {base_char}\", \"boundary_type\": \"word_start\"},\n            {\"type\": \"space_suffix\", \"base_char\": base_char, \"test\": f\"{base_char} \", \"boundary_type\": \"word_end\"},\n        ]\n    )\n    for other_char in [\"a\", \"中\", \"α\", \"А\", \"1\", \"!\"]:\n        if other_char != base_char:\n            pairs.append(\n                {\n                    \"type\": \"mixed_pair\",\n                    \"base_char\": base_char,\n                    \"other_char\": other_char,\n                    \"test\": f\"{base_char}{other_char}\",\n                    \"reverse_test\": f\"{other_char}{base_char}\",\n                    \"boundary_type\": \"script_transition\",\n                }\n            )\n\n    return {\n        \"probe_type\": \"minimal_pair\",\n        \"probe_character\": describe_character(base_char),\n        \"minimal_pairs\": pairs,\n        \"total_tests\": len(pairs),\n    }\n\ndef build_unicode_probe(sample_size: int = 5) -> dict[str, Any]:\n    categories = character_categories()\n    probes: dict[str, list[dict[str, Any]]] = {}\n    for name, chars in categories.items():\n        rows: list[dict[str, Any]] = []\n        for char in chars[:sample_size]:\n            rows.append(describe_character(char))\n        if len(chars) >= 3:\n            sequence = \"\".join(chars[:3])\n            rows.append({\"type\": \"intra_category_sequence\", \"sequence_test\": sequence, \"length\": len(sequence)})\n        probes[name] = rows\n    return {\n        \"probe_type\": \"systematic_unicode\",\n        \"categories_tested\": len(categories),\n        \"unicode_probes\": probes,\n        \"purpose\": \"Detect tokenizer Unicode handling, script boundaries, and encoding patterns\",\n        \"analysis_targets\": [\n            \"script boundary detection\",\n            \"multi-byte character handling\",\n            \"normalization behavior\",\n            \"emoji and special character treatment\",\n        ],\n    }\n\ndef build_bpe_merge_probe() -> dict[str, Any]:\n    common_bigrams = [\"th\", \"he\", \"in\", \"er\", \"an\", \"re\", \"ed\", \"nd\", \"ou\", \"ea\", \"ti\", \"to\", \"it\", \"st\", \"io\", \"le\", \"is\", \"ur\", \"ar\", \"as\"]\n    merge_probes: list[dict[str, Any]] = []\n    for bigram in common_bigrams:\n        merge_probes.append(\n            {\n                \"type\": \"common_bigram\",\n                \"pattern\": bigram,\n                \"test_cases\": [bigram, f\" {bigram}\", f\"{bigram} \", f\" {bigram} \", bigram + bigram, bigram + \"ing\", \"un\" + bigram],\n            }\n        )\n    for test_char in [\"a\", \"x\", \"1\", \"中\"]:\n        merge_probes.append(\n            {\n                \"type\": \"repetition_threshold\",\n                \"base_character\": test_char,\n                \"test_cases\": [test_char * length for length in range(1, 21)],\n                \"purpose\": \"detect at what length repetitions get merged\",\n            }\n        )\n    return {\n        \"probe_type\": \"bpe_merge_detection\",\n        \"merge_probes\": merge_probes,\n        \"purpose\": \"Reverse engineer BPE merge priorities and thresholds\",\n        \"analysis_targets\": [\"common bigram merge priority\", \"repetition merge thresholds\", \"context-dependent merging\", \"cross-linguistic merge patterns\"],\n    }\n\ndef build_token_boundary_probe() -> dict[str, Any]:\n    boundary_tests: list[dict[str, Any]] = []\n    base = \"hello\"\n    for position in range(1, len(base) + 1):\n        boundary_tests.append(\n            {\n                \"type\": \"progressive_addition\",\n                \"base\": base,\n                \"test\": base[:position],\n                \"position\": position,\n                \"purpose\": \"detect if tokenizer splits mid-word\",\n            }\n        )\n    for combo in [\"word.\", \"word,\", \"word!\", \"word?\", \"word;\", \"word:\", \".word\", \",word\", \"!word\", \"?word\", \";word\", \":word\", \"word.word\", \"word,word\", \"word!word\"]:\n        boundary_tests.append({\"type\": \"punctuation_boundary\", \"test\": combo, \"expected\": \"may split at punctuation\"})\n    for combo in [\"123abc\", \"abc123\", \"123.456\", \"word123\", \"123word\"]:\n        boundary_tests.append({\"type\": \"number_letter_boundary\", \"test\": combo, \"expected\": \"may split at number-letter boundary\"})\n    return {\n        \"probe_type\": \"token_boundary_detection\",\n        \"boundary_tests\": boundary_tests,\n        \"methodology\": \"Progressive addition and systematic boundary probing\",\n        \"purpose\": \"Map exact tokenizer split points\",\n        \"analysis_targets\": [\"word-internal splits\", \"punctuation handling\", \"number-letter transitions\", \"whitespace tokenization\"],\n    } |
37 | src/render/__init__.py | {2141 197 0.1864 4.7250} | 2255 | \"\"\"Render utilities (markdown reports, later HTML dashboards).\n\nCentralizes Jinja2 environment creation with safe defaults:\n- FileSystemLoader rooted at this package's templates directory\n- select_autoescape for html/xml; disabled for md/txt\n- Bytecode cache under `.cache/jinja2` (created on demand)\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom functools import lru_cache\n\nfrom jinja2 import Environment\n\nfrom .engine import TemplateEngine\n\ndef _templates_path() -> str:\n    return os.path.join(os.path.dirname(__file__), \"templates\")\n\ndef _bytecode_cache_path() -> str:\n    root = os.path.join(\n        os.path.dirname(os.path.dirname(__file__)),\n        \".cache\",\n        \"jinja2\",\n    )\n    os.makedirs(root, exist_ok=True)\n    return root\n\n@lru_cache(maxsize=1)\ndef get_environment() -> Environment:\n    \"\"\"Return a singleton Jinja2 Environment with safe defaults.\n\n    - Autoescape enabled for HTML / XML extensions only.\n    - Markdown left unescaped to preserve formatting.\n    - Bytecode caching for repeated batch renders.\n\n    This function maintains backward compatibility while using the new\n    general-purpose TemplateEngine under the hood.\n    \"\"\"\n    engine = TemplateEngine(\n        template_dirs=_templates_path(),\n        cache_dir=_bytecode_cache_path(),\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n    return engine.environment\n\n@lru_cache(maxsize=1)\ndef get_template_engine() -> TemplateEngine:\n    \"\"\"Return a singleton TemplateEngine configured for this project.\n\n    This provides access to the more powerful TemplateEngine interface\n    while maintaining the same configuration as get_environment().\n    \"\"\"\n    return TemplateEngine(\n        template_dirs=_templates_path(),\n        cache_dir=_bytecode_cache_path(),\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n\nfrom .report import build_report, write_report  # type: ignore F401,E402\n\n__all__ = [\"get_environment\", \"get_template_engine\", \"write_report\", \"build_report\"] |
