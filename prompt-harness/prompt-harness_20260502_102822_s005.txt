idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
43 | src/rubrics/engine.py | {7600 778 0.2200 4.7458} | 8010 | \"\"\"Rule-based rubric evaluation.\n\nThe engine stays deterministic and YAML-driven, but supports richer rubric\nfamilies than the original instruction-format check: format compliance,\nconstraint satisfaction, negative constraints, and hedging calibration.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport re\nimport json\nfrom typing import Any\n\n_DEFAULT_HEDGES = (\n    \"likely\",\n    \"may\",\n    \"might\",\n    \"could\",\n    \"depends\",\n    \"unclear\",\n    \"not sure\",\n    \"not certain\",\n    \"need more context\",\n    \"would help to know\",\n)\n\ndef _as_list(value: Any) -> list[Any]:\n    if value is None:\n        return []\n    if isinstance(value, list):\n        return value\n    if isinstance(value, tuple):\n        return list(value)\n    return [value]\n\ndef _criteria_items(rubric_cfg: dict[str, Any]) -> list[tuple[str, dict[str, Any]]]:\n    criteria_cfg = rubric_cfg.get(\"criteria\", {})\n    if isinstance(criteria_cfg, list):\n        return [(spec.get(\"name\") or f\"c{i}\", spec) for i, spec in enumerate(criteria_cfg)]\n    return list(criteria_cfg.items())\n\ndef _active(spec: dict[str, Any], slices: list[str] | None) -> bool:\n    active_on = set(str(s) for s in _as_list(spec.get(\"active_on_slices\")))\n    if not active_on:\n        return True\n    return bool(active_on & set(slices or []))\n\ndef _prompt_tokens(prompt: str) -> set[str]:\n    return {w.strip(\",.!?:;()[]{}\\\"\").lower() for w in prompt.split()[:40] if len(w.strip(\",.!?:;()[]{}\\\"\") ) > 3}\n\ndef _regex_score(answer: str, spec: dict[str, Any]) -> float:\n    pattern = spec.get(\"pattern\")\n    if not pattern:\n        return 0.0\n    return 1.0 if re.search(pattern, answer, flags=re.MULTILINE) else 0.0\n\ndef _keyword_presence_score(prompt: str, answer: str, spec: dict[str, Any]) -> float:\n    keywords = {str(k).lower() for k in _as_list(spec.get(\"keywords\"))}\n    if not keywords:\n        keywords = _prompt_tokens(prompt)\n    min_keywords = int(spec.get(\"min_keywords\", 1))\n    answer_lower = answer.lower()\n    hits = sum(1 for keyword in keywords if keyword and keyword in answer_lower)\n    if min_keywords <= 0:\n        return 1.0\n    return min(1.0, hits / min_keywords)\n\ndef _format_compliance_score(answer: str, spec: dict[str, Any]) -> float:\n    fmt = spec.get(\"format\") or spec.get(\"schema\")\n    text = answer.strip()\n    if not fmt or not text:\n        return 0.0\n    if fmt == \"json\":\n        try:\n            json.loads(text)\n            return 1.0\n        except json.JSONDecodeError:\n            return 0.0\n    if fmt == \"markdown_headers\":\n        return 1.0 if re.search(r\"^#{1,6}\\s+\\S\", text, flags=re.MULTILINE) else 0.0\n    if fmt == \"numbered_list\":\n        return 1.0 if re.search(r\"^\\s*\\d+[.)]\\s+\\S\", text, flags=re.MULTILINE) else 0.0\n    if fmt == \"bullet_list\":\n        return 1.0 if re.search(r\"^\\s*[-*]\\s+\\S\", text, flags=re.MULTILINE) else 0.0\n    if fmt == \"list\":\n        return max(\n            _format_compliance_score(answer, {\"format\": \"numbered_list\"}),\n            _format_compliance_score(answer, {\"format\": \"bullet_list\"}),\n        )\n    if fmt == \"table\":\n        return 1.0 if \"|\" in text and re.search(r\"\\|\\s*-{2,}\\s*\\|\", text) else 0.0\n    if fmt == \"single_paragraph\":\n        return 1.0 if \"\\n\\n\" not in text and len([ln for ln in text.splitlines() if ln.strip()]) <= 1 else 0.0\n    return 0.0\n\ndef _list_item_count(answer: str, fmt: str | None = None) -> int:\n    if fmt == \"bullet_list\":\n        pattern = r\"^\\s*[-*]\\s+\\S\"\n    elif fmt == \"numbered_list\":\n        pattern = r\"^\\s*\\d+[.)]\\s+\\S\"\n    else:\n        pattern = r\"^\\s*(?:[-*]|\\d+[.)])\\s+\\S\"\n    return sum(1 for line in answer.splitlines() if re.search(pattern, line))\n\ndef _exact_count_score(answer: str, spec: dict[str, Any]) -> float:\n    target = spec.get(\"target\")\n    expected = spec.get(\"count\")\n    if expected is None:\n        return 0.0\n    try:\n        expected_count = int(expected)\n    except (TypeError, ValueError):\n        return 0.0\n    if target == \"list_items\":\n        actual = _list_item_count(answer, spec.get(\"format\"))\n    elif target == \"non_empty_lines\":\n        actual = len([line for line in answer.splitlines() if line.strip()])\n    else:\n        return 0.0\n    return 1.0 if actual == expected_count else 0.0\n\ndef _constraint_satisfaction_score(answer: str, spec: dict[str, Any]) -> float:\n    answer_lower = answer.lower()\n    checks = []\n    for term in _as_list(spec.get(\"required_terms\")) + _as_list(spec.get(\"constraints\")):\n        checks.append(str(term).lower() in answer_lower)\n    for pattern in _as_list(spec.get(\"required_patterns\")):\n        checks.append(bool(re.search(str(pattern), answer, flags=re.MULTILINE | re.I)))\n    for term in _as_list(spec.get(\"negative_constraints\")) + _as_list(spec.get(\"forbidden_terms\")):\n        checks.append(str(term).lower() not in answer_lower)\n    for pattern in _as_list(spec.get(\"forbidden_patterns\")):\n        checks.append(not re.search(str(pattern), answer, flags=re.MULTILINE | re.I))\n    if not checks:\n        return 0.0\n    return sum(1 for check in checks if check) / len(checks)\n\ndef _negative_constraint_score(answer: str, spec: dict[str, Any]) -> float:\n    return _constraint_satisfaction_score(\n        answer,\n        {\n            \"forbidden_terms\": spec.get(\"forbidden_terms\") or spec.get(\"terms\"),\n            \"forbidden_patterns\": spec.get(\"forbidden_patterns\") or spec.get(\"patterns\"),\n        },\n    )\n\ndef _hedging_score(answer: str, spec: dict[str, Any]) -> float:\n    hedges = [str(h).lower() for h in (_as_list(spec.get(\"hedges\")) or list(_DEFAULT_HEDGES))]\n    answer_lower = answer.lower()\n    return 1.0 if any(hedge in answer_lower for hedge in hedges) else 0.0\n\ndef _criterion_score(prompt: str, answer: str, spec: dict[str, Any]) -> float:\n    ctype = spec.get(\"type\")\n    if ctype == \"regex\":\n        return _regex_score(answer, spec)\n    if ctype == \"keyword_presence\":\n        return _keyword_presence_score(prompt, answer, spec)\n    if ctype == \"format_compliance\":\n        return _format_compliance_score(answer, spec)\n    if ctype == \"exact_count\":\n        return _exact_count_score(answer, spec)\n    if ctype == \"constraint_satisfaction\":\n        return _constraint_satisfaction_score(answer, spec)\n    if ctype == \"negative_constraint\":\n        return _negative_constraint_score(answer, spec)\n    if ctype == \"hedging_calibration\":\n        return _hedging_score(answer, spec)\n    if ctype == \"citation_presence\":\n        return 1.0 if re.search(r\"\\[\\d+\\]\", answer) else 0.0\n    return 0.0\n\ndef evaluate_rubric(\n    prompt: str,\n    answer: str,\n    rubric_cfg: dict[str, Any],\n    *,\n    slices: list[str] | None = None,\n) -> dict[str, Any]:\n    if not _active(rubric_cfg, slices):\n        return {\"criteria\": {}, \"score\": None, \"skipped\": True}\n\n    weights = rubric_cfg.get(\"weights\", {})\n    results: dict[str, float] = {}\n    total = 0.0\n    weight_sum = 0.0\n    for name, spec in _criteria_items(rubric_cfg):\n        if not _active(spec, slices):\n            continue\n        score = _criterion_score(prompt, answer, spec)\n        results[name] = round(score, 4)\n        weight = float(spec.get(\"weight\", weights.get(name, 1.0)))\n        weight_sum += weight\n        total += weight * score\n\n    if not results or weight_sum == 0:\n        return {\"criteria\": results, \"score\": None, \"skipped\": True}\n    return {\"criteria\": results, \"score\": round(total / weight_sum, 4), \"skipped\": False}\n\ndef evaluate_instruction_format(\n    prompt: str, answer: str, rubric_cfg: dict[str, Any]\n) -> dict[str, Any]:\n\treturn evaluate_rubric(prompt, answer, rubric_cfg)\n\n__all__ = [\"evaluate_rubric\", \"evaluate_instruction_format\"] |
44 | src/runners/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
45 | src/runners/batch.py | {13675 1331 0.1487 5.0164} | 15153 | \"\"\"Batch runner (Phase 1)\n\nFeatures:\n- Load provider config YAML.\n- Read all JSONL prompts under a directory (one object per line with 'id' & 'prompt').\n- Deterministic seeding: seed = base_seed + index.\n- Write per-example trace JSONL & summary CSV under reports/<run_id>/.\n- Basic stats aggregation (latency p95 rough, avg tokens) for future gating.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse, os, sys, json, csv, time, math, glob\nfrom pathlib import Path\nfrom typing import List, Dict, Any\n\nimport yaml  # type: ignore\n\nfrom ..client.providers import (\n\tload_provider_from_config,\n\tProviderRequest,\n\tsave_trace_line,\n)\nfrom ..metrics.citations import score_citations\nfrom ..metrics.consistency import run_consistency\nfrom ..metrics.grounding import score_grounding\nfrom ..slices.label import label as slice_label\nfrom ..render.report import write_report  # type: ignore\nfrom ..rubrics.engine import evaluate_rubric\n\ndef _read_yaml(path: str) -> dict:\n\twith open(path, \"r\", encoding=\"utf-8\") as f:\n\t\treturn yaml.safe_load(f) or {}\n\n_RUBRIC_CACHE: dict | None = None\n\ndef _load_rubric_cfg() -> dict:\n\tglobal _RUBRIC_CACHE\n\tif _RUBRIC_CACHE is not None:\n\t\treturn _RUBRIC_CACHE\n\tpath = \"configs/rubrics.yaml\"\n\tif not os.path.exists(path):\n\t\t_RUBRIC_CACHE = {}\n\t\treturn _RUBRIC_CACHE\n\traw = _read_yaml(path)\n\t# normalize to dict keyed by rubric name\n\tout = {}\n\tfor item in raw.get(\"rubrics\", []) or []:\n\t\tnm = item.get(\"name\")\n\t\tif nm:\n\t\t\tout[nm] = item\n\t_RUBRIC_CACHE = out\n\treturn out\n\ndef _load_prompts(root: str) -> List[Dict[str, Any]]:\n\t\"\"\"Load JSONL prompt objects retaining all metadata fields.\n\n\tEach line becomes a dict with at least id & prompt keys preserved. Additional\n\tfields (e.g. reference_sources, slices) are passed through for metrics/slices.\n\t\"\"\"\n\tpaths = sorted(glob.glob(os.path.join(root, \"*.jsonl\")))\n\tprompts: List[Dict[str, Any]] = []\n\tfor p in paths:\n\t\twith open(p, \"r\", encoding=\"utf-8\") as f:\n\t\t\tfor line in f:\n\t\t\t\tline = line.strip()\n\t\t\t\tif not line:\n\t\t\t\t\tcontinue\n\t\t\t\tobj = json.loads(line)\n\t\t\t\tif \"id\" not in obj:\n\t\t\t\t\tobj[\"id\"] = f\"{Path(p).stem}:{len(prompts)}\"\n\t\t\t\t# normalize prompt field alias\n\t\t\t\tif not obj.get(\"prompt\") and obj.get(\"text\"):\n\t\t\t\t\tobj[\"prompt\"] = obj[\"text\"]\n\t\t\t\tprompts.append(obj)\n\treturn prompts\n\ndef _turn_label(role: str) -> str:\n\treturn {\"system\": \"System\", \"user\": \"User\", \"assistant\": \"Assistant\"}.get(role, role.title())\n\ndef _conversation_messages(ex: Dict[str, Any]) -> list[dict[str, str]]:\n\tmessages: list[dict[str, str]] = []\n\tif ex.get(\"system_prompt\"):\n\t\tmessages.append({\"role\": \"system\", \"content\": str(ex[\"system_prompt\"])})\n\tfor turn in ex.get(\"turns\") or []:\n\t\tif not isinstance(turn, dict):\n\t\t\tcontinue\n\t\tcontent = turn.get(\"content\")\n\t\tif content is None:\n\t\t\tcontinue\n\t\tmessages.append({\"role\": str(turn.get(\"role\", \"user\")), \"content\": str(content)})\n\treturn messages\n\ndef _render_conversation_prompt(ex: Dict[str, Any]) -> str:\n\tmessages = _conversation_messages(ex)\n\tif not messages:\n\t\treturn str(ex.get(\"prompt\", \"\"))\n\n\tlines = [\n\t\t\"Continue the following multi-turn conversation. Respond only to the final user turn.\",\n\t\t\"\",\n\t]\n\tfor msg in messages:\n\t\tlines.append(f\"{_turn_label(msg['role'])}: {msg['content']}\")\n\t\tlines.append(\"\")\n\treturn \"\\n\".join(lines).strip()\n\ndef _example_prompt(ex: Dict[str, Any]) -> str:\n\tif ex.get(\"turns\") or ex.get(\"system_prompt\"):\n\t\treturn _render_conversation_prompt(ex)\n\treturn str(ex.get(\"prompt\", \"\"))\n\ndef _example_metadata(ex: Dict[str, Any]) -> dict[str, Any] | None:\n\tmetadata: dict[str, Any] = {}\n\tmessages = _conversation_messages(ex)\n\tif messages:\n\t\tmetadata[\"messages\"] = messages\n\telif ex.get(\"system_prompt\"):\n\t\tmetadata[\"system\"] = str(ex[\"system_prompt\"])\n\tif ex.get(\"workflow_type\"):\n\t\tmetadata[\"workflow_type\"] = ex[\"workflow_type\"]\n\tif ex.get(\"expected_failure_mode\"):\n\t\tmetadata[\"expected_failure_mode\"] = ex[\"expected_failure_mode\"]\n\treturn metadata or None\n\ndef _example_rubrics(ex: Dict[str, Any]) -> dict[str, dict[str, Any]]:\n\tcriteria = ex.get(\"rubric_criteria\")\n\tif not criteria:\n\t\treturn {}\n\treturn {\n\t\t\"example_rubric\": {\n\t\t\t\"name\": \"example_rubric\",\n\t\t\t\"criteria\": criteria,\n\t\t}\n\t}\n\ndef _provider_model_tags(provider: Any) -> set[str]:\n\ttags = getattr(provider, \"kwargs\", {}).get(\"model_tags\") or []\n\treturn {str(tag) for tag in tags}\n\ndef _is_model_applicable(ex: Dict[str, Any], provider_tags: set[str]) -> bool:\n\tapplicability = ex.get(\"model_applicability\") or {}\n\tif not isinstance(applicability, dict):\n\t\treturn True\n\tinclude_tags = {str(tag) for tag in applicability.get(\"include_tags\") or []}\n\texclude_tags = {str(tag) for tag in applicability.get(\"exclude_tags\") or []}\n\tif include_tags and not (include_tags & provider_tags):\n\t\treturn False\n\tif exclude_tags and (exclude_tags & provider_tags):\n\t\treturn False\n\treturn True\n\ndef _ensure_run_dir(out_dir: str) -> str:\n\tos.makedirs(out_dir, exist_ok=True)\n\treturn out_dir\n\ndef _percentile(values: List[float], pct: float) -> float:\n\tif not values:\n\t\treturn 0.0\n\tk = (len(values) - 1) * (pct / 100.0)\n\tf = math.floor(k)\n\tc = math.ceil(k)\n\tif f == c:\n\t\treturn values[int(k)]\n\treturn values[f] * (c - k) + values[c] * (k - f)\n\ndef _add_value(totals: dict[str, float], counts: dict[str, int], key: str, value: Any) -> None:\n\tif value is None:\n\t\treturn\n\ttry:\n\t\tnumeric = float(value)\n\texcept (TypeError, ValueError):\n\t\treturn\n\ttotals[key] = totals.get(key, 0.0) + numeric\n\tcounts[key] = counts.get(key, 0) + 1\n\ndef run_batch(args: argparse.Namespace) -> int:\n\tcfg = _read_yaml(args.config)\n\tprovider = load_provider_from_config(cfg, override_name=args.provider, override_model=args.model)\n\n\tprompts = _load_prompts(args.prompts)\n\tif not prompts:\n\t\tprint(\"No prompts found\", file=sys.stderr)\n\t\treturn 1\n\tprovider_tags = _provider_model_tags(provider)\n\tskipped_for_model = [ex for ex in prompts if not _is_model_applicable(ex, provider_tags)]\n\tif skipped_for_model:\n\t\tprint(f\"Skipping {len(skipped_for_model)} prompt(s) due to model_applicability tags\", file=sys.stderr)\n\t\tprompts = [ex for ex in prompts if _is_model_applicable(ex, provider_tags)]\n\tif not prompts:\n\t\tprint(\"No prompts remain after model_applicability filtering\", file=sys.stderr)\n\t\treturn 1\n\n\trun_dir = _ensure_run_dir(args.out)\n\ttraces_path = os.path.join(run_dir, \"traces.jsonl\")\n\tcsv_path = os.path.join(run_dir, \"summary.csv\")\n\n\tlatencies: List[float] = []\n\ttotal_in_tokens = 0\n\ttotal_out_tokens = 0\n\tmetric_totals: dict[str, float] = {}\n\tmetric_counts: dict[str, int] = {}\n\trubric_totals: dict[str, float] = {}\n\trubric_counts: dict[str, int] = {}\n\n\tslice_aggs: dict[str, dict[str, Any]] = {}\n\n\twith open(csv_path, \"w\", newline=\"\", encoding=\"utf-8\") as csvfile:\n\t\twriter = csv.DictWriter(csvfile, fieldnames=[\"id\", \"model\", \"latency_ms\", \"input_tokens\", \"output_tokens\", \"cit_f1\", \"grounding_score\", \"consistency_similarity\", \"rubric_mean\", \"error\"])\n\t\twriter.writeheader()\n\t\tfor idx, ex in enumerate(prompts):\n\t\t\tseed = args.seed + idx\n\t\t\tprompt_text = _example_prompt(ex)\n\t\t\treq = ProviderRequest(\n\t\t\t\tprompt=prompt_text,\n\t\t\t\tmodel=provider.model,\n\t\t\t\ttemperature=args.temperature,\n\t\t\t\tmax_tokens=args.max_tokens,\n\t\t\t\tseed=seed,\n\t\t\t\tmetadata=_example_metadata(ex),\n\t\t\t)\n\t\t\tresp = provider.safe_generate(req)\n\t\t\tlatencies.append(resp.latency_ms)\n\t\t\ttotal_in_tokens += resp.input_tokens or 0\n\t\t\ttotal_out_tokens += resp.output_tokens or 0\n\t\t\t# metrics\n\t\t\texpected_sources = ex.get(\"reference_sources\") if isinstance(ex, dict) else None\n\t\t\tcit = score_citations(resp.text, expected_sources=expected_sources)\n\t\t\t_add_value(metric_totals, metric_counts, \"citations_f1\", cit.get(\"f1\"))\n\t\t\tcontext = ex.get(\"context\") or ex.get(\"contexts\") or ex.get(\"context_chunks\")\n\t\t\tgrounding = score_grounding(resp.text, context) if context else {\"score\": None, \"reason\": \"missing_context\"}\n\t\t\t_add_value(metric_totals, metric_counts, \"grounding_score\", grounding.get(\"score\"))\n\t\t\tif args.consistency_runs and args.consistency_runs > 1:\n\t\t\t\tconsistency = run_consistency(\n\t\t\t\t\tprompt_text,\n\t\t\t\t\tprovider,\n\t\t\t\t\tn_runs=args.consistency_runs,\n\t\t\t\t\ttemperature=args.consistency_temperature,\n\t\t\t\t\tbase_seed=seed,\n\t\t\t\t\tmax_tokens=args.max_tokens,\n\t\t\t\t)\n\t\t\telse:\n\t\t\t\tconsistency = {\"mean_similarity\": None, \"reason\": \"disabled\"}\n\t\t\t_add_value(metric_totals, metric_counts, \"consistency_similarity\", consistency.get(\"mean_similarity\"))\n\t\t\texplicit_slices = ex.get(\"slices\") or ex.get(\"slice_tags\")\n\t\t\tslices = slice_label(prompt_text, explicit_slices)\n\t\t\trubric_scores = {}\n\t\t\trubric_score_values: list[float] = []\n\t\t\tall_rubrics = {**_load_rubric_cfg(), **_example_rubrics(ex)}\n\t\t\tfor rubric_name, rubric_cfg in all_rubrics.items():\n\t\t\t\trubric_eval = evaluate_rubric(prompt_text, resp.text, rubric_cfg, slices=slices)\n\t\t\t\trubric_scores[rubric_name] = rubric_eval\n\t\t\t\trubric_score = rubric_eval.get(\"score\")\n\t\t\t\tif rubric_score is not None:\n\t\t\t\t\t_add_value(rubric_totals, rubric_counts, rubric_name, rubric_score)\n\t\t\t\t\trubric_score_values.append(float(rubric_score))\n\t\t\trubric_mean = (sum(rubric_score_values) / len(rubric_score_values)) if rubric_score_values else None\n\t\t\trecord = {\n\t\t\t\t\"id\": ex[\"id\"],\n\t\t\t\t\"prompt\": prompt_text,\n\t\t\t\t\"workflow_type\": ex.get(\"workflow_type\"),\n\t\t\t\t\"expected_failure_mode\": ex.get(\"expected_failure_mode\"),\n\t\t\t\t\"conversation\": ex.get(\"turns\"),\n\t\t\t\t\"response\": resp.text,\n\t\t\t\t\"model\": resp.model,\n\t\t\t\t\"latency_ms\": resp.latency_ms,\n\t\t\t\t\"input_tokens\": resp.input_tokens,\n\t\t\t\t\"output_tokens\": resp.output_tokens,\n\t\t\t\t\"metrics\": {\"citations\": cit, \"grounding\": grounding, \"consistency\": consistency},\n\t\t\t\t\"slices\": slices,\n\t\t\t\t\"rubric_scores\": rubric_scores,\n\t\t\t\t\"cost_usd\": resp.cost_usd,\n\t\t\t\t\"error\": resp.error,\n\t\t\t\t\"seed\": seed,\n\t\t\t\t\"ts\": time.time(),\n\t\t\t}\n\t\t\tsave_trace_line(traces_path, record)\n\t\t\twriter.writerow({\n\t\t\t\t\"id\": record[\"id\"],\n\t\t\t\t\"model\": record[\"model\"],\n\t\t\t\t\"latency_ms\": record[\"latency_ms\"],\n\t\t\t\t\"input_tokens\": record[\"input_tokens\"],\n\t\t\t\t\"output_tokens\": record[\"output_tokens\"],\n\t\t\t\t\"cit_f1\": cit[\"f1\"],\n\t\t\t\t\"grounding_score\": grounding.get(\"score\"),\n\t\t\t\t\"consistency_similarity\": consistency.get(\"mean_similarity\"),\n\t\t\t\t\"rubric_mean\": round(rubric_mean, 4) if rubric_mean is not None else None,\n\t\t\t\t\"error\": record[\"error\"],\n\t\t\t})\n\n\t\t\t# slice aggregation\n\t\t\tfor s in slices:\n\t\t\t\tagg = slice_aggs.setdefault(s, {\"count\": 0, \"total_latency\": 0.0, \"metric_totals\": {}, \"metric_counts\": {}, \"rubric_totals\": {}, \"rubric_counts\": {}})\n\t\t\t\tagg[\"count\"] += 1\n\t\t\t\tagg[\"total_latency\"] += resp.latency_ms\n\t\t\t\t_add_value(agg[\"metric_totals\"], agg[\"metric_counts\"], \"cit_f1\", cit.get(\"f1\"))\n\t\t\t\t_add_value(agg[\"metric_totals\"], agg[\"metric_counts\"], \"grounding_score\", grounding.get(\"score\"))\n\t\t\t\t_add_value(agg[\"metric_totals\"], agg[\"metric_counts\"], \"consistency_similarity\", consistency.get(\"mean_similarity\"))\n\t\t\t\tfor rubric_name, rubric_eval in rubric_scores.items():\n\t\t\t\t\t_add_value(agg[\"rubric_totals\"], agg[\"rubric_counts\"], f\"{rubric_name}_score\", rubric_eval.get(\"score\"))\n\n\t# aggregate stats\n\tlat_sorted = sorted(latencies)\n\tstats = {\n\t\t\"count\": len(latencies),\n\t\t\"latency_p50_ms\": _percentile(lat_sorted, 50),\n\t\t\"latency_p95_ms\": _percentile(lat_sorted, 95),\n\t\t\"avg_input_tokens\": (total_in_tokens / len(prompts)) if prompts else 0,\n\t\t\"avg_output_tokens\": (total_out_tokens / len(prompts)) if prompts else 0,\n\t\t\"model\": provider.model,\n\t\t\"provider\": getattr(provider, \"name\", \"unknown\"),\n\t}\n\tfor metric_name, total in sorted(metric_totals.items()):\n\t\tcount = metric_counts.get(metric_name, 0)\n\t\tif count:\n\t\t\tstats[f\"{metric_name}_mean\"] = round(total / count, 4)\n\tfor rubric_name, total in sorted(rubric_totals.items()):\n\t\tcount = rubric_counts.get(rubric_name, 0)\n\t\tif count:\n\t\t\tstats[f\"{rubric_name}_mean\"] = round(total / count, 4)\n\twith open(os.path.join(run_dir, \"run_stats.json\"), \"w\", encoding=\"utf-8\") as f:\n\t\tjson.dump(stats, f, indent=2)\n\t# write slice aggregates\n\tslice_out = {}\n\tfor s, a in slice_aggs.items():\n\t\tc = a[\"count\"] or 1\n\t\trow = {\n\t\t\t\"count\": a[\"count\"],\n\t\t\t\"avg_latency_ms\": round(a[\"total_latency\"] / c, 2),\n\t\t}\n\t\tfor metric_name, total in sorted(a[\"metric_totals\"].items()):\n\t\t\tcount = a[\"metric_counts\"].get(metric_name, 0)\n\t\t\tif count:\n\t\t\t\trow[f\"avg_{metric_name}\"] = round(total / count, 4)\n\t\tfor rubric_name, total in sorted(a[\"rubric_totals\"].items()):\n\t\t\tcount = a[\"rubric_counts\"].get(rubric_name, 0)\n\t\t\tif count:\n\t\t\t\trow[f\"avg_{rubric_name}\"] = round(total / count, 4)\n\t\tslice_out[s] = row\n\twith open(os.path.join(run_dir, \"slices.json\"), \"w\", encoding=\"utf-8\") as f:\n\t\tjson.dump(slice_out, f, indent=2)\n\t# write markdown report\n\ttry:\n\t\twrite_report(run_dir)\n\texcept Exception as e:  # pragma: no cover - non critical\n\t\tprint(f\"[warn] report generation failed: {e}\", file=sys.stderr)\n\tprint(f\"Run complete -> {run_dir}\")\n\treturn 0\n\ndef build_arg_parser() -> argparse.ArgumentParser:\n\tp = argparse.ArgumentParser(description=\"Batch prompt runner (Phase 1)\")\n\tp.add_argument(\"--config\", default=\"configs/providers.yaml\")\n\tp.add_argument(\"--prompts\", default=\"data/prompts\")\n\tp.add_argument(\"--out\", required=True, help=\"Output directory (run folder)\")\n\tp.add_argument(\"--provider\", help=\"Override provider name\")\n\tp.add_argument(\"--model\", help=\"Override model id\")\n\tp.add_argument(\"--seed\", type=int, default=13)\n\tp.add_argument(\"--temperature\", type=float, default=0.2)\n\tp.add_argument(\"--max-tokens\", type=int, default=512)\n\tp.add_argument(\"--consistency-runs\", type=int, default=1, help=\"Repeated generations per prompt; >1 enables consistency scoring\")\n\tp.add_argument(\"--consistency-temperature\", type=float, default=0.8)\n\treturn p\n\ndef main(argv: List[str] | None = None) -> int:\n\targs = build_arg_parser().parse_args(argv)\n\treturn run_batch(args)\n\nif __name__ == \"__main__\":  # pragma: no cover\n\traise SystemExit(main()) |
46 | src/runners/compare.py | {5186 473 0.2761 4.4474} | 5417 | \"\"\"Compare run against baseline applying gates.\n\nInputs:\n--baseline <dir>\n--candidate <dir>\n--gates configs/gates.yaml\n\nOutputs candidate_dir/gates_report.json with pass/fail per gate.\n\"\"\"\nfrom __future__ import annotations\nimport argparse, json, os, yaml\n\ndef _read_json(path: str):\n    with open(path, \"r\", encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef _read_yaml(path: str):\n    with open(path, \"r\", encoding=\"utf-8\") as f:\n        return yaml.safe_load(f) or {}\n\ndef evaluate_gates(gates_cfg: dict, baseline_dir: str, candidate_dir: str) -> dict:\n    gates = gates_cfg.get(\"gates\", []) or []\n    candidate_stats = _read_json(os.path.join(candidate_dir, \"run_stats.json\"))\n    baseline_stats = {}\n    if baseline_dir and os.path.exists(os.path.join(baseline_dir, 'run_stats.json')):\n        baseline_stats = _read_json(os.path.join(baseline_dir, 'run_stats.json'))\n    candidate_slices = {}\n    slices_path = os.path.join(candidate_dir, \"slices.json\")\n    if os.path.exists(slices_path):\n        candidate_slices = _read_json(slices_path)\n\n    global_metrics = dict(candidate_stats)\n    global_metrics.setdefault(\"latency_p95_ms\", candidate_stats.get(\"latency_p95_ms\"))\n    if candidate_slices:\n        cit_vals = [s.get(\"avg_cit_f1\") for s in candidate_slices.values() if s.get(\"avg_cit_f1\") is not None]\n        rubric_vals = [s.get(\"avg_instruction_format_score\") for s in candidate_slices.values() if s.get(\"avg_instruction_format_score\") is not None]\n        if cit_vals:\n            global_metrics[\"citations_f1_mean\"] = sum(cit_vals) / len(cit_vals)\n        if rubric_vals:\n            global_metrics[\"instruction_format_mean\"] = sum(rubric_vals) / len(rubric_vals)\n\n    flattened_slice = {}\n    for sname, data in candidate_slices.items():\n        for k, v in data.items():\n            flattened_slice[f\"slice.{sname}.{k}\"] = v\n\n    results = []\n    overall_pass = True\n    for gate in gates:\n        metric_path = gate.get(\"metric\") or gate.get(\"name\")\n        min_v = gate.get(\"min\")\n        max_v = gate.get(\"max\")\n        max_drop = gate.get(\"max_drop\")  # relative drop allowed (for higher-is-better metrics)\n        max_increase = gate.get(\"max_increase\")  # relative increase allowed (for lower-is-better metrics e.g. latency)\n        threshold = gate.get(\"threshold\")\n        gate_type = gate.get(\"type\")\n        if threshold is not None:\n            if gate_type == \"min\":\n                min_v = threshold\n            elif gate_type == \"max\":\n                max_v = threshold\n            elif gate_type == \"relative_drop\":\n                max_drop = threshold\n            elif gate_type == \"relative_increase\":\n                max_increase = threshold\n        value = None\n        if metric_path.startswith(\"global.\"):\n            value = global_metrics.get(metric_path.split('.',1)[1])\n        elif metric_path.startswith(\"slice.\"):\n            value = flattened_slice.get(metric_path)\n        else:\n            value = global_metrics.get(metric_path)\n        passed = True\n        if min_v is not None and (value is None or value < min_v):\n            passed = False\n        if max_v is not None and (value is None or value > max_v):\n            passed = False\n        # delta logic\n        if (max_drop is not None or max_increase is not None) and baseline_stats:\n            # only handle global.* for now\n            base_val = None\n            if metric_path.startswith('global.'):\n                base_key = metric_path.split('.',1)[1]\n                base_val = baseline_stats.get(base_key)\n            else:\n                base_val = baseline_stats.get(metric_path)\n            if base_val is not None and value is not None:\n                if max_drop is not None and base_val != 0:\n                    rel = (base_val - value) / base_val  # positive if drop\n                    if rel > max_drop:\n                        passed = False\n                if max_increase is not None and base_val != 0:\n                    rel_inc = (value - base_val) / base_val  # positive if increase\n                    if rel_inc > max_increase:\n                        passed = False\n        if not passed:\n            overall_pass = False\n        results.append({\n            \"gate\": gate.get(\"name\"),\n            \"metric\": metric_path,\n            \"value\": value,\n            \"min\": min_v,\n            \"max\": max_v,\n            \"max_drop\": max_drop,\n            \"max_increase\": max_increase,\n            \"pass\": passed,\n        })\n\n    report = {\"overall_pass\": overall_pass, \"gates\": results}\n    with open(os.path.join(candidate_dir, \"gates_report.json\"), \"w\", encoding=\"utf-8\") as f:\n        json.dump(report, f, indent=2)\n    return report\n\ndef main(argv=None):\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"--baseline\", required=True)\n    ap.add_argument(\"--candidate\", required=True)\n    ap.add_argument(\"--gates\", default=\"configs/gates.yaml\")\n    args = ap.parse_args(argv)\n    gates_cfg = _read_yaml(args.gates)\n    rep = evaluate_gates(gates_cfg, args.baseline, args.candidate)\n    print(json.dumps(rep, indent=2))\n    return 0 if rep[\"overall_pass\"] else 2\n\nif __name__ == \"__main__\":  # pragma: no cover\n    raise SystemExit(main()) |
47 | src/slices/__init__.py | {0 0 0.0000 0.0000} | 0 |  |
48 | src/slices/label.py | {2168 195 0.1218 5.2673} | 2352 | \"\"\"Slice labeling heuristics (Phase 2).\n\nRules (simple, fast, deterministic):\n- length buckets (short <80 chars, medium <180, else long)\n- underspecified / extremely_vague: presence of generic verbs without objects & very short text\n- needs_citations: keywords or explicit 'cite'/'citation'\n- constraint_heavy: contains 'exactly', 'no more than', pattern for 'under \\\\d+ words'\n\"\"\"\n\nfrom __future__ import annotations\n\nimport re\nfrom typing import List, Iterable, Optional\n\n_RE_UNDERSPEC = re.compile(r\"\\b(help|make|write|improve|design|plan|calculate|create)\\b\", re.I)\n_RE_CIT = re.compile(r\"\\b(cite|citation|sources?)\\b\", re.I)\n_RE_CONSTRAINT = re.compile(r\"\\b(exactly|no more than|under \\d+ words|at least \\d+)\\b\", re.I)\n_RE_FACTUAL = re.compile(r\"\\b(current|latest|when|what was|what is|according to|record|revenue|population|legal status|minimum wage)\\b\", re.I)\n_RE_PEDAGOGICAL = re.compile(r\"\\b(explain|teach|student|child|12-year-old|scaffold|learn|why|how does|how do)\\b\", re.I)\n_RE_RETRIEVAL = re.compile(r\"\\b(according to|provided context|source|sources|document|retrieved|cite|citation)\\b\", re.I)\n_RE_FORMAT = re.compile(r\"\\b(json|table|numbered list|bullet points?|single paragraph|q&a|headers?)\\b\", re.I)\n\ndef label(prompt: str, explicit: Optional[Iterable[str]] = None) -> List[str]:\n\ttags: List[str] = []\n\tif explicit:\n\t\tfor t in explicit:\n\t\t\tif t and t not in tags:\n\t\t\t\ttags.append(str(t))\n\tif not prompt:\n\t\treturn tags\n\ttxt = prompt.strip()\n\tL = len(txt)\n\tif L < 80:\n\t\ttags.append(\"length_short\")\n\telif L < 180:\n\t\ttags.append(\"length_medium\")\n\telse:\n\t\ttags.append(\"length_long\")\n\n\t# underspecified heuristics\n\tif _RE_UNDERSPEC.search(txt):\n\t\tif L < 45:\n\t\t\ttags.append(\"extremely_vague\")\n\t\telse:\n\t\t\ttags.append(\"underspecified\")\n\n\tif _RE_CIT.search(txt):\n\t\ttags.append(\"needs_citations\")\n\n\tif _RE_FACTUAL.search(txt):\n\t\ttags.append(\"factual_grounding_needed\")\n\n\tif _RE_PEDAGOGICAL.search(txt):\n\t\ttags.append(\"pedagogical\")\n\n\tif _RE_RETRIEVAL.search(txt):\n\t\ttags.append(\"retrieval_required\")\n\n\tif _RE_FORMAT.search(txt):\n\t\ttags.append(\"format_strict\")\n\n\tif _RE_CONSTRAINT.search(txt):\n\t\ttags.append(\"constraint_heavy\")\n\n\treturn tags\n\n__all__ = [\"label\"] |
49 | templates/probes/simple-integer.j2 | {209 34 0.1866 4.4817} | 216 | {# Integer payload template. Intentionally raw: do not JSON-escape content. #}\n{\n  {%- for line in payload_lines %}\n  \"{{ line.number }}\": {{ line.content }}{% if not loop.last %},{% endif %}\n  {%- endfor %}\n} |
50 | templates/probes/simple-whitespace.j2 | {214 34 0.1822 4.5238} | 223 | {# Whitespace payload template. Intentionally raw: do not JSON-escape content. #}\n{\n  {%- for line in payload_lines %}\n  \"{{ line.number }}\": \"{{ line.content }}\"{% if not loop.last %},{% endif %}\n  {%- endfor %}\n} |
51 | templates/table_specs/slice_summary.py | {697 94 0.1578 5.0508} | 748 | \"\"\"Generate dense slice-focused QA tables for anomaly scanning.\n\nPlaceholder producing static example until dynamic delta logic implemented.\n\"\"\"\nfrom __future__ import annotations\n\nEXAMPLE_TABLE = \"\"\"\n| Slice      | Citations F1 | Instr Score | P95 Latency | Gate Status |\n|------------|--------------|-------------|-------------|-------------|\n| needs_citations | 0.42 ↑0.05 | 0.61 → | 820ms ↑40ms | PASS |\n| length_short    | 0.38 ↓0.02 | 0.55 ↓0.03 | 760ms ↓10ms | PASS |\n| constraint_heavy| 0.31 → | 0.58 ↑0.01 | 910ms ↑60ms | PASS |\n\"\"\".strip()\n\ndef build_slice_gate_table(run_data=None):  # run_data placeholder\n    return EXAMPLE_TABLE\n\n__all__ = [\"build_slice_gate_table\", \"EXAMPLE_TABLE\"] |
