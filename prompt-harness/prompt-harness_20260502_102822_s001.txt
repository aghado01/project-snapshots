idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | .pytest_cache/CACHEDIR.TAG | {190 23 0.1158 4.8472} | 194 | Signature: 8a477f597d28d172789f06886806bc55\n# This file is a cache directory tag created by pytest.\n# For information about cache directory tags, see:\n#\thttps://bford.info/cachedir/spec.html |
1 | .pytest_cache/v/cache/lastfailed | {2 1 0.0000 1.0000} | 2 | {} |
2 | .pytest_cache/v/cache/nodeids | {4219 56 0.0386 4.4064} | 4382 | [\n  \"tests/unit/test_citations_metric.py::test_detection_and_scoring\",\n  \"tests/unit/test_citations_metric.py::test_no_answer\",\n  \"tests/unit/test_citations_metric.py::test_partial_match\",\n  \"tests/unit/test_compare_gates.py::test_evaluate_gates_min_max\",\n  \"tests/unit/test_compare_gates.py::test_evaluate_gates_relative_drop\",\n  \"tests/unit/test_consistency_metric.py::test_consistency_pairwise_similarity\",\n  \"tests/unit/test_consistency_metric.py::test_consistency_requires_multiple_responses\",\n  \"tests/unit/test_grounding_metric.py::test_grounding_missing_context_is_not_applicable\",\n  \"tests/unit/test_grounding_metric.py::test_grounding_scores_supported_claims\",\n  \"tests/unit/test_instruction_failure_workflow.py::test_instruction_failure_turns_render_as_transcript_and_messages\",\n  \"tests/unit/test_instruction_failure_workflow.py::test_model_applicability_excludes_abliterated_models\",\n  \"tests/unit/test_placeholders.py::test_placeholder\",\n  \"tests/unit/test_probe_payloads.py::test_integer_payload_round_trip\",\n  \"tests/unit/test_probe_payloads.py::test_minimal_pair_probe_shape\",\n  \"tests/unit/test_probe_payloads.py::test_template_rendering_can_intentionally_emit_invalid_json\",\n  \"tests/unit/test_probe_payloads.py::test_unicode_payload_keeps_actual_unicode\",\n  \"tests/unit/test_probe_payloads.py::test_unicode_probe_contains_zero_width_category\",\n  \"tests/unit/test_probe_payloads.py::test_whitespace_payload_analysis\",\n  \"tests/unit/test_render_report.py::test_environment_singleton\",\n  \"tests/unit/test_render_report.py::test_render_performance_smoke\",\n  \"tests/unit/test_render_report.py::test_report_missing_files_graceful\",\n  \"tests/unit/test_render_report.py::test_report_template_render\",\n  \"tests/unit/test_render_report.py::test_template_not_found_fallback\",\n  \"tests/unit/test_rubric_engine.py::test_exact_count_bullet_items\",\n  \"tests/unit/test_rubric_engine.py::test_format_compliance_active_by_slice\",\n  \"tests/unit/test_rubric_engine.py::test_instruction_failure_defaults_non_empty_regex\",\n  \"tests/unit/test_rubric_engine.py::test_rubric_scoring\",\n  \"tests/unit/test_rubric_engine.py::test_rubric_skips_when_not_applicable\",\n  \"tests/unit/test_rubric_engine.py::test_rubric_yaml_loading\",\n  \"tests/unit/test_significance_metric.py::test_bootstrap_mean_ci\",\n  \"tests/unit/test_significance_metric.py::test_compare_mean_delta\",\n  \"tests/unit/test_slice_label.py::test_basic_length_short\",\n  \"tests/unit/test_slice_label.py::test_constraint_heavy\",\n  \"tests/unit/test_slice_label.py::test_factual_grounding_needed\",\n  \"tests/unit/test_slice_label.py::test_needs_citations\",\n  \"tests/unit/test_slice_label.py::test_pedagogical_and_format_strict\",\n  \"tests/unit/test_template_engine.py::TestConvenienceFunctions::test_create_simple_engine\",\n  \"tests/unit/test_template_engine.py::TestConvenienceFunctions::test_create_string_engine\",\n  \"tests/unit/test_template_engine.py::TestConvenienceFunctions::test_create_string_engine_empty\",\n  \"tests/unit/test_template_engine.py::TestEdgeCases::test_empty_engine\",\n  \"tests/unit/test_template_engine.py::TestEdgeCases::test_invalid_template_syntax\",\n  \"tests/unit/test_template_engine.py::TestEdgeCases::test_template_not_found\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_add_template\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_add_template_without_dict_loader\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_autoescape_configuration\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_cache_configuration\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_filesystem_templates\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_list_templates\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_mixed_templates\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_multiple_template_dirs\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_render_string\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_safe_rendering\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_string_templates\",\n  \"tests/unit/test_template_engine.py::TestTemplateEngine::test_template_exists\"\n] |
3 | eval/__init__.py | {148 15 0.1081 4.3084} | 164 | \"\"\"Named evaluation suite definitions.\"\"\"\n\nfrom .suite import EvalSuite, get_suite, list_suites\n\n__all__ = [\"EvalSuite\", \"get_suite\", \"list_suites\"] |
4 | eval/suite.py | {3194 200 0.1838 4.6863} | 3404 | \"\"\"Evaluation suite registry.\n\nSuites are intentionally declarative. The batch runner can grow a `--suite`\nswitch later without hard-coding every metric/rubric combination into the runner.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\n\n@dataclass(frozen=True)\nclass EvalSuite:\n    name: str\n    description: str\n    prompt_globs: tuple[str, ...]\n    metrics: tuple[str, ...] = field(default_factory=tuple)\n    rubrics: tuple[str, ...] = field(default_factory=tuple)\n    slices: tuple[str, ...] = field(default_factory=tuple)\n\n_SUITES: dict[str, EvalSuite] = {\n    \"instruction\": EvalSuite(\n        name=\"instruction\",\n        description=\"Instruction adherence, format compliance, and constraint satisfaction.\",\n        prompt_globs=(\"data/prompts/instructions.jsonl\",),\n        metrics=(\"citations\",),\n        rubrics=(\"instruction_format\", \"constraint_satisfaction\", \"format_compliance\"),\n        slices=(\"constraint_heavy\", \"format_strict\"),\n    ),\n    \"citation\": EvalSuite(\n        name=\"citation\",\n        description=\"Citation marker precision/recall and source-use behavior.\",\n        prompt_globs=(\"data/prompts/citations.jsonl\",),\n        metrics=(\"citations\",),\n        rubrics=(\"constraint_satisfaction\",),\n        slices=(\"needs_citations\", \"factual_grounding_needed\"),\n    ),\n    \"ambiguity\": EvalSuite(\n        name=\"ambiguity\",\n        description=\"Clarification, assumption handling, and uncertainty calibration.\",\n        prompt_globs=(\"data/prompts/ambiguity.jsonl\",),\n        metrics=(\"consistency\",),\n        rubrics=(\"hedging_calibration\", \"constraint_satisfaction\"),\n        slices=(\"underspecified\", \"extremely_vague\", \"ambiguous\"),\n    ),\n    \"grounding\": EvalSuite(\n        name=\"grounding\",\n        description=\"Context-supported answer behavior for RAG-style tasks.\",\n        prompt_globs=(\"data/prompts/grounding.jsonl\",),\n        metrics=(\"grounding\", \"citations\"),\n        rubrics=(\"constraint_satisfaction\",),\n        slices=(\"retrieval_required\", \"factual_grounding_needed\"),\n    ),\n    \"instruction_failure\": EvalSuite(\n        name=\"instruction_failure\",\n        description=\"Mercury-inspired multi-turn instruction-following failure fixtures.\",\n        prompt_globs=(\"data/prompts/instruction_failure/*.jsonl\",),\n        metrics=(\"consistency\",),\n        rubrics=(\"instruction_failure_defaults\", \"example_rubric\"),\n        slices=(\"instruction_failure\", \"instruction_retention\", \"versioned_edit\", \"coherence_drift\"),\n    ),\n    \"broad_local\": EvalSuite(\n        name=\"broad_local\",\n        description=\"Wide local-model smoke suite across available prompt families.\",\n        prompt_globs=(\"data/prompts/*.jsonl\",),\n        metrics=(\"citations\", \"grounding\", \"consistency\"),\n        rubrics=(\"instruction_format\", \"constraint_satisfaction\", \"format_compliance\", \"hedging_calibration\"),\n    ),\n}\n\ndef list_suites() -> list[EvalSuite]:\n    return list(_SUITES.values())\n\ndef get_suite(name: str) -> EvalSuite:\n    try:\n        return _SUITES[name]\n    except KeyError as exc:\n        raise ValueError(f\"Unknown eval suite '{name}'. Available: {sorted(_SUITES)}\") from exc\n\n__all__ = [\"EvalSuite\", \"get_suite\", \"list_suites\"] |
5 | prompt-harness.code-workspace | {348 33 0.2328 4.4431} | 457 | {\n\t\"folders\": [\n\t\t{\n\t\t\t\"name\": \"prompt-harness\",\n\t\t\t\"path\": \".\"\n\t\t},\n\t\t{\n\t\t\t\"name\": \"context-probes\",\n\t\t\t\"path\": \"../context-probes\"\n\t\t},\n\t\t{\n\t\t\t\"name\": \"llama.cpp-build\",\n\t\t\t\"path\": \"../../../../llama.cpp-build\"\n\t\t},\n\t\t{\n\t\t\t\"name\": \"ps.core.bootstrap\",\n\t\t\t\"path\": \"../../ps.core.bootstrap\"\n\t\t},\n\t\t{\n\t\t\t\"path\": \"../../../project-snapshots\"\n\t\t}\n\t]\n} |
6 | scripts/_repo-init.ps1 | {3474 405 0.1580 5.0619} | 3817 | Param(\n  [string]$RepoName = \"conversational-quality-lab\"\n)\n\n# Resolve repo root\n$here = (Get-Location).Path\n$root = Join-Path $here $RepoName\n\n# Create root\nNew-Item -ItemType Directory -Path $root -Force | Out-Null\n\n# Directories to create\n$dirs = @(\n  \"configs\",\n  \"data/prompts\",\n  \"data/corpus\",\n  \"src\",\n  \"src/client\",\n  \"src/runners\",\n  \"src/metrics\",\n  \"src/rubrics\",\n  \"src/slices\",\n  \"src/render\",\n  \"eval\",\n  \"reports\",\n  \"templates/table_specs\",\n  \"scripts\",\n  \"tests/unit\"\n) | ForEach-Object { Join-Path $root $_ }\n\nforeach ($d in $dirs) {\n  New-Item -ItemType Directory -Path $d -Force | Out-Null\n}\n\n# Files to create (path => minimal content)\n$files = @{\n  (Join-Path $root \".gitignore\") = @\"\n.env\n__pycache__/\n*.pyc\n.reports_tmp/\nreports/\n.run/\n\"@;\n\n  (Join-Path $root \"README.md\") = @\"\n# Conversational Quality Lab\n\nEvaluation-first skeleton with batched runs, slice metrics, and regression gates.\n\"@;\n\n  (Join-Path $root \".env.example\") = @\"\n# Copy to .env and set keys\nPROVIDER_API_KEY=sk-xxx\n\"@;\n\n  (Join-Path $root \"requirements.txt\") = @\"\npydantic>=2\npython-dotenv\n\"@;\n\n  (Join-Path $root \"configs\\providers.yaml\") = @\"\ndefault_model: gpt-4o\nproviders:\n  - name: openai\n    model: gpt-4o\n\"@;\n\n  (Join-Path $root \"configs\\rubrics.yaml\") = @\"\nrubrics: []\n\"@;\n\n  (Join-Path $root \"configs\\gates.yaml\") = @\"\ngates:\n  - name: citations_f1\n    threshold: 0.70\n\"@;\n\n  (Join-Path $root \"data\\prompts\\README.md\") = \"Prompt sets with slice tags.\";\n  (Join-Path $root \"data\\prompts\\citations.jsonl\") = \"\";\n  (Join-Path $root \"data\\prompts\\instructions.jsonl\") = \"\";\n  (Join-Path $root \"data\\prompts\\ambiguity.jsonl\") = \"\";\n  (Join-Path $root \"data\\corpus\\README.md\") = \"Tiny retrieval corpus.\";\n\n  (Join-Path $root \"src\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\client\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\client\\providers.py\") = @\"\n# placeholder\nclass ProviderClient: ...\n\"@;\n\n  (Join-Path $root \"src\\runners\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\runners\\batch.py\") = @\"\n# placeholder\ndef main(): ...\nif __name__ == '__main__': main()\n\"@;\n\n  (Join-Path $root \"src\\metrics\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\metrics\\citations.py\") = @\"\n# placeholder\ndef score_citations(ans): return 0.0\n\"@;\n\n  (Join-Path $root \"src\\rubrics\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\slices\\__init__.py\") = \"\";\n  (Join-Path $root \"src\\slices\\label.py\") = @\"\n# placeholder\ndef label(prompt): return []\n\"@;\n\n  (Join-Path $root \"src\\render\\__init__.py\") = \"\";\n\n  (Join-Path $root \"eval\\__init__.py\") = \"\";\n\n  (Join-Path $root \"scripts\\run_baseline.ps1\") = @\"\npython -m src.runners.batch --config configs\\providers.yaml --prompts data\\prompts --out reports\\baseline\n\"@;\n\n  (Join-Path $root \"scripts\\compare_to_baseline.ps1\") = @\"\npython -m src.runners.batch --compare reports\\baseline --out reports\\candidate\n\"@;\n\n  (Join-Path $root \"templates\\table_specs\\README.md\") = \"Specs for dense QA tables.\";\n\n  (Join-Path $root \"tests\\unit\\test_placeholders.py\") = @\"\ndef test_placeholder():\n    assert True\n\"@;\n}\n\n# Create files with content\nforeach ($kvp in $files.GetEnumerator()) {\n  $path = $kvp.Key\n  $content = $kvp.Value\n  New-Item -ItemType File -Path $path -Force | Out-Null\n  if ($null -ne $content -and $content -ne \"\") {\n    Set-Content -Path $path -Value $content -Encoding UTF8\n  }\n}\n\n# Print a summary tree\nWrite-Host \"\"\nWrite-Host \"Created repo at: $root\"\nWrite-Host \"Top-level structure:\"\nGet-ChildItem -Path $root -Recurse | Select-Object FullName |
7 | scripts/background_run_worker.ps1 | {2421 225 0.2619 4.7000} | 2507 | param(\n    [Parameter(Mandatory = $true)]\n    [string]$RunDir,\n\n    [Parameter(Mandatory = $true)]\n    [string]$ScriptPath,\n\n    [Parameter(Mandatory = $true)]\n    [string]$ArgumentsPath\n)\n\n$ErrorActionPreference = 'Stop'\n\n$metadataPath = Join-Path $RunDir 'metadata.json'\n$statusPath = Join-Path $RunDir 'status.json'\n$metadata = Get-Content -LiteralPath $metadataPath -Raw | ConvertFrom-Json\n$startedUtc = (Get-Date).ToUniversalTime().ToString('o')\n\nfunction Write-RunStatus {\n    param(\n        [Parameter(Mandatory = $true)]\n        [string]$State,\n\n        [AllowNull()]\n        [int]$ExitCode,\n\n        [AllowNull()]\n        [string]$ErrorMessage\n    )\n\n    $finishedUtc = $null\n    if ($State -in @('completed', 'failed')) {\n        $finishedUtc = (Get-Date).ToUniversalTime().ToString('o')\n    }\n\n    $status = [ordered]@{\n        schema_version = 1\n        run_id         = $metadata.run_id\n        name           = $metadata.name\n        state          = $State\n        pid            = $PID\n        exit_code      = $ExitCode\n        created_utc    = $metadata.created_utc\n        started_utc    = $startedUtc\n        finished_utc   = $finishedUtc\n        status_utc     = (Get-Date).ToUniversalTime().ToString('o')\n        run_dir        = $RunDir\n        stdout_log     = $metadata.stdout_log\n        stderr_log     = $metadata.stderr_log\n        error          = $ErrorMessage\n    }\n\n    $tempPath = \"$statusPath.tmp\"\n    $status | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $tempPath -Encoding UTF8\n    Move-Item -LiteralPath $tempPath -Destination $statusPath -Force\n}\n\ntry {\n    Write-RunStatus -State 'running' -ExitCode $null -ErrorMessage $null\n\n    $argumentPayload = Get-Content -LiteralPath $ArgumentsPath -Raw | ConvertFrom-Json\n    $scriptArgs = @()\n    if ($null -ne $argumentPayload.arguments) {\n        $scriptArgs = @($argumentPayload.arguments | ForEach-Object { [string]$_ })\n    }\n\n    & $ScriptPath @scriptArgs\n    $exitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }\n\n    if ($exitCode -eq 0) {\n        Write-RunStatus -State 'completed' -ExitCode $exitCode -ErrorMessage $null\n    }\n    else {\n        Write-RunStatus -State 'failed' -ExitCode $exitCode -ErrorMessage \"Command exited with code $exitCode.\"\n    }\n\n    exit $exitCode\n}\ncatch {\n    Write-RunStatus -State 'failed' -ExitCode 1 -ErrorMessage $_.Exception.Message\n    Write-Error $_\n    exit 1\n} |
8 | scripts/compare_gates.ps1 | {454 51 0.1542 4.9130} | 474 | param(\n    [string]$Baseline = \"reports/baseline-latest\",\n    [string]$Candidate,\n    [string]$Gates = \"configs/gates.yaml\"\n)\nif (-not $Candidate) { throw \"--Candidate run directory required\" }\n& (Join-Path $PSScriptRoot 'python.ps1') -m src.runners.compare --baseline $Baseline --candidate $Candidate --gates $Gates\n$status = $LASTEXITCODE\nif ($status -ne 0)\n{\n    Write-Host \"Gate check failed with code $status\" -ForegroundColor Red\n    exit $status\n} |
9 | scripts/compare_to_baseline.ps1 | {131 12 0.0840 4.9113} | 134 | & (Join-Path $PSScriptRoot 'python.ps1') -m src.runners.batch --compare reports\\baseline --out reports\\candidate\nexit $LASTEXITCODE |
10 | scripts/ensure_venv.ps1 | {4451 447 0.2424 4.8123} | 4623 | param(\n    [string]$VenvPath = '.venv',\n    [switch]$Force,\n    [switch]$CheckOnly,\n    [switch]$UpgradePip\n)\n\n$ErrorActionPreference = 'Stop'\n\n$repoRoot = Split-Path -Parent $PSScriptRoot\n$venvRoot = if ([System.IO.Path]::IsPathRooted($VenvPath)) { $VenvPath } else { Join-Path $repoRoot $VenvPath }\n$requirementsPath = Join-Path $repoRoot 'requirements-dev.txt'\n$stampPath = Join-Path $venvRoot '.prompt-harness-env.json'\n\nfunction Get-VenvPythonPath\n{\n    param([string]$Root)\n\n    $candidates = @(\n        (Join-Path $Root 'Scripts\\python.exe'),\n        (Join-Path $Root 'bin/python')\n    )\n    foreach ($candidate in $candidates)\n    {\n        if (Test-Path -LiteralPath $candidate -PathType Leaf)\n        {\n            return $candidate\n        }\n    }\n    return $null\n}\n\nfunction Get-RequirementsFingerprint\n{\n    param([string]$Path)\n\n    if (-not (Test-Path -LiteralPath $Path -PathType Leaf))\n    {\n        throw \"Requirements file not found: $Path\"\n    }\n\n    $rootRequirements = Join-Path (Split-Path -Parent $Path) 'requirements.txt'\n    $items = [System.Collections.Generic.List[object]]::new()\n    foreach ($candidate in @($rootRequirements, $Path))\n    {\n        if (Test-Path -LiteralPath $candidate -PathType Leaf)\n        {\n            $hash = Get-FileHash -LiteralPath $candidate -Algorithm SHA256\n            $items.Add([pscustomobject]@{\n                    Path = (Resolve-Path -LiteralPath $candidate).Path\n                    Hash = $hash.Hash\n                })\n        }\n    }\n    return $items.ToArray()\n}\n\nfunction Test-StampCurrent\n{\n    param(\n        [string]$Path,\n        [object[]]$ExpectedRequirements\n    )\n\n    if (-not (Test-Path -LiteralPath $Path -PathType Leaf))\n    {\n        return $false\n    }\n    try\n    {\n        $stamp = Get-Content -LiteralPath $Path -Raw -Encoding UTF8 | ConvertFrom-Json\n    }\n    catch\n    {\n        return $false\n    }\n\n    $actual = @($stamp.Requirements)\n    if ($actual.Count -ne $ExpectedRequirements.Count)\n    {\n        return $false\n    }\n    for ($i = 0; $i -lt $ExpectedRequirements.Count; $i++)\n    {\n        if ($actual[$i].Path -ne $ExpectedRequirements[$i].Path -or $actual[$i].Hash -ne $ExpectedRequirements[$i].Hash)\n        {\n            return $false\n        }\n    }\n    return $true\n}\n\n$requirementsFingerprint = Get-RequirementsFingerprint -Path $requirementsPath\n$venvPython = Get-VenvPythonPath -Root $venvRoot\n\nif ($CheckOnly)\n{\n    if (-not $venvPython)\n    {\n        throw \"Local venv is missing. Run: pwsh scripts/ensure_venv.ps1\"\n    }\n    if (-not (Test-StampCurrent -Path $stampPath -ExpectedRequirements $requirementsFingerprint))\n    {\n        throw \"Local venv is out of date. Run: pwsh scripts/ensure_venv.ps1\"\n    }\n    Write-Host \"Local venv is ready: $venvRoot\" -ForegroundColor Green\n    exit 0\n}\n\nif (-not $venvPython)\n{\n    $bootstrapPython = & (Join-Path $PSScriptRoot 'resolve_python.ps1') -IgnoreVenv -Quiet\n    if (-not $bootstrapPython)\n    {\n        throw 'Python resolver returned no bootstrap interpreter path.'\n    }\n    Write-Host \"Creating local venv: $venvRoot\" -ForegroundColor Cyan\n    & $bootstrapPython -m venv $venvRoot\n    if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n    $venvPython = Get-VenvPythonPath -Root $venvRoot\n}\n\nif (-not $venvPython)\n{\n    throw \"Could not find venv Python after creation: $venvRoot\"\n}\n\nif ($Force -or -not (Test-StampCurrent -Path $stampPath -ExpectedRequirements $requirementsFingerprint))\n{\n    Write-Host \"Installing local development requirements into: $venvRoot\" -ForegroundColor Cyan\n    if ($UpgradePip)\n    {\n        & $venvPython -m pip install --upgrade pip\n        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n    }\n    & $venvPython -m pip install -r $requirementsPath\n    if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n\n    $pythonVersion = (& $venvPython --version) -join ' '\n    $stamp = [pscustomobject]@{\n        CreatedUtc    = (Get-Date).ToUniversalTime().ToString('o')\n        VenvPath      = (Resolve-Path -LiteralPath $venvRoot).Path\n        Python        = (Resolve-Path -LiteralPath $venvPython).Path\n        PythonVersion = $pythonVersion\n        Requirements  = $requirementsFingerprint\n    }\n    $stamp | ConvertTo-Json -Depth 6 | Set-Content -LiteralPath $stampPath -Encoding UTF8\n}\nelse\n{\n    Write-Host \"Local venv is already up to date: $venvRoot\" -ForegroundColor Green\n}\n\nWrite-Host \"Use via: pwsh scripts/python.ps1 --version\" -ForegroundColor DarkGray |
11 | scripts/generate_probe_payloads.ps1 | {960 89 0.1802 5.1373} | 1000 | param(\n    [ValidateSet('all', 'tokenizer_baseline', 'unicode_recon')]\n    [string]$Suite = 'all',\n    [string]$PayloadOut = 'data/probe_payloads',\n    [string]$PromptsOut = 'data/prompts/context_probes',\n    [ValidateSet('template', 'structured')]\n    [string]$RenderMode = 'template',\n    [int]$LineCount = 50,\n    [switch]$Compact,\n    [switch]$EnsureVenv\n)\n\n$ErrorActionPreference = 'Stop'\n\nif ($EnsureVenv)\n{\n    & (Join-Path $PSScriptRoot 'ensure_venv.ps1')\n    if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n}\nelse\n{\n    & (Join-Path $PSScriptRoot 'ensure_venv.ps1') -CheckOnly\n    if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n}\n\n$argsList = @(\n    '-m', 'src.probes.cli',\n    '--suite', $Suite,\n    '--payload-out', $PayloadOut,\n    '--prompts-out', $PromptsOut,\n    '--render-mode', $RenderMode,\n    '--line-count', $LineCount\n)\n\nif ($Compact)\n{\n    $argsList += '--compact'\n}\n\n& (Join-Path $PSScriptRoot 'python.ps1') @argsList\nexit $LASTEXITCODE |
12 | scripts/get_background_run.ps1 | {2157 226 0.2133 4.7183} | 2230 | param(\n    [string]$RunId,\n\n    [string]$RunRoot = 'reports/runs',\n\n    [int]$Tail = 40,\n\n    [switch]$Json\n)\n\n$ErrorActionPreference = 'Stop'\n\n$repoRoot = Split-Path -Parent $PSScriptRoot\n$runRootPath = if ([System.IO.Path]::IsPathRooted($RunRoot)) { $RunRoot } else { Join-Path $repoRoot $RunRoot }\n\nif (-not $RunId) {\n    $latest = Get-ChildItem -LiteralPath $runRootPath -Directory -ErrorAction Stop |\n        Sort-Object LastWriteTimeUtc -Descending |\n        Select-Object -First 1\n    if (-not $latest) { throw \"No background runs found under $runRootPath\" }\n    $RunId = $latest.Name\n}\n\n$runDir = Join-Path $runRootPath $RunId\n$metadataPath = Join-Path $runDir 'metadata.json'\n$statusPath = Join-Path $runDir 'status.json'\n\n$metadata = Get-Content -LiteralPath $metadataPath -Raw | ConvertFrom-Json\n$status = Get-Content -LiteralPath $statusPath -Raw | ConvertFrom-Json\n$observedState = $status.state\n\nif ($status.state -in @('launching', 'launched', 'running') -and $status.pid) {\n    $process = Get-Process -Id ([int]$status.pid) -ErrorAction SilentlyContinue\n    if (-not $process) {\n        $observedState = 'stale-or-finished'\n    }\n}\n\n$stdoutTail = @()\nif ($Tail -gt 0 -and (Test-Path -LiteralPath $metadata.stdout_log)) {\n    $stdoutTail = @(Get-Content -LiteralPath $metadata.stdout_log -Tail $Tail)\n}\n\n$stderrTail = @()\nif ($Tail -gt 0 -and (Test-Path -LiteralPath $metadata.stderr_log)) {\n    $stderrTail = @(Get-Content -LiteralPath $metadata.stderr_log -Tail $Tail)\n}\n\n$result = [ordered]@{\n    run_id         = $metadata.run_id\n    name           = $metadata.name\n    state          = $observedState\n    recorded_state = $status.state\n    pid            = $status.pid\n    exit_code      = $status.exit_code\n    created_utc    = $status.created_utc\n    started_utc    = $status.started_utc\n    finished_utc   = $status.finished_utc\n    run_dir        = $runDir\n    stdout_log     = $metadata.stdout_log\n    stderr_log     = $metadata.stderr_log\n    error          = $status.error\n    stdout_tail    = $stdoutTail\n    stderr_tail    = $stderrTail\n}\n\nif ($Json) {\n    $result | ConvertTo-Json -Depth 8\n    return\n}\n\n[pscustomobject]$result |
13 | scripts/install_deps.ps1 | {188 15 0.1223 5.0753} | 195 | param(\n    [switch]$Force,\n    [switch]$UpgradePip\n)\n\n$ErrorActionPreference = 'Stop'\n& (Join-Path $PSScriptRoot 'ensure_venv.ps1') -Force:$Force -UpgradePip:$UpgradePip\nexit $LASTEXITCODE |
14 | scripts/python.ps1 | {430 49 0.1674 5.1713} | 450 | param(\n    [Parameter(ValueFromRemainingArguments = $true)]\n    [string[]]$PythonArgs\n)\n\n$ErrorActionPreference = 'Stop'\n\n$python = & (Join-Path $PSScriptRoot 'resolve_python.ps1') -Quiet\nif (-not $python) {\n    throw 'Python resolver returned no interpreter path.'\n}\n\nif (-not $env:PYTHONNOUSERSITE) {\n    $env:PYTHONNOUSERSITE = '1'\n}\nif (-not $env:PIP_USER) {\n    $env:PIP_USER = '0'\n}\n\n& $python @PythonArgs\nexit $LASTEXITCODE |
15 | scripts/refresh_model_inventory.ps1 | {2247 212 0.2474 4.9563} | 2323 | param(\n    [string]$ModelRoot = $(if ($env:LLAMA_CPP_MODELS) { Join-Path $env:LLAMA_CPP_MODELS 'gguf' } else { 'D:\\models\\gguf' }),\n    [string]$OutputPath = 'data/model_inventory.json',\n    [switch]$IncludeSha256\n)\n\n$ErrorActionPreference = 'Stop'\n\nfunction Get-QuantizationFromName\n{\n    param([string]$Name)\n    $match = [regex]::Match($Name, '(?i)(IQ\\d+_[A-Z]+|Q\\d+_[A-Z]+(?:_[A-Z]+)?|F\\d+)')\n    if ($match.Success) { return $match.Value.ToUpperInvariant() }\n    return $null\n}\n\nfunction Get-ModelFamilyFromName\n{\n    param([string]$Name)\n    if ($Name -match '(?i)qwen') { return 'qwen' }\n    if ($Name -match '(?i)nemotron') { return 'nemotron' }\n    return 'unknown'\n}\n\nif (-not (Test-Path -LiteralPath $ModelRoot -PathType Container))\n{\n    throw \"Model root not found: $ModelRoot\"\n}\n\n$models = @(Get-ChildItem -LiteralPath $ModelRoot -Filter '*.gguf' -Recurse -File |\n    Sort-Object FullName |\n    ForEach-Object {\n        $sha256 = $null\n        if ($IncludeSha256)\n        {\n            $sha256 = (Get-FileHash -LiteralPath $_.FullName -Algorithm SHA256).Hash.ToLowerInvariant()\n        }\n        [ordered]@{\n            id            = $_.BaseName\n            name          = $_.Name\n            path          = $_.FullName\n            family        = Get-ModelFamilyFromName -Name $_.BaseName\n            quantization  = Get-QuantizationFromName -Name $_.BaseName\n            size_bytes    = $_.Length\n            modified_utc  = $_.LastWriteTimeUtc.ToString('o')\n            sha256        = $sha256\n            provider_hint = 'llama_cpp'\n            local_only    = $true\n        }\n    })\n\n$inventory = [ordered]@{\n    schema_version = 1\n    generated_utc  = (Get-Date).ToUniversalTime().ToString('o')\n    model_root     = (Resolve-Path -LiteralPath $ModelRoot).Path\n    count          = $models.Count\n    models         = $models\n}\n\n$parent = Split-Path -Parent $OutputPath\nif ($parent -and -not (Test-Path -LiteralPath $parent -PathType Container))\n{\n    New-Item -ItemType Directory -Path $parent -Force | Out-Null\n}\n\n$json = $inventory | ConvertTo-Json -Depth 8\nSet-Content -LiteralPath $OutputPath -Value $json -Encoding UTF8\nWrite-Host \"Wrote $OutputPath with $($models.Count) model(s) from $ModelRoot\" -ForegroundColor Cyan |
16 | scripts/resolve_python.ps1 | {2766 223 0.1949 5.2102} | 2903 | param(\n    [switch]$IgnoreVenv,\n    [switch]$Quiet\n)\n\n$ErrorActionPreference = 'Stop'\n\nfunction Test-UsablePythonPath\n{\n    param([string]$Path)\n    return ($Path -and (Test-Path -LiteralPath $Path -PathType Leaf))\n}\n\n$candidates = [System.Collections.Generic.List[string]]::new()\n\nif (-not $IgnoreVenv)\n{\n    $repoRoot = Split-Path -Parent $PSScriptRoot\n    $venvCandidates = @(\n        (Join-Path $repoRoot '.venv\\Scripts\\python.exe'),\n        (Join-Path $repoRoot '.venv/bin/python')\n    )\n    foreach ($path in $venvCandidates)\n    {\n        $candidates.Add($path)\n    }\n}\n\nif ($env:PROMPT_HARNESS_PYTHON)\n{\n    $candidates.Add($env:PROMPT_HARNESS_PYTHON)\n}\n\nif ($env:PYENV_ROOT -and $env:PYENV_VERSION)\n{\n    $candidates.Add((Join-Path $env:PYENV_ROOT (\"versions\\{0}\\python.exe\" -f $env:PYENV_VERSION)))\n}\n\nif ($env:PORTABLE_ROOT -and $env:PYTHON_VERSION)\n{\n    $candidates.Add((Join-Path $env:PORTABLE_ROOT (\"PyenvPython\\pyenv-win-{0}\\versions\\{0}\\python.exe\" -f $env:PYTHON_VERSION)))\n}\n\nif ($env:PORTABLE_ROOT)\n{\n    $portablePyenv = Join-Path $env:PORTABLE_ROOT 'PyenvPython'\n    if (Test-Path -LiteralPath $portablePyenv -PathType Container)\n    {\n        $found = @(Get-ChildItem -LiteralPath $portablePyenv -Filter 'python.exe' -Recurse -File -ErrorAction SilentlyContinue |\n            Where-Object { $_.FullName -match '\\\\versions\\\\[^\\\\]+\\\\python\\.exe$' } |\n            Sort-Object FullName |\n            Select-Object -ExpandProperty FullName)\n        foreach ($path in $found)\n        {\n            $candidates.Add($path)\n        }\n    }\n}\n\nforeach ($candidate in $candidates)\n{\n    if (Test-UsablePythonPath -Path $candidate)\n    {\n        if (-not $Quiet)\n        {\n            Write-Host \"Using Python: $candidate\" -ForegroundColor Cyan\n        }\n        Write-Output $candidate\n        exit 0\n    }\n}\n\n$fallbackCommands = @('python.exe', 'python', 'python3')\nforeach ($commandName in $fallbackCommands)\n{\n    $pythonCommand = Get-Command $commandName -ErrorAction SilentlyContinue\n    if ($pythonCommand -and $pythonCommand.Source -and $pythonCommand.Source -notlike '*\\Microsoft\\WindowsApps\\python.exe')\n    {\n        if (-not $Quiet)\n        {\n            Write-Host \"Using PATH Python: $($pythonCommand.Source)\" -ForegroundColor Cyan\n        }\n        Write-Output $pythonCommand.Source\n        exit 0\n    }\n}\n\nthrow @\"\nCould not resolve a usable Python interpreter.\n\nExpected one of:\n- `$env:PROMPT_HARNESS_PYTHON\n- `$env:PYENV_ROOT\\versions\\`$env:PYENV_VERSION\\python.exe\n- `$env:PORTABLE_ROOT\\PyenvPython\\pyenv-win-`$env:PYTHON_VERSION\\versions\\`$env:PYTHON_VERSION\\python.exe\n\nCurrent PYENV_ROOT=$env:PYENV_ROOT\nCurrent PYENV_VERSION=$env:PYENV_VERSION\nCurrent PORTABLE_ROOT=$env:PORTABLE_ROOT\nCurrent PYTHON_VERSION=$env:PYTHON_VERSION\n\"@ |
17 | scripts/run_baseline.ps1 | {382 42 0.1073 5.0875} | 393 | $ts = Get-Date -Format 'yyyyMMdd-HHmmss'\n$out = \"reports/baseline-$ts\"\n& (Join-Path $PSScriptRoot 'python.ps1') -m src.runners.batch --config configs\\providers.yaml --prompts data\\prompts --out $out\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\npwsh scripts/update_baseline_alias.ps1 -BaselineDir $out\nWrite-Host \"Baseline run saved to $out and aliased at reports/baseline-latest\" |
18 | scripts/run_tests.ps1 | {263 27 0.1483 4.9708} | 274 | param(\n    [Parameter(ValueFromRemainingArguments = $true)]\n    [string[]]$PytestArgs\n)\n\n$ErrorActionPreference = 'Stop'\nif (-not $PytestArgs)\n{\n    $PytestArgs = @('tests', '-v')\n}\n& (Join-Path $PSScriptRoot 'python.ps1') -m pytest @PytestArgs\nexit $LASTEXITCODE |
