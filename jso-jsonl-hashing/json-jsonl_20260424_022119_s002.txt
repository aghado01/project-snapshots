idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
2 | jso-utils/Bootstrap-Registries.ps1 | {10409 953 0.2162 4.9130} | 10921 | #Requires -Version 7.0\n\nparam(\n  [string[]]$DomainDirectories = @(),\n  [string]$WorkspaceDirectory = \".\",\n  [switch]$AutoDiscover,\n  [switch]$Force,\n  [switch]$ShowSummary,\n  [string]$SessionId = (New-Guid).ToString(),\n  [string]$MemoryContext = \"CorpusBootstrap\"\n)\n\nclass CorpusRegistryBootstrapper {\n  [string] $WorkspaceDirectory\n  [string] $RegistryFile\n  [hashtable] $Registry\n  [bool] $Force\n  [string] $SessionId\n\n  CorpusRegistryBootstrapper([string]$workspaceDirectory, [bool]$force = $false, [string]$sessionId = \"\") {\n    $this.WorkspaceDirectory = $workspaceDirectory\n    $this.RegistryFile = Join-Path $workspaceDirectory \"corpus-registry.json\"\n    $this.Force = $force\n    $this.SessionId = if ($sessionId) { $sessionId } else { (New-Guid).ToString() }\n    $this.LoadOrCreateRegistry()\n  }\n\n  [void] LoadOrCreateRegistry() {\n    if ((Test-Path $this.RegistryFile) -and -not $this.Force) {\n      try {\n        $this.Registry = Get-Content $this.RegistryFile -Raw | ConvertFrom-Json -AsHashtable\n        Write-Host \"📋 Loaded existing registry: $($this.Registry.domains.Count) domains\" -ForegroundColor Green\n        $this.WriteMemoryLog(\"Registry loaded successfully\", \"Session\", @{ DomainsCount = $this.Registry.domains.Count })\n      }\n      catch {\n        Write-Warning \"⚠️ Failed to load existing registry: $($_.Exception.Message)\"\n        $this.CreateNewRegistry()\n      }\n    } else {\n      $this.CreateNewRegistry()\n    }\n  }\n\n  [void] CreateNewRegistry() {\n    $this.Registry = @{\n      created = Get-Date -Format \"yyyy-MM-dd HH:mm:ss\"\n      updated = Get-Date -Format \"yyyy-MM-dd HH:mm:ss\"\n      version = \"1.0\"\n      workspace_directory = $this.WorkspaceDirectory\n      domains = @{}\n      global_stats = @{\n        total_documents = 0\n        total_stores = 0\n        total_size_mb = 0\n      }\n    }\n    Write-Host \"📋 Created new corpus registry\" -ForegroundColor Green\n    $this.WriteMemoryLog(\"New registry created\", \"Session\", @{ WorkspaceDirectory = $this.WorkspaceDirectory })\n  }\n\n  [void] WriteMemoryLog([string]$message, [string]$scope, [hashtable]$context = @{}) {\n    try {\n      $logEntry = @{\n        Timestamp = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'\n        Scope = $scope\n        Message = $message\n        Context = $context\n        SessionId = $this.SessionId\n        Component = \"CorpusRegistryBootstrapper\"\n      }\n\n      $memoryDir = \"memory\"\n      if (-not (Test-Path $memoryDir)) {\n        New-Item -ItemType Directory -Path $memoryDir -Force | Out-Null\n      }\n\n      $logPath = switch ($scope) {\n        'Session' { \"memory/session_$($this.SessionId.Substring(0,8)).log\" }\n        'Sequence' { \"memory/sequence_$(Get-Date -Format 'yyyyMMdd').log\" }\n        'Project' { \"memory/project_activity.log\" }\n        default { \"memory/bootstrap.log\" }\n      }\n\n      $logEntry | ConvertTo-Json -Compress | Out-File -FilePath $logPath -Append -Encoding UTF8\n    }\n    catch {\n\n      Write-Verbose \"Memory logging failed: $($_.Exception.Message)\"\n    }\n  }\n\n  [void] BootstrapFromDirectories([string[]]$domainDirectories) {\n    Write-Host \"`n🚀 Bootstrapping registry from specified directories...\" -ForegroundColor Cyan\n    $processedCount = 0\n\n    foreach ($dir in $domainDirectories) {\n      if (-not (Test-Path $dir)) {\n        Write-Warning \"⚠️ Not found: $dir\"\n        $this.WriteMemoryLog(\"Directory not found during bootstrap\", \"Sequence\", @{ Directory = $dir })\n        continue\n      }\n      $this.RegisterDomain($dir)\n      $processedCount++\n    }\n\n    $this.UpdateGlobalStats()\n    $this.SaveRegistry()\n    Write-Host \"✅ Registry bootstrap complete! Processed: $processedCount domains\" -ForegroundColor Green\n    $this.WriteMemoryLog(\"Bootstrap completed\", \"Project\", @{ ProcessedDomains = $processedCount })\n  }\n\n  [void] AutoDiscoverDomains() {\n    Write-Host \"`n🔍 Auto-discovering domain collections in: $($this.WorkspaceDirectory)\" -ForegroundColor Cyan\n\n    $domainDirs = Get-ChildItem -Path $this.WorkspaceDirectory -Directory | Where-Object {\n      $_.Name -like \"Collections-*\" -or (Test-Path (Join-Path $_.FullName \"domain-rules.ps1\"))\n    }\n\n    if ($domainDirs.Count -eq 0) {\n      Write-Warning \"⚠️ No domain collections found\"\n      $this.WriteMemoryLog(\"No domain collections found during auto-discovery\", \"Sequence\", @{ SearchPath = $this.WorkspaceDirectory })\n      return\n    }\n\n    Write-Host \"🎯 Found $($domainDirs.Count) potential domain collections\" -ForegroundColor Yellow\n    foreach ($d in $domainDirs) {\n      $this.RegisterDomain($d.FullName)\n    }\n\n    $this.UpdateGlobalStats()\n    $this.SaveRegistry()\n    Write-Host \"✅ Auto-discovery complete! Registered: $($domainDirs.Count) domains\" -ForegroundColor Green\n    $this.WriteMemoryLog(\"Auto-discovery completed\", \"Project\", @{ DiscoveredDomains = $domainDirs.Count })\n  }\n\n  [void] RegisterDomain([string]$domainDirectory) {\n    try {\n      $configFiles = Get-ChildItem -Path $domainDirectory -Filter \"*config.json\" -File -ErrorAction SilentlyContinue\n      $rulesFile = Join-Path $domainDirectory \"domain-rules.ps1\"\n\n      if ($configFiles.Count -eq 0) {\n        Write-Warning \"❌ No *config.json in $domainDirectory\"\n        $this.WriteMemoryLog(\"Missing config file\", \"Sequence\", @{ Directory = $domainDirectory })\n        return\n      }\n\n      if (-not (Test-Path $rulesFile)) {\n        Write-Warning \"❌ No domain-rules.ps1 in $domainDirectory\"\n        $this.WriteMemoryLog(\"Missing rules file\", \"Sequence\", @{ Directory = $domainDirectory })\n        return\n      }\n\n      $config = Get-Content $configFiles[0].FullName -Raw | ConvertFrom-Json -AsHashtable\n\n      $domainRulesScript = Get-Content $rulesFile -Raw\n      $domainRulesBlock = [ScriptBlock]::Create($domainRulesScript)\n      $domainRulesResult = & $domainRulesBlock\n\n      $DomainRules = $null\n      try {\n        $DomainRules = Invoke-Expression '$DomainRules' -ErrorAction SilentlyContinue\n        if (-not $DomainRules) {\n\n          if ($domainRulesScript -match '\\$DomainRules\\s*=\\s*@\\{([^}]+Domain\\s*=\\s*\"([^\"]+)\"[^}]*)\\}') {\n            $domainName = $matches[2]\n          } else {\n            Write-Warning \"❌ Could not extract domain name from $rulesFile\"\n            return\n          }\n        } else {\n          $domainName = $DomainRules.Domain\n        }\n      }\n      catch {\n\n        if ($domainRulesScript -match 'Domain\\s*=\\s*\"([^\"]+)\"') {\n          $domainName = $matches[1]\n        } else {\n          Write-Warning \"❌ Could not parse domain name from $rulesFile\"\n          return\n        }\n      }\n\n      if (-not $domainName) {\n        Write-Warning \"❌ Invalid domain rules in $rulesFile - no domain name found\"\n        return\n      }\n\n      $domainInfo = @{\n        name = $domainName\n        directory = $domainDirectory\n        config_file = $configFiles[0].FullName\n        rules_file = $rulesFile\n        registered_at = Get-Date -Format \"yyyy-MM-dd HH:mm:ss\"\n        last_scanned = Get-Date -Format \"yyyy-MM-dd HH:mm:ss\"\n        stores = @{}\n        stats = @{\n          total_documents = 0\n          total_stores = 0\n          total_size_mb = 0\n        }\n        paths = if ($config.Paths) { $config.Paths } else { @{} }\n        corpus_type = $domainName.ToLower()\n      }\n\n      $this.Registry.domains[$domainName] = $domainInfo\n      Write-Host \"✅ Registered: $domainName ($domainDirectory)\" -ForegroundColor Green\n      $this.WriteMemoryLog(\"Domain registered\", \"Sequence\", @{ Domain = $domainName; Directory = $domainDirectory })\n    }\n    catch {\n      Write-Warning \"❌ Failed to register domain in $($domainDirectory): $($_.Exception.Message)\"\n      $this.WriteMemoryLog(\"Domain registration failed\", \"Sequence\", @{ Directory = $domainDirectory; Error = $_.Exception.Message })\n    }\n  }\n\n  [void] UpdateGlobalStats() {\n    $totalDomains = $this.Registry.domains.Count\n    $this.Registry.global_stats.total_documents = 0\n    $this.Registry.global_stats.total_stores = $totalDomains\n    $this.Registry.global_stats.total_size_mb = 0\n    $this.Registry.updated = Get-Date -Format \"yyyy-MM-dd HH:mm:ss\"\n\n    $this.WriteMemoryLog(\"Global stats updated\", \"Session\", @{ TotalDomains = $totalDomains })\n  }\n\n  [void] SaveRegistry() {\n    try {\n      $this.Registry | ConvertTo-Json -Depth 10 | Set-Content $this.RegistryFile -Encoding UTF8\n      Write-Host \"💾 Registry saved: $($this.RegistryFile)\" -ForegroundColor Green\n      $this.WriteMemoryLog(\"Registry saved successfully\", \"Session\", @{ RegistryFile = $this.RegistryFile })\n    }\n    catch {\n      Write-Error \"❌ Failed to save registry: $($_.Exception.Message)\"\n      $this.WriteMemoryLog(\"Registry save failed\", \"Sequence\", @{ Error = $_.Exception.Message })\n    }\n  }\n\n  [void] ShowRegistrySummary() {\n    Write-Host \"`n📊 CORPUS WORKSPACE REGISTRY SUMMARY\" -ForegroundColor Cyan\n    Write-Host \"Workspace: $($this.WorkspaceDirectory)\" -ForegroundColor White\n    Write-Host \"Registry : $($this.RegistryFile)\" -ForegroundColor White\n    Write-Host \"Created  : $($this.Registry.created)\" -ForegroundColor White\n    Write-Host \"Updated  : $($this.Registry.updated)\" -ForegroundColor White\n    Write-Host \"Domains  : $($this.Registry.domains.Count)\" -ForegroundColor White\n\n    if ($this.Registry.domains.Count -gt 0) {\n      Write-Host \"`nRegistered Domains:\" -ForegroundColor Yellow\n      foreach ($k in $this.Registry.domains.Keys) {\n        $d = $this.Registry.domains[$k]\n        Write-Host \"  🎯 $k -> $($d.directory)\" -ForegroundColor Green\n      }\n    }\n\n    $this.WriteMemoryLog(\"Registry summary displayed\", \"Session\", @{ DomainsCount = $this.Registry.domains.Count })\n  }\n}\n\nWrite-Host \"🚀 Bootstrap Corpus Registries\" -ForegroundColor Cyan\n\ntry {\n  $bootstrapper = [CorpusRegistryBootstrapper]::new($WorkspaceDirectory, $Force.IsPresent, $SessionId)\n\n  if ($DomainDirectories.Count -gt 0) {\n    $bootstrapper.BootstrapFromDirectories($DomainDirectories)\n  } elseif ($AutoDiscover) {\n    $bootstrapper.AutoDiscoverDomains()\n  } else {\n    Write-Host \"ℹ️  Mode: Show existing registry or provide -DomainDirectories/-AutoDiscover\" -ForegroundColor Yellow\n    Write-Host \"   Use -AutoDiscover to scan for Collections-* directories\" -ForegroundColor Gray\n    Write-Host \"   Use -DomainDirectories @('path1','path2') to specify exact paths\" -ForegroundColor Gray\n  }\n\n  if ($ShowSummary) {\n    $bootstrapper.ShowRegistrySummary()\n  }\n}\ncatch {\n  Write-Error \"❌ Bootstrap operation failed: $($_.Exception.Message)\"\n  exit 1\n} |
