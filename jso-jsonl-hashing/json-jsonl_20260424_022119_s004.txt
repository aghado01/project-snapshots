idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
4 | jso-utils/Jso-Utils.JsoOrchestrator.psm1 | {18483 1447 0.3259 4.4951} | 19112 | using namespace System\nusing namespace System.Collections.Generic\nusing namespace System.Text\nusing namespace System.IO\n\n#Requires -Version 7.5.2\n\nSet-StrictMode -Version Latest\n\n$coreModule = Join-Path (Split-Path $PSScriptRoot) \"consolidation.v1\\Jso-Utils.Core.psm1\"\nif (Test-Path $coreModule) {\n    Import-Module $coreModule -Force -Global\n} else {\n\n    $coreModule = Join-Path (Split-Path $PSScriptRoot) \"Jso-Utils.Core.psm1\"\n    if (Test-Path $coreModule) {\n        Import-Module $coreModule -Force -Global\n    } else {\n        throw \"Required module Jso-Utils.Core.psm1 not found. Checked paths: $(Join-Path (Split-Path $PSScriptRoot) \"consolidation.v1\\Jso-Utils.Core.psm1\"), $(Join-Path (Split-Path $PSScriptRoot) \"Jso-Utils.Core.psm1\")\"\n    }\n}\n\nclass JsoEncoder {\n    [string]$Name\n    [hashtable]$Options\n    [scriptblock]$EncodeFunction\n\n    JsoEncoder([string]$Name, [hashtable]$Options, [scriptblock]$EncodeFunction) {\n        $this.Name = $Name\n        $this.Options = $Options\n        $this.EncodeFunction = $EncodeFunction\n    }\n\n    [object] Encode([object]$Content) {\n        return & $this.EncodeFunction.Invoke($Content, $this.Options)\n    }\n\n    static [JsoEncoder] CreateIdentity() {\n        return [JsoEncoder]::new('identity', @{}, { param($content, $options) return $content })\n    }\n\n    static [JsoEncoder] CreateMinifier() {\n        $minifyScript = {\n            param($content, $options)\n            if ($content -is [string]) {\n                return ($content -replace '\\s+', ' ').Trim()\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('minifier', @{}, $minifyScript)\n    }\n\n    static [JsoEncoder] CreateCanonical() {\n        $canonicalScript = {\n            param($content, $options)\n            if ($content -is [pscustomobject] -or $content -is [hashtable]) {\n                return ConvertTo-CanonicalJson -InputObject $content -Depth ($options.Depth ?? 100)\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('canonical', @{}, $canonicalScript)\n    }\n\n    static [JsoEncoder] CreateJsonPath([string]$Path) {\n        $jsonPathScript = {\n            param($content, $options)\n            $path = $options.Path\n            if ($content -is [pscustomobject] -or $content -is [hashtable]) {\n                return Select-JsonPath -InputObject $content -Path $path\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('jsonpath', @{Path = $Path}, $jsonPathScript)\n    }\n\n    static [JsoEncoder] CreateTabular([string[]]$Columns = $null, [switch]$AutoDiscover) {\n        $tabularScript = {\n            param($content, $options)\n            if ($content -is [array] -or $content -is [System.Collections.IEnumerable]) {\n                $params = @{ InputArray = $content }\n                if ($options.Columns) { $params.Columns = $options.Columns }\n                if ($options.AutoDiscover) { $params.AutoDiscover = $true }\n                return ConvertTo-JsonTable @params\n            }\n            return $content\n        }\n        $opts = @{}\n        if ($Columns) { $opts.Columns = $Columns }\n        if ($AutoDiscover) { $opts.AutoDiscover = $true }\n        return [JsoEncoder]::new('tabular', $opts, $tabularScript)\n    }\n}\n\nclass JsoSharding {\n    [int]$RecordsPerFile\n    [string]$NamingPattern  # Generic: \"{basename}_{index}{ext}\"\n    [int]$CurrentIndex\n    [int]$CurrentRecordCount\n\n    JsoSharding([int]$RecordsPerFile, [string]$NamingPattern) {\n        $this.RecordsPerFile = $RecordsPerFile\n        $this.NamingPattern = $NamingPattern\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n\n    [string] GetShardPath([string]$BasePath) {\n        $directory = [System.IO.Path]::GetDirectoryName($BasePath)\n        $nameWithoutExt = [System.IO.Path]::GetFileNameWithoutExtension($BasePath)\n        $extension = [System.IO.Path]::GetExtension($BasePath)\n\n        $fileName = $this.NamingPattern -f $nameWithoutExt, $this.CurrentIndex, $extension\n        return Join-Path $directory $fileName\n    }\n\n    [bool] ShouldCreateNewShard() {\n        return $this.CurrentRecordCount -ge $this.RecordsPerFile\n    }\n\n    [void] NextShard() {\n        $this.CurrentIndex++\n        $this.CurrentRecordCount = 0\n    }\n\n    [void] RecordWritten() {\n        $this.CurrentRecordCount++\n    }\n\n    [void] Reset() {\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n}\n\nclass JsoProcessor {\n    [JsoEncoder]$Encoder\n    [JsoSharding]$Sharding\n    [hashtable]$Config\n    [string]$OutputFormat\n    [object]$ValidationSchema\n    [object]$BloomFilter\n\n    JsoProcessor() {\n        $this.Encoder = [JsoEncoder]::CreateIdentity()\n        $this.Config = @{\n            JsonDepth = 100\n            StrictClean = $false\n            PostMinify = $false\n            SortOutput = $false\n            UseParallelism = $false\n            ParallelThrottle = 5\n            BufferSize = 1000\n            ShowProgress = $true\n            EnableDeduplication = $false\n            DeduplicationKeyPath = $null\n            FalsePositiveRate = 0.01\n        }\n        $this.OutputFormat = 'jsonl'\n        $this.ValidationSchema = $null\n        $this.BloomFilter = $null\n    }\n\n    [JsoProcessor] WithEncoder([JsoEncoder]$Encoder) {\n        $this.Encoder = $Encoder\n        return $this\n    }\n\n    [JsoProcessor] WithFormat([string]$Format) {\n        $this.OutputFormat = $Format.ToLowerInvariant()\n        return $this\n    }\n\n    [JsoProcessor] WithSharding([int]$RecordsPerFile, [string]$Pattern = '{0}_{1:D3}{2}') {\n        $this.Sharding = [JsoSharding]::new($RecordsPerFile, $Pattern)\n        return $this\n    }\n\n    [JsoProcessor] WithConfig([hashtable]$Config) {\n        foreach ($key in $Config.Keys) {\n            $this.Config[$key] = $Config[$key]\n        }\n        return $this\n    }\n\n    [JsoProcessor] WithValidation([object]$Schema) {\n        $this.ValidationSchema = $Schema\n        return $this\n    }\n\n    [JsoProcessor] EnableParallelism([int]$ThrottleLimit = 5) {\n        $this.Config.UseParallelism = $true\n        $this.Config.ParallelThrottle = $ThrottleLimit\n        return $this\n    }\n\n    [JsoProcessor] DisableParallelism() {\n        $this.Config.UseParallelism = $false\n        return $this\n    }\n\n    [JsoProcessor] EnableDeduplication([int]$ExpectedItems = 10000, [double]$FalsePositiveRate = 0.01, [string]$KeyPath = $null) {\n        $this.Config.EnableDeduplication = $true\n        $this.Config.FalsePositiveRate = $FalsePositiveRate\n        $this.Config.DeduplicationKeyPath = $KeyPath\n        $this.BloomFilter = New-BloomFilter -ExpectedItems $ExpectedItems -FalsePositiveRate $FalsePositiveRate\n        return $this\n    }\n\n    [JsoProcessor] DisableDeduplication() {\n        $this.Config.EnableDeduplication = $false\n        $this.BloomFilter = $null\n        return $this\n    }\n\n    [object[]] ProcessJsonlFile([string]$FilePath, [scriptblock]$Processor) {\n        if (-not (Test-Path $FilePath)) {\n            throw \"File not found: $FilePath\"\n        }\n\n        $items = Read-JsonFile -Path $FilePath -Kind JsonLines\n\n        if ($this.Config.ShowProgress) {\n            Write-Progress -Activity \"Processing JSONL file\" -Status \"Processing $($items.Count) records\"\n        }\n\n        if ($this.Config.UseParallelism -and $items.Count -gt 10) {\n            $results = $items | ForEach-Object -Parallel {\n                param($processor, $encoder, $config)\n                $item = $_\n\n                if ($encoder.Name -ne 'identity') {\n                    $item = $encoder.Encode($item)\n                }\n\n                return & $processor.Invoke($item, $config)\n            } -ArgumentList $Processor, $this.Encoder, $this.Config -ThrottleLimit $this.Config.ParallelThrottle\n\n            return $results\n        } else {\n\n            $results = @()\n            $processed = 0\n            foreach ($item in $items) {\n\n                if ($this.Encoder.Name -ne 'identity') {\n                    $item = $this.Encoder.Encode($item)\n                }\n\n                $results += & $Processor.Invoke($item, $this.Config)\n\n                $processed++\n                if ($this.Config.ShowProgress -and $processed % 100 -eq 0) {\n                    $percentComplete = ($processed / $items.Count) * 100\n                    Write-Progress -Activity \"Processing records\" -Status \"$processed of $($items.Count)\" -PercentComplete $percentComplete\n                }\n            }\n            return $results\n        }\n    }\n\n    [object] SplitJsonlFile([string]$InputPath, [string]$OutputBasePath) {\n        if (-not $this.Sharding) {\n            throw \"Sharding not configured. Call WithSharding() first.\"\n        }\n\n        $splitResult = Split-JsonlByRecordCount -InputPath $InputPath -OutputDirectory (Split-Path $OutputBasePath) -RecordsPerFile $this.Sharding.RecordsPerFile -NamingPattern $this.Sharding.NamingPattern\n\n        $genericResult = @()\n        for ($i = 0; $i -lt $splitResult.Files.Count; $i++) {\n            $genericResult += @{\n                Path = $splitResult.Files[$i]\n                RecordCount = $splitResult.RecordCounts[$i]\n                Index = $i + 1\n            }\n        }\n\n        return $genericResult\n    }\n\n    [object[]] ProcessJsonlFileWithDeduplication([string]$FilePath, [scriptblock]$Processor = $null) {\n        if (-not $this.Config.EnableDeduplication -or -not $this.BloomFilter) {\n            throw \"Deduplication not enabled. Call EnableDeduplication() first.\"\n        }\n\n        if (-not (Test-Path $FilePath)) {\n            throw \"File not found: $FilePath\"\n        }\n\n        $keyPath = $this.Config.DeduplicationKeyPath\n        $dedupItems = if ($keyPath) {\n            New-JsonlDeduplicatedStream -InputPath $FilePath -BloomFilter $this.BloomFilter -KeyPath $keyPath\n        } else {\n            New-JsonlDeduplicatedStream -InputPath $FilePath -BloomFilter $this.BloomFilter\n        }\n\n        $results = @()\n        $processed = 0\n\n        if ($this.Config.ShowProgress) {\n            Write-Progress -Activity \"Processing JSONL file with deduplication\" -Status \"Processing records from $FilePath\"\n        }\n\n        foreach ($item in $dedupItems) {\n\n            if ($this.Encoder.Name -ne 'identity') {\n                $item = $this.Encoder.Encode($item)\n            }\n\n            if ($Processor) {\n                $item = & $Processor.Invoke($item, $this.Config)\n            }\n\n            $results += $item\n            $processed++\n\n            if ($this.Config.ShowProgress -and $processed % 100 -eq 0) {\n                Write-Progress -Activity \"Processing records\" -Status \"Processed $processed unique records\"\n            }\n        }\n\n        if ($this.Config.ShowProgress) {\n            Write-Progress -Activity \"Processing JSONL file with deduplication\" -Completed\n        }\n\n        return $results\n    }\n\n    [hashtable] ProcessJsonFiles([string[]]$FilePaths, [scriptblock]$Processor) {\n        $results = @{\n            Processed = @()\n            Failed = @()\n            Stats = @{\n                TotalFiles = $FilePaths.Count\n                Successful = 0\n                Failed = 0\n                ProcessingTime = [System.Diagnostics.Stopwatch]::StartNew()\n            }\n        }\n\n        if ($this.Config.UseParallelism) {\n\n            $processResults = $FilePaths | ForEach-Object -Parallel {\n                param($processor, $encoder, $config)\n                $filePath = $_\n\n                try {\n\n                    $fileKind = Get-JsonKind -Path $filePath\n                    $content = switch ($fileKind) {\n                        'Json' { Read-JsonFile -Path $filePath }\n                        'JsonLines' { Read-JsonFile -Path $filePath -Kind JsonLines }\n                    }\n\n                    $processedContent = & $processor.Invoke($content, $config)\n\n                    return @{\n                        Path = $filePath\n                        Content = $processedContent\n                        Success = $true\n                        Error = $null\n                    }\n                } catch {\n                    return @{\n                        Path = $filePath\n                        Content = $null\n                        Success = $false\n                        Error = $_.Exception.Message\n                    }\n                }\n            } -ArgumentList $Processor, $this.Encoder, $this.Config -ThrottleLimit $this.Config.ParallelThrottle\n\n            foreach ($result in $processResults) {\n                if ($result.Success) {\n                    $results.Processed += $result\n                    $results.Stats.Successful++\n                } else {\n                    $results.Failed += $result\n                    $results.Stats.Failed++\n                }\n            }\n        } else {\n\n            foreach ($filePath in $FilePaths) {\n                try {\n                    $fileKind = Get-JsonKind -Path $filePath\n                    $content = switch ($fileKind) {\n                        'Json' { Read-JsonFile -Path $filePath }\n                        'JsonLines' { Read-JsonFile -Path $filePath -Kind JsonLines }\n                    }\n\n                    $processedContent = & $Processor.Invoke($content, $this.Config)\n\n                    $results.Processed += @{\n                        Path = $filePath\n                        Content = $processedContent\n                        Success = $true\n                        Error = $null\n                    }\n                    $results.Stats.Successful++\n                } catch {\n                    $results.Failed += @{\n                        Path = $filePath\n                        Content = $null\n                        Success = $false\n                        Error = $_.Exception.Message\n                    }\n                    $results.Stats.Failed++\n                }\n            }\n        }\n\n        $results.Stats.ProcessingTime.Stop()\n        if ($this.Config.ShowProgress) {\n            Write-Progress -Activity \"Batch Processing\" -Completed\n        }\n\n        return $results\n    }\n}\n\nfunction New-JsoProcessor {\n    \n    [CmdletBinding()]\n    param(\n        [hashtable]$Config = @{},\n        [switch]$EnableParallelism,\n        [int]$ParallelThrottle = 5\n    )\n\n    $processor = [JsoProcessor]::new()\n\n    if ($Config.Count -gt 0) {\n        $processor = $processor.WithConfig($Config)\n    }\n\n    if ($EnableParallelism) {\n        $processor = $processor.EnableParallelism($ParallelThrottle)\n    }\n\n    return $processor\n}\n\nfunction New-JsoEncoder {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [ValidateSet('Identity', 'Minifier', 'Canonical', 'JsonPath', 'Tabular', 'Custom')]\n        [string]$Type,\n        [hashtable]$Options = @{},\n        [scriptblock]$CustomScript,\n        [string]$JsonPath,\n        [string[]]$TableColumns,\n        [switch]$AutoDiscoverColumns\n    )\n\n    switch ($Type) {\n        'Identity' { return [JsoEncoder]::CreateIdentity() }\n        'Minifier' { return [JsoEncoder]::CreateMinifier() }\n        'Canonical' { return [JsoEncoder]::CreateCanonical() }\n        'JsonPath' {\n            if (-not $JsonPath) {\n                throw \"JsonPath parameter is required for JsonPath encoder type\"\n            }\n            return [JsoEncoder]::CreateJsonPath($JsonPath)\n        }\n        'Tabular' {\n            return [JsoEncoder]::CreateTabular($TableColumns, $AutoDiscoverColumns)\n        }\n        'Custom' {\n            if (-not $CustomScript) {\n                throw \"CustomScript is required for Custom encoder type\"\n            }\n            return [JsoEncoder]::new('custom', $Options, $CustomScript)\n        }\n    }\n}\n\nfunction Test-JsonlFileDuplicates {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath,\n        [string]$KeyPath = $null,\n        [double]$FalsePositiveRate = 0.01\n    )\n\n    $params = @{\n        Path = $FilePath\n        FalsePositiveRate = $FalsePositiveRate\n    }\n\n    if ($KeyPath) {\n        $params.KeyProperty = $KeyPath\n    }\n\n    return Test-JsonlDuplicates @params\n}\n\nfunction Invoke-JsonlDeduplication {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$InputPath,\n        [Parameter(Mandatory)]\n        [string]$OutputPath,\n        [string]$KeyPath = $null,\n        [double]$FalsePositiveRate = 0.01,\n        [switch]$ShowProgress\n    )\n\n    $params = @{\n        InputPath = $InputPath\n        OutputPath = $OutputPath\n        FalsePositiveRate = $FalsePositiveRate\n    }\n\n    if ($KeyPath) {\n        $params.KeyProperty = $KeyPath\n    }\n\n    New-JsonlDeduplicatedStream @params\n\n    $originalCount = (Get-Content $InputPath).Count\n    $deduplicatedCount = (Get-Content $OutputPath).Count\n\n    return @{\n        InputPath = $InputPath\n        OutputPath = $OutputPath\n        OriginalRecords = $originalCount\n        DeduplicatedRecords = $deduplicatedCount\n        DuplicatesRemoved = $originalCount - $deduplicatedCount\n    }\n}\n\nfunction Split-JsonlWithProcessor {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$InputPath,\n        [Parameter(Mandatory)]\n        [string]$OutputDirectory,\n        [Parameter(Mandatory)]\n        [int]$RecordsPerFile,\n        [JsoEncoder]$Encoder = $null,\n        [scriptblock]$Processor = $null,\n        [string]$NamingPattern = '{0}_{1:D3}{2}'\n    )\n\n    $jsoProcessor = New-JsoProcessor\n    $jsoProcessor = $jsoProcessor.WithSharding($RecordsPerFile, $NamingPattern)\n\n    if ($Encoder) {\n        $jsoProcessor = $jsoProcessor.WithEncoder($Encoder)\n    }\n\n    $splitResult = $jsoProcessor.SplitJsonlFile($InputPath, (Join-Path $OutputDirectory \"split\"))\n\n    if ($Processor) {\n        $processedFiles = @()\n        foreach ($fileInfo in $splitResult) {\n            $processedData = $jsoProcessor.ProcessJsonlFile($fileInfo.Path, $Processor)\n            $processedPath = $fileInfo.Path -replace '\\.jsonl$', '.processed.jsonl'\n            $processedData | ForEach-Object { ConvertTo-Json $_ -Compress } | Out-File -FilePath $processedPath -Encoding UTF8\n            $processedFiles += @{\n                OriginalPath = $fileInfo.Path\n                ProcessedPath = $processedPath\n                RecordCount = $fileInfo.RecordCount\n            }\n        }\n        return $processedFiles\n    }\n\n    return $splitResult\n}\n\nExport-ModuleMember -Function New-JsoProcessor, New-JsoEncoder, Test-JsonlFileDuplicates, Invoke-JsonlDeduplication, Split-JsonlWithProcessor |
5 | jso-utils/JsoPrettyPrinting.ps1 | {1988 167 0.2389 4.9198} | 2069 | \n\nfunction PrettyPrintJsonLines {\n    param (\n        [Parameter(Mandatory, Position = 0)]\n        [string]$Path,\n\n        [int]$Depth = 10\n    )\n\n    if (-not (Test-Path $Path)) {\n        Write-Warning \"File not found: $Path\"\n        return\n    }\n\n    Get-Content $Path | ForEach-Object {\n        try {\n            $_ | ConvertFrom-Json | ConvertTo-Json -Depth $Depth\n            \"\"\n        }\n        catch {\n            Write-Warning \"Invalid JSON line: $_\"\n        }\n    }\n}\nSet-Alias pprintjsl PrettyPrintJsonLines\n\nfunction PrettyJson {\n    param (\n        [Parameter(Mandatory)] [string]$Path,\n        [int]$Depth = 10\n    )\n\n    $fs = [System.IO.File]::OpenRead($Path)\n    $gz = New-Object System.IO.Compression.GZipStream($fs, [System.IO.Compression.CompressionMode]::Decompress)\n    $sr = New-Object System.IO.StreamReader($gz, [System.Text.Encoding]::UTF8)\n    $raw = $sr.ReadToEnd()\n    $sr.Close()\n    $gz.Close()\n    $fs.Close()\n\n    $raw | ConvertFrom-Json | ConvertTo-Json -Depth $Depth\n}\n\nfunction Get-PrettyJsonFromCompressed {\n    param (\n        [Parameter(Mandatory)] [string]$Path,\n        [int]$Depth = 10\n    )\n\n    $fs = [System.IO.File]::OpenRead($Path)\n    $gz = New-Object System.IO.Compression.GZipStream($fs, [System.IO.Compression.CompressionMode]::Decompress)\n    $sr = New-Object System.IO.StreamReader($gz, [System.Text.Encoding]::UTF8)\n    $raw = $sr.ReadToEnd()\n    $sr.Close()\n    $gz.Close()\n    $fs.Close()\n\n    $raw | ConvertFrom-Json | ConvertTo-Json -Depth $Depth\n}\n\nfunction Save-CompressedJson {\n    param (\n        [Parameter(Mandatory)] [string]$Path,\n        [Parameter(Mandatory)] [object]$Data\n    )\n\n    $json = $Data | ConvertTo-Json -Depth 10 -Compress\n    $bytes = [System.Text.Encoding]::UTF8.GetBytes($json)\n\n    $fs = [System.IO.File]::Create($Path)\n    $gz = New-Object System.IO.Compression.GZipStream($fs, [System.IO.Compression.CompressionMode]::Compress)\n    $gz.Write($bytes, 0, $bytes.Length)\n    $gz.Close()\n    $fs.Close()\n} |
6 | jso-utils/Logs-DumpJson.ps1 | {4034 340 0.2955 4.7537} | 4192 | function Get-ForensicSystemLogs {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory=$false)]\n        [TimeSpan]$TimeBack         = [TimeSpan]::FromHours(1),\n\n        [Parameter(Mandatory=$false)]\n        [string]$OutputFile         = \"$env:USERPROFILE\\Desktop\\SystemLogs_{0:yyyyMMdd_HHmm}.json\" -f (Get-Date),\n\n        [switch]$CheckRDPChannel,\n        [switch]$CheckSystemSetupChange,\n        [switch]$IncludeAllLogs      = $true,\n        [switch]$IncludeBonusDebugging,\n\n        [Parameter(Mandatory=$false)]\n        [string]$ModuleFile,\n\n        [switch]$ExportModule\n    )\n\n    $startTime = (Get-Date).Add(-$TimeBack)\n    $timestamp = Get-Date -Format \"yyyyMMdd_HHmm\"\n    $results   = [ordered]@{}\n\n    if ($CheckRDPChannel) {\n        try {\n            $rfxEvents = Get-WinEvent -FilterHashtable @{\n                LogName   = 'RemoteDesktopServices-RemoteFX-SessionLicensing-Debug'\n                StartTime = $startTime\n            } -ErrorAction Stop\n            $results.RDPChannel = $rfxEvents\n        } catch {\n            $results.RDPChannel = \"Error querying channel: $_\"\n        }\n    }\n\n    if ($CheckSystemSetupChange) {\n        $sysEvents   = Get-WinEvent -FilterHashtable @{\n            LogName   = 'System'\n            StartTime = $startTime\n        } | Where-Object { $_.Message -match 'event log|provider|RemoteFX' }\n\n        $setupEvents = Get-WinEvent -FilterHashtable @{\n            LogName   = 'Setup'\n            StartTime = $startTime\n        } | Where-Object { $_.Message -match 'RemoteFX' }\n\n        $results.SystemSetupChanges = $sysEvents + $setupEvents\n    }\n\n    if ($IncludeAllLogs) {\n        $allLogs = Get-WinEvent -ListLog * | Where-Object { $_.IsEnabled }\n        $logData = [ordered]@{}\n\n        foreach ($log in $allLogs) {\n            try {\n                $entries = Get-WinEvent -FilterHashtable @{\n                    LogName   = $log.LogName\n                    StartTime = $startTime\n                } -ErrorAction Stop |\n                Select-Object TimeCreated, ProviderName, Id, LevelDisplayName, Message\n\n                if ($entries) { $logData[$log.LogName] = $entries }\n            } catch {\n\n            }\n        }\n        $results.AllLogs = $logData\n    }\n\n    $results | ConvertTo-Json -Depth 6 | Out-File $OutputFile -Encoding utf8\n    Write-Host \"Log dump saved to $OutputFile\"\n\n    if ($IncludeBonusDebugging) {\n        Write-Host \"`nBonus Debugging Edge:\"\n\n        if (Get-Command procmon.exe -ErrorAction SilentlyContinue) {\n            Write-Host \"  • Launching ProcMon with audio filters...\"\n            Start-Process procmon.exe -ArgumentList `\n                '/Quiet','/Minimized',`\n                '/LoadConfig',\"C:\\Path\\To\\YourAudioFilter.PMC\" `\n                -NoNewWindow\n        } else {\n            Write-Host \"  • ProcMon not found. Get it from Sysinternals.\"\n        }\n\n        if (Get-Command wpr.exe -ErrorAction SilentlyContinue) {\n            $etlFile = \"$env:USERPROFILE\\Desktop\\AudioTrace_$timestamp.etl\"\n            Write-Host \"  • Starting ETW trace for audio stack...\"\n            Start-Process wpr -ArgumentList '-start','Audio','-filemode' -NoNewWindow\n            Write-Host \"  • To stop and save trace: wpr -stop $etlFile\"\n        } else {\n            Write-Host \"  • wpr.exe not found. Install Windows Performance Toolkit.\"\n        }\n    }\n\n    if ($ExportModule -and $ModuleFile) {\n        $def = $MyInvocation.MyCommand.Definition\n        Set-Content -Path $ModuleFile -Value $def -Encoding utf8\n        Add-Content   -Path $ModuleFile -Value \"`nExport-ModuleMember -Function Get-ForensicSystemLogs\"\n        Write-Host \"`nFunction exported into module: $ModuleFile\"\n    }\n}\n\n$extra = 'Microsoft-Windows-Audio/Operational',\n         'Microsoft-Windows-Audio/Diagnostic',\n         'Microsoft-Windows-DriverFrameworks-UserMode/Operational',\n         'Microsoft-Windows-Kernel-PnP/Configuration'\n\n$allLogs = (Get-WinEvent -ListLog * | Where-Object IsEnabled).LogName + $extra | Sort-Object -Unique\n\nforeach ($logName in $allLogs) {\n\n} |
