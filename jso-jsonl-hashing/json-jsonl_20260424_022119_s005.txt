idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
7 | jso-utils/MemorySystem.ps1 | {10443 1033 0.2349 4.9465} | 10703 | #Requires -Version 7.0\n\n$Script:MemoryRoot = \"$env:USERPROFILE/PortDenv/memory\"\n$Script:MemoryAggregatePath = Join-Path $Script:MemoryRoot 'memory_store.jsonl'\n$Script:MemoryValidScopes = @('global', 'project', 'session', 'sequence')\n$Script:MemoryValidTypes = @('patterns', 'insights', 'commands', 'state', 'workflow', 'activity', 'context', 'execution', 'error', 'diagnostic')\n\n$Script:MemoryWriterStarted = $false\n$Script:MemoryWriterJobName = 'MemoryWriter'\n$global:MemoryQueue = if (-not $global:MemoryQueue) { [System.Collections.Concurrent.ConcurrentQueue[psobject]]::new() } else { $global:MemoryQueue }\n\nfunction Initialize-MemorySystem {\n    if (-not (Test-Path $Script:MemoryRoot)) { New-Item -ItemType Directory -Path $Script:MemoryRoot -Force | Out-Null }\n    if (-not (Test-Path $Script:MemoryAggregatePath)) { New-Item -ItemType File -Path $Script:MemoryAggregatePath -Force | Out-Null }\n}\n\nfunction New-MemoryEntryId {\n    [CmdletBinding()] param(\n        [Parameter(Mandatory)][string]$Scope,\n        [Parameter(Mandatory)][string]$Type,\n        [string]$SessionId,\n        [string]$Project,\n        [string]$Workflow\n    )\n    $ts = (Get-Date).ToUniversalTime().ToString('yyyyMMddTHHmmssfffZ')\n    $guidFrag = (New-Guid).ToString().Substring(0, 6)\n    $context = if ($Scope -eq 'session' -and $SessionId) { $SessionId } elseif ($Scope -eq 'project' -and $Project) { $Project } elseif ($Scope -eq 'sequence' -and $Workflow) { $Workflow } else { $null }\n    if ($context) { return \"$( $Scope ):$( $Type ):$( $ts ):$( $context ):$( $guidFrag )\" }\n    return \"$( $Scope ):$( $Type ):$( $ts ):$( $guidFrag )\"\n}\n\nfunction New-MemoryRecord {\n    [CmdletBinding()] param(\n        [Parameter(Mandatory)][string]$Scope,\n        [Parameter(Mandatory)][string]$Type,\n        [Parameter(Mandatory)][string]$Message,\n        [hashtable]$Meta = @{},\n        [string]$SessionId,\n        [string]$Project,\n        [string]$Workflow\n    )\n    if ($Scope.ToLower() -notin $Script:MemoryValidScopes) { throw \"Invalid scope: $Scope\" }\n    if ($Type.ToLower() -notin $Script:MemoryValidTypes) { throw \"Invalid type: $Type\" }\n    $entryId = New-MemoryEntryId -Scope $Scope.ToLower() -Type $Type.ToLower() -SessionId $SessionId -Project $Project -Workflow $Workflow\n    $rec = [ordered]@{\n        id               = $entryId\n        ts               = (Get-Date).ToUniversalTime().ToString('o')\n        scope            = $Scope.ToLower()\n        type             = $Type.ToLower()\n        message          = $Message\n        session_id       = $SessionId\n        project          = $Project\n        workflow         = $Workflow\n        terminal_profile = ($env:TERMINAL_PROFILE ?? 'Unknown')\n        cwd              = (Get-Location).Path\n        pid              = $PID\n        meta             = $Meta\n    }\n    foreach ($k in @('session_id', 'project', 'workflow')) { if (-not $rec[$k]) { $rec.Remove($k) } }\n    if (-not $rec.meta.Count) { $rec.Remove('meta') }\n    return $rec\n}\n\nfunction Get-MemoryFilename {\n    param(\n        [Parameter(Mandatory)][string]$Scope,\n        [Parameter(Mandatory)][string]$Type,\n        [string]$SessionId,\n        [string]$Project,\n        [string]$Workflow,\n        [string]$Date = (Get-Date -Format 'yyyyMMdd')\n    )\n    switch ($Scope.ToLower()) {\n        'global' { return \"global_${Type}_${Date}.jsonl\" }\n        'project' { if (-not $Project) { $Project = (Split-Path -Leaf $PWD.Path) }; return \"project_${Project}_${Type}_${Date}.jsonl\" }\n        'session' { if (-not $SessionId) { $SessionId = ($env:COPILOT_SESSION_ID ?? (Get-Date -Format 'yyyyMMddHHmm')) }; return \"session_${Type}_${SessionId}_${Date}.jsonl\" }\n        'sequence' { if (-not $Workflow) { $Workflow = 'general' }; return \"sequence_${Type}_${Workflow}_${Date}.jsonl\" }\n        default { throw \"Unsupported scope: $Scope\" }\n    }\n}\n\nfunction Start-MemoryWriter {\n    [CmdletBinding()] param([int]$FlushIntervalMs = 250, [int]$BatchSize = 400)\n    Initialize-MemorySystem\n    if ($Script:MemoryWriterStarted -and (Get-Job -Name $Script:MemoryWriterJobName -State Running -ErrorAction SilentlyContinue)) { return }\n    $Script:MemoryWriterStarted = $true\n    Start-ThreadJob -Name $Script:MemoryWriterJobName -ScriptBlock {\n        param($root, $aggregatePath, $interval, $batchSize)\n        while ($true) {\n            $batch = @()\n            $item = $null\n            while ($global:MemoryQueue.TryDequeue([ref]$item)) {\n                $batch += $item\n                if ($batch.Count -ge $batchSize) { break }\n            }\n            if ($batch.Count -gt 0) {\n\n                $groups = $batch | Group-Object TargetPath\n                foreach ($g in $groups) {\n                    $lines = $g.Group.JsonLine -join [Environment]::NewLine\n                    Add-Content -Path $g.Name -Value $lines -Encoding UTF8\n                }\n\n                $aggLines = $batch | Where-Object { $_.Aggregate } | ForEach-Object { $_.JsonLine }\n                if ($aggLines.Count -gt 0) {\n                    $aggText = $aggLines -join [Environment]::NewLine\n                    Add-Content -Path $aggregatePath -Value $aggText -Encoding UTF8\n                }\n            }\n            Start-Sleep -Milliseconds $interval\n        }\n    } -ArgumentList $Script:MemoryRoot, $Script:MemoryAggregatePath, $FlushIntervalMs, $BatchSize | Out-Null\n}\n\nfunction Get-MemoryWriterStatus {\n    [CmdletBinding()] param()\n    $job = Get-Job -Name $Script:MemoryWriterJobName -ErrorAction SilentlyContinue\n    [pscustomobject]@{\n        Started    = $Script:MemoryWriterStarted\n        QueueCount = ($global:MemoryQueue.Count)\n        JobState   = $job.State\n        JobId      = $job.Id\n        Aggregate  = $Script:MemoryAggregatePath\n    }\n}\n\nfunction Flush-MemoryWriter {\n    [CmdletBinding()] param()\n\n    Initialize-MemorySystem\n    $drain = @()\n    $item = $null\n    while ($global:MemoryQueue.TryDequeue([ref]$item)) { $drain += $item }\n    if ($drain.Count -eq 0) { return 0 }\n    $groups = $drain | Group-Object TargetPath\n    foreach ($g in $groups) { Add-Content -Path $g.Name -Value ($g.Group.JsonLine -join [Environment]::NewLine) -Encoding UTF8 }\n    $aggLines = $drain | Where-Object { $_.Aggregate } | ForEach-Object { $_.JsonLine }\n    if ($aggLines.Count -gt 0) { Add-Content -Path $Script:MemoryAggregatePath -Value ($aggLines -join [Environment]::NewLine) -Encoding UTF8 }\n    return $drain.Count\n}\n\nfunction Stop-MemoryWriter {\n    [CmdletBinding()] param([switch]$Flush)\n    if ($Flush) { Flush-MemoryWriter | Out-Null }\n    $job = Get-Job -Name $Script:MemoryWriterJobName -ErrorAction SilentlyContinue\n    if ($job) { Stop-Job $job -Force; Remove-Job $job -Force }\n    $Script:MemoryWriterStarted = $false\n}\n\nfunction Write-MemoryLog {\n    \n    [CmdletBinding()] param(\n        [Parameter(Mandatory)][string]$Scope,\n        [Parameter(Mandatory)][string]$Type,\n        [Parameter(Mandatory)][string]$Message,\n        [hashtable]$Meta = @{},\n        [string]$SessionId,\n        [string]$Project,\n        [string]$Workflow,\n        [switch]$Aggregate,\n        [switch]$Async\n    )\n    Initialize-MemorySystem\n    $record = New-MemoryRecord -Scope $Scope -Type $Type -Message $Message -Meta $Meta -SessionId $SessionId -Project $Project -Workflow $Workflow\n    $filename = Get-MemoryFilename -Scope $Scope -Type $Type -SessionId $SessionId -Project $Project -Workflow $Workflow\n    $path = Join-Path $Script:MemoryRoot $filename\n    $jsonLine = $record | ConvertTo-Json -Compress -Depth 8\n\n    if ($Async) {\n        Start-MemoryWriter\n        $global:MemoryQueue.Enqueue([pscustomobject]@{ TargetPath = $path; JsonLine = $jsonLine; Aggregate = [bool]$Aggregate }) | Out-Null\n    }\n    else {\n        $jsonLine | Out-File -FilePath $path -Append -Encoding UTF8\n        if ($Aggregate) { Add-Content -Path $Script:MemoryAggregatePath -Value $jsonLine -Encoding UTF8 }\n    }\n    return $record.id\n}\n\nfunction Get-MemoryLog {\n\n    [CmdletBinding()] param(\n        [string]$Scope, [string]$Type, [string]$Project, [string]$SessionId, [string]$Workflow, [int]$Days = 7, [int]$Max = 200, [switch]$FromAggregate\n    )\n    Initialize-MemorySystem\n    if ($FromAggregate) {\n        if (-not (Test-Path $Script:MemoryAggregatePath)) { return @() }\n        $cutoff = (Get-Date).AddDays(-$Days)\n        $entries = foreach ($line in Get-Content -Path $Script:MemoryAggregatePath -ErrorAction SilentlyContinue) {\n            if (-not $line.Trim()) { continue }\n            try { $o = $line | ConvertFrom-Json } catch { continue }\n\n            $o\n        }\n        if ($Scope) { $entries = $entries | Where-Object { $_.scope -eq $Scope.ToLower() } }\n        if ($Type) { $entries = $entries | Where-Object { $_.type -eq $Type.ToLower() } }\n        if ($Project) { $entries = $entries | Where-Object { $_.project -eq $Project } }\n        if ($SessionId) { $entries = $entries | Where-Object { $_.session_id -eq $SessionId } }\n        if ($Workflow) { $entries = $entries | Where-Object { $_.workflow -eq $Workflow } }\n        return $entries | Sort-Object ts -Descending | Select-Object -First $Max\n    }\n    $cutoff = (Get-Date).AddDays(-$Days)\n    $files = Get-ChildItem $Script:MemoryRoot -File -Filter '*.jsonl' | Where-Object { $_.LastWriteTime -ge $cutoff }\n    if ($Scope) { $files = $files | Where-Object { $_.Name -like \"$($Scope.ToLower())_*\" } }\n    if ($Type) { $files = $files | Where-Object { $_.Name -like \"*_$($Type.ToLower())_*\" } }\n    if ($Project) { $files = $files | Where-Object { $_.Name -like \"*_${Project}_*\" } }\n    if ($SessionId) { $files = $files | Where-Object { $_.Name -like \"*_${SessionId}_*\" } }\n    if ($Workflow) { $files = $files | Where-Object { $_.Name -like \"*_${Workflow}_*\" } }\n    $entries = foreach ($f in ($files | Sort-Object LastWriteTime -Descending)) {\n        foreach ($line in Get-Content -Path $f.FullName -ErrorAction SilentlyContinue) {\n            if (-not $line.Trim()) { continue }\n            try { $obj = $line | ConvertFrom-Json } catch { continue }\n            $obj\n        }\n    }\n    $entries | Sort-Object ts -Descending | Select-Object -First $Max\n}\n\nfunction Invoke-MemoryWriterFlush {\n    [CmdletBinding()] param()\n    Flush-MemoryWriter\n}\n\nfunction Clear-MemoryLogs {\n    [CmdletBinding(SupportsShouldProcess)] param(\n        [int]$KeepDays = 14,\n        [switch]$Delete,\n        [switch]$Compress\n    )\n    Rotate-MemoryLogs -KeepDays $KeepDays -Delete:$Delete -Compress:$Compress\n} |
8 | mathdig/hashlib-new.ps1 | {19907 1742 0.3312 4.6903} | 20617 | class SimHash\n{\n\n    static [ulong[]] $Masks = [SimHash]::GenerateMasks()\n\n    static [System.Text.RegularExpressions.Regex] $WordRegex = [System.Text.RegularExpressions.Regex]::new(\n        '[\\p{L}\\p{Nd}_]+',\n        [System.Text.RegularExpressions.RegexOptions]::Compiled -bor\n        [System.Text.RegularExpressions.RegexOptions]::CultureInvariant -bor\n        [System.Text.RegularExpressions.RegexOptions]::NonBacktracking\n    )\n\n    hidden [System.Collections.Generic.Dictionary[string, double]] $IdfMap\n    hidden [double] $AvgDocLength\n    hidden [double] $K1\n    hidden [double] $B\n    hidden [double] $UnknownIdf\n    hidden [double] $MinWeight\n    hidden [double] $MaxIdf\n\n    static [ulong[]] GenerateMasks()\n    {\n        [ulong[]]$bitMasks = [ulong[]]::new(64)\n        for ([int]$i = 0; $i -lt 64; $i++)\n        {\n            $bitMasks[$i] = 1ul -shl $i\n        }\n        return $bitMasks\n    }\n\n    static [ulong] ComputeFnv1a64Utf8([string]$Value)\n    {\n        [ulong]$hash = 14695981039346656037ul\n        [ulong]$prime = 1099511628211ul\n        [byte[]]$bytes = [System.Text.Encoding]::UTF8.GetBytes($Value)\n\n        foreach ($b in $bytes)\n        {\n            $hash = $hash -bxor [ulong]$b\n\n            $hash = [ulong]([System.UInt128]$hash * [System.UInt128]$prime)\n        }\n\n        return $hash\n    }\n\n    SimHash([hashtable]$Options = @{})\n    {\n        $this.K1 = [double]($Options['k1'] ?? 1.5)\n        if ($this.K1 -lt 0.0)\n        {\n            throw \"k1 must be >= 0.0\"\n        }\n\n        $this.B = [double]($Options['b'] ?? 0.75)\n        if ($this.B -lt 0.0 -or $this.B -gt 1.0)\n        {\n            throw \"b must be in [0.0, 1.0]\"\n        }\n\n        $this.AvgDocLength = [double]($Options['AvgDocLength'] ?? 0.0)\n        $this.UnknownIdf = [double]($Options['UnknownIdf'] ?? 0.0)\n        $this.MinWeight = [double]($Options['MinWeight'] ?? 1e-6)\n        $this.MaxIdf = [double]($Options['MaxIdf'] ?? [double]::PositiveInfinity)\n\n        if ($this.MinWeight -lt 0.0)\n        {\n            throw \"MinWeight must be >= 0.0\"\n        }\n\n        [hashtable]$rawMap = $Options['IdfMap'] ?? @{}\n\n        $this.IdfMap = [System.Collections.Generic.Dictionary[string, double]]::new([System.StringComparer]::OrdinalIgnoreCase)\n\n        foreach ($key in $rawMap.Keys)\n        {\n            $this.IdfMap[[string]$key] = [double]$rawMap[$key]\n        }\n    }\n\n    [string] Compute([string]$Text)\n    {\n        if ([string]::IsNullOrWhiteSpace($Text)) { return '0' * 16 }\n\n        $tokenMatches = [SimHash]::WordRegex.Matches($Text)\n        [int]$docLength = $tokenMatches.Count\n        if ($docLength -eq 0) { return '0' * 16 }\n\n        [double]$avgLen = if ($this.AvgDocLength -le 0.0) { $docLength } else { $this.AvgDocLength }\n\n        [System.Collections.Generic.Dictionary[string, int]]$tfMap = [System.Collections.Generic.Dictionary[string, int]]::new([System.StringComparer]::Ordinal)\n\n        foreach ($match in $tokenMatches)\n        {\n            [string]$val = $match.Value.ToLowerInvariant()\n            [int]$count = 0\n\n            if ($tfMap.TryGetValue($val, [ref]$count))\n            {\n                $tfMap[$val] = $count + 1\n            }\n            else\n            {\n                $tfMap[$val] = 1\n            }\n        }\n\n        [double[]]$vector = [double[]]::new(64)\n\n        [double]$k1PlusOne = $this.K1 + 1.0\n        [double]$docLengthRatio = $this.B * ($docLength / $avgLen)\n        [double]$bm25DenomConst = $this.K1 * (1.0 - $this.B + $docLengthRatio)\n\n        [ulong[]]$localMasks = [SimHash]::Masks\n\n        foreach ($kvp in $tfMap.GetEnumerator())\n        {\n            [string]$token = $kvp.Key\n            [int]$tf = $kvp.Value\n\n            [double]$idf = $this.UnknownIdf\n            [double]$outIdf = 0.0\n            if ($this.IdfMap.TryGetValue($token, [ref]$outIdf))\n            {\n                $idf = $outIdf\n            }\n\n            if ($idf -lt 0.0)\n            {\n                $idf = 0.0\n            }\n            elseif ($idf -gt $this.MaxIdf)\n            {\n                $idf = $this.MaxIdf\n            }\n\n            [double]$weight = ($idf * $k1PlusOne * $tf) / ($bm25DenomConst + $tf)\n\n            if ($weight -le $this.MinWeight) { continue }\n\n            [ulong]$tokenHash = [SimHash]::ComputeFnv1a64Utf8($token)\n\n            for ([int]$i = 0; $i -lt 64; $i++)\n            {\n                if (($tokenHash -band $localMasks[$i]) -ne 0ul)\n                {\n                    $vector[$i] += $weight\n                }\n                else\n                {\n                    $vector[$i] -= $weight\n                }\n            }\n        }\n\n        [ulong]$hash = 0ul\n        for ([int]$i = 0; $i -lt 64; $i++)\n        {\n            if ($vector[$i] -gt 0.0)\n            {\n                $hash = $hash -bor $localMasks[$i]\n            }\n        }\n\n        return $hash.ToString(\"x16\").PadLeft(16, '0')\n    }\n}\n\nclass CTPH\n{\n    static [int] $MinBlockSize = 3\n    static [int] $MaxBlockSize = 96\n    static [ulong] $FnvPrime = 1099511628211ul\n    static [ulong] $FnvOffset = 14695981039346656037ul\n\n    static [string] Compute([string]$Content, [hashtable]$Options = @{})\n    {\n        if ([string]::IsNullOrEmpty($Content))\n        {\n            return '0::'\n        }\n\n        [int]$blockSize = if ($Options.ContainsKey('BlockSize')) { [int]$Options.BlockSize } else { [CTPH]::SelectBlockSize($Content.Length) }\n        if ($blockSize -lt [CTPH]::MinBlockSize)\n        {\n            $blockSize = [CTPH]::MinBlockSize\n        }\n\n        $chunks1 = [CTPH]::ComputeChunks($Content, $blockSize)\n        $hash1 = [CTPH]::HashSequence($chunks1)\n\n        $chunks2 = [CTPH]::ComputeChunks($Content, $blockSize * 2)\n        $hash2 = [CTPH]::HashSequence($chunks2)\n\n        return \"${blockSize}:${hash1}:${hash2}\"\n    }\n\n    static [int] SelectBlockSize([int]$ContentLength)\n    {\n        if ($ContentLength -lt 4096)\n        {\n            return [CTPH]::MinBlockSize\n        }\n\n        $candidate = [Math]::Max(\n            [CTPH]::MinBlockSize,\n            [int][Math]::Ceiling([Math]::Log($ContentLength / 64, 2))\n        )\n\n        return [Math]::Min($candidate, [CTPH]::MaxBlockSize)\n    }\n\n    static [System.Collections.Generic.List[ulong]] ComputeChunks([string]$Content, [int]$BlockSize)\n    {\n        if ($BlockSize -le 1)\n        {\n            throw 'BlockSize must be greater than 1'\n        }\n\n        $chunks = [System.Collections.Generic.List[ulong]]::new()\n        [ulong]$hash = [CTPH]::FnvOffset\n        [int]$windowStart = 0\n\n        for ([int]$i = 0; $i -lt $Content.Length; $i++)\n        {\n            [ulong]$charCode = [ulong][uint32][char]$Content[$i]\n            $hash = $hash -bxor $charCode\n            $hash = [ulong]([System.UInt128]$hash * [System.UInt128][CTPH]::FnvPrime)\n\n            if (($hash % [ulong]$BlockSize) -eq [ulong]($BlockSize - 1))\n            {\n                $chunkContent = $Content.Substring($windowStart, $i - $windowStart + 1)\n                $chunks.Add([CTPH]::HashChunk($chunkContent))\n                $windowStart = $i + 1\n            }\n        }\n\n        if ($windowStart -lt $Content.Length)\n        {\n            $chunks.Add([CTPH]::HashChunk($Content.Substring($windowStart)))\n        }\n\n        return $chunks\n    }\n\n    static [ulong] HashChunk([string]$Chunk)\n    {\n        [ulong]$hash = [CTPH]::FnvOffset\n\n        foreach ($char in $Chunk.ToCharArray())\n        {\n            $hash = $hash -bxor [ulong][uint32]$char\n            $hash = [ulong]([System.UInt128]$hash * [System.UInt128][CTPH]::FnvPrime)\n        }\n\n        return $hash\n    }\n\n    static [string] HashSequence([System.Collections.Generic.List[ulong]]$Chunks)\n    {\n        if ($Chunks.Count -eq 0)\n        {\n            return ''\n        }\n\n        [int]$maxChunks = [Math]::Min($Chunks.Count, 64)\n        [byte[]]$bytes = [byte[]]::new($maxChunks * 8)\n\n        for ([int]$i = 0; $i -lt $maxChunks; $i++)\n        {\n            $chunkBytes = [BitConverter]::GetBytes($Chunks[$i])\n            [Array]::Copy($chunkBytes, 0, $bytes, $i * 8, 8)\n        }\n\n        $base64 = [Convert]::ToBase64String($bytes)\n        return $base64.Substring(0, [Math]::Min(64, $base64.Length))\n    }\n\n    static [int] LevenshteinDistance([string]$A, [string]$B)\n    {\n        if ($null -eq $A) { $A = '' }\n        if ($null -eq $B) { $B = '' }\n\n        [int]$n = $A.Length\n        [int]$m = $B.Length\n\n        if ($n -eq 0) { return $m }\n        if ($m -eq 0) { return $n }\n\n        [int[]]$previous = [int[]]::new($m + 1)\n        [int[]]$current = [int[]]::new($m + 1)\n\n        for ([int]$j = 0; $j -le $m; $j++)\n        {\n            $previous[$j] = $j\n        }\n\n        for ([int]$i = 1; $i -le $n; $i++)\n        {\n            $current[0] = $i\n            $aChar = $A[$i - 1]\n\n            for ([int]$j = 1; $j -le $m; $j++)\n            {\n                [int]$cost = if ($aChar -eq $B[$j - 1]) { 0 } else { 1 }\n                [int]$deletion = $previous[$j] + 1\n                [int]$insertion = $current[$j - 1] + 1\n                [int]$substitution = $previous[$j - 1] + $cost\n                $current[$j] = [Math]::Min([Math]::Min($deletion, $insertion), $substitution)\n            }\n\n            $tmp = $previous\n            $previous = $current\n            $current = $tmp\n        }\n\n        return $previous[$m]\n    }\n\n    static [double] Compare([string]$Hash1, [string]$Hash2)\n    {\n        $parts1 = $Hash1 -split ':'\n        $parts2 = $Hash2 -split ':'\n\n        if ($parts1.Length -ne 3 -or $parts2.Length -ne 3)\n        {\n            throw 'Invalid CTPH hash format'\n        }\n\n        [int]$blockSize1 = [int]$parts1[0]\n        [int]$blockSize2 = [int]$parts2[0]\n        [double]$ratio = [Math]::Max($blockSize1, $blockSize2) / [Math]::Min($blockSize1, $blockSize2)\n        if ($ratio -gt 2.0)\n        {\n            return 0.0\n        }\n\n        $seq1 = if ($blockSize1 -eq $blockSize2) { $parts1[1] } else { $parts1[2] }\n        $seq2 = if ($blockSize1 -eq $blockSize2) { $parts2[1] } else { $parts2[2] }\n\n        [int]$distance = [CTPH]::LevenshteinDistance($seq1, $seq2)\n        [int]$maxLen = [Math]::Max($seq1.Length, $seq2.Length)\n        if ($maxLen -eq 0)\n        {\n            return 100.0\n        }\n\n        return [Math]::Max(0.0, (1.0 - ($distance / $maxLen)) * 100.0)\n    }\n}\n\nclass MinHash\n{\n    static [int] $DefaultNumHashes = 128\n    static [int] $DefaultShingleSize = 3\n\n    static [uint32[]] Compute([string]$Content, [hashtable]$Options = @{})\n    {\n        [int]$numHashes = if ($Options.ContainsKey('NumHashes')) { [int]$Options.NumHashes } else { [MinHash]::DefaultNumHashes }\n        [int]$shingleSize = if ($Options.ContainsKey('ShingleSize')) { [int]$Options.ShingleSize } else { [MinHash]::DefaultShingleSize }\n\n        if ($numHashes -le 0)\n        {\n            throw 'NumHashes must be greater than 0'\n        }\n        if ($shingleSize -le 0)\n        {\n            throw 'ShingleSize must be greater than 0'\n        }\n\n        $shingles = [MinHash]::GenerateShingles($Content, $shingleSize)\n        if ($shingles.Count -eq 0)\n        {\n            return [uint32[]]::new($numHashes)\n        }\n\n        [uint32[]]$signature = [uint32[]]::new($numHashes)\n        for ([int]$i = 0; $i -lt $numHashes; $i++)\n        {\n            $signature[$i] = [uint32]::MaxValue\n        }\n\n        foreach ($shingle in $shingles)\n        {\n            for ([int]$i = 0; $i -lt $numHashes; $i++)\n            {\n                $hash = [MinHash]::HashWithSeed($shingle, $i)\n                $signature[$i] = [Math]::Min($signature[$i], $hash)\n            }\n        }\n\n        return $signature\n    }\n\n    static [System.Collections.Generic.HashSet[string]] GenerateShingles([string]$Content, [int]$Size)\n    {\n        $shingles = [System.Collections.Generic.HashSet[string]]::new([System.StringComparer]::Ordinal)\n        if ([string]::IsNullOrEmpty($Content) -or $Content.Length -lt $Size)\n        {\n            return $shingles\n        }\n\n        for ([int]$i = 0; $i -le $Content.Length - $Size; $i++)\n        {\n            $null = $shingles.Add($Content.Substring($i, $Size))\n        }\n\n        return $shingles\n    }\n\n    static [uint32] HashWithSeed([string]$Value, [int]$Seed)\n    {\n        [uint64]$hash = [uint64](2166136261 + $Seed)\n        [uint64]$prime = 16777619ul\n        [byte[]]$bytes = [System.Text.Encoding]::UTF8.GetBytes($Value)\n\n        foreach ($b in $bytes)\n        {\n            $hash = $hash -bxor [uint64]$b\n            $hash = ($hash * $prime) -band 0xFFFFFFFFul\n        }\n\n        return [uint32]$hash\n    }\n\n    static [double] EstimateJaccard([uint32[]]$Sig1, [uint32[]]$Sig2)\n    {\n        if ($Sig1.Length -ne $Sig2.Length)\n        {\n            throw 'Signature lengths must match'\n        }\n\n        [int]$matchCount = 0\n        for ([int]$i = 0; $i -lt $Sig1.Length; $i++)\n        {\n            if ($Sig1[$i] -eq $Sig2[$i])\n            {\n                $matchCount++\n            }\n        }\n\n        return [double]$matchCount / $Sig1.Length\n    }\n}\n\nclass TLSH\n{\n    static [int] $WindowSize = 5\n    static [int] $BucketCount = 256\n\n    static [string] Compute([string]$Content)\n    {\n        if ([string]::IsNullOrEmpty($Content) -or $Content.Length -lt 50)\n        {\n            return ''\n        }\n\n        $buckets = [TLSH]::ComputeBuckets($Content)\n        $quartiles = [TLSH]::ComputeQuartiles($buckets)\n        $body = [TLSH]::BuildBody($buckets, $quartiles)\n        $checksum = [TLSH]::ComputeChecksum($Content)\n        $lenCode = [TLSH]::EncodeLength($Content.Length)\n\n        return \"T1${checksum}${lenCode}${body}\"\n    }\n\n    static [int[]] ComputeBuckets([string]$Content)\n    {\n        [int[]]$buckets = [int[]]::new([TLSH]::BucketCount)\n\n        for ([int]$i = 0; $i -le $Content.Length - [TLSH]::WindowSize; $i++)\n        {\n            $window = $Content.Substring($i, [TLSH]::WindowSize)\n            [byte]$hash = [TLSH]::ComputePearson($window)\n            $buckets[[int]$hash % [TLSH]::BucketCount]++\n        }\n\n        return $buckets\n    }\n\n    static [byte] ComputePearson([string]$Data)\n    {\n        [byte]$hash = 0\n        foreach ($char in $Data.ToCharArray())\n        {\n            [byte]$charByte = [byte]([int]$char -band 0xFF)\n            $hash = [byte]((((([int]$hash) -bxor ([int]$charByte)) * 31) -band 0xFF))\n        }\n\n        return $hash\n    }\n\n    static [int[]] ComputeQuartiles([int[]]$Buckets)\n    {\n        [int[]]$sorted = [int[]]::new($Buckets.Length)\n        [Array]::Copy($Buckets, $sorted, $Buckets.Length)\n        [Array]::Sort($sorted)\n\n        [int]$q1 = $sorted[[int]($sorted.Length * 0.25)]\n        [int]$q2 = $sorted[[int]($sorted.Length * 0.50)]\n        [int]$q3 = $sorted[[int]($sorted.Length * 0.75)]\n\n        return @($q1, $q2, $q3)\n    }\n\n    static [string] BuildBody([int[]]$Buckets, [int[]]$Quartiles)\n    {\n        $bits = [System.Collections.BitArray]::new([TLSH]::BucketCount * 2)\n\n        for ([int]$i = 0; $i -lt $Buckets.Length; $i++)\n        {\n            [int]$value = $Buckets[$i]\n            [int]$code = if ($value -le $Quartiles[0]) { 0 }\n            elseif ($value -le $Quartiles[1]) { 1 }\n            elseif ($value -le $Quartiles[2]) { 2 }\n            else { 3 }\n\n            $bits[$i * 2] = ($code -band 1) -ne 0\n            $bits[$i * 2 + 1] = ($code -band 2) -ne 0\n        }\n\n        [byte[]]$bytes = [byte[]]::new(([TLSH]::BucketCount * 2) / 8)\n        $bits.CopyTo($bytes, 0)\n        return [Convert]::ToHexString($bytes).ToLowerInvariant()\n    }\n\n    static [string] ComputeChecksum([string]$Content)\n    {\n        [byte[]]$bytes = [System.Text.Encoding]::UTF8.GetBytes($Content)\n        [int]$sum = 0\n        foreach ($b in $bytes)\n        {\n            $sum += $b\n        }\n\n        return ($sum % 256).ToString('x2')\n    }\n\n    static [string] EncodeLength([int]$Length)\n    {\n        if ($Length -le 0)\n        {\n            return '0'\n        }\n\n        $code = [Math]::Min([int][Math]::Log($Length, 2), 15)\n        return $code.ToString('x')\n    }\n\n    static [int] Compare([string]$Hash1, [string]$Hash2)\n    {\n        if (-not $Hash1.StartsWith('T1') -or -not $Hash2.StartsWith('T1'))\n        {\n            throw 'Invalid TLSH format'\n        }\n\n        $checksum1 = $Hash1.Substring(2, 2)\n        $checksum2 = $Hash2.Substring(2, 2)\n        $len1 = $Hash1.Substring(4, 1)\n        $len2 = $Hash2.Substring(4, 1)\n        $body1 = $Hash1.Substring(5)\n        $body2 = $Hash2.Substring(5)\n\n        [int]$lenDist = [Math]::Abs(([Convert]::ToInt32($len1, 16)) - ([Convert]::ToInt32($len2, 16)))\n        [int]$checksumDist = [Math]::Abs(([Convert]::ToInt32($checksum1, 16)) - ([Convert]::ToInt32($checksum2, 16)))\n\n        [int]$bodyDist = 0\n        [int]$minLen = [Math]::Min($body1.Length, $body2.Length)\n        for ([int]$i = 0; $i -lt $minLen; $i++)\n        {\n            if ($body1[$i] -ne $body2[$i])\n            {\n                $bodyDist++\n            }\n        }\n\n        return ($lenDist * 12) + $checksumDist + $bodyDist\n    }\n}\n\nfunction Get-DocStats\n{\n    \n    [CmdletBinding()]\n    [OutputType([hashtable])]\n    param(\n        [Parameter(Mandatory)]\n        [string[]]$Documents\n    )\n\n    [int]$docCount = $Documents.Count\n    if ($docCount -eq 0)\n    {\n        return @{ AvgDocLength = 0.0; IdfMap = @{}; DocumentCount = 0 }\n    }\n\n    [double]$totalLength = 0.0\n    $docFreq = [System.Collections.Generic.Dictionary[string, int]]::new([System.StringComparer]::OrdinalIgnoreCase)\n\n    foreach ($doc in $Documents)\n    {\n        $tokenMatches = [SimHash]::WordRegex.Matches($doc)\n        $totalLength += $tokenMatches.Count\n\n        $seen = [System.Collections.Generic.HashSet[string]]::new([System.StringComparer]::OrdinalIgnoreCase)\n        foreach ($match in $tokenMatches)\n        {\n            $null = $seen.Add($match.Value.ToLowerInvariant())\n        }\n\n        foreach ($token in $seen)\n        {\n            [int]$existing = 0\n            if ($docFreq.TryGetValue($token, [ref]$existing))\n            {\n                $docFreq[$token] = $existing + 1\n            }\n            else\n            {\n                $docFreq[$token] = 1\n            }\n        }\n    }\n\n    $idfMap = [System.Collections.Generic.Dictionary[string, double]]::new([System.StringComparer]::OrdinalIgnoreCase)\n    foreach ($kvp in $docFreq.GetEnumerator())\n    {\n        $idfMap[$kvp.Key] = [Math]::Log(($docCount + 1.0) / ($kvp.Value + 1.0)) + 1.0\n    }\n\n    return @{\n        AvgDocLength  = $totalLength / $docCount\n        IdfMap        = $idfMap\n        DocumentCount = $docCount\n    }\n}\n\nfunction Get-FuzzyHash\n{\n    \n    [CmdletBinding()]\n    [OutputType([object])]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [string]$Content,\n\n        [Parameter(Mandatory)]\n        [ValidateSet('SimHash', 'CTPH', 'MinHash', 'TLSH')]\n        [string]$Algorithm,\n\n        [hashtable]$Options = @{}\n    )\n\n    process\n    {\n        switch ($Algorithm)\n        {\n            'SimHash'\n            {\n                $engine = [SimHash]::new($Options)\n                return $engine.Compute($Content)\n            }\n            'CTPH'\n            {\n                return [CTPH]::Compute($Content, $Options)\n            }\n            'MinHash'\n            {\n                return [MinHash]::Compute($Content, $Options)\n            }\n            'TLSH'\n            {\n                return [TLSH]::Compute($Content)\n            }\n        }\n    }\n}\n\nfunction Get-SimHash\n{\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [string]$Text,\n        [double]$UnknownIdf = 1.0,       # safe default for no-corpus use\n        [double]$K1 = 1.5,\n        [double]$B = 0.75,\n        [hashtable]$IdfMap = @{},\n        [double]$MinWeight = 1e-6\n    )\n    process\n    {\n        $sh = [SimHash]::new(@{\n                UnknownIdf = $UnknownIdf; K1 = $K1; B = $B\n                IdfMap = $IdfMap; MinWeight = $MinWeight\n            })\n        $sh.Compute($Text)\n    }\n} |
