idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
12 | reposnapshot/rs.core.jso-engine.psm1 | {23735 1888 0.3274 4.6408} | 24781 | #Requires -Version 7.2\n\nusing namespace System\nusing namespace System.IO\nusing namespace System.Text\nusing namespace System.Collections.Generic\n\nSet-StrictMode -Version Latest\n\nImport-Module \"$PSScriptRoot/hashing-primitives.psm1\" -Force\n\nfunction Resolve-CanonicalPath\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Path\n    )\n\n    $resolved = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n\n    if ($IsWindows)\n    {\n        $resolved = $resolved.ToLowerInvariant()\n    }\n\n    return $resolved.Replace('\\', '/')\n}\n\nclass RabinKarpHash\n{\n    [uint32]$Hash\n    [int]$WindowSize\n    [uint32]$Base = 257\n    [uint32]$Mod = 1000000007\n    [uint32]$BasePower\n\n    RabinKarpHash([int]$windowSize)\n    {\n        $this.WindowSize = $windowSize\n        $this.Hash = 0\n\n        $this.BasePower = 1\n        for ($i = 0; $i -lt $windowSize; $i++)\n        {\n            $this.BasePower = ($this.BasePower * $this.Base) % $this.Mod\n        }\n    }\n\n    [uint32] AddChar([char]$c)\n    {\n\n        $this.Hash = (($this.Hash * $this.Base) + [int]$c) % $this.Mod\n        return $this.Hash\n    }\n\n    [uint32] RemoveChar([char]$c)\n    {\n\n        $removal = ([long][int]$c * $this.BasePower) % $this.Mod\n        $this.Hash = ($this.Hash + $this.Mod - $removal) % $this.Mod\n        return $this.Hash\n    }\n\n    [uint32] RollWindow([char]$oldChar, [char]$newChar)\n    {\n\n        $this.RemoveChar($oldChar)\n        $this.AddChar($newChar)\n        return $this.Hash\n    }\n\n    [void] Reset()\n    {\n        $this.Hash = 0\n    }\n}\n\nfunction Get-PathHash\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Path\n    )\n\n    $canonical = Resolve-CanonicalPath -Path $Path\n\n    $hash = Get-Hash -Content $canonical -Algorithm FNV1a\n\n    return [Convert]::ToString($hash, 16).PadLeft(8, '0')\n}\n\nfunction Get-ContentHash\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [AllowEmptyString()]\n        [string]$Content,\n\n        [int]$WindowSize = 32\n    )\n\n    if ([string]::IsNullOrWhiteSpace($Content))\n    {\n        return 0L\n    }\n\n    $roller = [RabinKarpHash]::new($WindowSize)\n    foreach ($char in $Content.ToCharArray())\n    {\n        [void]$roller.AddChar($char)\n    }\n\n    return [long]$roller.Hash\n}\n\nfunction Get-ContentChunks\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Content,\n\n        [int]$WindowSize = 64,\n\n        [int]$ChunkSize = 4096,\n\n        [int]$Mask = 0xFFF\n    )\n\n    $maskBits = [Math]::Floor([Math]::Log($Mask + 1, 2))\n    $boundaryModulo = [Math]::Pow(2, $maskBits)\n\n    $boundaryOffsets = @(Get-ContentBoundaryOffsets -Content $Content -WindowSize $WindowSize -BoundaryModulo $boundaryModulo -BoundaryValue 0)\n\n    $boundaries = [List[int]]::new()\n    foreach ($boundary in $boundaryOffsets)\n    {\n        $boundaries.Add($boundary.Offset)\n    }\n\n    if ($boundaries.Count -eq 0)\n    {\n        for ($i = $ChunkSize; $i -lt $Content.Length; $i += $ChunkSize)\n        {\n            $boundaries.Add($i)\n        }\n    }\n\n    $finalHash = Get-Hash -Content $Content -Algorithm FNV1a\n\n    return @{\n        Hash       = $finalHash\n        Boundaries = $boundaries\n        ChunkCount = $boundaries.Count + 1\n    }\n}\n\nfunction Find-StringPattern\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [string]$Text,\n\n        [Parameter(Mandatory)]\n        [string]$Pattern\n    )\n\n    $patternLength = $Pattern.Length\n    $textLength = $Text.Length\n    if ($patternLength -gt $textLength) { return }\n\n    $polyOptions = @{\n        Base = 257\n        Mod  = 1000000007\n    }\n\n    $polyContext = [Polynomial]::GetRollContext($patternLength, $polyOptions)\n\n    [uint32]$patternHash = [Polynomial]::Compute($Pattern, $polyContext)\n\n    $roller = [RollingWindow]::new([Polynomial], $patternLength, $polyOptions)\n\n    [void]$roller.Initialize($Text.Substring(0, $patternLength))\n\n    $foundIndices = [System.Collections.Generic.List[int]]::new()\n\n    if ($roller.Hash -eq $patternHash)\n    {\n\n        if ($Text.Substring(0, $patternLength) -eq $Pattern)\n        {\n            $foundIndices.Add(0)\n        }\n    }\n\n    for ($i = $patternLength; $i -lt $textLength; $i++)\n    {\n        [char]$newChar = $Text[$i]\n        [void]$roller.Roll($newChar)\n\n        if ($roller.Hash -eq $patternHash)\n        {\n            $matchIndex = $i - $patternLength + 1\n\n            if ($Text.Substring($matchIndex, $patternLength) -eq $Pattern)\n            {\n                $foundIndices.Add($matchIndex)\n            }\n        }\n    }\n\n    return $foundIndices\n}\n\nclass JsoEncoder\n{\n    [string]$Name\n    [hashtable]$Options\n    [scriptblock]$EncodeFunction\n\n    JsoEncoder([string]$Name, [hashtable]$Options, [scriptblock]$EncodeFunction)\n    {\n        $this.Name = $Name\n        $this.Options = $Options\n        $this.EncodeFunction = $EncodeFunction\n    }\n\n    [object] Encode([object]$Content)\n    {\n        return & $this.EncodeFunction.Invoke($Content, $this.Options)\n    }\n\n    static [JsoEncoder] CreateIdentity()\n    {\n        return [JsoEncoder]::new('identity', @{}, { param($content, $options) return $content })\n    }\n\n    static [JsoEncoder] CreateMinifier()\n    {\n        $minifyScript = {\n            param($content, $options)\n            if ($content -is [string])\n            {\n                return ($content -replace '\\s+', ' ').Trim()\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('minifier', @{}, $minifyScript)\n    }\n\n    static [JsoEncoder] CreateCanonical()\n    {\n        $canonicalScript = {\n            param($content, $options)\n            if ($content -is [pscustomobject] -or $content -is [hashtable])\n            {\n                return ConvertTo-CanonicalJson -InputObject $content -Depth ($options.Depth ?? 100)\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('canonical', @{}, $canonicalScript)\n    }\n\n    static [JsoEncoder] CreateJsonPath([string]$Path)\n    {\n        $jsonPathScript = {\n            param($content, $options)\n            $path = $options.Path\n\n            if ($content -is [pscustomobject] -or $content -is [hashtable])\n            {\n                $result = $content\n                foreach ($segment in $path.Split('.'))\n                {\n                    if ($null -eq $result) { return $null }\n                    $result = $result.$segment\n                }\n                return $result\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('jsonpath', @{Path = $Path }, $jsonPathScript)\n    }\n\n    static [JsoEncoder] CreateTabular([string[]]$Columns = $null)\n    {\n        $tabularScript = {\n            param($content, $options)\n            if ($content -is [array] -or $content -is [System.Collections.IEnumerable])\n            {\n\n                $allItems = @($content)\n                if ($allItems.Count -eq 0) { return @() }\n\n                $columns = $options.Columns\n                if (-not $columns)\n                {\n                    $uniqueKeys = [System.Collections.Generic.HashSet[string]]::new()\n                    foreach ($item in $allItems)\n                    {\n                        if ($item -is [pscustomobject] -or $item -is [hashtable])\n                        {\n                            foreach ($prop in $item.PSObject.Properties)\n                            {\n                                [void]$uniqueKeys.Add($prop.Name)\n                            }\n                        }\n                    }\n                    $columns = @($uniqueKeys)\n                }\n\n                $table = @()\n                foreach ($item in $allItems)\n                {\n                    $row = [ordered]@{}\n                    foreach ($col in $columns)\n                    {\n                        $row[$col] = if ($item.PSObject.Properties[$col]) { $item.$col } else { $null }\n                    }\n                    $table += [pscustomobject]$row\n                }\n                return $table\n            }\n            return $content\n        }\n        $opts = @{}\n        if ($Columns) { $opts.Columns = $Columns }\n        return [JsoEncoder]::new('tabular', $opts, $tabularScript)\n    }\n}\n\nclass JsoSharding\n{\n    [int]$RecordsPerFile\n    [string]$NamingPattern  # Generic: \"{0}_{1}{2}\" where {0}=basename, {1}=index, {2}=ext\n    [int]$CurrentIndex\n    [int]$CurrentRecordCount\n\n    JsoSharding([int]$RecordsPerFile, [string]$NamingPattern)\n    {\n        $this.RecordsPerFile = $RecordsPerFile\n        $this.NamingPattern = $NamingPattern\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n\n    [string] GetShardPath([string]$BasePath)\n    {\n        $directory = [System.IO.Path]::GetDirectoryName($BasePath)\n        $nameWithoutExt = [System.IO.Path]::GetFileNameWithoutExtension($BasePath)\n        $extension = [System.IO.Path]::GetExtension($BasePath)\n\n        $fileName = $this.NamingPattern -f $nameWithoutExt, $this.CurrentIndex, $extension\n        return Join-Path $directory $fileName\n    }\n\n    [bool] ShouldCreateNewShard()\n    {\n        return $this.CurrentRecordCount -ge $this.RecordsPerFile\n    }\n\n    [void] NextShard()\n    {\n        $this.CurrentIndex++\n        $this.CurrentRecordCount = 0\n    }\n\n    [void] RecordWritten()\n    {\n        $this.CurrentRecordCount++\n    }\n\n    [void] Reset()\n    {\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n}\n\nfunction New-JsoEncoder\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [ValidateSet('Identity', 'Minifier', 'Canonical', 'JsonPath', 'Tabular', 'Custom')]\n        [string]$Type,\n\n        [hashtable]$Options = @{},\n\n        [scriptblock]$CustomScript,\n\n        [string]$JsonPath,\n\n        [string[]]$TableColumns\n    )\n\n    switch ($Type)\n    {\n        'Identity' { return [JsoEncoder]::CreateIdentity() }\n        'Minifier' { return [JsoEncoder]::CreateMinifier() }\n        'Canonical' { return [JsoEncoder]::CreateCanonical() }\n        'JsonPath'\n        {\n            if (-not $JsonPath)\n            {\n                throw \"JsonPath parameter is required for JsonPath encoder type\"\n            }\n            return [JsoEncoder]::CreateJsonPath($JsonPath)\n        }\n        'Tabular'\n        {\n            return [JsoEncoder]::CreateTabular($TableColumns)\n        }\n        'Custom'\n        {\n            if (-not $CustomScript)\n            {\n                throw \"CustomScript is required for Custom encoder type\"\n            }\n            return [JsoEncoder]::new('custom', $Options, $CustomScript)\n        }\n    }\n}\n\nfunction New-BloomFilter\n{\n    [CmdletBinding()]\n    param(\n        [int]$ExpectedItems = 10000,\n        [double]$FalsePositiveRate = 0.01\n    )\n\n    $m = [Math]::Ceiling(($ExpectedItems * [Math]::Log($FalsePositiveRate)) / [Math]::Log(1 / [Math]::Pow(2, [Math]::Log(2))))\n\n    $k = [Math]::Ceiling(($m / $ExpectedItems) * [Math]::Log(2))\n\n    return [PSCustomObject]@{\n        PSTypeName        = 'Jso.BloomFilter'\n        BitArray          = [System.Collections.BitArray]::new([int]$m)\n        Size              = [int]$m\n        HashCount         = [int]$k\n        ItemCount         = 0\n        ExpectedItems     = $ExpectedItems\n        FalsePositiveRate = $FalsePositiveRate\n    }\n}\n\nfunction Get-BloomFilterHashes\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Item,\n\n        [Parameter(Mandatory)]\n        [int]$HashCount,\n\n        [Parameter(Mandatory)]\n        [int]$Size\n    )\n\n    $bytes = [Text.Encoding]::UTF8.GetBytes($Item)\n\n    [long]$hash1 = 2166136261\n    foreach ($byte in $bytes)\n    {\n        $hash1 = $hash1 -bxor $byte\n        $hash1 = ($hash1 * 16777619) % [Math]::Pow(2, 32)\n    }\n\n    [long]$hash2 = 5381\n    foreach ($byte in $bytes)\n    {\n        $hash2 = (($hash2 -shl 5) + $hash2) + $byte\n    }\n\n    $hashes = [List[int]]::new()\n    for ($i = 0; $i -lt $HashCount; $i++)\n    {\n        $combined = ($hash1 + $i * $hash2) % $Size\n        if ($combined -lt 0) { $combined += $Size }\n        $hashes.Add([int]$combined)\n    }\n\n    return $hashes\n}\n\nfunction Add-BloomFilterItem\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter,\n\n        [Parameter(Mandatory)]\n        [string]$Item\n    )\n\n    $hashes = Get-BloomFilterHashes -Item $Item -HashCount $BloomFilter.HashCount -Size $BloomFilter.Size\n\n    foreach ($hash in $hashes)\n    {\n        $BloomFilter.BitArray[$hash] = $true\n    }\n\n    $BloomFilter.ItemCount++\n}\n\nfunction Test-BloomFilterItem\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter,\n\n        [Parameter(Mandatory)]\n        [string]$Item\n    )\n\n    $hashes = Get-BloomFilterHashes -Item $Item -HashCount $BloomFilter.HashCount -Size $BloomFilter.Size\n\n    foreach ($hash in $hashes)\n    {\n        if (-not $BloomFilter.BitArray[$hash])\n        {\n            return $false  # Definitely not in set\n        }\n    }\n\n    return $true  # Probably in set\n}\n\nclass JsonlEnumerator : System.Collections.IEnumerable\n{\n    [string]$FilePath\n    [int]$Skip\n    [int]$Take\n\n    JsonlEnumerator([string]$filePath, [int]$skip, [int]$take)\n    {\n        $this.FilePath = $filePath\n        $this.Skip = $skip\n        $this.Take = $take\n    }\n\n    [System.Collections.IEnumerator] GetEnumerator()\n    {\n        return [JsonlEnumeratorImpl]::new($this.FilePath, $this.Skip, $this.Take)\n    }\n}\n\nclass JsonlEnumeratorImpl : System.Collections.IEnumerator\n{\n    [string]$FilePath\n    [System.IO.StreamReader]$Reader\n    [int]$Skip\n    [int]$Take\n    [int]$CurrentIndex\n    [int]$EmittedCount\n    [object]$Current\n\n    JsonlEnumeratorImpl([string]$filePath, [int]$skip, [int]$take)\n    {\n        $this.FilePath = $filePath\n        $this.Skip = $skip\n        $this.Take = $take\n        $this.CurrentIndex = -1\n        $this.EmittedCount = 0\n        $this.Reader = [System.IO.StreamReader]::new($filePath, [Text.Encoding]::UTF8)\n    }\n\n    [bool] MoveNext()\n    {\n        while ($true)\n        {\n            $line = $this.Reader.ReadLine()\n            if ($null -eq $line)\n            {\n                return $false  # EOF\n            }\n\n            $this.CurrentIndex++\n\n            if ([string]::IsNullOrWhiteSpace($line))\n            {\n                continue\n            }\n\n            if ($this.CurrentIndex -lt $this.Skip)\n            {\n                continue\n            }\n\n            if ($this.Take -gt 0 -and $this.EmittedCount -ge $this.Take)\n            {\n                return $false\n            }\n\n            try\n            {\n                $this.Current = $line | ConvertFrom-Json -ErrorAction Stop\n                $this.EmittedCount++\n                return $true\n            }\n            catch\n            {\n                Write-Warning \"Skipping invalid JSON at line $($this.CurrentIndex): $_\"\n                continue\n            }\n        }\n\n        return $false\n    }\n\n    [void] Reset()\n    {\n        $this.Reader.Close()\n        $this.Reader = [System.IO.StreamReader]::new($this.FilePath, [Text.Encoding]::UTF8)\n        $this.CurrentIndex = -1\n        $this.EmittedCount = 0\n        $this.Current = $null\n    }\n\n    [object] get_Current()\n    {\n        return $this.Current\n    }\n\n    [void] Dispose()\n    {\n        if ($this.Reader)\n        {\n            $this.Reader.Close()\n        }\n    }\n}\n\nfunction Get-JsonlEnumerator\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath,\n\n        [int]$Skip = 0,\n\n        [int]$Take = 0\n    )\n\n    if (-not (Test-Path $FilePath))\n    {\n        throw \"JSONL file not found: $FilePath\"\n    }\n\n    return [JsonlEnumerator]::new($FilePath, $Skip, $Take)\n}\n\nfunction New-JsonlBinaryIndex\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JsonlPath,\n\n        [string]$IndexPath = $null\n    )\n\n    if (-not (Test-Path $JsonlPath))\n    {\n        throw \"JSONL file not found: $JsonlPath\"\n    }\n\n    if (-not $IndexPath)\n    {\n        $IndexPath = \"$JsonlPath.idx\"\n    }\n\n    $stream = [FileStream]::new($IndexPath, [FileMode]::Create, [FileAccess]::Write, [FileShare]::None)\n    $writer = [BinaryWriter]::new($stream)\n\n    try\n    {\n\n        $writer.Write([byte[]]@(0x4A, 0x53, 0x4F, 0x49))\n\n        $writer.Write([int32]1)\n\n        $lineCountPos = $stream.Position\n        $writer.Write([int32]0)\n\n        $reader = [StreamReader]::new($JsonlPath, [Text.Encoding]::UTF8)\n        $lineCount = 0\n        $byteOffset = 0\n\n        while ($null -ne ($line = $reader.ReadLine()))\n        {\n            if (-not [string]::IsNullOrWhiteSpace($line))\n            {\n                $writer.Write([int64]$byteOffset)\n                $lineCount++\n            }\n\n            $byteOffset += [Text.Encoding]::UTF8.GetByteCount($line) + 1\n        }\n\n        $reader.Close()\n\n        $stream.Seek($lineCountPos, [SeekOrigin]::Begin) | Out-Null\n        $writer.Write([int32]$lineCount)\n\n        Write-Verbose \"Created index with $lineCount entries at $IndexPath\"\n\n        return @{\n            IndexPath = $IndexPath\n            LineCount = $lineCount\n        }\n    }\n    finally\n    {\n        $writer.Close()\n        $stream.Close()\n    }\n}\n\nfunction Read-JsonlBinaryIndex\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JsonlPath,\n\n        [Parameter(Mandatory)]\n        [int]$LineIndex,\n\n        [string]$IndexPath = $null\n    )\n\n    if (-not $IndexPath)\n    {\n        $IndexPath = \"$JsonlPath.idx\"\n    }\n\n    if (-not (Test-Path $IndexPath))\n    {\n        throw \"Index not found: $IndexPath. Run New-JsonlBinaryIndex first.\"\n    }\n\n    $indexStream = [FileStream]::new($IndexPath, [FileMode]::Open, [FileAccess]::Read, [FileShare]::Read)\n    $indexReader = [BinaryReader]::new($indexStream)\n\n    try\n    {\n\n        $magic = $indexReader.ReadBytes(4)\n        if ($magic[0] -ne 0x4A -or $magic[1] -ne 0x53 -or $magic[2] -ne 0x4F -or $magic[3] -ne 0x49)\n        {\n            throw \"Invalid index file format\"\n        }\n\n        $version = $indexReader.ReadInt32()\n        if ($version -ne 1)\n        {\n            throw \"Unsupported index version: $version\"\n        }\n\n        $lineCount = $indexReader.ReadInt32()\n\n        if ($LineIndex -lt 0 -or $LineIndex -ge $lineCount)\n        {\n            throw \"Line index out of range: $LineIndex (valid: 0-$($lineCount-1))\"\n        }\n\n        $offsetPos = 12 + ($LineIndex * 8)\n        $indexStream.Seek($offsetPos, [SeekOrigin]::Begin) | Out-Null\n\n        $byteOffset = $indexReader.ReadInt64()\n\n        $jsonlStream = [FileStream]::new($JsonlPath, [FileMode]::Open, [FileAccess]::Read, [FileShare]::Read)\n        $jsonlReader = [StreamReader]::new($jsonlStream, [Text.Encoding]::UTF8)\n\n        try\n        {\n            $jsonlStream.Seek($byteOffset, [SeekOrigin]::Begin) | Out-Null\n            $line = $jsonlReader.ReadLine()\n\n            return $line | ConvertFrom-Json\n        }\n        finally\n        {\n            $jsonlReader.Close()\n            $jsonlStream.Close()\n        }\n    }\n    finally\n    {\n        $indexReader.Close()\n        $indexStream.Close()\n    }\n}\n\nfunction ConvertTo-CanonicalJson\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [object]$InputObject,\n\n        [int]$Depth = 100,\n\n        [switch]$Compress\n    )\n\n    process\n    {\n        if ($Compress)\n        {\n            return $InputObject | ConvertTo-Json -Depth $Depth -Compress\n        }\n        else\n        {\n            return $InputObject | ConvertTo-Json -Depth $Depth\n        }\n    }\n}\n\nfunction Get-FileSha256\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath\n    )\n\n    if (-not (Test-Path $FilePath))\n    {\n        throw \"File not found: $FilePath\"\n    }\n\n    $stream = [FileStream]::new($FilePath, [FileMode]::Open, [FileAccess]::Read, [FileShare]::Read)\n    try\n    {\n        $sha256 = [Security.Cryptography.SHA256]::Create()\n        $hashBytes = $sha256.ComputeHash($stream)\n        return [BitConverter]::ToString($hashBytes).Replace('-', '').ToLowerInvariant()\n    }\n    finally\n    {\n        $stream.Close()\n    }\n}\n\nfunction Get-FileSha256\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)] [string] $Path)\n\n    if (-not (Test-Path -Path $Path)) { throw \"File not found: $Path\" }\n\n    $fs = [IO.File]::OpenRead($Path)\n    try\n    {\n\n        return [SHA256]::Compute($fs)\n    }\n    finally\n    {\n        $fs.Dispose()\n    }\n}\n\nfunction Get-FileContentHashSafe\n{\n    \n    [CmdletBinding()]\n    param([string]$FilePath)\n\n    if (-not (Test-Path $FilePath)) { return \"\" }\n\n    try\n    {\n        $content = Get-Content $FilePath -Raw -ErrorAction SilentlyContinue\n        if (-not $content) { return \"\" }\n\n        $bytes = [System.Text.Encoding]::UTF8.GetBytes($content)\n        $hash = [System.Security.Cryptography.MD5]::Create().ComputeHash($bytes)\n        return [System.BitConverter]::ToString($hash).Replace(\"-\", \"\")\n    }\n    catch\n    {\n        return \"\"\n    }\n}\n\nfunction Merge-JsonObjects\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$Left,\n\n        [Parameter(Mandatory)]\n        [PSCustomObject]$Right\n    )\n\n    $merged = [PSCustomObject]@{}\n\n    foreach ($prop in $Left.PSObject.Properties)\n    {\n        $merged | Add-Member -MemberType NoteProperty -Name $prop.Name -Value $prop.Value\n    }\n\n    foreach ($prop in $Right.PSObject.Properties)\n    {\n        if ($merged.PSObject.Properties[$prop.Name])\n        {\n            $merged.PSObject.Properties[$prop.Name].Value = $prop.Value\n        }\n        else\n        {\n            $merged | Add-Member -MemberType NoteProperty -Name $prop.Name -Value $prop.Value\n        }\n    }\n\n    return $merged\n}\n\nfunction Get-JsonDepth\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [object]$InputObject\n    )\n\n    function Get-Depth\n    {\n        param($obj, $currentDepth = 0)\n\n        if ($obj -is [PSCustomObject] -or $obj -is [hashtable])\n        {\n            $maxChildDepth = $currentDepth\n            foreach ($prop in $obj.PSObject.Properties)\n            {\n                $childDepth = Get-Depth -obj $prop.Value -currentDepth ($currentDepth + 1)\n                if ($childDepth -gt $maxChildDepth)\n                {\n                    $maxChildDepth = $childDepth\n                }\n            }\n            return $maxChildDepth\n        }\n        elseif ($obj -is [array] -or $obj -is [System.Collections.IEnumerable])\n        {\n            $maxChildDepth = $currentDepth\n            foreach ($item in $obj)\n            {\n                $childDepth = Get-Depth -obj $item -currentDepth ($currentDepth + 1)\n                if ($childDepth -gt $maxChildDepth)\n                {\n                    $maxChildDepth = $childDepth\n                }\n            }\n            return $maxChildDepth\n        }\n        else\n        {\n            return $currentDepth\n        }\n    }\n\n    return Get-Depth -obj $InputObject\n}\n\nExport-ModuleMember -Function @(\n\n    'Resolve-CanonicalPath',\n\n    'Get-PathHash',          # Uses Get-Hash primitive\n    'Get-ContentHash',       # Uses RabinKarpHash (different algorithm)\n    'Get-ContentChunks',     # Uses Get-ContentBoundaryOffsets primitive\n    'Find-StringPattern',    # Moved from hashing-primitives, uses Polynomial/RollingWindow\n\n    'New-JsoEncoder',\n\n    'New-BloomFilter',\n    'Add-BloomFilterItem',\n    'Test-BloomFilterItem',\n    'Get-BloomFilterHashes',\n\n    'Get-JsonlEnumerator',\n\n    'New-JsonlBinaryIndex',\n    'Read-JsonlBinaryIndex',\n\n    'ConvertTo-CanonicalJson',\n    'Get-FileSha256',\n    'Merge-JsonObjects',\n    'Get-JsonDepth'\n) |
13 | spcx-hashish/bm25-stats.cs | {2493 207 0.2948 4.6243} | 2575 | using System;\nusing System.Collections.Frozen;\nusing System.Collections.Generic;\nusing System.Runtime.CompilerServices;\nusing System.Runtime.InteropServices;\nusing System.Text.RegularExpressions;\n\nnamespace Hashish;\n\npublic static class Bm25Stats\n{\n\n    internal static readonly Regex WordRegex = new(\n        @\"\\w+\",\n        RegexOptions.Compiled | RegexOptions.CultureInvariant | RegexOptions.NonBacktracking\n    );\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public static (double AvgDocLength, FrozenDictionary<string, double> IdfMap)\n        Compute(IReadOnlyList<string> documents, bool ignoreCase = true)\n    {\n        ArgumentNullException.ThrowIfNull(documents);\n\n        int docCount = documents.Count;\n        if (docCount == 0)\n            return (0.0, FrozenDictionary<string, double>.Empty);\n\n        double totalTokens = 0.0;\n\n        var docFreq = new Dictionary<string, int>(\n            capacity: 1024,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        var seen = new HashSet<string>(\n            capacity: 256,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        for (int d = 0; d < docCount; d++)\n        {\n            string doc = documents[d];\n            if (string.IsNullOrEmpty(doc)) continue;\n\n            var matches = WordRegex.Matches(doc);\n            totalTokens += matches.Count;\n\n            seen.Clear();\n            foreach (System.Text.RegularExpressions.Match m in matches)\n            {\n\n                string token = ignoreCase ? m.Value.ToLowerInvariant() : m.Value;\n                seen.Add(token);\n            }\n\n            foreach (string token in seen)\n            {\n                ref int freq = ref CollectionsMarshal.GetValueRefOrAddDefault(docFreq, token, out _);\n                freq++;\n            }\n        }\n\n        double avgDocLength = totalTokens / docCount;\n\n        var idfBuilder = new Dictionary<string, double>(\n            capacity: docFreq.Count,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        double n1 = docCount + 1.0;\n        foreach (var kvp in docFreq)\n        {\n            idfBuilder[kvp.Key] = Math.Log(n1 / (kvp.Value + 1.0)) + 1.0;\n        }\n\n        return (avgDocLength, idfBuilder.ToFrozenDictionary(\n            ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        ));\n    }\n} |
