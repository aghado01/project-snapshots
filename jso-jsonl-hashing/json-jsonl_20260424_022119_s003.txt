idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
3 | jso-utils/Jso-Utils.Core.psm1 | {73238 6072 0.3740 4.4040} | 75901 | using namespace System\nusing namespace System.IO\nusing namespace System.Text\nusing namespace System.Collections.Generic\n#Requires -Version 7.0\nSet-StrictMode -Version 3.0\n\nfunction Get-JsonKind\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)] [string] $Path,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Preference = 'Auto'\n    )\n    if ($Preference -ne 'Auto') { return $Preference }\n    if ($Path -match '\\.jsonl?$')\n    {\n        if ($Path.ToLower().EndsWith('.jsonl')) { return 'JsonLines' }\n        else { return 'Json' }\n    }\n    return 'Json'\n}\n\nfunction ConvertTo-CanonicalJson\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [object] $InputObject,\n        [int] $Depth = 64\n    )\n    function Order-Props($obj)\n    {\n        if ($obj -is [hashtable] -or $obj -is [pscustomobject])\n        {\n            $ordered = [ordered]@{}\n            foreach ($p in ($obj.PSObject.Properties.Name | Sort-Object))\n            {\n                $ordered[$p] = Order-Props $obj.$p\n            }\n            return [pscustomobject]$ordered\n        }\n        elseif ($obj -is [System.Collections.IEnumerable] -and -not ($obj -is [string]))\n        {\n            return @($obj | ForEach-Object { Order-Props $_ })\n        }\n        else\n        {\n            return $obj\n        }\n    }\n    process {\n        $canon = Order-Props $InputObject\n        return (ConvertTo-Json -InputObject $canon -Depth $Depth)\n    }\n}\n\nfunction Strip-JsonComments\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, ValueFromPipeline)] [string] $Content)\n    begin { $sb = [System.Text.StringBuilder]::new() }\n    process\n    {\n        $s = $_\n        $len = $s.Length\n        $inString = $false\n        $escape = $false\n        $i = 0\n        while ($i -lt $len)\n        {\n            $ch = $s[$i]\n            if ($inString)\n            {\n                $sb.Append($ch) | Out-Null\n                if ($escape) { $escape = $false }\n                elseif ($ch -eq '\\\\') { $escape = $true }\n                elseif ($ch -eq '\"') { $inString = $false }\n                $i++\n                continue\n            }\n            if ($ch -eq '\"') { $inString = $true; $sb.Append($ch) | Out-Null; $i++; continue }\n            if ($ch -eq '/' -and ($i + 1) -lt $len)\n            {\n                $nxt = $s[$i + 1]\n                if ($nxt -eq '/')\n                {\n\n                    $i += 2\n                    while ($i -lt $len -and $s[$i] -ne \"`n\" -and $s[$i] -ne \"`r\") { $i++ }\n                    continue\n                }\n                elseif ($nxt -eq '*')\n                {\n\n                    $i += 2\n                    while ($i -lt ($len - 1))\n                    {\n                        if ($s[$i] -eq '*' -and $s[$i + 1] -eq '/') { $i += 2; break }\n                        $i++\n                    }\n                    continue\n                }\n            }\n            $sb.Append($ch) | Out-Null\n            $i++\n        }\n    }\n    end { $sb.ToString() }\n}\n\nfunction Remove-TrailingCommasJson\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, ValueFromPipeline)] [string] $Content)\n    begin { $sb = [System.Text.StringBuilder]::new() }\n    process\n    {\n        $s = $_\n        $len = $s.Length\n        $inString = $false\n        $escape = $false\n        for ($i = 0; $i -lt $len; $i++)\n        {\n            $ch = $s[$i]\n            if ($inString)\n            {\n                $sb.Append($ch) | Out-Null\n                if ($escape) { $escape = $false }\n                elseif ($ch -eq '\\\\') { $escape = $true }\n                elseif ($ch -eq '\"') { $inString = $false }\n                continue\n            }\n            if ($ch -eq '\"') { $inString = $true; $sb.Append($ch) | Out-Null; continue }\n            if ($ch -eq ',')\n            {\n                $j = $i + 1\n                while ($j -lt $len -and ([char]::IsWhiteSpace($s[$j]))) { $j++ }\n                if ($j -lt $len -and ($s[$j] -eq '}' -or $s[$j] -eq ']')) { continue }\n            }\n            $sb.Append($ch) | Out-Null\n        }\n    }\n    end { $sb.ToString() }\n}\n\nfunction Read-JsonFile\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)] [string] $Path,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Kind = 'Auto',\n        [switch] $AsHashtable,\n        [switch] $AllowComments,\n        [switch] $AllowTrailingCommas\n    )\n    $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n    if (-not (Test-Path -Path $Path)) { throw \"File not found: $Path\" }\n\n    $kind = Get-JsonKind -Path $Path -Preference $Kind\n    $raw = Get-Content -Path $Path -Raw -Encoding UTF8\n\n    if ($AllowComments) { $raw = $raw | Strip-JsonComments }\n    if ($AllowTrailingCommas) { $raw = $raw | Remove-TrailingCommasJson }\n\n    if ($kind -eq 'JsonLines')\n    {\n        $lines = $raw -split \"`r?`n\" | Where-Object { $_.Trim().Length -gt 0 }\n        $result = [List[object]]::new()\n        foreach ($ln in $lines)\n        {\n            $line = $ln\n            if ($AllowComments) { $line = $line | Strip-JsonComments }\n            if ($AllowTrailingCommas) { $line = $line | Remove-TrailingCommasJson }\n            if ($AsHashtable) { $result.Add((ConvertFrom-Json -InputObject $line -AsHashtable)) }\n            else { $result.Add((ConvertFrom-Json -InputObject $line)) }\n        }\n        return , $result.ToArray()\n    }\n    else\n    {\n        if ($AsHashtable) { return ConvertFrom-Json -InputObject $raw -AsHashtable }\n        else { return ConvertFrom-Json -InputObject $raw }\n    }\n}\n\nfunction Write-JsonFile\n{\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [Parameter(Mandatory, ValueFromPipeline)] [object] $InputObject,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Kind = 'Auto',\n        [switch] $Append,\n        [int] $Depth = 64,\n        [ValidateSet('utf8', 'utf8BOM')] [string] $Encoding = 'utf8',\n        [switch] $Deduplicate,\n        [string[]] $ByProperty\n    )\n    begin\n    {\n        $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n        $kindResolved = Get-JsonKind -Path $Path -Preference $Kind\n        $buffer = [List[object]]::new()\n    }\n    process { $buffer.Add($InputObject) | Out-Null }\n    end\n    {\n        if ($PSCmdlet.ShouldProcess($Path, \"Write JSON file\"))\n        {\n            if ($kindResolved -eq 'JsonLines')\n            {\n                $lines = [List[string]]::new()\n                foreach ($obj in $buffer)\n                {\n                    if ($obj -is [System.Collections.IEnumerable] -and -not ($obj -is [string]))\n                    {\n                        foreach ($item in $obj) { $lines.Add((ConvertTo-Json -InputObject $item -Depth $Depth -Compress)) }\n                    }\n                    else\n                    {\n                        $lines.Add((ConvertTo-Json -InputObject $obj -Depth $Depth -Compress))\n                    }\n                }\n                if ($Append -and (Test-Path -Path $Path)) { $lines | Add-Content -Path $Path -Encoding $Encoding }\n                else { $lines | Set-Content -Path $Path -Encoding $Encoding }\n                if ($Deduplicate) { Remove-JsonFileDuplicates -Path $Path -Kind JsonLines -ByProperty $ByProperty | Out-Null }\n            }\n            else\n            {\n                $outArray = @()\n                foreach ($obj in $buffer)\n                {\n                    if ($obj -is [System.Collections.IEnumerable] -and -not ($obj -is [string])) { $outArray += $obj }\n                    else { $outArray += , $obj }\n                }\n                if ($Append -and (Test-Path -Path $Path))\n                {\n                    try { $existing = Read-JsonFile -Path $Path }\n                    catch { $existing = @() }\n                    if ($existing -isnot [System.Collections.IEnumerable] -or $existing -is [string]) { $existing = @($existing) }\n                    $outArray = @($existing) + @($outArray)\n                }\n                if ($Deduplicate) { $outArray = Remove-JsonDuplicates -InputObject $outArray -ByProperty $ByProperty }\n                $json = ConvertTo-Json -InputObject $outArray -Depth $Depth\n                $json | Set-Content -Path $Path -Encoding $Encoding\n            }\n        }\n    }\n}\n\nfunction Add-JsonFileItem\n{\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [Parameter(Mandatory, ValueFromPipeline)] [object] $InputObject,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Kind = 'Auto',\n        [switch] $CreateIfMissing,\n        [switch] $Deduplicate,\n        [string[]] $ByProperty\n    )\n    begin\n    {\n        $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n        $kindResolved = Get-JsonKind -Path $Path -Preference $Kind\n        if (-not (Test-Path -Path $Path))\n        {\n            if ($CreateIfMissing)\n            {\n                $parentDir = Split-Path -Path $Path -Parent\n                if ($parentDir -and -not (Test-Path -Path $parentDir)) { New-Item -ItemType Directory -Path $parentDir -Force | Out-Null }\n                New-Item -ItemType File -Path $Path -Force | Out-Null\n            }\n            else { throw \"File not found: $Path\" }\n        }\n    }\n    process\n    {\n        if ($PSCmdlet.ShouldProcess($Path, \"Add JSON item\"))\n        {\n            if ($kindResolved -eq 'JsonLines')\n            {\n                ConvertTo-Json -InputObject $InputObject -Depth 64 -Compress | Add-Content -Path $Path -Encoding UTF8\n                if ($Deduplicate) { Remove-JsonFileDuplicates -Path $Path -Kind JsonLines -ByProperty $ByProperty | Out-Null }\n            }\n            else\n            {\n                $current = @()\n                try { $current = Read-JsonFile -Path $Path } catch { $current = @() }\n                if ($current -isnot [System.Collections.IEnumerable] -or $current -is [string]) { $current = @($current) }\n                $current = @($current) + @($InputObject)\n                if ($Deduplicate) { $current = Remove-JsonDuplicates -InputObject $current -ByProperty $ByProperty }\n                $current | Write-JsonFile -Path $Path -Kind Json\n            }\n        }\n    }\n}\n\nfunction Remove-JsonDuplicates\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [object[]] $InputObject,\n        [string[]] $ByProperty\n    )\n    begin\n    {\n        $seen = New-Object System.Collections.Generic.HashSet[string]\n        $out = [List[object]]::new()\n    }\n    process\n    {\n        foreach ($item in $InputObject)\n        {\n            $key = if ($ByProperty -and $ByProperty.Count -gt 0)\n            {\n                ($ByProperty | ForEach-Object { [string]($item | Select-Object -ExpandProperty $_ -ErrorAction SilentlyContinue) }) -join '|\\0|'\n            }\n            else\n            {\n                (ConvertTo-Json -InputObject $item -Compress -Depth 64)\n            }\n            if ($seen.Add($key)) { $out.Add($item) | Out-Null }\n        }\n    }\n    end { , $out.ToArray() }\n}\n\nfunction Remove-JsonFileDuplicates\n{\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Kind = 'Auto',\n        [string[]] $ByProperty\n    )\n    if ($PSCmdlet.ShouldProcess($Path, \"Remove duplicates\"))\n    {\n        $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n        $kindResolved = Get-JsonKind -Path $Path -Preference $Kind\n        if ($kindResolved -eq 'JsonLines')\n        {\n            $lines = Get-Content -Path $Path -Encoding UTF8\n            $objs = foreach ($ln in $lines) { if ($ln.Trim()) { ConvertFrom-Json -InputObject $ln } }\n            $dedup = Remove-JsonDuplicates -InputObject $objs -ByProperty $ByProperty\n            $dedup | Write-JsonFile -Path $Path -Kind JsonLines\n        }\n        else\n        {\n            $obj = Read-JsonFile -Path $Path\n            if ($obj -isnot [System.Collections.IEnumerable] -or $obj -is [string]) { return }\n            $dedup = Remove-JsonDuplicates -InputObject $obj -ByProperty $ByProperty\n            $dedup | Write-JsonFile -Path $Path -Kind Json\n        }\n    }\n}\n\nfunction Get-JsonDepth\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)] [object] $Object)\n\n    function GetDepthRecursive($obj, $currentDepth = 0)\n    {\n        $maxDepth = $currentDepth\n\n        if ($obj -is [System.Collections.IDictionary])\n        {\n            foreach ($value in $obj.Values)\n            {\n                $childDepth = GetDepthRecursive $value ($currentDepth + 1)\n                if ($childDepth -gt $maxDepth) { $maxDepth = $childDepth }\n            }\n        }\n        elseif ($obj -is [System.Collections.IEnumerable] -and $obj -isnot [string])\n        {\n            foreach ($item in $obj)\n            {\n                $childDepth = GetDepthRecursive $item ($currentDepth + 1)\n                if ($childDepth -gt $maxDepth) { $maxDepth = $childDepth }\n            }\n        }\n        elseif ($obj -is [PSCustomObject])\n        {\n            foreach ($prop in $obj.PSObject.Properties)\n            {\n                $childDepth = GetDepthRecursive $prop.Value ($currentDepth + 1)\n                if ($childDepth -gt $maxDepth) { $maxDepth = $childDepth }\n            }\n        }\n\n        return $maxDepth\n    }\n\n    return GetDepthRecursive $Object\n}\n\nfunction Write-JsonlRecords\n{\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [Parameter(Mandatory, ValueFromPipeline)] [object] $InputObject,\n        [int] $Depth = 64\n    )\n    begin\n    {\n        $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n        $dir = Split-Path -Path $Path -Parent\n        if ($dir -and -not (Test-Path -Path $dir)) { New-Item -ItemType Directory -Path $dir -Force | Out-Null }\n        $sw = [StreamWriter]::new($Path, $false, [UTF8Encoding]::new($false))\n    }\n    process\n    {\n        if ($PSCmdlet.ShouldProcess($Path, \"Write JSONL record\"))\n        {\n            $line = ConvertTo-Json -InputObject $InputObject -Depth $Depth -Compress\n            $sw.WriteLine($line)\n        }\n    }\n    end\n    {\n        $sw.Flush(); $sw.Close(); $sw.Dispose()\n    }\n}\n\nfunction New-JsonlChunk\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $RelativePath,\n        [Parameter(Mandatory)] [string] $Type,\n        [Parameter(Mandatory)] [string] $Text,\n        [int] $StartLine,\n        [int] $StartColumn,\n        [int] $EndLine,\n        [int] $EndColumn\n    )\n    $range = $null\n    if ($PSBoundParameters.ContainsKey('StartLine'))\n    {\n        $range = @{\n            start = @{ line = $StartLine; column = $StartColumn }\n            end   = @{ line = $EndLine; column = $EndColumn }\n        }\n    }\n    [pscustomobject]@{\n        relativePath = $RelativePath\n        type         = $Type\n        text         = $Text\n        range        = $range\n    }\n}\n\nfunction Append-JsonlChunk\n{\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [Parameter(Mandatory)] [pscustomobject] $Chunk\n    )\n    if ($PSCmdlet.ShouldProcess($Path, \"Append JSONL chunk\"))\n    {\n        $Chunk | Write-JsonFile -Path $Path -Kind JsonLines -Append\n    }\n}\n\nfunction New-JsonlIndex\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $JsonlPath,\n        [Parameter(Mandatory)] [string] $IndexPath\n    )\n    $offset = 0L\n    $entries = [List[object]]::new()\n    $sr = [StreamReader]::new($JsonlPath, [UTF8Encoding]::new($false))\n    try\n    {\n        while (-not $sr.EndOfStream)\n        {\n            $line = $sr.ReadLine()\n            $len = ([UTF8Encoding]::new($false)).GetByteCount($line + \"`n\")\n            $entries.Add([pscustomobject]@{ offset = $offset; length = $len })\n            $offset += $len\n        }\n    }\n    finally\n    {\n        $sr.Close()\n        $sr.Dispose()\n    }\n    $entries | Write-JsonFile -Path $IndexPath -Kind Json\n}\n\nfunction Write-VarInt\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [BinaryWriter] $Writer,\n        [Parameter(Mandatory)] [long] $Value\n    )\n    while ($Value -ge 0x80)\n    {\n        $Writer.Write([byte](($Value -band 0x7F) -bor 0x80))\n        $Value = $Value -shr 7\n    }\n    $Writer.Write([byte]($Value -band 0x7F))\n}\n\nfunction Read-VarInt\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)] [BinaryReader] $Reader)\n\n    $result = 0L\n    $shift = 0\n    while ($true)\n    {\n        $b = $Reader.ReadByte()\n        $result = $result -bor (($b -band 0x7F) -shl $shift)\n        if (($b -band 0x80) -eq 0) { break }\n        $shift += 7\n        if ($shift -ge 64) { throw \"VarInt too long\" }\n    }\n    return $result\n}\n\nfunction New-JsonlBinaryIndex\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $JsonlPath,\n        [Parameter(Mandatory)] [string] $IndexPath,\n        [switch] $WithChecksum\n    )\n\n    if (-not (Test-Path -Path $JsonlPath)) { throw \"JSONL file not found: $JsonlPath\" }\n\n    $indexDir = Split-Path -Path $IndexPath -Parent\n    if ($indexDir -and -not (Test-Path -Path $indexDir))\n    {\n        New-Item -ItemType Directory -Path $indexDir -Force | Out-Null\n    }\n\n    $entries = [List[pscustomobject]]::new()\n    $offset = 0L\n    $sr = [StreamReader]::new($JsonlPath, [UTF8Encoding]::new($false))\n\n    try\n    {\n        while (-not $sr.EndOfStream)\n        {\n            $line = $sr.ReadLine()\n            $len = ([UTF8Encoding]::new($false)).GetByteCount($line + \"`n\")\n            $entries.Add([pscustomobject]@{ offset = $offset; length = $len })\n            $offset += $len\n        }\n    }\n    finally\n    {\n        $sr.Close()\n        $sr.Dispose()\n    }\n\n    $binaryIndexPath = $IndexPath -replace '\\.[^.]+$', '.jli'\n    $fs = [FileStream]::new($binaryIndexPath, [FileMode]::Create, [FileAccess]::Write)\n    $writer = [BinaryWriter]::new($fs)\n\n    try\n    {\n\n        $writer.Write([byte[]]@(0x4A, 0x53, 0x4F, 0x01, 0x01))\n        $writer.Write([int32]$entries.Count)\n\n        foreach ($entry in $entries)\n        {\n            $writer.Write([long]$entry.offset)\n            $writer.Write([long]$entry.length)\n        }\n\n    }\n    finally\n    {\n        $writer.Close()\n        $fs.Close()\n        $writer.Dispose()\n        $fs.Dispose()\n    }\n\n    $entries | Write-JsonFile -Path $IndexPath -Kind Json\n\n    $checksumPath = $null\n    if ($WithChecksum)\n    {\n        $checksumPath = $IndexPath -replace '\\.[^.]+$', '.checksum'\n        $dataHash = Get-FileSha256 -Path $JsonlPath\n        $indexHash = Get-FileSha256 -Path $binaryIndexPath\n        $jsonIndexHash = Get-FileSha256 -Path $IndexPath\n\n        $checksumData = [pscustomobject]@{\n            dataFile    = @{\n                path   = $JsonlPath\n                sha256 = $dataHash\n                bytes  = (Get-Item $JsonlPath).Length\n            }\n            binaryIndex = @{\n                path   = $binaryIndexPath\n                sha256 = $indexHash\n                bytes  = (Get-Item $binaryIndexPath).Length\n            }\n            jsonIndex   = @{\n                path   = $IndexPath\n                sha256 = $jsonIndexHash\n                bytes  = (Get-Item $IndexPath).Length\n            }\n            generated   = (Get-Date).ToString('yyyy-MM-ddTHH:mm:ssZ')\n        }\n\n        $checksumData | Write-JsonFile -Path $checksumPath -Kind Json\n    }\n\n    return [pscustomobject]@{\n        jsonlFile    = $JsonlPath\n        binaryIndex  = $binaryIndexPath\n        jsonIndex    = $IndexPath\n        recordCount  = $entries.Count\n        checksumFile = $checksumPath\n    }\n}\n\nfunction Read-JsonlBinaryIndex\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $IndexPath,\n        [switch] $PreferBinary\n    )\n\n    $binaryIndexPath = $IndexPath -replace '\\.[^.]+$', '.jli'\n\n    if ($PreferBinary -and (Test-Path -Path $binaryIndexPath))\n    {\n        $fs = [FileStream]::new($binaryIndexPath, [FileMode]::Open, [FileAccess]::Read)\n        $reader = [BinaryReader]::new($fs)\n\n        try\n        {\n\n            $magic = $reader.ReadBytes(4)\n            $version = $reader.ReadByte()\n            if (-not (($magic -join ',') -eq '74,83,79,1' -and $version -eq 1))\n            {\n                throw \"Invalid binary index format\"\n            }\n\n            $recordCount = $reader.ReadInt32()\n\n            $entries = [List[pscustomobject]]::new()\n\n            for ($i = 0; $i -lt $recordCount; $i++)\n            {\n                $offset = $reader.ReadInt64()\n                $length = $reader.ReadInt64()\n\n                $entries.Add([pscustomobject]@{\n                        offset = $offset\n                        length = $length\n                    })\n            }\n\n            return $entries.ToArray()\n\n        }\n        finally\n        {\n            $reader.Close()\n            $fs.Close()\n            $reader.Dispose()\n            $fs.Dispose()\n        }\n    }\n    else\n    {\n\n        if (Test-Path -Path $IndexPath)\n        {\n            return Read-JsonFile -Path $IndexPath -Kind Json\n        }\n        else\n        {\n            throw \"Neither binary nor JSON index found\"\n        }\n    }\n}\n\nclass JsonlEnumerator : System.IDisposable\n{\n    [StreamReader] $reader\n    [string] $filePath\n    [scriptblock] $predicate\n    [bool] $disposed = $false\n    [long] $position = 0\n    [long] $lineNumber = 0\n\n    JsonlEnumerator([string] $path)\n    {\n        $this.filePath = $path\n        $this.reader = [StreamReader]::new($path, [UTF8Encoding]::new($false))\n    }\n\n    JsonlEnumerator([string] $path, [scriptblock] $predicate)\n    {\n        $this.filePath = $path\n        $this.predicate = $predicate\n        $this.reader = [StreamReader]::new($path, [UTF8Encoding]::new($false))\n    }\n\n    [pscustomobject] GetNext()\n    {\n        if ($this.disposed -or $this.reader.EndOfStream)\n        {\n            return $null\n        }\n\n        while (-not $this.reader.EndOfStream)\n        {\n            $line = $this.reader.ReadLine()\n            $this.lineNumber++\n            $this.position = $this.reader.BaseStream.Position\n\n            if ([string]::IsNullOrWhiteSpace($line))\n            {\n                continue\n            }\n\n            try\n            {\n                $obj = ConvertFrom-Json -InputObject $line\n                $record = [pscustomobject]@{\n                    data       = $obj\n                    lineNumber = $this.lineNumber\n                    position   = $this.position\n                }\n\n                if ($this.predicate -and -not (& $this.predicate $obj))\n                {\n                    continue\n                }\n\n                return $record\n\n            }\n            catch\n            {\n                Write-Warning \"Invalid JSON at line $($this.lineNumber): $line\"\n                continue\n            }\n        }\n\n        return $null\n    }\n\n    [System.Collections.Generic.IEnumerable[pscustomobject]] GetAll()\n    {\n        $results = [List[pscustomobject]]::new()\n        while ($true)\n        {\n            $record = $this.GetNext()\n            if ($null -eq $record) { break }\n            $results.Add($record)\n        }\n        return $results\n    }\n\n    [void] Dispose()\n    {\n        if (-not $this.disposed)\n        {\n            if ($this.reader)\n            {\n                $this.reader.Close()\n                $this.reader.Dispose()\n            }\n            $this.disposed = $true\n        }\n    }\n}\n\nfunction Get-JsonlEnumerator\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [scriptblock] $Predicate,\n        [switch] $EarlyStop\n    )\n\n    if (-not (Test-Path -Path $Path))\n    {\n        throw \"JSONL file not found: $Path\"\n    }\n\n    if ($Predicate)\n    {\n        return [JsonlEnumerator]::new($Path, $Predicate)\n    }\n    else\n    {\n        return [JsonlEnumerator]::new($Path)\n    }\n}\n\nfunction Read-JsonlStream\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [scriptblock] $Predicate,\n        [scriptblock] $ProcessRecord,\n        [int] $BatchSize = 100\n    )\n\n    $enumerator = Get-JsonlEnumerator -Path $Path -Predicate $Predicate\n    $batch = [List[pscustomobject]]::new()\n\n    try\n    {\n        while ($true)\n        {\n            $record = $enumerator.GetNext()\n            if ($null -eq $record) { break }\n\n            if ($ProcessRecord)\n            {\n                & $ProcessRecord $record\n            }\n            else\n            {\n                $batch.Add($record)\n                if ($batch.Count -ge $BatchSize)\n                {\n                    Write-Output $batch.ToArray()\n                    $batch.Clear()\n                }\n            }\n        }\n\n        if ($batch.Count -gt 0)\n        {\n            Write-Output $batch.ToArray()\n        }\n\n    }\n    finally\n    {\n        $enumerator.Dispose()\n    }\n}\n\nfunction Get-FileSha256\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)] [string] $Path)\n\n    if (-not (Test-Path -Path $Path)) { throw \"File not found: $Path\" }\n\n    $algo = [System.Security.Cryptography.SHA256]::Create()\n    $fs = [IO.File]::OpenRead($Path)\n    try\n    {\n        ($algo.ComputeHash($fs) | ForEach-Object { $_.ToString('x2') }) -join ''\n    }\n    finally\n    {\n        $fs.Dispose()\n        $algo.Dispose()\n    }\n}\n\nfunction Get-JsonFileChecksum\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Kind = 'Auto'\n    )\n    if (-not (Test-Path $Path)) { throw \"File not found: $Path\" }\n\n    $resolvedKind = if ($Kind -eq 'Auto')\n    {\n        if ($Path.ToLower().EndsWith('.jsonl')) { 'JsonLines' } else { 'Json' }\n    }\n    else { $Kind }\n\n    $sha256 = [System.Security.Cryptography.SHA256]::Create()\n    $stream = [System.IO.File]::OpenRead($Path)\n    try\n    {\n        $hashBytes = $sha256.ComputeHash($stream)\n        $hashHex = ($hashBytes | ForEach-Object { $_.ToString(\"x2\") }) -join ''\n        $byteSize = (Get-Item $Path).Length\n    }\n    finally\n    {\n        $stream.Dispose()\n        $sha256.Dispose()\n    }\n\n    return [pscustomobject]@{\n        path   = $Path\n        kind   = $resolvedKind\n        sha256 = $hashHex\n        bytes  = $byteSize\n    }\n}\n\nfunction New-BloomFilter\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [int]$ExpectedItems,\n\n        [Parameter()]\n        [double]$FalsePositiveRate = 0.01,\n\n        [Parameter()]\n        [string[]]$HashFunctions = @('SHA1', 'MD5'),\n\n        [Parameter()]\n        [switch]$Persistent,\n\n        [Parameter()]\n        [string]$FilePath\n    )\n\n    $m = [Math]::Ceiling(($ExpectedItems * [Math]::Log($FalsePositiveRate)) / [Math]::Log(1.0 / [Math]::Pow(2.0, [Math]::Log(2.0))))\n\n    $k = [Math]::Round(($m / $ExpectedItems) * [Math]::Log(2.0))\n\n    $filter = @{\n        BitArray          = [System.Collections.BitArray]::new($m)\n        Size              = $m\n        HashCount         = $k\n        HashFunctions     = $HashFunctions\n        ItemCount         = 0\n        ExpectedItems     = $ExpectedItems\n        FalsePositiveRate = $FalsePositiveRate\n        Created           = Get-Date\n        TypeName          = 'JsoUtils.BloomFilter'\n        Persistent        = $false\n    }\n\n    if ($Persistent -and $FilePath)\n    {\n        $filter.FilePath = $FilePath\n        $filter.Persistent = $true\n    }\n\n    $result = [PSCustomObject]$filter\n    $result.PSObject.TypeNames.Insert(0, 'JsoUtils.BloomFilter')\n    return $result\n}\n\nfunction Add-BloomFilterItem\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [PSCustomObject]$BloomFilter,\n\n        [Parameter(Mandatory)]\n        [string]$Item\n    )\n\n    process\n    {\n        if ($BloomFilter.PSObject.TypeNames -notcontains 'JsoUtils.BloomFilter')\n        {\n            throw \"Invalid BloomFilter object\"\n        }\n\n        $hashes = Get-BloomFilterHashes -Item $Item -HashFunctions $BloomFilter.HashFunctions -Size $BloomFilter.Size\n\n        foreach ($hash in $hashes)\n        {\n            $BloomFilter.BitArray[$hash] = $true\n        }\n\n        $BloomFilter.ItemCount++\n\n        if ($BloomFilter.Persistent)\n        {\n            Save-BloomFilter -BloomFilter $BloomFilter\n        }\n\n        return $BloomFilter\n    }\n}\n\nfunction Test-BloomFilterItem\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter,\n\n        [Parameter(Mandatory)]\n        [string]$Item\n    )\n\n    if ($BloomFilter.PSObject.TypeNames -notcontains 'JsoUtils.BloomFilter')\n    {\n        throw \"Invalid BloomFilter object\"\n    }\n\n    $hashes = Get-BloomFilterHashes -Item $Item -HashFunctions $BloomFilter.HashFunctions -Size $BloomFilter.Size\n\n    foreach ($hash in $hashes)\n    {\n        if (-not $BloomFilter.BitArray[$hash])\n        {\n            return $false\n        }\n    }\n\n    return $true\n}\n\nfunction Get-BloomFilterHashes\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Item,\n\n        [Parameter(Mandatory)]\n        [string[]]$HashFunctions,\n\n        [Parameter(Mandatory)]\n        [int]$Size\n    )\n\n    $hashes = @()\n\n    foreach ($hashFunc in $HashFunctions)\n    {\n        $hasher = [System.Security.Cryptography.HashAlgorithm]::Create($hashFunc)\n        if (-not $hasher)\n        {\n            Write-Warning \"Hash function '$hashFunc' not available, using SHA256\"\n            $hasher = [System.Security.Cryptography.SHA256]::Create()\n        }\n\n        try\n        {\n            $bytes = [System.Text.Encoding]::UTF8.GetBytes($Item)\n            $hashBytes = $hasher.ComputeHash($bytes)\n\n            for ($i = 0; $i -lt $hashBytes.Length - 3; $i += 4)\n            {\n                $value = [BitConverter]::ToUInt32($hashBytes, $i)\n                $hashes += [Math]::Abs($value) % $Size\n            }\n        }\n        finally\n        {\n            $hasher.Dispose()\n        }\n    }\n\n    return $hashes | Select-Object -Unique\n}\n\nfunction Get-BloomFilterStats\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter\n    )\n\n    if ($BloomFilter.PSObject.TypeNames -notcontains 'JsoUtils.BloomFilter')\n    {\n        throw \"Invalid BloomFilter object\"\n    }\n\n    $setBits = 0\n    for ($i = 0; $i -lt $BloomFilter.BitArray.Length; $i++)\n    {\n        if ($BloomFilter.BitArray[$i])\n        {\n            $setBits++\n        }\n    }\n\n    $loadFactor = if ($BloomFilter.Size -gt 0) { $setBits / $BloomFilter.Size } else { 0 }\n    $estimatedFalsePositiveRate = [Math]::Pow($loadFactor, $BloomFilter.HashCount)\n\n    return [pscustomobject]@{\n        Size                             = $BloomFilter.Size\n        ItemCount                        = $BloomFilter.ItemCount\n        SetBits                          = $setBits\n        LoadFactor                       = $loadFactor\n        ExpectedFalsePositiveRate        = $BloomFilter.FalsePositiveRate\n        EstimatedActualFalsePositiveRate = $estimatedFalsePositiveRate\n        HashFunctions                    = $BloomFilter.HashFunctions\n        HashCount                        = $BloomFilter.HashCount\n        Created                          = $BloomFilter.Created\n    }\n}\n\nfunction Save-BloomFilter\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter\n    )\n\n    if ($BloomFilter.PSObject.TypeNames -notcontains 'JsoUtils.BloomFilter')\n    {\n        throw \"Invalid BloomFilter object\"\n    }\n\n    if (-not $BloomFilter.Persistent -or -not $BloomFilter.FilePath)\n    {\n        throw \"BloomFilter is not configured for persistence\"\n    }\n\n    $metadata = @{\n        Size              = $BloomFilter.Size\n        HashCount         = $BloomFilter.HashCount\n        HashFunctions     = $BloomFilter.HashFunctions\n        ItemCount         = $BloomFilter.ItemCount\n        ExpectedItems     = $BloomFilter.ExpectedItems\n        FalsePositiveRate = $BloomFilter.FalsePositiveRate\n        Created           = $BloomFilter.Created\n        Saved             = Get-Date\n        Version           = \"1.0\"\n    }\n\n    $bytes = [byte[]]::new([Math]::Ceiling($BloomFilter.Size / 8))\n    $BloomFilter.BitArray.CopyTo($bytes, 0)\n\n    $data = @{\n        Metadata = $metadata\n        BitData  = [Convert]::ToBase64String($bytes)\n    }\n\n    $data | ConvertTo-Json -Depth 3 | Set-Content $BloomFilter.FilePath -Encoding UTF8\n}\n\nfunction Load-BloomFilter\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath\n    )\n\n    if (-not (Test-Path $FilePath))\n    {\n        throw \"Bloom filter file not found: $FilePath\"\n    }\n\n    $data = Get-Content $FilePath -Raw | ConvertFrom-Json\n    $metadata = $data.Metadata\n\n    $bytes = [Convert]::FromBase64String($data.BitData)\n    $bitArray = [System.Collections.BitArray]::new($bytes)\n    $bitArray.Length = $metadata.Size\n\n    $filter = @{\n        BitArray          = $bitArray\n        Size              = $metadata.Size\n        HashCount         = $metadata.HashCount\n        HashFunctions     = $metadata.HashFunctions\n        ItemCount         = $metadata.ItemCount\n        ExpectedItems     = $metadata.ExpectedItems\n        FalsePositiveRate = $metadata.FalsePositiveRate\n        Created           = $metadata.Created\n        Loaded            = Get-Date\n        FilePath          = $FilePath\n        Persistent        = $true\n        TypeName          = 'JsoUtils.BloomFilter'\n    }\n\n    $result = [PSCustomObject]$filter\n    $result.PSObject.TypeNames.Insert(0, 'JsoUtils.BloomFilter')\n    return $result\n}\n\nfunction Test-JsonlDuplicates\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Path,\n\n        [Parameter()]\n        [string]$KeyProperty = \"id\",\n\n        [Parameter()]\n        [double]$FalsePositiveRate = 0.001,\n\n        [Parameter()]\n        [switch]$ReturnDuplicates\n    )\n\n    if (-not (Test-Path $Path))\n    {\n        throw \"JSONL file not found: $Path\"\n    }\n\n    $fileSize = (Get-Item $Path).Length\n    $estimatedLines = [Math]::Max(1000, $fileSize / 100)  # Rough estimate\n    $bloomFilter = New-BloomFilter -ExpectedItems $estimatedLines -FalsePositiveRate $FalsePositiveRate\n\n    $duplicates = [List[pscustomobject]]::new()\n    $lineNumber = 0\n    $seenKeys = [System.Collections.Generic.HashSet[string]]::new()\n\n    Get-Content $Path -Encoding UTF8 | ForEach-Object {\n        $lineNumber++\n        $line = $_.Trim()\n        if ([string]::IsNullOrEmpty($line)) { return }\n\n        try\n        {\n            $json = ConvertFrom-Json $line\n            $key = if ($json.$KeyProperty) { [string]$json.$KeyProperty } else { $line }\n\n            if ($key)\n            {\n                if (Test-BloomFilterItem -BloomFilter $bloomFilter -Item $key)\n                {\n\n                    if ($seenKeys.Contains($key))\n                    {\n                        $duplicates.Add([pscustomobject]@{\n                                LineNumber = $lineNumber\n                                Key        = $key\n                                Content    = $line\n                            })\n                    }\n                    else\n                    {\n                        $seenKeys.Add($key) | Out-Null\n                    }\n                }\n                else\n                {\n                    Add-BloomFilterItem -BloomFilter $bloomFilter -Item $key | Out-Null\n                    $seenKeys.Add($key) | Out-Null\n                }\n            }\n        }\n        catch\n        {\n            Write-Warning \"Invalid JSON at line $lineNumber`: $_\"\n        }\n    }\n\n    if ($ReturnDuplicates)\n    {\n        return $duplicates.ToArray()\n    }\n\n    return $duplicates.Count -gt 0\n}\n\nfunction Get-JsonlDeduplicated\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [string[]]$JsonlLines,\n\n        [Parameter()]\n        [string]$KeyProperty = \"id\",\n\n        [Parameter()]\n        [double]$FalsePositiveRate = 0.001,\n\n        [Parameter()]\n        [int]$EstimatedItemCount = 1000,\n\n        [Parameter()]\n        [switch]$ReturnDuplicateInfo\n    )\n\n    begin\n    {\n        $bloomFilter = New-BloomFilter -ExpectedItems $EstimatedItemCount -FalsePositiveRate $FalsePositiveRate\n        $seenKeys = [System.Collections.Generic.HashSet[string]]::new()\n        $uniqueLines = [List[string]]::new()\n        $duplicateInfo = [List[pscustomobject]]::new()\n        $processed = 0\n        $duplicates = 0\n    }\n\n    process\n    {\n        foreach ($line in $JsonlLines)\n        {\n            $processed++\n\n            if ([string]::IsNullOrWhiteSpace($line))\n            {\n                continue\n            }\n\n            try\n            {\n                $json = ConvertFrom-Json $line\n                $key = if ($json.$KeyProperty) { [string]$json.$KeyProperty } else { $line }\n\n                if ($key)\n                {\n\n                    if (-not (Test-BloomFilterItem -BloomFilter $bloomFilter -Item $key))\n                    {\n\n                        Add-BloomFilterItem -BloomFilter $bloomFilter -Item $key | Out-Null\n                        $seenKeys.Add($key) | Out-Null\n                        $uniqueLines.Add($line)\n                    }\n                    elseif (-not $seenKeys.Contains($key))\n                    {\n\n                        $seenKeys.Add($key) | Out-Null\n                        $uniqueLines.Add($line)\n                    }\n                    else\n                    {\n\n                        $duplicates++\n                        if ($ReturnDuplicateInfo)\n                        {\n                            $duplicateInfo.Add([pscustomobject]@{\n                                    lineNumber = $processed\n                                    key        = $key\n                                    line       = $line\n                                })\n                        }\n                    }\n                }\n                else\n                {\n\n                    $uniqueLines.Add($line)\n                }\n            }\n            catch\n            {\n                Write-Warning \"Invalid JSON at line $processed`: $line\"\n                $uniqueLines.Add($line)  # Keep invalid JSON for manual review\n            }\n        }\n    }\n\n    end\n    {\n        $result = [pscustomobject]@{\n            UniqueLines      = $uniqueLines.ToArray()\n            ProcessedLines   = $processed\n            DuplicatesFound  = $duplicates\n            WrittenLines     = $uniqueLines.Count\n            BloomFilterStats = Get-BloomFilterStats -BloomFilter $bloomFilter\n        }\n\n        if ($ReturnDuplicateInfo)\n        {\n            $result | Add-Member -NotePropertyName 'DuplicateDetails' -NotePropertyValue $duplicateInfo.ToArray()\n        }\n\n        return $result\n    }\n}\n\nfunction New-JsonlDeduplicatedStream\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$InputPath,\n\n        [Parameter(Mandatory)]\n        [string]$OutputPath,\n\n        [Parameter()]\n        [string]$KeyProperty = \"id\",\n\n        [Parameter()]\n        [double]$FalsePositiveRate = 0.001,\n\n        [Parameter()]\n        [PSCustomObject]$BloomFilter = $null,\n\n        [Parameter()]\n        [string]$KeyPath\n    )\n\n    if ($KeyPath) { $KeyProperty = $KeyPath }\n\n    $resolvedInputPath = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($InputPath)\n    $resolvedOutputPath = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($OutputPath)\n\n    if (-not (Test-Path $resolvedInputPath))\n    {\n        throw \"Input JSONL file not found: $resolvedInputPath\"\n    }\n\n    $fileSize = (Get-Item $resolvedInputPath).Length\n    $estimatedLines = [Math]::Max(1000, $fileSize / 100)\n\n    $allLines = Get-Content $resolvedInputPath -Encoding UTF8\n    $deduplicationResult = $allLines | Get-JsonlDeduplicated -KeyProperty $KeyProperty -FalsePositiveRate $FalsePositiveRate -EstimatedItemCount $estimatedLines\n\n    $outputDir = Split-Path $resolvedOutputPath -Parent\n    if ($outputDir -and -not (Test-Path $outputDir))\n    {\n        New-Item -ItemType Directory -Path $outputDir -Force | Out-Null\n    }\n\n    $deduplicationResult.UniqueLines | Write-JsonlRecords -Path $resolvedOutputPath\n\n    return [pscustomobject]@{\n        ProcessedLines   = $deduplicationResult.ProcessedLines\n        DuplicatesFound  = $deduplicationResult.DuplicatesFound\n        WrittenLines     = $deduplicationResult.WrittenLines\n        InputPath        = $resolvedInputPath\n        OutputPath       = $resolvedOutputPath\n        BloomFilterStats = $deduplicationResult.BloomFilterStats\n    }\n}\n\nfunction Split-JsonlByRecordsCount\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [string[]]$JsonlLines,\n\n        [int]$RecordsPerSegment = 1000\n    )\n\n    begin\n    {\n        $segments = [List[string[]]]::new()\n        $currentSegment = [List[string]]::new()\n        $totalProcessed = 0\n    }\n\n    process\n    {\n        foreach ($line in $JsonlLines)\n        {\n            if ([string]::IsNullOrWhiteSpace($line))\n            {\n                continue\n            }\n\n            $currentSegment.Add($line)\n            $totalProcessed++\n\n            if ($currentSegment.Count -ge $RecordsPerSegment)\n            {\n                $segments.Add($currentSegment.ToArray())\n                $currentSegment = [List[string]]::new()\n            }\n        }\n    }\n\n    end\n    {\n\n        if ($currentSegment.Count -gt 0)\n        {\n            $segments.Add($currentSegment.ToArray())\n        }\n\n        return [pscustomobject]@{\n            Segments          = $segments.ToArray()\n            SegmentCounts     = ($segments | ForEach-Object { $_.Count })\n            TotalRecords      = $totalProcessed\n            RecordsPerSegment = $RecordsPerSegment\n        }\n    }\n}\n\nfunction Split-JsonlByRecordCount\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string] $InputPath,\n\n        [Parameter(Mandatory)]\n        [string] $OutputDirectory,\n\n        [int] $RecordsPerFile = 1000,\n\n        [string] $NamingPattern = '{0}_{1:D3}.jsonl'\n    )\n\n    $resolvedInputPath = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($InputPath)\n    $resolvedOutputDirectory = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($OutputDirectory)\n\n    if (-not (Test-Path $resolvedInputPath))\n    {\n        throw \"Input JSONL file not found: $resolvedInputPath\"\n    }\n\n    if (-not (Test-Path $resolvedOutputDirectory))\n    {\n        New-Item -ItemType Directory -Path $resolvedOutputDirectory -Force | Out-Null\n    }\n\n    $allLines = Get-Content $resolvedInputPath -Encoding UTF8\n    $splittingResult = $allLines | Split-JsonlByRecordsCount -RecordsPerSegment $RecordsPerFile\n\n    $baseName = [System.IO.Path]::GetFileNameWithoutExtension($resolvedInputPath)\n    $outputFiles = [List[string]]::new()\n\n    for ($i = 0; $i -lt $splittingResult.Segments.Count; $i++)\n    {\n        $segment = $splittingResult.Segments[$i]\n        $outputFileName = $NamingPattern -f $baseName, ($i + 1), '.jsonl'\n        $outputPath = Join-Path $resolvedOutputDirectory $outputFileName\n        $outputFiles.Add($outputPath)\n\n        $segment | Write-JsonlRecords -Path $outputPath\n    }\n\n    return [pscustomobject]@{\n        Files           = $outputFiles.ToArray()\n        RecordCounts    = $splittingResult.SegmentCounts\n        TotalRecords    = $splittingResult.TotalRecords\n        OutputDirectory = $resolvedOutputDirectory\n        RecordsPerFile  = $RecordsPerFile\n    }\n}\n\nfunction Select-JsonPath\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [object] $InputObject,\n\n        [Parameter(Mandatory)]\n        [string] $Path  # Simplified JSONPath-like: $.data.items[*].name\n    )\n\n    process\n    {\n        try\n        {\n            $current = $InputObject\n            $pathParts = $Path -replace '^\\$\\.', '' -split '\\.'\n\n            foreach ($part in $pathParts)\n            {\n                if ([string]::IsNullOrEmpty($part)) { continue }\n\n                if ($part -match '^(.+)\\[(.+)\\]$')\n                {\n                    $propertyName = $Matches[1]\n                    $arrayIndex = $Matches[2]\n\n                    if ($current -is [PSCustomObject] -and $current.PSObject.Properties.Name -contains $propertyName)\n                    {\n                        $current = $current.$propertyName\n                    }\n                    elseif ($current -is [hashtable] -and $current.ContainsKey($propertyName))\n                    {\n                        $current = $current[$propertyName]\n                    }\n                    else\n                    {\n                        return $null\n                    }\n\n                    if ($current -is [array] -or ($current -is [System.Collections.IEnumerable] -and $current -isnot [string]))\n                    {\n                        if ($arrayIndex -eq '*')\n                        {\n\n                            $results = @()\n                            foreach ($item in $current)\n                            {\n                                $results += $item\n                            }\n                            $current = $results\n                        }\n                        elseif ($arrayIndex -match '^\\d+$')\n                        {\n\n                            $index = [int]$arrayIndex\n                            if ($current -is [array])\n                            {\n                                $current = if ($index -lt $current.Length) { $current[$index] } else { $null }\n                            }\n                            else\n                            {\n                                $currentArray = @($current)\n                                $current = if ($index -lt $currentArray.Length) { $currentArray[$index] } else { $null }\n                            }\n                        }\n                        else\n                        {\n                            return $null\n                        }\n                    }\n                    else\n                    {\n                        return $null\n                    }\n                }\n                else\n                {\n\n                    if ($current -is [PSCustomObject] -and $current.PSObject.Properties.Name -contains $part)\n                    {\n                        $current = $current.$part\n                    }\n                    elseif ($current -is [hashtable] -and $current.ContainsKey($part))\n                    {\n                        $current = $current[$part]\n                    }\n                    elseif ($current -is [array])\n                    {\n\n                        $results = @()\n                        foreach ($item in $current)\n                        {\n                            if ($item -is [PSCustomObject] -and $item.PSObject.Properties.Name -contains $part)\n                            {\n                                $results += $item.$part\n                            }\n                            elseif ($item -is [hashtable] -and $item.ContainsKey($part))\n                            {\n                                $results += $item[$part]\n                            }\n                        }\n                        $current = $results\n                    }\n                    else\n                    {\n                        return $null\n                    }\n                }\n\n                if ($null -eq $current)\n                {\n                    break\n                }\n            }\n\n            return $current\n        }\n        catch\n        {\n            Write-Warning \"Error processing JSON path '$Path': $_\"\n            return $null\n        }\n    }\n}\n\nfunction ConvertTo-JsonTable\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [object[]] $JsonArray,\n\n        [string[]] $Properties  # Which properties to include as columns\n    )\n\n    begin\n    {\n        $allObjects = [List[object]]::new()\n    }\n\n    process\n    {\n        foreach ($item in $JsonArray)\n        {\n            if ($item -is [System.Collections.IEnumerable] -and $item -isnot [string])\n            {\n                foreach ($subItem in $item)\n                {\n                    $allObjects.Add($subItem)\n                }\n            }\n            else\n            {\n                $allObjects.Add($item)\n            }\n        }\n    }\n\n    end\n    {\n        if ($allObjects.Count -eq 0)\n        {\n            return @()\n        }\n\n        $propsToInclude = if ($Properties -and $Properties.Count -gt 0)\n        {\n            $Properties\n        }\n        else\n        {\n\n            $discoveredProps = [System.Collections.Generic.HashSet[string]]::new()\n            $sampleSize = [Math]::Min(10, $allObjects.Count)\n\n            for ($i = 0; $i -lt $sampleSize; $i++)\n            {\n                $obj = $allObjects[$i]\n                if ($obj -is [PSCustomObject])\n                {\n                    foreach ($prop in $obj.PSObject.Properties.Name)\n                    {\n                        $discoveredProps.Add($prop) | Out-Null\n                    }\n                }\n                elseif ($obj -is [hashtable])\n                {\n                    foreach ($key in $obj.Keys)\n                    {\n                        $discoveredProps.Add($key) | Out-Null\n                    }\n                }\n            }\n\n            $discoveredProps | Sort-Object\n        }\n\n        $tableData = [List[pscustomobject]]::new()\n\n        foreach ($obj in $allObjects)\n        {\n            $row = [ordered]@{}\n\n            foreach ($prop in $propsToInclude)\n            {\n                $value = $null\n\n                if ($obj -is [PSCustomObject] -and $obj.PSObject.Properties.Name -contains $prop)\n                {\n                    $value = $obj.$prop\n                }\n                elseif ($obj -is [hashtable] -and $obj.ContainsKey($prop))\n                {\n                    $value = $obj[$prop]\n                }\n\n                if ($value -is [array] -or ($value -is [System.Collections.IEnumerable] -and $value -isnot [string]))\n                {\n                    $value = ($value | ConvertTo-Json -Compress -Depth 2)\n                }\n                elseif ($value -is [PSCustomObject] -or $value -is [hashtable])\n                {\n                    $value = ($value | ConvertTo-Json -Compress -Depth 2)\n                }\n\n                $row[$prop] = $value\n            }\n\n            $tableData.Add([PSCustomObject]$row)\n        }\n\n        return $tableData.ToArray()\n    }\n}\n\nfunction Split-JsonArrayIntoSegments\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [object[]] $JsonArray,\n        [int] $SegmentSize = 1000\n    )\n\n    if ($SegmentSize -le 0) { throw \"SegmentSize must be greater than 0\" }\n\n    $segments = [List[object[]]]::new()\n    $currentSegment = @()\n\n    foreach ($item in $JsonArray)\n    {\n        $currentSegment += $item\n        if ($currentSegment.Count -ge $SegmentSize)\n        {\n            $segments.Add($currentSegment)\n            $currentSegment = @()\n        }\n    }\n\n    if ($currentSegment.Count -gt 0)\n    {\n        $segments.Add($currentSegment)\n    }\n\n    return $segments.ToArray()\n}\n\nfunction New-JsonlChunkCollection\n{\n    [CmdletBinding()]\n    param()\n\n    $collection = @{\n        chunks   = [List[pscustomobject]]::new()\n        metadata = @{\n            created     = Get-Date\n            totalChunks = 0\n        }\n    }\n\n    return $collection\n}\n\nfunction Add-ChunkToCollection\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [hashtable] $Collection,\n        [Parameter(Mandatory)] [string] $Type,\n        [Parameter(Mandatory)] [string] $RelativePath,\n        [Parameter(Mandatory)] [string] $Text,\n        [int] $StartLine,\n        [int] $StartColumn,\n        [int] $EndLine,\n        [int] $EndColumn\n    )\n\n    $chunk = New-JsonlChunk -Type $Type -RelativePath $RelativePath -Text $Text\n    if ($PSBoundParameters.ContainsKey('StartLine'))\n    {\n        $chunk.range = @{\n            start = @{ line = $StartLine; column = $StartColumn }\n            end   = @{ line = $EndLine; column = $EndColumn }\n        }\n    }\n\n    $Collection.chunks.Add($chunk)\n    $Collection.metadata.totalChunks = $Collection.chunks.Count\n}\n\nfunction Get-CanonicalFileIndex\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string[]] $Paths,\n        [ValidateSet('SHA256', 'SHA1', 'MD5')] [string] $Algorithm = 'SHA256'\n    )\n    $index = @{}\n\n    foreach ($path in $Paths)\n    {\n        if (-not $path -or -not (Test-Path $path)) { continue }\n\n        try\n        {\n            $hash = (Get-FileHash -Path $path -Algorithm $Algorithm).Hash\n            $index[$hash] = $path\n        }\n        catch\n        {\n            Write-Warning \"Could not hash file: $path - $($_.Exception.Message)\"\n        }\n    }\n\n    return $index\n}\n\nfunction Test-JsonValid\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [string] $JsonString\n    )\n\n    process\n    {\n        try\n        {\n            $null = ConvertFrom-Json -InputObject $JsonString -ErrorAction Stop\n            return $true\n        }\n        catch\n        {\n            return $false\n        }\n    }\n}\n\nfunction Merge-JsonObjects\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [object] $Target,\n        [Parameter(Mandatory)] [object] $Source,\n        [switch] $Force\n    )\n\n    if ($Target -isnot [PSCustomObject] -or $Source -isnot [PSCustomObject])\n    {\n        throw \"Both Target and Source must be PSCustomObject\"\n    }\n\n    $result = $Target.PSObject.Copy()\n\n    foreach ($prop in $Source.PSObject.Properties)\n    {\n        if ($result.PSObject.Properties.Name -contains $prop.Name)\n        {\n            if ($Force)\n            {\n                $result.PSObject.Properties.Remove($prop.Name)\n                $result.PSObject.Properties.Add($prop.Copy())\n            }\n        }\n        else\n        {\n            $result.PSObject.Properties.Add($prop.Copy())\n        }\n    }\n\n    return $result\n}\n\nfunction ConvertFrom-JsonLines\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [string] $InputObject\n    )\n\n    begin\n    {\n        $results = [List[object]]::new()\n    }\n\n    process\n    {\n        $lines = $InputObject -split \"`r?`n\"\n        foreach ($line in $lines)\n        {\n            if ([string]::IsNullOrWhiteSpace($line)) { continue }\n            try\n            {\n                $results.Add(($line | ConvertFrom-Json))\n            }\n            catch\n            {\n                Write-Warning \"Invalid JSON line: $line\"\n            }\n        }\n    }\n\n    end\n    {\n        return $results.ToArray()\n    }\n}\n\nfunction ConvertTo-JsonLines\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [object] $InputObject,\n        [int] $Depth = 64\n    )\n\n    begin\n    {\n        $lines = [List[string]]::new()\n    }\n\n    process\n    {\n        if ($InputObject -is [array])\n        {\n            foreach ($item in $InputObject)\n            {\n                $lines.Add((ConvertTo-Json -InputObject $item -Depth $Depth -Compress))\n            }\n        }\n        else\n        {\n            $lines.Add((ConvertTo-Json -InputObject $InputObject -Depth $Depth -Compress))\n        }\n    }\n\n    end\n    {\n        return ($lines -join \"`n\")\n    }\n}\n\nfunction New-JsonSchema\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [hashtable] $SchemaDefinition,\n        [string] $SchemaId = (New-Guid).ToString(),\n        [string] $SchemaName = \"Unnamed Schema\"\n    )\n\n    return [pscustomobject]@{\n        schemaId   = $SchemaId\n        schemaName = $SchemaName\n        definition = $SchemaDefinition\n        created    = Get-Date\n        PSTypeName = 'JsoUtils.JsonSchema'\n    }\n}\n\nfunction Test-JsonSchemaProperty\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [AllowNull()] [object] $Value,\n        [Parameter(Mandatory)] [hashtable] $PropertySchema,\n        [string] $PropertyPath = \"\",\n        [switch] $Strict\n    )\n\n    $errors = @()\n\n    if ($PropertySchema.ContainsKey('type'))\n    {\n        $expectedType = $PropertySchema.type\n        $actualType = if ($Value -eq $null) { 'null' }\n        elseif ($Value -is [string]) { 'string' }\n        elseif ($Value -is [int] -or $Value -is [long] -or $Value -is [double] -or $Value -is [decimal]) { 'number' }\n        elseif ($Value -is [bool]) { 'boolean' }\n        elseif ($Value -is [array] -or $Value -is [System.Collections.IEnumerable] -and $Value -isnot [string]) { 'array' }\n        elseif ($Value -is [PSCustomObject] -or $Value -is [hashtable]) { 'object' }\n        else { 'unknown' }\n\n        if ($expectedType -ne $actualType)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"Type mismatch: expected '$expectedType', got '$actualType'\"\n                value    = $Value\n                severity = 'Error'\n            }\n        }\n    }\n\n    if ($PropertySchema.ContainsKey('enum'))\n    {\n        $allowedValues = $PropertySchema.enum\n        if ($Value -notin $allowedValues)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"Value '$Value' not in allowed enum values: $($allowedValues -join ', ')\"\n                value    = $Value\n                severity = 'Error'\n            }\n        }\n    }\n\n    if ($PropertySchema.type -eq 'string' -and $Value -is [string])\n    {\n        if ($PropertySchema.ContainsKey('minLength') -and $Value.Length -lt $PropertySchema.minLength)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"String length $($Value.Length) is less than minimum $($PropertySchema.minLength)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n        if ($PropertySchema.ContainsKey('maxLength') -and $Value.Length -gt $PropertySchema.maxLength)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"String length $($Value.Length) exceeds maximum $($PropertySchema.maxLength)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n        if ($PropertySchema.ContainsKey('pattern') -and $Value -notmatch $PropertySchema.pattern)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"String does not match required pattern: $($PropertySchema.pattern)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n    }\n\n    if ($PropertySchema.type -eq 'number' -and ($Value -is [int] -or $Value -is [long] -or $Value -is [double] -or $Value -is [decimal]))\n    {\n        if ($PropertySchema.ContainsKey('minimum') -and $Value -lt $PropertySchema.minimum)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"Value $Value is less than minimum $($PropertySchema.minimum)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n        if ($PropertySchema.ContainsKey('maximum') -and $Value -gt $PropertySchema.maximum)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"Value $Value exceeds maximum $($PropertySchema.maximum)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n    }\n\n    if ($PropertySchema.type -eq 'array' -and ($Value -is [array] -or ($Value -is [System.Collections.IEnumerable] -and $Value -isnot [string])))\n    {\n        $arrayValue = if ($Value -is [array]) { $Value } else { @($Value) }\n\n        if ($PropertySchema.ContainsKey('minItems') -and $arrayValue.Count -lt $PropertySchema.minItems)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"Array has $($arrayValue.Count) items, minimum is $($PropertySchema.minItems)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n        if ($PropertySchema.ContainsKey('maxItems') -and $arrayValue.Count -gt $PropertySchema.maxItems)\n        {\n            $errors += [pscustomobject]@{\n                path     = $PropertyPath\n                error    = \"Array has $($arrayValue.Count) items, maximum is $($PropertySchema.maxItems)\"\n                value    = $Value\n                severity = if ($Strict) { 'Error' } else { 'Warning' }\n            }\n        }\n\n        if ($PropertySchema.ContainsKey('items'))\n        {\n            for ($i = 0; $i -lt $arrayValue.Count; $i++)\n            {\n                $itemErrors = Test-JsonSchemaProperty -Value $arrayValue[$i] -PropertySchema $PropertySchema.items -PropertyPath \"$PropertyPath[$i]\" -Strict:$Strict\n                if ($itemErrors -and $itemErrors.Count -gt 0)\n                {\n                    $errors += $itemErrors\n                }\n            }\n        }\n    }\n\n    return $errors\n}\n\nfunction Test-JsonObjectSchema\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [object] $JsonObject,\n        [Parameter(Mandatory)] [object] $Schema,\n        [switch] $Strict = $true,\n        [string] $ObjectPath = \"$\"\n    )\n\n    $errors = @()\n    $schemaDefinition = if ($Schema.PSObject.TypeNames -contains 'JsoUtils.JsonSchema')\n    {\n        $Schema.definition\n    }\n    else\n    {\n        $Schema\n    }\n\n    if ($schemaDefinition.ContainsKey('required'))\n    {\n        foreach ($requiredProp in $schemaDefinition.required)\n        {\n            $propExists = if ($JsonObject -is [hashtable])\n            {\n                $JsonObject.ContainsKey($requiredProp)\n            }\n            elseif ($JsonObject -is [PSCustomObject])\n            {\n                $JsonObject.PSObject.Properties.Name -contains $requiredProp\n            }\n            else\n            {\n                $false\n            }\n\n            if (-not $propExists)\n            {\n                $errors += [pscustomobject]@{\n                    path     = \"$ObjectPath.$requiredProp\"\n                    error    = \"Required property '$requiredProp' is missing\"\n                    value    = $null\n                    severity = 'Error'\n                }\n            }\n        }\n    }\n\n    if ($schemaDefinition.ContainsKey('properties'))\n    {\n        $objectProperties = if ($JsonObject -is [hashtable])\n        {\n            $JsonObject.Keys\n        }\n        elseif ($JsonObject -is [PSCustomObject])\n        {\n            $JsonObject.PSObject.Properties.Name\n        }\n        else\n        {\n            @()\n        }\n\n        foreach ($propName in $objectProperties)\n        {\n            $propValue = if ($JsonObject -is [hashtable])\n            {\n                $JsonObject[$propName]\n            }\n            else\n            {\n                $JsonObject.$propName\n            }\n\n            $propPath = \"$ObjectPath.$propName\"\n\n            if ($schemaDefinition.properties.ContainsKey($propName))\n            {\n\n                $propSchema = $schemaDefinition.properties[$propName]\n                $propErrors = Test-JsonSchemaProperty -Value $propValue -PropertySchema $propSchema -PropertyPath $propPath -Strict:$Strict\n                if ($propErrors -and $propErrors.Count -gt 0)\n                {\n                    $errors += $propErrors\n                }\n\n                if ($propSchema.type -eq 'object' -and $propSchema.ContainsKey('properties') -and\n                    ($propValue -is [PSCustomObject] -or $propValue -is [hashtable]))\n                {\n                    $nestedErrors = Test-JsonObjectSchema -JsonObject $propValue -Schema $propSchema -Strict:$Strict -ObjectPath $propPath\n                    if ($nestedErrors -and $nestedErrors.Count -gt 0)\n                    {\n                        $errors += $nestedErrors\n                    }\n                }\n            }\n            elseif ($Strict -and $schemaDefinition.ContainsKey('additionalProperties') -and -not $schemaDefinition.additionalProperties)\n            {\n\n                $errors += [pscustomobject]@{\n                    path     = $propPath\n                    error    = \"Additional property '$propName' is not allowed in strict mode\"\n                    value    = $propValue\n                    severity = 'Error'\n                }\n            }\n            elseif (-not $Strict)\n            {\n\n                $errors += [pscustomobject]@{\n                    path     = $propPath\n                    error    = \"Additional property '$propName' found (not in schema)\"\n                    value    = $propValue\n                    severity = 'Info'\n                }\n            }\n        }\n    }\n\n    return $errors\n}\n\nfunction Test-JsonAgainstSchema\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)] [object] $JsonObject,\n        [Parameter(Mandatory)] [object] $Schema,\n        [switch] $Strict = $true,\n        [switch] $ReturnDetails\n    )\n\n    process\n    {\n        $errors = Test-JsonObjectSchema -JsonObject $JsonObject -Schema $Schema -Strict:$Strict\n\n        if ($ReturnDetails)\n        {\n\n            $allErrors = if ($errors -and $errors.Count -gt 0) { $errors } else { @() }\n            $errorItems = @($allErrors | Where-Object { $_.severity -eq 'Error' })\n            $warningItems = @($allErrors | Where-Object { $_.severity -eq 'Warning' })\n            $infoItems = @($allErrors | Where-Object { $_.severity -eq 'Info' })\n\n            return [pscustomobject]@{\n                isValid      = $errorItems.Count -eq 0\n                errorCount   = $errorItems.Count\n                warningCount = $warningItems.Count\n                infoCount    = $infoItems.Count\n                errors       = $allErrors\n                schemaId     = if ($Schema.PSObject.TypeNames -contains 'JsoUtils.JsonSchema') { $Schema.schemaId } else { 'inline' }\n            }\n        }\n        else\n        {\n            $allErrors = if ($errors -and $errors.Count -gt 0) { $errors } else { @() }\n            $errorItems = @($allErrors | Where-Object { $_.severity -eq 'Error' })\n            return $errorItems.Count -eq 0\n        }\n    }\n}\n\nfunction Test-JsonFileSchema\n{\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)] [string] $Path,\n        [Parameter(Mandatory)] [object] $Schema,\n        [ValidateSet('Auto', 'Json', 'JsonLines')] [string] $Kind = 'Auto',\n        [switch] $Strict = $true,\n        [switch] $StopOnFirstError,\n        [int] $MaxErrors = 100\n    )\n\n    if (-not (Test-Path -Path $Path))\n    {\n        throw \"File not found: $Path\"\n    }\n\n    $kindResolved = Get-JsonKind -Path $Path -Preference $Kind\n    $allResults = @()\n    $errorCount = 0\n\n    if ($kindResolved -eq 'JsonLines')\n    {\n\n        $lineNumber = 0\n        $fullPath = Resolve-Path -Path $Path\n        $sr = [StreamReader]::new($fullPath.Path, [UTF8Encoding]::new($false))\n\n        try\n        {\n            while (-not $sr.EndOfStream -and $errorCount -lt $MaxErrors)\n            {\n                $line = $sr.ReadLine()\n                $lineNumber++\n\n                if ([string]::IsNullOrWhiteSpace($line)) { continue }\n\n                try\n                {\n                    $jsonObj = ConvertFrom-Json -InputObject $line\n                    $validation = Test-JsonAgainstSchema -JsonObject $jsonObj -Schema $Schema -Strict:$Strict -ReturnDetails\n                    $validation | Add-Member -NotePropertyName 'line' -NotePropertyValue $lineNumber -Force\n                    $validation | Add-Member -NotePropertyName 'source' -NotePropertyValue $line -Force\n                    $allResults += $validation\n\n                    if (-not $validation.isValid)\n                    {\n                        $errorCount += $validation.errorCount\n                        if ($StopOnFirstError) { break }\n                    }\n\n                }\n                catch\n                {\n                    $validation = [pscustomobject]@{\n                        line         = $lineNumber\n                        source       = $line\n                        isValid      = $false\n                        errorCount   = 1\n                        warningCount = 0\n                        infoCount    = 0\n                        errors       = @([pscustomobject]@{\n                                path     = \"line:$lineNumber\"\n                                error    = \"Invalid JSON: $($_.Exception.Message)\"\n                                value    = $line\n                                severity = 'Error'\n                            })\n                        schemaId     = if ($Schema.PSObject.TypeNames -contains 'JsoUtils.JsonSchema') { $Schema.schemaId } else { 'inline' }\n                    }\n                    $allResults += $validation\n                    $errorCount++\n                    if ($StopOnFirstError) { break }\n                }\n            }\n        }\n        finally\n        {\n            $sr.Close()\n            $sr.Dispose()\n        }\n    }\n    else\n    {\n\n        try\n        {\n            $jsonData = Read-JsonFile -Path $Path -Kind Json\n            $validation = Test-JsonAgainstSchema -JsonObject $jsonData -Schema $Schema -Strict:$Strict -ReturnDetails\n            $validation | Add-Member -NotePropertyName 'source' -NotePropertyValue $Path -Force\n            $allResults += $validation\n        }\n        catch\n        {\n            $validation = [pscustomobject]@{\n                source       = $Path\n                isValid      = $false\n                errorCount   = 1\n                warningCount = 0\n                infoCount    = 0\n                errors       = @([pscustomobject]@{\n                        path     = \"file:$Path\"\n                        error    = \"Could not parse JSON: $($_.Exception.Message)\"\n                        value    = $null\n                        severity = 'Error'\n                    })\n                schemaId     = if ($Schema.PSObject.TypeNames -contains 'JsoUtils.JsonSchema') { $Schema.schemaId } else { 'inline' }\n            }\n            $allResults += $validation\n        }\n    }\n\n    $totalErrors = if ($allResults.Count -gt 0) { ($allResults | Measure-Object -Property errorCount -Sum).Sum } else { 0 }\n    $totalWarnings = if ($allResults.Count -gt 0) { ($allResults | Measure-Object -Property warningCount -Sum).Sum } else { 0 }\n    $totalInfo = if ($allResults.Count -gt 0) { ($allResults | Measure-Object -Property infoCount -Sum).Sum } else { 0 }\n    $validCount = if ($allResults.Count -gt 0) { ($allResults | Where-Object { $_.isValid }).Count } else { 0 }\n\n    return [pscustomobject]@{\n        filePath       = $Path\n        fileType       = $kindResolved\n        schemaId       = if ($Schema.PSObject.TypeNames -contains 'JsoUtils.JsonSchema') { $Schema.schemaId } else { 'inline' }\n        validationMode = if ($Strict) { 'Strict' } else { 'Relaxed' }\n        summary        = [pscustomobject]@{\n            totalRecords   = $allResults.Count\n            validRecords   = $validCount\n            invalidRecords = $allResults.Count - $validCount\n            totalErrors    = $totalErrors\n            totalWarnings  = $totalWarnings\n            totalInfo      = $totalInfo\n            isValid        = $totalErrors -eq 0\n        }\n        details        = $allResults\n        validated      = Get-Date\n    }\n}\n\nExport-ModuleMember -Function @(\n    'Get-JsonKind',\n    'ConvertTo-CanonicalJson',\n    'Strip-JsonComments',\n    'Remove-TrailingCommasJson',\n    'Read-JsonFile',\n    'Write-JsonFile',\n    'Add-JsonFileItem',\n    'Remove-JsonDuplicates',\n    'Remove-JsonFileDuplicates',\n    'Get-JsonDepth',\n    'Write-JsonlRecords',\n    'New-JsonlChunk',\n    'Append-JsonlChunk',\n    'New-JsonlIndex',\n    'New-JsonlBinaryIndex',\n    'Read-JsonlBinaryIndex',\n    'Get-JsonlEnumerator',\n    'Read-JsonlStream',\n    'Get-FileSha256',\n    'Get-JsonFileChecksum',\n    'Split-JsonArrayIntoSegments',\n    'New-JsonlChunkCollection',\n    'Add-ChunkToCollection',\n    'Get-CanonicalFileIndex',\n    'Test-JsonValid',\n    'Merge-JsonObjects',\n    'ConvertFrom-JsonLines',\n    'ConvertTo-JsonLines',\n    'New-JsonSchema',\n    'Test-JsonAgainstSchema',\n    'Test-JsonFileSchema',\n    'New-BloomFilter',\n    'Add-BloomFilterItem',\n    'Test-BloomFilterItem',\n    'Get-BloomFilterHashes',\n    'Get-BloomFilterStats',\n    'Save-BloomFilter',\n    'Load-BloomFilter',\n    'Test-JsonlDuplicates',\n    'Get-JsonlDeduplicated',\n    'New-JsonlDeduplicatedStream',\n    'Split-JsonlByRecordsCount',\n    'Split-JsonlByRecordCount',\n    'Select-JsonPath',\n    'ConvertTo-JsonTable'\n) |
