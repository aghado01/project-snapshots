idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | generator_clean_payload.psm1 | {14462 1189 0.2812 4.7144} | 14953 | using namespace System.Collections.Generic\nusing namespace System.Text\n#Requires -Version 7.5.2\n<#\n.SYNOPSIS\nClean Payload Generator with Schema Validation for LLM Token Testing\n.DESCRIPTION\nGenerates clean JSON payload files (just \"index\":\"content\" pairs) for LLM consumption\nwith proper schema validation and precise byte counting.\n.NOTES\nAuthor: Sapienscens, Aipithicus\nPowerShell Central Space - Clean Payload Generation with Schema Validation\n#>\n\nclass CleanPayloadGenerator {\n    [string] $SchemaDirectory\n    [string] $TemplateDirectory\n    [string] $OutputDirectory\n    [string] $PythonExecutable\n    [string] $ExperimentId\n\n    CleanPayloadGenerator([string]$schemaDir, [string]$templateDir, [string]$outputDir = \".\", [string]$pythonExe = \"python\") {\n        $this.SchemaDirectory = $schemaDir\n        $this.TemplateDirectory = $templateDir\n        $this.OutputDirectory = $outputDir\n        $this.PythonExecutable = $pythonExe\n        $this.ExperimentId = [System.Guid]::NewGuid().ToString(\"N\").Substring(0, 8)\n\n        $this.ValidateEnvironment()\n    }\n\n    [void] ValidateEnvironment() {\n        if (!(Test-Path $this.SchemaDirectory)) {\n            throw \"Schema directory not found: $($this.SchemaDirectory)\"\n        }\n        if (!(Test-Path $this.TemplateDirectory)) {\n            throw \"Template directory not found: $($this.TemplateDirectory)\"\n        }\n    }\n\n    [PSCustomObject] GenerateWhitespacePayload([int]$lineCount, [int]$startWidth = 1, [int]$increment = 1, [int]$maxWidth = 100) {\n        Write-Host \"🔤 Generating clean whitespace payload: $lineCount lines\" -ForegroundColor Yellow\n\n        # Generate payload lines with precise byte calculation\n        $payloadLines = [List[PSCustomObject]]::new()\n        $totalPayloadBytes = 0\n\n        for ($i = 1; $i -le $lineCount; $i++) {\n            $width = [Math]::Min($startWidth + (($i - 1) * $increment), $maxWidth)\n            $content = \" \" * $width\n\n            # Calculate content bytes (just the string content)\n            $contentBytes = [Encoding]::UTF8.GetByteCount($content)\n            $totalPayloadBytes += $contentBytes\n\n            $lineData = [PSCustomObject]@{\n                number = $i\n                content = $content\n                content_bytes = $contentBytes\n            }\n\n            $payloadLines.Add($lineData)\n        }\n\n        # Generate clean JSON using template\n        $templateData = @{\n            payload_lines = $payloadLines.ToArray()\n        }\n\n        $jsonContent = $this.RenderTemplate(\"simple-whitespace.j2\", $templateData)\n\n        # Precise byte measurements\n        $totalJsonBytes = [Encoding]::UTF8.GetByteCount($jsonContent)\n        $jsonOverheadBytes = $totalJsonBytes - $totalPayloadBytes\n\n        # Schema validation\n        $schemaPath = Join-Path $this.SchemaDirectory \"whitespace-payload-schema.json\"\n        $validationResult = $this.ValidatePayloadSchema($jsonContent, $schemaPath)\n\n        return [PSCustomObject]@{\n            ExperimentId = $this.ExperimentId\n            PayloadType = \"whitespace\"\n            JsonContent = $jsonContent\n            LineCount = $lineCount\n\n            # Precise byte measurements\n            TotalJsonBytes = $totalJsonBytes\n            PayloadOnlyBytes = $totalPayloadBytes\n            JsonOverheadBytes = $jsonOverheadBytes\n            OverheadRatio = if ($totalJsonBytes -gt 0) { $jsonOverheadBytes / $totalJsonBytes } else { 0 }\n            OverheadPercentage = if ($totalJsonBytes -gt 0) { [Math]::Round(($jsonOverheadBytes / $totalJsonBytes) * 100, 2) } else { 0 }\n\n            # Schema validation\n            SchemaValid = $validationResult.IsValid\n            ValidationErrors = $validationResult.Errors\n            SchemaUsed = \"whitespace-payload-schema.json\"\n\n            # Generation parameters\n            Parameters = @{\n                StartWidth = $startWidth\n                Increment = $increment\n                MaxWidth = $maxWidth\n            }\n        }\n    }\n\n    [PSCustomObject] GenerateIntegerPayload([int]$lineCount, [int]$startValue = 1, [int]$increment = 1) {\n        Write-Host \"🔢 Generating clean integer payload: $lineCount lines\" -ForegroundColor Yellow\n\n        $payloadLines = [List[PSCustomObject]]::new()\n        $totalPayloadBytes = 0\n\n        for ($i = 1; $i -le $lineCount; $i++) {\n            $value = $startValue + (($i - 1) * $increment)\n\n            # Calculate content bytes (integer as string)\n            $contentBytes = [Encoding]::UTF8.GetByteCount($value.ToString())\n            $totalPayloadBytes += $contentBytes\n\n            $lineData = [PSCustomObject]@{\n                number = $i\n                content = $value\n                content_bytes = $contentBytes\n            }\n\n            $payloadLines.Add($lineData)\n        }\n\n        # Generate clean JSON\n        $templateData = @{\n            payload_lines = $payloadLines.ToArray()\n        }\n\n        $jsonContent = $this.RenderTemplate(\"simple-integer.j2\", $templateData)\n\n        # Precise measurements\n        $totalJsonBytes = [Encoding]::UTF8.GetByteCount($jsonContent)\n        $jsonOverheadBytes = $totalJsonBytes - $totalPayloadBytes\n\n        # Schema validation\n        $schemaPath = Join-Path $this.SchemaDirectory \"integer-payload-schema.json\"\n        $validationResult = $this.ValidatePayloadSchema($jsonContent, $schemaPath)\n\n        return [PSCustomObject]@{\n            ExperimentId = $this.ExperimentId\n            PayloadType = \"integer\"\n            JsonContent = $jsonContent\n            LineCount = $lineCount\n\n            TotalJsonBytes = $totalJsonBytes\n            PayloadOnlyBytes = $totalPayloadBytes\n            JsonOverheadBytes = $jsonOverheadBytes\n            OverheadRatio = if ($totalJsonBytes -gt 0) { $jsonOverheadBytes / $totalJsonBytes } else { 0 }\n            OverheadPercentage = if ($totalJsonBytes -gt 0) { [Math]::Round(($jsonOverheadBytes / $totalJsonBytes) * 100, 2) } else { 0 }\n\n            SchemaValid = $validationResult.IsValid\n            ValidationErrors = $validationResult.Errors\n            SchemaUsed = \"integer-payload-schema.json\"\n\n            Parameters = @{\n                StartValue = $startValue\n                Increment = $increment\n            }\n        }\n    }\n\n    [string] RenderTemplate([string]$templateName, [hashtable]$templateData) {\n        $templatePath = Join-Path $this.TemplateDirectory $templateName\n\n        # Create temp files for Python processing\n        $tempDataFile = [System.IO.Path]::GetTempFileName()\n        $tempOutputFile = [System.IO.Path]::GetTempFileName()\n\n        try {\n            # Prepare data for Python\n            $templateData | ConvertTo-Json -Depth 6 | Set-Content -Path $tempDataFile -Encoding UTF8\n\n            # Python script for Jinja2 rendering\n            $pythonScript = @\"\nimport json\nimport sys\nfrom jinja2 import Environment, FileSystemLoader\nimport os\n\ntry:\n    with open('$($tempDataFile.Replace('\\', '\\\\'))', 'r', encoding='utf-8') as f:\n        data = json.load(f)\n\n    template_dir = os.path.dirname('$($templatePath.Replace('\\', '\\\\'))')\n    template_name = os.path.basename('$($templatePath.Replace('\\', '\\\\'))')\n\n    env = Environment(loader=FileSystemLoader(template_dir))\n    template = env.get_template(template_name)\n\n    rendered = template.render(**data)\n\n    with open('$($tempOutputFile.Replace('\\', '\\\\'))', 'w', encoding='utf-8') as f:\n        f.write(rendered)\n\n    print(\"SUCCESS\")\n\nexcept Exception as e:\n    print(f\"ERROR: {e}\")\n    sys.exit(1)\n\"@\n\n            $scriptFile = [System.IO.Path]::GetTempFileName() + \".py\"\n            Set-Content -Path $scriptFile -Value $pythonScript -Encoding UTF8\n\n            $result = & $this.PythonExecutable $scriptFile 2>&1\n\n            if ($LASTEXITCODE -eq 0 -and $result -eq \"SUCCESS\") {\n                return Get-Content -Path $tempOutputFile -Raw -Encoding UTF8\n            } else {\n                throw \"Template rendering failed: $result\"\n            }\n        }\n        finally {\n            @($tempDataFile, $tempOutputFile, $scriptFile) | ForEach-Object {\n                if (Test-Path $_) { Remove-Item $_ -ErrorAction SilentlyContinue }\n            }\n        }\n    }\n\n    [PSCustomObject] ValidatePayloadSchema([string]$jsonContent, [string]$schemaPath) {\n        try {\n            # Basic JSON parsing test\n            $parsed = $jsonContent | ConvertFrom-Json\n\n            $errors = [List[string]]::new()\n\n            # Validate structure: all keys should be numeric strings\n            $invalidKeys = $parsed.PSObject.Properties | Where-Object { $_.Name -notmatch '^\\d+$' }\n            if ($invalidKeys) {\n                $errors.Add(\"Invalid keys found (must be numeric): $($invalidKeys.Name -join ', ')\")\n            }\n\n            # Validate values based on payload type\n            if ($schemaPath -like \"*whitespace*\") {\n                $nonStringValues = $parsed.PSObject.Properties | Where-Object { $_.Value -isnot [string] }\n                if ($nonStringValues) {\n                    $errors.Add(\"Non-string values found in whitespace payload: $($nonStringValues.Name -join ', ')\")\n                }\n            }\n            elseif ($schemaPath -like \"*integer*\") {\n                $nonIntegerValues = $parsed.PSObject.Properties | Where-Object { $_.Value -isnot [int] -and $_.Value -isnot [long] }\n                if ($nonIntegerValues) {\n                    $errors.Add(\"Non-integer values found in integer payload: $($nonIntegerValues.Name -join ', ')\")\n                }\n            }\n\n            # Check for empty payload\n            if ($parsed.PSObject.Properties.Count -eq 0) {\n                $errors.Add(\"Payload is empty\")\n            }\n\n            return [PSCustomObject]@{\n                IsValid = $errors.Count -eq 0\n                Errors = $errors.ToArray()\n                KeyCount = $parsed.PSObject.Properties.Count\n                SchemaPath = $schemaPath\n            }\n        }\n        catch {\n            return [PSCustomObject]@{\n                IsValid = $false\n                Errors = @(\"JSON parsing failed: $($_.Exception.Message)\")\n                Exception = $_\n            }\n        }\n    }\n\n    [void] SavePayload([PSCustomObject]$experiment, [string]$baseFileName = $null) {\n        if (-not $baseFileName) {\n            $baseFileName = \"payload-$($experiment.PayloadType)-$($experiment.ExperimentId)\"\n        }\n\n        # Save clean payload JSON (for LLM consumption)\n        $payloadPath = Join-Path $this.OutputDirectory \"$baseFileName.json\"\n        Set-Content -Path $payloadPath -Value $experiment.JsonContent -Encoding UTF8 -NoNewline\n\n        # Save analysis report (for human analysis)\n        $analysisPath = Join-Path $this.OutputDirectory \"$baseFileName-analysis.json\"\n        $analysis = @{\n            ExperimentId = $experiment.ExperimentId\n            PayloadType = $experiment.PayloadType\n            LineCount = $experiment.LineCount\n            SchemaValidation = @{\n                IsValid = $experiment.SchemaValid\n                SchemaUsed = $experiment.SchemaUsed\n                ValidationErrors = $experiment.ValidationErrors\n            }\n            ByteAnalysis = @{\n                TotalJsonBytes = $experiment.TotalJsonBytes\n                PayloadOnlyBytes = $experiment.PayloadOnlyBytes\n                JsonOverheadBytes = $experiment.JsonOverheadBytes\n                OverheadPercentage = $experiment.OverheadPercentage\n                PayloadEfficiency = [Math]::Round((1 - $experiment.OverheadRatio) * 100, 2)\n            }\n            GenerationParameters = $experiment.Parameters\n            GeneratedAt = Get-Date\n        }\n\n        $analysis | ConvertTo-Json -Depth 4 | Set-Content -Path $analysisPath -Encoding UTF8\n\n        # Display results\n        Write-Host \"📄 Clean payload saved:\" -ForegroundColor Green\n        Write-Host \"   🎯 LLM file: $payloadPath ($($experiment.TotalJsonBytes) bytes)\" -ForegroundColor Cyan\n        Write-Host \"   📊 Analysis: $analysisPath\" -ForegroundColor Cyan\n        Write-Host \"   ✅ Schema: $($experiment.SchemaValid) ($($experiment.SchemaUsed))\" -ForegroundColor $(if ($experiment.SchemaValid) { 'Green' } else { 'Red' })\n        Write-Host \"   📐 Efficiency: $([Math]::Round((1 - $experiment.OverheadRatio) * 100, 1))% payload, $($experiment.OverheadPercentage)% overhead\" -ForegroundColor Yellow\n\n        if (-not $experiment.SchemaValid) {\n            Write-Warning \"Schema validation failed:\"\n            $experiment.ValidationErrors | ForEach-Object { Write-Warning \"  - $_\" }\n        }\n    }\n\n    [PSCustomObject] CalculatePayloadBytes([PSCustomObject]$experiment) {\n        # Parse the JSON and calculate precise payload bytes\n        $parsed = $experiment.JsonContent | ConvertFrom-Json\n        $payloadBytes = 0\n\n        foreach ($property in $parsed.PSObject.Properties) {\n            $valueBytes = if ($property.Value -is [string]) {\n                [Encoding]::UTF8.GetByteCount($property.Value)\n            } else {\n                [Encoding]::UTF8.GetByteCount($property.Value.ToString())\n            }\n            $payloadBytes += $valueBytes\n        }\n\n        return [PSCustomObject]@{\n            PayloadOnlyBytes = $payloadBytes\n            TotalJsonBytes = [Encoding]::UTF8.GetByteCount($experiment.JsonContent)\n            CalculatedOverhead = [Encoding]::UTF8.GetByteCount($experiment.JsonContent) - $payloadBytes\n            PayloadRatio = $payloadBytes / [Encoding]::UTF8.GetByteCount($experiment.JsonContent)\n        }\n    }\n}\n\n# Factory functions\nfunction New-CleanPayloadGenerator {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$SchemaDirectory,\n\n        [Parameter(Mandatory)]\n        [string]$TemplateDirectory,\n\n        [string]$OutputDirectory = \".\",\n        [string]$PythonExecutable = \"python\"\n    )\n\n    return [CleanPayloadGenerator]::new($SchemaDirectory, $TemplateDirectory, $OutputDirectory, $PythonExecutable)\n}\n\nfunction Test-PayloadSchema {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$PayloadJsonPath,\n\n        [Parameter(Mandatory)]\n        [string]$SchemaPath\n    )\n\n    $generator = New-CleanPayloadGenerator -SchemaDirectory (Split-Path $SchemaPath) -TemplateDirectory \".\"\n    $jsonContent = Get-Content -Path $PayloadJsonPath -Raw -Encoding UTF8\n    return $generator.ValidatePayloadSchema($jsonContent, $SchemaPath)\n}\n\nExport-ModuleMember -Function @(\n    'New-CleanPayloadGenerator'\n    'Test-PayloadSchema'\n) -Class @('CleanPayloadGenerator') |
