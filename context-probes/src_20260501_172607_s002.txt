idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
1 | generator_unicode_probe.psm1 | {18888 1717 0.3240 4.6393} | 19966 | #Requires -Version 7.5.2\n<#\n.SYNOPSIS\nUnicode Tokenizer Reverse Engineering Probe Generator\n.DESCRIPTION\nGenerates systematic JSON payloads to probe and reverse-engineer tokenizer behavior\nusing Unicode character probing, boundary detection, and minimal pairs analysis.\n.NOTES\nBased on reverse engineering techniques from SPY Lab and tokenization research\n#>\n\nusing namespace System.Collections.Generic\nusing namespace System.Text\n\nclass UnicodeProbeGenerator {\n    [hashtable] $CharacterCategories\n    [List[string]] $ProbeCharacters\n    [string] $OutputDirectory\n    \n    UnicodeProbeGenerator([string]$outputDir = \"unicode-probes\") {\n        $this.OutputDirectory = $outputDir\n        $this.InitializeCharacterCategories()\n        $this.InitializeProbeCharacters()\n        \n        if (-not (Test-Path $this.OutputDirectory)) {\n            New-Item -ItemType Directory -Path $this.OutputDirectory -Force | Out-Null\n        }\n    }\n    \n    [void] InitializeCharacterCategories() {\n        $this.CharacterCategories = @{\n            \"ASCII_Printable\" = @(32..126 | ForEach-Object { [char]$_ })\n            \"ASCII_Control\" = @(0..31 + 127..159 | ForEach-Object { [char]$_ })\n            \"Latin_Extended\" = @(192..255 | ForEach-Object { [char]$_ })\n            \"Greek\" = @(913..937 + 945..969 | ForEach-Object { [char]$_ }) # Α-Ω α-ω\n            \"Cyrillic\" = @(1040..1071 + 1072..1103 | ForEach-Object { [char]$_ }) # А-Я а-я\n            \"Arabic\" = @(1575, 1576, 1578, 1579, 1580, 1582, 1583, 1584, 1585, 1586 | ForEach-Object { [char]$_ })\n            \"Chinese_Common\" = @(20013, 22269, 26159, 29992, 30340, 31616, 32463, 35821, 36825, 39564 | ForEach-Object { [char]$_ }) # 中国文用的等级话进验\n            \"Emoji_Basic\" = @(0x1F600, 0x1F601, 0x1F602, 0x1F603, 0x1F604, 0x1F605 | ForEach-Object { [char]::ConvertFromUtf32($_).ToCharArray() })\n            \"Mathematical\" = @(8704, 8706, 8707, 8709, 8711, 8712, 8713, 8715, 8716, 8719 | ForEach-Object { [char]$_ }) # ∀∂∃∅∇∈∉∋∌∏\n            \"Punctuation\" = @(\"!\", \"@\", \"#\", \"$\", \"%\", \"^\", \"&\", \"*\", \"(\", \")\", \"-\", \"_\", \"+\", \"=\", \"[\", \"]\", \"{\", \"}\", \"|\", \"\\\", \":\", \";\", '\"', \"'\", \"<\", \">\", \",\", \".\", \"?\", \"/\", \"~\", \"`\")\n            \"Whitespace\" = @(\" \", \"`t\", \"`n\", \"`r\", [char]0x00A0, [char]0x2000, [char]0x2001, [char]0x2002, [char]0x2003) # Various whitespace types\n            \"Zero_Width\" = @([char]0x200B, [char]0x200C, [char]0x200D, [char]0xFEFF) # Zero-width space, ZWNJ, ZWJ, BOM\n        }\n    }\n    \n    [void] InitializeProbeCharacters() {\n        $this.ProbeCharacters = [List[string]]::new()\n        \n        # Add representative characters from each category\n        foreach ($category in $this.CharacterCategories.Keys) {\n            $chars = $this.CharacterCategories[$category]\n            if ($chars.Count -gt 0) {\n                # Take first, middle, and last characters from each category\n                $this.ProbeCharacters.Add($chars[0])\n                if ($chars.Count -gt 2) {\n                    $this.ProbeCharacters.Add($chars[($chars.Count / 2)])\n                    $this.ProbeCharacters.Add($chars[-1])\n                }\n            }\n        }\n    }\n    \n    [PSCustomObject] GenerateMinimalPairProbe([string]$baseChar, [int]$maxLength = 10) {\n        <#\n        Generate minimal pairs to detect tokenizer boundaries\n        Based on the principle that tokenizers split at consistent boundaries\n        #>\n        \n        $pairs = @()\n        \n        # Single character probes\n        $pairs += @{\n            \"type\" = \"single\"\n            \"base_char\" = $baseChar\n            \"test\" = $baseChar\n            \"length\" = 1\n        }\n        \n        # Repetition probes (detect BPE merge patterns)\n        for ($i = 2; $i -le $maxLength; $i++) {\n            $repeated = $baseChar * $i\n            $pairs += @{\n                \"type\" = \"repetition\"\n                \"base_char\" = $baseChar\n                \"test\" = $repeated\n                \"length\" = $i\n                \"expected_pattern\" = \"May merge at specific lengths\"\n            }\n        }\n        \n        # Boundary probes (space + character combinations)\n        $pairs += @{\n            \"type\" = \"space_prefix\"\n            \"base_char\" = $baseChar\n            \"test\" = \" $baseChar\"\n            \"length\" = 2\n            \"boundary_type\" = \"word_start\"\n        }\n        \n        $pairs += @{\n            \"type\" = \"space_suffix\"\n            \"base_char\" = $baseChar\n            \"test\" = \"$baseChar \"\n            \"length\" = 2\n            \"boundary_type\" = \"word_end\"\n        }\n        \n        # Mixed character probes (detect cross-script boundaries)\n        foreach ($otherChar in @('a', '中', 'α', 'А', '1', '!')) {\n            if ($otherChar -ne $baseChar) {\n                $pairs += @{\n                    \"type\" = \"mixed_pair\"\n                    \"base_char\" = $baseChar\n                    \"other_char\" = $otherChar\n                    \"test\" = \"$baseChar$otherChar\"\n                    \"reverse_test\" = \"$otherChar$baseChar\"\n                    \"boundary_type\" = \"script_transition\"\n                }\n            }\n        }\n        \n        return [PSCustomObject]@{\n            \"probe_character\" = $baseChar\n            \"unicode_codepoint\" = [int][char]$baseChar\n            \"unicode_category\" = [System.Char]::GetUnicodeCategory($baseChar).ToString()\n            \"minimal_pairs\" = $pairs\n            \"total_tests\" = $pairs.Count\n            \"generated_at\" = (Get-Date).ToString(\"yyyy-MM-dd HH:mm:ss\")\n        }\n    }\n    \n    [PSCustomObject] GenerateSystematicUnicodeProbe() {\n        <#\n        Generate comprehensive Unicode probe covering major character categories\n        Designed to reveal tokenizer's Unicode handling patterns\n        #>\n        \n        $unicodeProbes = @{}\n        \n        foreach ($category in $this.CharacterCategories.Keys) {\n            $categoryChars = $this.CharacterCategories[$category]\n            $categoryProbes = @()\n            \n            # Test each character in isolation\n            foreach ($char in $categoryChars | Select-Object -First 5) { # Limit for manageable size\n                if ($char -is [string] -and $char.Length -eq 1) {\n                    $categoryProbes += @{\n                        \"char\" = $char\n                        \"codepoint\" = [int][char]$char\n                        \"utf8_bytes\" = [System.Text.Encoding]::UTF8.GetByteCount($char)\n                        \"utf16_bytes\" = [System.Text.Encoding]::Unicode.GetByteCount($char)\n                    }\n                }\n            }\n            \n            # Test character sequences within category\n            if ($categoryChars.Count -ge 3) {\n                $seq = ($categoryChars | Select-Object -First 3) -join \"\"\n                $categoryProbes += @{\n                    \"sequence_test\" = $seq\n                    \"type\" = \"intra_category_sequence\"\n                    \"length\" = $seq.Length\n                }\n            }\n            \n            $unicodeProbes[$category] = $categoryProbes\n        }\n        \n        return [PSCustomObject]@{\n            \"probe_type\" = \"systematic_unicode\"\n            \"categories_tested\" = $this.CharacterCategories.Keys.Count\n            \"unicode_probes\" = $unicodeProbes\n            \"purpose\" = \"Detect tokenizer Unicode handling, script boundaries, and encoding patterns\"\n            \"analysis_targets\" = @(\n                \"Script boundary detection\",\n                \"Multi-byte character handling\", \n                \"Normalization behavior\",\n                \"Emoji and special character treatment\"\n            )\n            \"generated_at\" = (Get-Date).ToString(\"yyyy-MM-dd HH:mm:ss\")\n        }\n    }\n    \n    [PSCustomObject] GenerateBPEMergeProbe() {\n        <#\n        Generate probes specifically designed to detect BPE merge patterns\n        Based on frequency-driven merge detection principles\n        #>\n        \n        $mergeProbes = @()\n        \n        # Common English bigrams (likely to be merged early in BPE)\n        $commonBigrams = @(\"th\", \"he\", \"in\", \"er\", \"an\", \"re\", \"ed\", \"nd\", \"ou\", \"ea\", \"ti\", \"to\", \"it\", \"st\", \"io\", \"le\", \"is\", \"ur\", \"ar\", \"as\")\n        \n        foreach ($bigram in $commonBigrams) {\n            # Test isolated bigram\n            $mergeProbes += @{\n                \"type\" = \"common_bigram\"\n                \"pattern\" = $bigram\n                \"test_cases\" = @(\n                    $bigram,                    # Just the bigram\n                    \" $bigram\",                 # Space + bigram  \n                    \"$bigram \",                 # Bigram + space\n                    \" $bigram \",                # Space + bigram + space\n                    $bigram + $bigram,          # Doubled bigram\n                    $bigram + \"ing\",            # Bigram + suffix\n                    \"un\" + $bigram              # Prefix + bigram\n                )\n            }\n        }\n        \n        # Artificial repetition patterns (to detect merge thresholds)\n        foreach ($testChar in @('a', 'x', '1', '中')) {\n            $repetitionTests = @()\n            for ($length = 1; $length -le 20; $length++) {\n                $repetitionTests += $testChar * $length\n            }\n            \n            $mergeProbes += @{\n                \"type\" = \"repetition_threshold\"\n                \"base_character\" = $testChar\n                \"test_cases\" = $repetitionTests\n                \"purpose\" = \"Detect at what length repetitions get merged\"\n            }\n        }\n        \n        return [PSCustomObject]@{\n            \"probe_type\" = \"bpe_merge_detection\"\n            \"merge_probes\" = $mergeProbes\n            \"purpose\" = \"Reverse engineer BPE merge priorities and thresholds\"\n            \"analysis_targets\" = @(\n                \"Common bigram merge priority\",\n                \"Repetition merge thresholds\",\n                \"Context-dependent merging\",\n                \"Cross-linguistic merge patterns\"\n            )\n            \"generated_at\" = (Get-Date).ToString(\"yyyy-MM-dd HH:mm:ss\")\n        }\n    }\n    \n    [PSCustomObject] GenerateTokenBoundaryProbe() {\n        <#\n        Generate probes to detect exact token boundaries\n        Uses streaming analysis technique from Claude reverse engineering\n        #>\n        \n        $boundaryTests = @()\n        \n        # Progressive addition technique (add one char at a time)\n        $testBase = \"hello\"\n        for ($i = 1; $i -le $testBase.Length; $i++) {\n            $boundaryTests += @{\n                \"type\" = \"progressive_addition\"\n                \"base\" = $testBase\n                \"test\" = $testBase.Substring(0, $i)\n                \"position\" = $i\n                \"purpose\" = \"Detect if tokenizer splits mid-word\"\n            }\n        }\n        \n        # Punctuation boundary tests\n        $punctuationCombos = @(\n            \"word.\", \"word,\", \"word!\", \"word?\", \"word;\", \"word:\",\n            \".word\", \",word\", \"!word\", \"?word\", \";word\", \":word\",\n            \"word.word\", \"word,word\", \"word!word\"\n        )\n        \n        foreach ($combo in $punctuationCombos) {\n            $boundaryTests += @{\n                \"type\" = \"punctuation_boundary\"\n                \"test\" = $combo\n                \"expected\" = \"May split at punctuation\"\n            }\n        }\n        \n        # Number-letter boundary tests\n        $numberLetterCombos = @(\"123abc\", \"abc123\", \"123.456\", \"word123\", \"123word\")\n        foreach ($combo in $numberLetterCombos) {\n            $boundaryTests += @{\n                \"type\" = \"number_letter_boundary\"\n                \"test\" = $combo\n                \"expected\" = \"May split at number-letter boundary\"\n            }\n        }\n        \n        return [PSCustomObject]@{\n            \"probe_type\" = \"token_boundary_detection\"\n            \"boundary_tests\" = $boundaryTests\n            \"methodology\" = \"Progressive addition and systematic boundary probing\"\n            \"purpose\" = \"Map exact tokenizer split points\"\n            \"analysis_targets\" = @(\n                \"Word-internal splits\",\n                \"Punctuation handling\",\n                \"Number-letter transitions\",\n                \"Whitespace tokenization\"\n            )\n            \"generated_at\" = (Get-Date).ToString(\"yyyy-MM-dd HH:mm:ss\")\n        }\n    }\n    \n    [void] GenerateProbePayload([string]$probeType, [string]$outputFile) {\n        $probe = switch ($probeType) {\n            \"unicode\" { $this.GenerateSystematicUnicodeProbe() }\n            \"bpe\" { $this.GenerateBPEMergeProbe() }\n            \"boundary\" { $this.GenerateTokenBoundaryProbe() }\n            default { throw \"Unknown probe type: $probeType\" }\n        }\n        \n        # Create JSON payload with proper structure for tokenizer testing\n        $payload = @{\n            \"probe_metadata\" = @{\n                \"probe_type\" = $probeType\n                \"generated_by\" = \"UnicodeProbeGenerator\"\n                \"purpose\" = \"Tokenizer reverse engineering\"\n                \"analysis_method\" = \"Systematic character probing and boundary detection\"\n            }\n            \"test_data\" = $probe\n            \"instructions_for_analysis\" = @(\n                \"1. Run each test case through target tokenizer\",\n                \"2. Record token count for each test\",\n                \"3. Analyze patterns in token boundaries\",\n                \"4. Look for merge patterns and thresholds\",\n                \"5. Identify Unicode handling behavior\",\n                \"6. Map tokenizer's character category preferences\"\n            )\n        }\n        \n        $outputPath = Join-Path $this.OutputDirectory $outputFile\n        $payload | ConvertTo-Json -Depth 10 | Set-Content -Path $outputPath -Encoding UTF8\n        Write-Host \"✅ Generated probe: $outputPath\" -ForegroundColor Green\n    }\n    \n    [void] GenerateFullProbeSuite() {\n        Write-Host \"🔬 Generating comprehensive tokenizer reverse engineering probe suite...\" -ForegroundColor Cyan\n        \n        # Generate all probe types\n        $this.GenerateProbePayload(\"unicode\", \"unicode-systematic-probe.json\")\n        $this.GenerateProbePayload(\"bpe\", \"bpe-merge-probe.json\") \n        $this.GenerateProbePayload(\"boundary\", \"token-boundary-probe.json\")\n        \n        # Generate minimal pair probes for key characters\n        $keyChars = @('a', ' ', '中', 'α', 'А', '1', '!', '😀')\n        foreach ($char in $keyChars) {\n            if ($char.Length -eq 1) {\n                $minimalProbe = $this.GenerateMinimalPairProbe($char)\n                $fileName = \"minimal-pair-$([int][char]$char).json\"\n                $minimalProbe | ConvertTo-Json -Depth 10 | Set-Content -Path (Join-Path $this.OutputDirectory $fileName) -Encoding UTF8\n                Write-Host \"✅ Generated minimal pair probe: $fileName\" -ForegroundColor Green\n            }\n        }\n        \n        # Generate analysis script\n        $this.GenerateAnalysisScript()\n        \n        Write-Host \"🎯 Probe suite generated in: $($this.OutputDirectory)\" -ForegroundColor Yellow\n        Write-Host \"📊 Ready for tokenizer reverse engineering analysis!\" -ForegroundColor Green\n    }\n    \n    [void] GenerateAnalysisScript() {\n        $analysisScript = @\"\n# Tokenizer Reverse Engineering Analysis Script\n\n## Usage with Unified Tokenizer Framework:\n\nImport-Module .\\UnifiedTokenCounterHelper.psm1\n\n`$tokenCounter = New-TokenCounterHelper\n\n# Register your tokenizers\nRegister-Tokenizer -TokenCounter `$tokenCounter -Name 'gpt2' -ExecutablePath 'universal-tokenizer.exe' -CommandTemplate '{exe} count --model gpt2 {file}'\nRegister-Tokenizer -TokenCounter `$tokenCounter -Name 'tiktoken' -ExecutablePath 'tiktoken.exe' -CommandTemplate '{exe} count --model gpt-4 {file}'\n\n# Run probes through all tokenizers\n`$probeFiles = Get-ChildItem -Path \"$($this.OutputDirectory)\" -Filter \"*.json\"\n`$results = @{}\n\nforeach (`$probeFile in `$probeFiles) {\n    Write-Host \"🔬 Analyzing: `$(`$probeFile.Name)\"\n    \n    # Convert probe to payload format for testing\n    `$probeData = Get-Content `$probeFile.FullName | ConvertFrom-Json\n    \n    # Create temporary payload for tokenizer testing\n    `$tempPayload = [PSCustomObject]@{\n        JsonContent = `$probeData | ConvertTo-Json -Compress\n        PayloadOnlyBytes = [System.Text.Encoding]::UTF8.GetByteCount((`$probeData | ConvertTo-Json -Compress))\n        TotalJsonBytes = [System.Text.Encoding]::UTF8.GetByteCount((`$probeData | ConvertTo-Json -Compress))\n    }\n    \n    # Test with each tokenizer\n    foreach (`$tokenizerName in `$tokenCounter.GetAvailableTokenizers()) {\n        try {\n            `$result = `$tokenCounter.CountTokens(`$tempPayload, `$tokenizerName)\n            if (-not `$results.ContainsKey(`$probeFile.BaseName)) {\n                `$results[`$probeFile.BaseName] = @{}\n            }\n            `$results[`$probeFile.BaseName][`$tokenizerName] = `$result.TokenCount\n            \n            Write-Host \"  `$tokenizerName`: `$(`$result.TokenCount) tokens\"\n        }\n        catch {\n            Write-Warning \"Failed to test `$tokenizerName with `$(`$probeFile.Name): `$_\"\n        }\n    }\n}\n\n# Analyze patterns\nWrite-Host \"`n🎯 REVERSE ENGINEERING ANALYSIS:\" -ForegroundColor Yellow\n\nforeach (`$probe in `$results.Keys) {\n    Write-Host \"`n📊 `$probe patterns:\" -ForegroundColor Cyan\n    `$tokenCounts = `$results[`$probe]\n    \n    # Look for patterns\n    `$values = `$tokenCounts.Values\n    `$min = (`$values | Measure-Object -Minimum).Minimum\n    `$max = (`$values | Measure-Object -Maximum).Maximum\n    `$variance = if (`$values.Count -gt 1) { (`$values | Measure-Object -StandardDeviation).StandardDeviation } else { 0 }\n    \n    Write-Host \"  Token count range: `$min - `$max\"\n    Write-Host \"  Variance: `$([Math]::Round(`$variance, 2))\"\n    \n    if (`$variance -lt 0.5) {\n        Write-Host \"  🟢 CONSISTENT: All tokenizers handle similarly\" -ForegroundColor Green\n    } elseif (`$variance -lt 2.0) {\n        Write-Host \"  🟡 MODERATE VARIANCE: Some differences in handling\" -ForegroundColor Yellow  \n    } else {\n        Write-Host \"  🔴 HIGH VARIANCE: Significant tokenizer differences\" -ForegroundColor Red\n    }\n    \n    foreach (`$tokenizer in `$tokenCounts.Keys) {\n        Write-Host \"    `$tokenizer`: `$(`$tokenCounts[`$tokenizer]) tokens\"\n    }\n}\n\n# Export detailed results\n`$results | ConvertTo-Json -Depth 5 | Set-Content -Path \"tokenizer-reverse-engineering-results.json\"\nWrite-Host \"`n✅ Results saved to: tokenizer-reverse-engineering-results.json\" -ForegroundColor Green\n\"@\n        \n        $scriptPath = Join-Path $this.OutputDirectory \"run-reverse-engineering-analysis.ps1\"\n        $analysisScript | Set-Content -Path $scriptPath -Encoding UTF8\n        Write-Host \"✅ Generated analysis script: $scriptPath\" -ForegroundColor Green\n    }\n}\n\n# Factory function\nfunction New-UnicodeProbeGenerator {\n    [CmdletBinding()]\n    param(\n        [string]$OutputDirectory = \"unicode-probes\"\n    )\n    \n    return [UnicodeProbeGenerator]::new($OutputDirectory)\n}\n\nExport-ModuleMember -Function 'New-UnicodeProbeGenerator' -Class 'UnicodeProbeGenerator'\n\"@ |
2 | jinja2-engine/jinja2_engine.py | {10451 930 0.3097 4.3098} | 10847 | \"\"\"General-purpose Jinja2 rendering engine.\n\nThis module provides a flexible Jinja2 rendering engine that doesn't assume\nspecific project structure or template locations. It can be configured with\ncustom template directories, loaders, and rendering options.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom pathlib import Path\nfrom typing import Any, Dict, List, Optional, Union, cast\n\nfrom jinja2 import (\n    BaseLoader,\n    DictLoader,\n    Environment,\n    FileSystemBytecodeCache,\n    FileSystemLoader,\n    Template,\n    TemplateNotFound,\n    select_autoescape,\n)\n\nclass TemplateEngine:\n    \"\"\"A configurable Jinja2 template rendering engine.\n\n    This engine can work with various template sources (filesystem, dictionary,\n    custom loaders) and provides flexible configuration options.\n    \"\"\"\n\n    def __init__(\n        self,\n        template_dirs: Optional[Union[str, Path, List[Union[str, Path]]]] = None,\n        template_dict: Optional[Dict[str, str]] = None,\n        loader: Optional[BaseLoader] = None,\n        cache_dir: Optional[Union[str, Path]] = None,\n        autoescape_extensions: Optional[List[str]] = None,\n        no_autoescape_extensions: Optional[List[str]] = None,\n        autoescape_default: bool = False,\n        **env_kwargs: Any,\n    ):\n        \"\"\"Initialize the template engine.\n\n        Args:\n            template_dirs: Directory or list of directories containing templates.\n                          If None, no filesystem templates will be available.\n            template_dict: Dictionary mapping template names to template strings.\n                          Useful for inline templates or testing.\n            loader: Custom Jinja2 loader. If provided, takes precedence over\n                   template_dirs and template_dict.\n            cache_dir: Directory for bytecode cache. If None, no caching is used.\n            autoescape_extensions: List of file extensions that should have\n                                 autoescaping enabled (e.g., ['html', 'xml']).\n                                 Defaults to ['html', 'xml'].\n            no_autoescape_extensions: List of file extensions that should have\n                                    autoescaping disabled (e.g., ['md', 'txt']).\n                                    Defaults to ['md', 'txt'].\n            autoescape_default: Default autoescaping behavior for templates\n                              without recognized extensions.\n            **env_kwargs: Additional keyword arguments passed to Jinja2 Environment.\n        \"\"\"\n        self._env = self._create_environment(\n            template_dirs=template_dirs,\n            template_dict=template_dict,\n            loader=loader,\n            cache_dir=cache_dir,\n            autoescape_extensions=autoescape_extensions or [\"html\", \"xml\"],\n            no_autoescape_extensions=no_autoescape_extensions or [\"md\", \"txt\"],\n            autoescape_default=autoescape_default,\n            **env_kwargs,\n        )\n\n    def _create_environment(\n        self,\n        template_dirs: Optional[Union[str, Path, List[Union[str, Path]]]],\n        template_dict: Optional[Dict[str, str]],\n        loader: Optional[BaseLoader],\n        cache_dir: Optional[Union[str, Path]],\n        autoescape_extensions: List[str],\n        no_autoescape_extensions: List[str],\n        autoescape_default: bool,\n        **env_kwargs: Any,\n    ) -> Environment:\n        \"\"\"Create and configure the Jinja2 environment.\"\"\"\n\n        # Set up loader\n        if loader is not None:\n            env_loader = loader\n        elif template_dirs is not None or template_dict is not None:\n            loaders = []\n\n            # Add filesystem loader if template directories provided\n            if template_dirs is not None:\n                if isinstance(template_dirs, (str, Path)):\n                    template_dirs = [template_dirs]\n                str_dirs = [str(d) for d in template_dirs]\n                loaders.append(FileSystemLoader(str_dirs))\n\n            # Add dictionary loader if template dict provided\n            if template_dict is not None:\n                loaders.append(DictLoader(template_dict))\n\n            # Use single loader or choice loader\n            if len(loaders) == 1:\n                env_loader = loaders[0]\n            else:\n                from jinja2 import ChoiceLoader\n                env_loader = ChoiceLoader(loaders)\n        else:\n            # No templates configured - create empty dict loader\n            env_loader = DictLoader({})\n\n        # Set up bytecode cache\n        bytecode_cache = None\n        if cache_dir is not None:\n            cache_path = Path(cache_dir)\n            cache_path.mkdir(parents=True, exist_ok=True)\n            bytecode_cache = FileSystemBytecodeCache(directory=str(cache_path))\n\n        # Set up autoescaping\n        autoescape = select_autoescape(\n            enabled_extensions=autoescape_extensions,\n            disabled_extensions=no_autoescape_extensions,\n            default_for_string=autoescape_default,\n        )\n\n        # Create environment\n        env = Environment(\n            loader=env_loader,\n            autoescape=autoescape,\n            bytecode_cache=bytecode_cache,\n            **env_kwargs,\n        )\n\n        return env\n\n    @property\n    def environment(self) -> Environment:\n        \"\"\"Get the underlying Jinja2 environment.\"\"\"\n        return self._env\n\n    def render_template(self, template_name: str, **context: Any) -> str:\n        \"\"\"Render a template by name with the given context.\n\n        Args:\n            template_name: Name of the template to render.\n            **context: Template context variables.\n\n        Returns:\n            Rendered template as string.\n\n        Raises:\n            TemplateNotFound: If the template doesn't exist.\n        \"\"\"\n        template = self._env.get_template(template_name)\n        return template.render(**context)\n\n    def render_string(self, template_string: str, **context: Any) -> str:\n        \"\"\"Render a template string with the given context.\n\n        Args:\n            template_string: Template content as string.\n            **context: Template context variables.\n\n        Returns:\n            Rendered template as string.\n        \"\"\"\n        template = self._env.from_string(template_string)\n        return template.render(**context)\n\n    def render_template_safe(\n        self,\n        template_name: str,\n        fallback_template: Optional[str] = None,\n        **context: Any,\n    ) -> str:\n        \"\"\"Render a template with fallback handling.\n\n        Args:\n            template_name: Name of the template to render.\n            fallback_template: Optional fallback template string if the\n                             named template is not found.\n            **context: Template context variables.\n\n        Returns:\n            Rendered template as string, or fallback if template not found.\n\n        Raises:\n            TemplateNotFound: If both the named template and fallback are unavailable.\n        \"\"\"\n        try:\n            return self.render_template(template_name, **context)\n        except TemplateNotFound:\n            if fallback_template is not None:\n                return self.render_string(fallback_template, **context)\n            raise\n\n    def add_template(self, name: str, content: str) -> None:\n        \"\"\"Add a template to the engine's dictionary loader.\n\n        Note: This only works if the engine was configured with a DictLoader\n        as part of its loader chain.\n\n        Args:\n            name: Template name.\n            content: Template content.\n        \"\"\"\n        # This is a limitation - we can't easily add templates to an existing\n        # FileSystemLoader. For runtime template addition, users should use\n        # DictLoader or implement a custom loader.\n        loader = self._env.loader\n\n        # Direct DictLoader\n        if isinstance(loader, DictLoader):\n            cast(Dict[str, str], loader.mapping)[name] = content\n            return\n\n        # ChoiceLoader that may contain a DictLoader\n        # Avoid importing ChoiceLoader at module top to keep deps minimal\n        if hasattr(loader, 'loaders') and isinstance(getattr(loader, 'loaders'), (list, tuple)):\n            for sub in loader.loaders:  # type: ignore[attr-defined]\n                if isinstance(sub, DictLoader):\n                    cast(Dict[str, str], sub.mapping)[name] = content\n                    return\n\n        raise NotImplementedError(\n            \"Adding templates at runtime is only supported with DictLoader. \"\n            \"Initialize the engine with template_dict or include a DictLoader in the loader chain.\"\n        )\n\n    def template_exists(self, template_name: str) -> bool:\n        \"\"\"Check if a template exists.\n\n        Args:\n            template_name: Name of the template to check.\n\n        Returns:\n            True if template exists, False otherwise.\n        \"\"\"\n        try:\n            self._env.get_template(template_name)\n            return True\n        except TemplateNotFound:\n            return False\n\n    def list_templates(self) -> List[str]:\n        \"\"\"List all available template names.\n\n        Returns:\n            List of template names.\n        \"\"\"\n        return self._env.list_templates()\n\ndef create_simple_engine(\n    template_dir: Optional[Union[str, Path]] = None,\n    cache_dir: Optional[Union[str, Path]] = None,\n) -> TemplateEngine:\n    \"\"\"Create a simple template engine with common defaults.\n\n    Args:\n        template_dir: Directory containing templates. If None, only string\n                     templates can be rendered.\n        cache_dir: Directory for bytecode cache. If None, no caching is used.\n\n    Returns:\n        Configured TemplateEngine instance.\n    \"\"\"\n    return TemplateEngine(\n        template_dirs=template_dir,\n        cache_dir=cache_dir,\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    )\n\ndef create_string_engine(templates: Optional[Dict[str, str]] = None) -> TemplateEngine:\n    \"\"\"Create a template engine that works only with string templates.\n\n    Args:\n        templates: Optional dictionary of template name -> template content.\n\n    Returns:\n        Configured TemplateEngine instance.\n    \"\"\"\n    return TemplateEngine(\n        template_dict=templates or {},\n        autoescape_extensions=[\"html\", \"xml\"],\n        no_autoescape_extensions=[\"md\", \"txt\"],\n        autoescape_default=False,\n    ) |
3 | pipeline.ps1 | {1255 110 0.0972 5.1962} | 1308 | # 1. Import all modules\nImport-Module .\\CleanPayloadGenerator.psm1\nImport-Module .\\TokenCounterIntegration.psm1\n\n# 2. Generate clean, schema-validated payload\n$generator = New-CleanPayloadGenerator -SchemaDirectory \"schemas\" -TemplateDirectory \"templates\"\n$payload = $generator.GenerateWhitespacePayload(50, 1, 2, 100)\n\n# 3. Validate schema compliance\nWrite-Host \"Schema valid: $($payload.SchemaValid)\"\nWrite-Host \"Payload efficiency: $([Math]::Round((1-$payload.OverheadRatio)*100,1))%\"\n\n# 4. Test with CLI tokenizers\n$proxyResult = Test-TokenProxy -PayloadExperiment $payload -ProxyCharacter ' ' -TiktokenPath \"tiktoken.exe\" -DeepSeekPath \"ds-tokenanalysis.exe\"\n\n# 5. Analyze results\nWrite-Host \"🎯 PROXY VALIDATION RESULTS:\"\nWrite-Host \"   Character: '$($proxyResult.ProxyCharacter)'\"\nWrite-Host \"   Total chars: $($proxyResult.ProxyCharacterCount)\"\nWrite-Host \"   Average ratio: $($proxyResult.RecommendedProxyRatio.ToString('F4')) tokens/char\"\nWrite-Host \"   Consistency: $($proxyResult.ProxyAccuracy.ConsistencyScore.ToString('F3'))/1.0\"\n\n# 6. Use for boundary prediction\n$targetTokens = 4000\n$estimatedChars = [int]($targetTokens / $proxyResult.RecommendedProxyRatio)\nWrite-Host \"To get ~$targetTokens tokens, use ~$estimatedChars proxy characters\" |
4 | templates/simple-integer.j2 | {201 35 0.1990 4.5838} | 208 | {# Simple Integer Payload Template - Clean JSON for LLM consumption #}\n{\n  {%- for line in payload_lines %}\n  \"{{ line.number }}\": {{ line.content }}{% if not loop.last %},{% endif %}\n  {%- endfor %}\n} |
