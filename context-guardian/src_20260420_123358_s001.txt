idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | copilot_sensor.py | {3459 293 0.2405 4.7824} | 3694 | \"\"\"\nSensor A — Wire interceptor.\nmitmproxy addon that captures every Copilot /chat/completions request + SSE response.\n\nUsage:\n    SENSOR_SESSION_ID=my-session mitmproxy --mode regular --listen-port 8888 -s copilot_sensor.py\n\nVS Code settings (or launch env):\n    \"http.proxy\": \"http://localhost:8888\"\n    \"http.proxyStrictSSL\": false\n\"\"\"\nimport json\nimport os\nimport time\nfrom datetime import datetime, timezone\nfrom mitmproxy import http\n\nOUTPUT_DIR = os.environ.get(\"SENSOR_OUTPUT\", os.path.expanduser(\"~/.copilot-sensor\"))\nos.makedirs(OUTPUT_DIR, exist_ok=True)\n\nSESSION_ID = os.environ.get(\"SENSOR_SESSION_ID\", str(int(time.time())))\n_log = open(os.path.join(OUTPUT_DIR, f\"wire-{SESSION_ID}.ndjson\"), \"a\", buffering=1)\n\nTARGET_HOST = \"api.githubcopilot.com\"\nTARGET_PATH = \"/chat/completions\"\n\ndef _write(record: dict) -> None:\n    _log.write(json.dumps(record) + \"\\n\")\n\ndef _is_target(flow: http.HTTPFlow) -> bool:\n    return (\n        TARGET_HOST in flow.request.pretty_host\n        and TARGET_PATH in flow.request.path\n    )\n\ndef request(flow: http.HTTPFlow) -> None:\n    if not _is_target(flow):\n        return\n\n    try:\n        body = json.loads(flow.request.content)\n    except Exception:\n        body = None\n\n    messages = body.get(\"messages\", []) if body else []\n    tool_results = [m for m in messages if m.get(\"role\") == \"tool\"]\n\n    _write({\n        \"event\": \"request\",\n        \"timestamp\": datetime.now(timezone.utc).isoformat(),\n        \"sessionId\": SESSION_ID,\n        \"model\": body.get(\"model\") if body else None,\n        \"messageCount\": len(messages),\n        \"toolResultCount\": len(tool_results),\n        \"toolResultTotalBytes\": sum(len(str(m.get(\"content\", \"\"))) for m in tool_results),\n        \"requestBodyBytes\": len(flow.request.content),\n        \"messages\": [\n            {\n                \"role\": m.get(\"role\"),\n                \"contentLength\": len(str(m.get(\"content\", \"\"))),\n                \"hasToolCalls\": \"tool_calls\" in m,\n                \"isToolResult\": m.get(\"role\") == \"tool\",\n                \"toolName\": m.get(\"name\") if m.get(\"role\") == \"tool\" else None,\n            }\n            for m in messages\n        ],\n    })\n\ndef response(flow: http.HTTPFlow) -> None:\n    if not _is_target(flow):\n        return\n\n    content = flow.response.content.decode(\"utf-8\", errors=\"replace\")\n    sse_lines = [l for l in content.splitlines() if l.startswith(\"data: \")]\n\n    usage_chunk = None\n    delta_count = 0\n\n    for line in sse_lines:\n        data = line[6:]\n        if data == \"[DONE]\":\n            continue\n        try:\n            chunk = json.loads(data)\n            delta_count += 1\n            if chunk.get(\"usage\"):\n                usage_chunk = chunk[\"usage\"]\n        except Exception:\n            pass\n\n    _write({\n        \"event\": \"response\",\n        \"timestamp\": datetime.now(timezone.utc).isoformat(),\n        \"sessionId\": SESSION_ID,\n        \"statusCode\": flow.response.status_code,\n        \"responseBodyBytes\": len(flow.response.content),\n        \"sseDeltaCount\": delta_count,\n        \"promptTokens\": usage_chunk.get(\"prompt_tokens\") if usage_chunk else None,\n        \"completionTokens\": usage_chunk.get(\"completion_tokens\") if usage_chunk else None,\n        \"totalTokens\": usage_chunk.get(\"total_tokens\") if usage_chunk else None,\n        # Some models return prompt_tokens_details with category-level breakdown\n        \"promptTokensDetails\": usage_chunk.get(\"prompt_tokens_details\") if usage_chunk else None,\n    }) |
1 | correlate.py | {3429 320 0.2689 4.6226} | 3629 | \"\"\"\nCorrelate wire (Sensor A) and surface (Sensor B) NDJSON logs by turn index.\nProduces a merged CSV for analysis.\n\nUsage:\n    python correlate.py wire-SESSION.ndjson surface-SESSION.ndjson\n\"\"\"\nimport json\nimport sys\nimport csv\nfrom pathlib import Path\n\ndef load_ndjson(path: str) -> list[dict]:\n    records = []\n    with open(path, encoding=\"utf-8\") as f:\n        for line in f:\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                records.append(json.loads(line))\n            except json.JSONDecodeError:\n                pass\n    return records\n\ndef align(wire_path: str, surface_path: str, out_path: str) -> None:\n    wire_all = load_ndjson(wire_path)\n    surface_all = load_ndjson(surface_path)\n\n    # Pull data turns only (skip _meta events)\n    wire_responses = [r for r in wire_all if r.get(\"event\") == \"response\"]\n    surface_turns = [r for r in surface_all if \"turnIndex\" in r and not r.get(\"_meta\")]\n\n    n = min(len(wire_responses), len(surface_turns))\n    if n == 0:\n        print(\"No matching turns found — check sessionId and file paths.\")\n        return\n\n    rows = []\n    for i in range(n):\n        w = wire_responses[i]\n        s = surface_turns[i]\n\n        tool_msgs = [m for m in w.get(\"messages\", []) if m.get(\"isToolResult\")]\n        tool_bytes = sum(m.get(\"contentLength\", 0) for m in tool_msgs)\n\n        rows.append({\n            \"turnIndex\":             i,\n            \"timestamp\":             s.get(\"timestamp\", w.get(\"timestamp\")),\n            # Wire ground truth\n            \"wire_messageCount\":     w.get(\"messageCount\"),\n            \"wire_toolResultCount\":  w.get(\"toolResultCount\", len(tool_msgs)),\n            \"wire_toolResultBytes\":  w.get(\"toolResultTotalBytes\", tool_bytes),\n            \"wire_promptTokens\":     w.get(\"promptTokens\"),\n            \"wire_completionTokens\": w.get(\"completionTokens\"),\n            # Surface widget data\n            \"surf_promptTokens\":     s.get(\"promptTokens\"),\n            \"surf_totalPct\":         s.get(\"totalPercentage\"),\n            \"surf_toolsPct\":         s.get(\"toolsPct\"),\n            \"surf_filesPct\":         s.get(\"filesPct\"),\n            \"surf_compositeRisk\":    s.get(\"compositeRisk\"),\n            # Delta — widget vs wire\n            \"token_delta\":           (s.get(\"promptTokens\") or 0) - (w.get(\"promptTokens\") or 0),\n        })\n\n    with open(out_path, \"w\", newline=\"\", encoding=\"utf-8\") as f:\n        writer = csv.DictWriter(f, fieldnames=rows[0].keys())\n        writer.writeheader()\n        writer.writerows(rows)\n\n    print(f\"Written {n} turns → {out_path}\")\n\n    # Quick summary\n    critical = [r for r in rows if r[\"surf_compositeRisk\"] == \"critical\"]\n    watch = [r for r in rows if r[\"surf_compositeRisk\"] == \"watch\"]\n    print(f\"  compositeRisk: ok={n - len(watch) - len(critical)}  watch={len(watch)}  critical={len(critical)}\")\n    if rows:\n        max_tools = max(rows, key=lambda r: r[\"wire_toolResultCount\"] or 0)\n        print(f\"  peak tool results: turn {max_tools['turnIndex']} — {max_tools['wire_toolResultCount']} msgs, {max_tools['wire_toolResultBytes']} bytes\")\n\nif __name__ == \"__main__\":\n    if len(sys.argv) < 3:\n        print(\"Usage: correlate.py <wire.ndjson> <surface.ndjson> [out.csv]\")\n        sys.exit(1)\n    wire_f, surface_f = sys.argv[1], sys.argv[2]\n    out_f = sys.argv[3] if len(sys.argv) > 3 else \"correlated.csv\"\n    align(wire_f, surface_f, out_f) |
2 | extension/scripts/validate-structure.ts | {4280 535 0.2418 4.8111} | 4469 | #!/usr/bin/env node\n/**\n * Pre-compilation structural validator\n * Checks TypeScript files for balanced braces, brackets, and parentheses\n * before the TypeScript compiler runs.\n */\n\nimport * as fs from 'fs';\nimport * as path from 'path';\n\n// Inline the detectUnbalancedBraces function so this script is standalone\nfunction detectUnbalancedBraces(code: string): { balanced: boolean; message?: string } {\n  let paren = 0;\n  let brace = 0;\n  let brack = 0;\n\n  let inSingleQuote = false;\n  let inDoubleQuote = false;\n  let inTemplateString = false;\n  let inBlockComment = false;\n  let inLineComment = false;\n\n  for (let i = 0; i < code.length; i++) {\n    const ch = code[i];\n    const prev = i > 0 ? code[i-1] : '';\n    const next = i < code.length - 1 ? code[i+1] : '';\n\n    // Skip escaped characters\n    if (prev === '\\\\') continue;\n\n    // Handle comments\n    if (!inSingleQuote && !inDoubleQuote && !inTemplateString) {\n      if (ch === '/' && next === '/') {\n        inLineComment = true;\n        i++; // skip next\n        continue;\n      }\n      if (ch === '/' && next === '*') {\n        inBlockComment = true;\n        i++; // skip next\n        continue;\n      }\n      if (inBlockComment && ch === '*' && next === '/') {\n        inBlockComment = false;\n        i++; // skip next\n        continue;\n      }\n      if (inLineComment && ch === '\\n') {\n        inLineComment = false;\n        continue;\n      }\n    }\n\n    // Skip everything in comments\n    if (inLineComment || inBlockComment) continue;\n\n    // Handle strings\n    if (!inSingleQuote && !inDoubleQuote && !inTemplateString) {\n      if (ch === '\"') { inDoubleQuote = true; continue; }\n      if (ch === \"'\") { inSingleQuote = true; continue; }\n      if (ch === '`') { inTemplateString = true; continue; }\n    } else {\n      if (inDoubleQuote && ch === '\"') { inDoubleQuote = false; continue; }\n      if (inSingleQuote && ch === \"'\") { inSingleQuote = false; continue; }\n      if (inTemplateString && ch === '`') { inTemplateString = false; continue; }\n      // Inside string, skip delimiter counting\n      continue;\n    }\n\n    // Count delimiters (only outside strings/comments)\n    switch (ch) {\n      case '(': paren++; break;\n      case ')': paren--; break;\n      case '{': brace++; break;\n      case '}': brace--; break;\n      case '[': brack++; break;\n      case ']': brack--; break;\n    }\n  }\n\n  const unbalanced = paren !== 0 || brace !== 0 || brack !== 0;\n  const unterminatedString = inSingleQuote || inDoubleQuote || inTemplateString;\n\n  if (unbalanced) {\n    return {\n      balanced: false,\n      message: `Unbalanced delimiters: { ${brace} } ( ${paren} ) [ ${brack} ]`\n    };\n  }\n\n  if (unterminatedString) {\n    return {\n      balanced: false,\n      message: \"Unterminated string literal\"\n    };\n  }\n\n  return { balanced: true };\n}\n\n// Main validation logic\nconst srcDir = path.join(__dirname, '..', 'src');\n\nconst filesToValidate = [\n  'src/extension.ts',\n  'src/toolbelt/copilot-toolbelt.ts',\n  'src/toolbelt/jso-blackbelt.ts',\n  'src/toolbelt/power-tools.ts',\n  'src/toolbelt/safe-shell.ts',\n  'src/toolbelt/tool-ready.ts',\n  'src/toolbelt/structural-linter.ts',\n  'analysis/session-telemetry.ps1',\n  'analysis/signal-processing.psm1',\n  'analysis/decay-math.psm1'\n];\n\nconsole.log('🔍 Running structural validation...\\n');\n\nlet hasErrors = false;\n\nfor (const file of filesToValidate) {\n  const filePath = path.join(__dirname, '..', file);\n\n  if (!fs.existsSync(filePath)) {\n    console.log(`⏭️  Skipped (not found): ${file}`);\n    continue;\n  }\n\n  try {\n    const sourceCode = fs.readFileSync(filePath, 'utf8');\n    const result = detectUnbalancedBraces(sourceCode);\n\n    if (!result.balanced) {\n      console.error(`❌ ${file}`);\n      console.error(`   ${result.message}\\n`);\n      hasErrors = true;\n    } else {\n      console.log(`✅ ${file}`);\n    }\n  } catch (error: any) {\n    console.error(`⚠️  Error reading ${file}: ${error.message}`);\n    hasErrors = true;\n  }\n}\n\nconsole.log('\\n' + '='.repeat(60));\n\nif (hasErrors) {\n  console.error('❌ Structural validation FAILED');\n  console.error('   Fix the errors above before compiling.\\n');\n  process.exit(1);\n} else {\n  console.log('✅ Structural validation PASSED');\n  console.log('   Proceeding with TypeScript compilation...\\n');\n  process.exit(0);\n} |
3 | extension/src/copilot-decay.d.ts | {632 61 0.1551 4.7620} | 666 | import { PromptTokenDetail } from \"./turn_records\";\n\nexport interface SessionRecord {\n  sessionId: string;\n  turnIndex: number;\n  timestamp: string; // ISO 8601\n  promptTokens: number;\n  completionTokens: number;\n  totalContextWindow: number;\n  usedTokens: number;\n  totalPercentage: number;\n  outputBufferPercentage?: number;\n  promptTokenDetails: PromptTokenDetail[];\n  toolsPct: number;\n  filesPct: number;\n  compositeRisk: \"ok\" | \"watch\" | \"congested\" | \"critical\";\n  modelId?: string;\n  requestText?: string;\n  wasSummarized?: boolean;\n}\n\nexport interface SessionTelemetryOptions {\n  outputDir?: string;\n  sessionId?: string;\n} |
4 | extension/src/copilot-decay.ts | {122 16 0.1230 4.2608} | 133 | export * from \"./extension\";\nexport * from \"./sessionTelemetry\";\nexport * from \"./turn_records\";\nexport * from \"./writer\"; |
5 | extension/src/extension.ts | {1426 121 0.1985 4.7839} | 1497 | import * as path from \"path\";\nimport * as vscode from \"vscode\";\nimport { SessionTelemetry } from \"./sessionTelemetry\";\n\nlet telemetry: SessionTelemetry | undefined;\n\nexport function activate(context: vscode.ExtensionContext) {\n  const outputDir = context.globalStorageUri?.fsPath ?? path.join(__dirname, \"..\", \"..\", \"output\");\n  telemetry = new SessionTelemetry(outputDir);\n\n  context.subscriptions.push(\n    vscode.commands.registerCommand(\"copilot-decay.begin\", () => {\n      try {\n        telemetry?.begin();\n      } catch (err) {\n        vscode.window.showErrorMessage(`copilot-decay.begin failed: ${err}`);\n      }\n    }),\n\n    vscode.commands.registerCommand(\"copilot-decay.end\", () => {\n      try {\n        telemetry?.end(\"user\");\n      } catch (err) {\n        vscode.window.showErrorMessage(`copilot-decay.end failed: ${err}`);\n      }\n    }),\n\n    vscode.commands.registerCommand(\"copilot-decay.openLatest\", async () => {\n      const filePath = telemetry?.path;\n      if (!filePath) {\n        vscode.window.showWarningMessage(\"No copilot decay session file found yet.\");\n        return;\n      }\n      const doc = await vscode.workspace.openTextDocument(filePath);\n      await vscode.window.showTextDocument(doc, { preview: false });\n    }),\n  );\n\n  if (vscode.workspace.getConfiguration(\"copilotDecay\").get(\"autoBegin\", false)) {\n    telemetry?.begin();\n  }\n}\n\nexport function deactivate() {\n  telemetry?.dispose();\n} |
6 | extension/src/sessionTelemetry.ts | {4080 376 0.2279 4.7420} | 4256 | import * as vscode from \"vscode\";\nimport { TurnRecord } from \"./turn_records\";\nimport { DurableWriter } from \"./writer\";\n\nexport class SessionTelemetry implements vscode.Disposable {\n  private writer: DurableWriter;\n  private sessionId: string;\n  private startedAt?: string;\n  private disposed = false;\n  private sessionFilePath?: string;\n\n  constructor(outputDir?: string, sessionId?: string) {\n    this.sessionId = sessionId || `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;\n    const outDir = outputDir || (vscode.workspace.workspaceFolders?.[0]?.uri.fsPath ?? process.cwd());\n    this.writer = new DurableWriter(outDir, this.sessionId);\n    this.sessionFilePath = this.writer.path;\n  }\n\n  public get path(): string | undefined {\n    return this.sessionFilePath;\n  }\n\n  public begin(): void {\n    if (this.startedAt) {\n      vscode.window.showInformationMessage(\"Copilot decay telemetry already started\");\n      return;\n    }\n\n    this.startedAt = new Date().toISOString();\n    this.writer.writeMeta(\"session.begin\", {\n      sessionId: this.sessionId,\n      startedAt: this.startedAt,\n    });\n\n    this.tryAttachHistoryListener();\n    vscode.window.showInformationMessage(`Copilot decay telemetry started (${this.sessionId})`);\n  }\n\n  public end(reason: \"user\" | \"process\" = \"user\"): void {\n    if (!this.startedAt) {\n      vscode.window.showWarningMessage(\"Copilot decay telemetry is not running.\");\n      return;\n    }\n\n    this.writer.writeMeta(\"session.end\", {\n      sessionId: this.sessionId,\n      endedAt: new Date().toISOString(),\n      reason,\n    });\n    this.writer.flush();\n    this.writer.close();\n    this.startedAt = undefined;\n    vscode.window.showInformationMessage(`Copilot decay telemetry stopped (${reason})`);\n  }\n\n  public recordTurn(record: TurnRecord): void {\n    if (this.disposed) {\n      throw new Error(\"SessionTelemetry is disposed\");\n    }\n    const enriched = {\n      ...record,\n      sessionId: this.sessionId,\n    };\n    this.writer.write(enriched);\n  }\n\n  private tryAttachHistoryListener(): void {\n    const chat = (vscode as any).chat;\n    if (!chat || typeof chat.onDidChangeChatSessions !== \"function\") {\n      return;\n    }\n\n    const listener = chat.onDidChangeChatSessions((event: any) => {\n      const session = event.sessions?.[0];\n      if (!session || !session.history) {\n        return;\n      }\n      const lastTurn = session.history[session.history.length - 1];\n      if (!lastTurn?.result?.metadata?.usage) {\n        return;\n      }\n\n      const usage = lastTurn.result.metadata.usage;\n      const promptTokenDetails = usage.promptTokenDetails || [];\n      const turn: TurnRecord = {\n        sessionId: this.sessionId,\n        turnIndex: lastTurn.index ?? -1,\n        timestamp: new Date().toISOString(),\n        promptTokens: usage.promptTokens ?? 0,\n        completionTokens: usage.completionTokens ?? 0,\n        totalContextWindow: usage.totalContextWindow ?? 0,\n        usedTokens: usage.usedTokens ?? 0,\n        totalPercentage: usage.percentage ?? 0,\n        outputBufferPercentage: usage.outputBufferPercentage ?? undefined,\n        promptTokenDetails,\n        toolsPct: usage.toolsPct ?? 0,\n        filesPct: usage.filesPct ?? 0,\n        compositeRisk: this.computeRisk(usage.toolsPct ?? 0, usage.filesPct ?? 0),\n        modelId: usage.modelId,\n        requestText: typeof lastTurn.requestText === \"string\" ? lastTurn.requestText.slice(0, 200) : undefined,\n        wasSummarized: !!lastTurn.wasSummarized,\n      };\n      this.recordTurn(turn);\n    });\n\n    // keep disposable in context\n    vscode.Disposable.from(listener);\n  }\n\n  private computeRisk(toolsPct: number, filesPct: number): \"ok\" | \"watch\" | \"congested\" | \"critical\" {\n    const sum = toolsPct + filesPct;\n    if (sum >= 50) {\n      return \"critical\";\n    }\n    if (sum >= 35) {\n      return \"congested\";\n    }\n    if (sum >= 20) {\n      return \"watch\";\n    }\n    return \"ok\";\n  }\n\n  public dispose(): void {\n    if (this.disposed) {\n      return;\n    }\n    this.disposed = true;\n    if (this.startedAt) {\n      this.end(\"process\");\n    }\n  }\n} |
7 | extension/src/turn_records.ts | {827 91 0.1753 4.7801} | 862 | export interface PromptTokenDetail {\n  category: string;\n  label: string;\n  percentageOfPrompt: number;\n  percentageOfWindow: number; // derived: (pctOfPrompt / 100) * totalPct\n}\n\nexport interface TurnRecord {\n  // identity\n  sessionId: string;\n  turnIndex: number;\n  timestamp: string; // ISO 8601\n\n  // token accounting\n  promptTokens: number;\n  completionTokens: number;\n  totalContextWindow: number;\n  usedTokens: number;\n  totalPercentage: number;\n  outputBufferPercentage?: number;\n\n  // category breakdown — the money field\n  promptTokenDetails: PromptTokenDetail[];\n\n  // derived health signal\n  toolsPct: number;\n  filesPct: number;\n  compositeRisk: 'ok' | 'watch' | 'congested' | 'critical';\n\n  // annotations\n  modelId?: string;\n  requestText?: string; // first 200 chars of user message\n  wasSummarized?: boolean;\n} |
8 | extension/src/writer.ts | {885 104 0.1921 4.9361} | 922 | import * as fs from 'fs';\nimport * as path from 'path';\n\nexport class DurableWriter {\n  private fd: number;\n  private filePath: string;\n\n  constructor(outputDir: string, sessionId: string) {\n    const filename = `copilot-ctx-${sessionId}-${Date.now()}.ndjson`;\n    this.filePath = path.join(outputDir, filename);\n    // O_APPEND | O_CREAT — record survives process kill\n    this.fd = fs.openSync(this.filePath, 'a');\n  }\n\n  write(record: object): void {\n    const line = JSON.stringify(record) + '\\\\n';\n    fs.writeSync(this.fd, line); // synchronous — no buffering\n  }\n\n  writeMeta(event: string, data: object = {}): void {\n    this.write({ _meta: true, event, timestamp: new Date().toISOString(), ...data });\n  }\n\n  flush(): void { fs.fsyncSync(this.fd); }\n\n  close(): void {\n    fs.fsyncSync(this.fd);\n    fs.closeSync(this.fd);\n  }\n\n  get path(): string { return this.filePath; }\n} |
