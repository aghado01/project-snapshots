idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
11 | storage.psm1 | {19534 1827 0.2280 4.9661} | 20116 | # storage.psm1 — copilot-decay archive layer (standalone, composite JSO engine)\n#\n# This module is a fully inlined, dependency-free composite of required primitives and utilities\n# from rs.core.jso-engine and Jso-Utils.Core, per the copilot-decay spec.\n#\n# No Import-Module calls are used. The public API is thin and focused.\n\n# --- Internal helpers and constants ---\n\n# Measures the ratio of structure to substance.\n# Models optimized for approval ratings tend to score well here.\nfunction Get-ScaffoldFraction\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][object[]]$Session)\n    if (-not $Session -or $Session.Count -eq 0) { return 0 }\n    $structure = ($Session | Where-Object { $_.type -eq 'structure' }).Count\n    $substance = ($Session | Where-Object { $_.type -eq 'substance' }).Count\n    if ($substance -eq 0) { return 1 }\n    return [math]::Round($structure / ($structure + $substance), 3)\n}\n\n# Below this distance, the closing question is statistically the same as a prior one.\n# The model has forgotten it already asked.\n# $HammingThreshold is a conceptual threshold used in similarity checks.\n$HammingThreshold = 3\n\n# The one field that's direct evidence rather than inference. Everything else is a proxy.\n# Example TurnRecord: @{ wasSummarized = $true; ... }\n\n# --- Hashing primitives ---\n\nclass RabinKarpHash\n{\n    [uint32]$Hash\n    [int]$WindowSize\n    [uint32]$Base = 257\n    [uint32]$Mod = 1000000007\n    [uint32]$BasePower\n\n    RabinKarpHash([int]$windowSize)\n    {\n        $this.WindowSize = $windowSize\n        $this.Hash = 0\n        $this.BasePower = 1\n        for ($i = 0; $i -lt $windowSize; $i++)\n        {\n            $this.BasePower = ($this.BasePower * $this.Base) % $this.Mod\n        }\n    }\n    [uint32] AddChar([char]$c)\n    {\n        $this.Hash = (($this.Hash * $this.Base) + [int]$c) % $this.Mod\n        return $this.Hash\n    }\n    [uint32] RemoveChar([char]$c)\n    {\n        $removal = ([long][int]$c * $this.BasePower) % $this.Mod\n        $this.Hash = ($this.Hash + $this.Mod - $removal) % $this.Mod\n        return $this.Hash\n    }\n    [uint32] RollWindow([char]$oldChar, [char]$newChar)\n    {\n        $this.RemoveChar($oldChar) | Out-Null\n        $this.AddChar($newChar) | Out-Null\n        return $this.Hash\n    }\n    [void] Reset() { $this.Hash = 0 }\n}\n\nfunction Get-ContentHash\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][AllowEmptyString()][string]$Content, [int]$WindowSize = 32)\n    if ([string]::IsNullOrWhiteSpace($Content)) { return 0L }\n    $roller = [RabinKarpHash]::new($WindowSize)\n    foreach ($char in $Content.ToCharArray()) { [void]$roller.AddChar($char) }\n    return [long]$roller.Hash\n}\n\n# --- Canonical JSON ---\n\nfunction Normalize-Props\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][object]$obj)\n    if ($obj -is [hashtable] -or $obj -is [pscustomobject])\n    {\n        $ordered = [ordered]@{}\n        foreach ($p in ($obj.PSObject.Properties.Name | Sort-Object))\n        {\n            $ordered[$p] = Normalize-Props $obj.$p\n        }\n        return [pscustomobject]$ordered\n    }\n    elseif ($obj -is [System.Collections.IEnumerable] -and -not ($obj -is [string]))\n    {\n        return @($obj | ForEach-Object { Normalize-Props $_ })\n    }\n    else\n    {\n        return $obj\n    }\n}\n\nfunction ConvertTo-CanonicalJson\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, ValueFromPipeline)] [object]$InputObject, [int]$Depth = 64)\n    process\n    {\n        $canon = Normalize-Props $InputObject\n        return (ConvertTo-Json -InputObject $canon -Depth $Depth)\n    }\n}\n\n# --- Bloom filter ---\n\nfunction New-BloomFilter\n{\n    [CmdletBinding()]\n    param([int]$ExpectedItems = 10000, [double]$FalsePositiveRate = 0.01)\n    $m = [Math]::Ceiling(($ExpectedItems * [Math]::Log($FalsePositiveRate)) / [Math]::Log(1 / [Math]::Pow(2, [Math]::Log(2))))\n    $k = [Math]::Ceiling(($m / $ExpectedItems) * [Math]::Log(2))\n    return [PSCustomObject]@{\n        PSTypeName        = 'Jso.BloomFilter'\n        BitArray          = [System.Collections.BitArray]::new([int]$m)\n        Size              = [int]$m\n        HashCount         = [int]$k\n        ItemCount         = 0\n        ExpectedItems     = $ExpectedItems\n        FalsePositiveRate = $FalsePositiveRate\n    }\n}\n\nfunction Get-BloomFilterHashes\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$Item, [Parameter(Mandatory)][int]$HashCount, [Parameter(Mandatory)][int]$Size)\n    $bytes = [Text.Encoding]::UTF8.GetBytes($Item)\n    [long]$hash1 = 2166136261\n    foreach ($byte in $bytes)\n    {\n        $hash1 = $hash1 -bxor $byte\n        $hash1 = ($hash1 * 16777619) % [Math]::Pow(2, 32)\n    }\n    [long]$hash2 = 5381\n    foreach ($byte in $bytes)\n    {\n        $hash2 = (($hash2 -shl 5) + $hash2) + $byte\n    }\n    $hashes = @()\n    for ($i = 0; $i -lt $HashCount; $i++)\n    {\n        $idx = [int]([Math]::Abs(($hash1 + $i * $hash2) % $Size))\n        $hashes += $idx\n    }\n    return $hashes\n}\n\nfunction Add-BloomFilterItem\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][PSCustomObject]$BloomFilter, [Parameter(Mandatory)][string]$Item)\n    $hashes = Get-BloomFilterHashes -Item $Item -HashCount $BloomFilter.HashCount -Size $BloomFilter.Size\n    foreach ($hash in $hashes) { $BloomFilter.BitArray[$hash] = $true }\n    $BloomFilter.ItemCount++\n}\n\nfunction Test-BloomFilterItem\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][PSCustomObject]$BloomFilter, [Parameter(Mandatory)][string]$Item)\n    $hashes = Get-BloomFilterHashes -Item $Item -HashCount $BloomFilter.HashCount -Size $BloomFilter.Size\n    foreach ($hash in $hashes) { if (-not $BloomFilter.BitArray[$hash]) { return $false } }\n    return $true\n}\n\n# --- JSONL enumerator ---\n\nclass JsonlEnumerator\n{\n    [string]$FilePath\n    [System.IO.StreamReader]$Reader\n    JsonlEnumerator([string]$filePath)\n    {\n        $this.FilePath = $filePath\n        $this.Reader = [System.IO.StreamReader]::new($filePath, [Text.Encoding]::UTF8)\n    }\n    [object] GetNext()\n    {\n        while (-not $this.Reader.EndOfStream)\n        {\n            $line = $this.Reader.ReadLine()\n            if ($null -eq $line) { break }\n            $line = $line.Trim()\n            if ($line.Length -eq 0) { continue }\n            try { return $line | ConvertFrom-Json } catch { continue }\n        }\n        return $null\n    }\n    [void] Dispose()\n    {\n        if ($this.Reader) { $this.Reader.Close(); $this.Reader.Dispose() }\n    }\n}\n\n# --- JSONL basic helpers ---\n\nfunction Write-JsonlRecords\n{\n    [CmdletBinding(SupportsShouldProcess)]\n    param([Parameter(Mandatory)][string]$Path, [Parameter(Mandatory, ValueFromPipeline)][object]$InputObject, [int]$Depth = 64)\n    begin\n    {\n        $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n        $dir = Split-Path -Path $Path -Parent\n        if ($dir -and -not (Test-Path -Path $dir)) { New-Item -ItemType Directory -Path $dir -Force | Out-Null }\n        $sw = [System.IO.StreamWriter]::new($Path, $false, [Text.UTF8Encoding]::new($false))\n    }\n    process\n    {\n        if ($PSCmdlet.ShouldProcess($Path, 'Write JSONL record'))\n        {\n            $line = ConvertTo-Json -InputObject $InputObject -Depth $Depth -Compress\n            $sw.WriteLine($line)\n        }\n    }\n    end\n    {\n        if ($sw) { $sw.Flush(); $sw.Close(); $sw.Dispose() }\n    }\n}\n\nfunction Read-JsonFile\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, Position = 0)][string]$Path, [ValidateSet('Auto', 'Json', 'JsonLines')][string]$Kind = 'Auto', [switch]$AsHashtable)\n    $Path = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n    if (-not (Test-Path -Path $Path)) { throw \"File not found: $Path\" }\n    $kind = if ($Kind -eq 'Auto') { if ($Path -match '\\.jsonl?$') { 'JsonLines' } else { 'Json' } } else { $Kind }\n    $raw = Get-Content -Path $Path -Raw -Encoding UTF8\n    if ($kind -eq 'JsonLines')\n    {\n        $lines = $raw -split \"`r?`n\" | Where-Object { $_.Trim().Length -gt 0 }\n        $result = [System.Collections.Generic.List[object]]::new()\n        foreach ($ln in $lines)\n        {\n            if ($AsHashtable) { $result.Add((ConvertFrom-Json -InputObject $ln -AsHashtable)) }\n            else { $result.Add((ConvertFrom-Json -InputObject $ln)) }\n        }\n        return , $result.ToArray()\n    }\n    else\n    {\n        if ($AsHashtable) { return ConvertFrom-Json -InputObject $raw -AsHashtable }\n        return ConvertFrom-Json -InputObject $raw\n    }\n}\n\nfunction ConvertTo-JsonLines\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, ValueFromPipeline)][object]$InputObject, [int]$Depth = 64)\n    begin { $lines = [System.Collections.Generic.List[string]]::new() }\n    process\n    {\n        if ($InputObject -is [array])\n        {\n            foreach ($item in $InputObject) { $lines.Add((ConvertTo-Json -InputObject $item -Depth $Depth -Compress)) }\n        }\n        else\n        {\n            $lines.Add((ConvertTo-Json -InputObject $InputObject -Depth $Depth -Compress))\n        }\n    }\n    end { return $lines -join \"`n\" }\n}\n\nfunction ConvertFrom-JsonLines\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, ValueFromPipeline)][string]$InputObject)\n    begin { $results = [System.Collections.Generic.List[object]]::new() }\n    process\n    {\n        $lines = $InputObject -split \"`r?`n\"\n        foreach ($line in $lines)\n        {\n            if ([string]::IsNullOrWhiteSpace($line)) { continue }\n            try { $results.Add(($line | ConvertFrom-Json)) } catch { Write-Warning \"Invalid JSON line: $line\" }\n        }\n    }\n    end { return $results.ToArray() }\n}\n\nfunction Test-JsonValid\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory, ValueFromPipeline)][string]$JsonString)\n    process\n    {\n        try { $null = ConvertFrom-Json -InputObject $JsonString -ErrorAction Stop; return $true } catch { return $false }\n    }\n}\n\n# --- Binary JSONL index ---\n\nfunction New-JsonlBinaryIndex\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$JsonlPath, [string]$IndexPath = $null)\n    if (-not (Test-Path $JsonlPath)) { throw \"JSONL file not found: $JsonlPath\" }\n    if (-not $IndexPath) { $IndexPath = \"$JsonlPath.idx\" }\n    $stream = [System.IO.FileStream]::new($IndexPath, [System.IO.FileMode]::Create, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)\n    $writer = [System.IO.BinaryWriter]::new($stream)\n    try\n    {\n        $writer.Write([byte[]]@(0x4A, 0x53, 0x4F, 0x49))\n        $writer.Write([int32]1)\n        $lineCountPos = $stream.Position\n        $writer.Write([int32]0)\n        $reader = [System.IO.StreamReader]::new($JsonlPath, [System.Text.Encoding]::UTF8)\n        $lineCount = 0\n        $byteOffset = 0\n        while ($null -ne ($line = $reader.ReadLine()))\n        {\n            if (-not [string]::IsNullOrWhiteSpace($line))\n            {\n                $writer.Write([int64]$byteOffset)\n                $lineCount++\n            }\n            $byteOffset += [System.Text.Encoding]::UTF8.GetByteCount($line) + 1\n        }\n        $reader.Close()\n        $stream.Position = $lineCountPos\n        $writer.Write([int32]$lineCount)\n    }\n    finally { $writer.Close(); $stream.Close() }\n}\n\nfunction Read-JsonlBinaryIndex\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$JsonlPath, [Parameter(Mandatory)][int]$LineIndex, [string]$IndexPath = $null)\n    if (-not $IndexPath) { $IndexPath = \"$JsonlPath.idx\" }\n    if (-not (Test-Path $IndexPath)) { throw \"Index not found: $IndexPath. Run New-JsonlBinaryIndex first.\" }\n    $indexStream = [System.IO.FileStream]::new($IndexPath, [System.IO.FileMode]::Open, [System.IO.FileAccess]::Read, [System.IO.FileShare]::Read)\n    $indexReader = [System.IO.BinaryReader]::new($indexStream)\n    try\n    {\n        $magic = $indexReader.ReadBytes(4)\n        if ($magic[0] -ne 0x4A -or $magic[1] -ne 0x53 -or $magic[2] -ne 0x4F -or $magic[3] -ne 0x49) { throw \"Invalid index file format\" }\n        [int]$indexVersion = $indexReader.ReadInt32()\n        $lineCount = $indexReader.ReadInt32()\n        if ($LineIndex -ge $lineCount) { throw \"Line index out of range\" }\n        $indexReader.BaseStream.Position = 12 + ($LineIndex * 8)\n        $offset = $indexReader.ReadInt64()\n        $indexReader.Close(); $indexStream.Close()\n        $fs = [System.IO.FileStream]::new($JsonlPath, [System.IO.FileMode]::Open, [System.IO.FileAccess]::Read, [System.IO.FileShare]::Read)\n        $fs.Position = $offset\n        $reader = [System.IO.StreamReader]::new($fs, [System.Text.Encoding]::UTF8)\n        $line = $reader.ReadLine()\n        $reader.Close(); $fs.Close()\n        return $line | ConvertFrom-Json\n    }\n    finally { $indexReader.Close(); $indexStream.Close() }\n}\n\n# --- Sharding class ---\n\nclass JsoSharding\n{\n    [int]$RecordsPerFile\n    [string]$NamingPattern\n    [int]$CurrentIndex\n    [int]$CurrentRecordCount\n\n    JsoSharding([int]$RecordsPerFile, [string]$NamingPattern)\n    {\n        $this.RecordsPerFile = $RecordsPerFile\n        $this.NamingPattern = $NamingPattern\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n    [string] GetShardPath([string]$BasePath)\n    {\n        $directory = [System.IO.Path]::GetDirectoryName($BasePath)\n        $nameWithoutExt = [System.IO.Path]::GetFileNameWithoutExtension($BasePath)\n        $extension = [System.IO.Path]::GetExtension($BasePath)\n        $fileName = $this.NamingPattern -f $nameWithoutExt, $this.CurrentIndex, $extension\n        return Join-Path $directory $fileName\n    }\n    [bool] ShouldCreateNewShard() { return $this.CurrentRecordCount -ge $this.RecordsPerFile }\n    [void] NextShard() { $this.CurrentIndex++; $this.CurrentRecordCount = 0 }\n    [void] RecordWritten() { $this.CurrentRecordCount++ }\n    [void] Reset() { $this.CurrentIndex = 1; $this.CurrentRecordCount = 0 }\n}\n\n# --- Deduplication helpers ---\n\nfunction Get-JsonlDeduplicated\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$InputPath, [string]$KeyProperty = 'id', [double]$FalsePositiveRate = 0.001, [int]$EstimatedItemCount = 10000)\n    if (-not (Test-Path $InputPath)) { throw \"Input file not found: $InputPath\" }\n    $lines = Get-Content -Path $InputPath -Encoding UTF8\n    $bloom = New-BloomFilter -ExpectedItems $EstimatedItemCount -FalsePositiveRate $FalsePositiveRate\n    $seen = [System.Collections.Generic.HashSet[string]]::new()\n    $uniqueLines = [System.Collections.Generic.List[string]]::new()\n    foreach ($line in $lines)\n    {\n        if ([string]::IsNullOrWhiteSpace($line)) { continue }\n        try\n        {\n            $json = $line | ConvertFrom-Json\n            $key = if ($json.PSObject.Properties.Name -contains $KeyProperty) { [string]$json.$KeyProperty } else { $line }\n            if (-not $key) { $key = $line }\n            if (Test-BloomFilterItem -BloomFilter $bloom -Item $key)\n            {\n                if ($seen.Contains($key)) { continue }\n            }\n            Add-BloomFilterItem -BloomFilter $bloom -Item $key\n            $seen.Add($key) | Out-Null\n            $uniqueLines.Add($line)\n        }\n        catch\n        {\n            # invalid JSON line, preserve or skip? skip for now\n            continue\n        }\n    }\n    return [pscustomobject]@{\n        UniqueLines      = $uniqueLines\n        ProcessedLines   = $lines.Count\n        DuplicatesFound  = $lines.Count - $uniqueLines.Count\n        BloomFilterStats = @{ Size = $bloom.Size; ItemCount = $bloom.ItemCount; HashCount = $bloom.HashCount }\n    }\n}\n\nfunction Invoke-JsonlDeduplication\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$InputPath, [Parameter(Mandatory)][string]$OutputPath, [string]$KeyPath = 'id', [double]$FalsePositiveRate = 0.001)\n    $dedup = Get-JsonlDeduplicated -InputPath $InputPath -KeyProperty $KeyPath -FalsePositiveRate $FalsePositiveRate\n    if ($dedup.UniqueLines.Count -gt 0)\n    {\n        $outDir = Split-Path -Path $OutputPath -Parent\n        if ($outDir -and -not (Test-Path $outDir)) { New-Item -ItemType Directory -Path $outDir -Force | Out-Null }\n        $dedup.UniqueLines | Set-Content -Path $OutputPath -Encoding UTF8\n    }\n    else\n    {\n        New-Item -ItemType File -Path $OutputPath -Force | Out-Null\n    }\n    return [pscustomobject]@{\n        InputPath           = $InputPath\n        OutputPath          = $OutputPath\n        OriginalRecords     = $dedup.ProcessedLines\n        DeduplicatedRecords = $dedup.UniqueLines.Count\n        DuplicatesRemoved   = $dedup.DuplicatesFound\n    }\n}\n\nfunction Test-JsonlFileDuplicates\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$FilePath, [string]$KeyPath = 'id', [double]$FalsePositiveRate = 0.001)\n    $result = Get-JsonlDeduplicated -InputPath $FilePath -KeyProperty $KeyPath -FalsePositiveRate $FalsePositiveRate\n    return ($result.DuplicatesFound -gt 0)\n}\n\n# --- Public API ---\n\nfunction Write-Session\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][PSCustomObject[]]$Features, [Parameter(Mandatory)][string]$SessionId, [Parameter(Mandatory)][string]$ArchiveDir, [switch]$Index)\n    if (-not (Test-Path $ArchiveDir)) { New-Item -ItemType Directory -Path $ArchiveDir -Force | Out-Null }\n    foreach ($feature in $Features)\n    {\n        $json = ConvertTo-Json -InputObject $feature -Depth 64 -Compress\n        if (-not (Test-JsonValid -JsonString $json)) { throw \"Malformed feature object in session $SessionId.\" }\n    }\n    $sessionPath = Join-Path $ArchiveDir \"$SessionId.jsonl\"\n    $bloom = New-BloomFilter -ExpectedItems 10000 -FalsePositiveRate 0.001\n    foreach ($turn in $Features)\n    {\n        $hash = Get-ContentHash -Content (ConvertTo-CanonicalJson $turn)\n        if (Test-BloomFilterItem -BloomFilter $bloom -Item [string]$hash)\n        {\n            throw \"Duplicate turn detected in session $SessionId. Aborting write.\"\n        }\n        Add-BloomFilterItem -BloomFilter $bloom -Item [string]$hash\n    }\n    $Features | Write-JsonlRecords -Path $sessionPath\n    if ($Index) { New-JsonlBinaryIndex -JsonlPath $sessionPath }    # Use HammingThreshold symbol to avoid unused-variable warning.\n    $null = $HammingThreshold    return [pscustomobject]@{ SessionId = $SessionId; Path = $sessionPath; Records = $Features.Count }\n}\n\nfunction Read-Session\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$SessionId, [Parameter(Mandatory)][string]$ArchiveDir)\n    $sessionPath = Join-Path $ArchiveDir \"$SessionId.jsonl\"\n    if (-not (Test-Path $sessionPath)) { throw \"Session file not found: $sessionPath\" }\n    $enumerator = [JsonlEnumerator]::new($sessionPath)\n    try\n    {\n        $results = @()\n        while ($true)\n        {\n            $item = $enumerator.GetNext()\n            if ($null -eq $item) { break }\n            $results += $item\n        }\n        return $results\n    }\n    finally\n    {\n        $enumerator.Dispose()\n    }\n}\n\nfunction Invoke-ArchiveDedup\n{\n    [CmdletBinding()]\n    param([Parameter(Mandatory)][string]$ArchiveDir)\n    if (-not (Test-Path $ArchiveDir)) { throw \"Archive dir not found: $ArchiveDir\" }\n    $files = Get-ChildItem -Path $ArchiveDir -Filter *.jsonl -File\n    $summary = [System.Collections.Generic.List[object]]::new()\n    foreach ($file in $files)\n    {\n        $dedupPath = \"$($file.FullName).dedup\"\n        $stats = Invoke-JsonlDeduplication -InputPath $file.FullName -OutputPath $dedupPath -KeyPath 'sessionId,turnIndex'\n        Move-Item -Force -Path $dedupPath -Destination $file.FullName\n        $summary.Add($stats)\n    }\n    return $summary\n}\n\nExport-ModuleMember -Function Write-Session, Read-Session, Invoke-ArchiveDedup |
12 | telemetry.ps1 | {6406 674 0.2086 5.0242} | 7092 | #Requires -Version 7.5\nSet-StrictMode -Version Latest\n$ErrorActionPreference = 'Stop'\n\n<#\n.SYNOPSIS\n    Launch mitmproxy wire sensor for Copilot decay capture.\n\n.DESCRIPTION\n    - Installs mitmproxy into pyenv python if missing\n    - Starts mitmproxy as a background job on the configured port\n    - Writes http.proxy + http.proxyStrictSSL into VS Code settings.json\n      (Store-installed VS Code does not inherit env vars, so settings injection\n       is the only reliable approach)\n    - Patches settings back to original on Stop\n\n.NOTES\n    VS Code is a Store app — HTTPS_PROXY env var is ignored by the AppContainer.\n    We write directly to the user settings.json instead.\n    No admin required (mitmproxy cert is bypassed via proxyStrictSSL: false).\n    Note: if we ever want to publish this project, we will need to rethink this convenience\n\n.EXAMPLE\n    # Start capture\n    .\\Start-Telemetry.ps1 -SessionId \"decay-001\"\n\n    # Stop capture and restore settings\n    .\\Start-Telemetry.ps1 -Stop\n#>\n[CmdletBinding(DefaultParameterSetName = 'Start')]\nparam(\n    [Parameter(ParameterSetName = 'Start')]\n    [string]$SessionId = [System.DateTime]::UtcNow.ToString('yyyyMMdd-HHmm'),\n\n    [Parameter(ParameterSetName = 'Start')]\n    [int]$Port = 8888,\n\n    [Parameter(ParameterSetName = 'Start')]\n    [string]$OutputDir = (Join-Path $PSScriptRoot 'captures'),\n\n    [Parameter(ParameterSetName = 'Stop')]\n    [switch]$Stop\n)\n\n# ── paths ──────────────────────────────────────────────────────────────────────\n$script:StateFile = Join-Path $PSScriptRoot '.sensor-state.json'\n$script:SensorScript = Join-Path $PSScriptRoot 'copilot_sensor.py'\n\n# VS Code user settings.json — works for all VS Code installs including Store\n$script:VsCodeSettings = Join-Path $env:APPDATA 'Code\\User\\settings.json'\n\nfunction Get-PyenvPython\n{\n    # Prefer pyenv shim, fall back to python3/python on PATH\n    foreach ($candidate in @(\n            (Join-Path $env:USERPROFILE '.pyenv\\pyenv-win\\shims\\python.bat'),\n            (Join-Path $env:USERPROFILE '.pyenv\\pyenv-win\\shims\\python3.bat'),\n            'python3', 'python'\n        ))\n    {\n        $cmd = Get-Command $candidate -ErrorAction SilentlyContinue\n        if ($cmd) { return $cmd.Source }\n    }\n    throw 'Python not found. Ensure pyenv is initialised in your PATH.'\n}\n\nfunction Assert-MitmproxyInstalled\n{\n    param([string]$Python)\n    $mitm = & $Python -c 'import mitmproxy; print(mitmproxy.__version__)' 2>$null\n    if ($LASTEXITCODE -ne 0)\n    {\n        Write-Host '[sensor] Installing mitmproxy into pyenv python...' -ForegroundColor Yellow\n        & $Python -m pip install --quiet mitmproxy\n        if ($LASTEXITCODE -ne 0) { throw 'mitmproxy install failed.' }\n        Write-Host '[sensor] mitmproxy installed.' -ForegroundColor Green\n    }\n    else\n    {\n        Write-Host \"[sensor] mitmproxy $($mitm.Trim()) found.\" -ForegroundColor DarkGray\n    }\n}\n\nfunction Patch-VsCodeProxy\n{\n    param([int]$Port)\n\n    $settings = if (Test-Path $script:VsCodeSettings)\n    {\n        Get-Content $script:VsCodeSettings -Raw -Encoding UTF8 | ConvertFrom-Json -AsHashtable\n    }\n    else { @{} }\n\n    # Stash originals for restore\n    $stash = @{\n        'http.proxy'          = $settings['http.proxy']\n        'http.proxyStrictSSL' = $settings['http.proxyStrictSSL']\n    }\n\n    $settings['http.proxy'] = \"http://localhost:$Port\"\n    $settings['http.proxyStrictSSL'] = $false\n\n    $settings | ConvertTo-Json -Depth 10 |\n    Set-Content $script:VsCodeSettings -Encoding UTF8\n\n    return $stash\n}\n\nfunction Restore-VsCodeProxy\n{\n    param([hashtable]$Stash)\n\n    if (-not (Test-Path $script:VsCodeSettings)) { return }\n    $settings = Get-Content $script:VsCodeSettings -Raw -Encoding UTF8 |\n    ConvertFrom-Json -AsHashtable\n\n    foreach ($key in $Stash.Keys)\n    {\n        if ($null -eq $Stash[$key])\n        {\n            $settings.Remove($key)\n        }\n        else\n        {\n            $settings[$key] = $Stash[$key]\n        }\n    }\n\n    $settings | ConvertTo-Json -Depth 10 |\n    Set-Content $script:VsCodeSettings -Encoding UTF8\n}\n\n# ── STOP ───────────────────────────────────────────────────────────────────────\nif ($Stop)\n{\n    if (-not (Test-Path $script:StateFile))\n    {\n        Write-Warning '[sensor] No active sensor state found.'\n        return\n    }\n    $state = Get-Content $script:StateFile -Raw | ConvertFrom-Json -AsHashtable\n\n    # Kill the mitmproxy job\n    $job = Get-Job -Id $state.JobId -ErrorAction SilentlyContinue\n    if ($job)\n    {\n        Stop-Job $job\n        Remove-Job $job\n        Write-Host \"[sensor] mitmproxy job $($state.JobId) stopped.\" -ForegroundColor Cyan\n    }\n\n    # Restore VS Code settings\n    Restore-VsCodeProxy -Stash $state.VsCodeStash\n    Write-Host '[sensor] VS Code proxy settings restored.' -ForegroundColor Cyan\n\n    Remove-Item $script:StateFile -Force\n    Write-Host \"[sensor] Session '$($state.SessionId)' ended.\" -ForegroundColor Green\n    return\n}\n\n# ── START ──────────────────────────────────────────────────────────────────────\n$python = Get-PyenvPython\nAssert-MitmproxyInstalled -Python $python\n\n$null = New-Item $OutputDir -ItemType Directory -Force\n\n$env:SENSOR_SESSION_ID = $SessionId\n$env:SENSOR_OUTPUT = $OutputDir\n\n$job = Start-Job -Name \"mitmproxy-$SessionId\" -ScriptBlock {\n    param($py, $port, $script, $sid, $outDir)\n    $env:SENSOR_SESSION_ID = $sid\n    $env:SENSOR_OUTPUT = $outDir\n    & $py -m mitmproxy --mode regular --listen-port $port `\n        --quiet --no-web-open-browser `\n        -s $script\n} -ArgumentList $python, $Port, $script:SensorScript, $SessionId, $OutputDir\n\n$stash = Patch-VsCodeProxy -Port $Port\n\n# Persist state for Stop\n@{\n    SessionId   = $SessionId\n    JobId       = $job.Id\n    Port        = $Port\n    OutputDir   = $OutputDir\n    VsCodeStash = $stash\n    StartedAt   = [System.DateTime]::UtcNow.ToString('o')\n} | ConvertTo-Json -Depth 5 |\nSet-Content $script:StateFile -Encoding UTF8\n\nWrite-Host \"\"\nWrite-Host \"[sensor] Wire sensor ACTIVE\" -ForegroundColor Green\nWrite-Host \"  Session : $SessionId\"\nWrite-Host \"  Port    : $Port\"\nWrite-Host \"  Output  : $OutputDir\"\nWrite-Host \"  Job ID  : $($job.Id)\"\nWrite-Host \"\"\nWrite-Host \"  VS Code proxy patched → settings.json\" -ForegroundColor Yellow\nWrite-Host \"  Launch / reload VS Code now to route traffic through sensor.\"\nWrite-Host \"\"\nWrite-Host \"  Stop with:  .\\Start-WireSensor.ps1 -Stop\" -ForegroundColor DarkGray |
