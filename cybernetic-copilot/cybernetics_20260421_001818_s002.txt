idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
3 | CyberneticAutomata.psm1 | {18848 1540 0.2645 4.7912} | 19473 | #Requires -Version 7.5\nSet-StrictMode -Version Latest\n\nclass CyberneticDaemon {\n    [hashtable]$BackgroundCommands = @{}\n    [System.Collections.Generic.List[PSCustomObject]]$ExecutionLog = @()\n    [datetime]$SessionStart = (Get-Date)\n\n    [void] RecordBackgroundExecution([string]$commandId, [string]$command, [string]$explanation) {\n        $this.BackgroundCommands[$commandId] = @{\n            Command = $command\n            Explanation = $explanation\n            StartTime = Get-Date\n            Status = 'Running'\n        }\n\n        $this.ExecutionLog.Add([PSCustomObject]@{\n            Type = 'BackgroundLaunch'\n            CommandId = $commandId\n            Command = $command\n            Timestamp = Get-Date\n        })\n    }\n\n    [void] ConfirmArtifact([string]$commandId, [string]$artifactPath) {\n        if ($this.BackgroundCommands.ContainsKey($commandId)) {\n            $this.BackgroundCommands[$commandId].Status = 'Confirmed'\n            $this.BackgroundCommands[$commandId].ArtifactPath = $artifactPath\n            $this.BackgroundCommands[$commandId].CompletedTime = Get-Date\n\n            $this.ExecutionLog.Add([PSCustomObject]@{\n                Type = 'ArtifactConfirmation'\n                CommandId = $commandId\n                ArtifactPath = $artifactPath\n                Timestamp = Get-Date\n            })\n        }\n    }\n\n}\n\n$global:CyberneticDaemon = [CyberneticDaemon]::new()\n\nfunction ConvertTo-CyberneticHashtable {\n    [CmdletBinding()]\n    [OutputType([hashtable])]\n    param(\n        [Parameter()]\n        [object]$InputObject\n    )\n\n    if ($null -eq $InputObject) {\n        return @{}\n    }\n\n    if ($InputObject -is [hashtable]) {\n        return @{} + $InputObject\n    }\n\n    $table = @{}\n    foreach ($property in $InputObject.PSObject.Properties) {\n        $table[$property.Name] = $property.Value\n    }\n\n    return $table\n}\n\nfunction Get-CyberneticJobRoot {\n    [CmdletBinding()]\n    [OutputType([string])]\n    param(\n        [Parameter()]\n        [string]$JobRoot,\n\n        [Parameter()]\n        [switch]$Ensure\n    )\n\n    $resolvedRoot = if (-not [string]::IsNullOrWhiteSpace($JobRoot)) {\n        $JobRoot\n    } elseif (-not [string]::IsNullOrWhiteSpace($env:CYBERNETIC_JOB_ROOT)) {\n        $env:CYBERNETIC_JOB_ROOT\n    } else {\n        Join-Path ([IO.Path]::GetTempPath()) 'vscode-copilot-jobs'\n    }\n\n    $resolvedRoot = [IO.Path]::GetFullPath($resolvedRoot)\n\n    if ($Ensure -and -not (Test-Path -LiteralPath $resolvedRoot)) {\n        New-Item -ItemType Directory -Path $resolvedRoot -Force | Out-Null\n    }\n\n    return $resolvedRoot\n}\n\nfunction Get-CyberneticJobStorePath {\n    [CmdletBinding()]\n    [OutputType([string])]\n    param(\n        [Parameter()]\n        [string]$JobRoot,\n\n        [Parameter()]\n        [switch]$Ensure\n    )\n\n    $resolvedRoot = Get-CyberneticJobRoot -JobRoot $JobRoot -Ensure:$Ensure\n    return Join-Path $resolvedRoot 'jobs.jsonl'\n}\n\nfunction Get-CyberneticJobPaths {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JobId,\n\n        [Parameter()]\n        [string]$JobRoot\n    )\n\n    $resolvedRoot = Get-CyberneticJobRoot -JobRoot $JobRoot -Ensure\n\n    return [PSCustomObject]@{\n        jobId = $JobId\n        jobRoot = $resolvedRoot\n        signalPath = Join-Path $resolvedRoot \"$JobId.signal\"\n        resultPath = Join-Path $resolvedRoot \"$JobId.jsonl\"\n        cancelSignalPath = Join-Path $resolvedRoot \"$($JobId)_cancel.signal\"\n        storePath = Get-CyberneticJobStorePath -JobRoot $resolvedRoot -Ensure\n    }\n}\n\nfunction Read-CyberneticJobResults {\n    [CmdletBinding()]\n    [OutputType([object[]])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$ResultPath\n    )\n\n    $resolvedPath = [IO.Path]::GetFullPath($ResultPath)\n    if (-not (Test-Path -LiteralPath $resolvedPath)) {\n        return @()\n    }\n\n    $results = foreach ($line in Get-Content -LiteralPath $resolvedPath -ErrorAction SilentlyContinue) {\n        if ([string]::IsNullOrWhiteSpace($line)) {\n            continue\n        }\n\n        try {\n            $line | ConvertFrom-Json -ErrorAction Stop\n        }\n        catch {\n            Write-Verbose \"Skipping invalid job result record in $resolvedPath\"\n        }\n    }\n\n    return @($results)\n}\n\nfunction Get-CyberneticJobStatus {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JobId,\n\n        [Parameter()]\n        [string]$SignalPath,\n\n        [Parameter()]\n        [string]$ResultPath,\n\n        [Parameter()]\n        [string]$CancelSignalPath,\n\n        [Parameter()]\n        [datetime]$StartTime,\n\n        [Parameter()]\n        [int]$TimeoutMilliseconds = 300000\n    )\n\n    if ($CancelSignalPath -and (Test-Path -LiteralPath $CancelSignalPath -PathType Leaf -ErrorAction Ignore)) {\n        return [PSCustomObject]@{\n            jobId = $JobId\n            status = 'cancelled'\n            timestamp = (Get-Date).ToString('o')\n            detectedBy = 'cancel-signal'\n        }\n    }\n\n    if ($SignalPath -and (Test-Path -LiteralPath $SignalPath -PathType Leaf -ErrorAction Ignore)) {\n        try {\n            $signal = Get-Content -LiteralPath $SignalPath -Raw -ErrorAction Stop | ConvertFrom-Json -ErrorAction Stop\n            return [PSCustomObject]@{\n                jobId = $JobId\n                status = [string]$signal.status\n                timestamp = if ($signal.timestamp) { [string]$signal.timestamp } else { (Get-Date).ToString('o') }\n                resultPath = if ($signal.resultPath) { [string]$signal.resultPath } else { $ResultPath }\n                itemCount = if ($null -ne $signal.itemCount) { [int]$signal.itemCount } else { $null }\n                message = if ($signal.message) { [string]$signal.message } else { $null }\n                error = if ($signal.error) { [string]$signal.error } else { $null }\n                detectedBy = 'signal'\n            }\n        }\n        catch {\n            Write-Verbose \"Signal file for job $JobId exists but could not be parsed\"\n        }\n    }\n\n    if ($ResultPath -and (Test-Path -LiteralPath $ResultPath -PathType Leaf -ErrorAction Ignore)) {\n        $resultInfo = Get-Item -LiteralPath $ResultPath -ErrorAction SilentlyContinue\n        if ($resultInfo) {\n            $ageMilliseconds = [int][Math]::Round(((Get-Date) - $resultInfo.LastWriteTime).TotalMilliseconds)\n            if ($ageMilliseconds -gt 5000) {\n                return [PSCustomObject]@{\n                    jobId = $JobId\n                    status = 'completed'\n                    timestamp = (Get-Date).ToString('o')\n                    resultPath = $ResultPath\n                    ageMilliseconds = $ageMilliseconds\n                    detectedBy = 'result-stability'\n                }\n            }\n        }\n    }\n\n    if ($PSBoundParameters.ContainsKey('StartTime') -and $StartTime) {\n        $elapsedMilliseconds = [int][Math]::Round(((Get-Date) - $StartTime).TotalMilliseconds)\n        if ($TimeoutMilliseconds -gt 0 -and $elapsedMilliseconds -gt $TimeoutMilliseconds) {\n            return [PSCustomObject]@{\n                jobId = $JobId\n                status = 'failed'\n                timestamp = (Get-Date).ToString('o')\n                error = \"Job timed out after $TimeoutMilliseconds ms\"\n                detectedBy = 'timeout'\n            }\n        }\n    }\n\n    return [PSCustomObject]@{\n        jobId = $JobId\n        status = 'running'\n        detectedBy = 'pending-artifacts'\n    }\n}\n\nfunction Request-CyberneticJobCancellation {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JobId,\n\n        [Parameter(Mandatory)]\n        [string]$CancelSignalPath,\n\n        [Parameter()]\n        [string]$Reason = 'user_requested'\n    )\n\n    $resolvedPath = [IO.Path]::GetFullPath($CancelSignalPath)\n    $parentPath = Split-Path -Path $resolvedPath -Parent\n    if (-not (Test-Path -LiteralPath $parentPath)) {\n        New-Item -ItemType Directory -Path $parentPath -Force | Out-Null\n    }\n\n    [PSCustomObject]@{\n        jobId = $JobId\n        cancelledAt = (Get-Date).ToString('o')\n        reason = $Reason\n    } | ConvertTo-Json -Depth 5 | Set-Content -LiteralPath $resolvedPath -Encoding UTF8\n\n    return [PSCustomObject]@{\n        jobId = $JobId\n        cancelSignalPath = $resolvedPath\n        acknowledged = $true\n    }\n}\n\nfunction Clear-CyberneticJobArtifacts {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter(Mandatory)]\n        [string[]]$Paths\n    )\n\n    $removedPaths = New-Object System.Collections.Generic.List[string]\n    $missingPaths = New-Object System.Collections.Generic.List[string]\n\n    foreach ($jobPath in $Paths | Where-Object { $_ } | Select-Object -Unique) {\n        $resolvedPath = [IO.Path]::GetFullPath($jobPath)\n        if (Test-Path -LiteralPath $resolvedPath -PathType Leaf -ErrorAction Ignore) {\n            Remove-Item -LiteralPath $resolvedPath -Force -ErrorAction SilentlyContinue\n            if (-not (Test-Path -LiteralPath $resolvedPath -PathType Leaf -ErrorAction Ignore)) {\n                $removedPaths.Add($resolvedPath)\n            }\n        } else {\n            $missingPaths.Add($resolvedPath)\n        }\n    }\n\n    return [PSCustomObject]@{\n        removedPaths = @($removedPaths)\n        missingPaths = @($missingPaths)\n    }\n}\n\nfunction Write-CyberneticJobStoreRecord {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter(Mandatory)]\n        [object]$Job,\n\n        [Parameter()]\n        [string]$JobRoot\n    )\n\n    $jobTable = ConvertTo-CyberneticHashtable -InputObject $Job\n    if (-not $jobTable.ContainsKey('jobId')) {\n        throw 'Job record must contain a jobId field.'\n    }\n\n    $canonicalJob = [ordered]@{\n        jobId = [string]$jobTable.jobId\n        status = if ($jobTable.ContainsKey('status') -and $jobTable.status) { [string]$jobTable.status } else { 'pending' }\n        startTime = if ($jobTable.ContainsKey('startTime') -and $jobTable.startTime) { [datetime]$jobTable.startTime } else { Get-Date }\n        endTime = if ($jobTable.ContainsKey('endTime') -and $jobTable.endTime) { [datetime]$jobTable.endTime } else { $null }\n        signalPath = if ($jobTable.ContainsKey('signalPath')) { [string]$jobTable.signalPath } else { $null }\n        resultPath = if ($jobTable.ContainsKey('resultPath')) { [string]$jobTable.resultPath } else { $null }\n        cancelSignalPath = if ($jobTable.ContainsKey('cancelSignalPath')) { [string]$jobTable.cancelSignalPath } else { $null }\n        operation = if ($jobTable.ContainsKey('operation')) { [string]$jobTable.operation } else { $null }\n        itemCount = if ($jobTable.ContainsKey('itemCount') -and $null -ne $jobTable.itemCount) { [int]$jobTable.itemCount } else { $null }\n        error = if ($jobTable.ContainsKey('error') -and $jobTable.error) { [string]$jobTable.error } else { $null }\n    }\n\n    $storePath = Get-CyberneticJobStorePath -JobRoot $JobRoot -Ensure\n    $jobs = Read-CyberneticJobStore -JobRoot $JobRoot\n    $jobs = @($jobs | Where-Object { $_.jobId -ne $canonicalJob.jobId }) + [PSCustomObject]$canonicalJob\n\n    if ($jobs.Count -gt 0) {\n        $jsonl = ($jobs | ForEach-Object { $_ | ConvertTo-Json -Depth 8 -Compress }) -join [Environment]::NewLine\n        Set-Content -LiteralPath $storePath -Value ($jsonl + [Environment]::NewLine) -Encoding UTF8\n    } else {\n        Set-Content -LiteralPath $storePath -Value '' -Encoding UTF8\n    }\n\n    return [PSCustomObject]$canonicalJob\n}\n\nfunction Read-CyberneticJobStore {\n    [CmdletBinding()]\n    [OutputType([object[]])]\n    param(\n        [Parameter()]\n        [string]$JobRoot\n    )\n\n    $storePath = Get-CyberneticJobStorePath -JobRoot $JobRoot\n    if (-not (Test-Path -LiteralPath $storePath -PathType Leaf -ErrorAction Ignore)) {\n        return @()\n    }\n\n    $jobs = foreach ($line in Get-Content -LiteralPath $storePath -ErrorAction SilentlyContinue) {\n        if ([string]::IsNullOrWhiteSpace($line)) {\n            continue\n        }\n\n        try {\n            $line | ConvertFrom-Json -ErrorAction Stop\n        }\n        catch {\n            Write-Verbose \"Skipping invalid jobs.jsonl record in $storePath\"\n        }\n    }\n\n    return @($jobs)\n}\n\nfunction Compress-CyberneticJobStore {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter()]\n        [string]$JobRoot,\n\n        [Parameter()]\n        [int]$RetentionHours = 24\n    )\n\n    $storePath = Get-CyberneticJobStorePath -JobRoot $JobRoot\n    if (-not (Test-Path -LiteralPath $storePath -PathType Leaf -ErrorAction Ignore)) {\n        return [PSCustomObject]@{\n            removedCount = 0\n            keptCount = 0\n        }\n    }\n\n    $jobs = Read-CyberneticJobStore -JobRoot $JobRoot\n    $cutoff = (Get-Date).AddHours(-1 * [Math]::Abs($RetentionHours))\n\n    $keptJobs = @($jobs | Where-Object {\n        if ($_.status -in @('pending', 'running')) {\n            return $true\n        }\n\n        $comparisonTime = if ($_.endTime) { [datetime]$_.endTime } else { [datetime]$_.startTime }\n        return $comparisonTime -ge $cutoff\n    })\n\n    $removedCount = $jobs.Count - $keptJobs.Count\n\n    if ($keptJobs.Count -gt 0) {\n        $jsonl = ($keptJobs | ForEach-Object { $_ | ConvertTo-Json -Depth 8 -Compress }) -join [Environment]::NewLine\n        Set-Content -LiteralPath $storePath -Value ($jsonl + [Environment]::NewLine) -Encoding UTF8\n    } else {\n        Set-Content -LiteralPath $storePath -Value '' -Encoding UTF8\n    }\n\n    return [PSCustomObject]@{\n        removedCount = $removedCount\n        keptCount = $keptJobs.Count\n    }\n}\n\nfunction Remove-CyberneticStaleJobArtifacts {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter()]\n        [string]$JobRoot,\n\n        [Parameter()]\n        [int]$RetentionHours = 24\n    )\n\n    $resolvedRoot = Get-CyberneticJobRoot -JobRoot $JobRoot\n    if (-not (Test-Path -LiteralPath $resolvedRoot -PathType Container -ErrorAction Ignore)) {\n        return [PSCustomObject]@{\n            removedCount = 0\n            removedPaths = @()\n        }\n    }\n\n    $cutoff = (Get-Date).AddHours(-1 * [Math]::Abs($RetentionHours))\n    $removedPaths = New-Object System.Collections.Generic.List[string]\n\n    foreach ($artifact in Get-ChildItem -LiteralPath $resolvedRoot -File -ErrorAction SilentlyContinue) {\n        if ($artifact.Name -eq 'jobs.jsonl') {\n            continue\n        }\n\n        if ($artifact.LastWriteTime -lt $cutoff) {\n            Remove-Item -LiteralPath $artifact.FullName -Force -ErrorAction SilentlyContinue\n            if (-not (Test-Path -LiteralPath $artifact.FullName -PathType Leaf -ErrorAction Ignore)) {\n                $removedPaths.Add($artifact.FullName)\n            }\n        }\n    }\n\n    return [PSCustomObject]@{\n        removedCount = $removedPaths.Count\n        removedPaths = @($removedPaths)\n    }\n}\n\nfunction Clear-CyberneticJobStore {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter()]\n        [string]$JobRoot\n    )\n\n    $storePath = Get-CyberneticJobStorePath -JobRoot $JobRoot\n    $cleared = $false\n    if (Test-Path -LiteralPath $storePath -PathType Leaf -ErrorAction Ignore) {\n        Remove-Item -LiteralPath $storePath -Force -ErrorAction SilentlyContinue\n        $cleared = -not (Test-Path -LiteralPath $storePath -PathType Leaf -ErrorAction Ignore)\n    }\n\n    return [PSCustomObject]@{\n        cleared = $cleared\n        storePath = $storePath\n    }\n}\n\nfunction Invoke-BackgroundCommand {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Command,\n\n        [Parameter(Mandatory)]\n        [string]$Explanation,\n\n        [Parameter()]\n        [string[]]$ExpectedArtifacts = @()\n    )\n\n    $commandId = [guid]::NewGuid().ToString('N')[0..7] -join ''\n\n    Write-Host \"🚀 RSVP Background Launch [$commandId]: $Explanation\" -ForegroundColor Cyan\n\n    $global:CyberneticDaemon.RecordBackgroundExecution($commandId, $Command, $Explanation)\n\n    return @{\n        CommandId = $commandId\n        Command = $Command\n        Explanation = $Explanation\n        ExpectedArtifacts = $ExpectedArtifacts\n        LaunchTime = Get-Date\n        Status = 'BackgroundLaunched'\n    }\n}\n\nfunction Confirm-BackgroundArtifacts {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$CommandId,\n\n        [Parameter()]\n        [string[]]$ArtifactPaths = @()\n    )\n\n    $confirmed = @()\n\n    foreach ($path in $ArtifactPaths) {\n        if (Test-Path $path) {\n            $confirmed += $path\n            Write-Host \"✅ RSVP Confirmed: $path\" -ForegroundColor Green\n            $global:CyberneticDaemon.ConfirmArtifact($CommandId, $path)\n        } else {\n            Write-Host \"❌ RSVP Missing: $path\" -ForegroundColor Red\n        }\n    }\n\n    return @{\n        CommandId = $CommandId\n        ConfirmedArtifacts = $confirmed\n        MissingArtifacts = $ArtifactPaths | Where-Object { -not (Test-Path $_) }\n        ConfirmationTime = Get-Date\n    }\n}\n\nfunction Test-CopilotPriming {\n    \n    [CmdletBinding()]\n    param()\n\n    $primingChecks = @{\n        'BackgroundExecutionAware' = $true  # This function existing means we're aware\n        'ConsoleHazardKnowledge' = (Test-Path \"$env:PORTABLE_ROOT\\GARGOYLE-DEFEATED.md\") -or\n                                    ($env:COPILOT_CORE_PATH -and (Test-Path (Join-Path $env:COPILOT_CORE_PATH 'GARGOYLE-DEFEATED.md')))\n        'CyberneticConsoleIntegration' = (Get-Module CyberneticConsole -ErrorAction SilentlyContinue) -ne $null\n        'CyberneticDaemonActive' = $global:CyberneticDaemon -ne $null\n        'MetacognitiveLambdaLoaded' = (Get-Command Invoke-BackgroundCommand -ErrorAction SilentlyContinue) -ne $null\n    }\n\n    $disciplineReport = if ($global:CyberneticDaemon -and $global:CyberneticDaemon.PSObject.Methods.Name -contains 'GetDisciplineReport') {\n        $global:CyberneticDaemon.GetDisciplineReport()\n    } else {\n        @{ Status = 'DisciplineReportNotAvailable' }\n    }\n\n    return [PSCustomObject]@{\n        PrimingChecks = $primingChecks\n        DisciplineReport = $disciplineReport\n        PrimingScore = ($primingChecks.Values | Where-Object { $_ }).Count / $primingChecks.Count\n        Timestamp = Get-Date\n    }\n}\n\nExport-ModuleMember -Function @(\n    'Get-CyberneticJobRoot'\n    'Get-CyberneticJobPaths'\n    'Get-CyberneticJobStatus'\n    'Read-CyberneticJobResults'\n    'Request-CyberneticJobCancellation'\n    'Clear-CyberneticJobArtifacts'\n    'Write-CyberneticJobStoreRecord'\n    'Read-CyberneticJobStore'\n    'Compress-CyberneticJobStore'\n    'Remove-CyberneticStaleJobArtifacts'\n    'Clear-CyberneticJobStore'\n    'Invoke-BackgroundCommand'\n    'Confirm-BackgroundArtifacts'\n    'Test-CopilotPriming'\n) -Alias @('bgcmd', 'bgconfirm','tsprim') -Variable @('CyberneticDaemon') |
