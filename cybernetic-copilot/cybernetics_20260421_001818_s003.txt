idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
4 | CyberneticConsole.psm1 | {26705 2270 0.2903 4.7438} | 27796 | using namespace System\nusing namespace System.IO\nusing namespace System.Text\nusing namespace System.Security.Cryptography\n\n#Requires -Version 7.2\n\nSet-StrictMode -Version Latest\n\n$promptTemplatePath = Join-Path $PSScriptRoot \"templates\\PromptContext.ps1\"\nif (Test-Path $promptTemplatePath) {\n    . $promptTemplatePath\n} else {\n    function Get-PromptContextTemplate {\n        return \"param()\n`$context = `$script:CyberneticPromptContext\nif (-not `$context) {\n    return '> '\n}\n\n`$workspaceName = `$context.WorkspaceName\n`$sessionId = `$context.SessionId\n`$template = `$context.Template\n\n`$gitInfo = ''\ntry {\n    if (Get-Command git -ErrorAction SilentlyContinue) {\n        `$status = & git status --short --branch 2>`$null\n        if (`$status) {\n            `$lines = `$status -split [Environment]::NewLine\n            `$gitInfo = `$lines[0]\n        }\n    }\n}\ncatch {\n    `$gitInfo = ''\n}\n\n`$promptText = [string]::Format(`$template, `$workspaceName, `$sessionId)\nif (`$gitInfo) {\n    `$promptText = \"`$promptText (git: `$gitInfo)\"\n}\nreturn `$promptText\"\n    }\n}\n\n$loggerPath = Join-Path $PSScriptRoot \"CyberneticLogger.psm1\"\nif (-not (Test-Path $loggerPath)) {\n    throw \"CyberneticLogger.psm1 not found at: $loggerPath\"\n}\nImport-Module $loggerPath -Force -ErrorAction Stop\n\nclass CyberneticConsole {\n    [string]$WorkspaceRoot\n    [string]$PromptTemplate\n    [string]$TranscriptPath\n    [scriptblock]$OriginalPrompt\n    [bool]$IsCapturing\n\n    CyberneticConsole([string]$workspaceRoot, [string]$transcriptPath) {\n        $this.WorkspaceRoot = $workspaceRoot\n        $this.TranscriptPath = $transcriptPath\n        $this.PromptTemplate = '({0}) [{1}] > '\n    }\n\n    [void] InstallPrompt([string]$sessionId) {\n        if ($null -eq $this.OriginalPrompt) {\n\n            $promptFunction = Get-Content function:\\prompt\n            $this.OriginalPrompt = if ($promptFunction) {\n                [ScriptBlock]::Create($promptFunction)\n            } else {\n                { \"PS $($executionContext.SessionState.Path.CurrentLocation)$('>' * ($nestedPromptLevel + 1)) \" }\n            }\n        }\n\n        $workspaceName = Split-Path -Path $this.WorkspaceRoot -Leaf\n        $template = $this.PromptTemplate\n\n        $script:CyberneticPromptContext = @{\n            Template      = $template\n            WorkspaceName = $workspaceName\n            SessionId     = $sessionId\n        }\n\n        $promptScriptText = Get-PromptContextTemplate\n        Set-Item -Path function:\\prompt -Value ([ScriptBlock]::Create($promptScriptText))\n    }\n\n    [void] RestorePrompt() {\n        if ($null -ne $this.OriginalPrompt) {\n            Set-Item -Path function:\\prompt -Value $this.OriginalPrompt\n        }\n    }\n\n    [void] StartCapture() {\n        if ($this.IsCapturing) {\n            return\n        }\n        try {\n            if (-not (Test-Path -LiteralPath (Split-Path -Path $this.TranscriptPath -Parent))) {\n                New-Item -ItemType Directory -Path (Split-Path -Path $this.TranscriptPath -Parent) -Force | Out-Null\n            }\n            Start-Transcript -Append -Path $this.TranscriptPath | Out-Null\n            $this.IsCapturing = $true\n        }\n        catch {\n            throw [InvalidOperationException]::new(\"Failed to start transcript capture at $($this.TranscriptPath): $($_.Exception.Message)\")\n        }\n    }\n\n    [void] StopCapture() {\n        if (-not $this.IsCapturing) {\n            return\n        }\n        try {\n            Stop-Transcript | Out-Null\n            $this.IsCapturing = $false\n        }\n        catch {\n\n        }\n    }\n}\n\nfunction Start-CyberneticConsole {\n\n    [CmdletBinding()]\n    param(\n        [Parameter(Position = 0)]\n        [string]$WorkspaceRoot = (Get-Location).ProviderPath,\n\n        [Parameter()]\n        [string]$MemoryRoot = (Join-Path -Path (Get-Location).ProviderPath -ChildPath '.copilot\\memory')\n    )\n\n    Write-Host \"🤖 Initializing Cybernetic Console...\" -ForegroundColor Cyan\n\n    try {\n        if (-not (Test-Path -LiteralPath $WorkspaceRoot)) {\n            throw [ItemNotFoundException]::new(\"Workspace root not found at $WorkspaceRoot.\")\n        }\n\n        $memoryRootFull = [Path]::GetFullPath($MemoryRoot)\n        if (-not (Test-Path -LiteralPath $memoryRootFull)) {\n            New-Item -ItemType Directory -Path $memoryRootFull -Force | Out-Null\n        }\n\n        $Global:CyberneticConsoleState = [Cybernetics]::new(\n            ([Path]::GetFullPath($WorkspaceRoot)),\n            $memoryRootFull\n        )\n        $Global:CyberneticConsoleState.Initialize()\n\n        $env:CYBERNETIC_CONSOLE_ACTIVE = \"true\"\n        $env:CYBERNETIC_SESSION_ID = $Global:CyberneticConsoleState.Session.SessionId\n\n        Write-Host \"✅ Cybernetic console ready. Session: $($Global:CyberneticConsoleState.Session.SessionId.Substring(0,8))\" -ForegroundColor Green\n    }\n    catch {\n        Write-Error \"Failed to initialize cybernetic console: $($_.Exception.Message)\"\n        throw\n    }\n}\n\nfunction Stop-CyberneticConsole {\n\n    [CmdletBinding()]\n    param()\n\n    if ($null -eq $Global:CyberneticConsoleState) {\n        Write-Warning \"No active Cybernetic Console session found.\"\n        return\n    }\n\n    Write-Host \"🛑 Shutting down Cybernetic Console...\" -ForegroundColor Yellow\n\n    try {\n        $Global:CyberneticConsoleState.Shutdown()\n        Remove-Variable -Name CyberneticConsoleState -Scope Global -Force\n        $env:CYBERNETIC_CONSOLE_ACTIVE = $null\n        $env:CYBERNETIC_SESSION_ID = $null\n\n        Write-Host \"✅ Cybernetic console shutdown complete.\" -ForegroundColor Green\n    }\n    catch {\n        Write-Error \"Error during shutdown: $($_.Exception.Message)\"\n    }\n}\n\nfunction Get-CyberneticStatus {\n\n    [CmdletBinding()]\n    [OutputType([hashtable])]\n    param()\n\n    if ($null -eq $Global:CyberneticConsoleState) {\n        return @{\n            Active = $false\n            Message = \"No active Cybernetic Console session\"\n        }\n    }\n\n    $session = $Global:CyberneticConsoleState.Session\n    $uptime = [DateTime]::UtcNow - $session.Started\n\n    $status = @{\n        Active            = $Global:CyberneticConsoleState.Initialized\n        SessionId         = $session.SessionId\n        SessionIdShort    = $session.SessionId.Substring(0, 8)\n        WorkspaceRoot     = $session.WorkspaceRoot\n        Started           = $session.Started\n        Uptime            = $uptime\n        UptimeFormatted   = \"{0:hh\\:mm\\:ss}\" -f $uptime\n        TranscriptActive  = $Global:CyberneticConsoleState.Console.IsCapturing\n        TranscriptPath    = $Global:CyberneticConsoleState.Console.TranscriptPath\n        MemoryPaths       = @{\n            Session  = $session.SessionDirectory\n            Sequence = $session.SequenceDirectory\n            Project  = $session.ProjectDirectory\n            Global   = $session.GlobalDirectory\n        }\n        PendingFeedback   = $Global:CyberneticConsoleState.Feedback.Events.Count\n        RegisteredHooks   = if ($Global:ConsoleContextHooks) { $Global:ConsoleContextHooks.Count } else { 0 }\n    }\n\n    return $status\n}\n\nfunction Get-CyberneticCopilotRoot {\n    [CmdletBinding()]\n    [OutputType([string])]\n    param()\n\n    if (Get-Command Find-CopilotRoot -ErrorAction SilentlyContinue) {\n        return Find-CopilotRoot\n    }\n\n    $currentPath = (Get-Location).ProviderPath\n    while (-not [string]::IsNullOrEmpty($currentPath)) {\n        $candidate = Join-Path $currentPath '.copilot'\n        if (Test-Path $candidate) {\n            return $candidate\n        }\n\n        $parentPath = Split-Path $currentPath -Parent\n        if ([string]::IsNullOrEmpty($parentPath) -or $parentPath -eq $currentPath) {\n            break\n        }\n\n        $currentPath = $parentPath\n    }\n\n    return $null\n}\n\nfunction Get-CyberneticDumpFiles {\n    [CmdletBinding()]\n    [OutputType([string[]])]\n    param(\n        [Parameter()]\n        [datetime]$Date = (Get-Date),\n\n        [Parameter()]\n        [string]$CopilotRoot = (Get-CyberneticCopilotRoot)\n    )\n\n    $dateText = $Date.ToString('yyyy-MM-dd')\n    $candidateFiles = New-Object System.Collections.Generic.List[string]\n\n    if ($env:COPILOT_GLOBAL_HOME) {\n        $candidateFiles.Add((Join-Path $env:COPILOT_GLOBAL_HOME \"dumps\\$dateText.jsonl\"))\n    }\n\n    if ($CopilotRoot) {\n        $candidateFiles.Add((Join-Path $CopilotRoot \"dumps\\$dateText.jsonl\"))\n    }\n\n    $artifactDirs = @()\n    if ($CopilotRoot) {\n        $artifactDirs += Join-Path $CopilotRoot 'console-artifacts'\n        $artifactDirs += Join-Path (Split-Path $CopilotRoot -Parent) '.copilot-sup\\console-artifacts'\n    }\n    if ($env:COPILOT_CORE_PATH) {\n        $artifactDirs += Join-Path $env:COPILOT_CORE_PATH 'console-artifacts'\n    }\n\n    foreach ($artifactDir in ($artifactDirs | Where-Object { $_ } | Select-Object -Unique)) {\n        if (-not (Test-Path $artifactDir)) {\n            continue\n        }\n\n        $candidateFiles.AddRange(@(\n            Get-ChildItem -Path $artifactDir -Filter \"${dateText}*_dumps.jsonl\" -ErrorAction SilentlyContinue |\n                Select-Object -ExpandProperty FullName\n        ))\n\n        $dumpsSubdir = Join-Path $artifactDir 'dumps'\n        if (Test-Path $dumpsSubdir) {\n            $candidateFiles.AddRange(@(\n                Get-ChildItem -Path $dumpsSubdir -Filter \"${dateText}*.jsonl\" -ErrorAction SilentlyContinue |\n                    Select-Object -ExpandProperty FullName\n            ))\n        }\n    }\n\n    return @($candidateFiles | Where-Object { $_ } | Select-Object -Unique)\n}\n\nfunction Get-CyberneticDumpPath {\n    [CmdletBinding()]\n    [OutputType([string])]\n    param(\n        [Parameter()]\n        [datetime]$Date = (Get-Date),\n\n        [Parameter()]\n        [string]$CopilotRoot = (Get-CyberneticCopilotRoot)\n    )\n\n    $dateText = $Date.ToString('yyyy-MM-dd')\n\n    if ($env:COPILOT_GLOBAL_HOME) {\n        return Join-Path $env:COPILOT_GLOBAL_HOME \"dumps\\$dateText.jsonl\"\n    }\n\n    if ($CopilotRoot) {\n        return Join-Path $CopilotRoot \"dumps\\$dateText.jsonl\"\n    }\n\n    throw 'Cannot determine console dump path: COPILOT_GLOBAL_HOME not set and no .copilot root found'\n}\n\nfunction Get-CyberneticDumpInfo {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter()]\n        [string]$DumpPath,\n\n        [Parameter()]\n        [datetime]$Date = (Get-Date),\n\n        [Parameter()]\n        [string]$CopilotRoot = (Get-CyberneticCopilotRoot)\n    )\n\n    $resolvedDumpPath = if ($DumpPath) {\n        [Path]::GetFullPath($DumpPath)\n    } else {\n        Get-CyberneticDumpPath -Date $Date -CopilotRoot $CopilotRoot\n    }\n\n    $alternatePaths = Get-CyberneticDumpFiles -Date $Date -CopilotRoot $CopilotRoot |\n        Where-Object { $_ -ne $resolvedDumpPath }\n    $exists = Test-Path $resolvedDumpPath\n    $size = if ($exists) { (Get-Item $resolvedDumpPath).Length } else { $null }\n\n    return [PSCustomObject]@{\n        DumpPath = $resolvedDumpPath\n        Exists = $exists\n        Size = $size\n        Date = $Date.ToString('yyyy-MM-dd')\n        AlternatePaths = @($alternatePaths)\n    }\n}\n\nfunction Read-CyberneticConsoleDump {\n    [CmdletBinding()]\n    [OutputType([object[]])]\n    param(\n        [Parameter()]\n        [string]$DumpPath,\n\n        [Parameter()]\n        [int]$Skip = 0,\n\n        [Parameter()]\n        [int]$Take = 100,\n\n        [Parameter()]\n        [ValidateSet('cmd', 'out')]\n        [string]$Type,\n\n        [Parameter()]\n        [string]$SessionId,\n\n        [Parameter()]\n        [int]$Seq\n    )\n\n    $resolvedDumpPath = if ($DumpPath) {\n        [Path]::GetFullPath($DumpPath)\n    } else {\n        Get-CyberneticDumpPath\n    }\n\n    if (-not (Test-Path $resolvedDumpPath)) {\n        return @()\n    }\n\n    $records = foreach ($line in Get-Content -LiteralPath $resolvedDumpPath -ErrorAction SilentlyContinue) {\n        if ([string]::IsNullOrWhiteSpace($line)) {\n            continue\n        }\n\n        try {\n            $line | ConvertFrom-Json -ErrorAction Stop\n        }\n        catch {\n            Write-Verbose \"Skipping invalid dump record in $resolvedDumpPath\"\n        }\n    }\n\n    $records = @($records)\n\n    if ($Type) {\n        $records = @($records | Where-Object { $_.type -eq $Type })\n    }\n    if ($SessionId) {\n        $records = @($records | Where-Object { $_.session -eq $SessionId })\n    }\n    if ($PSBoundParameters.ContainsKey('Seq')) {\n        $records = @($records | Where-Object { $_.seq -eq $Seq })\n    }\n    if ($Skip -gt 0) {\n        $records = @($records | Select-Object -Skip $Skip)\n    }\n\n    $takeCount = if ($Take -gt 0) { $Take } else { 100 }\n    return @($records | Select-Object -First $takeCount)\n}\n\nfunction Get-CyberneticRecentConsoleRecords {\n    [CmdletBinding()]\n    [OutputType([object[]])]\n    param(\n        [Parameter()]\n        [int]$Count = 10,\n\n        [Parameter()]\n        [string]$DumpPath\n    )\n\n    $records = Read-CyberneticConsoleDump -DumpPath $DumpPath -Take 1000\n    if ($Count -le 0) {\n        return @()\n    }\n\n    return @($records | Select-Object -Last $Count)\n}\n\nfunction Get-CyberneticLastCommand {\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter()]\n        [string]$DumpPath\n    )\n\n    $records = Get-CyberneticRecentConsoleRecords -Count 50 -DumpPath $DumpPath\n    $cmdRecords = @($records | Where-Object { $_.type -eq 'cmd' -and $_.command })\n    if ($cmdRecords.Count -eq 0) {\n        return $null\n    }\n\n    $lastCmd = $cmdRecords[-1]\n    $outRecord = $records |\n        Where-Object { $_.type -eq 'out' -and $_.seq -eq $lastCmd.seq -and $_.session -eq $lastCmd.session } |\n        Select-Object -First 1\n\n    if (-not $outRecord) {\n        return $null\n    }\n\n    return [PSCustomObject]@{\n        cmd = $lastCmd\n        out = $outRecord\n    }\n}\n\nfunction Get-CyberneticErrorCommands {\n    [CmdletBinding()]\n    [OutputType([object[]])]\n    param(\n        [Parameter()]\n        [int]$Limit = 10,\n\n        [Parameter()]\n        [string]$DumpPath\n    )\n\n    $records = Read-CyberneticConsoleDump -DumpPath $DumpPath -Type 'cmd' -Take 500\n    $errorRecords = @($records | Where-Object {\n        $null -ne $_.exit_code -and [int]$_.exit_code -ne 0\n    })\n\n    if ($Limit -le 0) {\n        return @()\n    }\n\n    return @($errorRecords | Select-Object -Last $Limit)\n}\n\nfunction Get-CopilotContext {\n\n    [CmdletBinding()]\n    [OutputType([PSCustomObject])]\n    param(\n        [Parameter()]\n        [bool]$IncludeMemory = $true,\n\n        [Parameter()]\n        [bool]$IncludeDumps = $true,\n\n        [Parameter()]\n        [int]$MaxMemoryEntries = 50,\n\n        [Parameter()]\n        [int]$MaxDumpEntries = 20\n    )\n\n    $copilotRoot = Get-CyberneticCopilotRoot\n\n    if (-not $copilotRoot) {\n        Write-Warning \"No .copilot context found in workspace hierarchy\"\n        return $null\n    }\n\n    $context = @{\n        Root = $copilotRoot\n        Injections = @{}\n        Memory = @()\n        RecentDumps = @()\n        TokenEstimate = 0\n    }\n\n    foreach ($file in @('conventions.md', 'development.md', 'imperatives.md', 'instructions.md')) {\n        $path = Join-Path $copilotRoot $file\n        if (Test-Path $path) {\n            $content = Get-Content $path -Raw\n\n            $minified = if (Get-Command Compress-WhitespaceRuns -ErrorAction SilentlyContinue) {\n                Compress-WhitespaceRuns -Text $content\n            } else {\n                $content -replace '\\s+', ' '  # Basic compression fallback\n            }\n            $context.Injections[$file] = $minified\n            $context.TokenEstimate += [Math]::Ceiling($minified.Length / 4)  # Rough token estimate\n        }\n    }\n\n    if ($IncludeMemory) {\n        $memoryPath = Join-Path $copilotRoot \"memory\"\n        if (Test-Path $memoryPath) {\n\n            $memoryFiles = Get-ChildItem -Path $memoryPath -Filter \"*.jsonl\" -ErrorAction SilentlyContinue |\n                Sort-Object LastWriteTime -Descending |\n                Select-Object -First 3\n\n            foreach ($memFile in $memoryFiles) {\n                $entries = Get-Content $memFile.FullName -ErrorAction SilentlyContinue |\n                    Select-Object -Last $MaxMemoryEntries |\n                    ForEach-Object { $_ | ConvertFrom-Json }\n\n                if ($entries) {\n                    $context.Memory += $entries\n                    $context.TokenEstimate += [Math]::Ceiling(($entries | ConvertTo-Json -Compress).Length / 4)\n                }\n            }\n        }\n    }\n\n    if ($IncludeDumps) {\n        $dumpFiles = Get-CyberneticDumpFiles -CopilotRoot $copilotRoot |\n            Where-Object { Test-Path $_ } |\n            Select-Object -First 3\n\n        foreach ($dumpFile in $dumpFiles) {\n            $entries = Read-CyberneticConsoleDump -DumpPath $dumpFile -Take $MaxDumpEntries\n\n            if ($entries) {\n                $context.RecentDumps += $entries\n                $context.TokenEstimate += [Math]::Ceiling(($entries | ConvertTo-Json -Compress).Length / 4)\n            }\n        }\n    }\n\n    return [PSCustomObject]$context\n}\n\nfunction Show-CopilotSequence {\n\n    [CmdletBinding()]\n    [Alias('cseq')]\n    param(\n        [int]$Last = 0,\n        [string]$EventType = \"\"\n    )\n\n    if (-not $Global:CyberneticConsoleState) {\n        Write-Warning \"Cybernetic Console not initialized. No session log available.\"\n        return\n    }\n\n    try {\n        $logPath = $Global:CyberneticConsoleState.Session.SessionLogPath\n\n        if (-not (Test-Path $logPath)) {\n            Write-Host \"No session events logged yet.\" -ForegroundColor Yellow\n            return\n        }\n\n        $events = Get-Content $logPath | ForEach-Object {\n            $_ | ConvertFrom-Json\n        }\n\n        if ($EventType) {\n            $events = $events | Where-Object { $_.event -eq $EventType }\n        }\n\n        if ($Last -gt 0) {\n            $events = $events | Select-Object -Last $Last\n        }\n\n        $events | Format-Table -Property @(\n            @{ Label = 'Time'; Expression = { ([DateTime]$_.recorded).ToLocalTime().ToString('HH:mm:ss') } }\n            @{ Label = 'Event'; Expression = { $_.event } }\n            @{ Label = 'Details'; Expression = {\n                $payload = $_.payload\n                if ($payload.command) { $payload.command }\n                elseif ($payload.Workspace) { \"Workspace: $($payload.Workspace)\" }\n                else { ($payload | ConvertTo-Json -Compress).Substring(0, [Math]::Min(60, ($payload | ConvertTo-Json -Compress).Length)) }\n            } }\n        ) -AutoSize\n\n        Write-Host \"`n📊 Total Events: $($events.Count)\" -ForegroundColor Cyan\n    }\n    catch {\n        Write-Error \"Failed to read session log: $($_.Exception.Message)\"\n    }\n}\n\nfunction Show-CopilotInstructions {\n\n    [CmdletBinding()]\n    [Alias('cinst')]\n    param()\n\n    Write-Host \"`n📄 Copilot Instructions Status\" -ForegroundColor Cyan\n    Write-Host (\"=\" * 50) -ForegroundColor DarkGray\n\n    Write-Host \"`nGlobal Instructions:\" -ForegroundColor White\n    if ($env:COPILOT_INSTRUCTIONS_LOADED -eq \"true\") {\n        Write-Host \"  Status: ✅ Loaded\" -ForegroundColor Green\n\n        $globalPath = if ($env:COPILOT_GLOBAL_INSTRUCTIONS_PATH) {\n            $env:COPILOT_GLOBAL_INSTRUCTIONS_PATH\n        } elseif ($env:COPILOT_GLOBAL_HOME) {\n            Join-Path $env:COPILOT_GLOBAL_HOME 'instructions'\n        } else {\n            Join-Path $env:PORTABLE_ROOT '.copilot\\instructions'\n        }\n        if (Test-Path $globalPath) {\n            $files = Get-ChildItem \"$globalPath/*.md\" -ErrorAction SilentlyContinue\n            foreach ($file in $files) {\n                $charCount = (Get-Content $file.FullName -Raw).Length\n                Write-Host \"  - $($file.Name) ($charCount chars)\" -ForegroundColor Gray\n            }\n        }\n    }\n    else {\n        Write-Host \"  Status: ❌ Not Loaded\" -ForegroundColor Red\n    }\n\n    Write-Host \"`nWorkspace Instructions:\" -ForegroundColor White\n    $workspacePaths = @(\n        \".copilot/instructions.md\"\n        \".github/copilot-instructions.md\"\n    )\n\n    $foundWorkspace = $false\n    foreach ($relPath in $workspacePaths) {\n        $fullPath = Join-Path $PWD $relPath\n        if (Test-Path $fullPath) {\n            $charCount = (Get-Content $fullPath -Raw).Length\n            Write-Host \"  - $relPath ($charCount chars)\" -ForegroundColor Green\n            $foundWorkspace = $true\n        }\n    }\n\n    if (-not $foundWorkspace) {\n        Write-Host \"  Status: No workspace instructions found\" -ForegroundColor Yellow\n    }\n\n    if ($Global:CyberneticConsoleState) {\n        Write-Host \"`nCybernetic Console:\" -ForegroundColor White\n        Write-Host \"  Workspace: $($Global:CyberneticConsoleState.WorkspaceRoot)\" -ForegroundColor Gray\n        Write-Host \"  Session:   $($Global:CyberneticConsoleState.Session.SessionId)\" -ForegroundColor Gray\n    }\n\n    Write-Host \"\"\n}\n\nfunction Test-CommandForErrors {\n\n    [CmdletBinding()]\n    [OutputType([bool])]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [object]$Output\n    )\n\n    $outputText = if ($Output -is [array]) {\n        $Output -join \"`n\"\n    } else {\n        $Output.ToString()\n    }\n\n    $errorPatterns = @(\n        \"ERROR\"\n        \"FAILED\"\n        \"EXCEPTION\"\n        \"FATAL\"\n        \"CRITICAL\"\n        \"(?<!not )(?<!no )error(?!-free)\"  # regex for \"error\" not preceded by \"not\"/\"no\" and not followed by \"-free\"\n    )\n\n    $foundErrors = @()\n    foreach ($pattern in $errorPatterns) {\n        if ($outputText -match $pattern) {\n            $foundErrors += $pattern\n        }\n    }\n\n    if ($foundErrors.Count -gt 0) {\n        Write-Host \"⚠️  Errors detected in command output\" -ForegroundColor Red\n\n        if ($Global:CyberneticConsoleState) {\n            try {\n                $Global:CyberneticConsoleState.Feedback.Push(\n                    'command_errors',\n                    'Error patterns detected in command output',\n                    @{\n                        patterns      = $foundErrors\n                        output_length = $outputText.Length\n                        timestamp     = Get-Date\n                    }\n                )\n            }\n            catch {\n                Write-Verbose \"Error feedback recording failed: $($_.Exception.Message)\"\n            }\n        }\n    }\n\n    return ($foundErrors.Count -gt 0)\n}\n\nfunction Invoke-CapturedCommand {\n\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [string]$Command,\n\n        [Parameter(Position = 1)]\n        [object[]]$ArgumentList = @(),\n\n        [Parameter()]\n        [ValidateSet('development', 'testing', 'deployment', 'debugging')]\n        [string]$SessionContext = 'development',\n\n        [Parameter()]\n        [switch]$EnableCopilotVisibility,\n\n        [Parameter()]\n        [switch]$EnableMemoryIntegration,\n\n        [Parameter()]\n        [switch]$SkipErrorAnalysis\n    )\n\n    Write-Host \"⚡ Capturing command: $Command\" -ForegroundColor Cyan\n\n    $startTime = Get-Date\n    $invocation = [scriptblock]::Create($Command)\n    $output = @()\n    $errorRecord = $null\n\n    try {\n        $output = & $invocation @ArgumentList 2>&1\n    }\n    catch {\n        $errorRecord = $_\n        $output = @($_.Exception.Message)\n    }\n\n    $duration = (Get-Date) - $startTime\n\n    $outputText = if ($output -is [System.Array]) {\n        $output -join [Environment]::NewLine\n    } else {\n        [string]$output\n    }\n\n    $hasErrors = if (-not $SkipErrorAnalysis) {\n        Test-CommandForErrors -Output $outputText\n    } else {\n        $false\n    }\n\n    if ($Global:CyberneticConsoleState) {\n        try {\n            $metadata = @{\n                has_errors      = $hasErrors\n                output_lines    = @($output).Count\n                session_context = $SessionContext\n                duration_ms     = $duration.TotalMilliseconds\n            }\n            Write-CopilotSessionLog -Action 'command_executed' -Command ($Command.Trim()) -Output $outputText -Metadata $metadata\n        }\n        catch {\n            Write-Verbose 'Failed to log command execution in session log.'\n        }\n    }\n\n    if ($EnableCopilotVisibility) {\n        $workspaceRoot = if ($Global:CyberneticConsoleState) {\n            $Global:CyberneticConsoleState.WorkspaceRoot\n        } else {\n            (Get-Location).ProviderPath\n        }\n\n        $contextDir = Join-Path $workspaceRoot \".copilot\\context\"\n        if (-not (Test-Path $contextDir)) {\n            New-Item -Path $contextDir -ItemType Directory -Force | Out-Null\n        }\n\n        $contextFile = Join-Path $contextDir \"recent-output.txt\"\n        $output | Out-File $contextFile -Encoding UTF8\n\n        $contextSummary = @{\n            timestamp       = Get-Date -Format \"yyyy-MM-ddTHH:mm:ss\"\n            command         = $Command\n            output          = $outputText\n            session_context = $SessionContext\n            duration_ms     = $duration.TotalMilliseconds\n            has_errors      = $hasErrors\n            workspace       = $workspaceRoot\n        } | ConvertTo-Json -Depth 3\n\n        $summaryFile = Join-Path $contextDir \"command-context.json\"\n        $contextSummary | Out-File $summaryFile -Encoding UTF8\n\n        Write-Verbose \"Context written to: $contextFile\"\n    }\n\n    if ($EnableMemoryIntegration -and $Global:CyberneticConsoleState) {\n        try {\n            $Global:CyberneticConsoleState.Memory.WriteSequenceEntry(\n                $Global:CyberneticConsoleState.Session.SessionId,\n                \"Command: $Command\",\n                @{\n                    output          = $outputText\n                    session_context = $SessionContext\n                    duration_ms     = $duration.TotalMilliseconds\n                    has_errors      = $hasErrors\n                }\n            )\n        }\n        catch {\n            Write-Verbose \"Memory integration failed: $($_.Exception.Message)\"\n        }\n    }\n\n    if ($errorRecord) {\n        throw $errorRecord\n    }\n\n    return $output\n}\n\nSet-Alias -Name 'clog' -Value Write-CopilotSessionLog -Scope Global -Force\nSet-Alias -Name 'cseq' -Value Show-CopilotSequence -Scope Global -Force\nSet-Alias -Name 'cws' -Value Get-CopilotWorkspaceContext -Scope Global -Force\nSet-Alias -Name 'cinst' -Value Show-CopilotInstructions -Scope Global -Force\n\nExport-ModuleMember -Function @(\n\n    'Start-CyberneticConsole'\n    'Stop-CyberneticConsole'\n\n    'Get-CyberneticStatus'\n    'Get-CopilotContext'\n    'Get-CyberneticDumpPath'\n    'Get-CyberneticDumpInfo'\n    'Read-CyberneticConsoleDump'\n    'Get-CyberneticRecentConsoleRecords'\n    'Get-CyberneticLastCommand'\n    'Get-CyberneticErrorCommands'\n\n    'Show-CopilotSequence'\n    'Show-CopilotInstructions'\n\n    'Invoke-CapturedCommand'\n    'Test-CommandForErrors'\n) -Alias @('clog', 'cseq', 'cws', 'cinst')\n\nSet-Alias -Name 'capture' -Value 'Invoke-CapturedCommand' -Scope Global -Force\nExport-ModuleMember -Alias 'capture' |
5 | CyberneticLogger.psm1 | {4604 342 0.2921 4.7175} | 4832 | class CyberneticLogger {\n    [string]$ProjectRoot\n    [string]$GlobalRoot\n    [string]$CurrentSequence\n\n    CyberneticLogger([string]$projectRoot) {\n        $this.ProjectRoot = $projectRoot\n        $this.GlobalRoot = if ($env:COPILOT_GLOBAL_HOME) {\n            Join-Path $env:COPILOT_GLOBAL_HOME 'cybernetic-interface\\logs'\n        } elseif ($env:COPILOT_CORE_PATH) {\n            Join-Path $env:COPILOT_CORE_PATH 'logs'\n        } else {\n            Join-Path $projectRoot '.copilot\\logs'\n        }\n        $this.EnsureLogDirectories()\n    }\n\n    [void]LogSession([string]$command, [string]$output) {\n\n        $sessionLog = \"$($this.ProjectRoot)\\.copilot\\logs\\session\\$(Get-Date -f yyyy-MM-dd).jsonl\"\n        $entry = @{\n            timestamp = Get-Date -f \"yyyy-MM-ddTHH:mm:ss.fffZ\"\n            type      = \"session\"\n            command   = $command\n            output    = $output.Substring(0, [Math]::Min(500, $output.Length))\n        } | ConvertTo-Json -Compress\n\n        Add-Content -Path $sessionLog -Value $entry\n    }\n\n    [void]LogSequenceStep([string]$sequenceName, [string]$step, [hashtable]$result) {\n\n        $sequenceLog = \"$($this.ProjectRoot)\\.copilot\\logs\\sequences\\$sequenceName\\execution.jsonl\"\n        $entry = @{\n            timestamp = Get-Date -f \"yyyy-MM-ddTHH:mm:ss.fffZ\"\n            type      = \"sequence_step\"\n            sequence  = $sequenceName\n            step      = $step\n            result    = $result\n        } | ConvertTo-Json -Compress\n\n        Add-Content -Path $sequenceLog -Value $entry\n    }\n\n    [void]LogProjectEvent([string]$event, [hashtable]$metadata) {\n\n        $projectLog = \"$($this.ProjectRoot)\\.copilot\\logs\\project\\events.jsonl\"\n        $entry = @{\n            timestamp = Get-Date -f \"yyyy-MM-ddTHH:mm:ss.fffZ\"\n            type      = \"project_event\"\n            event     = $event\n            metadata  = $metadata\n        } | ConvertTo-Json -Compress\n\n        Add-Content -Path $projectLog -Value $entry\n\n        $this.ArchiveToGlobal($entry, \"project_events\")\n    }\n\n    [void]ArchiveToGlobal([string]$logEntry, [string]$category) {\n\n        $globalLog = \"$($this.GlobalRoot)\\$category\\$(Get-Date -f yyyy-MM).jsonl\"\n        Add-Content -Path $globalLog -Value $logEntry\n    }\n\n    [void]EnsureLogDirectories() {\n        @(\n            \"$($this.ProjectRoot)\\.copilot\\logs\\session\",\n            \"$($this.ProjectRoot)\\.copilot\\logs\\sequences\",\n            \"$($this.ProjectRoot)\\.copilot\\logs\\project\",\n            $this.GlobalRoot\n        ) | ForEach-Object {\n            if (-not (Test-Path $_)) {\n                New-Item -ItemType Directory -Path $_ -Force | Out-Null\n            }\n        }\n    }\n}\n\nfunction Write-CopilotSessionLog {\n\n    [CmdletBinding()]\n    [Alias('clog')]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [string]$Action,\n\n        [Parameter(Position = 1)]\n        [hashtable]$Metadata = @{},\n\n        [string]$Command = \"\",\n        [string]$Output = \"\"\n    )\n\n    if (-not $Global:CyberneticConsoleState) {\n        Write-Warning \"Cybernetic Console not initialized. Run Start-CyberneticConsole first.\"\n        return\n    }\n\n    try {\n        $payload = $Metadata.Clone()\n        if ($Command) { $payload.command = $Command }\n        if ($Output) { $payload.output = $Output.Substring(0, [Math]::Min(500, $Output.Length)) }\n\n        $Global:CyberneticConsoleState.Session.RecordEvent($Action, $payload)\n        Write-Verbose \"Session event logged: $Action\"\n    }\n    catch {\n        Write-Warning \"Session logging failed: $($_.Exception.Message)\"\n    }\n}\n\nfunction Write-CopilotSequenceLog {\n\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Sequence,\n\n        [Parameter(Mandatory)]\n        [ValidateSet('started', 'in_progress', 'completed', 'failed')]\n        [string]$Status,\n\n        [hashtable]$Results = @{},\n        [string]$Step = \"\"\n    )\n\n    if (-not $Global:CyberneticConsoleState) {\n        Write-Verbose \"Cybernetic Console not initialized, skipping sequence log\"\n        return\n    }\n\n    try {\n        $metadata = @{\n            sequence = $Sequence\n            status = $Status\n            results = $Results\n        }\n\n        if ($Step) {\n            $metadata.step = $Step\n        }\n\n        $Global:CyberneticConsoleState.Memory.WriteSequenceEntry(\n            $Global:CyberneticConsoleState.Session.SessionId,\n            \"Sequence: $Sequence - Status: $Status\",\n            $metadata\n        )\n\n        Write-Verbose \"Sequence logged: $Sequence ($Status)\"\n    }\n    catch {\n        Write-Warning \"Sequence logging failed: $($_.Exception.Message)\"\n    }\n} |
