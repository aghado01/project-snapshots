idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
6 | CyberneticMemorySystem.psm1 | {5891 423 0.3071 4.6127} | 6093 | class CyberneticMemorySystem {\n    [string]$SequenceDirectory\n    [string]$ProjectDirectory\n    [string]$GlobalDirectory\n\n    CyberneticMemorySystem([string]$sequenceRoot, [string]$projectRoot, [string]$globalRoot) {\n        foreach ($path in @($sequenceRoot, $projectRoot, $globalRoot)) {\n            if (-not (Test-Path -LiteralPath $path)) {\n                New-Item -ItemType Directory -Path $path -Force | Out-Null\n            }\n        }\n\n        $this.SequenceDirectory = $sequenceRoot\n        $this.ProjectDirectory  = $projectRoot\n        $this.GlobalDirectory   = $globalRoot\n    }\n\n    [string] ComputeContentHash([string]$text) {\n        $bytes = [Encoding]::UTF8.GetBytes($text)\n        $hashAlgorithm = [SHA256]::Create()\n        try {\n            $hashBytes = $hashAlgorithm.ComputeHash($bytes)\n            return ([BitConverter]::ToString($hashBytes)).Replace('-', '').ToLowerInvariant()\n        }\n        finally {\n            $hashAlgorithm.Dispose()\n        }\n    }\n\n    [void] WriteSequenceEntry([string]$sessionId, [string]$text, [hashtable]$metadata) {\n        $hash = $this.ComputeContentHash($text)\n        $entry = @{\n            sessionId = $sessionId\n            hash      = $hash\n            text      = $text\n            metadata  = $metadata\n            recorded  = [DateTime]::UtcNow.ToString('o')\n        }\n\n        $fileName = \"sequence-$($sessionId).jsonl\"\n        $path = Join-Path -Path $this.SequenceDirectory -ChildPath $fileName\n        $entry | ConvertTo-Json -Depth 6 | Add-Content -Path $path\n    }\n\n    [void] WriteProjectEntry([string]$workspaceRoot, [string]$text, [hashtable]$metadata) {\n        $hash = $this.ComputeContentHash($text)\n        $entry = @{\n            workspace = $workspaceRoot\n            hash      = $hash\n            text      = $text\n            metadata  = $metadata\n            recorded  = [DateTime]::UtcNow.ToString('o')\n        }\n\n        $path = Join-Path -Path $this.ProjectDirectory -ChildPath 'project-log.jsonl'\n        $entry | ConvertTo-Json -Depth 6 | Add-Content -Path $path\n    }\n\n    [void] WriteGlobalInsight([string]$text, [hashtable]$metadata) {\n        $hash = $this.ComputeContentHash($text)\n        $entry = @{\n            hash     = $hash\n            text     = $text\n            metadata = $metadata\n            recorded = [DateTime]::UtcNow.ToString('o')\n        }\n\n        $path = Join-Path -Path $this.GlobalDirectory -ChildPath 'global-log.jsonl'\n        $entry | ConvertTo-Json -Depth 6 | Add-Content -Path $path\n    }\n}\n\nclass Cybernetics {\n    [CyberneticSession]$Session\n    [CyberneticConsole]$Console\n    [CyberneticFeedback]$Feedback\n    [string]$WorkspaceRoot\n    [string]$MemoryRoot\n    [bool]$Initialized\n\n    Cybernetics([string]$workspaceRoot, [string]$memoryRoot) {\n        $this.WorkspaceRoot = $workspaceRoot\n        $this.MemoryRoot = $memoryRoot\n    }\n\n    [void] Initialize() {\n        if ($this.Initialized) {\n            return\n        }\n\n        $this.Session = [CyberneticSession]::new($this.WorkspaceRoot, $this.MemoryRoot)\n        $this.Session.Start()\n\n        $transcriptPath = Join-Path -Path $this.Session.SessionDirectory -ChildPath \"console-$($this.Session.SessionId).transcript\"\n        $this.Console = [CyberneticConsole]::new($this.WorkspaceRoot, $transcriptPath)\n        $this.Console.InstallPrompt($this.Session.SessionId)\n        $this.Console.StartCapture()\n\n        $this.Feedback = [CyberneticFeedback]::new()\n\n        $this.Session.RecordEvent(\n            'ConsoleInitialized',\n            @{\n                transcript = $transcriptPath\n                prompt     = 'CyberneticPrompt'\n                sessionId  = $this.Session.SessionId\n                timestamp  = [DateTime]::UtcNow.ToString('o')\n            }\n        )\n\n        $this.Initialized = $true\n    }\n\n    [void] Shutdown() {\n        try {\n            if ($null -ne $this.Console) {\n                $this.Console.StopCapture()\n                $this.Console.RestorePrompt()\n            }\n            if ($null -ne $this.Session) {\n                $this.Session.RecordEvent(\n                    'SessionEnded',\n                    @{\n                        Timestamp = [DateTime]::UtcNow.ToString('o')\n                    }\n                )\n                $this.Session.Dispose()\n            }\n        }\n        catch {\n\n        }\n    }\n}\n\nfunction Invoke-MemoryHousekeeping {\n    \n    [CmdletBinding()]\n    param()\n\n    $copilotRoot = Find-CopilotRoot\n    if (-not $copilotRoot) { return }\n\n    $dumpsDir = Join-Path $copilotRoot \"dumps\"\n    $oldDumps = Get-ChildItem $dumpsDir -Filter \"*.psd\" |\n        Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-1) }\n\n    foreach ($dump in $oldDumps) {\n\n        $gzPath = \"$($dump.FullName).gz\"\n        if (-not (Test-Path $gzPath)) {\n            $content = Get-Content $dump.FullName -Raw\n            $compressed = if (Get-Command Compress-WhitespaceRuns -ErrorAction SilentlyContinue) {\n                Compress-WhitespaceRuns -Text $content\n            } else {\n                [regex]::Replace($content, '\\s+', ' ').Trim()\n            }\n            $bytes = [System.Text.Encoding]::UTF8.GetBytes($compressed)\n\n            $gzStream = [System.IO.File]::Create($gzPath)\n            $gzWriter = New-Object System.IO.Compression.GZipStream($gzStream,\n                [System.IO.Compression.CompressionMode]::Compress)\n            $gzWriter.Write($bytes, 0, $bytes.Length)\n            $gzWriter.Close()\n\n            Remove-Item $dump.FullName\n            Write-Verbose \"Compressed: $($dump.Name) → $($dump.Name).gz\"\n        }\n    }\n\n    $veryOldDumps = Get-ChildItem $dumpsDir -Filter \"*.psd.gz\" |\n        Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-7) }\n\n    foreach ($dump in $veryOldDumps) {\n        Remove-Item $dump.FullName\n        Write-Verbose \"Rotated: $($dump.Name)\"\n    }\n\n    Write-Verbose \"✓ Housekeeping complete\"\n} |
7 | CyberneticSupervisor.psm1 | {10681 828 0.2899 4.6459} | 11086 | \n\nclass CyberneticSession {\n    [string]$SessionId\n    [string]$WorkspaceRoot\n    [datetime]$Started\n    [string]$SessionDirectory\n    [string]$SequenceDirectory\n    [string]$ProjectDirectory\n    [string]$GlobalDirectory\n    [string]$SessionLogPath\n    [System.Collections.Generic.List[System.IDisposable]]$Disposables\n\n    CyberneticSession([string]$workspaceRoot, [string]$memoryRoot) {\n        if ([string]::IsNullOrWhiteSpace($workspaceRoot)) {\n            throw [ArgumentException]::new(\"Workspace root cannot be empty.\", 'workspaceRoot')\n        }\n        if ([string]::IsNullOrWhiteSpace($memoryRoot)) {\n            throw [ArgumentException]::new(\"Memory root cannot be empty.\", 'memoryRoot')\n        }\n\n        $this.WorkspaceRoot = [Path]::GetFullPath($workspaceRoot)\n        $memoryRootFull = [Path]::GetFullPath($memoryRoot)\n\n        $this.SessionDirectory  = Join-Path -Path $memoryRootFull -ChildPath 'session'\n        $this.SequenceDirectory = Join-Path -Path $memoryRootFull -ChildPath 'sequence'\n        $this.ProjectDirectory  = Join-Path -Path $memoryRootFull -ChildPath 'project'\n        $this.GlobalDirectory   = Join-Path -Path $memoryRootFull -ChildPath 'global'\n        $this.Disposables = [System.Collections.Generic.List[System.IDisposable]]::new()\n    }\n\n    [void] Start() {\n        $this.SessionId = [Guid]::NewGuid().ToString('N')\n        $this.Started = [DateTime]::UtcNow\n\n        foreach ($directory in @(\n            $this.SessionDirectory,\n            $this.SequenceDirectory,\n            $this.ProjectDirectory,\n            $this.GlobalDirectory\n        )) {\n            if (-not (Test-Path -LiteralPath $directory)) {\n                New-Item -ItemType Directory -Path $directory -Force | Out-Null\n            }\n        }\n\n        $sessionFileName = \"session-$($this.SessionId).jsonl\"\n        $this.SessionLogPath = Join-Path -Path $this.SessionDirectory -ChildPath $sessionFileName\n\n        $this.RecordEvent(\n            'SessionStarted',\n            @{\n                Workspace = $this.WorkspaceRoot\n                Timestamp = $this.Started.ToString('o')\n            }\n        )\n\n        $null = Register-EngineEvent -SourceIdentifier PowerShell.Exiting -Action {\n            try {\n                if ($null -ne $args[1]) {\n                    $args[1].Unregister()\n                }\n            }\n            catch {\n\n            }\n        } -MessageData $this | ForEach-Object {\n            $this.Disposables.Add($_)\n        }\n    }\n\n    [void] RecordEvent([string]$eventName, [hashtable]$payload) {\n        if ([string]::IsNullOrWhiteSpace($this.SessionLogPath)) {\n            throw [InvalidOperationException]::new(\"Session has not been started; call Start() before RecordEvent().\")\n        }\n\n        $entry = @{\n            sessionId = $this.SessionId\n            event     = $eventName\n            payload   = $payload\n            recorded  = [DateTime]::UtcNow.ToString('o')\n        }\n\n        $json = $entry | ConvertTo-Json -Depth 6\n        Add-Content -Path $this.SessionLogPath -Value $json\n    }\n\n    [void] Dispose() {\n        foreach ($disposable in $this.Disposables) {\n            try {\n                if ($null -ne $disposable) {\n                    $disposable.Dispose()\n                }\n            }\n            catch {\n\n            }\n        }\n        $this.Disposables.Clear()\n    }\n}\n\nfunction Get-CyberneticSession {\n    \n    if ($null -eq $Global:CyberneticSession) {\n        throw \"No active session. Run Start-CyberneticSession first.\"\n    }\n    return $Global:CyberneticSession\n}\n\nclass CyberneticFeedback {\n    [System.Collections.Generic.List[hashtable]]$Events\n\n    CyberneticFeedback() {\n        $this.Events = [System.Collections.Generic.List[hashtable]]::new()\n    }\n\n    [void] Push([string]$category, [string]$message, [hashtable]$metadata) {\n        $this.Events.Add(@{\n            category = $category\n            message  = $message\n            metadata = $metadata\n            recorded = [DateTime]::UtcNow.ToString('o')\n        })\n    }\n\n    [hashtable[]] Flush() {\n        $snapshot = $this.Events.ToArray()\n        $this.Events.Clear()\n        return $snapshot\n    }\n}\n\nfunction Add-CyberneticEvent {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$EventName,\n\n        [hashtable]$Payload = @{}\n    )\n\n    $session = Get-CyberneticSession\n    $session.RecordEvent($EventName, $Payload)\n\n    Write-Verbose \"Event recorded: $EventName\"\n}\n\nfunction Get-SessionStats {\n    \n    [CmdletBinding()]\n    param()\n\n    $session = Get-CyberneticSession\n\n    return [PSCustomObject]@{\n        PSTypeName = 'CyberneticSession.Stats'\n        SessionId = $session.SessionId\n        Started = $session.Started\n        Duration = (Get-Date) - $session.Started\n        WorkspaceRoot = $session.WorkspaceRoot\n        SessionLog = $session.SessionLogPath\n        TranscriptPath = if ($Global:ConsoleUI) { $Global:ConsoleUI.TranscriptPath } else { \"N/A\" }\n        MemoryLocations = [PSCustomObject]@{\n            Session = $session.SessionDirectory\n            Sequence = $session.SequenceDirectory\n            Project = $session.ProjectDirectory\n            Global = $session.GlobalDirectory\n        }\n    }\n}\n\nfunction Get-SessionHistory {\n    \n    [CmdletBinding()]\n    param(\n        [ValidateSet('All', 'Event', 'Error', 'Warning', 'Info')]\n        [string]$Type = 'All',\n\n        [int]$Last = 0\n    )\n\n    $session = Get-CyberneticSession\n    $logPath = $session.SessionLogPath\n\n    if (-not (Test-Path $logPath)) {\n        Write-Warning \"Session log not found: $logPath\"\n        return @()\n    }\n\n    $entries = Get-Content $logPath | ForEach-Object {\n        try {\n            $_ | ConvertFrom-Json\n        } catch {\n            Write-Warning \"Failed to parse log entry: $_\"\n        }\n    }\n\n    if ($Type -ne 'All' -and $entries) {\n        $entries = $entries | Where-Object { $_.Type -eq $Type -or $_.Level -eq $Type }\n    }\n\n    if ($Last -gt 0 -and $entries) {\n        $entries = $entries | Select-Object -Last $Last\n    }\n\n    return $entries\n}\n\nfunction Test-SessionHealth {\n    \n    [CmdletBinding()]\n    param()\n\n    $health = @{\n        SessionActive = $false\n        TranscriptWorking = $false\n        MemoryAccessible = $false\n        LoggingWorking = $false\n    }\n\n    try {\n        $session = Get-CyberneticSession\n        $health.SessionActive = $true\n\n        if ($Global:ConsoleUI -and $Global:ConsoleUI.TranscriptPath) {\n            if (Test-Path $Global:ConsoleUI.TranscriptPath) {\n                $health.TranscriptWorking = $true\n            }\n        }\n\n        if ((Test-Path $session.SessionDirectory) -and\n            (Test-Path $session.SequenceDirectory) -and\n            (Test-Path $session.ProjectDirectory) -and\n            (Test-Path $session.GlobalDirectory)) {\n            $health.MemoryAccessible = $true\n        }\n\n        if (Test-Path $session.SessionLogPath) {\n            $health.LoggingWorking = $true\n        }\n\n    } catch {\n\n    }\n\n    $allHealthy = ($health.Values | Where-Object { $_ -eq $false }).Count -eq 0\n\n    return [PSCustomObject]@{\n        Healthy = $allHealthy\n        Details = $health\n        Timestamp = Get-Date\n    }\n}\n\nfunction Register-ConsoleContextHook {\n\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$TriggerCommand,\n\n        [Parameter(Mandatory)]\n        [string]$AssistantAgent,\n\n        [Parameter()]\n        [ValidateSet('testing', 'building', 'deploying', 'debugging', 'general')]\n        [string]$ContextType = 'general',\n\n        [Parameter()]\n        [int]$MonitorDuration = 60\n    )\n\n    if (-not (Get-Command Set-PSReadLineOption -ErrorAction SilentlyContinue)) {\n        Write-Warning \"PSReadLine not available. Context hooks require PSReadLine.\"\n        return\n    }\n\n    $hookDefinition = @{\n        TriggerCommand    = $TriggerCommand\n        AssistantAgent    = $AssistantAgent\n        ContextType       = $ContextType\n        MonitorDuration   = $MonitorDuration\n        Registered        = Get-Date\n        ConsoleAware      = $true\n        AutoTrigger       = $true\n    }\n\n    if ($null -eq $Global:ConsoleContextHooks) {\n        $Global:ConsoleContextHooks = @{}\n    }\n\n    $hookKey = \"$ContextType`:$TriggerCommand\"\n    $Global:ConsoleContextHooks[$hookKey] = $hookDefinition\n\n    if ($Global:CyberneticConsoleState) {\n        $Global:CyberneticConsoleState.Feedback.Push(\n            'context_hook_registered',\n            \"Registered hook: $TriggerCommand → $AssistantAgent\",\n            $hookDefinition\n        )\n    }\n\n    Write-Host \"✅ Console context hook registered: $TriggerCommand → $AssistantAgent [$ContextType]\" -ForegroundColor Green\n}\n\nfunction Register-ConsoleAwareAssistanceHook {\n\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$TriggerCommand,\n\n        [Parameter(Mandatory)]\n        [string]$TetheredAgent,\n\n        [Parameter(Mandatory)]\n        [string]$ConsoleContext,\n\n        [int]$MonitoringDuration = 60\n    )\n\n    if (-not $script:ConsoleAssistanceHooks) {\n        $script:ConsoleAssistanceHooks = @{}\n    }\n\n    $descriptor = [ordered]@{\n        TriggerCommand     = $TriggerCommand\n        TetheredAgent      = $TetheredAgent\n        ConsoleContext     = $ConsoleContext\n        MonitoringDuration = $MonitoringDuration\n        ConsoleAware       = $true\n        AutoTrigger        = $true\n        Registered         = [DateTime]::UtcNow\n    }\n\n    $script:ConsoleAssistanceHooks[$ConsoleContext] = $descriptor\n\n    if ($Global:CyberneticConsoleState) {\n        try {\n            Write-CopilotSessionLog -Action 'console_hook_registered' -Metadata @{\n                context = $ConsoleContext\n                trigger = $TriggerCommand\n                agent   = $TetheredAgent\n            }\n        }\n        catch {\n            Write-Verbose \"Console hook registration logged without session context: $ConsoleContext\"\n        }\n    }\n\n    Write-Verbose \"Console-aware assistance hook registered: $ConsoleContext\"\n    return [PSCustomObject]$descriptor\n}\n\nSet-Alias -Name 'qstats' -Value Get-SessionStats -Scope Global -Force\nSet-Alias -Name 'qhist' -Value Get-SessionHistory -Scope Global -Force\nSet-Alias -Name 'qevent' -Value Add-CyberneticEvent -Scope Global -Force\nSet-Alias -Name 'qfeedback' -Value Write-CyberneticFeedback -Scope Global -Force\nSet-Alias -Name 'qhealth' -Value Test-SessionHealth -Scope Global -Force\n\nExport-ModuleMember -Function @(\n    'Get-CyberneticSession'\n    'Add-CyberneticEvent'\n    'Get-SessionStats'\n    'Get-SessionHistory'\n    'Write-CyberneticFeedback'\n    'Test-SessionHealth'\n) -Alias @('qstats', 'qhist', 'qevent', 'qfeedback', 'qstop', 'qhealth') |
