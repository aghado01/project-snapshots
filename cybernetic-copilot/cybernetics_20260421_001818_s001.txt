idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | CopilotContextManagement.psm1 | {5866 504 0.2426 4.8754} | 6113 | #Requires -Version 7.5\nSet-StrictMode -Version Latest\n\nclass ProactiveGuidanceInterceptor {\n    [hashtable]$ViolationPatterns = @{\n        'run_in_terminal_blocking' = @{\n            Pattern = '\"isBackground\":\\s*false|\"isBackground\":\\s*\\$false'\n            Guidance = 'METACOGNITIVE GUIDANCE: Use background execution (isBackground: true) to prevent console hazard. Follow RSVP patterns for confirmation.'\n            Severity = 'High'\n            Alternative = 'Use isBackground: true and get_terminal_output for confirmation'\n        }\n        'missing_structured_results' = @{\n            Pattern = '(?<!PSTypeName)return\\s+@?\\{[^}]*\\}'\n            Guidance = 'METACOGNITIVE GUIDANCE: Add PSTypeName to all structured results for harness discipline compliance.'\n            Severity = 'Medium'\n            Alternative = 'Add PSTypeName = \"Your.Type.Name\" to result objects'\n        }\n        'placeholder_code' = @{\n            Pattern = '\\.\\.\\.|\\#.*rest of|placeholder|TODO:|FIXME:'\n            Guidance = 'METACOGNITIVE GUIDANCE: Complete all code. No placeholders allowed in harness discipline.'\n            Severity = 'High'\n            Alternative = 'Write complete, executable code'\n        }\n        'missing_validation' = @{\n            Pattern = '(?<!ValidateScript|ExpectedType|-Validate)\\[Parameter.*\\]'\n            Guidance = 'METACOGNITIVE GUIDANCE: Add explicit validation (ValidateScript, ExpectedType) to all parameters.'\n            Severity = 'Medium'\n            Alternative = 'Use [ValidateScript()] or [ExpectedType()] attributes'\n        }\n    }\n\n    [PSCustomObject[]] AnalyzeToolCall([string]$toolName, [hashtable]$parameters) {\n        $violations = @()\n\n        $paramText = $parameters | ConvertTo-Json -Depth 5\n\n        foreach ($violationType in $this.ViolationPatterns.Keys) {\n            $pattern = $this.ViolationPatterns[$violationType]\n\n            if ($paramText -match $pattern.Pattern) {\n                $violations += [PSCustomObject]@{\n                    PSTypeName = 'ProactiveGuidance.Violation'\n                    Type = $violationType\n                    Severity = $pattern.Severity\n                    Guidance = $pattern.Guidance\n                    Alternative = $pattern.Alternative\n                    ToolName = $toolName\n                }\n            }\n        }\n\n        return $violations\n    }\n\n    [string] GenerateProactiveGuidance([PSCustomObject[]]$violations) {\n        if ($violations.Count -eq 0) {\n            return \"\"\n        }\n\n        $guidanceText = @\"\n🧠 METACOGNITIVE INTERCEPTOR ACTIVATED\n\nThe following protocol violations were detected in your planned tool call:\n\n\"@\n\n        foreach ($violation in $violations) {\n            $guidanceText += @\"\n\n⚠️  VIOLATION: $($violation.Type.ToUpper()) [$($violation.Severity)]\n    Guidance: $($violation.Guidance)\n    Alternative: $($violation.Alternative)\n\n\"@\n        }\n\n        $guidanceText += @\"\n\nPROSTHETIC DISCIPLINE REMINDERS:\n✓ Background execution ONLY (isBackground: true)\n✓ Structured results ONLY (PSTypeName required)\n✓ Complete code ONLY (no placeholders)\n✓ Explicit validation ONLY (ValidateScript, ExpectedType)\n✓ Toolkit functions ONLY (Test-ModuleFunction, Invoke-WithSnapshot)\n\nPlease modify your tool call to follow these protocols before execution.\n\"@\n\n        return $guidanceText\n    }\n}\n\n$global:ProactiveGuidanceInterceptor = [ProactiveGuidanceInterceptor]::new()\nWrite-Verbose \"Proactive guidance interceptor initialized\"\n\nfunction Invoke-ProactiveGuidanceCheck {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$ToolName,\n\n        [Parameter(Mandatory)]\n        [hashtable]$Parameters\n    )\n\n    if (-not $global:ProactiveGuidanceInterceptor) {\n        $global:ProactiveGuidanceInterceptor = [ProactiveGuidanceInterceptor]::new()\n    }\n\n    $violations = $global:ProactiveGuidanceInterceptor.AnalyzeToolCall($ToolName, $Parameters)\n\n    if ($violations -and $violations.Count -gt 0) {\n        $guidance = $global:ProactiveGuidanceInterceptor.GenerateProactiveGuidance($violations)\n\n        if ($global:MetacognitivePrimingTracker) {\n\n            $global:MetacognitivePrimingTracker.RecordQuality(0.8, @(\"Proactive intervention prevented $($violations.Count) violations\"))\n        }\n\n        return $guidance\n    }\n\n    return $null\n}\n\nfunction Test-ProactiveGuidance {\n    \n    [CmdletBinding()]\n    param()\n\n    Write-Host \"🧪 Testing Proactive Metacognitive Guidance\" -ForegroundColor Cyan\n    Write-Host \"=\" * 50 -ForegroundColor Gray\n\n    Write-Host \"`n📋 Test 1: Blocking Terminal Call\" -ForegroundColor Yellow\n    $guidance1 = Invoke-ProactiveGuidanceCheck -ToolName 'run_in_terminal' -Parameters @{\n        command = 'Get-Process'\n        isBackground = $false\n        explanation = 'Check processes'\n    }\n    if ($guidance1) {\n        Write-Host $guidance1 -ForegroundColor Red\n    } else {\n        Write-Host \"✅ No violations detected\" -ForegroundColor Green\n    }\n\n    Write-Host \"`n📋 Test 2: Correct Background Execution\" -ForegroundColor Yellow\n    $guidance2 = Invoke-ProactiveGuidanceCheck -ToolName 'run_in_terminal' -Parameters @{\n        command = 'Get-Process'\n        isBackground = $true\n        explanation = 'Check processes in background'\n    }\n    if ($guidance2) {\n        Write-Host $guidance2 -ForegroundColor Red\n    } else {\n        Write-Host \"✅ No violations detected - proper background execution\" -ForegroundColor Green\n    }\n\n    Write-Host \"`n🎯 Proactive guidance system ready to intercept violations!\" -ForegroundColor Cyan\n}\n\nExport-ModuleMember -Function @(\n    'Invoke-ProactiveGuidanceCheck'\n    'Test-ProactiveGuidance'\n) -Variable @('ProactiveGuidanceInterceptor')\n\nWrite-Host \"🛡️  Proactive Metacognitive Interceptor loaded\" -ForegroundColor Green\nWrite-Host \"   Use 'Invoke-ProactiveGuidanceCheck' before tool calls\" -ForegroundColor Gray |
1 | CopilotObservation.psm1 | {10170 944 0.2779 4.8506} | 10641 | using namespace System\nusing namespace System.IO\nusing namespace System.Text\n\n#Requires -Version 7.5\n\nSet-StrictMode -Version Latest\n$ErrorActionPreference = 'Stop'\n\n[Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSUseApprovedVerbs', '')]\nparam()\n\n$script:RepoRoot = Split-Path (Split-Path $PSScriptRoot -Parent) -Parent\n$script:MathdigHashPath = Join-Path $script:RepoRoot 'ps.core.mathdig\\src\\ps.core.math.hash.psm1'\nif (-not (Test-Path $script:MathdigHashPath)) {\n    throw \"mathdig hashing module not found at: $script:MathdigHashPath — cannot load CopilotObservation without it\"\n}\nImport-Module $script:MathdigHashPath -Force -ErrorAction Stop\n\nfunction Compress-WhitespaceRuns {\n    \n    [CmdletBinding()]\n    [OutputType([string])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Text\n    )\n\n    if ([string]::IsNullOrEmpty($Text)) { return '' }\n    return [regex]::Replace($Text, '\\s+', ' ').Trim()\n}\n\nAdd-Type -TypeDefinition @\"\nusing System;\npublic class CopilotObservation\n{\n    public string Type { get; set; }           // console | file | git | test | error\n    public DateTime Timestamp { get; set; }\n    public string Hash { get; set; }           // 8-char rolling hash\n    public string Summary { get; set; }        // Compressed, structured text\n    public object Metadata { get; set; }       // Context dictionary\n    public int TokenEstimate { get; set; }     // Estimated tokens\n}\n\"@\n\nfunction New-ConsoleObservation {\n    \n    [CmdletBinding()]\n    [OutputType([CopilotObservation])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Command,\n\n        [Parameter(Mandatory)]\n        [string]$Output,\n\n        [Parameter()]\n        [int]$ExitCode = 0,\n\n        [Parameter()]\n        [int]$Duration = 0\n    )\n\n    $compressedOutput = Compress-WhitespaceRuns -Text $Output\n\n    $maxLength = 1000\n    if ($compressedOutput.Length -gt $maxLength) {\n        $head = $compressedOutput.Substring(0, $maxLength / 2)\n        $tail = $compressedOutput.Substring($compressedOutput.Length - ($maxLength / 2))\n        $compressedOutput = \"$head`n[... truncated ...]`n$tail\"\n    }\n\n    $hash = (Get-ContentHash -Content \"$Command|$compressedOutput\").Substring(0, 8)\n\n    $summary = if ($ExitCode -eq 0) {\n        \"✓ $Command → ${compressedOutput}\"\n    } else {\n        \"✗ [$ExitCode] $Command → ${compressedOutput}\"\n    }\n\n    $tokenEstimate = [Math]::Ceiling($summary.Length / 4)\n\n    $observation = [CopilotObservation]@{\n        Type = 'console'\n        Timestamp = [DateTime]::UtcNow\n        Hash = $hash\n        Summary = $summary\n        Metadata = @{\n            command = $Command\n            exitCode = $ExitCode\n            duration_ms = $Duration\n        }\n        TokenEstimate = $tokenEstimate\n    }\n\n    return $observation\n}\n\nfunction New-FileObservation {\n    \n    [CmdletBinding()]\n    [OutputType([CopilotObservation])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath,\n\n        [Parameter(Mandatory)]\n        [ValidateSet('created', 'modified', 'deleted')]\n        [string]$ChangeType,\n\n        [Parameter()]\n        [string]$Content = $null\n    )\n\n    $fileName = Split-Path -Leaf $FilePath\n    $hash = if (Get-Command Get-PathHash -ErrorAction SilentlyContinue) {\n        (Get-PathHash -Path $FilePath).Substring(0, 8)\n    } elseif (Get-Command Get-ContentHash -ErrorAction SilentlyContinue) {\n        (Get-ContentHash -Content $FilePath).Substring(0, 8)\n    } else {\n        $bytes = [Text.Encoding]::UTF8.GetBytes($FilePath)\n        $h = 2166136261L\n        foreach ($b in $bytes) { $h = ($h -bxor $b) * 16777619 }\n        [Convert]::ToString($h, 16).PadLeft(8, '0').Substring(0, 8)\n    }\n\n    $summary = switch ($ChangeType) {\n        'created' { \"📄 Created: $fileName\" }\n        'modified' { \"✏️  Modified: $fileName\" }\n        'deleted' { \"🗑️  Deleted: $fileName\" }\n    }\n\n    if ($Content -and $ChangeType -ne 'deleted') {\n        $preview = $Content.Substring(0, [Math]::Min(200, $Content.Length))\n        if ($Content.Length -gt 200) {\n            $preview += \"...\"\n        }\n        $summary += \"`n  Preview: $preview\"\n    }\n\n    $tokenEstimate = [Math]::Ceiling($summary.Length / 4)\n\n    $observation = [CopilotObservation]@{\n        Type = 'file'\n        Timestamp = [DateTime]::UtcNow\n        Hash = $hash\n        Summary = $summary\n        Metadata = @{\n            path = $FilePath\n            changeType = $ChangeType\n            hasContent = ($null -ne $Content)\n        }\n        TokenEstimate = $tokenEstimate\n    }\n\n    return $observation\n}\n\nfunction New-GitObservation {\n    \n    [CmdletBinding()]\n    [OutputType([CopilotObservation])]\n    param(\n        [Parameter(Mandatory)]\n        [ValidateSet('commit', 'branch', 'merge', 'pull', 'push', 'checkout')]\n        [string]$Operation,\n\n        [Parameter(Mandatory)]\n        [string]$Details\n    )\n\n    $hash = if (Get-Command Get-ContentHash -ErrorAction SilentlyContinue) {\n        (Get-ContentHash -Content \"$Operation|$Details\").Substring(0, 8)\n    } else {\n        $bytes = [Text.Encoding]::UTF8.GetBytes(\"$Operation|$Details\")\n        $h = 2166136261L\n        foreach ($b in $bytes) { $h = ($h -bxor $b) * 16777619 }\n        [Convert]::ToString($h, 16).PadLeft(8, '0').Substring(0, 8)\n    }\n\n    $icon = switch ($Operation) {\n        'commit' { '💾' }\n        'branch' { '🌿' }\n        'merge' { '🔀' }\n        'pull' { '⬇️' }\n        'push' { '⬆️' }\n        'checkout' { '↪️' }\n    }\n\n    $summary = \"$icon Git $Operation`: $Details\"\n    $tokenEstimate = [Math]::Ceiling($summary.Length / 4)\n\n    $observation = [CopilotObservation]@{\n        Type = 'git'\n        Timestamp = [DateTime]::UtcNow\n        Hash = $hash\n        Summary = $summary\n        Metadata = @{\n            operation = $Operation\n            details = $Details\n        }\n        TokenEstimate = $tokenEstimate\n    }\n\n    return $observation\n}\n\nfunction New-TestObservation {\n    \n    [CmdletBinding()]\n    [OutputType([CopilotObservation])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$TestName,\n\n        [Parameter(Mandatory)]\n        [ValidateSet('passed', 'failed', 'skipped')]\n        [string]$Result,\n\n        [Parameter()]\n        [int]$Duration = 0,\n\n        [Parameter()]\n        [string]$FailureMessage = $null\n    )\n\n    $hash = if (Get-Command Get-ContentHash -ErrorAction SilentlyContinue) {\n        (Get-ContentHash -Content \"$TestName|$Result\").Substring(0, 8)\n    } else {\n        $bytes = [Text.Encoding]::UTF8.GetBytes(\"$TestName|$Result\")\n        $h = 2166136261L\n        foreach ($b in $bytes) { $h = ($h -bxor $b) * 16777619 }\n        [Convert]::ToString($h, 16).PadLeft(8, '0').Substring(0, 8)\n    }\n\n    $icon = switch ($Result) {\n        'passed' { '✅' }\n        'failed' { '❌' }\n        'skipped' { '⏭️' }\n    }\n\n    $summary = \"$icon Test $Result`: $TestName\"\n    if ($Duration -gt 0) {\n        $summary += \" (${Duration}ms)\"\n    }\n    if ($FailureMessage) {\n        $failurePreview = $FailureMessage.Substring(0, [Math]::Min(200, $FailureMessage.Length))\n        $summary += \"`n  Error: $failurePreview\"\n    }\n\n    $tokenEstimate = [Math]::Ceiling($summary.Length / 4)\n\n    $observation = [CopilotObservation]@{\n        Type = 'test'\n        Timestamp = [DateTime]::UtcNow\n        Hash = $hash\n        Summary = $summary\n        Metadata = @{\n            testName = $TestName\n            result = $Result\n            duration_ms = $Duration\n            hasFailureMessage = ($null -ne $FailureMessage)\n        }\n        TokenEstimate = $tokenEstimate\n    }\n\n    return $observation\n}\n\nfunction New-ErrorObservation {\n    \n    [CmdletBinding()]\n    [OutputType([CopilotObservation])]\n    param(\n        [Parameter(Mandatory)]\n        [string]$ErrorMessage,\n\n        [Parameter()]\n        [string]$Source = 'runtime',\n\n        [Parameter()]\n        [string]$StackTrace = $null\n    )\n\n    $hash = if (Get-Command Get-ContentHash -ErrorAction SilentlyContinue) {\n        (Get-ContentHash -Content $ErrorMessage).Substring(0, 8)\n    } else {\n        $bytes = [Text.Encoding]::UTF8.GetBytes($ErrorMessage)\n        $h = 2166136261L\n        foreach ($b in $bytes) { $h = ($h -bxor $b) * 16777619 }\n        [Convert]::ToString($h, 16).PadLeft(8, '0').Substring(0, 8)\n    }\n\n    $compressedError = Compress-WhitespaceRuns -Text $ErrorMessage\n    if ($compressedError.Length -gt 300) {\n        $compressedError = $compressedError.Substring(0, 300) + \"...\"\n    }\n\n    $summary = \"🚨 Error [$Source]: $compressedError\"\n\n    if ($StackTrace) {\n        $stackPreview = $StackTrace -split \"`n\" | Select-Object -First 3 | ForEach-Object { \"  $_\" }\n        $summary += \"`n\" + ($stackPreview -join \"`n\")\n    }\n\n    $tokenEstimate = [Math]::Ceiling($summary.Length / 4)\n\n    $observation = [CopilotObservation]@{\n        Type = 'error'\n        Timestamp = [DateTime]::UtcNow\n        Hash = $hash\n        Summary = $summary\n        Metadata = @{\n            source = $Source\n            hasStackTrace = ($null -ne $StackTrace)\n        }\n        TokenEstimate = $tokenEstimate\n    }\n\n    return $observation\n}\n\nfunction ConvertTo-ObservationLog {\n    \n    [CmdletBinding()]\n    [OutputType([string])]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [CopilotObservation]$Observation\n    )\n\n    process {\n        $logEntry = @{\n            type = $Observation.Type\n            timestamp = $Observation.Timestamp.ToString('o')\n            hash = $Observation.Hash\n            summary = $Observation.Summary\n            metadata = $Observation.Metadata\n            tokens = $Observation.TokenEstimate\n        }\n\n        return ($logEntry | ConvertTo-Json -Depth 5 -Compress)\n    }\n}\n\nfunction Get-ObservationTokenCount {\n    \n    [CmdletBinding()]\n    [OutputType([int])]\n    param(\n        [Parameter(Mandatory)]\n        [CopilotObservation[]]$Observations\n    )\n\n    $total = 0\n    foreach ($obs in $Observations) {\n        $total += $obs.TokenEstimate\n    }\n\n    return $total\n}\n\nExport-ModuleMember -Function @(\n    'New-ConsoleObservation'\n    'New-FileObservation'\n    'New-GitObservation'\n    'New-TestObservation'\n    'New-ErrorObservation'\n    'ConvertTo-ObservationLog'\n    'Get-ObservationTokenCount'\n) |
2 | CopilotSupervision.psm1 | {8697 681 0.2497 4.7130} | 9061 | \n\n$script:SupervisionConfig = @{\n    CircularThreshold = 3                    # Repetitions before triggering alert\n    MonitoringWindowMinutes = 30             # Sliding window for pattern detection\n    HyperactivityThreshold = 20              # File changes in 10min = thrashing\n    InactivityThresholdMinutes = 15          # No activity = potential stuck state\n    SimilarityThreshold = 0.8                # Code similarity score for circular detection\n}\n\n$script:SupervisionTriggers = @{\n    circular_code_generation = @{\n        threshold = 3\n        detection = \"code_similarity_analysis\"\n        severity = \"critical\"\n        action = \"halt_and_redirect\"\n    }\n    repetitive_file_modifications = @{\n        threshold = 4\n        detection = \"file_change_pattern\"\n        severity = \"warning\"\n        action = \"diplomatic_redirection\"\n    }\n    task_drift_detection = @{\n        threshold = 2\n        detection = \"objective_deviation_analysis\"\n        severity = \"moderate\"\n        action = \"goal_realignment\"\n    }\n    context_loss_indicators = @{\n        threshold = 1\n        detection = \"memory_continuity_check\"\n        severity = \"critical\"\n        action = \"context_reconstruction\"\n    }\n    infinite_loop_behavior = @{\n        threshold = 2\n        detection = \"action_sequence_analysis\"\n        severity = \"critical\"\n        action = \"emergency_halt\"\n    }\n}\n\n$script:SessionHistory = @{\n    SessionStart = Get-Date\n    Operations = @()\n    FileModifications = @()\n    CodeGenerations = @()\n    SupervisionEvents = @()\n}\n\nfunction Initialize-MetacognitiveOversight {\n    \n    [CmdletBinding()]\n    param()\n\n    $script:SessionHistory.SessionStart = Get-Date\n    $script:SessionHistory.Operations = @()\n    $script:SessionHistory.FileModifications = @()\n    $script:SessionHistory.CodeGenerations = @()\n    $script:SessionHistory.SupervisionEvents = @()\n\n    Write-Host \"🎯 Metacognitive Oversight Initialized\" -ForegroundColor Green\n    Write-Host \"👁️  Pattern monitoring: Active\" -ForegroundColor Cyan\n    Write-Host \"🚨 Circuit breakers: Armed\" -ForegroundColor Yellow\n}\n\nfunction Register-FileModification {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath,\n\n        [Parameter(Mandatory)]\n        [ValidateSet('Modified', 'Created', 'Deleted')]\n        [string]$ChangeType\n    )\n\n    if ($FilePath -match \"(supervision|monitor|log)\" -or $FilePath -match \"\\.(log|tmp)$\") {\n        return\n    }\n\n    $record = @{\n        FilePath = $FilePath\n        ChangeType = $ChangeType\n        Timestamp = Get-Date\n        ContentHash = Get-FileContentHashSafe $FilePath\n    }\n\n    $script:SessionHistory.FileModifications += $record\n\n    Test-CircularBehavior -BehaviorType 'file_modification' -CurrentAction $record\n}\n\nfunction Test-CircularBehavior {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [ValidateSet('file_modification', 'code_generation', 'task_execution')]\n        [string]$BehaviorType,\n\n        [Parameter(Mandatory)]\n        [hashtable]$CurrentAction\n    )\n\n    $recentActions = Get-RecentActions -BehaviorType $BehaviorType -WindowMinutes $script:SupervisionConfig.MonitoringWindowMinutes\n\n    switch ($BehaviorType) {\n        'file_modification' {\n            Test-RepetitiveFileChanges -RecentActions $recentActions -CurrentAction $CurrentAction\n        }\n        'code_generation' {\n            Test-CircularCodeGeneration -RecentActions $recentActions -CurrentAction $CurrentAction\n        }\n        'task_execution' {\n            Test-TaskDrift -RecentActions $recentActions -CurrentAction $CurrentAction\n        }\n    }\n}\n\nfunction Test-RepetitiveFileChanges {\n    \n    [CmdletBinding()]\n    param(\n        [array]$RecentActions,\n        [hashtable]$CurrentAction\n    )\n\n    $sameFileChanges = $RecentActions | Where-Object { $_.FilePath -eq $CurrentAction.FilePath }\n\n    if ($sameFileChanges.Count -ge $script:SupervisionTriggers.repetitive_file_modifications.threshold) {\n        $contentHashes = $sameFileChanges | Select-Object -ExpandProperty ContentHash\n        $uniqueHashes = $contentHashes | Select-Object -Unique\n\n        if ($uniqueHashes.Count -le 2) {\n\n            Invoke-SupervisionAlert -TriggerType 'repetitive_file_modifications' -Context @{\n                FilePath = $CurrentAction.FilePath\n                ChangeCount = $sameFileChanges.Count\n                ContentHashes = $contentHashes\n                Pattern = \"circular_file_thrashing\"\n            }\n        }\n    }\n}\n\nfunction Test-CircularCodeGeneration {\n    \n    [CmdletBinding()]\n    param(\n        [array]$RecentActions,\n        [hashtable]$CurrentAction\n    )\n\n    $codeGenerations = $RecentActions | Where-Object { $_.Type -eq 'code_generation' }\n\n    if ($codeGenerations.Count -ge $script:SupervisionTriggers.circular_code_generation.threshold) {\n        $similarities = Get-CodeSimilarities -CodeGenerations $codeGenerations\n\n        if ($similarities.MaxSimilarity -gt $script:SupervisionConfig.SimilarityThreshold) {\n            Invoke-SupervisionAlert -TriggerType 'circular_code_generation' -Context @{\n                SimilarityScore = $similarities.MaxSimilarity\n                GenerationCount = $codeGenerations.Count\n                Pattern = $similarities.Pattern\n            }\n        }\n    }\n}\n\nfunction Invoke-SupervisionAlert {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$TriggerType,\n\n        [Parameter(Mandatory)]\n        [hashtable]$Context\n    )\n\n    $trigger = $script:SupervisionTriggers[$TriggerType]\n\n    Write-Host \"`n⚠️  SUPERVISION ALERT: $TriggerType\" -ForegroundColor Red\n    Write-Host \"📊 Detection: $($trigger.detection)\" -ForegroundColor Yellow\n    Write-Host \"🔴 Severity: $($trigger.severity)\" -ForegroundColor Magenta\n    Write-Host \"🔧 Recommended: $($trigger.action)\" -ForegroundColor Cyan\n\n    $event = @{\n        Timestamp = Get-Date\n        TriggerType = $TriggerType\n        Severity = $trigger.severity\n        Action = $trigger.action\n        Context = $Context\n    }\n\n    $script:SessionHistory.SupervisionEvents += $event\n\n    return $event\n}\n\nfunction Get-RecentActions {\n    \n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$BehaviorType,\n\n        [Parameter(Mandatory)]\n        [int]$WindowMinutes\n    )\n\n    $cutoffTime = (Get-Date).AddMinutes(-$WindowMinutes)\n\n    switch ($BehaviorType) {\n        'file_modification' {\n            return $script:SessionHistory.FileModifications | Where-Object { $_.Timestamp -gt $cutoffTime }\n        }\n        'code_generation' {\n            return $script:SessionHistory.CodeGenerations | Where-Object { $_.Timestamp -gt $cutoffTime }\n        }\n        default {\n            return @()\n        }\n    }\n}\n\nfunction Get-CodeSimilarities {\n    \n    [CmdletBinding()]\n    param([array]$CodeGenerations)\n\n    if ($CodeGenerations.Count -lt 2) {\n        return @{ MaxSimilarity = 0; Pattern = \"insufficient_data\" }\n    }\n\n    $fileTargets = $CodeGenerations | Group-Object -Property FilePath\n    $maxRepeats = ($fileTargets | Measure-Object -Property Count -Maximum).Maximum\n\n    $similarity = [Math]::Min(0.9, $maxRepeats / 10.0)\n\n    return @{\n        MaxSimilarity = $similarity\n        Pattern = \"repetitive_file_targeting\"\n        Details = $fileTargets\n    }\n}\n\nfunction Get-SupervisionReport {\n    \n    [CmdletBinding()]\n    param()\n\n    $sessionDuration = (Get-Date) - $script:SessionHistory.SessionStart\n\n    return @{\n        SessionDuration = $sessionDuration\n        TotalFileModifications = $script:SessionHistory.FileModifications.Count\n        TotalCodeGenerations = $script:SessionHistory.CodeGenerations.Count\n        SupervisionAlerts = $script:SessionHistory.SupervisionEvents.Count\n        AlertsByType = $script:SessionHistory.SupervisionEvents | Group-Object -Property TriggerType\n        SessionHealth = Get-SessionHealthStatus\n    }\n}\n\nfunction Get-SessionHealthStatus {\n    \n    [CmdletBinding()]\n    param()\n\n    $alertCount = $script:SessionHistory.SupervisionEvents.Count\n    $criticalAlerts = $script:SessionHistory.SupervisionEvents | Where-Object { $_.Severity -eq 'critical' }\n\n    if ($criticalAlerts.Count -gt 0) {\n        return 'Critical - Immediate intervention required'\n    } elseif ($alertCount -gt 5) {\n        return 'Warning - Multiple patterns detected'\n    } elseif ($alertCount -gt 0) {\n        return 'Caution - Monitoring active'\n    } else {\n        return 'Healthy - No patterns detected'\n    }\n}\n\nExport-ModuleMember -Function @(\n    'Initialize-MetacognitiveOversight',\n    'Register-FileModification',\n    'Test-CircularBehavior',\n    'Invoke-SupervisionAlert',\n    'Get-SupervisionReport'\n) |
