idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
10 | graphs/MstAugmented.cs | {4177 451 0.4077 4.1669} | 4288 | using System;\nusing System.Collections.Generic;\n\nnamespace ProximityGraphs\n{\n    public static partial class ProximityGraph\n    {\n        /// <summary>\n        /// MST-Augmented Mutual KNN proximity.\n        /// Starts with Mutual KNN (AND-rule), then adds edges from the\n        /// Minimum Spanning Tree to guarantee a connected graph.\n        /// This is the recommended proximity for distributional metrics (JSD)\n        /// and high-dimensional data where Mutual KNN alone risks severe\n        /// connectivity loss (~40% reachability in high-D).\n        ///\n        /// MST computed via Kruskal's algorithm.\n        /// Only MST edges that bridge disconnected components are added;\n        /// edges already present from Mutual KNN are not duplicated.\n        ///\n        /// Returns per-node neighbor lists and 1-NN distances for auto-delta.\n        /// </summary>\n        public static NeighborSelection SelectMstAugmented(\n            int n, int k, Func<int, int, double> pairDistance)\n        {\n            // Step 1: Build Mutual KNN base graph\n            var mutualSelection = SelectMutualKnn(n, k, pairDistance);\n            var mutualNeighbors = mutualSelection.AllNeighbors;\n\n            // Collect existing mutual pairs into a set for dedup\n            var existingPairs = new HashSet<long>();\n            var pairMap = new Dictionary<long, double>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in mutualNeighbors[i])\n                {\n                    int lo = Math.Min(i, nb.Index);\n                    int hi = Math.Max(i, nb.Index);\n                    long key = (long)lo * n + hi;\n                    existingPairs.Add(key);\n                    if (!pairMap.ContainsKey(key))\n                        pairMap[key] = nb.Distance;\n                }\n            }\n\n            // Step 2: Compute MST via Kruskal's to find bridging edges.\n            // Build candidate edge list from all pairs (O(N^2)).\n            var candidates = new List<(int i, int j, double d)>();\n            for (int i = 0; i < n; i++)\n            {\n                for (int j = i + 1; j < n; j++)\n                {\n                    candidates.Add((i, j, pairDistance(i, j)));\n                }\n            }\n            candidates.Sort((a, b) => a.d.CompareTo(b.d));\n\n            // Kruskal's: greedily add shortest edges that connect components\n            var uf = new DisjointSet(n);\n\n            // Pre-union with existing mutual KNN edges\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                uf.Union(lo, hi);\n            }\n\n            // Add MST bridging edges\n            foreach (var (ci, cj, cd) in candidates)\n            {\n                if (uf.Find(ci) != uf.Find(cj))\n                {\n                    long key = (long)ci * n + cj;\n                    if (!existingPairs.Contains(key))\n                    {\n                        pairMap[key] = cd;\n                    }\n                    uf.Union(ci, cj);\n                }\n            }\n\n            // Rebuild per-node neighbor lists from combined pairs\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                double d = kvp.Value;\n                lists[lo].Add(new Neighbor { Index = hi, Distance = d });\n                lists[hi].Add(new Neighbor { Index = lo, Distance = d });\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return new NeighborSelection(allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
11 | graphs/MutualKnn.cs | {3341 351 0.3987 4.1975} | 3433 | using System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace ProximityGraphs\n{\n    public static partial class ProximityGraph\n    {\n        /// <summary>\n        /// Mutual KNN proximity (AND-rule).\n        /// Edge (i,j) exists only if i is in j's K-nearest AND j is in i's K-nearest.\n        /// Inherently symmetric — no symmetrization step needed.\n        /// Produces a sparser graph than OR-symmetrized KNN with higher-confidence edges,\n        /// but risks connectivity loss in high dimensions.\n        ///\n        /// Returns per-node neighbor lists and 1-NN distances for auto-delta.\n        /// </summary>\n        public static NeighborSelection SelectMutualKnn(\n            int n, int k, Func<int, int, double> pairDistance)\n        {\n            // Pass 1: directed KNN via bounded min-heap — parallel over rows.\n            // Each row i owns its own heap and writes only directedNeighbors[i]/directedSets[i].\n            var directedNeighbors = new Neighbor[n][];\n            var directedSets = new HashSet<int>[n];\n\n            Parallel.For(0, n, i =>\n            {\n                var heap = new BoundedMinHeap(k);\n                for (int j = 0; j < n; j++)\n                {\n                    if (i == j) continue;\n                    heap.TryAdd(j, pairDistance(i, j));\n                }\n                directedNeighbors[i] = heap.GetSorted();\n                directedSets[i] = new HashSet<int>();\n                foreach (var nb in directedNeighbors[i])\n                    directedSets[i].Add(nb.Index);\n            });\n\n            // Pass 2: AND-filter — keep only mutual pairs\n            var pairMap = new Dictionary<long, double>();\n\n            for (int i = 0; i < n; i++)\n            {\n                foreach (var nb in directedNeighbors[i])\n                {\n                    int j = nb.Index;\n                    if (i < j && directedSets[j].Contains(i))\n                    {\n                        long key = (long)i * n + j;\n                        // Both directions agree this pair is mutual; take smaller distance\n                        if (!pairMap.TryGetValue(key, out double existing) || nb.Distance < existing)\n                            pairMap[key] = nb.Distance;\n                    }\n                }\n            }\n\n            // Rebuild per-node neighbor lists\n            var lists = new List<Neighbor>[n];\n            for (int i = 0; i < n; i++)\n                lists[i] = new List<Neighbor>();\n\n            foreach (var kvp in pairMap)\n            {\n                int lo = (int)(kvp.Key / n);\n                int hi = (int)(kvp.Key % n);\n                double d = kvp.Value;\n                lists[lo].Add(new Neighbor { Index = hi, Distance = d });\n                lists[hi].Add(new Neighbor { Index = lo, Distance = d });\n            }\n\n            var allNeighbors = new Neighbor[n][];\n            var nnDistances = new List<double>(n);\n\n            for (int i = 0; i < n; i++)\n            {\n                lists[i].Sort((a, b) => a.Distance.CompareTo(b.Distance));\n                allNeighbors[i] = lists[i].ToArray();\n\n                if (allNeighbors[i].Length > 0)\n                    nnDistances.Add(allNeighbors[i][0].Distance);\n            }\n\n            return new NeighborSelection(allNeighbors, nnDistances.ToArray());\n        }\n    }\n} |
12 | hashish/bloom.cs | {3430 319 0.2525 4.8793} | 3539 | #nullable enable\nusing System;\nusing System.Numerics;\nusing System.Runtime.CompilerServices;\n\nnamespace Hashish;\n\n/// <summary>Classic Bloom filter for approximate membership queries.</summary>\npublic sealed class BloomFilter\n{\n    private readonly ulong[] _bits;\n    private readonly int _bitCount;\n    private readonly int _hashCount;\n\n    public BloomFilter(int bitCount, int hashCount)\n    {\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(bitCount, 0, nameof(bitCount));\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(hashCount, 0, nameof(hashCount));\n\n        _bitCount = bitCount;\n        _hashCount = hashCount;\n        _bits = new ulong[(bitCount + 63) / 64];\n    }\n\n    public int BitCount => _bitCount;\n    public int HashCount => _hashCount;\n    public long Insertions { get; private set; }\n\n    public static BloomFilter Create(int expectedItems, double falsePositiveProbability)\n    {\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(expectedItems, 0, nameof(expectedItems));\n        if (falsePositiveProbability <= 0.0 || falsePositiveProbability >= 1.0)\n            throw new ArgumentOutOfRangeException(nameof(falsePositiveProbability), \"Must be in (0, 1).\");\n\n        double ln2 = Math.Log(2.0);\n        int bits = (int)Math.Ceiling(-(expectedItems * Math.Log(falsePositiveProbability)) / (ln2 * ln2));\n        int hashes = Math.Max(1, (int)Math.Round((bits / (double)expectedItems) * ln2));\n        return new BloomFilter(bits, hashes);\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Add(string value)\n    {\n        ArgumentNullException.ThrowIfNull(value);\n        AddHash(SeededHash.Fnv1a(value.AsSpan(), SeededHash.Seed(0)));\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Add(ReadOnlySpan<byte> value)\n        => AddHash(SeededHash.Fnv1a(value, SeededHash.Seed(0)));\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public bool Contains(string value)\n    {\n        ArgumentNullException.ThrowIfNull(value);\n        return ContainsHash(SeededHash.Fnv1a(value.AsSpan(), SeededHash.Seed(0)));\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public bool Contains(ReadOnlySpan<byte> value)\n        => ContainsHash(SeededHash.Fnv1a(value, SeededHash.Seed(0)));\n\n    public double FillRatio()\n    {\n        long setBits = 0;\n        for (int i = 0; i < _bits.Length; i++)\n            setBits += BitOperations.PopCount(_bits[i]);\n\n        return (double)setBits / _bitCount;\n    }\n\n    public void Clear()\n    {\n        Array.Clear(_bits);\n        Insertions = 0;\n    }\n\n    private void AddHash(ulong hash)\n    {\n        ulong h1 = hash;\n        ulong h2 = SeededHash.Mix64(hash ^ 0x9e3779b97f4a7c15UL) | 1UL;\n\n        for (int i = 0; i < _hashCount; i++)\n        {\n            int bit = (int)((h1 + ((ulong)i * h2)) % (uint)_bitCount);\n            int word = bit >> 6;\n            _bits[word] |= 1UL << (bit & 63);\n        }\n\n        Insertions++;\n    }\n\n    private bool ContainsHash(ulong hash)\n    {\n        ulong h1 = hash;\n        ulong h2 = SeededHash.Mix64(hash ^ 0x9e3779b97f4a7c15UL) | 1UL;\n\n        for (int i = 0; i < _hashCount; i++)\n        {\n            int bit = (int)((h1 + ((ulong)i * h2)) % (uint)_bitCount);\n            int word = bit >> 6;\n            if ((_bits[word] & (1UL << (bit & 63))) == 0UL)\n                return false;\n        }\n\n        return true;\n    }\n} |
13 | hashish/bm25.cs | {3619 357 0.2675 4.7087} | 3730 | using System;\nusing System.Collections.Frozen;\nusing System.Collections.Generic;\nusing System.Runtime.CompilerServices;\nusing System.Runtime.InteropServices;\nusing System.Text.RegularExpressions;\n\nnamespace Hashish;\n\n/// <summary>\n/// Computes BM25 document-frequency statistics from a text corpus.\n/// Output feeds directly into <see cref=\"SimHash\"/> as an immutable IDF map.\n/// </summary>\npublic static class Bm25Stats\n{\n    // Shared with SimHash — compiled once, linear-time (NonBacktracking), no per-call overhead.\n    internal static readonly Regex WordRegex = new(\n        @\"\\w+\",\n        RegexOptions.Compiled | RegexOptions.CultureInvariant | RegexOptions.NonBacktracking\n    );\n\n    /// <summary>\n    /// Computes average document length and IDF weights from a collection of documents.\n    /// IDF formula: log((N + 1) / (df + 1)) + 1  (Lucene/BM25+ smoothed variant).\n    /// </summary>\n    /// <param name=\"documents\">Input corpus.</param>\n    /// <param name=\"ignoreCase\">Fold tokens to lowercase before counting. Default true.</param>\n    /// <returns>\n    /// A tuple of <c>AvgDocLength</c> (mean token count) and a frozen IDF map\n    /// ready for direct use in <see cref=\"SimHash\"/>.\n    /// </returns>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public static (double AvgDocLength, FrozenDictionary<string, double> IdfMap)\n        Compute(IReadOnlyList<string> documents, bool ignoreCase = true)\n    {\n        ArgumentNullException.ThrowIfNull(documents);\n\n        int docCount = documents.Count;\n        if (docCount == 0)\n            return (0.0, FrozenDictionary<string, double>.Empty);\n\n        double totalTokens = 0.0;\n\n        // doc-frequency: how many documents contain each token (not total occurrences).\n        var docFreq = new Dictionary<string, int>(\n            capacity: 1024,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        // per-document seen-set reused across iterations.\n        var seen = new HashSet<string>(\n            capacity: 256,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        for (int d = 0; d < docCount; d++)\n        {\n            string doc = documents[d];\n            if (string.IsNullOrEmpty(doc)) continue;\n\n            var matches = WordRegex.Matches(doc);\n            totalTokens += matches.Count;\n\n            seen.Clear();\n            foreach (System.Text.RegularExpressions.Match m in matches)\n            {\n                // AsSpan avoids allocating a lowercase string until we need it for insertion.\n                string token = ignoreCase ? m.Value.ToLowerInvariant() : m.Value;\n                seen.Add(token);\n            }\n\n            foreach (string token in seen)\n            {\n                ref int freq = ref CollectionsMarshal.GetValueRefOrAddDefault(docFreq, token, out _);\n                freq++;\n            }\n        }\n\n        double avgDocLength = totalTokens / docCount;\n\n        // Build IDF map and freeze — one allocation, then permanently immutable.\n        var idfBuilder = new Dictionary<string, double>(\n            capacity: docFreq.Count,\n            comparer: ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        );\n\n        double n1 = docCount + 1.0;\n        foreach (var kvp in docFreq)\n        {\n            idfBuilder[kvp.Key] = Math.Log(n1 / (kvp.Value + 1.0)) + 1.0;\n        }\n\n        return (avgDocLength, idfBuilder.ToFrozenDictionary(\n            ignoreCase ? StringComparer.OrdinalIgnoreCase : StringComparer.Ordinal\n        ));\n    }\n} |
14 | hashish/countmin.cs | {3034 277 0.2432 4.8220} | 3133 | #nullable enable\nusing System;\nusing System.Runtime.CompilerServices;\n\nnamespace Hashish;\n\n/// <summary>Count-Min Sketch for streaming approximate frequency estimation.</summary>\npublic sealed class CountMin\n{\n    private readonly long[,] _table;\n    private readonly int _width;\n    private readonly int _depth;\n\n    public CountMin(int width, int depth)\n    {\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(width, 0, nameof(width));\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(depth, 0, nameof(depth));\n\n        _width = width;\n        _depth = depth;\n        _table = new long[depth, width];\n    }\n\n    public int Width => _width;\n    public int Depth => _depth;\n    public long TotalCount { get; private set; }\n\n    public static CountMin Create(double epsilon, double delta)\n    {\n        if (epsilon <= 0.0 || epsilon >= 1.0)\n            throw new ArgumentOutOfRangeException(nameof(epsilon), \"Must be in (0, 1).\");\n        if (delta <= 0.0 || delta >= 1.0)\n            throw new ArgumentOutOfRangeException(nameof(delta), \"Must be in (0, 1).\");\n\n        int width = (int)Math.Ceiling(Math.E / epsilon);\n        int depth = (int)Math.Ceiling(Math.Log(1.0 / delta));\n        return new CountMin(width, depth);\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Add(string value, long count = 1)\n    {\n        ArgumentNullException.ThrowIfNull(value);\n        AddHash(SeededHash.Fnv1a(value.AsSpan(), SeededHash.Seed(0)), count);\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Add(ReadOnlySpan<byte> value, long count = 1)\n        => AddHash(SeededHash.Fnv1a(value, SeededHash.Seed(0)), count);\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public long Estimate(string value)\n    {\n        ArgumentNullException.ThrowIfNull(value);\n        return EstimateHash(SeededHash.Fnv1a(value.AsSpan(), SeededHash.Seed(0)));\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public long Estimate(ReadOnlySpan<byte> value)\n        => EstimateHash(SeededHash.Fnv1a(value, SeededHash.Seed(0)));\n\n    public void Clear()\n    {\n        Array.Clear(_table);\n        TotalCount = 0;\n    }\n\n    private void AddHash(ulong hash, long count)\n    {\n        ArgumentOutOfRangeException.ThrowIfLessThanOrEqual(count, 0, nameof(count));\n\n        for (int row = 0; row < _depth; row++)\n        {\n            int column = Column(hash, row);\n            _table[row, column] += count;\n        }\n\n        TotalCount += count;\n    }\n\n    private long EstimateHash(ulong hash)\n    {\n        long estimate = long.MaxValue;\n        for (int row = 0; row < _depth; row++)\n        {\n            int column = Column(hash, row);\n            estimate = Math.Min(estimate, _table[row, column]);\n        }\n\n        return estimate == long.MaxValue ? 0 : estimate;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private int Column(ulong hash, int row)\n        => (int)(SeededHash.Mix64(hash ^ SeededHash.Seed(row + 1)) % (uint)_width);\n} |
15 | hashish/ctph.cs | {7650 817 0.2837 4.8082} | 7889 | #nullable enable\nusing System;\nusing System.Buffers;\nusing System.Collections.Generic;\nusing System.Runtime.CompilerServices;\nusing System.Runtime.InteropServices;\n\nnamespace Hashish;\n\n/// <summary>\n/// Context-Triggered Piecewise Hashing (CTPH / ssdeep-style fuzzy hash).\n/// Splits content into variable-length chunks via a rolling FNV-1a trigger,\n/// then hashes each chunk to produce a comparison-friendly digest.\n/// </summary>\npublic static class ContextTriggeredPiecewiseHash\n{\n    private const int MinBlockSize = 3;\n    private const int MaxBlockSize = 96;\n    private const ulong FnvPrime = 1099511628211UL;\n    private const ulong FnvOffset = 14695981039346656037UL;\n\n    // Upper bound for stackalloc DP rows in LevenshteinDistance.\n    private const int LevenshteinStackLimit = 512;\n\n    /// <summary>\n    /// Computes a CTPH digest of the form <c>blockSize:hash1:hash2</c>.\n    /// </summary>\n    /// <param name=\"content\">Input text.</param>\n    /// <param name=\"blockSize\">\n    /// Override automatic block-size selection. Must be >= <c>MinBlockSize</c> (3) if provided.\n    /// </param>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public static string Compute(string content, int blockSize = 0)\n    {\n        if (string.IsNullOrEmpty(content)) return \"0::\";\n\n        int bs = blockSize > 0\n            ? Math.Max(blockSize, MinBlockSize)\n            : SelectBlockSize(content.Length);\n\n        string hash1 = HashSequence(ComputeChunks(content, bs));\n        string hash2 = HashSequence(ComputeChunks(content, bs * 2));\n\n        return $\"{bs}:{hash1}:{hash2}\";\n    }\n\n    /// <summary>\n    /// Similarity score in [0, 100] between two CTPH digests.\n    /// Returns 0 if block sizes differ by more than 2x.\n    /// </summary>\n    public static double Compare(string hash1, string hash2)\n    {\n        ReadOnlySpan<char> s1 = hash1.AsSpan();\n        ReadOnlySpan<char> s2 = hash2.AsSpan();\n\n        int colon1a = s1.IndexOf(':');\n        int colon1b = s1[(colon1a + 1)..].IndexOf(':') + colon1a + 1;\n        int colon2a = s2.IndexOf(':');\n        int colon2b = s2[(colon2a + 1)..].IndexOf(':') + colon2a + 1;\n\n        if (colon1a < 0 || colon1b <= colon1a || colon2a < 0 || colon2b <= colon2a)\n            throw new ArgumentException(\"Invalid CTPH hash format.\");\n\n        int bs1 = int.Parse(s1[..colon1a]);\n        int bs2 = int.Parse(s2[..colon2a]);\n\n        double ratio = (double)Math.Max(bs1, bs2) / Math.Min(bs1, bs2);\n        if (ratio > 2.0) return 0.0;\n\n        // Choose matching sequence portions based on block-size relationship.\n        ReadOnlySpan<char> seq1 = bs1 == bs2\n            ? s1[(colon1a + 1)..colon1b]\n            : s1[(colon1b + 1)..];\n        ReadOnlySpan<char> seq2 = bs1 == bs2\n            ? s2[(colon2a + 1)..colon2b]\n            : s2[(colon2b + 1)..];\n\n        int distance = LevenshteinDistance(seq1, seq2);\n        int maxLen = Math.Max(seq1.Length, seq2.Length);\n        if (maxLen == 0) return 100.0;\n\n        return Math.Max(0.0, (1.0 - (double)distance / maxLen) * 100.0);\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static int SelectBlockSize(int contentLength)\n    {\n        if (contentLength < 4096) return MinBlockSize;\n\n        int candidate = Math.Max(\n            MinBlockSize,\n            (int)Math.Ceiling(Math.Log(contentLength / 64.0, 2))\n        );\n        return Math.Min(candidate, MaxBlockSize);\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    private static List<ulong> ComputeChunks(string content, int blockSize)\n    {\n        if (blockSize <= 1) throw new ArgumentOutOfRangeException(nameof(blockSize), \"Must be > 1.\");\n\n        var chunks = new List<ulong>();\n        ulong hash = FnvOffset;\n        int windowStart = 0;\n        ulong trigger = (ulong)(blockSize - 1);\n\n        // Iterate via ReadOnlySpan — bounds checks eliminated by the JIT for span-length loops.\n        ReadOnlySpan<char> span = content.AsSpan();\n\n        for (int i = 0; i < span.Length; i++)\n        {\n            ulong charCode = span[i];\n            hash ^= charCode;\n            hash *= FnvPrime;\n\n            if (hash % (ulong)blockSize == trigger)\n            {\n                chunks.Add(HashChunk(span[windowStart..(i + 1)]));\n                windowStart = i + 1;\n            }\n        }\n\n        if (windowStart < span.Length)\n            chunks.Add(HashChunk(span[windowStart..]));\n\n        return chunks;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static ulong HashChunk(ReadOnlySpan<char> chunk)\n    {\n        ulong hash = FnvOffset;\n        for (int i = 0; i < chunk.Length; i++)\n        {\n            hash ^= chunk[i];\n            hash *= FnvPrime;\n        }\n        return hash;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    private static string HashSequence(List<ulong> chunks)\n    {\n        if (chunks.Count == 0) return string.Empty;\n\n        int take = Math.Min(chunks.Count, 64);\n\n        // MemoryMarshal.Cast: zero-copy reinterpret List<ulong> backing store as byte span.\n        Span<ulong> chunkSpan = CollectionsMarshal.AsSpan(chunks)[..take];\n        ReadOnlySpan<byte> byteView = MemoryMarshal.Cast<ulong, byte>(chunkSpan);\n\n        string base64 = Convert.ToBase64String(byteView);\n        int len = Math.Min(64, base64.Length);\n        return base64[..len];\n    }\n\n    /// <summary>\n    /// Edit distance between two character spans.\n    /// Uses stackalloc DP rows for inputs up to <c>LevenshteinStackLimit</c> chars;\n    /// falls back to <see cref=\"ArrayPool{T}\"/> for larger inputs.\n    /// </summary>\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    public static int LevenshteinDistance(ReadOnlySpan<char> a, ReadOnlySpan<char> b)\n    {\n        int n = a.Length, m = b.Length;\n        if (n == 0) return m;\n        if (m == 0) return n;\n\n        // Stack path: stackalloc must not be inside a try block.\n        if (m <= LevenshteinStackLimit)\n        {\n            Span<int> prev = stackalloc int[m + 1];\n            Span<int> curr = stackalloc int[m + 1];\n            return LevenshteinCore(a, b, prev, curr);\n        }\n\n        // Heap path: ArrayPool for large inputs.\n        int[] rentedPrev = ArrayPool<int>.Shared.Rent(m + 1);\n        int[] rentedCurr = ArrayPool<int>.Shared.Rent(m + 1);\n        try\n        {\n            return LevenshteinCore(a, b, rentedPrev.AsSpan(0, m + 1), rentedCurr.AsSpan(0, m + 1));\n        }\n        finally\n        {\n            ArrayPool<int>.Shared.Return(rentedPrev);\n            ArrayPool<int>.Shared.Return(rentedCurr);\n        }\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveOptimization)]\n    private static int LevenshteinCore(ReadOnlySpan<char> a, ReadOnlySpan<char> b, Span<int> prev, Span<int> curr)\n    {\n        int n = a.Length, m = b.Length;\n        for (int j = 0; j <= m; j++) prev[j] = j;\n\n        for (int i = 1; i <= n; i++)\n        {\n            curr[0] = i;\n            char aChar = a[i - 1];\n\n            for (int j = 1; j <= m; j++)\n            {\n                int cost = aChar == b[j - 1] ? 0 : 1;\n                curr[j] = Math.Min(\n                    Math.Min(prev[j] + 1, curr[j - 1] + 1),\n                    prev[j - 1] + cost\n                );\n            }\n\n            // Swap rows — no copy.\n            Span<int> tmp = prev; prev = curr; curr = tmp;\n        }\n\n        return prev[m];\n    }\n\n    /// <summary>String overload for callers with allocated strings.</summary>\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public static int LevenshteinDistance(string a, string b)\n        => LevenshteinDistance(a.AsSpan(), b.AsSpan());\n} |
16 | hashish/hyperloglog.cs | {3000 281 0.2623 4.7674} | 3101 | #nullable enable\nusing System;\nusing System.Numerics;\nusing System.Runtime.CompilerServices;\n\nnamespace Hashish;\n\n/// <summary>HyperLogLog cardinality estimator for approximate distinct counts.</summary>\npublic sealed class HyperLogLog\n{\n    private readonly byte[] _registers;\n    private readonly int _precision;\n    private readonly int _registerCount;\n\n    public HyperLogLog(int precision = 14)\n    {\n        ArgumentOutOfRangeException.ThrowIfLessThan(precision, 4, nameof(precision));\n        ArgumentOutOfRangeException.ThrowIfGreaterThan(precision, 18, nameof(precision));\n\n        _precision = precision;\n        _registerCount = 1 << precision;\n        _registers = new byte[_registerCount];\n    }\n\n    public int Precision => _precision;\n    public int RegisterCount => _registerCount;\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Add(string value)\n    {\n        ArgumentNullException.ThrowIfNull(value);\n        AddHash(SeededHash.Fnv1a(value.AsSpan(), SeededHash.Seed(0)));\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    public void Add(ReadOnlySpan<byte> value)\n        => AddHash(SeededHash.Fnv1a(value, SeededHash.Seed(0)));\n\n    public double Estimate()\n    {\n        double sum = 0.0;\n        int zeroRegisters = 0;\n\n        for (int i = 0; i < _registers.Length; i++)\n        {\n            int rank = _registers[i];\n            sum += Math.Pow(2.0, -rank);\n            if (rank == 0)\n                zeroRegisters++;\n        }\n\n        double alpha = Alpha(_registerCount);\n        double estimate = alpha * _registerCount * _registerCount / sum;\n\n        if (estimate <= 2.5 * _registerCount && zeroRegisters > 0)\n            return _registerCount * Math.Log(_registerCount / (double)zeroRegisters);\n\n        return estimate;\n    }\n\n    public void Merge(HyperLogLog other)\n    {\n        ArgumentNullException.ThrowIfNull(other);\n        if (other._precision != _precision)\n            throw new ArgumentException(\"HyperLogLog precision must match.\", nameof(other));\n\n        for (int i = 0; i < _registers.Length; i++)\n            _registers[i] = Math.Max(_registers[i], other._registers[i]);\n    }\n\n    public void Clear() => Array.Clear(_registers);\n\n    private void AddHash(ulong hash)\n    {\n        int index = (int)(hash & (ulong)(_registerCount - 1));\n        ulong remainder = hash >> _precision;\n        int rank = Rank(remainder);\n        if (rank > _registers[index])\n            _registers[index] = (byte)rank;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private int Rank(ulong remainder)\n    {\n        int width = 64 - _precision;\n        if (remainder == 0UL)\n            return width + 1;\n\n        return BitOperations.LeadingZeroCount(remainder) - _precision + 1;\n    }\n\n    [MethodImpl(MethodImplOptions.AggressiveInlining)]\n    private static double Alpha(int m) => m switch\n    {\n        16 => 0.673,\n        32 => 0.697,\n        64 => 0.709,\n        _ => 0.7213 / (1.0 + 1.079 / m)\n    };\n} |
