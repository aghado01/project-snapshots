idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
9 | environment/volatility_controller.py | {12373 1014 0.2885 4.4474} | 12933 | \"\"\"\nVolatility controller for managing environmental changes.\n\nControls the time-varying bias in the environment with smooth transitions\nand context switches.\n\"\"\"\n\nimport logging\nfrom enum import Enum\n\nimport numpy as np\n\nlogger = logging.getLogger(__name__)\n\nclass VolatilityRegime(Enum):\n    \"\"\"Types of volatility regimes.\"\"\"\n\n    STABLE = \"stable\"\n    TRANSITIONING = \"transitioning\"\n    VOLATILE = \"volatile\"\n\nclass VolatilityController:\n    \"\"\"\n    Controls environmental volatility with smooth transitions.\n\n    Implements a drift-diffusion process with occasional context switches,\n    following the LC-NE framework for environmental adaptation.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"\n        Initialize volatility controller.\n\n        Args:\n            config: Configuration dictionary\n        \"\"\"\n        self.config = config\n\n        # Volatility parameters\n        self.hazard_rate = config[\"volatility_parameters\"][\"hazard_rate\"]\n        self.tau_drift = config[\"volatility_parameters\"][\"tau_drift\"]\n        self.bias_extremes = config[\"volatility_parameters\"][\"bias_extremes\"]\n\n        # Current state\n        self.current_bias = 0.5  # Start at neutral\n        self.target_bias = 0.5\n        self.volatility_regime = VolatilityRegime.STABLE\n\n        # History tracking\n        self.bias_history = []\n        self.regime_history = []\n        self.context_switches = []\n\n        # Adaptive parameters\n        self.adaptive_hazard_rate = self.hazard_rate\n        self.surprise_memory = []\n\n        logger.info(\n            f\"Initialized VolatilityController with hazard_rate={self.hazard_rate}\"\n        )\n\n    def update(self, step: int, surprise_signal: float | None = None) -> dict:\n        \"\"\"\n        Update volatility state for current step.\n\n        Args:\n            step: Current simulation step\n            surprise_signal: Optional surprise signal to adapt volatility\n\n        Returns:\n            Current volatility state\n        \"\"\"\n        # Store surprise signal for adaptation\n        if surprise_signal is not None:\n            self.surprise_memory.append(surprise_signal)\n            if len(self.surprise_memory) > 50:  # Keep last 50 signals\n                self.surprise_memory.pop(0)\n\n        # Adapt hazard rate based on recent surprise\n        self._adapt_hazard_rate()\n\n        # Check for context switch\n        context_switched = False\n        if np.random.rand() < self.adaptive_hazard_rate:\n            self._switch_context()\n            context_switched = True\n\n        # Drift toward target bias\n        self._drift_toward_target()\n\n        # Update volatility regime\n        self._update_volatility_regime()\n\n        # Store history\n        self.bias_history.append(self.current_bias)\n        self.regime_history.append(self.volatility_regime)\n\n        if context_switched:\n            self.context_switches.append(step)\n\n        return {\n            \"current_bias\": self.current_bias,\n            \"target_bias\": self.target_bias,\n            \"volatility_regime\": self.volatility_regime.value,\n            \"context_switched\": context_switched,\n            \"adaptive_hazard_rate\": self.adaptive_hazard_rate,\n            \"drift_rate\": self._compute_drift_rate(),\n        }\n\n    def _adapt_hazard_rate(self):\n        \"\"\"Adapt hazard rate based on recent surprise signals.\"\"\"\n        if len(self.surprise_memory) < 10:\n            return\n\n        # High surprise increases hazard rate (more likely to switch)\n        recent_surprise = np.mean(self.surprise_memory[-10:])\n\n        # Adaptive factor based on surprise\n        if recent_surprise > 0.7:\n            adaptation_factor = 1.5\n        elif recent_surprise > 0.3:\n            adaptation_factor = 1.0\n        else:\n            adaptation_factor = 0.8\n\n        # Update hazard rate with bounds\n        self.adaptive_hazard_rate = np.clip(\n            self.hazard_rate * adaptation_factor,\n            0.001,  # Minimum hazard rate\n            0.05,  # Maximum hazard rate\n        )\n\n    def _switch_context(self):\n        \"\"\"Switch to new context bias.\"\"\"\n        # Choose new target bias\n        if self.target_bias < 0.5:\n            self.target_bias = np.random.uniform(0.6, self.bias_extremes[1])\n        else:\n            self.target_bias = np.random.uniform(self.bias_extremes[0], 0.4)\n\n        # Set volatility regime\n        self.volatility_regime = VolatilityRegime.TRANSITIONING\n\n        logger.info(f\"Context switch: target_bias={self.target_bias:.3f}\")\n\n    def _drift_toward_target(self):\n        \"\"\"Drift current bias toward target bias.\"\"\"\n        drift_rate = self._compute_drift_rate()\n\n        # Drift with some noise\n        drift = (self.target_bias - self.current_bias) * drift_rate\n        noise = np.random.randn() * 0.02  # Small amount of noise\n\n        self.current_bias += drift + noise\n\n        # Keep within bounds\n        self.current_bias = np.clip(self.current_bias, 0.0, 1.0)\n\n    def _compute_drift_rate(self) -> float:\n        \"\"\"Compute current drift rate.\"\"\"\n        base_rate = 1.0 / self.tau_drift\n\n        # Faster drift during transitions\n        if self.volatility_regime == VolatilityRegime.TRANSITIONING:\n            return base_rate * 2.0\n        elif self.volatility_regime == VolatilityRegime.VOLATILE:\n            return base_rate * 1.5\n        else:\n            return base_rate\n\n    def _update_volatility_regime(self):\n        \"\"\"Update volatility regime based on current state.\"\"\"\n        # Distance from target\n        distance_to_target = abs(self.current_bias - self.target_bias)\n\n        # Recent volatility\n        if len(self.bias_history) >= 10:\n            recent_volatility = np.std(self.bias_history[-10:])\n        else:\n            recent_volatility = 0.0\n\n        # Determine regime\n        if distance_to_target < 0.05 and recent_volatility < 0.02:\n            self.volatility_regime = VolatilityRegime.STABLE\n        elif distance_to_target > 0.2 or recent_volatility > 0.05:\n            self.volatility_regime = VolatilityRegime.VOLATILE\n        else:\n            self.volatility_regime = VolatilityRegime.TRANSITIONING\n\n    def get_current_bias(self) -> float:\n        \"\"\"Get current directional bias.\"\"\"\n        return self.current_bias\n\n    def get_volatility_regime(self) -> VolatilityRegime:\n        \"\"\"Get current volatility regime.\"\"\"\n        return self.volatility_regime\n\n    def get_statistics(self) -> dict:\n        \"\"\"Get statistics about volatility behavior.\"\"\"\n        if len(self.bias_history) < 2:\n            return {\n                \"mean_bias\": 0.5,\n                \"bias_std\": 0.0,\n                \"n_context_switches\": 0,\n                \"time_in_regimes\": {},\n            }\n\n        # Compute basic statistics\n        mean_bias = np.mean(self.bias_history)\n        bias_std = np.std(self.bias_history)\n        n_context_switches = len(self.context_switches)\n\n        # Time in each regime\n        regime_counts = {}\n        for regime in self.regime_history:\n            regime_counts[regime.value] = regime_counts.get(regime.value, 0) + 1\n\n        total_steps = len(self.regime_history)\n        time_in_regimes = {k: v / total_steps for k, v in regime_counts.items()}\n\n        return {\n            \"mean_bias\": mean_bias,\n            \"bias_std\": bias_std,\n            \"n_context_switches\": n_context_switches,\n            \"time_in_regimes\": time_in_regimes,\n            \"current_regime\": self.volatility_regime.value,\n            \"recent_surprise\": np.mean(self.surprise_memory[-10:])\n            if self.surprise_memory\n            else 0.0,\n        }\n\n    def reset(self):\n        \"\"\"Reset volatility controller state.\"\"\"\n        self.current_bias = 0.5\n        self.target_bias = 0.5\n        self.volatility_regime = VolatilityRegime.STABLE\n        self.adaptive_hazard_rate = self.hazard_rate\n\n        # Clear history\n        self.bias_history.clear()\n        self.regime_history.clear()\n        self.context_switches.clear()\n        self.surprise_memory.clear()\n\n        logger.info(\"Reset VolatilityController\")\n\nclass HMMVolatilityController(VolatilityController):\n    \"\"\"\n    HMM-based volatility controller for more complex state dynamics.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        super().__init__(config)\n\n        # HMM parameters\n        self.n_hidden_states = config.get(\"n_hidden_states\", 3)\n        self.current_hidden_state = 0\n\n        # Transition matrix (states x states)\n        self.transition_matrix = self._initialize_transition_matrix()\n\n        # Emission parameters (hidden state -> bias parameters)\n        self.emission_params = self._initialize_emission_params()\n\n        logger.info(\n            f\"Initialized HMMVolatilityController with {self.n_hidden_states} hidden states\"\n        )\n\n    def _initialize_transition_matrix(self) -> np.ndarray:\n        \"\"\"Initialize HMM transition matrix.\"\"\"\n        # Create transition matrix with self-transitions and occasional switches\n        transition_matrix = np.zeros((self.n_hidden_states, self.n_hidden_states))\n\n        for i in range(self.n_hidden_states):\n            # High probability of staying in same state\n            transition_matrix[i, i] = 1.0 - self.hazard_rate * (\n                self.n_hidden_states - 1\n            )\n\n            # Equal probability of transitioning to other states\n            for j in range(self.n_hidden_states):\n                if i != j:\n                    transition_matrix[i, j] = self.hazard_rate\n\n        return transition_matrix\n\n    def _initialize_emission_params(self) -> dict:\n        \"\"\"Initialize emission parameters for each hidden state.\"\"\"\n        # Each hidden state has different bias characteristics\n        params = {}\n\n        for i in range(self.n_hidden_states):\n            # Distribute bias targets across extremes\n            if i == 0:\n                # Low bias state\n                params[i] = {\"mean\": self.bias_extremes[0], \"std\": 0.05}\n            elif i == self.n_hidden_states - 1:\n                # High bias state\n                params[i] = {\"mean\": self.bias_extremes[1], \"std\": 0.05}\n            else:\n                # Intermediate states\n                bias_range = self.bias_extremes[1] - self.bias_extremes[0]\n                mean_bias = (\n                    self.bias_extremes[0]\n                    + (i / (self.n_hidden_states - 1)) * bias_range\n                )\n                params[i] = {\"mean\": mean_bias, \"std\": 0.1}\n\n        return params\n\n    def update(self, step: int, surprise_signal: float | None = None) -> dict:\n        \"\"\"Update HMM volatility state.\"\"\"\n        # Update hidden state\n        self._update_hidden_state()\n\n        # Generate bias from current hidden state\n        state_params = self.emission_params[self.current_hidden_state]\n        self.target_bias = np.random.normal(state_params[\"mean\"], state_params[\"std\"])\n        self.target_bias = np.clip(self.target_bias, 0.0, 1.0)\n\n        # Update current bias (faster convergence)\n        self.current_bias = 0.7 * self.current_bias + 0.3 * self.target_bias\n\n        # Determine volatility regime based on hidden state\n        self._update_volatility_regime_hmm()\n\n        # Store history\n        self.bias_history.append(self.current_bias)\n        self.regime_history.append(self.volatility_regime)\n\n        return {\n            \"current_bias\": self.current_bias,\n            \"target_bias\": self.target_bias,\n            \"hidden_state\": self.current_hidden_state,\n            \"volatility_regime\": self.volatility_regime.value,\n            \"transition_prob\": self.transition_matrix[\n                self.current_hidden_state, :\n            ].tolist(),\n        }\n\n    def _update_hidden_state(self):\n        \"\"\"Update hidden state based on transition probabilities.\"\"\"\n        # Sample next state\n        probs = self.transition_matrix[self.current_hidden_state, :]\n        self.current_hidden_state = np.random.choice(self.n_hidden_states, p=probs)\n\n    def _update_volatility_regime_hmm(self):\n        \"\"\"Update volatility regime based on hidden state.\"\"\"\n        # Map hidden states to volatility regimes\n        if self.current_hidden_state in [0, self.n_hidden_states - 1]:\n            # Extreme states are more stable\n            self.volatility_regime = VolatilityRegime.STABLE\n        else:\n            # Intermediate states are more volatile\n            self.volatility_regime = VolatilityRegime.VOLATILE |
10 | models/__init__.py | {1211 74 0.1742 4.6236} | 1301 | \"\"\"Models package for adaptive Bayesian driver.\"\"\"\n\nfrom .base import (\n    BaseModel,\n    ConvolutionalVAE,\n    GenerativeModel,\n    ParticleFilter,\n    Simple2LayerReLU,\n    VariationalAutoencoder,\n)\nfrom .pretrained_relu_generator import PretrainedReLUGenerator\nfrom .recursive_bayesian_learner import (\n    InferenceMode,\n    RecursiveBayesianLearner,\n    recursive_bayesian_learner,\n)\nfrom .simple_relu_generator import Simple2LayerReLU as Simple2LayerReLUGenerator\nfrom .surprise_detector import (\n    AdaptiveSurpriseDetector,\n    SurpriseDetector,\n    SurpriseMeter,\n    SurpriseType,\n)\nfrom .utils_particle import (\n    AdaptiveParticleFilter,\n    BayesianParticleFilter,\n    ContextAwareParticleFilter,\n)\n\n__all__ = [\n    \"BaseModel\",\n    \"GenerativeModel\",\n    \"Simple2LayerReLU\",\n    \"Simple2LayerReLUGenerator\",\n    \"VariationalAutoencoder\",\n    \"ConvolutionalVAE\",\n    \"ParticleFilter\",\n    \"PretrainedReLUGenerator\",\n    \"RecursiveBayesianLearner\",\n    \"InferenceMode\",\n    \"recursive_bayesian_learner\",\n    \"SurpriseDetector\",\n    \"SurpriseType\",\n    \"SurpriseMeter\",\n    \"AdaptiveSurpriseDetector\",\n    \"AdaptiveParticleFilter\",\n    \"ContextAwareParticleFilter\",\n    \"BayesianParticleFilter\",\n] |
11 | models/base.py | {12195 1140 0.2686 4.5734} | 12799 | \"\"\"\nBase model classes for the adaptive Bayesian driver.\n\nReconstructed from PYC bytecode analysis (src/models/__pycache__/base.cpython-313.pyc).\nClasses recovered: BaseModel, GenerativeModel, Simple2LayerReLU (VAE variant),\n                   VariationalAutoencoder, ConvolutionalVAE, ParticleFilter.\n\"\"\"\n\nimport logging\nfrom typing import Any\n\nimport numpy as np\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nlogger = logging.getLogger(__name__)\n\nclass BaseModel(nn.Module):\n    \"\"\"\n    Abstract base class for all models.\n\n    Key Features:\n    - Automatic CUDA / CPU device management\n    - Common forward / loss / tensor-move interface\n    \"\"\"\n\n    def __init__(self, config: dict[str, Any]):\n        super().__init__()\n        self.config = config\n        self.device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        \"\"\"Forward pass through the model.\"\"\"\n        raise NotImplementedError\n\n    def compute_loss(self, output: torch.Tensor, target: torch.Tensor) -> torch.Tensor:\n        \"\"\"Compute the loss for training.\"\"\"\n        raise NotImplementedError\n\n    def to_device(self, tensor: torch.Tensor) -> torch.Tensor:\n        \"\"\"Move tensor to the appropriate device.\"\"\"\n        return tensor.to(self.device)\n\nclass GenerativeModel(BaseModel):\n    \"\"\"\n    Abstract generative model with encode / decode / reconstruct interface.\n    \"\"\"\n\n    def __init__(self, config: dict[str, Any]):\n        super().__init__(config)\n\n    def generate(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate data from latent code.\"\"\"\n        raise NotImplementedError\n\n    def encode(self, x: torch.Tensor) -> torch.Tensor:\n        \"\"\"Encode data to latent space.\"\"\"\n        raise NotImplementedError\n\n    def reconstruct(self, x: torch.Tensor) -> torch.Tensor:\n        \"\"\"Reconstruct input through encode-decode cycle.\"\"\"\n        z = self.encode(x)\n        return self.generate(z)\n\nclass Simple2LayerReLU(GenerativeModel):\n    \"\"\"\n    Simple 2-layer ReLU generator for fork intersection scenes.\n\n    Key Features:\n    - Lightweight two-layer architecture matching Voroninski's approach\n    - Pre-trained on basic oriented grating patterns\n    - Supports latent optimisation for reconstruction error computation\n    \"\"\"\n\n    def __init__(\n        self, latent_dim: int = 8, output_dim: int = 4096, hidden_dim: int = 32\n    ):\n        config: dict[str, Any] = {\n            \"latent_dim\": latent_dim,\n            \"output_dim\": output_dim,\n            \"hidden_dim\": hidden_dim,\n        }\n        super().__init__(config)\n        self.latent_dim = latent_dim\n        self.output_dim = output_dim\n\n        self.network = nn.Sequential(\n            nn.Linear(latent_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Linear(hidden_dim, output_dim),\n            nn.Sigmoid(),\n        )\n\n    def forward(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate output from latent code.\"\"\"\n        return self.network(z)\n\n    def generate(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate output from latent code.\"\"\"\n        return self.forward(z)\n\n    def encode(self, x: torch.Tensor) -> torch.Tensor:\n        \"\"\"Encode input to latent space (via optimisation).\"\"\"\n        return self.optimize_latent(x)\n\n    def decode(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Decode latent code to output.\"\"\"\n        return self.forward(z)\n\n    def reconstruct(self, x: torch.Tensor) -> torch.Tensor:\n        \"\"\"Reconstruct input.\"\"\"\n        z = self.encode(x)\n        return self.decode(z)\n\n    def compute_reconstruction_error(self, x: torch.Tensor) -> float:\n        \"\"\"Compute reconstruction error.\"\"\"\n        with torch.no_grad():\n            z = self.optimize_latent(x)\n            recon = self.decode(z).view(x.shape)\n            return torch.norm(x - recon).item()\n\n    def optimize_latent(\n        self, target: torch.Tensor, n_steps: int = 50, lr: float = 0.1\n    ) -> torch.Tensor:\n        \"\"\"Optimise latent code to reconstruct target.\"\"\"\n        target_flat = target.flatten().detach()\n        z = torch.randn(1, self.latent_dim, requires_grad=True, device=self.device)\n        optimizer = torch.optim.Adam([z], lr=lr)\n        for _ in range(n_steps):\n            recon = self.forward(z).flatten()\n            loss = F.mse_loss(recon, target_flat)\n            optimizer.zero_grad()\n            loss.backward()\n            optimizer.step()\n        return z.detach()\n\n    def compute_loss(self, output: torch.Tensor, target: torch.Tensor) -> torch.Tensor:\n        \"\"\"Compute reconstruction loss.\"\"\"\n        return F.mse_loss(output, target)\n\nclass VariationalAutoencoder(GenerativeModel):\n    \"\"\"\n    Variational autoencoder for scene encoding.\n\n    Key Features:\n    - Reparameterisation trick for differentiable sampling\n    - Combined reconstruction + KL-divergence loss\n    - Latent codes as inputs to surprise detection\n    \"\"\"\n\n    def __init__(self, config: dict[str, Any]):\n        super().__init__(config)\n        latent_dim = config[\"latent_dim\"]\n        input_dim = config[\"input_dim\"]\n        hidden_dim = config.get(\"hidden_dim\", 128)\n\n        self.encoder = nn.Sequential(\n            nn.Linear(input_dim, hidden_dim),\n            nn.ReLU(),\n        )\n        self.fc_mu = nn.Linear(hidden_dim, latent_dim)\n        self.fc_logvar = nn.Linear(hidden_dim, latent_dim)\n\n        self.decoder = nn.Sequential(\n            nn.Linear(latent_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Linear(hidden_dim, input_dim),\n            nn.Sigmoid(),\n        )\n\n    def encode(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:  # type: ignore[override]\n        \"\"\"Encode input to latent distribution parameters.\"\"\"\n        h = self.encoder(x)\n        return self.fc_mu(h), self.fc_logvar(h)\n\n    def reparameterize(self, mu: torch.Tensor, logvar: torch.Tensor) -> torch.Tensor:\n        \"\"\"Reparameterization trick.\"\"\"\n        if self.training:\n            std = torch.exp(0.5 * logvar)\n            eps = torch.randn_like(std)\n            return mu + eps * std\n        return mu\n\n    def decode(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Decode latent code to output.\"\"\"\n        return self.decoder(z)\n\n    def forward(\n        self, x: torch.Tensor\n    ) -> tuple[torch.Tensor, torch.Tensor, torch.Tensor]:\n        \"\"\"Forward pass through VAE.\"\"\"\n        mu, logvar = self.encode(x)\n        z = self.reparameterize(mu, logvar)\n        return self.decode(z), mu, logvar\n\n    def generate(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate data from latent code.\"\"\"\n        return self.decode(z)\n\n    def compute_loss(\n        self,\n        output: torch.Tensor,\n        target: torch.Tensor,\n        mu: torch.Tensor | None = None,\n        logvar: torch.Tensor | None = None,\n    ) -> torch.Tensor:  # type: ignore[override]\n        \"\"\"Compute VAE loss (reconstruction + KL divergence).\"\"\"\n        recon_loss = F.binary_cross_entropy(output, target, reduction=\"sum\")\n        if mu is not None and logvar is not None:\n            kl_loss = -0.5 * torch.sum(1 + logvar - mu.pow(2) - logvar.exp())\n            return recon_loss + kl_loss\n        return recon_loss\n\nclass ConvolutionalVAE(GenerativeModel):\n    \"\"\"\n    Convolutional VAE for image-based scene encoding.\n\n    Key Features:\n    - Convolutional encoder / decoder for spatial structure\n    - Compatible with fork intersection scene images\n    \"\"\"\n\n    def __init__(self, config: dict[str, Any]):\n        super().__init__(config)\n        self.latent_dim = config[\"latent_dim\"]\n        self.image_size = config.get(\"image_size\", 64)\n        self.channels = config.get(\"channels\", 1)\n\n        # Encoder\n        self.enc_conv = nn.Sequential(\n            nn.Conv2d(self.channels, 32, 4, 2, 1),\n            nn.ReLU(),\n            nn.Conv2d(32, 64, 4, 2, 1),\n            nn.ReLU(),\n            nn.Conv2d(64, 128, 4, 2, 1),\n            nn.ReLU(),\n        )\n        enc_flat = 128 * (self.image_size // 8) ** 2\n        self.fc_mu = nn.Linear(enc_flat, self.latent_dim)\n        self.fc_logvar = nn.Linear(enc_flat, self.latent_dim)\n\n        # Decoder\n        dec_flat = 128 * (self.image_size // 8) ** 2\n        self.fc_dec = nn.Linear(self.latent_dim, dec_flat)\n        self.dec_conv = nn.Sequential(\n            nn.ConvTranspose2d(128, 64, 4, 2, 1),\n            nn.ReLU(),\n            nn.ConvTranspose2d(64, 32, 4, 2, 1),\n            nn.ReLU(),\n            nn.ConvTranspose2d(32, self.channels, 4, 2, 1),\n            nn.Sigmoid(),\n        )\n\n    def encode(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:  # type: ignore[override]\n        \"\"\"Encode input to latent distribution parameters.\"\"\"\n        h = self.enc_conv(x).flatten(1)\n        return self.fc_mu(h), self.fc_logvar(h)\n\n    def decode(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Decode latent code to output.\"\"\"\n        s = self.image_size // 8\n        h = self.fc_dec(z).view(-1, 128, s, s)\n        return self.dec_conv(h)\n\n    def forward(\n        self, x: torch.Tensor\n    ) -> tuple[torch.Tensor, torch.Tensor, torch.Tensor]:\n        \"\"\"Forward pass through ConvVAE.\"\"\"\n        mu, logvar = self.encode(x)\n        std = torch.exp(0.5 * logvar)\n        z = mu + std * torch.randn_like(std) if self.training else mu\n        return self.decode(z), mu, logvar\n\n    def generate(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate data from latent code.\"\"\"\n        return self.decode(z)\n\n    def compute_loss(self, output: torch.Tensor, target: torch.Tensor) -> torch.Tensor:\n        \"\"\"Compute reconstruction loss.\"\"\"\n        return F.binary_cross_entropy(output, target, reduction=\"sum\")\n\nclass ParticleFilter:\n    \"\"\"\n    Basic particle filter for state estimation.\n\n    Key Features:\n    - Standard predict / update loop\n    - Weighted mean and covariance estimation\n    - Systematic resampling\n    \"\"\"\n\n    def __init__(self, n_particles: int = 100, state_dim: int = 4):\n        \"\"\"\n        Initialize particle filter.\n\n        Args:\n            n_particles: Number of particles.\n            state_dim: Dimensionality of the state space.\n        \"\"\"\n        self.n_particles = n_particles\n        self.state_dim = state_dim\n        self.particles = np.random.randn(n_particles, state_dim)\n        self.weights = np.ones(n_particles) / n_particles\n\n    def predict(self, control_input: np.ndarray | None = None) -> None:\n        \"\"\"Prediction step.\"\"\"\n        noise = np.random.randn(self.n_particles, self.state_dim) * 0.1\n        self.particles = self.particles + noise\n\n    def update(\n        self, observation: np.ndarray, observation_model: Any | None = None\n    ) -> None:\n        \"\"\"Update step.\"\"\"\n        if observation_model is not None:\n            predicted = observation_model(self.particles)\n        else:\n            predicted = self.particles[:, : len(observation)]\n\n        diff = observation - predicted\n        log_likelihoods = -0.5 * np.sum(diff**2, axis=-1)\n        self.weights *= np.exp(log_likelihoods - log_likelihoods.max())\n        weight_sum = self.weights.sum()\n        if weight_sum > 0:\n            self.weights /= weight_sum\n        else:\n            self.weights = np.ones(self.n_particles) / self.n_particles\n\n        eff_n = 1.0 / (self.weights**2).sum()\n        if eff_n < self.n_particles / 2:\n            self._resample()\n\n    def _resample(self) -> None:\n        \"\"\"Resample particles based on weights.\"\"\"\n        positions = (\n            np.arange(self.n_particles) + np.random.uniform()\n        ) / self.n_particles\n        cumsum = np.cumsum(self.weights)\n        indices = np.searchsorted(cumsum, positions)\n        self.particles = self.particles[np.clip(indices, 0, self.n_particles - 1)]\n        self.weights = np.ones(self.n_particles) / self.n_particles\n\n    def get_state_estimate(self) -> np.ndarray:\n        \"\"\"Get weighted mean state estimate.\"\"\"\n        return np.average(self.particles, weights=self.weights, axis=0)\n\n    def get_covariance(self) -> np.ndarray:\n        \"\"\"Get weighted covariance estimate.\"\"\"\n        mean = self.get_state_estimate()\n        diff = self.particles - mean\n        return (self.weights[:, None] * diff).T @ diff |
12 | models/pretrained_relu_generator.py | {5181 493 0.2729 4.5550} | 5375 | \"\"\"\nPre-trained ReLU generator using hand-crafted grating patterns.\n\nProvides a generator that is functional from day 1 without any training loop,\nusing parametric oriented gratings as a prior over scene structure.\n\"\"\"\n\nimport logging\n\nimport numpy as np\nimport torch\nimport torch.nn as nn\n\nlogger = logging.getLogger(__name__)\n\nclass PretrainedReLUGenerator(nn.Module):\n    \"\"\"\n    2-layer ReLU generator pre-initialised on oriented grating patterns.\n\n    Key Features:\n    - Parametric grating generation (no data-driven training required)\n    - Supports orientation and difficulty modulation\n    - Compatible with Simple2LayerReLU interface for drop-in usage\n    \"\"\"\n\n    def __init__(\n        self,\n        latent_dim: int = 8,\n        output_dim: int = 4096,\n        hidden_dim: int = 32,\n        scene_size: tuple[int, int] = (64, 64),\n    ):\n        \"\"\"\n        Initialize pretrained ReLU generator.\n\n        Args:\n            latent_dim: Dimensionality of the latent code.\n            output_dim: Flattened output size (height × width).\n            hidden_dim: Hidden layer width.\n            scene_size: (height, width) of generated scenes.\n        \"\"\"\n        super().__init__()\n        self.latent_dim = latent_dim\n        self.output_dim = output_dim\n        self.hidden_dim = hidden_dim\n        self.scene_size = scene_size\n\n        self.network = nn.Sequential(\n            nn.Linear(latent_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Linear(hidden_dim, output_dim),\n            nn.Sigmoid(),\n        )\n\n        # Load / craft pretrained weights\n        self.W1, self.W2 = self._load_pretrained_weights()\n        self._install_weights()\n\n    # ------------------------------------------------------------------\n    # Weight initialisation\n    # ------------------------------------------------------------------\n\n    def _load_pretrained_weights(self) -> tuple[np.ndarray, np.ndarray]:\n        \"\"\"\n        Create hand-crafted weights based on basic oriented grating patterns.\n\n        Returns:\n            (W1, W2) weight matrices.\n        \"\"\"\n        h, w = self.scene_size\n        W1 = np.zeros((self.hidden_dim, self.latent_dim))\n        W2 = np.zeros((self.output_dim, self.hidden_dim))\n\n        # Encode orientation axes in W1\n        for i in range(self.hidden_dim):\n            angle = np.pi * i / self.hidden_dim\n            W1[i] = np.array([np.cos(angle * (j + 1)) for j in range(self.latent_dim)])\n\n        # Encode grating patterns in W2\n        y_coords, x_coords = np.meshgrid(np.arange(h), np.arange(w), indexing=\"ij\")\n        for i in range(self.hidden_dim):\n            angle = np.pi * i / self.hidden_dim\n            frequency = 0.15 + 0.05 * (i % 4)\n            x_rot = x_coords * np.cos(angle) + y_coords * np.sin(angle)\n            grating = 0.5 + 0.3 * np.sin(2 * np.pi * frequency * x_rot)\n            W2[:, i] = grating.flatten()\n\n        return W1.T, W2.T  # transpose to (latent_dim, hidden_dim) etc.\n\n    def _install_weights(self) -> None:\n        \"\"\"Install crafted weights into network Linear layers.\"\"\"\n        with torch.no_grad():\n            w1_t = torch.tensor(self.W1.T, dtype=torch.float32)\n            w2_t = torch.tensor(self.W2.T, dtype=torch.float32)\n            self.network[0].weight.copy_(w1_t[: self.hidden_dim, : self.latent_dim])\n            self.network[2].weight.copy_(w2_t[: self.output_dim, : self.hidden_dim])\n\n    # ------------------------------------------------------------------\n    # Generation\n    # ------------------------------------------------------------------\n\n    def forward(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate scene from latent code.\"\"\"\n        return self.network(z)\n\n    def generate_pattern(self, orientation: float, difficulty: float) -> np.ndarray:\n        \"\"\"\n        Generate a simple parametric oriented grating pattern.\n\n        Args:\n            orientation: Bar orientation in degrees.\n            difficulty: Noise / occlusion level in [0, 1].\n\n        Returns:\n            Flattened float32 array of shape (output_dim,).\n        \"\"\"\n        h, w = self.scene_size\n        y, x = np.meshgrid(np.arange(h), np.arange(w), indexing=\"ij\")\n        angle_rad = np.radians(orientation)\n        x_rot = x * np.cos(angle_rad) + y * np.sin(angle_rad)\n        grating = 0.5 + 0.3 * np.sin(2 * np.pi * 0.15 * x_rot)\n\n        if difficulty > 0:\n            noise = np.random.randn(h, w) * difficulty * 0.2\n            grating = np.clip(grating + noise, 0.0, 1.0)\n\n        return grating.flatten().astype(np.float32)\n\n    def compute_reconstruction_error(self, scene: torch.Tensor) -> float:\n        \"\"\"Compute reconstruction error via latent optimisation.\"\"\"\n        target = scene.flatten().detach()\n        z = torch.randn(1, self.latent_dim, requires_grad=True)\n        optimizer = torch.optim.Adam([z], lr=0.05)\n        for _ in range(30):\n            recon = self.forward(z).flatten()\n            loss = nn.MSELoss()(recon, target)\n            optimizer.zero_grad()\n            loss.backward()\n            optimizer.step()\n        with torch.no_grad():\n            recon = self.forward(z).flatten()\n            return float(torch.norm(target - recon).item()) |
