idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
14 | models/simple_relu_generator.py | {3831 344 0.2918 4.4260} | 3989 | import numpy as np\nimport torch\nimport torch.nn as nn\n\nclass Simple2LayerReLU(nn.Module):\n    \"\"\"\n    Simple 2-layer ReLU generator for fork intersection scenes.\n\n    Architecture matches Voroninski's 2-layer approach while being\n    pre-trained on basic oriented grating patterns.\n    \"\"\"\n\n    def __init__(\n        self, latent_dim: int = 8, output_dim: int = 4096, hidden_dim: int = 32\n    ):\n        super().__init__()\n        self.latent_dim = latent_dim\n        self.output_dim = output_dim\n\n        self.network = nn.Sequential(\n            nn.Linear(latent_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Linear(hidden_dim, output_dim),\n            nn.Sigmoid(),\n        )\n\n        # Initialize for grating generation\n        self._pretrain_on_gratings()\n\n    def forward(self, z: torch.Tensor) -> torch.Tensor:\n        \"\"\"Generate scene from latent code\"\"\"\n        return self.network(z)\n\n    def _pretrain_on_gratings(self):\n        \"\"\"Pre-train on basic oriented grating patterns\"\"\"\n        print(\"Pre-training generator on oriented gratings...\")\n\n        # Generate training patterns\n        n_patterns = 200\n        patterns = []\n        latent_codes = []\n\n        for _ in range(n_patterns):\n            # Random grating parameters\n            orientation = np.random.choice([45, 135]) + np.random.normal(0, 10)\n            frequency = 0.2 + np.random.uniform(-0.1, 0.1)\n            phase = np.random.uniform(0, 2 * np.pi)\n\n            # Generate pattern\n            pattern = self._create_grating(64, 64, orientation, frequency, phase)\n            patterns.append(pattern.flatten())\n\n            # Create corresponding latent code\n            z = torch.randn(self.latent_dim)\n            latent_codes.append(z)\n\n        patterns = torch.stack(patterns)\n        latent_codes = torch.stack(latent_codes)\n\n        # Training loop\n        optimizer = torch.optim.Adam(self.parameters(), lr=0.01)\n\n        for epoch in range(100):\n            optimizer.zero_grad()\n\n            # Forward pass\n            reconstructions = self.forward(latent_codes)\n            loss = nn.MSELoss()(reconstructions, patterns)\n\n            # Backward pass\n            loss.backward()\n            optimizer.step()\n\n            if epoch % 20 == 0:\n                print(f\"Epoch {epoch}, Loss: {loss.item():.4f}\")\n\n    def _create_grating(\n        self, h: int, w: int, orientation: float, frequency: float, phase: float\n    ) -> torch.Tensor:\n        \"\"\"Create oriented grating pattern\"\"\"\n        y, x = torch.meshgrid(torch.arange(h), torch.arange(w), indexing=\"ij\")\n\n        # Rotate coordinates\n        angle_rad = np.radians(orientation)\n        x_rot = x * np.cos(angle_rad) + y * np.sin(angle_rad)\n\n        # Create grating\n        grating = 0.5 + 0.3 * torch.sin(2 * np.pi * frequency * x_rot + phase)\n\n        return grating\n\n    def optimize_latent(\n        self, target_scene: torch.Tensor, n_steps: int = 50, lr: float = 0.1\n    ) -> torch.Tensor:\n        \"\"\"Optimize latent code to reconstruct target scene\"\"\"\n        target_flat = target_scene.flatten()\n\n        # Initialize latent code\n        z_opt = torch.randn(1, self.latent_dim, requires_grad=True)\n        optimizer = torch.optim.Adam([z_opt], lr=lr)\n\n        for _ in range(n_steps):\n            reconstruction = self.forward(z_opt).flatten()\n            loss = nn.MSELoss()(reconstruction, target_flat)\n\n            optimizer.zero_grad()\n            loss.backward()\n            optimizer.step()\n\n        return z_opt.detach()\n\n    def compute_reconstruction_error(self, scene: torch.Tensor) -> float:\n        \"\"\"Compute reconstruction error for surprise detection\"\"\"\n        z_optimal = self.optimize_latent(scene)\n        reconstruction = self.forward(z_optimal).view(scene.shape)\n        error = torch.norm(scene - reconstruction).item()\n        return error |
15 | models/surprise_detector.py | {18230 1585 0.2892 4.5481} | 19025 | \"\"\"\nSurprise detection for LC-NE inspired adaptive learning.\n\nReconstructed from:\n- src/models/__pycache__/surprise.cpython-313.pyc  (SurpriseType, SurpriseMeter, AdaptiveSurpriseDetector)\n- _wip_import/src/models/SurpriseDetector.py        (SurpriseDetector with adaptive thresholds)\n\"\"\"\n\nimport logging\nfrom enum import Enum\n\nimport numpy as np\n\nlogger = logging.getLogger(__name__)\n\nclass SurpriseType(Enum):\n    \"\"\"Types of surprise detection methods.\"\"\"\n\n    CHI2 = \"chi2\"\n    LATENT_KL = \"latent_kl\"\n    MANIFOLD_DIST = \"manifold_dist\"\n    RECONSTRUCTION = \"reconstruction\"\n    KL_DIVERGENCE = \"kl_divergence\"\n    PREDICTIVE = \"predictive\"\n\nclass SurpriseDetector:\n    \"\"\"\n    LC-NE inspired surprise detection from reconstruction errors.\n\n    Implements:\n    - Phasic NE: Sudden deviations from baseline\n    - Tonic NE: Overall uncertainty level\n    - Adaptive baseline tracking\n    - Adaptive thresholds modulated by context uncertainty and prediction confidence\n    \"\"\"\n\n    def __init__(self, baseline_window: int = 20):\n        self.baseline_window = baseline_window\n        self.baseline_error: float | None = None\n        self.surprise_history: list[float] = []\n\n    def adaptive_surprise_threshold(\n        self,\n        current_uncertainty: float,\n        prediction_confidence: float,\n        volatility_state: str,\n    ) -> float:\n        \"\"\"\n        Compute adaptive surprise threshold based on current epistemic state.\n\n        Args:\n            current_uncertainty: Context uncertainty from bayesian_learner\n            prediction_confidence: Recent prediction accuracy\n            volatility_state: Current environmental volatility level\n\n        Returns:\n            Adaptive threshold clipped to [0.1, 0.9].\n        \"\"\"\n        base_threshold = 0.5\n        uncertainty_modulation = 1.0 - current_uncertainty\n        confidence_modulation = 1.0 - prediction_confidence\n        volatility_modulation = self.get_volatility_factor(volatility_state)\n        adaptive_threshold = (\n            base_threshold\n            * uncertainty_modulation\n            * confidence_modulation\n            * volatility_modulation\n        )\n        return float(np.clip(adaptive_threshold, 0.1, 0.9))\n\n    def get_volatility_factor(self, volatility_state: str) -> float:\n        \"\"\"Map volatility state label to a numeric modulation factor.\"\"\"\n        mapping = {\"stable\": 1.0, \"moderate\": 0.8, \"volatile\": 0.6}\n        return mapping.get(volatility_state, 0.8)\n\n    def _compute_base_surprise(\n        self, current_error: float, error_history: list[float]\n    ) -> float:\n        \"\"\"Compute baseline-normalised surprise.\"\"\"\n        if len(error_history) < 5:\n            return 0.0\n        recent_errors = error_history[-self.baseline_window :]\n        self.baseline_error = float(np.mean(recent_errors))\n        if self.baseline_error > 0:\n            surprise = (current_error - self.baseline_error) / self.baseline_error\n        else:\n            surprise = 0.0\n        return float(np.clip(surprise, -2.0, 2.0))\n\n    def compute_surprise(\n        self,\n        current_error: float,\n        error_history: list[float],\n        context_uncertainty: float | None = None,\n        prediction_confidence: float | None = None,\n    ) -> float:\n        \"\"\"\n        Enhanced surprise computation with optional adaptive thresholds.\n\n        Args:\n            current_error: Current reconstruction error.\n            error_history: Recent history of reconstruction errors.\n            context_uncertainty: Optional; if given, modulates threshold.\n            prediction_confidence: Optional; if given, modulates threshold.\n\n        Returns:\n            Surprise signal (float).\n        \"\"\"\n        surprise = self._compute_base_surprise(current_error, error_history)\n\n        if context_uncertainty is not None and prediction_confidence is not None:\n            threshold = self.adaptive_surprise_threshold(\n                context_uncertainty, prediction_confidence, \"moderate\"\n            )\n            surprise = surprise / max(threshold, 1e-6)\n\n        self.surprise_history.append(surprise)\n        return surprise\n\n    def get_phasic_ne_signal(self, surprise: float) -> float:\n        \"\"\"Generate phasic NE signal (positive surprise only).\"\"\"\n        return max(0.0, surprise)\n\n    def get_tonic_ne_signal(self, context_uncertainty: float) -> float:\n        \"\"\"Generate tonic NE signal from context uncertainty.\"\"\"\n        return context_uncertainty\n\n    def get_surprise_statistics(self) -> dict:\n        \"\"\"Get surprise detection statistics.\"\"\"\n        if len(self.surprise_history) < 10:\n            return {\"mean_surprise\": 0.0, \"surprise_variance\": 0.0}\n        return {\n            \"mean_surprise\": float(np.mean(self.surprise_history)),\n            \"surprise_variance\": float(np.var(self.surprise_history)),\n            \"recent_surprise_rate\": float(\n                np.mean([abs(s) > 0.5 for s in self.surprise_history[-20:]])\n            ),\n        }\n\nclass SurpriseMeter:\n    \"\"\"\n    Multi-metric surprise meter supporting several detection methods.\n\n    Supports chi2, latent_kl, manifold_dist, reconstruction, and predictive modes.\n\n    Usage (as expected by tests)::\n\n        meter = SurpriseMeter(mode=SurpriseType.CHI2)\n        surprise = meter.compute_surprise(innov=innov_vec, S=cov_matrix)\n\n        meter2 = SurpriseMeter(mode=SurpriseType.RECONSTRUCTION)\n        surprise = meter2.compute_surprise(x=original, x_recon=reconstructed)\n    \"\"\"\n\n    def __init__(\n        self,\n        metrics: list[str] | None = None,\n        mode: \"SurpriseType | str\" = SurpriseType.RECONSTRUCTION,\n        baseline_window: int = 50,\n        window_size: int = 20,\n        adaptive_threshold: bool = True,\n    ):\n        \"\"\"\n        Initialize surprise meter.\n\n        Args:\n            metrics: List of SurpriseType values to track.\n            mode: Primary surprise metric (SurpriseType enum or string value).\n            baseline_window: Rolling window size for baseline statistics.\n            window_size: Window for rolling statistics output.\n            adaptive_threshold: Whether to use adaptive thresholds.\n        \"\"\"\n        # Normalise mode to SurpriseType enum\n        if isinstance(mode, str):\n            mode = SurpriseType(mode)\n        self.mode: SurpriseType = mode\n        self.baseline_window = baseline_window\n        self.window_size = window_size\n        self.adaptive_threshold = adaptive_threshold\n\n        self.metrics = metrics or [m.value for m in SurpriseType]\n        self.surprise_history: list[float] = []\n        self.baseline_history: list[float] = []\n\n        # Recent errors kept to exactly baseline_window entries (for tests)\n        self.recent_errors: list[float] = []\n        self.baseline_stats: dict = {\"mean\": 0.0, \"std\": 0.0, \"n\": 0}\n\n        logger.info(\"Initialized SurpriseMeter with mode: %s\", self.mode)\n\n    # ------------------------------------------------------------------\n    # Public interface expected by tests\n    # ------------------------------------------------------------------\n\n    def compute_surprise(\n        self,\n        innov: np.ndarray | None = None,\n        S: np.ndarray | None = None,\n        x: object | None = None,\n        x_recon: object | None = None,\n        observation: np.ndarray | None = None,\n        prediction: np.ndarray | None = None,\n        **kwargs: object,\n    ) -> float:\n        \"\"\"\n        Compute surprise for the current mode.\n\n        Keyword args dispatched by mode:\n        - CHI2: ``innov`` (innovation vector) + ``S`` (covariance matrix)\n        - RECONSTRUCTION: ``x`` + ``x_recon`` (tensors or arrays)\n        - LATENT_KL: ``q_mu`` + ``q_logvar``\n        - MANIFOLD_DIST: ``q_mu``\n        - PREDICTIVE: ``observation`` + ``prediction``\n\n        Returns:\n            Surprise value (float ≥ 0).\n        \"\"\"\n        mode = self.mode\n        if mode == SurpriseType.CHI2:\n            surprise = self._chi2_surprise(innov, S)\n        elif mode == SurpriseType.RECONSTRUCTION:\n            surprise = self._reconstruction_error_tensors(x, x_recon)\n        elif mode == SurpriseType.LATENT_KL or mode == SurpriseType.KL_DIVERGENCE:\n            q_mu = kwargs.get(\"q_mu\")\n            q_logvar = kwargs.get(\"q_logvar\")\n            surprise = self._kl_divergence(q_mu, q_logvar)  # type: ignore[arg-type]\n        elif mode == SurpriseType.MANIFOLD_DIST:\n            q_mu = kwargs.get(\"q_mu\")\n            surprise = self._manifold_distance(q_mu)  # type: ignore[arg-type]\n        elif mode == SurpriseType.PREDICTIVE:\n            surprise = self._predictive_surprise(observation, prediction)\n        else:\n            surprise = 0.0\n\n        self.surprise_history.append(surprise)\n        self.update_baseline(surprise)\n        return surprise\n\n    def compute_normalized_surprise(\n        self,\n        innov: np.ndarray | None = None,\n        S: np.ndarray | None = None,\n        **kwargs: object,\n    ) -> float:\n        \"\"\"\n        Compute surprise normalised by running baseline statistics.\n\n        Args:\n            innov: Innovation vector (for CHI2 mode).\n            S: Covariance matrix (for CHI2 mode).\n            **kwargs: Forwarded to compute_surprise.\n\n        Returns:\n            Normalised surprise value (float ≥ 0).\n        \"\"\"\n        raw = self.compute_surprise(innov=innov, S=S, **kwargs)\n        baseline_std = self.baseline_stats.get(\"std\", 0.0)\n        if baseline_std > 1e-8:\n            return float(abs(raw - self.baseline_stats[\"mean\"]) / baseline_std)\n        return float(abs(raw))\n\n    def update_baseline(self, error: float) -> None:\n        \"\"\"\n        Update rolling baseline statistics with a new error value.\n\n        Args:\n            error: New error / surprise value to incorporate.\n        \"\"\"\n        self.baseline_history.append(error)\n        # Keep only the most recent baseline_window entries\n        if len(self.baseline_history) > self.baseline_window:\n            self.baseline_history.pop(0)\n\n        # recent_errors mirrors the bounded baseline history\n        self.recent_errors = list(self.baseline_history)\n\n        # Recompute stats\n        arr = np.array(self.recent_errors)\n        self.baseline_stats = {\n            \"mean\": float(np.mean(arr)),\n            \"std\": float(np.std(arr)),\n            \"n\": len(arr),\n        }\n\n    # ------------------------------------------------------------------\n    # Per-metric helpers\n    # ------------------------------------------------------------------\n\n    def _chi2_surprise(\n        self,\n        innovation: np.ndarray | None,\n        covariance: np.ndarray | None = None,\n    ) -> float:\n        \"\"\"Compute chi-squared surprise from innovation.\"\"\"\n        try:\n            if innovation is None:\n                return 0.0\n            innov = np.asarray(innovation, dtype=float)\n            if covariance is not None:\n                cov = np.asarray(covariance, dtype=float)\n                cov_inv = np.linalg.inv(cov + 1e-6 * np.eye(len(cov)))\n                return float(innov @ cov_inv @ innov)\n            return float(np.sum(innov**2))\n        except Exception as exc:\n            logger.warning(\"Chi2 computation failed: %s\", exc)\n            return 0.0\n\n    def _reconstruction_error_tensors(\n        self,\n        x: object | None,\n        x_recon: object | None,\n    ) -> float:\n        \"\"\"Compute reconstruction error for tensor or array inputs.\"\"\"\n        try:\n            if x is None or x_recon is None:\n                return 0.0\n            import torch\n\n            if isinstance(x, torch.Tensor):\n                x = x.detach().float()\n            if isinstance(x_recon, torch.Tensor):\n                x_recon = x_recon.detach().float()\n            x_arr = np.asarray(x, dtype=float).flatten()\n            xr_arr = np.asarray(x_recon, dtype=float).flatten()\n            return float(np.linalg.norm(x_arr - xr_arr))\n        except Exception as exc:\n            logger.warning(\"Reconstruction error computation failed: %s\", exc)\n            return 0.0\n\n    def _kl_divergence(\n        self,\n        q_mu: np.ndarray | None,\n        q_logvar: np.ndarray | None,\n    ) -> float:\n        \"\"\"Compute KL divergence of latent posterior from prior.\"\"\"\n        try:\n            if q_mu is None or q_logvar is None:\n                return 0.0\n            return float(-0.5 * np.sum(1 + q_logvar - q_mu**2 - np.exp(q_logvar)))\n        except Exception as exc:\n            logger.warning(\"KL divergence computation failed: %s\", exc)\n            return 0.0\n\n    def _manifold_distance(self, q_mu: np.ndarray | None) -> float:\n        \"\"\"Compute manifold distance (Euclidean norm of latent mean).\"\"\"\n        try:\n            if q_mu is None:\n                return 0.0\n            return float(np.linalg.norm(q_mu))\n        except Exception as exc:\n            logger.warning(\"Manifold distance computation failed: %s\", exc)\n            return 0.0\n\n    def _reconstruction_error(\n        self,\n        x_recon: np.ndarray | None,\n        observation: np.ndarray | None,\n    ) -> float:\n        \"\"\"Compute reconstruction error for numpy arrays.\"\"\"\n        try:\n            if x_recon is None or observation is None:\n                return 0.0\n            return float(np.linalg.norm(np.asarray(observation) - np.asarray(x_recon)))\n        except Exception as exc:\n            logger.warning(\"Reconstruction error computation failed: %s\", exc)\n            return 0.0\n\n    def _predictive_surprise(\n        self,\n        observation: np.ndarray | None,\n        prediction: np.ndarray | None,\n    ) -> float:\n        \"\"\"Compute predictive surprise.\"\"\"\n        if observation is None or prediction is None:\n            return 0.0\n        return float(np.linalg.norm(np.asarray(observation) - np.asarray(prediction)))\n\n    # ------------------------------------------------------------------\n    # Batch compute (legacy interface)\n    # ------------------------------------------------------------------\n\n    def compute(\n        self,\n        observation: np.ndarray | None = None,\n        prediction: np.ndarray | None = None,\n        innov: np.ndarray | None = None,\n        q_mu: np.ndarray | None = None,\n        q_logvar: np.ndarray | None = None,\n        x_recon: np.ndarray | None = None,\n    ) -> dict[str, float]:\n        \"\"\"\n        Compute surprise signals for all configured metrics (batch interface).\n\n        Returns:\n            Dict of metric name → surprise value.\n        \"\"\"\n        results: dict[str, float] = {}\n        for metric in self.metrics:\n            if metric in (SurpriseType.CHI2.value, \"chi2\"):\n                results[\"chi2\"] = self._chi2_surprise(innov)\n            elif metric in (SurpriseType.LATENT_KL.value, \"latent_kl\"):\n                results[\"latent_kl\"] = self._kl_divergence(q_mu, q_logvar)\n            elif metric in (SurpriseType.MANIFOLD_DIST.value, \"manifold_dist\"):\n                results[\"manifold_dist\"] = self._manifold_distance(q_mu)\n            elif metric in (SurpriseType.RECONSTRUCTION.value, \"reconstruction\"):\n                results[\"reconstruction\"] = self._reconstruction_error(\n                    x_recon, observation\n                )\n            elif metric in (SurpriseType.PREDICTIVE.value, \"predictive\"):\n                results[\"predictive\"] = self._predictive_surprise(\n                    observation, prediction\n                )\n        mode_key = self.mode.value if isinstance(self.mode, SurpriseType) else self.mode\n        primary = results.get(mode_key, 0.0)\n        self.surprise_history.append(primary)\n        return results\n\n    def get_statistics(self) -> dict:\n        \"\"\"Get rolling statistics from surprise history.\"\"\"\n        if not self.surprise_history:\n            return {\"mean\": 0.0, \"std\": 0.0}\n        window = self.surprise_history[-self.window_size :]\n        return {\n            \"mean\": float(np.mean(window)),\n            \"std\": float(np.std(window)),\n            \"max\": float(np.max(window)),\n            \"n_samples\": len(self.surprise_history),\n        }\n\n    def reset(self) -> None:\n        \"\"\"Reset surprise meter state.\"\"\"\n        self.surprise_history.clear()\n        self.baseline_history.clear()\n        self.recent_errors.clear()\n        self.baseline_stats = {\"mean\": 0.0, \"std\": 0.0, \"n\": 0}\n        logger.debug(\"Reset surprise meter state\")\n\nclass AdaptiveSurpriseDetector:\n    \"\"\"\n    Adaptive multi-mode surprise detector.\n\n    Reconstructed from src/models/__pycache__/surprise.cpython-313.pyc.\n    Wraps SurpriseMeter with dynamic mode switching and failure handling.\n    \"\"\"\n\n    def __init__(self, config: dict, baseline_window: int = 50):\n        \"\"\"\n        Initialize adaptive surprise detector.\n\n        Args:\n            config: Configuration dict.\n            baseline_window: Window for baseline estimation.\n        \"\"\"\n        self.config = config\n        self.mode = SurpriseType.RECONSTRUCTION.value\n        self.meter = SurpriseMeter(\n            metrics=[m.value for m in SurpriseType],\n            mode=self.mode,\n            baseline_window=baseline_window,\n            adaptive_threshold=True,\n        )\n        logger.info(\"Initialized AdaptiveSurpriseDetector\")\n\n    def switch_mode(self, new_mode: str) -> None:\n        \"\"\"Switch primary surprise mode.\"\"\"\n        self.mode = new_mode\n        self.meter.mode = new_mode\n        logger.info(\"Switched to surprise mode: %s\", new_mode)\n\n    def compute(\n        self, observation: np.ndarray, prediction: np.ndarray, **kwargs\n    ) -> dict[str, float]:\n        \"\"\"\n        Compute surprise using current mode, with failure recovery.\n\n        Args:\n            observation: Current observation.\n            prediction: Model prediction.\n            **kwargs: Passed through to SurpriseMeter.compute().\n\n        Returns:\n            Dict of metric → surprise value.\n        \"\"\"\n        try:\n            return self.meter.compute(observation, prediction, **kwargs)\n        except Exception as e:\n            logger.warning(\"Surprise computation failed for %s: %s\", self.mode, e)\n            return {self.mode: 0.0}\n\n    def get_statistics(self) -> dict:\n        \"\"\"Get current surprise statistics.\"\"\"\n        return self.meter.get_statistics()\n\n    def reset(self) -> None:\n        \"\"\"Reset detector state.\"\"\"\n        self.meter.reset() |
