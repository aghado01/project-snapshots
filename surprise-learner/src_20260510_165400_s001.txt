idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | __init__.py | {928 97 0.2241 4.9915} | 1010 | \"\"\"\nAdaptive Bayesian Driver - Production ML Package\n\"\"\"\n\n__version__ = \"0.2.0\"\n__author__ = \"Azriel Ghadooshahy\"\n\n# Core system imports with robust error handling\ntry:\n    import torch\n\n    CUDA_AVAILABLE = torch.cuda.is_available()\n    DEVICE = torch.device(\"cuda\" if CUDA_AVAILABLE else \"cpu\")\n\n    if CUDA_AVAILABLE:\n        GPU_COUNT = torch.cuda.device_count()\n        GPU_NAME = torch.cuda.get_device_name(0)\n    else:\n        GPU_COUNT = 0\n        GPU_NAME = \"No GPU Available\"\n\nexcept ImportError as e:\n    print(f\"⚠️  Critical dependency missing: {e}\")\n    CUDA_AVAILABLE = False\n    DEVICE = \"cpu\"\n    GPU_COUNT = 0\n    GPU_NAME = \"Dependencies Missing\"\n\nfrom .config import load_config\nfrom .utils.device import get_device, setup_cuda_environment\n\n__all__ = [\n    \"__version__\",\n    \"CUDA_AVAILABLE\",\n    \"DEVICE\",\n    \"GPU_COUNT\",\n    \"GPU_NAME\",\n    \"load_config\",\n    \"get_device\",\n    \"setup_cuda_environment\",\n] |
1 | applications/__init__.py | {69 9 0.1304 4.5050} | 77 | \"\"\"Applications module for Adaptive Bayesian Driver.\"\"\"\n\n__all__ = [] |
2 | config.py | {2979 257 0.2548 4.5280} | 3152 | \"\"\"Production-grade configuration management.\"\"\"\n\nimport logging\nimport sys\nfrom pathlib import Path\nfrom typing import Any\n\nimport torch\nimport yaml  # type: ignore\n\nlogger = logging.getLogger(__name__)\n\nclass ConfigurationError(Exception):\n    \"\"\"Configuration-related errors.\"\"\"\n\n    pass\n\ndef load_config(config_path: str = \"config/experiment.yaml\") -> dict[str, Any]:\n    \"\"\"\n    Load and validate configuration from YAML file.\n\n    Args:\n        config_path: Path to YAML configuration file\n\n    Returns:\n        Validated configuration dictionary\n\n    Raises:\n        ConfigurationError: If configuration is invalid or missing\n    \"\"\"\n    config_file = Path(config_path)\n\n    if not config_file.exists():\n        alternative_paths = [\n            \"config/default.yaml\",\n            \"adaptive_bayesian_driver/config/default.yaml\",\n            \"experiments/config.yaml\",\n        ]\n\n        for alt_path in alternative_paths:\n            if Path(alt_path).exists():\n                config_file = Path(alt_path)\n                logger.warning(f\"Using alternative config: {alt_path}\")\n                break\n        else:\n            raise ConfigurationError(\n                f\"No configuration file found. \"\n                f\"Tried: {config_path}, {alternative_paths}\"\n            )\n\n    try:\n        with open(config_file, encoding=\"utf-8\") as f:\n            config_data = yaml.safe_load(f)\n    except yaml.YAMLError as e:\n        raise ConfigurationError(f\"Invalid YAML in {config_path}: {e}\") from e\n\n    # Ensure config is a dictionary\n    if not isinstance(config_data, dict):\n        raise ConfigurationError(\n            f\"Configuration must be a dictionary, got {type(config_data)}\"\n        )\n\n    config: dict[str, Any] = config_data\n\n    # Validate and enhance configuration\n    config = _validate_and_enhance_config(config)\n    return config\n\ndef _validate_and_enhance_config(config: dict[str, Any]) -> dict[str, Any]:\n    \"\"\"Validate and enhance configuration with system information.\"\"\"\n\n    # Add device configuration\n    cuda_available = torch.cuda.is_available()\n    device_name = str(torch.device(\"cuda\" if cuda_available else \"cpu\"))\n    gpu_count = torch.cuda.device_count() if cuda_available else 0\n\n    config[\"device\"] = {\n        \"cuda_available\": cuda_available,\n        \"device_name\": device_name,\n        \"gpu_count\": gpu_count,\n    }\n\n    # Add system information\n    python_version = f\"{sys.version_info.major}.{sys.version_info.minor}\"\n    cuda_version = torch.version.cuda if cuda_available else None\n\n    config[\"system\"] = {\n        \"python_version\": python_version,\n        \"torch_version\": torch.__version__,\n        \"cuda_version\": cuda_version,\n    }\n\n    # Validate critical sections\n    required_sections = [\"model\", \"training\", \"environment\"]\n    for section in required_sections:\n        if section not in config:\n            logger.warning(f\"Missing configuration section: {section}\")\n            config[section] = {}\n\n    return config |
3 | environment/__init__.py | {671 46 0.1237 4.6560} | 714 | \"\"\"Environment package for adaptive Bayesian driver.\"\"\"\n\nfrom .fork_intersection_generator import ForkIntersectionGenerator\nfrom .hmm_volatility_controller import HMMVolatilityController\nfrom .scene_renderer import SceneRenderer\nfrom .task_hmm import AdaptiveTaskHMM, TaskHMM, TaskState\nfrom .time_varying_bias_environment import TimeVaryingBiasEnvironment\nfrom .volatility_controller import VolatilityController, VolatilityRegime\n\n__all__ = [\n    \"ForkIntersectionGenerator\",\n    \"SceneRenderer\",\n    \"VolatilityRegime\",\n    \"VolatilityController\",\n    \"HMMVolatilityController\",\n    \"TaskState\",\n    \"TaskHMM\",\n    \"AdaptiveTaskHMM\",\n    \"TimeVaryingBiasEnvironment\",\n] |
4 | environment/fork_intersection_generator.py | {7892 779 0.3018 4.4042} | 8246 | \"\"\"Fork intersection scene generator for the adaptive Bayesian driving task.\n\nGenerates continuous sequences of fork intersection scenes with time-varying\nleft/right bias, danger signs, and oriented grating bars for the avoidance task.\n\"\"\"\n\nimport logging\nfrom typing import Any\n\nimport numpy as np\nimport torch\n\nfrom .volatility_controller import VolatilityController\n\nlogger = logging.getLogger(__name__)\n\nclass ForkIntersectionGenerator:\n    \"\"\"\n    Generates continuous fork intersection scenes with danger avoidance task.\n\n    Key Features:\n    - Yellow background = danger sign (apply avoidance rule)\n    - White background = safe (follow bar direction)\n    - Time-varying left/right bias driven by VolatilityController\n    - Oriented grating bars with controllable perceptual difficulty\n    - Self-contained rendering via torch tensor operations (no PIL dependency)\n    \"\"\"\n\n    def __init__(self, config: dict[str, Any]):\n        \"\"\"\n        Initialize fork intersection generator.\n\n        Args:\n            config: Configuration dictionary with sections:\n                - visual_parameters: scene_size, orientations, difficulties\n                - task_parameters: danger_probability\n                - volatility_parameters: hazard_rate, tau_drift, bias_extremes\n        \"\"\"\n        self.config = config\n        self.volatility = VolatilityController(config)\n\n        # Visual parameters\n        self.scene_size: tuple[int, int] = config[\"visual_parameters\"][\"scene_size\"]\n        self.orientations: list[float] = config[\"visual_parameters\"][\"orientations\"]\n        self.difficulties: list[float] = config[\"visual_parameters\"][\"difficulties\"]\n\n        # Task parameters\n        self.danger_prob: float = config[\"task_parameters\"][\"danger_probability\"]\n\n        # Step counter for VolatilityController.update() calls\n        self._step: int = 0\n\n        logger.info(\n            \"Initialized ForkIntersectionGenerator: scene_size=%s, danger_prob=%.3f\",\n            self.scene_size,\n            self.danger_prob,\n        )\n\n    def generate_continuous_sequence(self, length: int) -> dict[str, Any]:\n        \"\"\"\n        Generate a continuous sequence of fork intersection scenes.\n\n        Args:\n            length: Number of time steps to generate.\n\n        Returns:\n            Dictionary with keys:\n                - scenes: torch.Tensor of shape (length, H, W), values in [0, 1]\n                - metadata: list of per-step scene parameter dicts\n                - bias_history: list of recorded current_bias values\n        \"\"\"\n        scenes: list[torch.Tensor] = []\n        metadata: list[dict[str, Any]] = []\n\n        for _ in range(length):\n            volatility_state = self.volatility.update(step=self._step)\n            self._step += 1\n\n            current_bias: float = volatility_state[\"current_bias\"]\n            scene_data = self._generate_scene_parameters(current_bias)\n            scene_image = self._render_fork_scene(scene_data)\n\n            scenes.append(scene_image)\n            metadata.append(scene_data)\n\n        return {\n            \"scenes\": torch.stack(scenes),\n            \"metadata\": metadata,\n            \"bias_history\": list(self.volatility.bias_history),\n        }\n\n    def _generate_scene_parameters(self, current_bias: float) -> dict[str, Any]:\n        \"\"\"\n        Generate parameters for a single scene.\n\n        Args:\n            current_bias: Current P(left cue) from the volatility controller.\n\n        Returns:\n            Scene parameter dictionary.\n        \"\"\"\n        left_cue = bool(np.random.random() < current_bias)\n        danger_present = bool(np.random.random() < self.danger_prob)\n\n        difficulty_idx = int(np.random.choice(len(self.difficulties)))\n        difficulty = self.difficulties[difficulty_idx]\n\n        # Avoidance rule: danger inverts the cue meaning\n        if danger_present:\n            correct_action = \"right\" if left_cue else \"left\"\n        else:\n            correct_action = \"left\" if left_cue else \"right\"\n\n        return {\n            \"left_cue\": left_cue,\n            \"danger_present\": danger_present,\n            \"difficulty\": difficulty,\n            \"difficulty_idx\": difficulty_idx,\n            \"correct_action\": correct_action,\n            \"current_bias\": current_bias,\n            \"scene_type\": \"danger\" if danger_present else \"safe\",\n        }\n\n    def _render_fork_scene(self, scene_data: dict[str, Any]) -> torch.Tensor:\n        \"\"\"\n        Render a fork intersection scene as a 2-D float tensor.\n\n        Args:\n            scene_data: Parameter dict from _generate_scene_parameters.\n\n        Returns:\n            Greyscale scene tensor of shape (H, W), values in [0, 1].\n        \"\"\"\n        h, w = self.scene_size\n        scene = torch.zeros((h, w))\n\n        # Background brightness encodes context: bright = danger, mid = safe\n        background_intensity = 0.8 if scene_data[\"danger_present\"] else 0.4\n        scene.fill_(background_intensity)\n\n        scene = self._draw_fork_intersection(scene)\n        scene = self._draw_oriented_bars(scene, scene_data)\n\n        return scene\n\n    def _draw_fork_intersection(self, scene: torch.Tensor) -> torch.Tensor:\n        \"\"\"\n        Draw a basic Y-shaped fork intersection on the scene tensor.\n\n        Args:\n            scene: Existing scene tensor to draw onto.\n\n        Returns:\n            Updated scene tensor.\n        \"\"\"\n        h, w = scene.shape\n        cx, cy = w // 2, h // 2\n\n        # Main stem — bottom vertical road segment\n        scene[cy : h - 5, cx - 2 : cx + 2] = 0.1\n\n        # Left fork — upper-left diagonal\n        for i in range(cy):\n            offset = int((cy - i) * 0.5)\n            x_start = max(0, cx - offset - 1)\n            x_end = cx - offset + 1\n            if x_start < x_end:\n                scene[i, x_start:x_end] = 0.1\n\n        # Right fork — upper-right diagonal\n        for i in range(cy):\n            offset = int((cy - i) * 0.5)\n            x_start = cx + offset - 1\n            x_end = min(w, cx + offset + 1)\n            if x_start < x_end:\n                scene[i, x_start:x_end] = 0.1\n\n        return scene\n\n    def _draw_oriented_bars(\n        self, scene: torch.Tensor, scene_data: dict[str, Any]\n    ) -> torch.Tensor:\n        \"\"\"\n        Draw an oriented grating patch at the fork centre indicating turn direction.\n\n        Orientation is selected from self.orientations and perturbed by Gaussian noise\n        scaled by the current difficulty level.\n\n        Args:\n            scene: Existing scene tensor to draw onto.\n            scene_data: Scene parameters (left_cue, difficulty).\n\n        Returns:\n            Updated scene tensor with grating patch overlaid.\n        \"\"\"\n        h, w = scene.shape\n        cx, cy = w // 2, h // 2\n\n        base_orientation: float = (\n            self.orientations[0] if scene_data[\"left_cue\"] else self.orientations[1]\n        )\n        noise_std = float(scene_data[\"difficulty\"]) * 15.0  # degrees\n        actual_orientation = base_orientation + float(np.random.normal(0, noise_std))\n\n        bar_size = 16\n        y_coords, x_coords = torch.meshgrid(\n            torch.arange(bar_size, dtype=torch.float32),\n            torch.arange(bar_size, dtype=torch.float32),\n            indexing=\"ij\",\n        )\n\n        angle_rad = float(np.radians(actual_orientation))\n        x_rot = x_coords * float(np.cos(angle_rad)) + y_coords * float(\n            np.sin(angle_rad)\n        )\n\n        frequency = 0.3\n        grating = 0.5 + 0.3 * torch.sin(2 * np.pi * frequency * x_rot)\n\n        y0 = cy - bar_size // 2\n        y1 = y0 + bar_size\n        x0 = cx - bar_size // 2\n        x1 = x0 + bar_size\n\n        scene[y0:y1, x0:x1] = grating\n\n        return scene\n\n    def reset(self) -> None:\n        \"\"\"Reset generator state and underlying volatility controller.\"\"\"\n        self.volatility.reset()\n        self._step = 0\n        logger.info(\"Reset ForkIntersectionGenerator\") |
5 | environment/hmm_volatility_controller.py | {6832 597 0.2826 4.5559} | 7185 | \"\"\"\nHMM Volatility Controller for hierarchical volatility regime management.\n\nImplements a Hidden Markov Model over volatility regimes, providing a\ntwo-level hierarchy: HMM regime transitions control the parameters of\nthe lower-level context-bias drift process.\n\nHMM volatility states → Context bias y(t) → Stimulus z(t) → Observations x(t,k)\n\"\"\"\n\nimport logging\nfrom enum import Enum\n\nimport numpy as np\n\nlogger = logging.getLogger(__name__)\n\nclass VolatilityRegime(Enum):\n    \"\"\"Volatility regime labels for the HMM states.\"\"\"\n\n    STABLE = \"stable\"\n    MODERATE = \"moderate\"\n    VOLATILE = \"volatile\"\n\nclass HMMVolatilityController:\n    \"\"\"\n    Hierarchical volatility controller using a Hidden Markov Model.\n\n    Level 1: HMM transitions between volatility regimes (stable / moderate / volatile).\n    Level 2: Drift-diffusion context-bias dynamics whose parameters are set by\n             the current HMM regime.\n    \"\"\"\n\n    STATE_NAMES = [\n        VolatilityRegime.STABLE,\n        VolatilityRegime.MODERATE,\n        VolatilityRegime.VOLATILE,\n    ]\n\n    def __init__(self, config: dict | None = None):\n        \"\"\"\n        Initialize HMM volatility controller.\n\n        Args:\n            config: Optional configuration dict with key ``n_hidden_states``.\n        \"\"\"\n        self.config = config or {}\n        n_hidden_states = self.config.get(\"n_hidden_states\", 3)\n\n        # HMM states represent different volatility regimes\n        self.volatility_states = {\n            \"stable\": {\"hazard_rate\": 0.001, \"tau_drift\": 50},\n            \"moderate\": {\"hazard_rate\": 0.005, \"tau_drift\": 30},\n            \"volatile\": {\"hazard_rate\": 0.02, \"tau_drift\": 10},\n        }\n\n        # HMM transition matrix between volatility regimes\n        self.transition_matrix = self._init_transition_matrix(n_hidden_states)\n        self.emission_params = self._init_emission_params(n_hidden_states)\n\n        self.current_volatility_state = 0  # Start in 'stable'\n        self.current_bias = 0.5\n        self.target_bias = 0.5\n        self.step_count = 0\n        self.history: list[dict] = []\n\n        logger.info(\n            \"Initialized HMMVolatilityController with %d hidden states\",\n            n_hidden_states,\n        )\n\n    # ------------------------------------------------------------------\n    # Initialisation helpers\n    # ------------------------------------------------------------------\n\n    def _init_transition_matrix(self, n: int) -> np.ndarray:\n        \"\"\"Initialize HMM transition matrix.\"\"\"\n        if n == 3:\n            return np.array(\n                [\n                    [0.95, 0.04, 0.01],  # stable   → {stable, moderate, volatile}\n                    [0.05, 0.90, 0.05],  # moderate → …\n                    [0.10, 0.20, 0.70],  # volatile → …\n                ]\n            )\n        # Generic: high self-transition probability\n        T = np.full((n, n), 0.05 / max(n - 1, 1))\n        np.fill_diagonal(T, 0.95)\n        T /= T.sum(axis=1, keepdims=True)\n        return T\n\n    def _init_emission_params(self, n: int) -> list[dict]:\n        \"\"\"Initialize emission parameters for each hidden state.\"\"\"\n        params = []\n        for i in range(n):\n            params.append(\n                {\n                    \"mean\": i / max(n - 1, 1),  # 0 = stable, 1 = volatile\n                    \"std\": 0.1,\n                }\n            )\n        return params\n\n    # ------------------------------------------------------------------\n    # Core update\n    # ------------------------------------------------------------------\n\n    def update(self, surprise_signal: float = 0.0) -> float:\n        \"\"\"\n        Advance one timestep: update HMM state, then evolve context bias.\n\n        Args:\n            surprise_signal: Optional external signal that can nudge the\n                             hidden state distribution.\n\n        Returns:\n            Current context bias (float in [0, 1]).\n        \"\"\"\n        self._update_hidden_state(surprise_signal)\n        self._evolve_bias()\n        self.step_count += 1\n\n        state_name = self.STATE_NAMES[self.current_volatility_state].value\n        self.history.append(\n            {\n                \"step\": self.step_count,\n                \"volatility_state\": state_name,\n                \"current_bias\": self.current_bias,\n                \"target_bias\": self.target_bias,\n            }\n        )\n        return self.current_bias\n\n    # Alias used by VolatilityController-style callers\n    def update_step(self) -> float:\n        \"\"\"Alias for :meth:`update`.\"\"\"\n        return self.update()\n\n    def _update_hidden_state(self, surprise_signal: float) -> None:\n        \"\"\"Update hidden state based on transition probabilities.\"\"\"\n        probs = self.transition_matrix[self.current_volatility_state]\n        self.current_volatility_state = int(np.random.choice(len(probs), p=probs))\n\n    def _evolve_bias(self) -> None:\n        \"\"\"Drift current bias toward target bias using regime parameters.\"\"\"\n        state_key = self.STATE_NAMES[self.current_volatility_state].value\n        params = self.volatility_states[state_key]\n        hazard_rate = params[\"hazard_rate\"]\n        tau_drift = params[\"tau_drift\"]\n\n        # Occasional target switches driven by hazard rate\n        if np.random.random() < hazard_rate:\n            self.target_bias = float(np.random.choice([0.2, 0.8]))\n            logger.debug(\"Context switch: target_bias=%.2f\", self.target_bias)\n\n        # Smooth drift toward target\n        alpha = 1.0 / tau_drift\n        self.current_bias += alpha * (self.target_bias - self.current_bias)\n\n    # ------------------------------------------------------------------\n    # Accessors\n    # ------------------------------------------------------------------\n\n    def get_current_bias(self) -> float:\n        \"\"\"Get current directional bias.\"\"\"\n        return self.current_bias\n\n    def get_current_regime(self) -> str:\n        \"\"\"Get current volatility regime label.\"\"\"\n        return self.STATE_NAMES[self.current_volatility_state].value\n\n    def get_statistics(self) -> dict:\n        \"\"\"Get statistics about volatility behaviour.\"\"\"\n        if not self.history:\n            return {}\n        regimes = [h[\"volatility_state\"] for h in self.history]\n        biases = [h[\"current_bias\"] for h in self.history]\n        return {\n            \"mean_bias\": float(np.mean(biases)),\n            \"bias_std\": float(np.std(biases)),\n            \"regime_counts\": {\n                r.value: regimes.count(r.value) for r in VolatilityRegime\n            },\n            \"total_steps\": self.step_count,\n        }\n\n    def reset(self) -> None:\n        \"\"\"Reset controller state.\"\"\"\n        self.current_volatility_state = 0\n        self.current_bias = 0.5\n        self.target_bias = 0.5\n        self.step_count = 0\n        self.history.clear()\n        logger.debug(\"Reset HMMVolatilityController\") |
