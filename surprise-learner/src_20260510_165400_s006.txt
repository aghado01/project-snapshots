idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
16 | models/utils_particle.py | {12827 973 0.2605 4.5020} | 13391 | \"\"\"\nParticle filter utilities for adaptive Bayesian learning.\n\nReconstructed from PYC bytecode analysis (src/models/__pycache__/utils_particle.cpython-313.pyc).\nClasses recovered: AdaptiveParticleFilter, ContextAwareParticleFilter, BayesianParticleFilter.\n\"\"\"\n\nimport logging\nfrom collections.abc import Callable\n\nimport numpy as np\n\nlogger = logging.getLogger(__name__)\n\nclass AdaptiveParticleFilter:\n    \"\"\"\n    Particle filter with adaptive noise and resampling.\n\n    Key Features:\n    - Adaptive process noise based on particle spread\n    - Adaptive resampling threshold\n    - Systematic resampling algorithm\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"\n        Initialize adaptive particle filter.\n\n        Args:\n            config: Configuration with keys:\n                n_particles, state_dim, observation_dim,\n                process_noise_std, observation_noise_std,\n                adaptive_noise, resample_threshold, adaptive_resampling.\n        \"\"\"\n        self.n_particles = config.get(\"n_particles\", 100)\n        self.state_dim = config.get(\"state_dim\", 4)\n        self.observation_dim = config.get(\"observation_dim\", 2)\n        self.process_noise_std = config.get(\"process_noise_std\", 0.1)\n        self.observation_noise_std = config.get(\"observation_noise_std\", 0.5)\n        self.adaptive_noise = config.get(\"adaptive_noise\", True)\n        self.resample_threshold = config.get(\"resample_threshold\", 0.5)\n        self.adaptive_resampling = config.get(\"adaptive_resampling\", True)\n\n        # Initialise particles and weights\n        self.particles = np.random.randn(self.n_particles, self.state_dim)\n        self.weights = np.ones(self.n_particles) / self.n_particles\n\n        # History tracking\n        self.state_history: list[np.ndarray] = []\n\n        logger.info(\n            \"Initialized AdaptiveParticleFilter with %d particles\", self.n_particles\n        )\n\n    # ------------------------------------------------------------------\n    # Core predict / update\n    # ------------------------------------------------------------------\n\n    def predict(\n        self,\n        control_input: np.ndarray | None = None,\n        transition_model: Callable | None = None,\n    ) -> dict:\n        \"\"\"\n        Prediction step: propagate particles through transition model.\n\n        Args:\n            control_input: Optional control signal.\n            transition_model: Optional callable(particles, control) → particles.\n\n        Returns:\n            Dictionary with ``mean`` and ``covariance`` of predicted state.\n        \"\"\"\n        if transition_model is not None:\n            self.particles = transition_model(self.particles, control_input)\n        else:\n            noise_std = (\n                self._compute_adaptive_noise()\n                if self.adaptive_noise\n                else self.process_noise_std\n            )\n            noise = np.random.randn(self.n_particles, self.state_dim) * noise_std\n            self.particles = self.particles + noise\n\n        mean = self.get_state_estimate()\n        covariance = self.get_weighted_covariance()\n        return {\"mean\": mean, \"covariance\": covariance}\n\n    def update(\n        self,\n        observation: np.ndarray,\n        observation_model: Callable | None = None,\n        likelihood_function: Callable | None = None,\n    ) -> dict:\n        \"\"\"\n        Update step: reweight particles from observation likelihood.\n\n        Args:\n            observation: Current observation vector.\n            observation_model: Optional callable(particles) → predicted observations.\n            likelihood_function: Optional callable(observation, predicted) → likelihoods.\n\n        Returns:\n            Dictionary with ``mean``, ``effective_sample_size``, and ``resampled``.\n        \"\"\"\n        if likelihood_function is not None:\n            likelihoods = likelihood_function(observation, self.particles)\n        else:\n            likelihoods = self._default_gaussian_likelihoods(\n                observation, observation_model\n            )\n\n        self.weights *= likelihoods\n        weight_sum = self.weights.sum()\n        if weight_sum > 0:\n            self.weights /= weight_sum\n        else:\n            self.weights = np.ones(self.n_particles) / self.n_particles\n\n        # Effective sample size\n        eff_n = float(1.0 / (self.weights**2).sum())\n\n        # Resample if effective sample size is low\n        threshold = (\n            self._compute_adaptive_threshold()\n            if self.adaptive_resampling\n            else self.resample_threshold\n        )\n        resampled = False\n        if eff_n < threshold * self.n_particles:\n            self._resample()\n            resampled = True\n\n        mean = self.get_state_estimate()\n        self.state_history.append(mean.copy())\n\n        return {\n            \"mean\": mean,\n            \"effective_sample_size\": eff_n,\n            \"resampled\": resampled,\n        }\n\n    # ------------------------------------------------------------------\n    # Internal helpers\n    # ------------------------------------------------------------------\n\n    def _default_gaussian_likelihoods(\n        self,\n        observation: np.ndarray,\n        observation_model: Callable | None,\n    ) -> np.ndarray:\n        \"\"\"Compute default Gaussian likelihoods.\"\"\"\n        if observation_model is not None:\n            predicted = observation_model(self.particles)\n        else:\n            predicted = self.particles[:, : len(observation)]\n\n        diff = observation - predicted\n        log_likelihoods = -0.5 * np.sum(\n            (diff / self.observation_noise_std) ** 2, axis=-1\n        )\n        return np.exp(log_likelihoods - log_likelihoods.max())\n\n    def _compute_adaptive_noise(self) -> float:\n        \"\"\"Compute adaptive process noise based on particle spread.\"\"\"\n        spread = np.std(self.particles, axis=0).mean()\n        return max(\n            self.process_noise_std * 0.1, min(spread * 0.1, self.process_noise_std)\n        )\n\n    def _compute_adaptive_threshold(self) -> float:\n        \"\"\"Compute adaptive resampling threshold.\"\"\"\n        return self.resample_threshold\n\n    def _resample(self) -> None:\n        \"\"\"Resample particles using systematic resampling.\"\"\"\n        indices = self._systematic_resample()\n        self.particles = self.particles[indices]\n        self.weights = np.ones(self.n_particles) / self.n_particles\n\n    def _systematic_resample(self) -> np.ndarray:\n        \"\"\"Systematic resampling algorithm.\"\"\"\n        positions = (\n            np.arange(self.n_particles) + np.random.uniform()\n        ) / self.n_particles\n        cumsum = np.cumsum(self.weights)\n        indices = np.searchsorted(cumsum, positions)\n        return np.clip(indices, 0, self.n_particles - 1)\n\n    # ------------------------------------------------------------------\n    # State accessors\n    # ------------------------------------------------------------------\n\n    def get_state_estimate(self) -> np.ndarray:\n        \"\"\"Get current state estimate (weighted mean).\"\"\"\n        return np.average(self.particles, weights=self.weights, axis=0)\n\n    def get_state_uncertainty(self) -> np.ndarray:\n        \"\"\"Get current state uncertainty (weighted covariance diagonal).\"\"\"\n        mean = self.get_state_estimate()\n        diff = self.particles - mean\n        return np.average(diff**2, weights=self.weights, axis=0)\n\n    def get_uncertainty(self) -> float:\n        \"\"\"Get scalar uncertainty estimate (mean of weighted variance diagonal).\"\"\"\n        return float(np.mean(self.get_state_uncertainty()))\n\n    def get_weighted_covariance(self) -> np.ndarray:\n        \"\"\"Compute weighted covariance matrix.\"\"\"\n        mean = self.get_state_estimate()\n        diff = self.particles - mean\n        return (self.weights[:, None] * diff).T @ diff\n\n    def reset(self) -> None:\n        \"\"\"Reset the particle filter.\"\"\"\n        self.particles = np.random.randn(self.n_particles, self.state_dim)\n        self.weights = np.ones(self.n_particles) / self.n_particles\n        self.state_history.clear()\n        logger.debug(\"Reset AdaptiveParticleFilter\")\n\nclass ContextAwareParticleFilter(AdaptiveParticleFilter):\n    \"\"\"\n    Particle filter with explicit context / regime tracking.\n\n    Maintains a distribution over context labels alongside the state,\n    enabling soft switching between behavioural modes.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"\n        Initialize context-aware particle filter.\n\n        Args:\n            config: Extends AdaptiveParticleFilter config with:\n                n_contexts, context_transition_prob, context_noise_std.\n        \"\"\"\n        super().__init__(config)\n        self.n_contexts = config.get(\"n_contexts\", 3)\n        self.context_transition_prob = config.get(\"context_transition_prob\", 0.05)\n        self.context_noise_std = config.get(\"context_noise_std\", 0.1)\n\n        # Context beliefs: (n_particles,) integer labels\n        self.context_labels = np.zeros(self.n_particles, dtype=int)\n        self.context_probs = (\n            np.ones((self.n_particles, self.n_contexts)) / self.n_contexts\n        )\n\n        logger.info(\n            \"Initialized ContextAwareParticleFilter with %d contexts\", self.n_contexts\n        )\n\n    def predict(\n        self,\n        control_input: np.ndarray | None = None,\n        transition_model: Callable | None = None,\n    ) -> None:\n        \"\"\"Prediction step with context awareness.\"\"\"\n        super().predict(control_input, transition_model)\n        self._predict_context_evolution()\n\n    def _predict_context_evolution(self) -> None:\n        \"\"\"Predict context evolution.\"\"\"\n        switch_mask = np.random.random(self.n_particles) < self.context_transition_prob\n        new_contexts = np.random.randint(0, self.n_contexts, self.n_particles)\n        self.context_labels = np.where(switch_mask, new_contexts, self.context_labels)\n\n    def update_context(\n        self,\n        observation: np.ndarray,\n        context_likelihood: Callable | None = None,\n    ) -> None:\n        \"\"\"Update context beliefs.\"\"\"\n        if context_likelihood is not None:\n            likelihoods = context_likelihood(observation, self.context_labels)\n            self.weights *= likelihoods\n            weight_sum = self.weights.sum()\n            if weight_sum > 0:\n                self.weights /= weight_sum\n            else:\n                self.weights = np.ones(self.n_particles) / self.n_particles\n\n    def get_context_estimate(self) -> dict:\n        \"\"\"Get current context estimate.\"\"\"\n        context_probs = np.bincount(\n            self.context_labels, weights=self.weights, minlength=self.n_contexts\n        )\n        return {\n            \"most_likely_context\": int(np.argmax(context_probs)),\n            \"context_probabilities\": context_probs.tolist(),\n        }\n\nclass BayesianParticleFilter(AdaptiveParticleFilter):\n    \"\"\"\n    Particle filter with Bayesian uncertainty propagation.\n\n    Integrates a Gaussian prior over state with the particle-based\n    posterior, providing principled uncertainty quantification.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"\n        Initialize Bayesian particle filter.\n\n        Args:\n            config: Extends AdaptiveParticleFilter config with:\n                prior_precision, likelihood_precision.\n        \"\"\"\n        super().__init__(config)\n        self.prior_precision = config.get(\"prior_precision\", 1.0)\n        self.likelihood_precision = config.get(\"likelihood_precision\", 1.0)\n\n        # Prior parameters\n        self.prior_mean = np.zeros(self.state_dim)\n        self.prior_cov = np.eye(self.state_dim) / self.prior_precision\n\n        logger.info(\"Initialized BayesianParticleFilter\")\n\n    def bayesian_update(\n        self,\n        observation: np.ndarray,\n        observation_model: Callable | None = None,\n    ) -> dict:\n        \"\"\"Bayesian update with uncertainty propagation.\"\"\"\n        self.update(observation, observation_model)\n\n        # Update Gaussian posterior approximation\n        posterior_mean = self.get_state_estimate()\n        posterior_cov = self.get_weighted_covariance()\n\n        # Combine prior and particle posterior\n        prior_precision_mat = np.linalg.inv(self.prior_cov)\n        posterior_precision = prior_precision_mat + self.likelihood_precision * np.eye(\n            self.state_dim\n        )\n        posterior_cov_combined = np.linalg.inv(posterior_precision)\n\n        return {\n            \"posterior_mean\": posterior_mean,\n            \"posterior_cov\": posterior_cov,\n            \"combined_posterior_cov\": posterior_cov_combined,\n        }\n\n    def get_bayesian_estimate(self) -> dict:\n        \"\"\"Get Bayesian state estimate with uncertainty.\"\"\"\n        return {\n            \"mean\": self.get_state_estimate(),\n            \"uncertainty\": self.get_state_uncertainty(),\n            \"covariance\": self.get_weighted_covariance(),\n        } |
17 | utils/__init__.py | {111 13 0.1261 4.3884} | 123 | \"\"\"Utilities package for adaptive Bayesian driver.\"\"\"\n\nfrom .device import get_device\n\n__all__ = [\"get_device\"] |
18 | utils/compute_optimizations.py | {9087 694 0.2861 4.6174} | 9531 | #  compute_optimizer.py\nimport os\nimport platform\nfrom typing import Any\n\nimport psutil\nimport torch\n\nclass IntelComputeOptimizer:\n    \"\"\"\n    Hardware-aware compute optimization for Intel 8-core + 32GB RAM system.\n    Optimizes PyTorch performance for uncertainty-aware computer vision demo.\n    \"\"\"\n\n    def __init__(self, target_gpu_memory_fraction: float = 0.8):\n        self.system_specs = self._detect_system_specs()\n        self.target_gpu_memory_fraction = target_gpu_memory_fraction\n        self.device = self._setup_optimal_device()\n        self.config = self._generate_optimal_config()\n\n    def _detect_system_specs(self) -> dict[str, Any]:\n        \"\"\"Detect and validate system specifications\"\"\"\n        specs = {\n            \"cpu_count\": os.cpu_count(),\n            \"physical_cores\": psutil.cpu_count(logical=False),\n            \"logical_cores\": psutil.cpu_count(logical=True),\n            \"total_memory_gb\": psutil.virtual_memory().total / (1024**3),\n            \"available_memory_gb\": psutil.virtual_memory().available / (1024**3),\n            \"platform\": platform.processor(),\n            \"is_intel\": \"intel\" in platform.processor().lower(),\n            \"supports_mkldnn\": torch.backends.mkldnn.is_available(),\n        }\n        return specs\n\n    def _setup_optimal_device(self) -> torch.device:\n        \"\"\"Configure optimal PyTorch device with Intel-specific optimizations\"\"\"\n        device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n\n        if device.type == \"cpu\":\n            self._optimize_cpu_performance()\n        else:\n            self._optimize_gpu_performance()\n\n        return device\n\n    def _optimize_cpu_performance(self):\n        \"\"\"Aggressive CPU optimizations for 8-core Intel system\"\"\"\n\n        # Intel MKL optimizations - aggressive for your hardware\n        if self.system_specs[\"is_intel\"] and self.system_specs[\"supports_mkldnn\"]:\n            torch.backends.mkldnn.enabled = True\n            torch.backends.mkldnn.verbose = 0  # Reduce logging overhead\n\n        # Threading optimization for 8-core system\n        # Use 6 threads (reserve 2 for system + other processes)\n        optimal_threads = min(6, self.system_specs[\"physical_cores\"])\n        torch.set_num_threads(optimal_threads)\n\n        # Single interop thread to reduce overhead on multi-core systems\n        torch.set_num_interop_threads(1)\n\n        # Intel-specific environment variables\n        os.environ[\"OMP_NUM_THREADS\"] = str(optimal_threads)\n        os.environ[\"MKL_NUM_THREADS\"] = str(optimal_threads)\n        os.environ[\"NUMEXPR_NUM_THREADS\"] = str(optimal_threads)\n\n        # Intel MKL specific optimizations\n        os.environ[\"KMP_AFFINITY\"] = \"granularity=fine,compact,1,0\"\n        os.environ[\"KMP_BLOCKTIME\"] = \"1\"  # Reduce thread idle time\n\n        # Memory optimization for 32GB system\n        torch.set_default_dtype(torch.float32)  # Efficient precision\n\n        print(\"🔧 Intel CPU optimizations enabled:\")\n        print(f\"   - Threads: {optimal_threads}/{self.system_specs['logical_cores']}\")\n        print(f\"   - MKL-DNN: {torch.backends.mkldnn.enabled}\")\n        print(f\"   - Available RAM: {self.system_specs['available_memory_gb']:.1f}GB\")\n\n    def _optimize_gpu_performance(self):\n        \"\"\"GPU optimizations with CPU fallback awareness\"\"\"\n        if torch.cuda.is_available():\n            # Enable optimized attention for GPU\n            torch.backends.cuda.enable_flash_sdp(True)\n\n            # Memory management\n            torch.cuda.empty_cache()\n            gpu_memory = torch.cuda.get_device_properties(0).total_memory / (1024**3)\n\n            print(f\"🚀 GPU acceleration: {torch.cuda.get_device_name(0)}\")\n            print(f\"   - GPU Memory: {gpu_memory:.1f}GB\")\n            print(f\"   - Target usage: {self.target_gpu_memory_fraction * 100}%\")\n\n    def _generate_optimal_config(self) -> dict[str, Any]:\n        \"\"\"Generate hardware-optimized configuration\"\"\"\n\n        if self.device.type == \"cpu\":\n            # Aggressive CPU config for your 32GB + 8-core system\n            return {\n                # Batch sizing - larger batches for 32GB RAM\n                \"batch_size_train\": 64,  # Increased from conservative 32\n                \"batch_size_eval\": 128,  # Large eval batches for throughput\n                # Uncertainty quantification\n                \"mc_samples\": 8,  # More samples for better uncertainty\n                \"ensemble_size\": 4,  # Multiple models for robust UQ\n                # DataLoader optimization for 8 cores\n                \"num_workers\": 6,  # Reserve 2 cores for training process\n                \"pin_memory\": True,  # Faster CPU->GPU transfer when available\n                \"persistent_workers\": True,  # Reduce worker startup overhead\n                # Memory and precision\n                \"precision\": torch.float32,\n                \"memory_format\": torch.channels_last,  # Intel optimization\n                # Training optimization\n                \"gradient_accumulation_steps\": 1,  # Direct updates with large RAM\n                \"max_memory_usage_gb\": 24,  # Conservative limit for 32GB\n            }\n        else:\n            # GPU configuration with CPU preprocessing support\n            return {\n                \"batch_size_train\": 256,  # Large GPU batches\n                \"batch_size_eval\": 512,\n                \"mc_samples\": 10,\n                \"ensemble_size\": 5,\n                \"num_workers\": 8,  # Full core utilization for data loading\n                \"pin_memory\": True,\n                \"persistent_workers\": True,\n                \"precision\": torch.float16,  # GPU half precision\n                \"memory_format\": torch.channels_last,\n                \"gradient_accumulation_steps\": 1,\n                \"max_memory_usage_gb\": self.target_gpu_memory_fraction\n                * torch.cuda.get_device_properties(0).total_memory\n                / (1024**3),\n            }\n\n    def create_optimized_dataloader(\n        self, dataset, shuffle: bool = True, drop_last: bool = True\n    ):\n        \"\"\"Create hardware-optimized DataLoader\"\"\"\n        return torch.utils.data.DataLoader(\n            dataset,\n            batch_size=self.config[\"batch_size_train\"]\n            if shuffle\n            else self.config[\"batch_size_eval\"],\n            shuffle=shuffle,\n            num_workers=self.config[\"num_workers\"],\n            pin_memory=self.config[\"pin_memory\"],\n            persistent_workers=self.config[\"persistent_workers\"],\n            drop_last=drop_last,\n        )\n\n    def profile_performance(self, model, test_loader) -> dict[str, float]:\n        \"\"\"Benchmark model performance on your hardware\"\"\"\n        import time\n\n        model.eval()\n        total_samples = 0\n        total_time = 0\n\n        with torch.no_grad():\n            start_time = time.time()\n            for batch_idx, (data, _targets) in enumerate(test_loader):\n                data = data.to(self.device)\n\n                # Your uncertainty-aware prediction\n                if hasattr(model, \"predict_with_uncertainty\"):\n                    _predictions, uncertainty = model.predict_with_uncertainty(data)\n                else:\n                    _predictions = model(data)\n\n                total_samples += data.size(0)\n\n                # Break after reasonable sample for quick profiling\n                if batch_idx > 50:  # ~6400 samples with batch_size=128\n                    break\n\n            total_time = time.time() - start_time\n\n        throughput = total_samples / total_time\n        latency = 1000 / throughput  # ms per sample\n\n        return {\n            \"device\": str(self.device),\n            \"samples_processed\": total_samples,\n            \"total_time_sec\": total_time,\n            \"throughput_samples_per_sec\": throughput,\n            \"avg_latency_ms\": latency,\n            \"memory_usage_gb\": self._get_memory_usage(),\n        }\n\n    def _get_memory_usage(self) -> float:\n        \"\"\"Get current memory usage\"\"\"\n        if self.device.type == \"cuda\":\n            return torch.cuda.memory_allocated() / (1024**3)\n        else:\n            return psutil.Process().memory_info().rss / (1024**3)\n\n    def print_optimization_summary(self):\n        \"\"\"Print comprehensive optimization summary\"\"\"\n        print(\"\\n\" + \"=\" * 60)\n        print(\"🎯 INTEL COMPUTE OPTIMIZATION SUMMARY\")\n        print(\"=\" * 60)\n        print(\n            f\"Hardware: {self.system_specs['physical_cores']}-core Intel, {self.system_specs['total_memory_gb']:.0f}GB RAM\"\n        )\n        print(f\"Device: {self.device}\")\n        print(f\"PyTorch threads: {torch.get_num_threads()}\")\n        print(f\"MKL-DNN enabled: {torch.backends.mkldnn.enabled}\")\n        print(\"\\nOptimal Configuration:\")\n        for key, value in self.config.items():\n            print(f\"  {key}: {value}\")\n        print(\"=\" * 60)\n\n# Convenience function for immediate use\ndef setup_optimal_compute(\n    target_gpu_memory_fraction: float = 0.8,\n) -> IntelComputeOptimizer:\n    \"\"\"One-line setup for your Intel 8-core system\"\"\"\n    optimizer = IntelComputeOptimizer(target_gpu_memory_fraction)\n    optimizer.print_optimization_summary()\n    return optimizer |
19 | utils/demo.py | {872 85 0.1800 4.9057} | 950 | #!/usr/bin/env python3\n\"\"\"\nDemo script for adaptive-bayesian-driver package.\nTests basic functionality and package import.\n\"\"\"\n\nimport os\nimport sys\n\n# Add current directory to Python path for development\nsys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))\n\ntry:\n    import src\n\n    print(\"✓ Package import successful\")\n    print(f\"✓ Version: {src.__version__}\")\n    print(f\"✓ Author: {src.__author__}\")\n    print(f\"✓ CUDA Available: {src.CUDA_AVAILABLE}\")\n    print(f\"✓ Device: {src.DEVICE}\")\n\n    # Test device utility\n    from src.utils.device import get_device, get_device_info\n\n    device = get_device()\n    print(f\"✓ Device utility: {device}\")\n    print(f\"✓ Device info: {get_device_info()}\")\n\n    print(\"\\n🎉 Demo completed successfully!\")\n\nexcept Exception as e:\n    print(f\"❌ Error: {e}\")\n    import traceback\n\n    traceback.print_exc()\n    sys.exit(1) |
20 | utils/device.py | {3452 265 0.2683 4.5102} | 3659 | \"\"\"Advanced GPU device management for CUDA-first ML development.\"\"\"\n\nimport logging\nimport platform\nimport sys\nfrom typing import Any\n\nimport torch\n\nlogger = logging.getLogger(__name__)\n\ndef get_device(prefer_cuda: bool = True) -> torch.device:\n    \"\"\"\n    Get optimal compute device with fallback hierarchy.\n\n    Args:\n        prefer_cuda: Whether to prefer CUDA if available\n\n    Returns:\n        PyTorch device optimized for current hardware\n    \"\"\"\n    if prefer_cuda and torch.cuda.is_available():\n        device = torch.device(\"cuda\")\n        logger.info(f\"Using CUDA device: {torch.cuda.get_device_name()}\")\n        return device\n    elif hasattr(torch.backends, \"mps\") and torch.backends.mps.is_available():\n        device = torch.device(\"mps\")\n        logger.info(\"Using Apple Silicon MPS device\")\n        return device\n    else:\n        device = torch.device(\"cpu\")\n        logger.info(\"Using CPU device\")\n        return device\n\ndef setup_cuda_environment() -> dict[str, Any]:\n    \"\"\"\n    Comprehensive CUDA environment setup and diagnostics.\n\n    Returns:\n        Complete device information dictionary\n    \"\"\"\n    device_info = {\n        \"cuda_available\": torch.cuda.is_available(),\n        \"cuda_version\": torch.version.cuda,\n        \"cudnn_version\": (\n            torch.backends.cudnn.version()\n            if torch.backends.cudnn.is_available()\n            else None\n        ),\n        \"device_count\": (torch.cuda.device_count() if torch.cuda.is_available() else 0),\n        \"devices\": [],\n    }\n\n    if torch.cuda.is_available():\n        for i in range(torch.cuda.device_count()):\n            device_props = torch.cuda.get_device_properties(i)\n            major_minor = f\"{device_props.major}.{device_props.minor}\"\n            device_info[\"devices\"].append(\n                {\n                    \"index\": i,\n                    \"name\": device_props.name,\n                    \"total_memory\": device_props.total_memory,\n                    \"memory_allocated\": torch.cuda.memory_allocated(i),\n                    \"memory_reserved\": torch.cuda.memory_reserved(i),\n                    \"compute_capability\": major_minor,\n                }\n            )\n\n        # Set memory allocation strategy for better performance\n        torch.cuda.empty_cache()\n        if hasattr(torch.cuda, \"set_per_process_memory_fraction\"):\n            torch.cuda.set_per_process_memory_fraction(0.8)\n\n    # Add system information\n    device_info[\"system\"] = {\n        \"platform\": platform.platform(),\n        \"python_version\": sys.version,\n        \"torch_version\": torch.__version__,\n        \"cpu_count\": torch.get_num_threads(),\n    }\n\n    return device_info\n\ndef optimize_cuda_performance():\n    \"\"\"Apply CUDA performance optimizations.\"\"\"\n    if not torch.cuda.is_available():\n        logger.warning(\"CUDA not available - skipping optimizations\")\n        return\n\n    # Enable optimizations\n    torch.backends.cudnn.benchmark = True\n    torch.backends.cudnn.deterministic = False\n\n    # Memory management\n    torch.cuda.empty_cache()\n\n    logger.info(\"CUDA performance optimizations applied\")\n\ndef get_device_info() -> str:\n    \"\"\"\n    Get information about the current device.\n\n    Returns:\n        Device information string\n    \"\"\"\n    device = get_device()\n    if device.type == \"cuda\":\n        return f\"CUDA device: {torch.cuda.get_device_name()}\"\n    elif device.type == \"mps\":\n        return \"Apple Silicon MPS device\"\n    else:\n        return \"CPU device\" |
