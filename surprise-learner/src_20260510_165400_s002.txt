idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
6 | environment/scene_renderer.py | {13142 1300 0.3291 4.3707} | 13726 | \"\"\"\nScene renderer for generating driving scenarios.\n\nCreates simple 64x64 fork intersection scenes with oriented bars and danger signs.\n\"\"\"\n\nimport logging\n\nimport numpy as np\nimport torch\nfrom PIL import Image\n\nlogger = logging.getLogger(__name__)\n\nclass SceneRenderer:\n    \"\"\"\n    Renders driving scenes with fork intersections and oriented bars.\n\n    Creates simple sprite-based scenes for testing adaptive learning algorithms.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"\n        Initialize scene renderer.\n\n        Args:\n            config: Configuration dictionary\n        \"\"\"\n        self.config = config\n        self.scene_size = config[\"visual_parameters\"][\"scene_size\"]\n        self.safe_color = config[\"visual_parameters\"][\"safe_background_color\"]\n        self.danger_color = config[\"visual_parameters\"][\"danger_background_color\"]\n\n        # Scene parameters\n        self.bar_width = 4\n        self.bar_length = 20\n        self.intersection_width = 8\n\n        logger.info(f\"Initialized SceneRenderer with scene_size={self.scene_size}\")\n\n    def render_scene(self, scene_params: dict) -> tuple[np.ndarray, dict]:\n        \"\"\"\n        Render a single scene based on parameters.\n\n        Args:\n            scene_params: Dictionary containing scene parameters\n\n        Returns:\n            Tuple of (rendered_scene, metadata)\n        \"\"\"\n        # Extract parameters\n        has_danger = scene_params.get(\"has_danger\", False)\n        orientation_angle = scene_params.get(\"orientation_angle\", 0)\n        difficulty = scene_params.get(\"difficulty\", 0.0)\n        correct_direction = scene_params.get(\"correct_direction\", \"right\")\n\n        # Create base scene\n        scene = self._create_base_scene(has_danger)\n\n        # Add fork intersection\n        scene = self._add_fork_intersection(scene)\n\n        # Add oriented bar\n        scene = self._add_oriented_bar(scene, orientation_angle)\n\n        # Add difficulty (noise, occlusion)\n        if difficulty > 0:\n            scene = self._add_difficulty(scene, difficulty)\n\n        # Convert to tensor\n        scene_tensor = torch.tensor(scene, dtype=torch.float32).unsqueeze(0)\n\n        # Normalize to [0, 1]\n        scene_tensor = scene_tensor / 255.0\n\n        # Metadata\n        metadata = {\n            \"has_danger\": has_danger,\n            \"orientation_angle\": orientation_angle,\n            \"difficulty\": difficulty,\n            \"correct_direction\": correct_direction,\n            \"scene_size\": self.scene_size,\n        }\n\n        return scene_tensor, metadata\n\n    def _create_base_scene(self, has_danger: bool) -> np.ndarray:\n        \"\"\"Create base scene with background color.\"\"\"\n        scene = np.zeros((self.scene_size[0], self.scene_size[1], 3), dtype=np.uint8)\n\n        if has_danger:\n            # Yellow background for danger\n            scene[:, :] = self.danger_color\n        else:\n            # Gray background for safe\n            scene[:, :] = self.safe_color\n\n        return scene\n\n    def _add_fork_intersection(self, scene: np.ndarray) -> np.ndarray:\n        \"\"\"Add fork intersection to scene.\"\"\"\n        h, w = scene.shape[:2]\n        center_x, center_y = w // 2, h // 2\n\n        # Main road (vertical)\n        road_width = self.intersection_width\n        x_start = center_x - road_width // 2\n        x_end = center_x + road_width // 2\n\n        # Darker color for roads\n        road_color = [30, 30, 30]\n        scene[center_y - h // 4 : center_y + h // 4, x_start:x_end] = road_color\n\n        # Fork branches (diagonal)\n        branch_length = h // 4\n        branch_width = road_width // 2\n\n        # Left branch\n        for i in range(branch_length):\n            y = center_y + i\n            x_left = center_x - i // 2\n            x_right = x_left + branch_width\n            if 0 <= y < h and 0 <= x_left < w and 0 <= x_right < w:\n                scene[y, x_left:x_right] = road_color\n\n        # Right branch\n        for i in range(branch_length):\n            y = center_y + i\n            x_left = center_x + i // 2\n            x_right = x_left + branch_width\n            if 0 <= y < h and 0 <= x_left < w and 0 <= x_right < w:\n                scene[y, x_left:x_right] = road_color\n\n        return scene\n\n    def _add_oriented_bar(self, scene: np.ndarray, angle: float) -> np.ndarray:\n        \"\"\"Add oriented bar indicating direction.\"\"\"\n        h, w = scene.shape[:2]\n        center_x, center_y = w // 2, h // 2 - 10  # Slightly above center\n\n        # Create oriented bar\n        bar_color = [255, 255, 255]  # White bar\n\n        # Simple line based on angle\n        if -45 <= angle <= 45:  # Horizontal-ish\n            # Horizontal bar\n            x_start = center_x - self.bar_length // 2\n            x_end = center_x + self.bar_length // 2\n            y_start = center_y - self.bar_width // 2\n            y_end = center_y + self.bar_width // 2\n\n            scene[y_start:y_end, x_start:x_end] = bar_color\n\n        elif angle > 45 or angle < -45:  # Vertical-ish\n            # Vertical bar\n            x_start = center_x - self.bar_width // 2\n            x_end = center_x + self.bar_width // 2\n            y_start = center_y - self.bar_length // 2\n            y_end = center_y + self.bar_length // 2\n\n            scene[y_start:y_end, x_start:x_end] = bar_color\n\n        return scene\n\n    def _add_difficulty(self, scene: np.ndarray, difficulty: float) -> np.ndarray:\n        \"\"\"Add difficulty through noise and occlusion.\"\"\"\n        # Add Gaussian noise\n        noise_std = difficulty * 30  # Scale noise with difficulty\n        noise = np.random.normal(0, noise_std, scene.shape).astype(np.int16)\n        scene = scene.astype(np.int16) + noise\n        scene = np.clip(scene, 0, 255).astype(np.uint8)\n\n        # Add occlusion\n        if difficulty > 0.5:\n            occlusion_size = int(difficulty * 20)\n            h, w = scene.shape[:2]\n\n            # Random occlusion location\n            top = np.random.randint(0, h - occlusion_size)\n            left = np.random.randint(0, w - occlusion_size)\n\n            # Black occlusion\n            scene[top : top + occlusion_size, left : left + occlusion_size] = [0, 0, 0]\n\n        return scene\n\n    def render_scene_batch(\n        self, scene_params_list: list[dict]\n    ) -> tuple[torch.Tensor, list[dict]]:\n        \"\"\"\n        Render a batch of scenes.\n\n        Args:\n            scene_params_list: List of scene parameter dictionaries\n\n        Returns:\n            Tuple of (batch_tensor, metadata_list)\n        \"\"\"\n        scenes = []\n        metadata_list = []\n\n        for params in scene_params_list:\n            scene, metadata = self.render_scene(params)\n            scenes.append(scene)\n            metadata_list.append(metadata)\n\n        # Stack into batch\n        batch_tensor = torch.stack(scenes, dim=0)\n\n        return batch_tensor, metadata_list\n\n    def create_scene_sequence(\n        self, n_scenes: int, bias: float, danger_probability: float = 0.8\n    ) -> tuple[torch.Tensor, list[dict]]:\n        \"\"\"\n        Create a sequence of scenes with specified bias.\n\n        Args:\n            n_scenes: Number of scenes to generate\n            bias: Directional bias (0=left, 1=right)\n            danger_probability: Probability of danger background\n\n        Returns:\n            Tuple of (scene_sequence, metadata_list)\n        \"\"\"\n        scene_params_list = []\n\n        for _ in range(n_scenes):\n            # Determine if scene has danger\n            has_danger = np.random.random() < danger_probability\n\n            # Determine correct direction based on bias\n            if np.random.random() < bias:\n                correct_direction = \"right\"\n                orientation_angle = np.random.uniform(-30, 30)  # Right-leaning\n            else:\n                correct_direction = \"left\"\n                orientation_angle = np.random.uniform(60, 120)  # Left-leaning\n\n            # Random difficulty\n            difficulty = np.random.uniform(0.0, 0.5)\n\n            params = {\n                \"has_danger\": has_danger,\n                \"orientation_angle\": orientation_angle,\n                \"difficulty\": difficulty,\n                \"correct_direction\": correct_direction,\n            }\n\n            scene_params_list.append(params)\n\n        return self.render_scene_batch(scene_params_list)\n\n    def visualize_scene(\n        self, scene_tensor: torch.Tensor, save_path: str = None\n    ) -> np.ndarray:\n        \"\"\"\n        Visualize a scene tensor.\n\n        Args:\n            scene_tensor: Scene tensor to visualize\n            save_path: Optional path to save image\n\n        Returns:\n            Scene as numpy array\n        \"\"\"\n        # Convert tensor to numpy\n        if scene_tensor.dim() == 4:\n            scene_tensor = scene_tensor[0]  # Take first in batch\n\n        scene_np = scene_tensor.squeeze().numpy()\n\n        # Convert to uint8\n        scene_np = (scene_np * 255).astype(np.uint8)\n\n        # Convert to PIL Image if needed\n        if save_path:\n            if scene_np.ndim == 3:\n                image = Image.fromarray(scene_np)\n            else:\n                image = Image.fromarray(scene_np, mode=\"L\")\n            image.save(save_path)\n\n        return scene_np\n\nclass MNISTSceneRenderer:\n    \"\"\"\n    MNIST-based scene renderer for initial testing.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"Initialize MNIST scene renderer.\"\"\"\n        self.config = config\n        self.scene_size = config[\"visual_parameters\"][\"scene_size\"]\n\n        # Try to load MNIST data\n        try:\n            from torchvision import datasets, transforms\n\n            self.transform = transforms.Compose(\n                [transforms.ToTensor(), transforms.Normalize((0.1307,), (0.3081,))]\n            )\n\n            # Load MNIST dataset\n            self.dataset = datasets.MNIST(\n                root=\"./data\", train=True, download=True, transform=self.transform\n            )\n\n            # Separate low and high digits\n            self.low_indices = [\n                i for i, (_, label) in enumerate(self.dataset) if label < 5\n            ]\n            self.high_indices = [\n                i for i, (_, label) in enumerate(self.dataset) if label >= 5\n            ]\n\n            logger.info(\"Loaded MNIST dataset for scene rendering\")\n\n        except Exception as e:\n            logger.warning(f\"Could not load MNIST: {e}\")\n            self.dataset = None\n\n    def render_mnist_scene(self, scene_params: dict) -> tuple[torch.Tensor, dict]:\n        \"\"\"\n        Render MNIST-based scene.\n\n        Args:\n            scene_params: Scene parameters\n\n        Returns:\n            Tuple of (scene_tensor, metadata)\n        \"\"\"\n        if self.dataset is None:\n            # Fallback to dummy scene\n            scene = torch.zeros(1, self.scene_size[0], self.scene_size[1])\n            metadata = {\"is_high\": False, \"digit\": 0}\n            return scene, metadata\n\n        # Get parameters\n        bias = scene_params.get(\"bias\", 0.5)\n        difficulty = scene_params.get(\"difficulty\", 0.0)\n\n        # Select high or low digit based on bias\n        if np.random.random() < bias:\n            idx = np.random.choice(self.high_indices)\n            is_high = True\n        else:\n            idx = np.random.choice(self.low_indices)\n            is_high = False\n\n        # Get image and label\n        image, label = self.dataset[idx]\n\n        # Add difficulty\n        if difficulty > 0:\n            image = self._add_mnist_difficulty(image, difficulty)\n\n        # Resize to target size if needed\n        if image.shape[-1] != self.scene_size[0]:\n            import torch.nn.functional as F\n\n            image = F.interpolate(\n                image.unsqueeze(0),\n                size=self.scene_size,\n                mode=\"bilinear\",\n                align_corners=False,\n            ).squeeze(0)\n\n        metadata = {\"is_high\": is_high, \"digit\": int(label), \"difficulty\": difficulty}\n\n        return image, metadata\n\n    def _add_mnist_difficulty(\n        self, image: torch.Tensor, difficulty: float\n    ) -> torch.Tensor:\n        \"\"\"Add difficulty to MNIST image.\"\"\"\n        # Add noise\n        noise = torch.randn_like(image) * difficulty * 0.5\n        image = image + noise\n\n        # Add occlusion\n        if difficulty > 0.5:\n            occlusion_size = int(difficulty * 10)\n            h, w = image.shape[-2:]\n\n            top = np.random.randint(0, h - occlusion_size)\n            left = np.random.randint(0, w - occlusion_size)\n\n            image[..., top : top + occlusion_size, left : left + occlusion_size] = 0\n\n        return torch.clamp(image, 0, 1)\n\n    def create_mnist_sequence(\n        self, n_scenes: int, bias: float\n    ) -> tuple[torch.Tensor, list[dict]]:\n        \"\"\"Create sequence of MNIST scenes.\"\"\"\n        scenes = []\n        metadata_list = []\n\n        for _ in range(n_scenes):\n            difficulty = np.random.uniform(0.0, 0.3)\n            params = {\"bias\": bias, \"difficulty\": difficulty}\n\n            scene, metadata = self.render_mnist_scene(params)\n            scenes.append(scene)\n            metadata_list.append(metadata)\n\n        # Stack into batch\n        batch_tensor = torch.stack(scenes, dim=0)\n\n        return batch_tensor, metadata_list |
7 | environment/task_hmm.py | {14722 1187 0.3218 4.3222} | 15404 | \"\"\"\nTask HMM for modeling hidden state dynamics in the driving task.\n\nImplements a Hidden Markov Model for task structure discovery and context tracking.\n\"\"\"\n\nimport logging\nfrom enum import Enum\n\nimport numpy as np\n\nlogger = logging.getLogger(__name__)\n\nclass TaskState(Enum):\n    \"\"\"Task states for the driving scenario.\"\"\"\n\n    SAFE_STRAIGHT = \"safe_straight\"\n    SAFE_LEFT = \"safe_left\"\n    SAFE_RIGHT = \"safe_right\"\n    DANGER_AVOID_LEFT = \"danger_avoid_left\"\n    DANGER_AVOID_RIGHT = \"danger_avoid_right\"\n\nclass TaskHMM:\n    \"\"\"\n    Hidden Markov Model for task structure discovery.\n\n    Models the hidden states of the driving task and their transitions.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        \"\"\"\n        Initialize Task HMM.\n\n        Args:\n            config: Configuration dictionary\n        \"\"\"\n        self.config = config\n        self.n_states = len(TaskState)\n        self.state_names = [state.value for state in TaskState]\n\n        # Initialize HMM parameters\n        self.transition_matrix = self._initialize_transition_matrix()\n        self.emission_params = self._initialize_emission_params()\n        self.initial_state_probs = self._initialize_initial_state_probs()\n\n        # Current state tracking\n        self.current_state = 0\n        self.state_history = []\n        self.observation_history = []\n\n        # Learning parameters\n        self.learning_rate = config.get(\"hmm_learning_rate\", 0.01)\n        self.adaptation_enabled = config.get(\"hmm_adaptation\", True)\n\n        logger.info(f\"Initialized TaskHMM with {self.n_states} states\")\n\n    def _initialize_transition_matrix(self) -> np.ndarray:\n        \"\"\"Initialize transition matrix with task-specific structure.\"\"\"\n        # Create transition matrix with task logic\n        A = np.zeros((self.n_states, self.n_states))\n\n        # Define transition probabilities based on task structure\n        # High probability of staying in same state\n        for i in range(self.n_states):\n            A[i, i] = 0.8\n\n        # Specific transition patterns\n        # Safe states can transition to any state\n        for i in range(3):  # Safe states\n            remaining_prob = 1.0 - A[i, i]\n            for j in range(self.n_states):\n                if i != j:\n                    A[i, j] = remaining_prob / (self.n_states - 1)\n\n        # Danger states prefer to stay or go to safe states\n        for i in range(3, self.n_states):  # Danger states\n            remaining_prob = 1.0 - A[i, i]\n            # Higher probability to safe states\n            for j in range(3):\n                A[i, j] = remaining_prob * 0.4 / 3\n            # Lower probability to other danger states\n            for j in range(3, self.n_states):\n                if i != j:\n                    A[i, j] = remaining_prob * 0.2 / (self.n_states - 4)\n\n        return A\n\n    def _initialize_emission_params(self) -> dict:\n        \"\"\"Initialize emission parameters for each state.\"\"\"\n        params = {}\n\n        # Each state has different observation characteristics\n        for i, state in enumerate(TaskState):\n            if state == TaskState.SAFE_STRAIGHT:\n                params[i] = {\n                    \"background_danger_prob\": 0.2,\n                    \"orientation_mean\": 0.0,\n                    \"orientation_std\": 15.0,\n                    \"correct_direction\": \"straight\",\n                }\n            elif state == TaskState.SAFE_LEFT:\n                params[i] = {\n                    \"background_danger_prob\": 0.2,\n                    \"orientation_mean\": 90.0,\n                    \"orientation_std\": 20.0,\n                    \"correct_direction\": \"left\",\n                }\n            elif state == TaskState.SAFE_RIGHT:\n                params[i] = {\n                    \"background_danger_prob\": 0.2,\n                    \"orientation_mean\": -90.0,\n                    \"orientation_std\": 20.0,\n                    \"correct_direction\": \"right\",\n                }\n            elif state == TaskState.DANGER_AVOID_LEFT:\n                params[i] = {\n                    \"background_danger_prob\": 0.9,\n                    \"orientation_mean\": 90.0,\n                    \"orientation_std\": 25.0,\n                    \"correct_direction\": \"left\",  # Avoid by going left\n                }\n            elif state == TaskState.DANGER_AVOID_RIGHT:\n                params[i] = {\n                    \"background_danger_prob\": 0.9,\n                    \"orientation_mean\": -90.0,\n                    \"orientation_std\": 25.0,\n                    \"correct_direction\": \"right\",  # Avoid by going right\n                }\n\n        return params\n\n    def _initialize_initial_state_probs(self) -> np.ndarray:\n        \"\"\"Initialize initial state probabilities.\"\"\"\n        # Start with uniform distribution\n        return np.ones(self.n_states) / self.n_states\n\n    def predict_next_state(self) -> np.ndarray:\n        \"\"\"Predict next state probabilities.\"\"\"\n        current_state_probs = np.zeros(self.n_states)\n        current_state_probs[self.current_state] = 1.0\n\n        next_state_probs = current_state_probs @ self.transition_matrix\n        return next_state_probs\n\n    def update_state(self, observation: dict) -> dict:\n        \"\"\"\n        Update state based on observation.\n\n        Args:\n            observation: Dictionary containing observation features\n\n        Returns:\n            State update results\n        \"\"\"\n        # Compute emission probabilities\n        emission_probs = self._compute_emission_probabilities(observation)\n\n        # Predict next state\n        predicted_probs = self.predict_next_state()\n\n        # Update state (Bayes rule)\n        posterior_probs = predicted_probs * emission_probs\n        posterior_probs = posterior_probs / (np.sum(posterior_probs) + 1e-10)\n\n        # Select most likely state\n        self.current_state = np.argmax(posterior_probs)\n\n        # Store history\n        self.state_history.append(self.current_state)\n        self.observation_history.append(observation)\n\n        # Adapt parameters if enabled\n        if self.adaptation_enabled:\n            self._adapt_parameters(observation, posterior_probs)\n\n        return {\n            \"current_state\": self.current_state,\n            \"state_name\": self.state_names[self.current_state],\n            \"state_probabilities\": posterior_probs,\n            \"emission_probabilities\": emission_probs,\n            \"predicted_probabilities\": predicted_probs,\n        }\n\n    def _compute_emission_probabilities(self, observation: dict) -> np.ndarray:\n        \"\"\"Compute emission probabilities for current observation.\"\"\"\n        probs = np.zeros(self.n_states)\n\n        for state_idx, params in self.emission_params.items():\n            prob = 1.0\n\n            # Background danger probability\n            if \"has_danger\" in observation:\n                danger_prob = params[\"background_danger_prob\"]\n                if observation[\"has_danger\"]:\n                    prob *= danger_prob\n                else:\n                    prob *= 1.0 - danger_prob\n\n            # Orientation likelihood\n            if \"orientation_angle\" in observation:\n                angle = observation[\"orientation_angle\"]\n                mean = params[\"orientation_mean\"]\n                std = params[\"orientation_std\"]\n\n                # Gaussian likelihood\n                angle_prob = np.exp(-0.5 * ((angle - mean) / std) ** 2)\n                prob *= angle_prob\n\n            # Correct direction consistency\n            if \"correct_direction\" in observation:\n                if observation[\"correct_direction\"] == params[\"correct_direction\"]:\n                    prob *= 1.0\n                else:\n                    prob *= 0.3  # Lower but non-zero probability\n\n            probs[state_idx] = prob\n\n        return probs\n\n    def _adapt_parameters(self, observation: dict, posterior_probs: np.ndarray):\n        \"\"\"Adapt HMM parameters based on observation.\"\"\"\n        # Simple online adaptation\n        for state_idx in range(self.n_states):\n            if posterior_probs[state_idx] > 0.1:  # Only adapt if state is likely\n                params = self.emission_params[state_idx]\n\n                # Adapt orientation parameters\n                if \"orientation_angle\" in observation:\n                    angle = observation[\"orientation_angle\"]\n                    current_mean = params[\"orientation_mean\"]\n\n                    # Update mean\n                    new_mean = (\n                        1 - self.learning_rate\n                    ) * current_mean + self.learning_rate * angle\n                    params[\"orientation_mean\"] = new_mean\n\n                # Adapt danger probability\n                if \"has_danger\" in observation:\n                    current_prob = params[\"background_danger_prob\"]\n                    target_prob = 1.0 if observation[\"has_danger\"] else 0.0\n\n                    new_prob = (\n                        1 - self.learning_rate\n                    ) * current_prob + self.learning_rate * target_prob\n                    params[\"background_danger_prob\"] = np.clip(new_prob, 0.01, 0.99)\n\n    def generate_observation(self, state: int | None = None) -> dict:\n        \"\"\"\n        Generate observation from current or specified state.\n\n        Args:\n            state: State to generate from (uses current state if None)\n\n        Returns:\n            Generated observation\n        \"\"\"\n        if state is None:\n            state = self.current_state\n\n        params = self.emission_params[state]\n\n        # Generate background danger\n        has_danger = np.random.random() < params[\"background_danger_prob\"]\n\n        # Generate orientation\n        orientation_angle = np.random.normal(\n            params[\"orientation_mean\"], params[\"orientation_std\"]\n        )\n\n        # Get correct direction\n        correct_direction = params[\"correct_direction\"]\n\n        return {\n            \"has_danger\": has_danger,\n            \"orientation_angle\": orientation_angle,\n            \"correct_direction\": correct_direction,\n            \"state\": state,\n            \"state_name\": self.state_names[state],\n        }\n\n    def compute_likelihood(self, observation_sequence: list[dict]) -> float:\n        \"\"\"\n        Compute likelihood of observation sequence.\n\n        Args:\n            observation_sequence: Sequence of observations\n\n        Returns:\n            Log-likelihood of sequence\n        \"\"\"\n        if not observation_sequence:\n            return 0.0\n\n        # Forward algorithm\n        alpha = np.zeros((len(observation_sequence), self.n_states))\n\n        # Initialize\n        emission_probs = self._compute_emission_probabilities(observation_sequence[0])\n        alpha[0] = self.initial_state_probs * emission_probs\n\n        # Forward pass\n        for t in range(1, len(observation_sequence)):\n            emission_probs = self._compute_emission_probabilities(\n                observation_sequence[t]\n            )\n\n            for j in range(self.n_states):\n                alpha[t, j] = emission_probs[j] * np.sum(\n                    alpha[t - 1] * self.transition_matrix[:, j]\n                )\n\n        # Compute log-likelihood\n        likelihood = np.sum(alpha[-1])\n        return np.log(likelihood + 1e-10)\n\n    def get_state_statistics(self) -> dict:\n        \"\"\"Get statistics about state transitions.\"\"\"\n        if len(self.state_history) < 2:\n            return {\"n_states\": self.n_states, \"n_transitions\": 0}\n\n        # Count transitions\n        transition_counts = np.zeros((self.n_states, self.n_states))\n\n        for i in range(len(self.state_history) - 1):\n            from_state = self.state_history[i]\n            to_state = self.state_history[i + 1]\n            transition_counts[from_state, to_state] += 1\n\n        # State occupancy\n        state_counts = np.bincount(self.state_history, minlength=self.n_states)\n        state_frequencies = state_counts / len(self.state_history)\n\n        return {\n            \"n_states\": self.n_states,\n            \"n_transitions\": len(self.state_history) - 1,\n            \"transition_counts\": transition_counts.tolist(),\n            \"state_frequencies\": state_frequencies.tolist(),\n            \"current_state\": self.current_state,\n            \"current_state_name\": self.state_names[self.current_state],\n        }\n\n    def reset(self):\n        \"\"\"Reset HMM state.\"\"\"\n        self.current_state = 0\n        self.state_history.clear()\n        self.observation_history.clear()\n\n        # Reset to initial parameters\n        self.transition_matrix = self._initialize_transition_matrix()\n        self.emission_params = self._initialize_emission_params()\n\n        logger.info(\"Reset TaskHMM\")\n\nclass AdaptiveTaskHMM(TaskHMM):\n    \"\"\"\n    Adaptive Task HMM that learns structure online.\n    \"\"\"\n\n    def __init__(self, config: dict):\n        super().__init__(config)\n\n        # Adaptive parameters\n        self.structure_learning_rate = config.get(\"structure_learning_rate\", 0.001)\n        self.structure_adaptation = config.get(\"structure_adaptation\", True)\n\n        # Sufficient statistics for EM-style updates\n        self.transition_counts = np.zeros((self.n_states, self.n_states))\n        self.state_counts = np.zeros(self.n_states)\n\n        logger.info(\"Initialized AdaptiveTaskHMM\")\n\n    def update_structure(self, observation: dict, posterior_probs: np.ndarray):\n        \"\"\"Update HMM structure based on observation.\"\"\"\n        if not self.structure_adaptation:\n            return\n\n        # Update sufficient statistics\n        self.state_counts += posterior_probs\n\n        if len(self.state_history) > 1:\n            prev_state = self.state_history[-2]\n\n            # Update transition counts\n            for j in range(self.n_states):\n                self.transition_counts[prev_state, j] += posterior_probs[j]\n\n        # Periodically update transition matrix\n        if len(self.state_history) % 100 == 0:\n            self._update_transition_matrix()\n\n    def _update_transition_matrix(self):\n        \"\"\"Update transition matrix from sufficient statistics.\"\"\"\n        # Normalize transition counts\n        for i in range(self.n_states):\n            row_sum = np.sum(self.transition_counts[i])\n            if row_sum > 0:\n                new_row = self.transition_counts[i] / row_sum\n\n                # Smooth update\n                self.transition_matrix[i] = (\n                    1 - self.structure_learning_rate\n                ) * self.transition_matrix[i] + self.structure_learning_rate * new_row\n\n    def update_state(self, observation: dict) -> dict:\n        \"\"\"Update state with structure learning.\"\"\"\n        # Call parent method\n        result = super().update_state(observation)\n\n        # Update structure\n        self.update_structure(observation, result[\"state_probabilities\"])\n\n        return result |
8 | environment/time_varying_bias_environment.py | {994 105 0.2575 4.4701} | 1018 | import numpy as np\n\nclass TimeVaryingBiasEnvironment:\n    def __init__(self):\n        self.current_bias = 0.5  # initialize as unbiased\n        self.target_bias = 0.5  # Current target bias\n        self.tau_volatility = 50  # Time constant for bias evolution\n        self.hazard_rate = 0.02  # Probability of bias target change per trial\n\n    def update_bias_target(self):\n        # Hazard-based target switching\n        if np.random.random() < self.hazard_rate:\n            # Sample new target: 0.2 (80% left) to 0.8 (80% right)\n            self.target_bias = np.random.choice([0.2, 0.8])\n\n    def evolve_bias(self):\n        # Gradual drift toward target with time constant\n        alpha = 1.0 / self.tau_volatility\n        self.current_bias += alpha * (self.target_bias - self.current_bias)\n\n    def generate_trial(self):\n        self.update_bias_target()\n        self.evolve_bias()\n        # Generate left/right cue based on current bias\n        return np.random.random() < self.current_bias |
