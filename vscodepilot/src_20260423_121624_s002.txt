idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
1 | powershell/jso-engine.psm1 | {36365 3612 0.2559 4.9241} | 37696 | #Requires -Version 7.2\n\nusing namespace System\nusing namespace System.IO\nusing namespace System.Text\nusing namespace System.Collections.Generic\n\nSet-StrictMode -Version Latest\n\n# Import hashing primitives for low-level hash algorithms\n# Provides: Get-Hash, Get-StreamHash, Get-BlockHashes, Get-RollingHashes, Get-ContentBoundaryOffsets\n# Plus classes: FNV1a, SHA256, Polynomial, RollingWindow\nImport-Module \"$PSScriptRoot/hashing-primitives.psm1\" -Force\n\n<#\n.SYNOPSIS\n    JSO Engine - JSON/JSONL primitives library (v2).\n\n.DESCRIPTION\n    Pure, dependency-free JSON/JSONL infrastructure providing core operations:\n\n    DOMAIN: JSO = JSON + JSONL (combined handling)\n    ROLE: Engine (primitives and utilities)\n    PATTERN: Library module (no entry points)\n\n    HASH PRIMITIVES:\n    - RabinKarpHash class - Rabin-Karp rolling hash algorithm (O(1) sliding window)\n    - Get-PathHash - FNV-1a hash for path addressing (location-based)\n    - Get-ContentHash - Content fingerprinting using Rabin-Karp (whole-content hash)\n\n    STORAGE PRIMITIVES:\n    - JSONL streaming (constant memory, lazy evaluation)\n    - Binary indexing (O(1) line access with byte offsets)\n    - Bloom filters (probabilistic set membership)\n    - Canonical JSON (deterministic serialization)\n\n    APPLICATIONS (built on primitives):\n    - Content-Defined Chunking (CDC) - uses RabinKarpHash for boundary detection\n    - String pattern matching - Rabin-Karp algorithm for exact matches\n    - JSON transformation - Encoders for minify, canonical, jsonpath, tabular\n\n.NOTES\n    Domain: JSO (JSON/JSONL)\n    Role: Core Library (Primitives + Applications)\n    Version: 2.0.0\n    Requires: PowerShell 7.2+\n    Depends: hashing-primitives.psm1\n\n    Part of Memoria Architectura - provides storage layer for:\n    - Memoria.Experientia (experientia.jsonl streaming)\n    - Codex.Scientiae (corpus JSONL processing)\n    - Ars.Prudentia (pattern detection applications)\n\n.LINK\n    https://github.com/aghado01/vscodepilot\n#>\n\nfunction Resolve-CanonicalPath {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Path\n    )\n\n    # Normalize path separators and resolve relative paths\n    $resolved = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Path)\n\n    # Convert to lowercase for case-insensitive filesystems (Windows)\n    if ($IsWindows) {\n        $resolved = $resolved.ToLowerInvariant()\n    }\n\n    # Normalize separators to forward slash\n    return $resolved.Replace('\\', '/')\n}\n\n<#\n.SYNOPSIS\nRabin-Karp polynomial rolling hash algorithm.\n\n.DESCRIPTION\nImplements the Rabin-Karp rolling hash algorithm with O(1) sliding window updates.\nThis is a PRIMITIVE hash algorithm that provides the rolling hash capability.\n\nUses:\n- Base 257 (prime larger than ASCII range 0-255)\n- Modulo 1000000007 (large prime for good distribution)\n- Character-based operations (not byte-based)\n- Precomputed BasePower for efficient character removal\n\nNote: \"Rolling hash\" refers to the CAPABILITY of O(1) window sliding.\n\"Rabin-Karp\" is the specific ALGORITHM that provides this capability.\n#>\nclass RabinKarpHash {\n    [uint32]$Hash\n    [int]$WindowSize\n    [uint32]$Base = 257\n    [uint32]$Mod = 1000000007\n    [uint32]$BasePower\n\n    RabinKarpHash([int]$windowSize) {\n        $this.WindowSize = $windowSize\n        $this.Hash = 0\n\n        # Precompute base^windowSize mod Mod for efficient removal\n        $this.BasePower = 1\n        for ($i = 0; $i -lt $windowSize; $i++) {\n            $this.BasePower = ($this.BasePower * $this.Base) % $this.Mod\n        }\n    }\n\n    [uint32] AddChar([char]$c) {\n        # Hash = (Hash * Base + charValue) mod Mod\n        $this.Hash = (($this.Hash * $this.Base) + [int]$c) % $this.Mod\n        return $this.Hash\n    }\n\n    [uint32] RemoveChar([char]$c) {\n        # Hash = (Hash - charValue * Base^(WindowSize-1)) mod Mod\n        $removal = ([long][int]$c * $this.BasePower) % $this.Mod\n        $this.Hash = ($this.Hash + $this.Mod - $removal) % $this.Mod\n        return $this.Hash\n    }\n\n    [uint32] RollWindow([char]$oldChar, [char]$newChar) {\n        # Efficiently slide window: remove old character, add new character\n        $this.RemoveChar($oldChar)\n        $this.AddChar($newChar)\n        return $this.Hash\n    }\n\n    [void] Reset() {\n        $this.Hash = 0\n    }\n}\n\n<#\n.SYNOPSIS\nCompute FNV-1a hash for file path addressing.\n\n.DESCRIPTION\nPRIMITIVE hash function using FNV-1a (Fowler-Noll-Vo variant 1a) algorithm.\nProvides fast, deterministic hashing for path-based addressing with good distribution.\n\nUses canonical path normalization for consistent hashing across:\n- Path separators (/ vs \\)\n- Case sensitivity (Windows vs Linux)\n- Relative vs absolute paths\n\n.PARAMETER Path\nFile path to hash (relative or absolute)\n\n.OUTPUTS\nSystem.String - 8-character hexadecimal hash\n\n.EXAMPLE\nGet-PathHash -Path \"src/utils/helper.ps1\"\n# Returns deterministic hash like \"a3f2b1c0\"\n\n.NOTES\nThis is a PRIMITIVE hash function. It is NOT an application like CDC or SimHash.\nRefactored in Phase 1 to use Get-Hash primitive from hashing-primitives.\n#>\nfunction Get-PathHash {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Path\n    )\n\n    $canonical = Resolve-CanonicalPath -Path $Path\n\n    # Use Get-Hash primitive with FNV1a algorithm\n    $hash = Get-Hash -Content $canonical -Algorithm FNV1a\n\n    # Format as 8-character hex string (consistent with old implementation)\n    return [Convert]::ToString($hash, 16).PadLeft(8, '0')\n}\n\n<#\n.SYNOPSIS\nCompute content hash using Rabin-Karp rolling hash algorithm.\n\n.DESCRIPTION\nPRIMITIVE hash function that computes a single hash value over entire content.\nUses the Rabin-Karp rolling hash algorithm for content-based addressing.\n\nThis is NOT content-defined chunking (CDC) - it simply hashes the entire content\nto produce a fingerprint for deduplication and change detection.\n\n.PARAMETER Content\nContent string to hash\n\n.PARAMETER WindowSize\nRolling window size for hash computation (default: 32 characters)\n\n.OUTPUTS\nSystem.Int64 - Content hash value\n\n.EXAMPLE\nGet-ContentHash -Content $fileContent\n# Returns single hash for entire content\n\n.NOTES\nApplications built on this primitive:\n- Content-Defined Chunking (CDC) - uses boundaries for chunk detection\n- SimHash - combines multiple window hashes for near-duplicate detection\n- Fuzzy matching - sliding window similarity detection\n#>\nfunction Get-ContentHash {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [AllowEmptyString()]\n        [string]$Content,\n\n        [int]$WindowSize = 32\n    )\n\n    if ([string]::IsNullOrWhiteSpace($Content)) {\n        return 0L\n    }\n\n    $roller = [RabinKarpHash]::new($WindowSize)\n    foreach ($char in $Content.ToCharArray()) {\n        [void]$roller.AddChar($char)\n    }\n\n    return [long]$roller.Hash\n}\n\n<#\n.SYNOPSIS\nPerform content-defined chunking (CDC) using Rabin-Karp boundaries.\n\n.DESCRIPTION\nAPPLICATION function (not a primitive) that uses RabinKarpHash to detect chunk boundaries.\nThis implements the CDC algorithm for efficient file synchronization and deduplication.\n\nCDC finds natural boundaries in content based on hash values, making chunks stable\nacross edits (inserting text at the start doesn't re-chunk the entire file).\n\n.PARAMETER Content\nContent string to chunk\n\n.PARAMETER WindowSize\nRolling window size for boundary detection (default: 64 characters)\n\n.PARAMETER ChunkSize\nTarget chunk size for fallback fixed chunking (default: 4096 bytes)\n\n.PARAMETER Mask\nBit mask for boundary detection (default: 0xFFF = ~1/4096 probability)\n\n.OUTPUTS\nHashtable with Hash, Boundaries array, and ChunkCount\n\n.EXAMPLE\n$result = Get-ContentChunks -Content $largeFile\n# Returns: @{ Hash=...; Boundaries=@(1024,3456,...); ChunkCount=5 }\n\n.NOTES\nThis is an APPLICATION built on Get-ContentBoundaryOffsets primitive from hashing-primitives.\nUses rolling hash to detect content-defined boundaries for stable chunking.\nRefactored in Phase 1 to use primitives instead of direct RabinKarpHash calls.\n#>\nfunction Get-ContentChunks {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Content,\n\n        [int]$WindowSize = 64,\n\n        [int]$ChunkSize = 4096,\n\n        [int]$Mask = 0xFFF\n    )\n\n    # Convert mask to modulo for Get-ContentBoundaryOffsets\n    # Mask 0xFFF means \"last 12 bits = 0\" which is equivalent to \"divisible by 4096\"\n    # In general: Mask with N trailing bits set = modulo (2^N)\n    $maskBits = [Math]::Floor([Math]::Log($Mask + 1, 2))\n    $boundaryModulo = [Math]::Pow(2, $maskBits)\n\n    # Use primitive to find boundaries\n    $boundaryOffsets = @(Get-ContentBoundaryOffsets -Content $Content -WindowSize $WindowSize -BoundaryModulo $boundaryModulo -BoundaryValue 0)\n\n    # Convert offset objects to simple array of integers\n    $boundaries = [List[int]]::new()\n    foreach ($boundary in $boundaryOffsets) {\n        $boundaries.Add($boundary.Offset)\n    }\n\n    # If no boundaries found, use fixed chunking as fallback\n    if ($boundaries.Count -eq 0) {\n        for ($i = $ChunkSize; $i -lt $Content.Length; $i += $ChunkSize) {\n            $boundaries.Add($i)\n        }\n    }\n\n    # Compute final hash using Get-Hash primitive\n    $finalHash = Get-Hash -Content $Content -Algorithm FNV1a\n\n    return @{\n        Hash = $finalHash\n        Boundaries = $boundaries\n        ChunkCount = $boundaries.Count + 1\n    }\n}\n\n<#\n.SYNOPSIS\n    Finds all occurrences of a pattern in a text using Rabin-Karp algorithm.\n\n.DESCRIPTION\n    APPLICATION function (not a primitive) that uses RollingWindow and Polynomial classes\n    from hashing-primitives to perform pattern matching with rolling polynomial hash.\n\n    This is the Rabin-Karp string search algorithm - efficient for finding exact matches\n    of a pattern string within a larger text. Uses O(1) rolling hash updates.\n\n    Moved from hashing-primitives to jso-engine in Phase 1 refactor.\n\n.PARAMETER Text\n    The text to search within.\n\n.PARAMETER Pattern\n    The pattern string to find.\n\n.OUTPUTS\n    System.Int32[] - Array of 0-based indices where pattern was found.\n\n.EXAMPLE\n    Find-StringPattern -Text \"abcabcabc\" -Pattern \"cab\"\n    # Returns: @(2, 5) - pattern found at positions 2 and 5\n\n.EXAMPLE\n    \"the quick brown fox\" | Find-StringPattern -Pattern \"quick\"\n    # Returns: @(4) - pattern found at position 4\n\n.NOTES\n    This is an APPLICATION built on Polynomial and RollingWindow primitives.\n    For more flexible rolling hash operations, use Get-RollingHashes directly.\n#>\nfunction Find-StringPattern {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [string]$Text,\n\n        [Parameter(Mandatory)]\n        [string]$Pattern\n    )\n\n    $patternLength = $Pattern.Length\n    $textLength = $Text.Length\n    if ($patternLength -gt $textLength) { return }\n\n    # Setup polynomial hash algorithm options\n    $polyOptions = @{\n        Base = 257\n        Mod  = 1000000007\n    }\n\n    # Get the algorithm context for pattern length\n    $polyContext = [Polynomial]::GetRollContext($patternLength, $polyOptions)\n\n    # Compute hash of pattern (one-shot)\n    [uint32]$patternHash = [Polynomial]::Compute($Pattern, $polyContext)\n\n    # Create rolling window for text scanning\n    $roller = [RollingWindow]::new([Polynomial], $patternLength, $polyOptions)\n\n    # Initialize with first window of text\n    [void]$roller.Initialize($Text.Substring(0, $patternLength))\n\n    # Track found match indices\n    $foundIndices = [System.Collections.Generic.List[int]]::new()\n\n    # Check the initial window\n    if ($roller.Hash -eq $patternHash) {\n        # Hash match - verify with actual substring comparison\n        if ($Text.Substring(0, $patternLength) -eq $Pattern) {\n            $foundIndices.Add(0)\n        }\n    }\n\n    # Roll through remaining text\n    for ($i = $patternLength; $i -lt $textLength; $i++) {\n        [char]$newChar = $Text[$i]\n        [void]$roller.Roll($newChar)\n\n        # Check if hash matches\n        if ($roller.Hash -eq $patternHash) {\n            $matchIndex = $i - $patternLength + 1\n            # Hash match - verify with actual substring comparison\n            if ($Text.Substring($matchIndex, $patternLength) -eq $Pattern) {\n                $foundIndices.Add($matchIndex)\n            }\n        }\n    }\n\n    return $foundIndices\n}\n\n# ================================================================\n# SECTION 2: JSO-SCRIPTS ORCHESTRATION (~165 lines)\n# ================================================================\n# CONTENT TRANSFORMATION & FILE SHARDING\n# Provides data processing coordination without external dependencies:\n# - JsoEncoder: Content transformation (minify, canonical, jsonpath, tabular)\n# - JsoSharding: Generic file splitting logic\n# - New-JsoEncoder: Factory for creating encoders\n#\n# Note: JsoProcessor class (full orchestration) and whitespace compression\n# modes are NOT included - they require additional jso-primitives functions.\n# ================================================================\n\n<#\n.SYNOPSIS\nJsoEncoder - Generic content transformation class.\n\n.DESCRIPTION\nProvides pluggable content transformation strategies:\n- Identity: Pass-through (no transformation)\n- Minifier: Whitespace reduction for strings\n- Canonical: Deterministic JSON serialization\n- JsonPath: Extract specific JSON paths\n- Tabular: Convert arrays to tabular format\n- Custom: User-provided transformation scriptblock\n#>\nclass JsoEncoder {\n    [string]$Name\n    [hashtable]$Options\n    [scriptblock]$EncodeFunction\n\n    JsoEncoder([string]$Name, [hashtable]$Options, [scriptblock]$EncodeFunction) {\n        $this.Name = $Name\n        $this.Options = $Options\n        $this.EncodeFunction = $EncodeFunction\n    }\n\n    [object] Encode([object]$Content) {\n        return & $this.EncodeFunction.Invoke($Content, $this.Options)\n    }\n\n    static [JsoEncoder] CreateIdentity() {\n        return [JsoEncoder]::new('identity', @{}, { param($content, $options) return $content })\n    }\n\n    static [JsoEncoder] CreateMinifier() {\n        $minifyScript = {\n            param($content, $options)\n            if ($content -is [string]) {\n                return ($content -replace '\\s+', ' ').Trim()\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('minifier', @{}, $minifyScript)\n    }\n\n    static [JsoEncoder] CreateCanonical() {\n        $canonicalScript = {\n            param($content, $options)\n            if ($content -is [pscustomobject] -or $content -is [hashtable]) {\n                return ConvertTo-CanonicalJson -InputObject $content -Depth ($options.Depth ?? 100)\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('canonical', @{}, $canonicalScript)\n    }\n\n    static [JsoEncoder] CreateJsonPath([string]$Path) {\n        $jsonPathScript = {\n            param($content, $options)\n            $path = $options.Path\n            # Simple JSONPath implementation (dot notation only)\n            if ($content -is [pscustomobject] -or $content -is [hashtable]) {\n                $result = $content\n                foreach ($segment in $path.Split('.')) {\n                    if ($null -eq $result) { return $null }\n                    $result = $result.$segment\n                }\n                return $result\n            }\n            return $content\n        }\n        return [JsoEncoder]::new('jsonpath', @{Path = $Path}, $jsonPathScript)\n    }\n\n    static [JsoEncoder] CreateTabular([string[]]$Columns = $null) {\n        $tabularScript = {\n            param($content, $options)\n            if ($content -is [array] -or $content -is [System.Collections.IEnumerable]) {\n                # Simple tabular conversion - collect all unique keys if no columns specified\n                $allItems = @($content)\n                if ($allItems.Count -eq 0) { return @() }\n\n                $columns = $options.Columns\n                if (-not $columns) {\n                    $uniqueKeys = [System.Collections.Generic.HashSet[string]]::new()\n                    foreach ($item in $allItems) {\n                        if ($item -is [pscustomobject] -or $item -is [hashtable]) {\n                            foreach ($prop in $item.PSObject.Properties) {\n                                [void]$uniqueKeys.Add($prop.Name)\n                            }\n                        }\n                    }\n                    $columns = @($uniqueKeys)\n                }\n\n                # Build table\n                $table = @()\n                foreach ($item in $allItems) {\n                    $row = [ordered]@{}\n                    foreach ($col in $columns) {\n                        $row[$col] = if ($item.PSObject.Properties[$col]) { $item.$col } else { $null }\n                    }\n                    $table += [pscustomobject]$row\n                }\n                return $table\n            }\n            return $content\n        }\n        $opts = @{}\n        if ($Columns) { $opts.Columns = $Columns }\n        return [JsoEncoder]::new('tabular', $opts, $tabularScript)\n    }\n}\n\n<#\n.SYNOPSIS\nJsoSharding - Generic file splitting logic.\n\n.DESCRIPTION\nManages file sharding state for splitting large files into smaller chunks.\nTracks shard indices, record counts, and generates shard file paths.\n#>\nclass JsoSharding {\n    [int]$RecordsPerFile\n    [string]$NamingPattern  # Generic: \"{0}_{1}{2}\" where {0}=basename, {1}=index, {2}=ext\n    [int]$CurrentIndex\n    [int]$CurrentRecordCount\n\n    JsoSharding([int]$RecordsPerFile, [string]$NamingPattern) {\n        $this.RecordsPerFile = $RecordsPerFile\n        $this.NamingPattern = $NamingPattern\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n\n    [string] GetShardPath([string]$BasePath) {\n        $directory = [System.IO.Path]::GetDirectoryName($BasePath)\n        $nameWithoutExt = [System.IO.Path]::GetFileNameWithoutExtension($BasePath)\n        $extension = [System.IO.Path]::GetExtension($BasePath)\n\n        $fileName = $this.NamingPattern -f $nameWithoutExt, $this.CurrentIndex, $extension\n        return Join-Path $directory $fileName\n    }\n\n    [bool] ShouldCreateNewShard() {\n        return $this.CurrentRecordCount -ge $this.RecordsPerFile\n    }\n\n    [void] NextShard() {\n        $this.CurrentIndex++\n        $this.CurrentRecordCount = 0\n    }\n\n    [void] RecordWritten() {\n        $this.CurrentRecordCount++\n    }\n\n    [void] Reset() {\n        $this.CurrentIndex = 1\n        $this.CurrentRecordCount = 0\n    }\n}\n\n<#\n.SYNOPSIS\nCreate a new JsoEncoder with the specified transformation.\n\n.DESCRIPTION\nFactory function for creating content encoders. Supported types:\n- Identity: No transformation\n- Minifier: Simple whitespace reduction\n- Canonical: Deterministic JSON serialization\n- JsonPath: Extract specific JSON paths (dot notation)\n- Tabular: Convert arrays to tabular format with column alignment\n- Custom: User-provided transformation scriptblock\n\n.PARAMETER Type\nThe type of encoder to create.\n\n.PARAMETER Options\nOptional configuration hashtable for the encoder.\n\n.PARAMETER CustomScript\nRequired for Custom type - scriptblock accepting ($content, $options).\n\n.PARAMETER JsonPath\nRequired for JsonPath type - dot-notation path (e.g., \"data.items.name\").\n\n.PARAMETER TableColumns\nOptional for Tabular type - array of column names to include.\n\n.EXAMPLE\n$encoder = New-JsoEncoder -Type Minifier\n$minified = $encoder.Encode(\"  lots   of   spaces  \")\n# Returns: \"lots of spaces\"\n\n.EXAMPLE\n$encoder = New-JsoEncoder -Type JsonPath -JsonPath \"user.email\"\n$email = $encoder.Encode($jsonObject)\n#>\nfunction New-JsoEncoder {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [ValidateSet('Identity', 'Minifier', 'Canonical', 'JsonPath', 'Tabular', 'Custom')]\n        [string]$Type,\n\n        [hashtable]$Options = @{},\n\n        [scriptblock]$CustomScript,\n\n        [string]$JsonPath,\n\n        [string[]]$TableColumns\n    )\n\n    switch ($Type) {\n        'Identity' { return [JsoEncoder]::CreateIdentity() }\n        'Minifier' { return [JsoEncoder]::CreateMinifier() }\n        'Canonical' { return [JsoEncoder]::CreateCanonical() }\n        'JsonPath' {\n            if (-not $JsonPath) {\n                throw \"JsonPath parameter is required for JsonPath encoder type\"\n            }\n            return [JsoEncoder]::CreateJsonPath($JsonPath)\n        }\n        'Tabular' {\n            return [JsoEncoder]::CreateTabular($TableColumns)\n        }\n        'Custom' {\n            if (-not $CustomScript) {\n                throw \"CustomScript is required for Custom encoder type\"\n            }\n            return [JsoEncoder]::new('custom', $Options, $CustomScript)\n        }\n    }\n}\n\n# ================================================================\n# SECTION 3: BLOOM FILTERS (~128 lines)\n# ================================================================\n\n<#\n.SYNOPSIS\nCreate new Bloom filter for probabilistic set membership testing.\n#>\nfunction New-BloomFilter {\n    [CmdletBinding()]\n    param(\n        [int]$ExpectedItems = 10000,\n        [double]$FalsePositiveRate = 0.01\n    )\n\n    # Calculate optimal bit array size\n    $m = [Math]::Ceiling(($ExpectedItems * [Math]::Log($FalsePositiveRate)) / [Math]::Log(1 / [Math]::Pow(2, [Math]::Log(2))))\n\n    # Calculate optimal number of hash functions\n    $k = [Math]::Ceiling(($m / $ExpectedItems) * [Math]::Log(2))\n\n    return [PSCustomObject]@{\n        PSTypeName = 'Jso.BloomFilter'\n        BitArray = [System.Collections.BitArray]::new([int]$m)\n        Size = [int]$m\n        HashCount = [int]$k\n        ItemCount = 0\n        ExpectedItems = $ExpectedItems\n        FalsePositiveRate = $FalsePositiveRate\n    }\n}\n\n<#\n.SYNOPSIS\nGet multiple hash values for Bloom filter (double hashing).\n#>\nfunction Get-BloomFilterHashes {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Item,\n\n        [Parameter(Mandatory)]\n        [int]$HashCount,\n\n        [Parameter(Mandatory)]\n        [int]$Size\n    )\n\n    $bytes = [Text.Encoding]::UTF8.GetBytes($Item)\n\n    # Primary hash (FNV-1a)\n    [long]$hash1 = 2166136261\n    foreach ($byte in $bytes) {\n        $hash1 = $hash1 -bxor $byte\n        $hash1 = ($hash1 * 16777619) % [Math]::Pow(2, 32)\n    }\n\n    # Secondary hash (djb2)\n    [long]$hash2 = 5381\n    foreach ($byte in $bytes) {\n        $hash2 = (($hash2 -shl 5) + $hash2) + $byte\n    }\n\n    # Generate k hashes using double hashing\n    $hashes = [List[int]]::new()\n    for ($i = 0; $i -lt $HashCount; $i++) {\n        $combined = ($hash1 + $i * $hash2) % $Size\n        if ($combined -lt 0) { $combined += $Size }\n        $hashes.Add([int]$combined)\n    }\n\n    return $hashes\n}\n\n<#\n.SYNOPSIS\nAdd item to Bloom filter.\n#>\nfunction Add-BloomFilterItem {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter,\n\n        [Parameter(Mandatory)]\n        [string]$Item\n    )\n\n    $hashes = Get-BloomFilterHashes -Item $Item -HashCount $BloomFilter.HashCount -Size $BloomFilter.Size\n\n    foreach ($hash in $hashes) {\n        $BloomFilter.BitArray[$hash] = $true\n    }\n\n    $BloomFilter.ItemCount++\n}\n\n<#\n.SYNOPSIS\nTest if item might be in Bloom filter (no false negatives, possible false positives).\n#>\nfunction Test-BloomFilterItem {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$BloomFilter,\n\n        [Parameter(Mandatory)]\n        [string]$Item\n    )\n\n    $hashes = Get-BloomFilterHashes -Item $Item -HashCount $BloomFilter.HashCount -Size $BloomFilter.Size\n\n    foreach ($hash in $hashes) {\n        if (-not $BloomFilter.BitArray[$hash]) {\n            return $false  # Definitely not in set\n        }\n    }\n\n    return $true  # Probably in set\n}\n\n# ================================================================\n# SECTION 4: JSONL STREAMING (~92 lines)\n# ================================================================\n\n<#\n.SYNOPSIS\nJSONL enumerator for constant-memory streaming.\n#>\nclass JsonlEnumerator : System.Collections.IEnumerable {\n    [string]$FilePath\n    [int]$Skip\n    [int]$Take\n\n    JsonlEnumerator([string]$filePath, [int]$skip, [int]$take) {\n        $this.FilePath = $filePath\n        $this.Skip = $skip\n        $this.Take = $take\n    }\n\n    [System.Collections.IEnumerator] GetEnumerator() {\n        return [JsonlEnumeratorImpl]::new($this.FilePath, $this.Skip, $this.Take)\n    }\n}\n\nclass JsonlEnumeratorImpl : System.Collections.IEnumerator {\n    [string]$FilePath\n    [System.IO.StreamReader]$Reader\n    [int]$Skip\n    [int]$Take\n    [int]$CurrentIndex\n    [int]$EmittedCount\n    [object]$Current\n\n    JsonlEnumeratorImpl([string]$filePath, [int]$skip, [int]$take) {\n        $this.FilePath = $filePath\n        $this.Skip = $skip\n        $this.Take = $take\n        $this.CurrentIndex = -1\n        $this.EmittedCount = 0\n        $this.Reader = [System.IO.StreamReader]::new($filePath, [Text.Encoding]::UTF8)\n    }\n\n    [bool] MoveNext() {\n        while ($true) {\n            $line = $this.Reader.ReadLine()\n            if ($null -eq $line) {\n                return $false  # EOF\n            }\n\n            $this.CurrentIndex++\n\n            # Skip blank lines\n            if ([string]::IsNullOrWhiteSpace($line)) {\n                continue\n            }\n\n            # Apply skip offset\n            if ($this.CurrentIndex -lt $this.Skip) {\n                continue\n            }\n\n            # Apply take limit\n            if ($this.Take -gt 0 -and $this.EmittedCount -ge $this.Take) {\n                return $false\n            }\n\n            # Parse JSON\n            try {\n                $this.Current = $line | ConvertFrom-Json -ErrorAction Stop\n                $this.EmittedCount++\n                return $true\n            }\n            catch {\n                Write-Warning \"Skipping invalid JSON at line $($this.CurrentIndex): $_\"\n                continue\n            }\n        }\n        # This should never be reached due to while($true), but satisfies parser\n        return $false\n    }\n\n    [void] Reset() {\n        $this.Reader.Close()\n        $this.Reader = [System.IO.StreamReader]::new($this.FilePath, [Text.Encoding]::UTF8)\n        $this.CurrentIndex = -1\n        $this.EmittedCount = 0\n        $this.Current = $null\n    }\n\n    [object] get_Current() {\n        return $this.Current\n    }\n\n    [void] Dispose() {\n        if ($this.Reader) {\n            $this.Reader.Close()\n        }\n    }\n}\n\n<#\n.SYNOPSIS\nGet JSONL enumerator for lazy streaming (constant memory).\n#>\nfunction Get-JsonlEnumerator {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath,\n\n        [int]$Skip = 0,\n\n        [int]$Take = 0\n    )\n\n    if (-not (Test-Path $FilePath)) {\n        throw \"JSONL file not found: $FilePath\"\n    }\n\n    return [JsonlEnumerator]::new($FilePath, $Skip, $Take)\n}\n\n# ================================================================\n# SECTION 5: BINARY INDEXING (~118 lines)\n# ================================================================\n\n<#\n.SYNOPSIS\nCreate binary index for JSONL file (byte offsets for O(1) access).\n#>\nfunction New-JsonlBinaryIndex {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JsonlPath,\n\n        [string]$IndexPath = $null\n    )\n\n    if (-not (Test-Path $JsonlPath)) {\n        throw \"JSONL file not found: $JsonlPath\"\n    }\n\n    if (-not $IndexPath) {\n        $IndexPath = \"$JsonlPath.idx\"\n    }\n\n    # Create index file\n    $stream = [FileStream]::new($IndexPath, [FileMode]::Create, [FileAccess]::Write, [FileShare]::None)\n    $writer = [BinaryWriter]::new($stream)\n\n    try {\n        # Write magic header: \"JSOI\" (JSON Index)\n        $writer.Write([byte[]]@(0x4A, 0x53, 0x4F, 0x49))\n\n        # Write version: 1\n        $writer.Write([int32]1)\n\n        # Write line count placeholder (will update at end)\n        $lineCountPos = $stream.Position\n        $writer.Write([int32]0)\n\n        # Read JSONL and write offsets\n        $reader = [StreamReader]::new($JsonlPath, [Text.Encoding]::UTF8)\n        $lineCount = 0\n        $byteOffset = 0\n\n        while ($null -ne ($line = $reader.ReadLine())) {\n            if (-not [string]::IsNullOrWhiteSpace($line)) {\n                $writer.Write([int64]$byteOffset)\n                $lineCount++\n            }\n\n            # Calculate byte offset (UTF-8 encoding + newline)\n            $byteOffset += [Text.Encoding]::UTF8.GetByteCount($line) + 1\n        }\n\n        $reader.Close()\n\n        # Update line count in header\n        $stream.Seek($lineCountPos, [SeekOrigin]::Begin) | Out-Null\n        $writer.Write([int32]$lineCount)\n\n        Write-Verbose \"Created index with $lineCount entries at $IndexPath\"\n\n        return @{\n            IndexPath = $IndexPath\n            LineCount = $lineCount\n        }\n    }\n    finally {\n        $writer.Close()\n        $stream.Close()\n    }\n}\n\n<#\n.SYNOPSIS\nRead JSONL line by index using binary index (O(1) access).\n#>\nfunction Read-JsonlBinaryIndex {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JsonlPath,\n\n        [Parameter(Mandatory)]\n        [int]$LineIndex,\n\n        [string]$IndexPath = $null\n    )\n\n    if (-not $IndexPath) {\n        $IndexPath = \"$JsonlPath.idx\"\n    }\n\n    if (-not (Test-Path $IndexPath)) {\n        throw \"Index not found: $IndexPath. Run New-JsonlBinaryIndex first.\"\n    }\n\n    # Read index file\n    $indexStream = [FileStream]::new($IndexPath, [FileMode]::Open, [FileAccess]::Read, [FileShare]::Read)\n    $indexReader = [BinaryReader]::new($indexStream)\n\n    try {\n        # Verify magic header\n        $magic = $indexReader.ReadBytes(4)\n        if ($magic[0] -ne 0x4A -or $magic[1] -ne 0x53 -or $magic[2] -ne 0x4F -or $magic[3] -ne 0x49) {\n            throw \"Invalid index file format\"\n        }\n\n        # Read version\n        $version = $indexReader.ReadInt32()\n        if ($version -ne 1) {\n            throw \"Unsupported index version: $version\"\n        }\n\n        # Read line count\n        $lineCount = $indexReader.ReadInt32()\n\n        if ($LineIndex -lt 0 -or $LineIndex -ge $lineCount) {\n            throw \"Line index out of range: $LineIndex (valid: 0-$($lineCount-1))\"\n        }\n\n        # Seek to offset position (header: 12 bytes, each offset: 8 bytes)\n        $offsetPos = 12 + ($LineIndex * 8)\n        $indexStream.Seek($offsetPos, [SeekOrigin]::Begin) | Out-Null\n\n        # Read byte offset\n        $byteOffset = $indexReader.ReadInt64()\n\n        # Read line from JSONL file\n        $jsonlStream = [FileStream]::new($JsonlPath, [FileMode]::Open, [FileAccess]::Read, [FileShare]::Read)\n        $jsonlReader = [StreamReader]::new($jsonlStream, [Text.Encoding]::UTF8)\n\n        try {\n            $jsonlStream.Seek($byteOffset, [SeekOrigin]::Begin) | Out-Null\n            $line = $jsonlReader.ReadLine()\n\n            return $line | ConvertFrom-Json\n        }\n        finally {\n            $jsonlReader.Close()\n            $jsonlStream.Close()\n        }\n    }\n    finally {\n        $indexReader.Close()\n        $indexStream.Close()\n    }\n}\n\n# ================================================================\n# SECTION 6: JSON UTILITIES (~112 lines)\n# ================================================================\n\n<#\n.SYNOPSIS\nConvert to canonical JSON (deterministic serialization).\n#>\nfunction ConvertTo-CanonicalJson {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [object]$InputObject,\n\n        [int]$Depth = 100,\n\n        [switch]$Compress\n    )\n\n    process {\n        if ($Compress) {\n            return $InputObject | ConvertTo-Json -Depth $Depth -Compress\n        }\n        else {\n            return $InputObject | ConvertTo-Json -Depth $Depth\n        }\n    }\n}\n\n<#\n.SYNOPSIS\nGet SHA256 hash of file content.\n#>\nfunction Get-FileSha256 {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath\n    )\n\n    if (-not (Test-Path $FilePath)) {\n        throw \"File not found: $FilePath\"\n    }\n\n    $stream = [FileStream]::new($FilePath, [FileMode]::Open, [FileAccess]::Read, [FileShare]::Read)\n    try {\n        $sha256 = [Security.Cryptography.SHA256]::Create()\n        $hashBytes = $sha256.ComputeHash($stream)\n        return [BitConverter]::ToString($hashBytes).Replace('-', '').ToLowerInvariant()\n    }\n    finally {\n        $stream.Close()\n    }\n}\n\n# Move this somewhere appropriate during update\nfunction Get-FileSha256 {\n    [CmdletBinding()]\n    param([Parameter(Mandatory)] [string] $Path)\n\n    if (-not (Test-Path -Path $Path)) { throw \"File not found: $Path\" }\n\n    $fs = [IO.File]::OpenRead($Path)\n    try {\n        # It just delegates to the primitive\n        return [SHA256]::Compute($fs)\n    }\n    finally {\n        $fs.Dispose()\n    }\n}\n\nfunction Get-FileContentHashSafe {\n    <#\n    .SYNOPSIS\n    Safely computes MD5 hash of file content.\n    #>\n    [CmdletBinding()]\n    param([string]$FilePath)\n\n    if (-not (Test-Path $FilePath)) { return \"\" }\n\n    try {\n        $content = Get-Content $FilePath -Raw -ErrorAction SilentlyContinue\n        if (-not $content) { return \"\" }\n\n        $bytes = [System.Text.Encoding]::UTF8.GetBytes($content)\n        $hash = [System.Security.Cryptography.MD5]::Create().ComputeHash($bytes)\n        return [System.BitConverter]::ToString($hash).Replace(\"-\", \"\")\n    } catch {\n        return \"\"\n    }\n}\n\n<#\n.SYNOPSIS\nMerge JSON objects (shallow merge, right precedence).\n#>\nfunction Merge-JsonObjects {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$Left,\n\n        [Parameter(Mandatory)]\n        [PSCustomObject]$Right\n    )\n\n    $merged = [PSCustomObject]@{}\n\n    # Copy left properties\n    foreach ($prop in $Left.PSObject.Properties) {\n        $merged | Add-Member -MemberType NoteProperty -Name $prop.Name -Value $prop.Value\n    }\n\n    # Overwrite with right properties\n    foreach ($prop in $Right.PSObject.Properties) {\n        if ($merged.PSObject.Properties[$prop.Name]) {\n            $merged.PSObject.Properties[$prop.Name].Value = $prop.Value\n        }\n        else {\n            $merged | Add-Member -MemberType NoteProperty -Name $prop.Name -Value $prop.Value\n        }\n    }\n\n    return $merged\n}\n\n<#\n.SYNOPSIS\nGet maximum depth of JSON structure.\n#>\nfunction Get-JsonDepth {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [object]$InputObject\n    )\n\n    function Get-Depth {\n        param($obj, $currentDepth = 0)\n\n        if ($obj -is [PSCustomObject] -or $obj -is [hashtable]) {\n            $maxChildDepth = $currentDepth\n            foreach ($prop in $obj.PSObject.Properties) {\n                $childDepth = Get-Depth -obj $prop.Value -currentDepth ($currentDepth + 1)\n                if ($childDepth -gt $maxChildDepth) {\n                    $maxChildDepth = $childDepth\n                }\n            }\n            return $maxChildDepth\n        }\n        elseif ($obj -is [array] -or $obj -is [System.Collections.IEnumerable]) {\n            $maxChildDepth = $currentDepth\n            foreach ($item in $obj) {\n                $childDepth = Get-Depth -obj $item -currentDepth ($currentDepth + 1)\n                if ($childDepth -gt $maxChildDepth) {\n                    $maxChildDepth = $childDepth\n                }\n            }\n            return $maxChildDepth\n        }\n        else {\n            return $currentDepth\n        }\n    }\n\n    return Get-Depth -obj $InputObject\n}\n\n# ================================================================\n# EXPORTS\n# ================================================================\n\nExport-ModuleMember -Function @(\n    # Hash Primitives\n    'Resolve-CanonicalPath',\n\n    # Hash Applications (built on hashing-primitives)\n    # Refactored in Phase 1 to use primitives from hashing-primitives\n    'Get-PathHash',          # Uses Get-Hash primitive\n    'Get-ContentHash',       # Uses RabinKarpHash (different algorithm)\n    'Get-ContentChunks',     # Uses Get-ContentBoundaryOffsets primitive\n    'Find-StringPattern',    # Moved from hashing-primitives, uses Polynomial/RollingWindow\n\n    # JSO-Scripts Orchestration\n    'New-JsoEncoder',\n\n    # Bloom Filters\n    'New-BloomFilter',\n    'Add-BloomFilterItem',\n    'Test-BloomFilterItem',\n    'Get-BloomFilterHashes',\n\n    # JSONL Streaming\n    'Get-JsonlEnumerator',\n\n    # Binary Indexing\n    'New-JsonlBinaryIndex',\n    'Read-JsonlBinaryIndex',\n\n    # JSON Utilities\n    'ConvertTo-CanonicalJson',\n    'Get-FileSha256',\n    'Merge-JsonObjects',\n    'Get-JsonDepth'\n) |
