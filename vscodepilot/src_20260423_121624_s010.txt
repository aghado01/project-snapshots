idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
19 | typescript/parallel-tools.ts | {28993 3379 0.1959 5.0355} | 30329 | /**\n * Parallel Processing Tools for VsCodePilot Extension\n *\n * @module parallel-tools\n *\n * @description\n * Provides user-friendly TypeScript API for async parallel PowerShell jobs\n * with cancellation support, signal file detection, and result management.\n *\n * @architecture\n * ```\n * TypeScript API → PowerShell CLI → ParForPowerShellCP Module → RunspacePool\n * ```\n *\n * @features\n * - **Async Job Management**: Non-blocking parallel execution with job tracking\n * - **Cancellation Support**: Graceful job cancellation via signal files\n * - **Dual Detection**: Signal files + file stability fallback\n * - **Type Safety**: Generic results with full TypeScript typing\n * - **Auto Cleanup**: Temp file management and resource disposal\n * - **Progress Tracking**: Optional callbacks for status updates\n *\n * @signalFiles\n * Each job creates three temp files in `%TEMP%\\vscode-copilot-jobs\\`:\n * - `{jobId}.signal` - Completion signal (status: completed|failed|cancelled)\n * - `{jobId}.jsonl` - Results file (one JSON object per line)\n * - `{jobId}_cancel.signal` - Cancellation request (created by cancelJob())\n *\n * @workflow\n * 1. **Start Job**: `startParallelJob(operation)` returns jobId immediately\n * 2. **Poll Status**: `checkJobStatus(jobId)` checks signal files\n * 3. **Get Results**: `getJobResults(jobId)` reads JSONL and cleans up\n * 4. **Cancel**: `cancelJob(jobId)` writes cancel signal for graceful stop\n *\n * @example Basic Usage\n * ```typescript\n * // Start parallel job\n * const jobId = await startParallelJob(`\n *   $files = Get-ChildItem -Recurse\n *   parforeach $files { param($f) Get-FileHash $f.FullName }\n * `);\n *\n * // Poll until complete\n * const status = await pollJobUntilComplete(jobId);\n *\n * // Get results\n * if (status === 'completed') {\n *   const results = await getJobResults(jobId);\n *   console.log(`Processed ${results.length} files`);\n * }\n * ```\n *\n * @example Simple API (for Copilot)\n * ```typescript\n * // Parallel foreach\n * const jobId = await parforeach(\n *   'Get-ChildItem -Recurse',\n *   '{ param($f) Get-FileHash $f.FullName }',\n *   { throttleLimit: 4 }\n * );\n * ```\n *\n * @example Cancellation\n * ```typescript\n * const jobId = await startParallelJob(longRunningOperation);\n *\n * // User clicks cancel button\n * await cancelJob(jobId); // PowerShell detects and stops gracefully\n *\n * // Extension deactivation cancels all\n * context.subscriptions.push({\n *   dispose: () => cancelAllJobs()\n * });\n * ```\n *\n * @performance\n * - Scales with CPU count (default throttle = logical processors)\n * - Typical speedup: 3-4x on 4-core systems for I/O-bound work\n * - File existence checks: ~0.1ms per check (minimal overhead)\n *\n * @see {@link ./parallel-tools.ts} for implementation\n * @see {@link ../lib/powershell/ParForPowerShellCP.psm1} for PowerShell module\n * @see {@link ../lib/powershell/USAGE_EXAMPLES.md} for comprehensive examples\n */\n\nimport * as fs from \"fs\";\nimport * as os from \"os\";\nimport * as path from \"path\";\nimport * as vscode from \"vscode\";\nimport { invokeCybernetics } from \"./cybernetics-bridge\";\nimport { supervisorBridge } from \"./supervisor-bridge\";\n\n// ============================================================\n// TYPE DEFINITIONS\n// ============================================================\n\n/**\n * Status of a parallel job\n */\nexport type JobStatus =\n  | \"pending\"\n  | \"running\"\n  | \"completed\"\n  | \"failed\"\n  | \"cancelled\";\n\n/**\n * A parallel job tracked by the extension\n */\nexport interface ParallelJob {\n  /** Unique job identifier */\n  jobId: string;\n\n  /** Current job status */\n  status: JobStatus;\n\n  /** When the job was created */\n  startTime: Date;\n\n  /** When the job finished (if completed/failed/cancelled) */\n  endTime?: Date;\n\n  /** Path to completion signal file */\n  signalPath: string;\n\n  /** Path to results JSONL file */\n  resultPath: string;\n\n  /** Path to cancellation signal file */\n  cancelSignalPath: string;\n\n  /** The PowerShell operation being executed */\n  operation: string;\n\n  /** Number of items processed (from signal file) */\n  itemCount?: number;\n\n  /** Error message (if failed) */\n  error?: string;\n}\n\n/**\n * Signal file content written by PowerShell\n */\ninterface JobSignal {\n  jobId: string;\n  status: \"completed\" | \"failed\" | \"cancelled\";\n  timestamp: string;\n  resultPath?: string;\n  itemCount?: number;\n  message?: string;\n  error?: string;\n}\n\n/**\n * Options for starting a parallel job\n */\nexport interface ParallelJobOptions {\n  /** Throttle limit (max parallel threads) */\n  throttleLimit?: number;\n\n  /** Timeout in milliseconds (default: 5 minutes) */\n  timeout?: number;\n\n  /** Whether to show progress notification */\n  showProgress?: boolean;\n\n  /** Custom temp directory for job files */\n  tempDir?: string;\n\n  /** Whether to run job in visible terminal (default: false, headless) */\n  visible?: boolean;\n\n  /** Custom terminal name (only used when visible: true) */\n  terminalName?: string;\n}\n\ninterface CyberneticJobPaths {\n  jobId: string;\n  jobRoot: string;\n  signalPath: string;\n  resultPath: string;\n  cancelSignalPath: string;\n  storePath?: string;\n}\n\ninterface CyberneticJobStatusInfo {\n  jobId: string;\n  status: JobStatus;\n  timestamp?: string;\n  resultPath?: string;\n  itemCount?: number;\n  error?: string;\n}\n\n// ============================================================\n// MODULE STATE\n// ============================================================\n\n/** Active jobs tracked by the extension */\nconst activeJobs = new Map<string, ParallelJob>();\n\n/** Default temp directory for job files */\nconst DEFAULT_TEMP_DIR = path.join(os.tmpdir(), \"vscode-copilot-jobs\");\n\n/** Polling interval in milliseconds */\nconst POLL_INTERVAL_MS = 2000;\n\n/** Default timeout in milliseconds (5 minutes) */\nconst DEFAULT_TIMEOUT_MS = 5 * 60 * 1000;\n\n/** Path to PowerShell module */\nlet powerShellModulePath: string;\n\nfunction isJobStatus(value: unknown): value is JobStatus {\n  return (\n    value === \"pending\" ||\n    value === \"running\" ||\n    value === \"completed\" ||\n    value === \"failed\" ||\n    value === \"cancelled\"\n  );\n}\n\n// ============================================================\n// INITIALIZATION\n// ============================================================\n\n/**\n * Initialize the parallel tools system.\n * Must be called during extension activation.\n *\n * @param extensionPath - The extension's installation path\n */\nexport async function initialize(extensionPath: string): Promise<void> {\n  powerShellModulePath = path.join(\n    extensionPath,\n    \"lib\",\n    \"powershell\",\n    \"ParForPowerShellCP.psm1\"\n  );\n\n  // Ensure temp directory exists\n  if (!fs.existsSync(DEFAULT_TEMP_DIR)) {\n    fs.mkdirSync(DEFAULT_TEMP_DIR, { recursive: true });\n  }\n\n  // Initialize supervisor\n  const workspacePath =\n    vscode.workspace.workspaceFolders?.[0]?.uri.fsPath || \"\";\n\n  try {\n    await supervisorBridge.initialize(extensionPath, workspacePath);\n    console.log(\n      `[ParallelTools] Initialized with supervisor and module: ${powerShellModulePath}`\n    );\n  } catch (error) {\n    console.error(\"[ParallelTools] Failed to initialize supervisor:\", error);\n    throw error;\n  }\n}\n\n/**\n * Get the temp directory path for job files.\n * Creates it if it doesn't exist.\n */\nfunction getTempDir(customPath?: string): string {\n  const dir = customPath || DEFAULT_TEMP_DIR;\n\n  if (!fs.existsSync(dir)) {\n    fs.mkdirSync(dir, { recursive: true });\n  }\n\n  return dir;\n}\n\n/**\n * Generate a unique job ID\n */\nfunction generateJobId(): string {\n  const timestamp = Date.now();\n  const random = Math.random().toString(36).substring(2, 11);\n  return `job_${timestamp}_${random}`;\n}\n\n// ============================================================\n// CORE API: JOB MANAGEMENT\n// ============================================================\n\n/**\n * Start a parallel PowerShell job asynchronously.\n * Returns immediately with a job ID for tracking.\n *\n * @param operation - PowerShell parallel operation (using parfor/parforeach/etc)\n * @param options - Job configuration options\n * @returns Job ID for tracking and cancellation\n *\n * @example\n * ```typescript\n * const jobId = await startParallelJob(`\n *   $files = Get-ChildItem -Path . -Recurse -File\n *   parforeach $files { param($f) Get-FileHash $f.FullName }\n * `);\n * ```\n */\nexport async function startParallelJob(\n  operation: string,\n  options: ParallelJobOptions = {}\n): Promise<string> {\n  const jobId = generateJobId();\n  const tempDir = getTempDir(options.tempDir);\n  const jobPaths = resolveCyberneticJobPaths(jobId, tempDir);\n\n  // Create job tracking object\n  const job: ParallelJob = {\n    jobId,\n    status: \"pending\",\n    startTime: new Date(),\n    signalPath: jobPaths.signalPath,\n    resultPath: jobPaths.resultPath,\n    cancelSignalPath: jobPaths.cancelSignalPath,\n    operation,\n  };\n\n  activeJobs.set(jobId, job);\n\n  // Build PowerShell script for the job\n  const throttleLimit = options.throttleLimit || \"default\";\n  const psScript = buildPowerShellCommand(\n    jobId,\n    operation,\n    job.signalPath,\n    job.resultPath,\n    job.cancelSignalPath,\n    throttleLimit\n  );\n\n  // Execute via supervisor (non-blocking)\n  await executeShellAsync(jobId, psScript, options);\n\n  job.status = \"running\";\n\n  // Show progress if requested\n  if (options.showProgress) {\n    vscode.window.showInformationMessage(`⚡ Parallel job ${jobId} started`);\n  }\n\n  console.log(`[ParallelTools] Started job ${jobId}`);\n\n  return jobId;\n}\n\n/**\n * Check the status of a running job.\n * Uses dual detection: signal file + result file stability.\n *\n * @param jobId - The job to check\n * @returns Current job status or null if not found\n *\n * @example\n * ```typescript\n * const status = await checkJobStatus(jobId);\n * if (status === 'completed') {\n *   const results = await getJobResults(jobId);\n * }\n * ```\n */\nexport async function checkJobStatus(jobId: string): Promise<JobStatus | null> {\n  const job = activeJobs.get(jobId);\n\n  if (!job) {\n    return null;\n  }\n\n  // If already in terminal state, return cached status\n  if ([\"completed\", \"failed\", \"cancelled\"].includes(job.status)) {\n    return job.status;\n  }\n\n  const cyberneticStatus = getCyberneticJobStatus(job);\n  if (cyberneticStatus) {\n    job.status = cyberneticStatus.status;\n    job.itemCount = cyberneticStatus.itemCount;\n    job.error = cyberneticStatus.error;\n\n    if (cyberneticStatus.resultPath) {\n      job.resultPath = cyberneticStatus.resultPath;\n    }\n\n    if (\n      [\"completed\", \"failed\", \"cancelled\"].includes(cyberneticStatus.status)\n    ) {\n      job.endTime = cyberneticStatus.timestamp\n        ? new Date(cyberneticStatus.timestamp)\n        : new Date();\n    }\n\n    return cyberneticStatus.status;\n  }\n\n  // Check for user-requested cancellation\n  if (fs.existsSync(job.cancelSignalPath)) {\n    job.status = \"cancelled\";\n    job.endTime = new Date();\n    return \"cancelled\";\n  }\n\n  // Check for completion signal file\n  if (fs.existsSync(job.signalPath)) {\n    try {\n      const signalContent = fs.readFileSync(job.signalPath, \"utf-8\");\n      const signal: JobSignal = JSON.parse(signalContent);\n\n      // Update job with signal data\n      job.status = signal.status as JobStatus;\n      job.endTime = new Date();\n      job.itemCount = signal.itemCount;\n      job.error = signal.error;\n\n      console.log(\n        `[ParallelTools] Job ${jobId} ${signal.status} (${\n          signal.itemCount || 0\n        } items)`\n      );\n\n      return job.status;\n    } catch (error) {\n      // Signal file exists but is incomplete/corrupted, continue polling\n      console.warn(\n        `[ParallelTools] Could not parse signal for ${jobId}:`,\n        error\n      );\n    }\n  }\n\n  // Fallback: Check result file stability\n  if (fs.existsSync(job.resultPath)) {\n    const stats = fs.statSync(job.resultPath);\n    const ageMs = Date.now() - stats.mtimeMs;\n\n    // If file hasn't been modified in 3 seconds, consider job done\n    if (ageMs > 5000) {\n      job.status = \"completed\";\n      job.endTime = new Date();\n      console.log(\n        `[ParallelTools] Job ${jobId} completed (detected via file stability)`\n      );\n      return \"completed\";\n    }\n  }\n\n  // Check for timeout (use default timeout for now)\n  const elapsedMs = Date.now() - job.startTime.getTime();\n\n  if (elapsedMs > DEFAULT_TIMEOUT_MS) {\n    job.status = \"failed\";\n    job.endTime = new Date();\n    job.error = `Job timed out after ${DEFAULT_TIMEOUT_MS}ms`;\n    console.warn(`[ParallelTools] Job ${jobId} timed out`);\n    return \"failed\";\n  }\n\n  return \"running\";\n}\n\n/**\n * Poll a job until completion with progress updates.\n *\n * @param jobId - The job to poll\n * @param onProgress - Optional callback for progress updates\n * @returns Final job status\n *\n * @example\n * ```typescript\n * const status = await pollJobUntilComplete(jobId, (status) => {\n *   console.log(`Job status: ${status}`);\n * });\n * ```\n */\nexport async function pollJobUntilComplete(\n  jobId: string,\n  onProgress?: (status: JobStatus) => void\n): Promise<JobStatus> {\n  let status = await checkJobStatus(jobId);\n\n  while (status && [\"pending\", \"running\"].includes(status)) {\n    if (onProgress) {\n      onProgress(status);\n    }\n\n    // Wait before next poll\n    await new Promise((resolve) => setTimeout(resolve, POLL_INTERVAL_MS));\n\n    status = await checkJobStatus(jobId);\n  }\n\n  return status || \"failed\";\n}\n\n/**\n * Get results from a completed job.\n * Reads JSONL file and cleans up temporary files.\n *\n * @param jobId - The job to retrieve results from\n * @param keepFiles - Whether to keep temp files (default: false)\n * @returns Array of result objects or null if not ready\n *\n * @example\n * ```typescript\n * const results = await getJobResults(jobId);\n * results.forEach(item => console.log(item));\n * ```\n */\nexport async function getJobResults<T = any>(\n  jobId: string,\n  keepFiles: boolean = false\n): Promise<T[] | null> {\n  const job = activeJobs.get(jobId);\n\n  if (!job) {\n    console.warn(`[ParallelTools] Job ${jobId} not found`);\n    return null;\n  }\n\n  if (!fs.existsSync(job.resultPath)) {\n    console.warn(`[ParallelTools] Result file not found for ${jobId}`);\n    return null;\n  }\n\n  try {\n    const cyberneticResults = readCyberneticJobResults<T>(job.resultPath);\n    if (cyberneticResults) {\n      console.log(\n        `[ParallelTools] Retrieved ${cyberneticResults.length} results for ${jobId}`\n      );\n\n      if (!keepFiles) {\n        cleanupJobFiles(job);\n        activeJobs.delete(jobId);\n      }\n\n      return cyberneticResults;\n    }\n\n    // Read JSONL file (one JSON object per line)\n    const content = fs.readFileSync(job.resultPath, \"utf-8\");\n    const results = content\n      .split(\"\\n\")\n      .filter((line) => line.trim())\n      .map((line) => JSON.parse(line) as T);\n\n    console.log(\n      `[ParallelTools] Retrieved ${results.length} results for ${jobId}`\n    );\n\n    // Cleanup files unless requested to keep\n    if (!keepFiles) {\n      cleanupJobFiles(job);\n      activeJobs.delete(jobId);\n    }\n\n    return results;\n  } catch (error) {\n    console.error(\n      `[ParallelTools] Failed to read results for ${jobId}:`,\n      error\n    );\n    job.status = \"failed\";\n    job.error = `Failed to parse results: ${error}`;\n    return null;\n  }\n}\n\n/**\n * Cancel a running job.\n * Creates cancellation signal file that PowerShell will detect.\n *\n * @param jobId - The job to cancel\n * @returns true if cancellation signal sent, false otherwise\n *\n * @example\n * ```typescript\n * const cancelled = await cancelJob(jobId);\n * if (cancelled) {\n *   console.log('Job cancellation requested');\n * }\n * ```\n */\nexport async function cancelJob(jobId: string): Promise<boolean> {\n  const job = activeJobs.get(jobId);\n\n  if (!job) {\n    console.warn(`[ParallelTools] Cannot cancel ${jobId}: job not found`);\n    return false;\n  }\n\n  if (job.status !== \"running\" && job.status !== \"pending\") {\n    console.warn(\n      `[ParallelTools] Cannot cancel ${jobId}: job is ${job.status}`\n    );\n    return false;\n  }\n\n  try {\n    if (requestCyberneticJobCancellation(job)) {\n      job.status = \"cancelled\";\n      console.log(`[ParallelTools] Cancellation signal sent for ${jobId}`);\n      return true;\n    }\n\n    // Create cancellation signal file\n    const cancelSignal = {\n      jobId: jobId,\n      cancelledAt: new Date().toISOString(),\n      reason: \"user_requested\",\n    };\n\n    fs.writeFileSync(\n      job.cancelSignalPath,\n      JSON.stringify(cancelSignal, null, 2),\n      \"utf-8\"\n    );\n\n    job.status = \"cancelled\";\n    console.log(`[ParallelTools] Cancellation signal sent for ${jobId}`);\n\n    return true;\n  } catch (error) {\n    console.error(`[ParallelTools] Failed to cancel ${jobId}:`, error);\n    return false;\n  }\n}\n\n/**\n * Cancel all running jobs.\n * Useful for cleanup during extension deactivation.\n *\n * @returns Number of jobs cancelled\n */\nexport function cancelAllJobs(): number {\n  const runningJobs = Array.from(activeJobs.values()).filter(\n    (j) => j.status === \"running\" || j.status === \"pending\"\n  );\n\n  console.log(`[ParallelTools] Cancelling ${runningJobs.length} running jobs`);\n\n  let cancelled = 0;\n  for (const job of runningJobs) {\n    cancelJob(job.jobId).then((success) => {\n      if (success) cancelled++;\n    });\n  }\n\n  return cancelled;\n}\n\n/**\n * Get information about a job.\n *\n * @param jobId - The job to query\n * @returns Job object or null if not found\n */\nexport function getJobInfo(jobId: string): ParallelJob | null {\n  return activeJobs.get(jobId) || null;\n}\n\n/**\n * Get all active jobs.\n *\n * @returns Array of all tracked jobs\n */\nexport function getAllJobs(): ParallelJob[] {\n  return Array.from(activeJobs.values());\n}\n\n/**\n * Get jobs by status.\n *\n * @param status - Filter by this status\n * @returns Array of matching jobs\n */\nexport function getJobsByStatus(status: JobStatus): ParallelJob[] {\n  return Array.from(activeJobs.values()).filter((j) => j.status === status);\n}\n\n// ============================================================\n// SIMPLE USER-FACING API (for Copilot)\n// ============================================================\n\n/**\n * Execute a parallel for loop.\n *\n * @param count - Number of iterations\n * @param scriptBlock - PowerShell scriptblock to execute per iteration\n * @param options - Optional job configuration\n * @returns Job ID\n *\n * @example\n * ```typescript\n * const jobId = await parfor(100, '{ param($i) \"Iteration $i\" }');\n * ```\n */\nexport async function parfor(\n  count: number,\n  scriptBlock: string,\n  options: ParallelJobOptions = {}\n): Promise<string> {\n  const throttle = options.throttleLimit\n    ? `-ThrottleLimit ${options.throttleLimit}`\n    : \"\";\n  const script = `\n    Import-Module '${getModulePath(\"ParForPowerShellCP.psm1\")}'\n    parfor ${count} { ${scriptBlock} } ${throttle}\n  `.trim();\n  return executeParallelScript(script, options);\n}\n\n/**\n * Execute a parallel foreach operation.\n * Simple wrapper that Copilot can easily use.\n *\n * @param items - Array of items to process in parallel\n * @param scriptBlock - PowerShell scriptblock to execute per item\n * @param options - Optional job configuration\n * @returns Job ID\n *\n * @example\n * ```typescript\n * const files = ['file1.txt', 'file2.txt', 'file3.txt'];\n * const jobId = await parforeach(\n *   files,\n *   '{ param($f) Get-Content $f }'\n * );\n * ```\n */\nexport async function parrange(\n  range: number[] | [number, number],\n  scriptBlock: string,\n  options: ParallelJobOptions = {}\n): Promise<string> {\n  let rangeExpr: string;\n  if (Array.isArray(range) && range.length === 2) {\n    rangeExpr = `(${range[0]}..${range[1]})`;\n  } else {\n    rangeExpr = `@(${range.join(\",\")})`;\n  }\n  const throttle = options.throttleLimit\n    ? `-ThrottleLimit ${options.throttleLimit}`\n    : \"\";\n  const script = `\n    Import-Module '${getModulePath(\"ParForPowerShellCP.psm1\")}'\n    parrange ${rangeExpr} { ${scriptBlock} } ${throttle}\n  `.trim();\n  return executeParallelScript(script, options);\n}\n\n/**\n * Execute a parallel range operation.\n *\n * @param range - Numeric range as [start, end] tuple or array of numbers\n * @param scriptBlock - PowerShell scriptblock to execute per item\n * @param options - Optional job configuration\n * @returns Job ID\n *\n * @example\n * ```typescript\n * // Range as tuple\n * const jobId = await parrange([1, 1000], '{ param($n) $n * $n }');\n *\n * // Range as array\n * const jobId = await parrange([1, 5, 10, 15, 20], '{ param($n) $n * 2 }');\n * ```\n */\nexport async function parrange(\n  range: number[] | [number, number],\n  scriptBlock: string,\n  options: ParallelJobOptions = {}\n): Promise<string> {\n  let rangeExpr: string;\n  if (Array.isArray(range) && range.length === 2) {\n    rangeExpr = `(${range[0]}..${range[1]})`;\n  } else {\n    rangeExpr = `@(${range.join(\",\")})`;\n  }\n  const throttle = options.throttleLimit\n    ? `-ThrottleLimit ${options.throttleLimit}`\n    : \"\";\n  const script = `\n    Import-Module '${getModulePath(\"ParForPowerShellCP.psm1\")}'\n    parrange ${rangeExpr} { ${scriptBlock} } ${throttle}\n  `.trim();\n  return executeParallelScript(script, options);\n}\n\n/**\n * Execute a parallel while loop.\n * Continues while condition evaluates to true.\n *\n * @param condition - PowerShell condition scriptblock\n * @param scriptBlock - PowerShell scriptblock to execute per iteration\n * @param options - Optional job configuration\n * @returns Job ID\n *\n * @example\n * ```typescript\n * const jobId = await parwhile(\n *   '{ $script:count -lt 100 }',\n *   '{ $script:count++; \"Item $count\" }'\n * );\n * ```\n */\nexport async function parwhile(\n  condition: string,\n  scriptBlock: string,\n  options: ParallelJobOptions = {}\n): Promise<string> {\n  const throttle = options.throttleLimit\n    ? `-ThrottleLimit ${options.throttleLimit}`\n    : \"\";\n  const script = `\n    Import-Module '${getModulePath(\"ParForPowerShellCP.psm1\")}'\n    parwhile { ${condition} } { ${scriptBlock} } ${throttle}\n  `.trim();\n  return executeParallelScript(script, options);\n}\n\n/**\n * Execute a parallel until loop.\n * Continues until condition evaluates to true.\n *\n * @param condition - PowerShell condition scriptblock\n * @param scriptBlock - PowerShell scriptblock to execute per iteration\n * @param options - Optional job configuration\n * @returns Job ID\n *\n * @example\n * ```typescript\n * const jobId = await paruntil(\n *   '{ $script:done }',\n *   '{ Test-Condition; if ($result) { $script:done = $true } }'\n * );\n * ```\n */\nexport async function paruntil(\n  condition: string,\n  scriptBlock: string,\n  options: ParallelJobOptions = {}\n): Promise<string> {\n  const throttle = options.throttleLimit\n    ? `-ThrottleLimit ${options.throttleLimit}`\n    : \"\";\n  const script = `\n    Import-Module '${getModulePath(\"ParForPowerShellCP.psm1\")}'\n    paruntil { ${condition} } { ${scriptBlock} } ${throttle}\n  `.trim();\n  return executeParallelScript(script, options);\n}\n// ============================================================\n// HELPER FUNCTIONS\n// ============================================================\n\n/**\n * Build the PowerShell command to execute a parallel job.\n */\nfunction buildPowerShellCommand(\n  jobId: string,\n  operation: string,\n  signalPath: string,\n  resultPath: string,\n  cancelSignalPath: string,\n  throttleLimit: number | string\n): string {\n  // Escape paths for PowerShell\n  const escapedSignal = signalPath.replace(/\\\\/g, \"\\\\\\\\\");\n  const escapedResult = resultPath.replace(/\\\\/g, \"\\\\\\\\\");\n  const escapedCancel = cancelSignalPath.replace(/\\\\/g, \"\\\\\\\\\");\n  const escapedModule = powerShellModulePath.replace(/\\\\/g, \"\\\\\\\\\");\n\n  return `\nImport-Module \"${escapedModule}\" -Force\n\nInvoke-ParallelJobWithSignal \\`\n    -JobId \"${jobId}\" \\`\n    -SignalPath \"${escapedSignal}\" \\`\n    -ResultPath \"${escapedResult}\" \\`\n    -CancelSignalPath \"${escapedCancel}\" \\`\n    -ScriptBlock {\n        ${operation}\n    }\n`.trim();\n}\n\n/**\n * Execute PowerShell command asynchronously via supervisor.\n * Non-blocking dispatch to supervisor shell.\n */\nasync function executeShellAsync(\n  jobId: string,\n  script: string,\n  options: ParallelJobOptions = {}\n): Promise<void> {\n  console.log(`[ParallelTools] Dispatching job ${jobId} to supervisor`);\n\n  try {\n    const result = await supervisorBridge.startParallelJob(jobId, script, {\n      visible: options.visible || false,\n      terminalName: options.terminalName,\n      throttleLimit: options.throttleLimit,\n    });\n\n    console.log(\n      `[ParallelTools] Job ${jobId} dispatched - worker PID: ${result.pid}, visible: ${result.visible}`\n    );\n  } catch (error) {\n    console.error(`[ParallelTools] Failed to dispatch job ${jobId}:`, error);\n    throw error;\n  }\n}\n\n/**\n * Clean up temporary files for a job.\n */\nfunction cleanupJobFiles(job: ParallelJob): void {\n  const cleanedByCybernetics = clearCyberneticJobArtifacts([\n    job.signalPath,\n    job.resultPath,\n    job.cancelSignalPath,\n  ]);\n\n  if (cleanedByCybernetics) {\n    return;\n  }\n\n  const files = [job.signalPath, job.resultPath, job.cancelSignalPath];\n\n  files.forEach((filePath) => {\n    try {\n      if (fs.existsSync(filePath)) {\n        fs.unlinkSync(filePath);\n        console.log(`[ParallelTools] Cleaned up ${path.basename(filePath)}`);\n      }\n    } catch (error) {\n      console.warn(`[ParallelTools] Failed to delete ${filePath}:`, error);\n    }\n  });\n}\n\n/**\n * Shutdown parallel tools system.\n * Should be called during extension deactivation.\n */\nexport async function shutdown(): Promise<void> {\n  console.log(\"[ParallelTools] Shutting down...\");\n\n  // Cancel all running jobs\n  const cancelledCount = cancelAllJobs();\n  console.log(`[ParallelTools] Cancelled ${cancelledCount} running jobs`);\n\n  // Shutdown supervisor\n  await supervisorBridge.shutdown();\n\n  console.log(\"[ParallelTools] Shutdown complete\");\n}\n\nfunction resolveCyberneticJobPaths(\n  jobId: string,\n  tempDir: string\n): CyberneticJobPaths {\n  const result = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Get-CyberneticJobPaths\",\n    {\n      JobId: jobId,\n      JobRoot: tempDir,\n    }\n  );\n\n  if (\n    result &&\n    typeof result.signalPath === \"string\" &&\n    typeof result.resultPath === \"string\" &&\n    typeof result.cancelSignalPath === \"string\"\n  ) {\n    return {\n      jobId: typeof result.jobId === \"string\" ? result.jobId : jobId,\n      jobRoot: typeof result.jobRoot === \"string\" ? result.jobRoot : tempDir,\n      signalPath: result.signalPath,\n      resultPath: result.resultPath,\n      cancelSignalPath: result.cancelSignalPath,\n      storePath:\n        typeof result.storePath === \"string\" ? result.storePath : undefined,\n    };\n  }\n\n  return {\n    jobId,\n    jobRoot: tempDir,\n    signalPath: path.join(tempDir, `${jobId}.signal`),\n    resultPath: path.join(tempDir, `${jobId}.jsonl`),\n    cancelSignalPath: path.join(tempDir, `${jobId}_cancel.signal`),\n  };\n}\n\nfunction getCyberneticJobStatus(\n  job: ParallelJob\n): CyberneticJobStatusInfo | null {\n  const result = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Get-CyberneticJobStatus\",\n    {\n      JobId: job.jobId,\n      SignalPath: job.signalPath,\n      ResultPath: job.resultPath,\n      CancelSignalPath: job.cancelSignalPath,\n      StartTime: job.startTime.toISOString(),\n      TimeoutMilliseconds: DEFAULT_TIMEOUT_MS,\n    }\n  );\n\n  if (!result || !isJobStatus(result.status)) {\n    return null;\n  }\n\n  return {\n    jobId: typeof result.jobId === \"string\" ? result.jobId : job.jobId,\n    status: result.status,\n    timestamp: typeof result.timestamp === \"string\" ? result.timestamp : undefined,\n    resultPath:\n      typeof result.resultPath === \"string\" ? result.resultPath : undefined,\n    itemCount:\n      typeof result.itemCount === \"number\" ? result.itemCount : undefined,\n    error: typeof result.error === \"string\" ? result.error : undefined,\n  };\n}\n\nfunction readCyberneticJobResults<T = any>(resultPath: string): T[] | null {\n  const result = invokeCybernetics<unknown>(\n    \"CyberneticAutomata.psm1\",\n    \"Read-CyberneticJobResults\",\n    {\n      ResultPath: resultPath,\n    },\n    { forceArray: true }\n  );\n\n  if (!result) {\n    return null;\n  }\n\n  return (Array.isArray(result) ? result : [result]) as T[];\n}\n\nfunction requestCyberneticJobCancellation(job: ParallelJob): boolean {\n  const result = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Request-CyberneticJobCancellation\",\n    {\n      JobId: job.jobId,\n      CancelSignalPath: job.cancelSignalPath,\n      Reason: \"user_requested\",\n    }\n  );\n\n  return Boolean(result?.acknowledged);\n}\n\nfunction clearCyberneticJobArtifacts(paths: string[]): boolean {\n  const result = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Clear-CyberneticJobArtifacts\",\n    {\n      Paths: paths,\n    }\n  );\n\n  if (!result) {\n    return false;\n  }\n\n  const removedPaths = Array.isArray(result.removedPaths)\n    ? result.removedPaths.filter((value): value is string => typeof value === \"string\")\n    : [];\n\n  for (const removedPath of removedPaths) {\n    console.log(`[ParallelTools] Cleaned up ${path.basename(removedPath)}`);\n  }\n\n  return true;\n} |
20 | typescript/power-tools.d.ts | {1911 263 0.1795 4.6518} | 1991 | /**\n * power-tools.d.ts\n *\n * Type definitions for Power Tools - ripgrep and fd wrappers for code search.\n */\n\n// ============================================================================\n// RIPGREP SEARCH\n// ============================================================================\n\n/**\n * A single ripgrep search hit\n */\nexport interface RgHit {\n  /** Full path to the file containing the match */\n  file: string;\n\n  /** Line number (1-based) */\n  line: number;\n\n  /** Column number (1-based) */\n  column: number;\n\n  /** The matched line text (trimmed) */\n  text: string;\n}\n\n/**\n * Arguments for ripgrep search\n */\nexport interface RgSearchArgs {\n  /** Pattern to search for (regex or literal) */\n  pattern: string;\n\n  /** Working directory to search in */\n  cwd: string;\n\n  /** Optional glob pattern to filter files (e.g., \"*.ts\") */\n  glob?: string;\n\n  /** Maximum number of results to return (default: 200) */\n  maxResults?: number;\n}\n\n/**\n * Search code using ripgrep with JSON output\n *\n * @param args - Search parameters\n * @returns Array of search hits\n * @throws Error if ripgrep fails with non-zero/non-1 exit code\n */\nexport function rgSearch(args: RgSearchArgs): RgHit[];\n\n// ============================================================================\n// FD FILE LISTING\n// ============================================================================\n\n/**\n * Arguments for fd file listing\n */\nexport interface FdListArgs {\n  /** Pattern to match file names against (regex or glob) */\n  pattern: string;\n\n  /** Working directory to search in */\n  cwd: string;\n\n  /** Maximum number of files to return (default: 500) */\n  maxResults?: number;\n}\n\n/**\n * List files matching a pattern using fd\n *\n * @param args - File listing parameters\n * @returns Array of file paths (absolute)\n * @throws Error if fd fails with non-zero/non-1 exit code\n */\nexport function fdList(args: FdListArgs): string[]; |
