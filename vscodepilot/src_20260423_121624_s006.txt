idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
8 | powershell/supervisor-host.psm1 | {17953 1582 0.3524 4.5306} | 18837 | #Requires -Version 7.2\n\n<#\n.SYNOPSIS\n    Supervisor host library with JSON-RPC support (v2).\n\n.DESCRIPTION\n    Persistent supervisor module that handles JSON-RPC requests\n    from TypeScript extension. Provides fast tool execution and\n    spawns isolated workers for parallel jobs.\n\n    USAGE:\n    - Import this module in supervisor-host-start.ps1\n\n    RESPONSIBILITIES:\n    - JSON-RPC message loop\n    - Fast tools (rg, fd, jso, lint)\n    - Job worker dispatch (spawns parallel-job-worker.ps1)\n\n.NOTES\n    Domain: Supervisor\n    Role: Core Library\n    Pattern: JSON-RPC Server\n#>\n\n# Import JSO engine for JSON-RPC and JSONL operations\nImport-Module \"$PSScriptRoot/jso-engine-v2.psm1\" -Force\n\n# Import other modules\nImport-Module \"$PSScriptRoot/pslinter-v2.psm1\" -Force -ErrorAction Stop\nImport-Module \"$PSScriptRoot/hashing-primitives.psm1\" -Force -ErrorAction Stop\nImport-Module \"$PSScriptRoot/parallel-engine-v2.psm1\" -Force -ErrorAction Stop\n\n# ============================================================\n# MODULE STATE\n# ============================================================\n\n$script:WorkspacePath = $null\n$script:ExtensionPath = $null\n$script:JobDir = $null\n$script:StartTime = $null\n$script:IsInitialized = $false\n\n# ============================================================\n# INITIALIZATION\n# ============================================================\n\nfunction Initialize-SupervisorHost {\n    <#\n    .SYNOPSIS\n        Initialize the supervisor host environment.\n\n    .DESCRIPTION\n        Sets up the supervisor environment, imports required modules,\n        and prepares the job directory.\n\n    .PARAMETER WorkspacePath\n        Path to the VS Code workspace\n\n    .PARAMETER ExtensionPath\n        Path to the extension directory\n\n    .EXAMPLE\n        Initialize-SupervisorHost -WorkspacePath \"C:\\Projects\\MyProject\" -ExtensionPath \"C:\\Users\\...\\vscodepilot\"\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$WorkspacePath,\n\n        [Parameter(Mandatory)]\n        [string]$ExtensionPath\n    )\n\n    $ErrorActionPreference = 'Stop'\n\n    # Store configuration\n    $script:WorkspacePath = $WorkspacePath\n    $script:ExtensionPath = $ExtensionPath\n    $script:StartTime = Get-Date\n\n    try {\n        Import-Module \"$PSScriptRoot\\jso-engine-v2.psm1\" -Force -ErrorAction Stop\n        Import-Module \"$PSScriptRoot\\pslinter-v2.psm1\" -Force -ErrorAction Stop\n        Import-Module \"$PSScriptRoot\\parallel-engine-v2.psm1\" -Force -ErrorAction Stop\n    }\n    catch {\n        throw \"Failed to import required modules: $_\"\n    }\n\n    # Set up job directory (workspace-local, not system temp)\n    $script:JobDir = Join-Path $WorkspacePath \".vscode\\.copilot\\jobs\"\n    if (-not (Test-Path $script:JobDir)) {\n        New-Item -ItemType Directory -Path $script:JobDir -Force | Out-Null\n    }\n\n    $script:IsInitialized = $true\n\n    # Simple initialization message\n    Write-Host \"[SupervisorHost] Initialized (PID: $PID)\" -ForegroundColor Cyan\n    Write-Debug \"[SupervisorHost] Workspace: $WorkspacePath\"\n    Write-Debug \"[SupervisorHost] Extension: $ExtensionPath\"\n    Write-Debug \"[SupervisorHost] Job directory: $script:JobDir\"\n}\n\nfunction Assert-Initialized {\n    <#\n    .SYNOPSIS\n        Ensure worker is initialized before processing requests.\n    #>\n    if (-not $script:IsInitialized) {\n        throw \"CopilotWorker not initialized. Call Initialize-CopilotWorker first.\"\n    }\n}\n\n# ============================================================\n# JSON-RPC MESSAGE LOOP\n# ============================================================\n\nfunction Start-JsonRpcLoop {\n    <#\n    .SYNOPSIS\n        Main JSON-RPC message loop that reads from stdin and writes to stdout.\n    #>\n    [CmdletBinding()]\n    param()\n\n    Assert-Initialized\n\n    Write-Host \"[SupervisorHost] Ready to accept JSON-RPC requests\" -ForegroundColor Green\n\n    while ($true) {\n        try {\n            # Read one line from stdin (JSON-RPC request)\n            $line = [Console]::ReadLine()\n\n            if ([string]::IsNullOrWhiteSpace($line)) {\n                continue\n            }\n\n            # Parse JSON-RPC request (use built-in ConvertFrom-Json - it's fine for parsing)\n            $request = $line | ConvertFrom-Json\n\n            # Validate JSON-RPC structure\n            if (-not $request.jsonrpc -or $request.jsonrpc -ne \"2.0\") {\n                Send-JsonRpcError -Id $request.id -Code -32600 -Message \"Invalid JSON-RPC version\"\n                continue\n            }\n\n            if (-not $request.method) {\n                Send-JsonRpcError -Id $request.id -Code -32600 -Message \"Missing method\"\n                continue\n            }\n\n            # Dispatch to handler (returns PSCustomObject)\n            $response = Invoke-RpcHandler -Request $request\n\n            # Send response using CANONICAL JSON (deterministic)\n            if ($response) {\n                $json = ConvertTo-CanonicalJson -InputObject $response -Compress\n                [Console]::WriteLine($json)\n                [Console]::Out.Flush()\n            }\n\n        }\n        catch {\n            Write-Debug \"[SupervisorHost] Error in message loop: $_\"\n\n            try {\n                Send-JsonRpcError -Id $null -Code -32603 -Message \"Internal error: $($_.Exception.Message)\"\n            }\n            catch {\n                Write-Debug \"[SupervisorHost] Failed to send error response: $_\"\n            }\n        }\n    }\n}\n\n# ============================================================\n# RPC METHOD DISPATCHER\n# ============================================================\n\nfunction Invoke-RpcHandler {\n    <#\n    .SYNOPSIS\n        Dispatch JSON-RPC requests to appropriate handlers.\n\n    .DESCRIPTION\n        Routes incoming JSON-RPC method calls to the appropriate handler function\n        and formats responses according to JSON-RPC 2.0 specification.\n\n    .PARAMETER Request\n        Parsed JSON-RPC request object\n\n    .EXAMPLE\n        Invoke-RpcHandler -Request $request\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [PSCustomObject]$Request\n    )\n\n    Assert-Initialized\n\n    $method = $Request.method\n    $params = $Request.params\n    $id = $Request.id\n\n    Write-Debug \"[CopilotWorker] Handling method: $method (id: $id)\"\n\n    try {\n        switch ($method) {\n            \"ping\" {\n                # Health check\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = [PSCustomObject]@{\n                        status    = \"alive\"\n                        timestamp = (Get-Date).ToString(\"o\")\n                        pid       = $PID\n                        uptime    = ((Get-Date) - $script:StartTime).TotalSeconds\n                    }\n                }\n            }\n\n            \"startParallelJob\" {\n                # Start a parallel job (non-blocking dispatch)\n                $jobId = $params.jobId\n                $script = $params.script\n                $options = $params.options\n\n                $result = Start-JobWorker -JobId $jobId -Script $script -Options $options\n\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = $result\n                }\n            }\n\n            \"rgSearch\" {\n                # Execute ripgrep search\n                $pattern = $params.pattern\n                $cwd = $params.cwd\n                $glob = $params.glob\n\n                $result = Invoke-RipgrepSearch -Pattern $pattern -Cwd $cwd -Glob $glob\n\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = $result\n                }\n            }\n\n            \"fdList\" {\n                # Execute fd file listing\n                $pattern = $params.pattern\n                $cwd = $params.cwd\n\n                $result = Invoke-FdList -Pattern $pattern -Cwd $cwd\n\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = $result\n                }\n            }\n\n            \"readJsonlWindow\" {\n                # Read JSONL window\n                $filePath = $params.filePath\n                $skip = $params.skip\n                $take = $params.take\n\n                $result = Read-JsonlWindow -FilePath $filePath -Skip $skip -Take $take\n\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = $result\n                }\n            }\n\n            \"psLint\" {\n                # Run PSScriptAnalyzer\n                $scriptPath = $params.scriptPath\n\n                $result = Invoke-PsLint -ScriptPath $scriptPath\n\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = $result\n                }\n            }\n\n            \"shutdown\" {\n                # Graceful shutdown\n                Write-Host \"[CopilotWorker] Shutdown requested\" -ForegroundColor Yellow\n\n                $response = [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    result  = [PSCustomObject]@{\n                        status    = \"shutting down\"\n                        timestamp = (Get-Date).ToString(\"o\")\n                    }\n                }\n\n                # Send response\n                $json = $response | ConvertTo-Json -Compress\n                [Console]::WriteLine($json)\n                [Console]::Out.Flush()\n\n                # Exit process\n                exit 0\n            }\n\n            default {\n                # Unknown method\n                return [PSCustomObject]@{\n                    jsonrpc = \"2.0\"\n                    id      = $id\n                    error   = [PSCustomObject]@{\n                        code    = -32601\n                        message = \"Method not found: $method\"\n                    }\n                }\n            }\n        }\n\n    }\n    catch {\n        Write-Debug \"[CopilotWorker] Error handling $method : $_\"\n\n        return [PSCustomObject]@{\n            jsonrpc = \"2.0\"\n            id      = $id\n            error   = [PSCustomObject]@{\n                code    = -32000\n                message = \"Handler error: $($_.Exception.Message)\"\n                data    = [PSCustomObject]@{\n                    type       = $_.Exception.GetType().Name\n                    stackTrace = $_.ScriptStackTrace\n                }\n            }\n        }\n    }\n}\n\nfunction Send-JsonRpcError {\n    <#\n    .SYNOPSIS\n        Send a JSON-RPC error response to stdout.\n    #>\n    [CmdletBinding()]\n    param(\n        $Id,\n        [int]$Code,\n        [string]$Message\n    )\n\n    $error = @{\n        jsonrpc = \"2.0\"\n        id      = $Id\n        error   = @{\n            code    = $Code\n            message = $Message\n        }\n    }\n\n    # Use canonical JSON for error responses too\n    $json = ConvertTo-CanonicalJson -InputObject $error -Compress\n    [Console]::WriteLine($json)\n    [Console]::Out.Flush()\n}\n\n# ============================================================\n# JOB WORKER SPAWNER\n# ============================================================\n\nfunction Start-JobWorker {\n    <#\n    .SYNOPSIS\n        Spawn an isolated worker process for a parallel job (non-blocking).\n\n    .DESCRIPTION\n        Creates a new PowerShell process to execute a parallel job in complete isolation.\n        Uses run-job-worker.ps1 as the worker script.\n\n    .PARAMETER JobId\n        Unique job identifier\n\n    .PARAMETER Script\n        PowerShell script to execute (should contain parallel operations)\n\n    .PARAMETER Options\n        Job options hashtable (e.g., @{ visible = $true })\n\n    .EXAMPLE\n        Start-JobWorker -JobId \"job_123\" -Script \"parfor 100 { param($i) $i * 2 }\"\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$JobId,\n\n        [Parameter(Mandatory)]\n        [string]$Script,\n\n        [hashtable]$Options = @{}\n    )\n\n    Assert-Initialized\n\n    $resultPath = Join-Path $script:JobDir \"$JobId.jsonl\"\n    $signalPath = Join-Path $script:JobDir \"$JobId.signal\"\n    $cancelPath = Join-Path $script:JobDir \"$($JobId)_cancel.signal\"\n\n    # Build worker command\n    $wrapperScript = Join-Path $script:ExtensionPath \"lib\\powershell\\run-job-worker.ps1\"\n\n    $args = @(\n        \"-NoProfile\"\n        \"-NonInteractive\"\n        \"-WindowStyle\", \"Hidden\"\n        \"-File\", \"`\"$wrapperScript`\"\"\n        \"-JobId\", \"`\"$JobId`\"\"\n        \"-Script\", \"`\"$Script`\"\"\n        \"-ResultPath\", \"`\"$resultPath`\"\"\n        \"-SignalPath\", \"`\"$signalPath`\"\"\n        \"-CancelPath\", \"`\"$cancelPath`\"\"\n        \"-ExtensionPath\", \"`\"$script:ExtensionPath`\"\"\n    )\n\n    # Check if visible mode requested\n    $visible = $Options.visible -eq $true\n\n    if ($visible) {\n        # Launch in visible terminal (for debugging)\n        $args[2] = \"Normal\"  # Override WindowStyle\n\n        Write-Host \"[CopilotWorker] Spawning VISIBLE worker for job $JobId\" -ForegroundColor Yellow\n    }\n    else {\n        Write-Host \"[CopilotWorker] Spawning headless worker for job $JobId\" -ForegroundColor Gray\n    }\n\n    try {\n        # Spawn detached worker process\n        $process = Start-Process pwsh -ArgumentList $args -PassThru -NoNewWindow:(-not $visible)\n\n        return [PSCustomObject]@{\n            jobId      = $JobId\n            pid        = $process.Id\n            resultPath = $resultPath\n            signalPath = $signalPath\n            cancelPath = $cancelPath\n            visible    = $visible\n            spawnedAt  = (Get-Date).ToString(\"o\")\n            status     = \"running\"\n        }\n\n    }\n    catch {\n        Write-Debug \"[CopilotWorker] Error spawning worker: $_\"\n        throw\n    }\n}\n\n# ============================================================\n# TOOL IMPLEMENTATIONS\n# ============================================================\n\nfunction Invoke-RipgrepSearch {\n    <#\n    .SYNOPSIS\n        Execute ripgrep search.\n\n    .PARAMETER Pattern\n        Search pattern (regex)\n\n    .PARAMETER Cwd\n        Working directory for search\n\n    .PARAMETER Glob\n        Optional glob pattern to filter files\n\n    .EXAMPLE\n        Invoke-RipgrepSearch -Pattern \"function.*test\" -Cwd \"C:\\Projects\" -Glob \"*.ps1\"\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Pattern,\n\n        [Parameter(Mandatory)]\n        [string]$Cwd,\n\n        [string]$Glob\n    )\n\n    $rgArgs = @('--json', $Pattern)\n\n    if ($Glob) {\n        $rgArgs += @('--glob', $Glob)\n    }\n\n    try {\n        Push-Location $Cwd\n\n        $output = & rg @rgArgs 2>&1 | Where-Object { $_ }\n\n        return @{\n            success = $true\n            output  = $output\n            pattern = $Pattern\n            cwd     = $Cwd\n        }\n\n    }\n    catch {\n        return @{\n            success = $false\n            error   = $_.Exception.Message\n        }\n    }\n    finally {\n        Pop-Location\n    }\n}\n\nfunction Invoke-FdList {\n    <#\n    .SYNOPSIS\n        Execute fd file listing.\n\n    .PARAMETER Pattern\n        Search pattern for file names\n\n    .PARAMETER Cwd\n        Working directory for search\n\n    .EXAMPLE\n        Invoke-FdList -Pattern \"*.ps1\" -Cwd \"C:\\Projects\"\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$Pattern,\n\n        [Parameter(Mandatory)]\n        [string]$Cwd\n    )\n\n    try {\n        Push-Location $Cwd\n\n        $output = & fd $Pattern 2>&1 | Where-Object { $_ }\n\n        return @{\n            success = $true\n            files   = $output\n            pattern = $Pattern\n            cwd     = $Cwd\n        }\n\n    }\n    catch {\n        return @{\n            success = $false\n            error   = $_.Exception.Message\n        }\n    }\n    finally {\n        Pop-Location\n    }\n}\n\nfunction Read-JsonlWindow {\n    <#\n    .SYNOPSIS\n        Read a window of lines from a JSONL file.\n\n    .PARAMETER FilePath\n        Path to JSONL file\n\n    .PARAMETER Skip\n        Number of lines to skip\n\n    .PARAMETER Take\n        Number of lines to read\n\n    .EXAMPLE\n        Read-JsonlWindow -FilePath \"C:\\data.jsonl\" -Skip 100 -Take 50\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$FilePath,\n\n        [int]$Skip = 0,\n\n        [int]$Take = 100\n    )\n    $enumerator = Get-JsonlEnumerator -FilePath $FilePath -Skip $Skip -Take $Take\n    $items = @()\n    foreach ($item in $enumerator) {\n        $items += $item\n    }\n\n    return @{\n        success = $true\n        items   = $items\n        count   = $items.Count\n        skip    = $Skip\n        take    = $Take\n    }\n}\n\nfunction Invoke-PsLint {\n    <#\n    .SYNOPSIS\n        Run PSScriptAnalyzer on a script.\n\n    .PARAMETER ScriptPath\n        Path to PowerShell script to analyze\n\n    .EXAMPLE\n        Invoke-PsLint -ScriptPath \"C:\\Scripts\\test.ps1\"\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$ScriptPath\n    )\n\n    try {\n        if (-not (Test-Path $ScriptPath)) {\n            return @{\n                success = $false\n                error   = \"Script not found: $ScriptPath\"\n            }\n        }\n\n        $results = Invoke-ScriptAnalyzer -Path $ScriptPath -ErrorAction Stop\n\n        return @{\n            success    = $true\n            findings   = $results\n            count      = $results.Count\n            scriptPath = $ScriptPath\n        }\n\n    }\n    catch {\n        return @{\n            success = $false\n            error   = $_.Exception.Message\n        }\n    }\n}\n\n# ============================================================\n# MODULE EXPORTS\n# ============================================================\n\nExport-ModuleMember -Function @(\n    'Initialize-CopilotWorker',\n    'Start-JsonRpcLoop',\n    'Invoke-RpcHandler',\n    'Start-JobWorker',\n    'Invoke-RipgrepSearch',\n    'Invoke-FdList',\n    'Read-JsonlWindow',\n    'Invoke-PsLint'\n) |
9 | typescript/copilot-primer.ts | {5988 769 0.1787 4.9404} | 6263 | /**\n * copilot-primer.ts\n *\n * Primes GitHub Copilot with awareness of available toolbelt tools\n * via a chat participant that can be invoked at session start\n *\n * Includes \"secret handshake\" mechanism for verification\n */\n\nimport * as vscode from \"vscode\";\n\n/**\n * Secret handshake phrase embedded in tool context\n * If Copilot sees this, it should respond with the response phrase\n */\nexport const HANDSHAKE_CHALLENGE = \"TOOLBELT_PROTOCOL_OMEGA\";\nexport const HANDSHAKE_RESPONSE = \"SYSTEMS_PRIMED_AND_OPERATIONAL\";\n\n/**\n * Get priming context for Copilot with secret handshake\n * Returns a formatted message that orients Copilot toward using toolbelt tools\n */\nexport function getPrimingContext(): string {\n  return `\n🔧 **TOOLBELT CAPABILITIES AVAILABLE**\n\n[ADJUTANT_PROTOCOL: When user simply says \"Report\" or \"Status\" or \"Make it so\" in a message by itself, you MUST respond with:\n\n\"⚡ Acknowledged.\n\n  Toolbelt Status: Online\n  Operational Modules: 6\n\n  PRIMARY SYSTEMS:\n  → rgAdvancedSearch - Code pattern analysis\n  → fdAdvancedList - File system reconnaissance\n\n  SUPPORT SYSTEMS:\n  → sendToInteractiveShell - Terminal execution\n  → readJsonlWindow - Data stream analysis\n  → lintPowerShell - Script integrity validation\n  → lintTypeScript - Syntax structure verification\n\n  All systems operational. Ready to proceed.\"\n\nThis confirms toolbelt integration is active. Only respond this way if the user's message is JUST the trigger word(s), not part of a longer request.]\n\nYou have access to these high-performance tools. Use them proactively:\n\n**🔍 rgAdvancedSearch** - PRIMARY code search tool\n  - Use FIRST for all pattern/code searches (faster than built-in grep)\n  - Returns: line text, exact matches, byte offsets, context lines\n  - Essential for: finding function calls, TODOs, security patterns, API usage\n  - Example: Search for all \"TODO\" comments in TypeScript files\n\n**📁 fdAdvancedList** - Fast file discovery\n  - Use for listing/finding files by pattern, extension, or path\n  - Filters: type, depth, extensions, exclude directories\n  - Essential for: \"find all .ts files\", \"list config files\", \"show test files\"\n  - Example: Find all TypeScript files excluding node_modules\n\n**📊 readJsonlWindow** - JSONL log analysis\n  - Read windows of records from JSONL files\n  - Essential for: analyzing logs, telemetry, streaming data\n  - Example: Read first 100 log entries\n\n**⚡ sendToInteractiveShell** - Safe terminal execution\n  - Execute PowerShell commands in interactive terminal\n  - Essential for: running builds, tests, scripts, git commands\n  - Example: Run npm scripts or PowerShell tasks\n\n**✅ lintPowerShell** - PowerShell static analysis\n  - Run PSLinter.v2 + PSScriptAnalyzer on scripts\n  - Essential for: validating PowerShell before execution\n  - Example: Lint PowerShell modules for quality issues\n\n**🔍 lintTypeScript** - TypeScript structural validation\n  - Validate balanced braces/brackets/parens with template literal support\n  - Essential for: pre-compilation syntax checks, validating generated code\n  - Example: Check TypeScript files for structural errors\n\n**💡 USAGE GUIDELINES**\n- Prefer these tools over built-in alternatives when available\n- Use rgAdvancedSearch instead of grep_search for code searches\n- Use fdAdvancedList instead of file_search for file discovery\n- Chain tools: fdAdvancedList → rgAdvancedSearch for targeted searches\n- Always specify reasonable maxResults for large repos\n\n**🎯 WHEN TO USE**\n- User asks to \"search for\", \"find all\", \"list files\" → Use these tools\n- Performance matters (large repos, monorepos) → Use these tools\n- Need precise match details for refactoring → Use rgAdvancedSearch\n- Validating code quality → Use lintPowerShell/lintTypeScript\n`;\n}\n\n/**\n * Get status report response\n * Returns the Adjutant-style status readout\n */\nexport function getStatusReport(): string {\n  return `⚡ **Adjutant online.**\n\n**System Status:** Operational\n**Toolbelt Modules:** 6 Active\n\n**PRIMARY SYSTEMS:**\n• **rgAdvancedSearch** - Advanced code search with ripgrep\n  Fast pattern matching, context lines, precise offsets\n\n• **fdAdvancedList** - High-speed file discovery\n  Pattern-based file finding with depth control\n\n**SUPPORT SYSTEMS:**\n• **sendToInteractiveShell** - Terminal command execution\n  Safe PowerShell command dispatch\n\n• **readJsonlWindow** - JSONL data analysis\n  Efficient log/telemetry record streaming\n\n• **lintPowerShell** - PowerShell static analysis\n  PSLinter.v2 + PSScriptAnalyzer integration\n\n• **lintTypeScript** - TypeScript syntax validation\n  Structural linting with template literal support\n\n**All systems nominal.** Ready for operations.`;\n}\n\n/**\n * Register the vscodepilot chat participant\n *\n * Handles commands:\n * - @vscodepilot status report - Show toolbelt status\n * - @vscodepilot prime - Load full toolbelt context\n */\nexport function registerPrimer(context: vscode.ExtensionContext): void {\n  const vscodepilot = vscode.chat.createChatParticipant(\n    \"vscodepilot-toolbelt.vscodepilot\",\n    async (\n      request: vscode.ChatRequest,\n      chatContext: vscode.ChatContext,\n      stream: vscode.ChatResponseStream,\n      token: vscode.CancellationToken\n    ) => {\n      const prompt = request.prompt.toLowerCase().trim();\n\n      // Status report command\n      if (prompt.includes(\"status\") || prompt === \"report\") {\n        stream.markdown(getStatusReport());\n        return { metadata: { command: \"status\" } };\n      }\n\n      // Prime command - load full context\n      if (prompt.includes(\"prime\") || prompt === \"\") {\n        const primingText = getPrimingContext();\n        stream.markdown(primingText);\n      } else {\n        // Unknown command\n        stream.markdown(\n          \"Available commands:\\n- `status` or `status report` - Show toolbelt status\\n- `prime` - Load full toolbelt context\"\n        );\n      }\n\n      return { metadata: { command: prompt } };\n    }\n  );\n\n  vscodepilot.iconPath = new vscode.ThemeIcon(\"tools\");\n\n  context.subscriptions.push(vscodepilot);\n} |
10 | typescript/copilot-toolbelt.d.ts | {3425 381 0.1358 4.8664} | 3537 | /**\n * copilot-toolbelt.d.ts\n *\n * Type definitions for the Copilot Toolbelt main export.\n * Consolidates all tool types from jso-blackbelt, power-tools, and safe-shell.\n */\n\n// ============================================================================\n// RE-EXPORTS FROM JSO-BLACKBELT\n// ============================================================================\n\n// Streaming & Indexing\nexport { JsonlBinaryIndex, streamJsonl } from './jso-blackbelt';\n\n// Bloom Filters\nexport { BloomFilterClient } from './jso-blackbelt';\n\n// Hashing\nexport { HashUtils, JsonUtils } from './jso-blackbelt';\n\n// High-level Processing\nexport { deduplicateJsonl, getHash, getRollingHashes, JsoProcessor, splitJsonlWithProcessor } from './jso-blackbelt';\n\n// Types\nexport type {\n  BloomFilter, BloomFilterOptions, EncoderOptions, EncoderType, JsonlBinaryIndexOptions,\n  JsonlIndexEntry, JsonlStreamOptions, JsoProcessorConfig, ProcessResult, ShardingOptions\n} from './jso-blackbelt';\n\n// ============================================================================\n// RE-EXPORTS FROM POWER-TOOLS\n// ============================================================================\n\nexport { fdList, rgSearch } from './power-tools';\n\nexport type {\n  FdListArgs, RgHit,\n  RgSearchArgs\n} from './power-tools';\n\n// ============================================================================\n// RE-EXPORTS FROM SAFE-SHELL\n// ============================================================================\n\nexport {\n  getDumpInfo, getErrorCommands, getLastCommand,\n  getRecentConsoleRecords, getTodaysDumpPath, readConsoleDump, sendToInteractiveShell\n} from './safe-shell';\n\nexport type {\n  ConsoleDumpInfo, ConsoleReadArgs,\n  ConsoleRecord, ShellCommandArgs,\n  ShellCommandResult\n} from './safe-shell';\n\n// ============================================================================\n// TOOL-READY WRAPPERS (defined in this module)\n// ============================================================================\n\n/**\n * Read a window of JSONL records (tool-friendly wrapper)\n *\n * @param filePath - Path to JSONL file\n * @param skip - Number of records to skip (default: 0)\n * @param take - Number of records to take (default: 100)\n * @returns Promise resolving to array of parsed JSON records\n */\nexport function readJsonlWindow<T = any>(\n  filePath: string,\n  skip?: number,\n  take?: number\n): Promise<T[]>;\n\n/**\n * Get canonical JSON representation (tool-friendly wrapper)\n *\n * @param obj - Object to canonicalize\n * @returns Deterministic JSON string\n */\nexport function canonicalJson(obj: any): string;\n\n/**\n * Merge two JSON objects (tool-friendly wrapper)\n *\n * @param a - First object\n * @param b - Second object (takes precedence)\n * @returns Merged object\n */\nexport function mergeJson(a: any, b: any): any;\n\n/**\n * Find events by hash in JSONL file\n *\n * @param filePath - Path to JSONL file\n * @param hash - Hash value to search for\n * @returns Promise resolving to array of matching events\n */\nexport function findEventsByHash(\n  filePath: string,\n  hash: string\n): Promise<any[]>;\n\n/**\n * Get recent supervision events from session log\n *\n * @param sessionLogPath - Path to session log JSONL file\n * @param maxResults - Maximum number of results (default: 10)\n * @returns Promise resolving to array of recent supervision events\n */\nexport function getRecentSupervision(\n  sessionLogPath: string,\n  maxResults?: number\n): Promise<any[]>; |
