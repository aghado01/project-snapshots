idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
2 | powershell/linter-ps.psm1 | {21661 1970 0.2422 4.9280} | 22240 | using namespace System\nusing namespace System.Management.Automation\nusing namespace System.Management.Automation.Language\n#Requires -Version 7.5.2\n\n<#\n.SYNOPSIS\nSingle-file PSLinter with conventional PS-style rule names, embedded profiles, and structural lexing.\n.DESCRIPTION\nProvides Invoke-PSLinter plus custom PSScriptAnalyzer rules:\n- PSUseRequiresVersion75\n- PSHeaderOrderConvention\n- PSProvideFunctionHelpExtended\n- PSModuleManifestMetadata\n- PSAvoidParallelWithoutThrottle\n- PSAvoidDangerousInvocation\n- PSAvoidInterpolatedCommand\n- PSDetectInvisibleCharacters\n- PSDetectStructuralLexingErrors\n\nProfiles (Strict, Formatting, Repo) are embedded and selected via -Profile; Gallery passes through to built-in preset.\n#>\n\nSet-StrictMode -Version Latest\n\n# ------------------------------\n# Embedded settings profiles\n# ------------------------------\n$script:PSL_Profiles = @{\n    Strict     = @{\n        IncludeDefaultRules = $true\n        Severity            = @('Error', 'Warning')\n        IncludeRules        = @(\n            'PSUseApprovedVerbs',\n            'PSAvoidUsingCmdletAliases',\n            'PSAvoidUsingWriteHost',\n            'PSAvoidUsingPlainTextForPassword',\n            'PSAvoidUsingConvertToSecureStringWithPlainText',\n            'PSUseCompatibleCmdlets',\n            'PSUseCompatibleCommands',\n            'PSUseCompatibleSyntax',\n            'PSUseCompatibleTypes',\n            'PSUseConsistentIndentation',\n            'PSUseConsistentWhitespace',\n            'PSProvideCommentHelp',\n            'PSUseRequiresVersion75',\n            'PSHeaderOrderConvention',\n            'PSProvideFunctionHelpExtended',\n            'PSModuleManifestMetadata',\n            'PSAvoidParallelWithoutThrottle',\n            'PSAvoidDangerousInvocation',\n            'PSAvoidInterpolatedCommand',\n            'PSDetectInvisibleCharacters',\n            'PSDetectStructuralLexingErrors'\n        )\n        Rules               = @{\n            PSUseConsistentIndentation = @{ Enable = $true; IndentationSize = 4; Kind = 'space' }\n            PSUseConsistentWhitespace  = @{\n                CheckParameter = $true; CheckInnerBrace = $true; CheckOpenBrace = $true;\n                CheckPipe = $true; CheckOperator = $true; CheckSeparator = $true\n            }\n        }\n    }\n    Formatting = @{\n        IncludeDefaultRules = $true\n        Severity            = @('Warning', 'Information')\n        IncludeRules        = @(\n            'PSUseConsistentIndentation',\n            'PSUseConsistentWhitespace',\n            'PSPlaceOpenBrace',\n            'PSPlaceCloseBrace',\n            'PSAlignAssignmentStatement',\n            'PSAvoidSemicolonsAsLineTerminators',\n            'PSAvoidTrailingWhitespace',\n            'PSHeaderOrderConvention',\n            'PSProvideFunctionHelpExtended',\n            'PSDetectInvisibleCharacters'\n        )\n        Rules               = @{\n            PSUseConsistentIndentation = @{ Enable = $true; IndentationSize = 4; Kind = 'space' }\n            PSUseConsistentWhitespace  = @{\n                CheckParameter = $true; CheckInnerBrace = $true; CheckOpenBrace = $true;\n                CheckPipe = $true; CheckOperator = $true; CheckSeparator = $true\n            }\n        }\n    }\n    Repo       = @{\n        IncludeDefaultRules = $true\n        Severity            = @('Error', 'Warning', 'Information')\n        IncludeRules        = @(\n            'PSUseApprovedVerbs',\n            'PSAvoidUsingCmdletAliases',\n            'PSProvideCommentHelp',\n            'PSUseConsistentIndentation',\n            'PSUseConsistentWhitespace',\n            'PSUseRequiresVersion75',\n            'PSHeaderOrderConvention',\n            'PSProvideFunctionHelpExtended',\n            'PSModuleManifestMetadata',\n            'PSAvoidParallelWithoutThrottle',\n            'PSAvoidDangerousInvocation',\n            'PSAvoidInterpolatedCommand',\n            'PSDetectInvisibleCharacters',\n            'PSDetectStructuralLexingErrors'\n        )\n    }\n}\n\n# ------------------------------\n# Shared messages and helpers\n# ------------------------------\n$script:PSLinterRuleMessages = @{\n    HeaderOrder = \"Header order must be: using namespace lines first, then '#Requires -Version', then a top-level docstring.\"\n    RequiresV   = \"Missing or insufficient '#Requires -Version 7.5' at the top of the script.\"\n    FuncHelp    = \"Function is missing comment-based help with .SYNOPSIS and .DESCRIPTION.\"\n    ParallelTL  = \"ForEach-Object -Parallel should specify -ThrottleLimit to control runspace concurrency.\"\n    ManifestMV  = \"Module manifest must define a valid semantic ModuleVersion (e.g., 1.2.3).\"\n    ManifestPE  = \"Module manifest CompatiblePSEditions must include 'Core' for PowerShell 7+.\"\n    TokenInv    = \"Suspicious or invisible characters detected (e.g., NBSP, zero-width, bidi controls), or mixed line endings.\"\n    DynInvoke   = \"Dangerous dynamic invocation detected (Invoke-Expression or ampersand with string).\"\n    InterpCmd   = \"Interpolated command or parameter detected; prefer fixed names or validated values.\"\n    StructLex   = \"Structural lexing detected likely parse-adjacent defects (unbalanced delimiters, unterminated strings/here-strings, dangling backticks, or unclosed block comments).\"\n}\n\nfunction New-PSLinterRecord {\n    param(\n        [Parameter(Mandatory)][string]$Message,\n        [Parameter(Mandatory)][IScriptExtent]$Extent,\n        [Parameter(Mandatory)][string]$RuleName,\n        [ValidateSet('Error', 'Warning', 'Information')][string]$Severity = 'Warning'\n    )\n    [Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord]@{\n        Message  = $Message\n        Extent   = $Extent\n        RuleName = $RuleName\n        Severity = $Severity\n    }\n}\n\n# Parse helper using the platform parser for AST/tokens\nfunction Get-PSLinterParsed {\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $errors = $null\n    $tokens = $null\n    $text = $ScriptBlockAst.Extent.Text\n    $ast = [Parser]::ParseInput($text, [ref]$tokens, [ref]$errors)\n    [pscustomobject]@{ Ast = $ast; Tokens = $tokens; Errors = $errors; Text = $text; File = $ScriptBlockAst.Extent.File }\n}\n\n# ------------------------------\n# Custom rules (PS-style names)\n# ------------------------------\n\nfunction PSUseRequiresVersion75 {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $text = $ScriptBlockAst.Extent.Text\n    $m = [regex]::Match($text, '(?im)^[ \\t]*#requires[ \\t]+-version[ \\t]+(?<ver>\\d+(\\.\\d+)*)\\b')\n    if (-not $m.Success) {\n        return , (New-PSLinterRecord -Message $script:PSLinterRuleMessages.RequiresV -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Error')\n    }\n    try {\n        $ver = [version]$m.Groups['ver'].Value\n        if ($ver -lt [version]'7.5') {\n            $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.RequiresV -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Error')\n        }\n    }\n    catch {\n        $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.RequiresV -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Error')\n    }\n    $out\n}\n\nfunction PSHeaderOrderConvention {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $lines = $ScriptBlockAst.Extent.Text -split \"(`r`n|`n|`r)\"\n    if ($lines.Length -eq 0) { return $out }\n    $usingIdxs = @(for ($i = 0; $i -lt $lines.Length; $i++) { if ($lines[$i] -match '^\\s*using\\s+namespace\\s+') { $i } })\n    $reqIdx = -1; for ($i = 0; $i -lt $lines.Length; $i++) { if ($lines[$i] -match '^\\s*#requires\\s+-version\\b') { $reqIdx = $i; break } }\n    $docIdx = -1; for ($i = 0; $i -lt $lines.Length; $i++) { if ($lines[$i] -match '^\\s*<#') { $docIdx = $i; break } }\n    if ($docIdx -lt 0) { return $out }\n    $firstNonBlank = -1\n    for ($i = 0; $i -lt $lines.Length; $i++) { if ($lines[$i] -notmatch '^\\s*(#.*)?$') { $firstNonBlank = $i; break } }\n    $hasUsing = $usingIdxs.Count -gt 0\n    $usingBlockEndsAt = if ($hasUsing) { ($usingIdxs | Measure-Object -Maximum).Maximum } else { -1 }\n    $ok = $true\n    if ($hasUsing -and $firstNonBlank -ne $usingIdxs) { $ok = $false }\n    if ($hasUsing -and $usingIdxs | Where-Object { $_ -gt $docIdx }) { $ok = $false }\n    if ($reqIdx -lt 0 -or ($hasUsing -and $reqIdx -lt $usingBlockEndsAt) -or ($docIdx -lt $reqIdx)) { $ok = $false }\n    if (-not $ok) {\n        $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.HeaderOrder -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n    }\n    $out\n}\n\nfunction PSProvideFunctionHelpExtended {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst, [Token[]]$Token)\n    $out = @()\n    $funcs = @($ScriptBlockAst.FindAll({ param($a) $a -is [FunctionDefinitionAst] }, $true))\n    foreach ($f in $funcs) {\n        $hasHelp = $false; $hasSynopsis = $false; $hasDesc = $false\n        if ($Token) {\n            $before = $Token | Where-Object { $_.Extent.EndOffset -le $f.Extent.StartOffset -and $_.Kind -eq 'Comment' }\n            $nearest = $before | Sort-Object { [math]::Abs($_.Extent.StartLineNumber - $f.Extent.StartLineNumber) } | Select-Object -First 1\n            if ($nearest -and $nearest.Text -match '<#' -and $nearest.Text -match '#>') {\n                $hasHelp = $true\n                if ($nearest.Text -match '(?im)^\\s*\\.SYNOPSIS\\b') { $hasSynopsis = $true }\n                if ($nearest.Text -match '(?im)^\\s*\\.DESCRIPTION\\b') { $hasDesc = $true }\n            }\n        }\n        if (-not ($hasHelp -and $hasSynopsis -and $hasDesc)) {\n            $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.FuncHelp -Extent $f.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n        }\n    }\n    $out\n}\n\nfunction PSModuleManifestMetadata {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $file = $ScriptBlockAst.Extent.File\n    if ([string]::IsNullOrWhiteSpace($file)) { return $out }\n    if ([IO.Path]::GetExtension($file) -notin @('.psd1', '.PSD1')) { return $out }\n    try {\n        $data = Import-PowerShellDataFile -Path $file\n        $mv = $data['ModuleVersion']; $mvOk = $false\n        try { $null = [version]$mv; if ($mv -match '^\\d+\\.\\d+\\.\\d+(\\.\\d+)?$') { $mvOk = $true } } catch { $mvOk = $false }\n        if (-not $mvOk) {\n            $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.ManifestMV -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Error')\n        }\n        $cpse = @($data['CompatiblePSEditions'])\n        if (-not ($cpse -contains 'Core')) {\n            $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.ManifestPE -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Error')\n        }\n    }\n    catch {\n        return $out\n    }\n    $out\n}\n\nfunction PSAvoidParallelWithoutThrottle {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $cmds = @($ScriptBlockAst.FindAll({ param($a) $a -is [CommandAst] }, $true))\n    foreach ($c in $cmds) {\n        $name = $c.GetCommandName()\n        if ($null -eq $name) { continue }\n        if ($name -ieq 'ForEach-Object') {\n            $hasParallel = $false; $hasThrottle = $false\n            foreach ($el in $c.CommandElements) {\n                if ($el -is [CommandParameterAst]) {\n                    if ($el.ParameterName -ieq 'Parallel') { $hasParallel = $true }\n                    if ($el.ParameterName -ieq 'ThrottleLimit') { $hasThrottle = $true }\n                }\n            }\n            if ($hasParallel -and -not $hasThrottle) {\n                $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.ParallelTL -Extent $c.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Information')\n            }\n        }\n    }\n    $out\n}\n\nfunction PSAvoidDangerousInvocation {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst, [Token[]]$Token)\n    $out = @()\n    $cmds = @($ScriptBlockAst.FindAll({ param($a) $a -is [CommandAst] }, $true))\n    foreach ($c in $cmds) {\n        $name = $c.GetCommandName()\n        if ($name -and ($name -ieq 'Invoke-Expression' -or $name -ieq 'iex')) {\n            $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.DynInvoke -Extent $c.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n        }\n    }\n    if ($Token) {\n        for ($i = 0; $i -lt $Token.Count; $i++) {\n            $t = $Token[$i]\n            if ($t.Kind -eq [TokenKind]::Ampersand) {\n                $j = $i + 1\n                while ($j -lt $Token.Count -and $Token[$j].Kind -eq [TokenKind]::NewLine) { $j++ }\n                if ($j -lt $Token.Count) {\n                    $next = $Token[$j]\n                    if ($next.Kind -in @([TokenKind]::StringExpandable, [TokenKind]::StringLiteral)) {\n                        $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.DynInvoke -Extent $t.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n                    }\n                }\n            }\n        }\n    }\n    $out\n}\n\nfunction PSAvoidInterpolatedCommand {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $cmds = @($ScriptBlockAst.FindAll({ param($a) $a -is [CommandAst] }, $true))\n    foreach ($c in $cmds) {\n        if ($c.CommandElements.Count -gt 0) {\n            $first = $c.CommandElements[0]\n            if ($first -is [ExpandableStringExpressionAst] -or $first -is [SubExpressionAst]) {\n                $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.InterpCmd -Extent $c.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n                continue\n            }\n        }\n        foreach ($el in $c.CommandElements) {\n            if ($el -is [CommandParameterAst]) {\n                if ($el.Argument -is [ExpandableStringExpressionAst] -or $el.Argument -is [SubExpressionAst]) {\n                    $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.InterpCmd -Extent $el.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Information')\n                }\n            }\n        }\n    }\n    $out\n}\n\nfunction PSDetectInvisibleCharacters {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $p = Get-PSLinterParsed -ScriptBlockAst $ScriptBlockAst\n    $text = $p.Text\n    # NBSP, zero-width marks, bidi controls\n    $suspiciousPattern = [regex]::new(\"[\\u00A0\\u200B\\u200C\\u200D\\u200E\\u200F\\u202A-\\u202E\\u2066-\\u2069]\", 'Compiled')\n    $hasSuspicious = $suspiciousPattern.IsMatch($text)\n    $hasCRLF = $text -match \"`r`n\"\n    $hasLF = $text -match \"(?<!`r)`n\"\n    $mixed = $hasCRLF -and $hasLF\n    if ($hasSuspicious -or $mixed) {\n        $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.TokenInv -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n    }\n    $out\n}\n\nfunction PSDetectStructuralLexingErrors {\n    [CmdletBinding()]\n    [OutputType([Microsoft.Windows.PowerShell.ScriptAnalyzer.Generic.DiagnosticRecord[]])]\n    param([Parameter(Mandatory)][ScriptBlockAst]$ScriptBlockAst)\n    $out = @()\n    $text = $ScriptBlockAst.Extent.Text\n    $lines = $text -split \"(`r`n|`n|`r)\"\n    $paren = 0; $brace = 0; $brack = 0\n    $inSingle = $false; $inDouble = $false\n    $inHereD = $false; $inHereS = $false\n    $blockCommentCountOpen = 0; $blockCommentCountClose = 0\n\n    # Simple block comment balance check\n    $blockCommentCountOpen = ([regex]::Matches($text, '(?s)<#')).Count\n    $blockCommentCountClose = ([regex]::Matches($text, '(?s)#>')).Count\n\n    for ($i = 0; $i -lt $lines.Length; $i++) {\n        $line = $lines[$i]\n        # here-string start/end (must be at line start ignoring whitespace)\n        if (-not $inHereD -and -not $inHereS -and $line -match '^\\s*@\\\"') { $inHereD = $true; continue }\n        if (-not $inHereS -and -not $inHereD -and $line -match \"^\\s*@'\") { $inHereS = $true; continue }\n        if ($inHereD -and $line -match '^\\s*\\\"@') { $inHereD = $false; continue }\n        if ($inHereS -and $line -match \"^\\s*'@\") { $inHereS = $false; continue }\n        if ($inHereD -or $inHereS) { continue }\n\n        # detect dangling backtick at end of line\n        if ($line -match '(?<!`)`$') {\n            # If last line or next line is empty, treat as structural risk\n            $next = if ($i + 1 -lt $lines.Length) { $lines[$i + 1] } else { '' }\n            if ([string]::IsNullOrWhiteSpace($next)) {\n                $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.StructLex -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Warning')\n                break\n            }\n        }\n\n        # string scanning per line (ignoring escaped quotes with backtick)\n        for ($c = 0; $c -lt $line.Length; $c++) {\n            $ch = $line[$c]\n            $prevBacktick = ($c -gt 0 -and $line[$c - 1] -eq '`')\n\n            if (-not $inSingle -and -not $inDouble) {\n                switch ($ch) {\n                    '(' { $paren++ }\n                    ')' { $paren-- }\n                    '{' { $brace++ }\n                    '}' { $brace-- }\n                    '[' { $brack++ }\n                    ']' { $brack-- }\n                    '\"' { if (-not $prevBacktick) { $inDouble = $true } }\n                    \"'\" { if (-not $prevBacktick) { $inSingle = $true } }\n                }\n            }\n            elseif ($inDouble) {\n                if ($ch -eq '\"' -and -not $prevBacktick) { $inDouble = $false }\n            }\n            elseif ($inSingle) {\n                if ($ch -eq \"'\" -and -not $prevBacktick) { $inSingle = $false }\n            }\n        }\n    }\n\n    $unbalanced = ($paren -ne 0) -or ($brace -ne 0) -or ($brack -ne 0)\n    $unterminatedString = $inSingle -or $inDouble -or $inHereD -or $inHereS\n    $unclosedBlockComment = $blockCommentCountOpen -ne $blockCommentCountClose\n\n    if ($unbalanced -or $unterminatedString -or $unclosedBlockComment) {\n        $out += (New-PSLinterRecord -Message $script:PSLinterRuleMessages.StructLex -Extent $ScriptBlockAst.Extent -RuleName $PSCmdlet.MyInvocation.InvocationName -Severity 'Error')\n    }\n\n    $out\n}\n\nExport-ModuleMember -Function `\n    PSUseRequiresVersion75, `\n    PSHeaderOrderConvention, `\n    PSProvideFunctionHelpExtended, `\n    PSModuleManifestMetadata, `\n    PSAvoidParallelWithoutThrottle, `\n    PSAvoidDangerousInvocation, `\n    PSAvoidInterpolatedCommand, `\n    PSDetectInvisibleCharacters, `\n    PSDetectStructuralLexingErrors\n\n# ------------------------------\n# Unified wrapper\n# ------------------------------\nfunction Invoke-PSLinter {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [string]$Path,\n        [Parameter()][ValidateSet('Strict', 'Formatting', 'Repo', 'Gallery')]\n        [string]$Profile = 'Repo',\n        [Parameter()][switch]$Recurse,\n        [Parameter()][switch]$Fix,\n        [ValidateSet('Error', 'Warning', 'Information', 'ParseError')]\n        [string[]]$Severity\n    )\n    $ErrorActionPreference = 'Stop'\n    if (-not (Test-Path -LiteralPath $Path)) { throw \"Path not found: $Path\" }\n\n    if ($Profile -eq 'Gallery') {\n        $splat = @{ Path = $Path; Settings = 'PSGallery' }\n        if ($Recurse) { $splat.Recurse = $true }\n        if ($Fix) { $splat.Fix = $true }\n        if ($Severity) { $splat.Severity = $Severity }\n\n        # Ensure we always return an array (even if empty or single result)\n        $results = @(Invoke-ScriptAnalyzer @splat)\n        return $results\n    }\n\n    $base = $script:PSL_Profiles[$Profile]\n    if (-not $base) { throw \"Unknown profile: $Profile\" }\n\n    $settings = @{}\n    $base.GetEnumerator() | ForEach-Object { $settings[$_.Key] = $_.Value }\n\n    $modulePath = $PSCommandPath\n    if ($settings.ContainsKey('CustomRulePath')) {\n        $existing = @($settings['CustomRulePath'])\n        if ($existing -notcontains $modulePath) { $settings['CustomRulePath'] = @($existing + $modulePath) }\n    }\n    else {\n        $settings['CustomRulePath'] = @($modulePath)\n    }\n\n    if (-not $settings.ContainsKey('IncludeDefaultRules')) { $settings['IncludeDefaultRules'] = $true }\n\n    $splat = @{ Path = $Path; Settings = $settings }\n    if ($Recurse) { $splat.Recurse = $true }\n    if ($Fix) { $splat.Fix = $true }\n    if ($Severity) { $splat.Severity = $Severity }\n\n    # Ensure we always return an array (even if empty or single result)\n    $results = @(Invoke-ScriptAnalyzer @splat)\n    return $results\n}\n\nExport-ModuleMember -Function Invoke-PSLinter\n# End of PSLinter.v2.psm1 |
3 | powershell/online-learner.psm1 | {4357 445 0.3268 4.5571} | 4490 | # rs.core.learning.psm1\n#Requires -Version 7.2\n\nusing namespace System.Collections.Generic\n\n<#\n.SYNOPSIS\n    Online learning primitive for tracking \"Expected Uncertainty\".\n.DESCRIPTION\n    Implements Welford's Algorithm for one-pass variance/covariance updates.\n    Serves as the \"Internal Model\" for the Supervisor, allowing it to\n    quantify Surprisal (Mahalanobis distance) without storing history.\n#>\nclass OnlineLearner {\n    # The \"Internal Model\" state\n    [long] $Count\n    [Dictionary[string, double]] $Mean\n    [Dictionary[string, Dictionary[string, double]]] $M2 # Sum of squares of differences\n\n    # Configuration\n    [double] $LearningRate # For exponential moving average (forgetting factor)\n\n    OnlineLearner([double]$learningRate = 0.0) {\n        $this.Count = 0\n        $this.Mean = [Dictionary[string, double]]::new()\n        $this.M2 = [Dictionary[string, Dictionary[string, double]]]::new()\n        $this.LearningRate = $learningRate\n    }\n\n    <#\n    .SYNOPSIS\n        Updates the internal model with a new observation.\n        Returns the \"Surprisal\" (Anomaly Score) of this observation *before* integration.\n    #>\n    [double] Learn([hashtable]$Observation) {\n        # 1. Calculate Surprisal (Prediction Error) against CURRENT model\n        $surprise = $this.GetMahalanobisDistance($Observation)\n\n        # 2. Update Model (Homeostasis)\n        $this.Count++\n\n        foreach ($key in $Observation.Keys) {\n            $x = [double]$Observation[$key]\n\n            # Initialize if new dimension discovered\n            if (-not $this.Mean.ContainsKey($key)) {\n                $this.Mean[$key] = 0.0\n                $this.M2[$key] = [Dictionary[string, double]]::new()\n            }\n\n            # Welford's Update: Delta from old mean\n            $delta = $x - $this.Mean[$key]\n\n            # Update Mean\n            if ($this.LearningRate -gt 0) {\n                # Exponential Moving Average (Adaptation)\n                $this.Mean[$key] += $this.LearningRate * $delta\n            } else {\n                # Standard Cumulative Average\n                $this.Mean[$key] += $delta / $this.Count\n            }\n\n            # Update Covariance Matrix (M2)\n            foreach ($otherKey in $Observation.Keys) {\n                if (-not $this.M2[$key].ContainsKey($otherKey)) {\n                    $this.M2[$key][$otherKey] = 0.0\n                }\n\n                $y = [double]$Observation[$otherKey]\n                # Critical: Use Mean *after* update for one term, *before* for the other?\n                # Welford standard: M2 += (x - oldMean)(x - newMean)\n                # Simplification for diagonal (Variance):\n                if ($key -eq $otherKey) {\n                    $delta2 = $x - $this.Mean[$key]\n                    $this.M2[$key][$key] += $delta * $delta2\n                }\n            }\n        }\n\n        return $surprise\n    }\n\n    <#\n    .SYNOPSIS\n        Calculates Mahalanobis distance of observation from the current distribution.\n        d(x) = sqrt( (x-u)^T * C^-1 * (x-u) )\n    #>\n    [double] GetMahalanobisDistance([hashtable]$Vector) {\n        if ($this.Count -lt 2) { return 0.0 } # Not enough data to be surprised\n\n        # Simplified: Calculating diagonal distance (Z-score sum) for performance\n        # Full Mahalanobis inversion is O(d^3), might be too heavy for endoskeleton loops\n        # We use a diagonal approximation (Euclidean distance normalized by Variance)\n\n        $sumSqDist = 0.0\n\n        foreach ($key in $Vector.Keys) {\n            if ($this.Mean.ContainsKey($key)) {\n                $mu = $this.Mean[$key]\n                $var = $this.M2[$key][$key] / ($this.Count - 1)\n\n                if ($var -gt 1e-9) {\n                    $diff = [double]$Vector[$key] - $mu\n                    $sumSqDist += ($diff * $diff) / $var\n                }\n            }\n        }\n\n        return [Math]::Sqrt($sumSqDist)\n    }\n\n    <#\n    .SYNOPSIS\n        Exports the internal model state (Semantic Memory).\n    #>\n    [PSCustomObject] GetState() {\n        return [PSCustomObject]@{\n            Count = $this.Count\n            Mean = $this.Mean\n            # Return Variance vector instead of full Covariance matrix for readability\n            Variance = $this.Mean.Keys | ForEach-Object {\n                @{$_ = $this.M2[$_][$_] / [Math]::Max(1, $this.Count - 1)}\n            }\n        }\n    }\n} |
4 | powershell/parallel-async-worker.ps1 | {2460 232 0.2285 4.9623} | 2575 | #Requires -Version 7.2\n\n<#\n.SYNOPSIS\n    Isolated job worker for asynchronous parallel operations.\n\n.DESCRIPTION\n    Worker script spawned by supervisor-host for background parallel jobs.\n    Runs in complete isolation with its own runspace pool.\n\n    COMMUNICATION: Signal files (*.signal, *.jsonl)\n    LIFECYCLE: Per-job (self-terminates on completion)\n    SPAWNED BY: supervisor-host-v2.psm1 (Start-JobWorker function)\n\n.NOTES\n    Domain: Parallel Processing\n    Role: Job Worker (Asynchronous)\n    Pattern: Background Execution\n#>\n\n[CmdletBinding()]\nparam(\n    [Parameter(Mandatory)]\n    [string]$JobId,\n\n    [Parameter(Mandatory)]\n    [string]$Script,\n\n    [Parameter(Mandatory)]\n    [string]$ResultPath,\n\n    [Parameter(Mandatory)]\n    [string]$SignalPath,\n\n    [Parameter(Mandatory)]\n    [string]$CancelPath,\n\n    [Parameter(Mandatory)]\n    [string]$ExtensionPath\n)\n\n$ErrorActionPreference = 'Continue'\n\nWrite-Host \"[JobWorker:$JobId] Starting at $(Get-Date)\" -ForegroundColor Cyan\n\ntry {\n\n    $moduleName = \"parallel-engine-v2\"\n    $modulePath = Join-Path $PSScriptRoot \"$moduleName.psm1\"\n\n    if (-not (Test-Path $modulePath -PathType Leaf)) {\n        throw \"Cannot find module $moduleName.psm1 at: $modulePath\"\n    }\n\n    try {\n        Import-Module $modulePath -Force -ErrorAction Stop\n    }\n    catch {\n        throw \"Failed to import module $moduleName : $_\"\n    }\n\n    Write-Host \"[JobWorker:$JobId] Module loaded: $moduleName\" -ForegroundColor Gray\n\n    # Execute the job script using Invoke-ParallelJobWithSignal\n    Invoke-ParallelJobWithSignal `\n        -ScriptBlock ([scriptblock]::Create($Script)) `\n        -JobId $JobId `\n        -SignalPath $SignalPath `\n        -ResultPath $ResultPath `\n        -CancelSignalPath $CancelPath\n\n    Write-Host \"[JobWorker:$JobId] Completed successfully at $(Get-Date)\" -ForegroundColor Green\n\n}\ncatch {\n    Write-Host \"[JobWorker:$JobId] FAILED: $_\" -ForegroundColor Red\n    Write-Host $_.ScriptStackTrace -ForegroundColor Red\n\n    # Ensure error signal is written\n    $errorSignal = @{\n        jobId     = $JobId\n        status    = 'failed'\n        timestamp = (Get-Date).ToString('o')\n        error     = $_.Exception.Message\n        stack     = $_.ScriptStackTrace\n    }\n\n    try {\n        $errorSignal | ConvertTo-Json | Set-Content $SignalPath -Encoding UTF8\n    }\n    catch {\n        Write-Host \"[JobWorker:$JobId] Could not write error signal: $_\" -ForegroundColor Red\n    }\n\n    exit 1\n}\n\nexit 0 |
