idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
6 | powershell/parallel-engine-v2.psm1 | {24687 2193 0.3397 4.5097} | 25511 | #Requires -Version 7.2\n\n<#\n.SYNOPSIS\n    Parallel processing engine (toolbox) with hybrid class/function architecture (v2).\n    Inspired by MATLAB's original Parallel Computing Toolbox and adapted for VsCodePilot extension.\n\n.DESCRIPTION\n    User-friendly parallel processing module with internal class-based engine\n    and public function API for idiomatic PowerShell usage.\n\n    Features:\n      - Class-based internal engine for performance and encapsulation\n      - Function wrappers for familiar PowerShell API\n      - parfor, parforeach, parrange: Simple parallel loop wrappers\n      - parwhile, paruntil: Conditional parallel loops\n      - Invoke-ParallelBatchProcessing: High-level batch file processor\n      - Signal file support for async job detection\n      - Thread-safe result collection optimized for PowerShell 7.2+\n      - Error handling per item\n\n    Performance Optimizations for PowerShell 7.2+:\n      - Uses optimized += operator (10-100x faster than PS 7.0-7.1)\n      - Class methods for faster internal dispatch\n      - Leverages improved runspace pool management\n      - Takes advantage of enhanced parallel foreach capabilities\n\n.NOTES\n    Author: azriy\n    Adapted for: VsCodePilot Extension\n    Version: 2.0.0 (Hybrid Architecture)\n    Requires: PowerShell 7.2 or later (for optimal performance)\n    Uses: RunspacePool for robust parallelism\n\n.LINK\n    https://github.com/azriy/vscodepilot\n#>\n\n# Import jso-engine module\n\nImport-Module \"$PSScriptRoot/jso-engine.psm1\" -Force -ErrorAction Stop\n\n# ============================================================\n# INTERNAL CLASS (hidden implementation)\n# ============================================================\n\nclass ParallelEngine {\n    [int] $ThrottleLimit\n    hidden [RunspacePool] $Pool\n\n    # Constructor\n    ParallelEngine([int]$throttle) {\n        $this.ThrottleLimit = $this.EnforceThrottleLimit($throttle)\n    }\n\n    # Enforce sane throttle limits\n    hidden [int] EnforceThrottleLimit([int]$limit) {\n        $maxCores = [Environment]::ProcessorCount\n\n        if ($limit -gt $maxCores) {\n            Write-Warning \"ThrottleLimit ($limit) exceeds logical processor count ($maxCores). Using $maxCores.\"\n            return $maxCores\n        }\n\n        if ($limit -lt 1) {\n            Write-Warning \"ThrottleLimit must be at least 1. Using 1.\"\n            return 1\n        }\n\n        return $limit\n    }\n\n    # Main execution method - orchestrates parallel work\n    [array] ExecuteParallel(\n        [string]$loopType,\n        $data,\n        [scriptblock]$scriptBlock,\n        [scriptblock]$condition,\n        [string]$cancelPath\n    ) {\n        # Create runspace pool\n        $this.Pool = [runspacefactory]::CreateRunspacePool(1, $this.ThrottleLimit)\n        $this.Pool.Open()\n\n        try {\n            # Dispatch work based on loop type\n            $runspaces = $this.DispatchWork($loopType, $data, $scriptBlock, $condition, $cancelPath)\n\n            # Collect results from all runspaces\n            return $this.CollectResults($runspaces, $cancelPath)\n        }\n        finally {\n            # Always clean up pool\n            $this.Pool.Close()\n            $this.Pool.Dispose()\n        }\n    }\n\n    # Dispatch work to runspaces based on loop type\n    hidden [array] DispatchWork(\n        [string]$loopType,\n        $data,\n        [scriptblock]$scriptBlock,\n        [scriptblock]$condition,\n        [string]$cancelPath\n    ) {\n        $runspaces = @()\n\n        switch ($loopType) {\n            'foreach' {\n                foreach ($item in $data) {\n                    $ps = [powershell]::Create()\n                    $ps.RunspacePool = $this.Pool\n                    [void]$ps.AddScript($scriptBlock).AddArgument($item)\n\n                    $runspaces += [PSCustomObject]@{\n                        Pipe   = $ps\n                        Handle = $ps.BeginInvoke()\n                    }\n                }\n            }\n\n            'for' {\n                for ($i = 0; $i -lt $data; $i++) {\n                    $ps = [powershell]::Create()\n                    $ps.RunspacePool = $this.Pool\n                    [void]$ps.AddScript($scriptBlock).AddArgument($i)\n\n                    $runspaces += [PSCustomObject]@{\n                        Pipe   = $ps\n                        Handle = $ps.BeginInvoke()\n                    }\n                }\n            }\n\n            'range' {\n                foreach ($i in $data) {\n                    $ps = [powershell]::Create()\n                    $ps.RunspacePool = $this.Pool\n                    [void]$ps.AddScript($scriptBlock).AddArgument($i)\n\n                    $runspaces += [PSCustomObject]@{\n                        Pipe   = $ps\n                        Handle = $ps.BeginInvoke()\n                    }\n                }\n            }\n\n            'while' {\n                $counter = 0\n                while (& $condition) {\n                    $ps = [powershell]::Create()\n                    $ps.RunspacePool = $this.Pool\n                    [void]$ps.AddScript($scriptBlock).AddArgument($counter)\n\n                    $runspaces += [PSCustomObject]@{\n                        Pipe   = $ps\n                        Handle = $ps.BeginInvoke()\n                    }\n\n                    $counter++\n\n                    # Safety: prevent infinite loop runaway\n                    if ($counter -gt 1000000) {\n                        Write-Warning \"While loop exceeded 1,000,000 iterations. Breaking.\"\n                        break\n                    }\n                }\n            }\n\n            'until' {\n                $counter = 0\n                while (-not (& $condition)) {\n                    $ps = [powershell]::Create()\n                    $ps.RunspacePool = $this.Pool\n                    [void]$ps.AddScript($scriptBlock).AddArgument($counter)\n\n                    $runspaces += [PSCustomObject]@{\n                        Pipe   = $ps\n                        Handle = $ps.BeginInvoke()\n                    }\n\n                    $counter++\n\n                    # Safety: prevent infinite loop runaway\n                    if ($counter -gt 1000000) {\n                        Write-Warning \"Until loop exceeded 1,000,000 iterations. Breaking.\"\n                        break\n                    }\n                }\n            }\n        }\n\n        return $runspaces\n    }\n\n    # Collect results from runspaces with cancellation support\n    hidden [array] CollectResults([array]$runspaces, [string]$cancelPath) {\n        $results = @()\n        $cancelled = $false\n        $lastCheck = [DateTime]::Now\n\n        foreach ($r in $runspaces) {\n            # Periodic cancellation check (every 500ms)\n            if (([DateTime]::Now - $lastCheck).TotalMilliseconds -ge 500) {\n                if ($this.ShouldCancel($cancelPath)) {\n                    $cancelled = $true\n                    Write-Warning \"Job cancellation detected. Stopping result collection.\"\n                    $this.StopAllRunspaces($runspaces)\n                    break\n                }\n                $lastCheck = [DateTime]::Now\n            }\n\n            try {\n                # Collect output from completed runspace\n                $output = $r.Pipe.EndInvoke($r.Handle)\n                $errors = $r.Pipe.Streams.Error\n\n                if ($errors.Count -gt 0) {\n                    # Include errors in results for transparency\n                    foreach ($err in $errors) {\n                        $results += [PSCustomObject]@{\n                            Error = $err.Exception.Message\n                            Type  = 'Error'\n                        }\n                    }\n                }\n                else {\n                    # Add successful output\n                    # PowerShell 7.2+: += handles collections efficiently\n                    if ($null -ne $output) {\n                        if ($output -is [System.Collections.IEnumerable] -and $output -isnot [string]) {\n                            # Flatten collections into results array\n                            $results += @($output)\n                        }\n                        else {\n                            # Single result\n                            $results += $output\n                        }\n                    }\n                }\n            }\n            catch {\n                # Capture any EndInvoke errors\n                $results += [PSCustomObject]@{\n                    Error = $_.Exception.Message\n                    Type  = 'InvocationError'\n                }\n            }\n            finally {\n                # Always dispose runspace\n                $r.Pipe.Dispose()\n            }\n        }\n\n        # If cancelled, add cancellation metadata to results\n        if ($cancelled) {\n            $results += [PSCustomObject]@{\n                Cancelled = $true\n                Message   = 'Job was cancelled by user'\n                Timestamp = (Get-Date).ToString('o')\n            }\n        }\n\n        return $results\n    }\n\n    # Check if cancellation signal file exists\n    hidden [bool] ShouldCancel([string]$cancelPath) {\n        if ([string]::IsNullOrWhiteSpace($cancelPath)) {\n            return $false\n        }\n        return (Test-Path -LiteralPath $cancelPath -PathType Leaf -ErrorAction Ignore)\n    }\n\n    # Stop all running runspaces\n    hidden [void] StopAllRunspaces([array]$runspaces) {\n        foreach ($r in $runspaces) {\n            try {\n                if (-not $r.Handle.IsCompleted) {\n                    $r.Pipe.Stop()\n                }\n            }\n            catch {\n                # Best effort stop - don't throw\n            }\n            finally {\n                $r.Pipe.Dispose()\n            }\n        }\n    }\n}\n\n# ============================================================\n# PUBLIC API (function wrappers)\n# ============================================================\n\nfunction parfor {\n    <#\n    .SYNOPSIS\n        Parallel for loop (0 to N-1).\n\n    .DESCRIPTION\n        Executes a scriptblock N times in parallel, passing iteration index as parameter.\n        Uses internal class engine for performance, exposed via idiomatic function API.\n        Optimized for PowerShell 7.2+ with fast array operations.\n\n    .PARAMETER Count\n        Number of iterations (0 to Count-1)\n\n    .PARAMETER ScriptBlock\n        ScriptBlock to execute for each iteration. Receives iteration index as param($i)\n\n    .PARAMETER ThrottleLimit\n        Maximum number of concurrent runspaces (defaults to processor count)\n\n    .PARAMETER CancelSignalPath\n        Optional path to signal file for cancellation detection\n\n    .EXAMPLE\n        parfor 10 { param($i) \"Iteration $i\" }\n\n    .EXAMPLE\n        parfor 100 { param($i) Get-Process | Select-Object -First 1 } -ThrottleLimit 8\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [ValidateRange(0, [int]::MaxValue)]\n        [int]$Count,\n\n        [Parameter(Mandatory, Position = 1)]\n        [scriptblock]$ScriptBlock,\n\n        [ValidateRange(1, 128)]\n        [int]$ThrottleLimit = [Environment]::ProcessorCount,\n\n        [string]$CancelSignalPath\n    )\n\n    $engine = [ParallelEngine]::new($ThrottleLimit)\n    return $engine.ExecuteParallel('for', $Count, $ScriptBlock, $null, $CancelSignalPath)\n}\n\nfunction parforeach {\n    <#\n    .SYNOPSIS\n        Parallel foreach over a collection.\n\n    .DESCRIPTION\n        Executes a scriptblock for each item in a collection in parallel.\n        Supports pipeline input with begin/process/end blocks.\n        Uses internal class engine for performance.\n        Optimized for PowerShell 7.2+ with efficient array concatenation.\n\n    .PARAMETER Data\n        Collection of items to process\n\n    .PARAMETER ScriptBlock\n        ScriptBlock to execute for each item. Receives item as param($item)\n\n    .PARAMETER ThrottleLimit\n        Maximum number of concurrent runspaces (defaults to processor count)\n\n    .PARAMETER CancelSignalPath\n        Optional path to signal file for cancellation detection\n\n    .EXAMPLE\n        parforeach @(1..10) { param($n) $n * 2 }\n\n    .EXAMPLE\n        $files = Get-ChildItem -Recurse\n        parforeach $files { param($f) Get-FileHash $f.FullName }\n\n    .EXAMPLE\n        Get-ChildItem -Recurse | parforeach { param($f) $f.Length } -ThrottleLimit 4\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0, ValueFromPipeline)]\n        [object[]]$Data,\n\n        [Parameter(Mandatory, Position = 1)]\n        [scriptblock]$ScriptBlock,\n\n        [ValidateRange(1, 128)]\n        [int]$ThrottleLimit = [Environment]::ProcessorCount,\n\n        [string]$CancelSignalPath\n    )\n\n    begin {\n        # PowerShell 7.2+: Array initialization and += are optimized\n        $items = @()\n    }\n\n    process {\n        # Accumulate pipeline input efficiently (7.2+ optimized +=)\n        $items += $Data\n    }\n\n    end {\n        # Use class internally\n        $engine = [ParallelEngine]::new($ThrottleLimit)\n        return $engine.ExecuteParallel('foreach', $items, $ScriptBlock, $null, $CancelSignalPath)\n    }\n}\n\nfunction parrange {\n    <#\n    .SYNOPSIS\n        Parallel iteration over a range.\n\n    .DESCRIPTION\n        Executes a scriptblock for each item in a numeric range in parallel.\n        Uses internal class engine for performance.\n        Optimized for PowerShell 7.2+ performance.\n\n    .PARAMETER Range\n        Numeric range (e.g., 1..100) or array of numbers\n\n    .PARAMETER ScriptBlock\n        ScriptBlock to execute for each item. Receives item as param($n)\n\n    .PARAMETER ThrottleLimit\n        Maximum number of concurrent runspaces (defaults to processor count)\n\n    .PARAMETER CancelSignalPath\n        Optional path to signal file for cancellation detection\n\n    .EXAMPLE\n        parrange (1..100) { param($n) $n * $n }\n\n    .EXAMPLE\n        parrange (10..20) { param($n) \"Number: $n\" } -ThrottleLimit 2\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [object[]]$Range,\n\n        [Parameter(Mandatory, Position = 1)]\n        [scriptblock]$ScriptBlock,\n\n        [ValidateRange(1, 128)]\n        [int]$ThrottleLimit = [Environment]::ProcessorCount,\n\n        [string]$CancelSignalPath\n    )\n\n    $engine = [ParallelEngine]::new($ThrottleLimit)\n    return $engine.ExecuteParallel('range', $Range, $ScriptBlock, $null, $CancelSignalPath)\n}\n\nfunction parwhile {\n    <#\n    .SYNOPSIS\n        Parallel while loop (runs while condition is true).\n\n    .DESCRIPTION\n        Executes a scriptblock repeatedly in parallel while condition evaluates to true.\n        Uses internal class engine for performance.\n        Includes safety limit of 1,000,000 iterations to prevent runaway loops.\n\n    .PARAMETER Condition\n        ScriptBlock condition to evaluate (loop continues while true)\n\n    .PARAMETER ScriptBlock\n        ScriptBlock to execute for each iteration. Receives counter as param($i)\n\n    .PARAMETER ThrottleLimit\n        Maximum number of concurrent runspaces (defaults to processor count)\n\n    .PARAMETER CancelSignalPath\n        Optional path to signal file for cancellation detection\n\n    .EXAMPLE\n        $counter = 0\n        parwhile { $counter -lt 10 } { param($i) $script:counter++; \"Item $i\" }\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [scriptblock]$Condition,\n\n        [Parameter(Mandatory, Position = 1)]\n        [scriptblock]$ScriptBlock,\n\n        [ValidateRange(1, 128)]\n        [int]$ThrottleLimit = [Environment]::ProcessorCount,\n\n        [string]$CancelSignalPath\n    )\n\n    $engine = [ParallelEngine]::new($ThrottleLimit)\n    return $engine.ExecuteParallel('while', 0, $ScriptBlock, $Condition, $CancelSignalPath)\n}\n\nfunction paruntil {\n    <#\n    .SYNOPSIS\n        Parallel until loop (runs until condition is true).\n\n    .DESCRIPTION\n        Executes a scriptblock repeatedly in parallel until condition evaluates to true.\n        Uses internal class engine for performance.\n        Includes safety limit of 1,000,000 iterations to prevent runaway loops.\n\n    .PARAMETER Condition\n        ScriptBlock condition to evaluate (loop continues until true)\n\n    .PARAMETER ScriptBlock\n        ScriptBlock to execute for each iteration. Receives counter as param($i)\n\n    .PARAMETER ThrottleLimit\n        Maximum number of concurrent runspaces (defaults to processor count)\n\n    .PARAMETER CancelSignalPath\n        Optional path to signal file for cancellation detection\n\n    .EXAMPLE\n        $counter = 0\n        paruntil { $counter -ge 10 } { param($i) $script:counter++; \"Item $i\" }\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory, Position = 0)]\n        [scriptblock]$Condition,\n\n        [Parameter(Mandatory, Position = 1)]\n        [scriptblock]$ScriptBlock,\n\n        [ValidateRange(1, 128)]\n        [int]$ThrottleLimit = [Environment]::ProcessorCount,\n\n        [string]$CancelSignalPath\n    )\n\n    $engine = [ParallelEngine]::new($ThrottleLimit)\n    return $engine.ExecuteParallel('until', 0, $ScriptBlock, $Condition, $CancelSignalPath)\n}\n\n# ============================================================\n# HIGH-LEVEL BATCH PROCESSING\n# ============================================================\n\nfunction Invoke-ParallelBatchProcessing {\n    <#\n    .SYNOPSIS\n        Process files in parallel with built-in processing types.\n\n    .DESCRIPTION\n        High-level batch file processor supporting:\n        - FileHash: Calculate file hashes\n        - FileStats: Collect file metadata\n        - TextProcessing: Count lines in text files\n        - LogAnalysis: Count ERROR occurrences in logs\n\n        Uses parforeach internally (class-based engine).\n        Optimized for PowerShell 7.2+ with efficient result collection.\n\n    .PARAMETER InputPath\n        Directory path containing files to process\n\n    .PARAMETER OutputPath\n        Optional path to save results JSON file\n\n    .PARAMETER ProcessingType\n        Type of processing to perform (FileHash, LogAnalysis, FileStats, TextProcessing)\n\n    .PARAMETER ThrottleLimit\n        Maximum number of concurrent runspaces (defaults to processor count)\n\n    .PARAMETER ShowProgress\n        Display progress messages to console\n\n    .EXAMPLE\n        Invoke-ParallelBatchProcessing -InputPath .\\logs -OutputPath .\\results.json -ProcessingType LogAnalysis\n\n    .EXAMPLE\n        Invoke-ParallelBatchProcessing -InputPath .\\src -ProcessingType FileHash -ThrottleLimit 4\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [ValidateScript({ Test-Path $_ })]\n        [string]$InputPath,\n\n        [string]$OutputPath,\n\n        [ValidateSet('FileHash', 'LogAnalysis', 'FileStats', 'TextProcessing')]\n        [string]$ProcessingType = 'FileStats',\n\n        [ValidateRange(1, 128)]\n        [int]$ThrottleLimit = [Environment]::ProcessorCount,\n\n        [switch]$ShowProgress\n    )\n\n    # Get all files\n    $files = Get-ChildItem -Path $InputPath -File -Recurse\n\n    if ($ShowProgress) {\n        Write-Host \"🔍 Found $($files.Count) files to process\" -ForegroundColor Cyan\n        Write-Host \"⚡ Processing with $ThrottleLimit threads...\" -ForegroundColor Green\n    }\n\n    # Process files in parallel using parforeach (which uses class engine)\n    $results = parforeach $files {\n        param($file)\n\n        $result = [ordered]@{\n            File    = $file.FullName\n            Success = $true\n            Error   = $null\n        }\n\n        try {\n            switch ($using:ProcessingType) {\n                'FileHash' {\n                    $hash = Get-FileHash $file.FullName -Algorithm SHA256\n                    $result.Hash = $hash.Hash\n                    $result.Algorithm = $hash.Algorithm\n                }\n                'FileStats' {\n                    $result.Stats = @{\n                        Name          = $file.Name\n                        Length        = $file.Length\n                        LastWriteTime = $file.LastWriteTime.ToString('o')\n                        Extension     = $file.Extension\n                    }\n                }\n                'TextProcessing' {\n                    $lines = (Get-Content $file.FullName -ErrorAction Stop).Count\n                    $result.Lines = $lines\n                }\n                'LogAnalysis' {\n                    $errors = (Get-Content $file.FullName -ErrorAction Stop | Select-String 'ERROR').Count\n                    $result.ErrorCount = $errors\n                }\n            }\n        }\n        catch {\n            $result.Success = $false\n            $result.Error = $_.Exception.Message\n        }\n\n        $result\n    } -ThrottleLimit $ThrottleLimit\n\n    # Save results if OutputPath provided\n    if ($OutputPath) {\n        $results | ConvertTo-Json -Depth 10 | Set-Content $OutputPath -Encoding UTF8\n\n        if ($ShowProgress) {\n            Write-Host \"✅ Results saved to: $OutputPath\" -ForegroundColor Green\n        }\n    }\n\n    return $results\n}\n\n# ============================================================\n# ASYNC JOB SUPPORT (for VS Code Extension)\n# ============================================================\n\nfunction Invoke-ParallelJobWithSignal {\n    <#\n    .SYNOPSIS\n        Execute parallel job with signal file support for async detection.\n\n    .DESCRIPTION\n        Wraps a parallel operation with signal file writing for VS Code extension\n        to detect completion asynchronously. Used internally by the extension.\n\n        Supports cancellation via cancel signal file monitoring.\n\n        Optimized for PowerShell 7.2+ with efficient JSONL writing.\n\n    .PARAMETER ScriptBlock\n        The parallel operation to execute (should use parforeach/parfor/etc)\n\n    .PARAMETER SignalPath\n        Path where completion signal JSON will be written\n\n    .PARAMETER ResultPath\n        Path where results JSONL will be written\n\n    .PARAMETER CancelSignalPath\n        Path to monitor for cancellation signal (optional)\n\n    .PARAMETER JobId\n        Unique job identifier\n    #>\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [scriptblock]$ScriptBlock,\n\n        [Parameter(Mandatory)]\n        [string]$SignalPath,\n\n        [Parameter(Mandatory)]\n        [string]$ResultPath,\n\n        [string]$CancelSignalPath,\n\n        [Parameter(Mandatory)]\n        [string]$JobId\n    )\n\n    try {\n        # Check for cancellation before starting\n        if (Test-Path -LiteralPath $CancelSignalPath -PathType Leaf -ErrorAction Ignore) {\n            $cancelSignal = @{\n                jobId     = $JobId\n                status    = 'cancelled'\n                timestamp = (Get-Date).ToString('o')\n                message   = 'Job cancelled before execution'\n            }\n\n            $cancelSignal | ConvertTo-Json | Set-Content $SignalPath -Encoding UTF8\n            return\n        }\n\n        # Execute the parallel operation\n        $results = & $ScriptBlock\n\n        # Check if cancellation occurred during execution\n        $wasCancelled = $results | Where-Object { $_.Cancelled -eq $true }\n\n        # Write results to JSONL (PowerShell 7.2+ optimized)\n        # Using pipeline for memory efficiency with large result sets\n        $results | ForEach-Object {\n            $_ | ConvertTo-Json -Compress -Depth 10\n        } | Set-Content $ResultPath -Encoding UTF8\n\n        # Write signal file (phone home to extension)\n        if ($wasCancelled) {\n            $signal = @{\n                jobId      = $JobId\n                status     = 'cancelled'\n                timestamp  = (Get-Date).ToString('o')\n                resultPath = $ResultPath\n                itemCount  = ($results | Where-Object { -not $_.Cancelled }).Count\n                message    = 'Job was cancelled during execution'\n            }\n        }\n        else {\n            $signal = @{\n                jobId      = $JobId\n                status     = 'completed'\n                timestamp  = (Get-Date).ToString('o')\n                resultPath = $ResultPath\n                itemCount  = $results.Count\n            }\n        }\n\n        $signal | ConvertTo-Json | Set-Content $SignalPath -Encoding UTF8\n\n        return $results\n    }\n    catch {\n        # Write error signal\n        $errorSignal = @{\n            jobId     = $JobId\n            status    = 'failed'\n            timestamp = (Get-Date).ToString('o')\n            error     = $_.Exception.Message\n        }\n\n        $errorSignal | ConvertTo-Json | Set-Content $SignalPath -Encoding UTF8\n        throw\n    }\n}\n\n# ============================================================\n# MODULE EXPORTS\n# ============================================================\n\nExport-ModuleMember -Function @(\n    'parfor',\n    'parforeach',\n    'parrange',\n    'parwhile',\n    'paruntil',\n    'Invoke-ParallelBatchProcessing',\n    'Invoke-ParallelJobWithSignal'\n) |
7 | powershell/supervisor-host-launch.ps1 | {1536 145 0.1621 4.8953} | 1606 | #Requires -Version 7.2\n\n<#\n.SYNOPSIS\n    Starts the persistent supervisor host (JSON-RPC server).\n\n.DESCRIPTION\n    Entry point that spawns the persistent supervisor process.\n    Handles JSON-RPC communication with TypeScript extension.\n\n    COMMUNICATION: JSON-RPC via stdin/stdout\n    LIFECYCLE: Persistent (lives until shutdown request)\n    SPAWNED BY: TypeScript extension on activation\n\n.NOTES\n    Domain: Supervisor\n    Role: Launcher (Persistent Server)\n    Pattern: JSON-RPC Server\n#>\n\n[CmdletBinding()]\nparam(\n    [Parameter(Mandatory)]\n    [string]$WorkspacePath,\n\n    [Parameter(Mandatory)]\n    [string]$ExtensionPath\n)\n\n# ============================================================\n# MODULE IMPORT\n# ============================================================\n\n$ErrorActionPreference = 'Stop'\n$DebugPreference = 'Continue'\n\n$moduleName = \"supervisor-host\"\n$modulePath = Join-Path $PSScriptRoot \"$moduleName.psm1\"\n\ntry {\n    Import-Module $modulePath -Force -ErrorAction Stop\n}\ncatch {\n    Write-Error \"Failed to import module $moduleName : $_\"\n    exit 1\n}\n\n# ============================================================\n# INITIALIZATION & STARTUP\n# ============================================================\n\ntry {\n    # Initialize the worker\n    Initialize-CopilotWorker -WorkspacePath $WorkspacePath -ExtensionPath $ExtensionPath\n\n    # Start the JSON-RPC message loop (blocks until shutdown)\n    Start-JsonRpcLoop\n}\ncatch {\n    Write-Error \"Copilot Worker failed: $_\"\n    Write-Error $_.ScriptStackTrace\n    exit 1\n} |
