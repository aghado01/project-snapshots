idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
11 | typescript/copilot-toolbelt.ts | {5863 674 0.1556 4.8565} | 6084 | /**\n * copilot-toolbelt.ts\n *\n * Main export point for all Copilot toolbelt utilities.\n * Consolidates: jso-blackbelt, power-tools, safe-shell\n */\n\n// # Copilot-Toolbelt Modules Overview\n\n// * JSO Blackbelt Tools:\n//  * - streamJsonl: Memory-efficient JSONL streaming\n//  * - JsonlBinaryIndex: Fast random access with binary indexing\n//  * - BloomFilterClient: Probabilistic deduplication\n//  * - HashUtils: FNV-1a, SHA256, rolling hashes\n//  * - JsonUtils: Canonical JSON, merge, depth calculation\n//  * - JsoProcessor: High-level JSONL orchestration\n//  *\n//  * Power Tools:\n//  * - rgSearch: ripgrep pattern search with JSON output\n//  * - fdList: fd file listing\n//  *\n//  * Safe Shell Tools:\n//  * - sendToInteractiveShell: Fire-and-forget terminal commands\n//  * - readConsoleDump: Read from JSONL console dumps\n//  * - getLastCommand: Get most recent command result\n//  * - getErrorCommands: Find failed commands\n//  *\n//  * Tool-Ready Wrappers:\n//  * - readJsonlWindow: Paginated JSONL reading\n//  * - canonicalJson: Deterministic JSON serialization\n//  * - mergeJson: Shallow object merge\n//  * - findEventsByHash: Event search by hash\n//  * - getRecentSupervision: Recent supervision events\n//  */\n\n// ============================================================================\n// JSO BLACKBELT - JSONL/JSON Utilities\n// ============================================================================\n\nexport {\n  // Bloom Filters\n  BloomFilterClient,\n  deduplicateJsonl,\n  getHash,\n  getRollingHashes,\n  // Hashing\n  HashUtils,\n  JsonlBinaryIndex,\n  JsonUtils,\n\n  // High-level Processing\n  JsoProcessor,\n  splitJsonlWithProcessor,\n  // Streaming & Indexing\n  streamJsonl,\n  type BloomFilter,\n  // Types\n  type BloomFilterOptions,\n  type EncoderOptions,\n  type EncoderType,\n  type JsonlBinaryIndexOptions,\n  type JsonlIndexEntry,\n  type JsonlStreamOptions,\n  type JsoProcessorConfig,\n  type ProcessResult,\n  type ShardingOptions,\n} from \"./jso-blackbelt\";\n\n// ============================================================================\n// POWER TOOLS - Search & Discovery\n// ============================================================================\n\nexport {\n  fdAdvancedList,\n  // Fd file discovery functions\n  fdList,\n  rgAdvancedSearch,\n  // Ripgrep search functions\n  rgSearch,\n  type FdAdvancedListArgs,\n  // Types - Fd\n  type FdListArgs,\n  type FdResult,\n  type RgAdvancedSearchArgs,\n  type RgDetailedHit,\n  // Types - Ripgrep\n  type RgHit,\n  type RgSearchArgs,\n} from \"./power-tools\";\n\n// ============================================================================\n// PS-LINTER - PowerShell Static Analysis\n// ============================================================================\n\nexport {\n  formatLintResults,\n  lintPowerShell,\n  type PSLintArgs,\n  type PSLintResult,\n} from \"./linter-ps\";\n\n// ============================================================================\n// SAFE SHELL - Terminal Commands\n// ============================================================================\n\nexport {\n  getDumpInfo,\n  getErrorCommands,\n  getLastCommand,\n  getRecentConsoleRecords,\n  getTodaysDumpPath,\n  readConsoleDump,\n  sendToInteractiveShell,\n  type ConsoleDumpInfo,\n  type ConsoleReadArgs,\n  type ConsoleRecord,\n  type ShellCommandArgs,\n  type ShellCommandResult,\n} from \"./safe-shell\";\n\n// ============================================================================\n// TOOL-READY WRAPPERS - Tool Registration\n// ============================================================================\n\nimport { JsonUtils, streamJsonl } from \"./jso-blackbelt\";\n\n/**\n * Read a window of JSONL records (tool-friendly wrapper)\n *\n * @param filePath - Path to JSONL file\n * @param skip - Number of records to skip (default: 0)\n * @param take - Number of records to take (default: 100)\n * @returns Array of parsed JSON records\n */\nexport async function readJsonlWindow<T = any>(\n  filePath: string,\n  skip = 0,\n  take = 100\n): Promise<T[]> {\n  const results: T[] = [];\n  for await (const entry of streamJsonl<T>({ filePath, skip, take })) {\n    results.push(entry);\n  }\n  return results;\n}\n\n/**\n * Get canonical JSON representation (tool-friendly wrapper)\n *\n * @param obj - Object to canonicalize\n * @returns Deterministic JSON string\n */\nexport function canonicalJson(obj: any): string {\n  return JsonUtils.toCanonical(obj);\n}\n\n/**\n * Merge two JSON objects (tool-friendly wrapper)\n *\n * @param a - First object\n * @param b - Second object (takes precedence)\n * @returns Merged object\n */\nexport function mergeJson(a: any, b: any): any {\n  return JsonUtils.merge(a, b);\n}\n\n/**\n * Find events by hash in JSONL file\n *\n * @param filePath - Path to JSONL file\n * @param hash - Hash value to search for\n * @returns Array of matching events\n */\nexport async function findEventsByHash(\n  filePath: string,\n  hash: string\n): Promise<any[]> {\n  const allEvents = await readJsonlWindow(filePath, 0, 0); // 0 = no limit\n  return allEvents.filter((e) => e.hash === hash);\n}\n\n/**\n * Get recent supervision events from session log\n *\n * @param sessionLogPath - Path to session log JSONL file\n * @param maxResults - Maximum number of results (default: 10)\n * @returns Array of recent supervision events\n */\nexport async function getRecentSupervision(\n  sessionLogPath: string,\n  maxResults: number = 10\n): Promise<any[]> {\n  const events = await readJsonlWindow(sessionLogPath, 0, 100);\n  return events.filter((e) => e.kind === \"supervision\").slice(-maxResults);\n}\n\n// ============================================================================\n// STRUCTURAL LINTER - TypeScript Syntax Validation\n// ============================================================================\n\nexport {\n  detectUnbalancedBraces,\n  formatLintResults as formatStructuralLintResults,\n  lintTypeScript,\n  type LintTypeScriptOptions,\n  type StructuralLintResult,\n} from \"./linter-ts\"; |
12 | typescript/cybernetics-bridge.ts | {3233 305 0.1695 5.1243} | 3382 | import { execFileSync } from \"child_process\";\nimport * as fs from \"fs\";\nimport * as path from \"path\";\nimport * as vscode from \"vscode\";\n\ninterface InvokeCyberneticsOptions {\n  forceArray?: boolean;\n}\n\nconst modulePathCache = new Map<string, string | null>();\n\nfunction encodePowerShellCommand(script: string): string {\n  return Buffer.from(script, \"utf16le\").toString(\"base64\");\n}\n\nfunction getCandidateModulePaths(moduleFile: string): string[] {\n  const candidates: string[] = [];\n  const envRoots = [\n    process.env.CYBERNETICS_ROOT,\n    process.env.COPILOT_CORE_SRC_PATH,\n    process.env.COPILOT_CORE_PATH,\n  ].filter((value): value is string => Boolean(value));\n\n  for (const root of envRoots) {\n    candidates.push(path.join(root, moduleFile));\n    candidates.push(path.join(root, \"cybernetics\", moduleFile));\n  }\n\n  for (const folder of vscode.workspace.workspaceFolders ?? []) {\n    const folderPath = folder.uri.fsPath;\n    candidates.push(path.join(folderPath, moduleFile));\n    candidates.push(path.join(folderPath, \"cybernetics\", moduleFile));\n    candidates.push(path.join(folderPath, \"..\", \"cybernetics\", moduleFile));\n  }\n\n  return Array.from(\n    new Set(candidates.map((candidate) => path.resolve(candidate)))\n  );\n}\n\nexport function resolveCyberneticsModule(moduleFile: string): string | null {\n  if (modulePathCache.has(moduleFile)) {\n    return modulePathCache.get(moduleFile) ?? null;\n  }\n\n  const resolvedPath =\n    getCandidateModulePaths(moduleFile).find((candidate) =>\n      fs.existsSync(candidate)\n    ) ?? null;\n\n  modulePathCache.set(moduleFile, resolvedPath);\n  return resolvedPath;\n}\n\nexport function invokeCybernetics<T = unknown>(\n  moduleFile: string,\n  functionName: string,\n  args: Record<string, unknown> = {},\n  options: InvokeCyberneticsOptions = {}\n): T | null {\n  const modulePath = resolveCyberneticsModule(moduleFile);\n  if (!modulePath) {\n    return null;\n  }\n\n  const payload = Buffer.from(JSON.stringify(args), \"utf8\").toString(\"base64\");\n  const escapedModulePath = modulePath.replace(/'/g, \"''\");\n  const escapedFunctionName = functionName.replace(/'/g, \"''\");\n  const forceArrayScript = options.forceArray ? \"$result = @($result)\" : \"\";\n\n  const script = `\n$ErrorActionPreference = 'Stop'\n$payloadJson = [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String('${payload}'))\n$parameters = if ([string]::IsNullOrWhiteSpace($payloadJson)) { @{} } else { $payloadJson | ConvertFrom-Json -AsHashtable }\n$result = & {\n    Import-Module '${escapedModulePath}' -Force -DisableNameChecking -ErrorAction Stop\n    & '${escapedFunctionName}' @parameters\n} 3>$null 4>$null 5>$null 6>$null\nif ($null -eq $result) {\n    return\n}\n${forceArrayScript}\n$result | ConvertTo-Json -Depth 16 -Compress\n  `.trim();\n\n  try {\n    const output = execFileSync(\n      \"pwsh\",\n      [\"-NoProfile\", \"-NonInteractive\", \"-EncodedCommand\", encodePowerShellCommand(script)],\n      {\n        encoding: \"utf8\",\n        maxBuffer: 10 * 1024 * 1024,\n      }\n    ).trim();\n\n    if (!output) {\n      return null;\n    }\n\n    return JSON.parse(output) as T;\n  } catch (error) {\n    console.warn(\n      `[CyberneticsBridge] ${functionName} via ${moduleFile} failed:`,\n      error\n    );\n    return null;\n  }\n} |
13 | typescript/job-store.ts | {10320 1091 0.2319 4.9751} | 10834 | /**\n * Persistent Job Store Implementation\n *\n * Adds write/rehydrate functionality to parallel-tools.ts for jobs.jsonl\n */\n\nimport * as fs from \"fs\";\nimport * as path from \"path\";\nimport { invokeCybernetics } from \"./cybernetics-bridge\";\nimport type { JobStatus, ParallelJob } from \"./parallel-tools\";\n\nconst DEFAULT_TEMP_DIR = path.join(\n  process.env.TEMP || \"/tmp\",\n  \"vscode-copilot-jobs\"\n);\nconst JOBS_FILE = path.join(DEFAULT_TEMP_DIR, \"jobs.jsonl\");\n\n/**\n * Persist a job to the jobs.jsonl store\n */\nexport async function persistJob(job: ParallelJob): Promise<void> {\n  const persisted = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Write-CyberneticJobStoreRecord\",\n    {\n      Job: job,\n      JobRoot: DEFAULT_TEMP_DIR,\n    }\n  );\n\n  if (persisted) {\n    console.log(`[JobStore] Persisted job ${job.jobId}`);\n    return;\n  }\n\n  try {\n    // Ensure directory exists\n    if (!fs.existsSync(DEFAULT_TEMP_DIR)) {\n      fs.mkdirSync(DEFAULT_TEMP_DIR, { recursive: true });\n    }\n\n    // Append job as JSONL line\n    const line = JSON.stringify(job) + \"\\n\";\n    fs.appendFileSync(JOBS_FILE, line, \"utf-8\");\n\n    console.log(`[JobStore] Persisted job ${job.jobId}`);\n  } catch (error) {\n    console.error(`[JobStore] Failed to persist job ${job.jobId}:`, error);\n  }\n}\n\n/**\n * Update an existing job in the store\n */\nexport async function updateJob(job: ParallelJob): Promise<void> {\n  const persisted = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Write-CyberneticJobStoreRecord\",\n    {\n      Job: job,\n      JobRoot: DEFAULT_TEMP_DIR,\n    }\n  );\n\n  if (persisted) {\n    console.log(`[JobStore] Updated job ${job.jobId}`);\n    return;\n  }\n\n  try {\n    if (!fs.existsSync(JOBS_FILE)) {\n      // File doesn't exist, just persist the job\n      await persistJob(job);\n      return;\n    }\n\n    // Read all jobs\n    const content = fs.readFileSync(JOBS_FILE, \"utf-8\");\n    const jobs = content\n      .split(\"\\n\")\n      .filter((line) => line.trim())\n      .map((line) => JSON.parse(line) as ParallelJob);\n\n    // Update the job\n    const index = jobs.findIndex((j) => j.jobId === job.jobId);\n    if (index >= 0) {\n      jobs[index] = job;\n    } else {\n      jobs.push(job);\n    }\n\n    // Rewrite file\n    const newContent = jobs.map((j) => JSON.stringify(j)).join(\"\\n\") + \"\\n\";\n    fs.writeFileSync(JOBS_FILE, newContent, \"utf-8\");\n\n    console.log(`[JobStore] Updated job ${job.jobId}`);\n  } catch (error) {\n    console.error(`[JobStore] Failed to update job ${job.jobId}:`, error);\n  }\n}\n\n/**\n * Rehydrate jobs from persistent store\n * Returns map of jobId → ParallelJob\n */\nexport async function rehydrateJobs(): Promise<Map<string, ParallelJob>> {\n  const jobs = new Map<string, ParallelJob>();\n\n  const persistedJobs = invokeCybernetics<unknown>(\n    \"CyberneticAutomata.psm1\",\n    \"Read-CyberneticJobStore\",\n    {\n      JobRoot: DEFAULT_TEMP_DIR,\n    },\n    { forceArray: true }\n  );\n\n  if (persistedJobs) {\n    const jobArray = (Array.isArray(persistedJobs)\n      ? persistedJobs\n      : [persistedJobs]) as ParallelJob[];\n\n    console.log(`[JobStore] Rehydrating ${jobArray.length} jobs...`);\n\n    for (const job of jobArray) {\n      job.startTime = new Date(job.startTime);\n      if (job.endTime) {\n        job.endTime = new Date(job.endTime);\n      }\n\n      if (job.status === \"running\") {\n        await checkOrphanedJob(job);\n      }\n\n      jobs.set(job.jobId, job);\n    }\n\n    console.log(`[JobStore] Rehydrated ${jobs.size} jobs`);\n    return jobs;\n  }\n\n  try {\n    if (!fs.existsSync(JOBS_FILE)) {\n      console.log(\"[JobStore] No jobs.jsonl file found, starting fresh\");\n      return jobs;\n    }\n\n    const content = fs.readFileSync(JOBS_FILE, \"utf-8\");\n    const jobArray = content\n      .split(\"\\n\")\n      .filter((line) => line.trim())\n      .map((line) => JSON.parse(line) as ParallelJob);\n\n    console.log(`[JobStore] Rehydrating ${jobArray.length} jobs...`);\n\n    for (const job of jobArray) {\n      // Restore Date objects\n      job.startTime = new Date(job.startTime);\n      if (job.endTime) {\n        job.endTime = new Date(job.endTime);\n      }\n\n      // Check for orphaned jobs (running when extension closed)\n      if (job.status === \"running\") {\n        await checkOrphanedJob(job);\n      }\n\n      jobs.set(job.jobId, job);\n    }\n\n    console.log(`[JobStore] Rehydrated ${jobs.size} jobs`);\n    console.log(`[JobStore] Running: ${getJobCountByStatus(jobs, \"running\")}`);\n    console.log(\n      `[JobStore] Completed: ${getJobCountByStatus(jobs, \"completed\")}`\n    );\n    console.log(`[JobStore] Failed: ${getJobCountByStatus(jobs, \"failed\")}`);\n    console.log(\n      `[JobStore] Cancelled: ${getJobCountByStatus(jobs, \"cancelled\")}`\n    );\n\n    return jobs;\n  } catch (error) {\n    console.error(\"[JobStore] Failed to rehydrate jobs:\", error);\n    return jobs;\n  }\n}\n\n/**\n * Check if a \"running\" job is actually orphaned\n * Updates job status based on signal files\n */\nasync function checkOrphanedJob(job: ParallelJob): Promise<void> {\n  try {\n    // Check if completion signal exists\n    if (fs.existsSync(job.signalPath)) {\n      const signal = JSON.parse(fs.readFileSync(job.signalPath, \"utf-8\"));\n      job.status = signal.status as JobStatus;\n      job.endTime = new Date(signal.timestamp);\n\n      if (signal.error) {\n        job.error = signal.error;\n      }\n\n      console.log(\n        `[JobStore] Job ${job.jobId} actually ${job.status} (signal file found)`\n      );\n    } else {\n      // No signal file, job was orphaned\n      job.status = \"failed\";\n      job.error = \"Orphaned job (extension restarted before completion)\";\n      job.endTime = new Date();\n\n      console.log(`[JobStore] Job ${job.jobId} marked as orphaned`);\n    }\n  } catch (error) {\n    // Error reading signal, mark as failed\n    job.status = \"failed\";\n    job.error = `Failed to check orphan status: ${error}`;\n    job.endTime = new Date();\n\n    console.error(\n      `[JobStore] Error checking orphaned job ${job.jobId}:`,\n      error\n    );\n  }\n}\n\n/**\n * Clean up orphaned signal files\n * Removes signal/result files for completed jobs older than retention period\n */\nexport async function cleanupOrphanedFiles(\n  retentionHours: number = 24\n): Promise<number> {\n  const cleanupResult = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Remove-CyberneticStaleJobArtifacts\",\n    {\n      JobRoot: DEFAULT_TEMP_DIR,\n      RetentionHours: retentionHours,\n    }\n  );\n\n  if (cleanupResult && typeof cleanupResult.removedCount === \"number\") {\n    if (cleanupResult.removedCount > 0) {\n      console.log(\n        `[JobStore] Cleaned up ${cleanupResult.removedCount} orphaned files`\n      );\n    }\n    return cleanupResult.removedCount;\n  }\n\n  let cleaned = 0;\n\n  try {\n    if (!fs.existsSync(DEFAULT_TEMP_DIR)) {\n      return 0;\n    }\n\n    const now = Date.now();\n    const retentionMs = retentionHours * 60 * 60 * 1000;\n\n    const files = fs.readdirSync(DEFAULT_TEMP_DIR);\n\n    for (const file of files) {\n      const filePath = path.join(DEFAULT_TEMP_DIR, file);\n      const stats = fs.statSync(filePath);\n\n      // Check if file is old enough\n      if (now - stats.mtimeMs > retentionMs) {\n        fs.unlinkSync(filePath);\n        cleaned++;\n        console.log(`[JobStore] Cleaned up old file: ${file}`);\n      }\n    }\n\n    if (cleaned > 0) {\n      console.log(`[JobStore] Cleaned up ${cleaned} orphaned files`);\n    }\n  } catch (error) {\n    console.error(\"[JobStore] Error during cleanup:\", error);\n  }\n\n  return cleaned;\n}\n\n/**\n * Compact jobs.jsonl file by removing completed/failed/cancelled jobs older than retention\n */\nexport async function compactJobStore(\n  retentionHours: number = 24\n): Promise<void> {\n  const compacted = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Compress-CyberneticJobStore\",\n    {\n      JobRoot: DEFAULT_TEMP_DIR,\n      RetentionHours: retentionHours,\n    }\n  );\n\n  if (compacted) {\n    const removedCount =\n      typeof compacted.removedCount === \"number\" ? compacted.removedCount : 0;\n    const keptCount =\n      typeof compacted.keptCount === \"number\" ? compacted.keptCount : 0;\n\n    if (removedCount > 0) {\n      console.log(\n        `[JobStore] Compacted store: removed ${removedCount} old jobs, kept ${keptCount}`\n      );\n    }\n    return;\n  }\n\n  try {\n    if (!fs.existsSync(JOBS_FILE)) {\n      return;\n    }\n\n    const content = fs.readFileSync(JOBS_FILE, \"utf-8\");\n    const jobs = content\n      .split(\"\\n\")\n      .filter((line) => line.trim())\n      .map((line) => JSON.parse(line) as ParallelJob);\n\n    const now = Date.now();\n    const retentionMs = retentionHours * 60 * 60 * 1000;\n\n    // Keep only recent jobs or running jobs\n    const keptJobs = jobs.filter((job) => {\n      if (job.status === \"running\" || job.status === \"pending\") {\n        return true; // Always keep active jobs\n      }\n\n      const jobTime = job.endTime\n        ? new Date(job.endTime).getTime()\n        : new Date(job.startTime).getTime();\n      return now - jobTime < retentionMs;\n    });\n\n    const removedCount = jobs.length - keptJobs.length;\n\n    if (removedCount > 0) {\n      const newContent =\n        keptJobs.map((j) => JSON.stringify(j)).join(\"\\n\") + \"\\n\";\n      fs.writeFileSync(JOBS_FILE, newContent, \"utf-8\");\n\n      console.log(\n        `[JobStore] Compacted store: removed ${removedCount} old jobs, kept ${keptJobs.length}`\n      );\n    }\n  } catch (error) {\n    console.error(\"[JobStore] Error compacting store:\", error);\n  }\n}\n\n/**\n * Get count of jobs by status\n */\nfunction getJobCountByStatus(\n  jobs: Map<string, ParallelJob>,\n  status: JobStatus\n): number {\n  return Array.from(jobs.values()).filter((j) => j.status === status).length;\n}\n\n/**\n * Delete all jobs from store (for testing)\n */\nexport async function clearJobStore(): Promise<void> {\n  const cleared = invokeCybernetics<Record<string, unknown>>(\n    \"CyberneticAutomata.psm1\",\n    \"Clear-CyberneticJobStore\",\n    {\n      JobRoot: DEFAULT_TEMP_DIR,\n    }\n  );\n\n  if (cleared?.cleared) {\n    console.log(\"[JobStore] Cleared job store\");\n    return;\n  }\n\n  try {\n    if (fs.existsSync(JOBS_FILE)) {\n      fs.unlinkSync(JOBS_FILE);\n      console.log(\"[JobStore] Cleared job store\");\n    }\n  } catch (error) {\n    console.error(\"[JobStore] Error clearing store:\", error);\n  }\n} |
14 | typescript/jso-blackbelt.d.ts | {7500 751 0.1675 4.8068} | 7867 | /**\n * jso-blackbelt.d.ts\n *\n * Type definitions for JSO Blackbelt - TypeScript wrapper for jso-engine.\n * Provides type-safe interface to PowerShell-based JSO primitives.\n */\n\n// ============================================================================\n// POWERSHELL BRIDGE\n// ============================================================================\n\nexport interface PowerShellResult<T = any> {\n  success: boolean;\n  data?: T;\n  error?: string;\n  stderr?: string;\n}\n\n// ============================================================================\n// BLOOM FILTER (Deduplication)\n// ============================================================================\n\nexport interface BloomFilterOptions {\n  expectedItems: number;\n  falsePositiveRate?: number;\n}\n\nexport interface BloomFilter {\n  size: number;\n  hashCount: number;\n  itemCount: number;\n  expectedItems: number;\n  falsePositiveRate: number;\n  _psState?: string;\n}\n\nexport class BloomFilterClient {\n  constructor(options: BloomFilterOptions);\n\n  /**\n   * Add item to bloom filter\n   */\n  add(item: string): void;\n\n  /**\n   * Test if item might be in filter (no false negatives)\n   */\n  test(item: string): boolean;\n\n  /**\n   * Get filter statistics\n   */\n  getStats(): {\n    size: number;\n    hashCount: number;\n    itemCount: number;\n    expectedItems: number;\n    falsePositiveRate: number;\n  };\n}\n\n// ============================================================================\n// JSONL STREAMING (Memory-Efficient)\n// ============================================================================\n\nexport interface JsonlStreamOptions {\n  filePath: string;\n  skip?: number;\n  take?: number;\n}\n\n/**\n * Stream JSONL file with constant memory usage\n *\n * @param options - Streaming options\n * @returns AsyncGenerator yielding parsed JSON entries\n */\nexport function streamJsonl<T = any>(\n  options: JsonlStreamOptions\n): AsyncGenerator<T, void, unknown>;\n\n// ============================================================================\n// BINARY INDEXING (Fast Random Access)\n// ============================================================================\n\nexport interface JsonlIndexEntry {\n  lineNumber: number;\n  byteOffset: number;\n}\n\nexport interface JsonlBinaryIndexOptions {\n  jsonlPath: string;\n  indexPath?: string;\n}\n\nexport class JsonlBinaryIndex {\n  constructor(options: JsonlBinaryIndexOptions);\n\n  /**\n   * Build binary index file (one-time operation)\n   */\n  build(): Promise<void>;\n\n  /**\n   * Read entry by line number (0-based)\n   */\n  readByIndex<T = any>(lineIndex: number): Promise<T>;\n\n  /**\n   * Check if index exists\n   */\n  exists(): boolean;\n}\n\n// ============================================================================\n// HASHING (Content Addressing)\n// ============================================================================\n\nexport class HashUtils {\n  /**\n   * Get FNV-1a path hash (deterministic)\n   */\n  static getPathHash(filePath: string): string;\n\n  /**\n   * Get SHA256 file hash\n   */\n  static getFileSha256(filePath: string): string;\n\n  /**\n   * Get rolling content hash with boundaries\n   */\n  static getContentHash(content: string): {\n    hash: number;\n    boundaries: number[];\n    chunkCount: number;\n  };\n}\n\n// ============================================================================\n// JSON UTILITIES\n// ============================================================================\n\nexport class JsonUtils {\n  /**\n   * Convert to canonical JSON (deterministic serialization)\n   */\n  static toCanonical(obj: any): string;\n\n  /**\n   * Merge JSON objects (shallow merge, right precedence)\n   */\n  static merge<T = any>(...objects: any[]): T;\n\n  /**\n   * Get JSON nesting depth\n   */\n  static getDepth(obj: any): number;\n}\n\n// ============================================================================\n// JSO-SCRIPTS ORCHESTRATOR (High-Level Operations)\n// ============================================================================\n\nexport type EncoderType =\n  | \"Identity\"\n  | \"Minifier\"\n  | \"Canonical\"\n  | \"WhitespaceCompact\"\n  | \"Preprocess\"\n  | \"JsonPath\"\n  | \"Tabular\"\n  | \"Custom\";\n\nexport interface EncoderOptions {\n  mode?: \"minimal\" | \"standard\" | \"aggressive\";\n  profile?: \"lossless\" | \"conservative\" | \"compact\";\n  zoneAware?: boolean;\n  groupNewlines?: boolean;\n  groupTabs?: boolean;\n  mapSingleSpaces?: boolean;\n  compactFields?: string[];\n  jsonPath?: string;\n  tableColumns?: string[];\n  autoDiscoverColumns?: boolean;\n}\n\nexport interface JsoProcessorConfig {\n  jsonDepth?: number;\n  strictClean?: boolean;\n  postMinify?: boolean;\n  sortOutput?: boolean;\n  useParallelism?: boolean;\n  parallelThrottle?: number;\n  bufferSize?: number;\n  showProgress?: boolean;\n  enableDeduplication?: boolean;\n  deduplicationKeyPath?: string;\n  falsePositiveRate?: number;\n}\n\nexport interface ShardingOptions {\n  recordsPerFile: number;\n  namingPattern?: string;\n}\n\nexport interface ProcessResult<T = any> {\n  processed: T[];\n  failed: any[];\n  stats: {\n    totalFiles: number;\n    successful: number;\n    failed: number;\n    processingTime: number;\n  };\n}\n\n/**\n * JsoProcessor - High-level JSONL orchestration with fluent API\n */\nexport class JsoProcessor {\n  /**\n   * Configure content encoder\n   */\n  withEncoder(type: EncoderType, options?: EncoderOptions): this;\n\n  /**\n   * Set output format\n   */\n  withFormat(format: string): this;\n\n  /**\n   * Enable file sharding\n   */\n  withSharding(options: ShardingOptions): this;\n\n  /**\n   * Configure processor options\n   */\n  withConfig(config: JsoProcessorConfig): this;\n\n  /**\n   * Enable parallel processing\n   */\n  enableParallelism(throttleLimit?: number): this;\n\n  /**\n   * Enable deduplication with bloom filter\n   */\n  enableDeduplication(\n    expectedItems?: number,\n    falsePositiveRate?: number,\n    keyPath?: string\n  ): this;\n\n  /**\n   * Process JSONL file with configured settings\n   */\n  processJsonlFile<T = any>(\n    filePath: string,\n    processor?: (entry: any, config: JsoProcessorConfig) => any\n  ): Promise<T[]>;\n\n  /**\n   * Split JSONL file into shards\n   */\n  splitJsonl(inputPath: string, outputDirectory: string): Promise<string[]>;\n}\n\n// ============================================================================\n// CONVENIENCE FUNCTIONS\n// ============================================================================\n\n/**\n * Deduplicate JSONL file using bloom filter\n */\nexport function deduplicateJsonl(\n  inputPath: string,\n  outputPath: string,\n  keyPath?: string,\n  falsePositiveRate?: number\n): Promise<{\n  originalRecords: number;\n  deduplicatedRecords: number;\n  duplicatesRemoved: number;\n}>;\n\n/**\n * Split JSONL file with optional processing\n */\nexport function splitJsonlWithProcessor(\n  inputPath: string,\n  outputDirectory: string,\n  recordsPerFile: number,\n  encoderType?: EncoderType,\n  encoderOptions?: EncoderOptions\n): Promise<any[]>;\n\n/**\n * Get hash of content using specified algorithm\n */\nexport function getHash(\n  content: string,\n  algorithm?: \"FNV1a\" | \"SHA256\"\n): string;\n\n/**\n * Get rolling hashes for content with specified window size\n */\nexport function getRollingHashes(\n  content: string,\n  windowSize: number\n): Array<{\n  index: number;\n  hash: number;\n}>;\n\n/**\n * Get hash of content using specified algorithm\n */\nexport function getHash(\n  content: string,\n  algorithm?: \"FNV1a\" | \"SHA256\"\n): string;\n\n/**\n * Get rolling hashes with window size\n */\nexport function getRollingHashes(\n  content: string,\n  windowSize: number\n): Array<{ index: number; hash: number }>; |
