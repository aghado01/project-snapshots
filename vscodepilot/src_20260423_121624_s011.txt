idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
21 | typescript/power-tools.ts | {16795 2225 0.2009 5.0243} | 17593 | /**\n * power-tools.ts\n *\n * High-performance code search and file discovery for VS Code Copilot.\n *\n * Tools:\n * - fdAdvancedList  → fast file and directory discovery\n * - rgAdvancedSearch → fast code/pattern search with rich match data\n *\n * WHEN TO USE THESE TOOLS\n * -----------------------\n * Use these instead of built-in file/grep search when:\n * - The workspace is large (monorepo, 10k+ files).\n * - You need precise match details (offsets, context) for refactors.\n * - You want to avoid scanning node_modules, dist, etc. unnecessarily.\n *\n * fdAdvancedList (FILE DISCOVERY)\n * -------------------------------\n * Use fdAdvancedList when the task is about files/paths, not contents:\n * - “Find all TypeScript files in src/”\n * - “List config files at the repo root”\n * - “Show candidate files before searching inside them”\n *\n * Strategy:\n * - Prefer filters like cwd, extensions, includeGlobs, excludeDirs, maxDepth.\n * - Use it first to narrow candidates, then call rgAdvancedSearch on them.\n * - Avoid reading file contents if you only need counts or paths.\n *\n * rgAdvancedSearch (CODE SEARCH)\n * ------------------------------\n * Use rgAdvancedSearch for content/pattern queries:\n * - “Find usages of this function/API”\n * - “Search for TODO/FIXME comments”\n * - “Locate suspicious patterns (eval, innerHTML, SQL strings, etc.)”\n *\n * Strategy:\n * - Use specific patterns, not “.*”.\n * - Constrain with includeGlobs (e.g. *.ts, *.tsx) and maxResults.\n * - Use contextLines, lineText, and offsets when planning edits.\n *\n * COMBINED WORKFLOWS\n * ------------------\n * Typical two-step pattern:\n * 1) fdAdvancedList → get candidate files.\n * 2) rgAdvancedSearch → search within those files for patterns.\n *\n * Examples:\n * - “Find TS files without tests”:\n *   - fdAdvancedList for source .ts files (excluding *.test.ts / *.spec.ts).\n *   - fdAdvancedList for test files.\n *   - Compare sets to find untested files.\n *\n * - “Rename deprecated API”:\n *   - rgAdvancedSearch to locate all occurrences.\n *   - Review matches (lineText + contextLines).\n *   - Use offsets to generate precise replacements.\n *\n * CHOOSING BETWEEN BUILT-IN TOOLS AND POWER-TOOLS\n * ----------------------------------------------\n * Prefer power-tools when:\n * - The query is pattern-based and performance-sensitive.\n * - The repo is large and you need fast, repeatable queries.\n * - You need structured results for follow-up edits or analysis.\n *\n * Prefer built-in tools when:\n * - Using semantic_search for high-level conceptual queries\n *   (e.g. “find authentication logic”).\n * - power-tools are unavailable in the current environment.\n *\n * BEST PRACTICES FOR COPILOT\n * --------------------------\n * - Start narrow: restrict by directory, file type, and pattern.\n * - Use fdAdvancedList for “find files”, rgAdvancedSearch for “search code”.\n * - Always set reasonable maxResults for large repositories.\n * - Clearly state in explanations which tool you used and why.\n *\n * ANTI-PATTERNS TO AVOID\n * ----------------------\n * - Don’t search with extremely broad patterns like “.*”.\n * - Don’t scan node_modules, dist, .git, or vendor unless explicitly asked.\n * - Don’t use rgAdvancedSearch when a simple file listing is enough.\n */\n\nimport { spawnSync } from \"child_process\";\nimport * as path from \"path\";\n\n// ============================================================================\n// JSON/JSONL UTILITIES (Internal Helpers)\n// ============================================================================\n\n/**\n * Parse JSONL (JSON Lines) output from CLI tools\n *\n * Handles:\n * - Line splitting (CRLF/LF)\n * - Empty line filtering\n * - Parse error recovery\n * - Optional debug logging\n *\n * @param output - Raw JSONL string from CLI tool\n * @param debugEnvVar - Environment variable name for debug logging (e.g., 'DEBUG_RG')\n * @returns Array of parsed objects\n *\n * @internal Used by rgAdvancedSearch() for parsing rg --json output\n */\nfunction parseJsonLines<T = any>(output: string, debugEnvVar?: string): T[] {\n  const results: T[] = [];\n\n  for (const line of output.split(/\\r?\\n/)) {\n    const trimmed = line.trim();\n    if (!trimmed) continue;\n\n    try {\n      const obj = JSON.parse(trimmed) as T;\n      results.push(obj);\n    } catch (err) {\n      // Optional debug logging via environment variable\n      if (debugEnvVar && process.env[debugEnvVar]) {\n        console.error(`Failed to parse JSON line: ${trimmed}`, err);\n      }\n      // Continue parsing remaining lines\n    }\n  }\n\n  return results;\n}\n\n/**\n * Parse NUL-delimited output from CLI tools\n *\n * Use when CLI tool supports -0 or --null flag.\n * Handles filenames with newlines safely.\n *\n * @param output - Raw NUL-delimited string\n * @returns Array of strings (empty strings filtered)\n *\n * @internal Reserved for future use (fd --print0, rg --null)\n */\nfunction parseNulDelimited(output: string): string[] {\n  return output.split(\"\\0\").filter(Boolean);\n}\n\n// ============================================================================\n// RIPGREP (RG) - CONTENT SEARCH\n// ============================================================================\n\n/**\n * Raw ripgrep JSON match object structure\n * Corresponds to rg --json output format\n */\ninterface RgJsonLine {\n  type: \"begin\" | \"match\" | \"end\" | \"context\" | \"summary\";\n  data?: any;\n}\n\ninterface RgJsonMatchData {\n  path: { text: string };\n  lines: { text: string };\n  line_number: number;\n  absolute_offset: number;\n  submatches: Array<{\n    match: { text: string };\n    start: number; // byte offset in line\n    end: number; // byte offset in line\n  }>;\n}\n\n/**\n * Basic search result (backwards compatible)\n */\nexport interface RgHit {\n  file: string;\n  line: number;\n  column: number;\n  text: string;\n}\n\n/**\n * Detailed search result with full match information\n */\nexport interface RgDetailedHit {\n  /** Absolute file path */\n  file: string;\n\n  /** 1-indexed line number */\n  line: number;\n\n  /** 1-indexed column (approximate, byte-based) */\n  column: number;\n\n  /** Full line text (untrimmed) */\n  lineText: string;\n\n  /** Exact matched text portion */\n  matchText: string;\n\n  /** Byte offset of match start within line (0-indexed) */\n  startOffset: number;\n\n  /** Byte offset of match end within line (0-indexed) */\n  endOffset: number;\n}\n\n/**\n * Basic search arguments (backwards compatible)\n */\nexport interface RgSearchArgs {\n  pattern: string;\n  cwd: string;\n  glob?: string;\n  maxResults?: number;\n}\n\n/**\n * Advanced search arguments with full feature set\n */\nexport interface RgAdvancedSearchArgs {\n  /** Pattern to search (regex or literal) */\n  pattern: string;\n\n  /** Working directory (base path for search) */\n  cwd: string;\n\n  /** Include globs (e.g., ['*.ts', '*.js']) */\n  includeGlobs?: string[];\n\n  /** Exclude globs (e.g., ['node_modules/**', '*.min.js']) */\n  excludeGlobs?: string[];\n\n  /** Maximum directory depth to search */\n  maxDepth?: number;\n\n  /** Treat pattern as literal string (not regex) */\n  literal?: boolean;\n\n  /** Case-sensitive search (default: smart case) */\n  caseSensitive?: boolean;\n\n  /** Search hidden files/directories */\n  hidden?: boolean;\n\n  /** Don't respect .gitignore files */\n  noIgnore?: boolean;\n\n  /** Lines of context before each match */\n  contextBefore?: number;\n\n  /** Lines of context after each match */\n  contextAfter?: number;\n\n  /** Maximum number of results to return */\n  maxResults?: number;\n\n  /** Additional rg arguments (escape hatch) */\n  extraArgs?: string[];\n}\n\n/**\n * Build ripgrep command-line arguments from search options\n */\nfunction buildRgArgs(args: RgAdvancedSearchArgs): string[] {\n  const rgArgs: string[] = [\"--json\"];\n\n  // Boolean flags map\n  const flags: Record<string, boolean | undefined> = {\n    \"-F\": args.literal,\n    \"--case-sensitive\": args.caseSensitive,\n    \"--hidden\": args.hidden,\n    \"--no-ignore\": args.noIgnore,\n  };\n\n  for (const [flag, enabled] of Object.entries(flags)) {\n    if (enabled) rgArgs.push(flag);\n  }\n\n  // Value flags\n  if (args.maxDepth !== undefined) {\n    rgArgs.push(\"--max-depth\", args.maxDepth.toString());\n  }\n\n  if (args.contextBefore !== undefined) {\n    rgArgs.push(\"-B\", args.contextBefore.toString());\n  }\n\n  if (args.contextAfter !== undefined) {\n    rgArgs.push(\"-A\", args.contextAfter.toString());\n  }\n\n  // Include globs\n  for (const glob of args.includeGlobs ?? []) {\n    rgArgs.push(\"-g\", glob);\n  }\n\n  // Exclude globs (with negation prefix)\n  for (const glob of args.excludeGlobs ?? []) {\n    rgArgs.push(\"--glob\", `!${glob}`);\n  }\n\n  // Extra args passthrough\n  if (args.extraArgs) {\n    rgArgs.push(...args.extraArgs);\n  }\n\n  // Pattern and path (always last, after --)\n  rgArgs.push(\"--\", args.pattern, \".\");\n\n  return rgArgs;\n}\n\n/**\n * Type guard for ripgrep match lines\n */\nfunction isMatchLine(\n  obj: RgJsonLine\n): obj is { type: \"match\"; data: RgJsonMatchData } {\n  return obj.type === \"match\" && obj.data !== undefined;\n}\n\n/**\n * Advanced search with full feature set and detailed match information.\n *\n * Returns detailed hits with:\n * - Full line text (untrimmed)\n * - Exact match text\n * - Byte offsets for highlighting\n * - Multiple matches per line\n *\n * @param args - Search arguments with full control over rg behavior\n * @returns Array of detailed match results\n *\n * @example\n * ```typescript\n * const hits = rgAdvancedSearch({\n *   pattern: 'TODO',\n *   cwd: '/project',\n *   includeGlobs: ['*.ts', '*.js'],\n *   excludeGlobs: ['node_modules/**', 'dist/**'],\n *   maxDepth: 3,\n *   caseSensitive: false\n * });\n * ```\n */\nexport function rgAdvancedSearch(args: RgAdvancedSearchArgs): RgDetailedHit[] {\n  const { cwd, maxResults = 200 } = args;\n\n  const rgArgs = buildRgArgs(args);\n\n  const { stdout, stderr, status } = spawnSync(\"rg\", rgArgs, {\n    cwd,\n    encoding: \"utf8\",\n  });\n\n  if (status !== 0 && status !== 1) {\n    throw new Error(`rg failed (status ${status}): ${stderr}`);\n  }\n\n  const hits: RgDetailedHit[] = [];\n\n  // Parse JSONL output with error recovery\n  const lines = parseJsonLines<RgJsonLine>(stdout, \"DEBUG_RG\");\n\n  for (const obj of lines) {\n    if (isMatchLine(obj)) {\n      const relativePath = obj.data.path.text;\n      const absolutePath = path.resolve(cwd, relativePath);\n      const lineText = obj.data.lines.text;\n      const lineNumber = obj.data.line_number;\n\n      // Process each submatch (multiple matches per line)\n      for (const submatch of obj.data.submatches ?? []) {\n        hits.push({\n          file: absolutePath,\n          line: lineNumber,\n          column: submatch.start + 1, // 1-indexed column\n          lineText: lineText,\n          matchText: submatch.match.text,\n          startOffset: submatch.start,\n          endOffset: submatch.end,\n        });\n\n        if (hits.length >= maxResults) break;\n      }\n    }\n\n    if (hits.length >= maxResults) break;\n  }\n\n  return hits;\n}\n\n/**\n * Search code using ripgrep with JSON output and return structured hits.\n *\n * This is the basic/legacy version maintained for backwards compatibility.\n * For advanced features, use rgAdvancedSearch() instead.\n *\n * @deprecated Use rgAdvancedSearch() for more control and better match details\n */\nexport function rgSearch(args: RgSearchArgs): RgHit[] {\n  // Convert to advanced args and call new implementation\n  const detailedHits = rgAdvancedSearch({\n    pattern: args.pattern,\n    cwd: args.cwd,\n    includeGlobs: args.glob ? [args.glob] : undefined,\n    maxResults: args.maxResults,\n  });\n\n  // Convert detailed hits to basic format (with trimmed text for backwards compatibility)\n  return detailedHits.map((hit) => ({\n    file: hit.file,\n    line: hit.line,\n    column: hit.column,\n    text: hit.lineText.trim(),\n  }));\n}\n\n// ============================================================================\n// FD - FILESYSTEM SEARCH\n// ============================================================================\n\n/**\n * Basic file list arguments (backwards compatible)\n */\nexport interface FdListArgs {\n  pattern: string;\n  cwd: string;\n  maxResults?: number;\n}\n\n/**\n * Advanced file discovery arguments with full feature set\n */\nexport interface FdAdvancedListArgs {\n  /** Filename pattern (regex or literal) */\n  pattern: string;\n\n  /** Working directory (base path for search) */\n  cwd: string;\n\n  /** File extensions to match (e.g., ['.ts', '.js', '.tsx']) */\n  extensions?: string[];\n\n  /** Filter by type */\n  type?: \"file\" | \"directory\" | \"symlink\" | \"executable\";\n\n  /** Include hidden files/directories */\n  hidden?: boolean;\n\n  /** Don't respect .gitignore */\n  noIgnore?: boolean;\n\n  /** Maximum directory depth */\n  maxDepth?: number;\n\n  /** Exclude directories from search */\n  excludeDirs?: string[];\n\n  /** Follow symbolic links */\n  followLinks?: boolean;\n\n  /** Case-sensitive search */\n  caseSensitive?: boolean;\n\n  /** Return absolute paths (default: true) */\n  absolute?: boolean;\n\n  /** Maximum results to return */\n  maxResults?: number;\n\n  /** Additional fd arguments (escape hatch) */\n  extraArgs?: string[];\n}\n\n/**\n * File discovery result\n */\nexport interface FdResult {\n  /** File path (absolute or relative based on args.absolute) */\n  path: string;\n\n  /** File type (if filtered by type) */\n  type?: \"file\" | \"directory\" | \"symlink\" | \"executable\";\n}\n\n/**\n * Build fd command-line arguments from search options\n */\nfunction buildFdArgs(args: FdAdvancedListArgs): string[] {\n  const fdArgs: string[] = [];\n\n  // Type filtering\n  if (args.type) {\n    fdArgs.push(\"--type\", args.type);\n  }\n\n  // Boolean flags\n  if (args.hidden) {\n    fdArgs.push(\"--hidden\");\n  }\n\n  if (args.noIgnore) {\n    fdArgs.push(\"--no-ignore\");\n  }\n\n  if (args.followLinks) {\n    fdArgs.push(\"--follow\");\n  }\n\n  if (args.caseSensitive) {\n    fdArgs.push(\"--case-sensitive\");\n  }\n\n  if (args.absolute ?? true) {\n    fdArgs.push(\"--absolute-path\");\n  }\n\n  // Max depth\n  if (args.maxDepth !== undefined) {\n    fdArgs.push(\"--max-depth\", args.maxDepth.toString());\n  }\n\n  // Exclude directories\n  for (const dir of args.excludeDirs ?? []) {\n    fdArgs.push(\"--exclude\", dir);\n  }\n\n  // Extension filtering\n  if (args.extensions && args.extensions.length > 0) {\n    for (const ext of args.extensions) {\n      // Remove leading dot if present\n      const cleanExt = ext.replace(/^\\./, \"\");\n      fdArgs.push(\"--extension\", cleanExt);\n    }\n  }\n\n  // Extra args passthrough\n  if (args.extraArgs) {\n    fdArgs.push(...args.extraArgs);\n  }\n\n  // Pattern and search path\n  fdArgs.push(args.pattern, \".\");\n\n  return fdArgs;\n}\n\n/**\n * Advanced file discovery with full feature set.\n *\n * Provides rich control over filesystem traversal:\n * - Filter by type, extension, depth\n * - Include/exclude specific directories\n * - Control hidden files and .gitignore behavior\n * - Follow symlinks\n *\n * @param args - Search arguments with full control over fd behavior\n * @returns Array of file results with paths and optional type info\n *\n * @example\n * ```typescript\n * // Find all TypeScript files, excluding node_modules\n * const tsFiles = fdAdvancedList({\n *   extensions: ['.ts', '.tsx'],\n *   cwd: '/project',\n *   type: 'file',\n *   excludeDirs: ['node_modules', 'dist', '.git']\n * });\n *\n * // Find all package.json files (shallow search)\n * const packageFiles = fdAdvancedList({\n *   pattern: 'package.json',\n *   cwd: '/project',\n *   maxDepth: 3,\n *   type: 'file'\n * });\n *\n * // Find hidden config files\n * const configs = fdAdvancedList({\n *   pattern: '^\\\\..*rc$',\n *   cwd: '/project',\n *   hidden: true,\n *   type: 'file'\n * });\n * ```\n */\nexport function fdAdvancedList(args: FdAdvancedListArgs): FdResult[] {\n  const fdArgs = buildFdArgs(args);\n\n  const { stdout, stderr, status } = spawnSync(\"fd\", fdArgs, {\n    cwd: args.cwd,\n    encoding: \"utf8\",\n  });\n\n  if (status !== 0 && status !== 1) {\n    throw new Error(`fd failed (status ${status}): ${stderr}`);\n  }\n\n  const maxResults = args.maxResults ?? 500;\n  const files = stdout\n    .split(/\\r?\\n/)\n    .filter(Boolean)\n    .slice(0, maxResults)\n    .map((p) => ({\n      path: p,\n      type: args.type, // Known from filter if specified\n    }));\n\n  return files;\n}\n\n/**\n * List files matching a pattern using fd.\n *\n * This is the basic/legacy version maintained for backwards compatibility.\n * For advanced features, use fdAdvancedList() instead.\n *\n * @deprecated Use fdAdvancedList() for more control over discovery\n */\nexport function fdList(args: FdListArgs): string[] {\n  // Convert to advanced args and call new implementation\n  const results = fdAdvancedList({\n    pattern: args.pattern,\n    cwd: args.cwd,\n    maxResults: args.maxResults,\n    type: \"file\", // Default to files only for backwards compatibility\n    absolute: false, // Original used relative paths\n  });\n\n  // Convert to simple string array and make paths absolute (original behavior)\n  return results.map((r) => path.join(args.cwd, r.path));\n} |
22 | typescript/readSnapshot-stub.ts | {339 28 0.1563 4.8981} | 350 | export async function readRepoSnapshot(id: string): Promise<SnapshotCodex> {\n  const manifest = await readJson<Manifest>(`${id}/manifest.json`);\n  const toc = await readJsonl<TocEntry>(`${id}/toc.jsonl`);\n  return {\n    manifest,\n    toc,\n    getShard: (n) =>\n      readJsonl(`${id}/shards/s${n.toString().padStart(3, \"0\")}.jsonl`),\n  };\n} |
23 | typescript/recipe-types.ts | {3989 558 0.2051 4.7553} | 4210 | /**\n * Type definitions for the Recipe Store system\n *\n * Recipes are reusable PowerShell snippets that combine tools (rg, fd, parfor, jso, etc.)\n * into composable patterns for common tasks.\n */\n\n/**\n * Input parameter definition for a recipe\n */\nexport interface RecipeInput {\n  /** Parameter name (e.g., 'path', 'pattern', 'extension') */\n  name: string;\n\n  /** Data type ('string', 'number', 'boolean', 'array', 'path') */\n  type: \"string\" | \"number\" | \"boolean\" | \"array\" | \"path\";\n\n  /** Human-readable description */\n  description: string;\n\n  /** Whether parameter is required */\n  required: boolean;\n\n  /** Default value if not provided */\n  defaultValue?: any;\n\n  /** Example value for documentation */\n  example?: string;\n}\n\n/**\n * Output definition for a recipe\n */\nexport interface RecipeOutput {\n  /** Output format ('json', 'jsonl', 'text', 'csv', 'object') */\n  format: \"json\" | \"jsonl\" | \"text\" | \"csv\" | \"object\";\n\n  /** Description of what is returned */\n  description: string;\n\n  /** TypeScript type definition (optional) */\n  typeDefinition?: string;\n}\n\n/**\n * Usage example for a recipe\n */\nexport interface RecipeExample {\n  /** Description of the use case */\n  description: string;\n\n  /** Example command/invocation */\n  code: string;\n\n  /** Expected output (optional) */\n  output?: string;\n}\n\n/**\n * A reusable PowerShell recipe\n */\nexport interface PowerShellRecipe {\n  /** Unique identifier (e.g., 'find-todos-parallel') */\n  id: string;\n\n  /** Human-readable name */\n  name: string;\n\n  /** Detailed description of what the recipe does */\n  description: string;\n\n  /** PowerShell script template (may contain ${param} placeholders) */\n  script: string;\n\n  /** Tools used in the recipe (e.g., ['rg', 'parforeach', 'jso']) */\n  tools: string[];\n\n  /** Categorization tags */\n  tags: string[];\n\n  /** Input parameters */\n  inputs: RecipeInput[];\n\n  /** Output specification */\n  output: RecipeOutput;\n\n  /** Usage examples */\n  examples: RecipeExample[];\n\n  /** Whether this recipe can be composed with others */\n  composable: boolean;\n\n  /** Estimated complexity ('simple', 'moderate', 'complex') */\n  complexity: \"simple\" | \"moderate\" | \"complex\";\n\n  /** Author/contributor */\n  author?: string;\n\n  /** Creation timestamp */\n  createdAt?: string;\n\n  /** Last modified timestamp */\n  updatedAt?: string;\n\n  /** Usage count (for learning system) */\n  usageCount?: number;\n}\n\n/**\n * Query parameters for searching recipes\n */\nexport interface RecipeQuery {\n  /** Search by recipe ID */\n  id?: string;\n\n  /** Search by tool used */\n  tool?: string;\n\n  /** Search by tag */\n  tag?: string;\n\n  /** Full-text search in name/description */\n  search?: string;\n\n  /** Filter by complexity */\n  complexity?: \"simple\" | \"moderate\" | \"complex\";\n\n  /** Only return composable recipes */\n  composableOnly?: boolean;\n\n  /** Maximum number of results */\n  limit?: number;\n}\n\n/**\n * Result of a recipe search\n */\nexport interface RecipeSearchResult {\n  /** Matching recipes */\n  recipes: PowerShellRecipe[];\n\n  /** Total matches found */\n  totalCount: number;\n\n  /** Query that was executed */\n  query: RecipeQuery;\n}\n\n/**\n * Pattern detected by the learning system\n */\nexport interface DetectedPattern {\n  /** The normalized command pattern */\n  pattern: string;\n\n  /** How many times this pattern has been used */\n  frequency: number;\n\n  /** Tools involved in the pattern */\n  tools: string[];\n\n  /** Suggested recipe ID if pattern should become a recipe */\n  suggestedRecipeId?: string;\n\n  /** Confidence score (0-1) that this should be a recipe */\n  confidence?: number;\n}\n\n/**\n * Usage tracking entry\n */\nexport interface RecipeUsageEntry {\n  /** Recipe ID that was used */\n  recipeId?: string;\n\n  /** The actual command executed */\n  command: string;\n\n  /** Timestamp of execution */\n  timestamp: string;\n\n  /** Whether execution was successful */\n  success: boolean;\n\n  /** Execution time in milliseconds */\n  executionTime?: number;\n\n  /** Tools used */\n  tools: string[];\n} |
24 | typescript/safe-shell.d.ts | {5410 716 0.1758 5.0256} | 5615 | /**\n * safe-shell.d.ts\n *\n * Type definitions for safe-shell module.\n * Use for importing types without implementation.\n */\n\n// ==================== COMMAND INTERFACES ====================\n\nexport interface ShellCommandArgs {\n  /** Command string to execute in the interactive shell */\n  command: string;\n\n  /** Whether to append newline after command (default: true) */\n  newLine?: boolean;\n\n  /** Optional: specific terminal name to target (default: \"Copilot\") */\n  terminalName?: string;\n}\n\nexport interface ShellCommandResult {\n  /** Whether command was dispatched successfully */\n  ok: boolean;\n\n  /** Human-readable status message */\n  message?: string;\n\n  /** Session ID if console capture is active */\n  sessionId?: string;\n}\n\n// ==================== CONSOLE READ INTERFACES ====================\n\nexport interface ConsoleReadArgs {\n  /** Path to console dump JSONL file (default: today's dump) */\n  dumpPath?: string;\n\n  /** Number of records to skip from start (pagination) */\n  skip?: number;\n\n  /** Number of records to return (default: 100) */\n  take?: number;\n\n  /** Filter by record type */\n  type?: 'cmd' | 'out';\n\n  /** Filter by session ID (8-char hex string) */\n  sessionId?: string;\n\n  /** Filter by sequence number within session */\n  seq?: number;\n}\n\nexport interface ConsoleRecord {\n  /** Record type: 'cmd' for command, 'out' for output */\n  type: 'cmd' | 'out';\n\n  /** ISO 8601 timestamp with timezone */\n  timestamp: string;\n\n  /** Session ID (8-character hex string) */\n  session: string;\n\n  /** Sequence number within session (1-based) */\n  seq: number;\n\n  // ===== Command Record Fields (type: 'cmd') =====\n\n  /** Command string (present in 'cmd' records) */\n  command?: string;\n\n  /** Command content hash (present in 'cmd' records) */\n  cmd_hash?: string;\n\n  /** Exit code (0 = success, present in 'cmd' records) */\n  exit_code?: number;\n\n  /** Execution duration in milliseconds (present in 'cmd' records) */\n  duration_ms?: number;\n\n  /** Current working directory (present in 'cmd' records) */\n  cwd?: string;\n\n  // ===== Output Record Fields (type: 'out') =====\n\n  /** Output content hash (present in 'out' records) */\n  out_hash?: string;\n\n  /** Output text (present in 'out' records) */\n  output?: string;\n\n  /** Whether whitespace was compressed (present in 'out' records) */\n  compressed?: boolean;\n}\n\nexport interface ConsoleDumpInfo {\n  /** Full path to today's console dump file */\n  dumpPath: string;\n\n  /** Whether the dump file exists on disk */\n  exists: boolean;\n\n  /** File size in bytes (only if exists) */\n  size?: number;\n\n  /** Date of dump in YYYY-MM-DD format */\n  date: string;\n}\n\n// ==================== FUNCTION SIGNATURES ====================\n\n/**\n * Send a command to the interactive PowerShell terminal.\n *\n * GARGOYLE-RESISTANT: Returns immediately without waiting for output.\n * Use readConsoleDump() to retrieve results from JSONL after execution.\n *\n * @param args - Command and options\n * @returns Promise with acknowledgment (not output!)\n */\nexport function sendToInteractiveShell(\n  args: ShellCommandArgs\n): Promise<ShellCommandResult>;\n\n/**\n * Read console dump records from JSONL file.\n *\n * GARGOYLE-RESISTANT: Reads from file artifacts, not live terminal output.\n *\n * @param args - Query parameters (filters, pagination)\n * @returns Array of console records (commands and/or outputs)\n */\nexport function readConsoleDump(\n  args?: ConsoleReadArgs\n): Promise<ConsoleRecord[]>;\n\n/**\n * Get the most recent console records.\n * Convenience wrapper around readConsoleDump.\n *\n * @param count - Number of recent records to retrieve (default: 10)\n * @returns Array of most recent console records\n */\nexport function getRecentConsoleRecords(\n  count?: number\n): Promise<ConsoleRecord[]>;\n\n/**\n * Get the last command and its output from console dump.\n *\n * @returns Object with command and output records, or null if not found\n */\nexport function getLastCommand(): Promise<{\n  cmd: ConsoleRecord;\n  out: ConsoleRecord;\n} | null>;\n\n/**\n * Get commands with non-zero exit codes (errors).\n *\n * @param limit - Maximum number of error commands to return (default: 10)\n * @returns Array of command records that failed\n */\nexport function getErrorCommands(\n  limit?: number\n): Promise<ConsoleRecord[]>;\n\n/**\n * Get path to today's console dump file.\n * Uses COPILOT_GLOBAL_HOME env var or falls back to workspace .copilot\n *\n * @returns Absolute path to YYYY-MM-DD.jsonl file\n */\nexport function getTodaysDumpPath(): string;\n\n/**\n * Get info about today's console dump file.\n *\n * @returns Object with path, existence, size, and date\n */\nexport function getDumpInfo(): ConsoleDumpInfo;\n\n// ==================== CONSTANTS ====================\n\n/**\n * Instructions for disabling native terminal tools.\n * Add this to your copilot-instructions.md or global instructions.\n */\nexport const DISABLE_NATIVE_TOOLS_INSTRUCTIONS: string;\n\n// ==================== DEFAULT EXPORT ====================\n\ndeclare const safeShell: {\n  sendToInteractiveShell: typeof sendToInteractiveShell;\n  readConsoleDump: typeof readConsoleDump;\n  getRecentConsoleRecords: typeof getRecentConsoleRecords;\n  getLastCommand: typeof getLastCommand;\n  getErrorCommands: typeof getErrorCommands;\n  getTodaysDumpPath: typeof getTodaysDumpPath;\n  getDumpInfo: typeof getDumpInfo;\n  DISABLE_NATIVE_TOOLS_INSTRUCTIONS: typeof DISABLE_NATIVE_TOOLS_INSTRUCTIONS;\n};\n\nexport default safeShell; |
