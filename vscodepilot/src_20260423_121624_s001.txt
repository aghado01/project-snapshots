idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
0 | extension.ts | {20565 1656 0.2794 4.6276} | 21503 | import * as vscode from \"vscode\";\n\n// Import toolbelt functions\nimport {\n  fdAdvancedList,\n  type FdAdvancedListArgs,\n  formatLintResults,\n  formatStructuralLintResults,\n  // JSO Blackbelt\n  lintPowerShell,\n  // Power Tools - Advanced\n  lintTypeScript,\n  type LintTypeScriptOptions,\n  type PSLintArgs,\n  readJsonlWindow,\n  rgAdvancedSearch,\n  type RgAdvancedSearchArgs,\n  // PS-Linter\n  // Safe Shell\n  sendToInteractiveShell,\n  type ShellCommandArgs,\n} from \"./toolbelt/copilot-toolbelt\";\n\n// Import parallel tools\nimport * as parallelTools from \"./toolbelt/parallel-tools\";\n\n// Import supervisor bridge for status commands\nimport { supervisorBridge } from \"./toolbelt/supervisor-bridge\";\n\n// Import primer\nimport { registerPrimer } from \"./toolbelt/copilot-primer\";\n\n/**\n * VS Code Copilot Toolbelt Extension\n *\n * Registers custom chat tools for:\n * - Safe Shell: Terminal command execution\n * - JSO Blackbelt: JSONL/JSON processing\n * - Power Tools: Code search utilities\n * - PS-Linter: PowerShell static analysis\n * - Parallel Tools: Async parallel PowerShell jobs with supervisor\n *\n * Note: Structural validation runs during compile-time via npm run validate\n */\n\nexport async function activate(context: vscode.ExtensionContext) {\n  console.log(\"🚀 Copilot Toolbelt extension is now active!\");\n\n  // ========================================================================\n  // PARALLEL TOOLS - Initialize Supervisor\n  // ========================================================================\n\n  try {\n    console.log(\"⚙️ Initializing parallel processing supervisor...\");\n    await parallelTools.initialize(context.extensionPath);\n    console.log(\"✅ Supervisor initialized successfully\");\n  } catch (error) {\n    console.error(\"❌ Failed to initialize supervisor:\", error);\n    vscode.window.showErrorMessage(\n      `Failed to initialize parallel processing: ${error}`\n    );\n  }\n\n  // Register cleanup on deactivation\n  context.subscriptions.push({\n    dispose: async () => {\n      console.log(\"🛑 Shutting down parallel tools...\");\n      await parallelTools.shutdown();\n    },\n  });\n\n  // ========================================================================\n  // PRIMER - Copilot Tool Awareness\n  // ========================================================================\n\n  registerPrimer(context);\n\n  // ========================================================================\n  // SAFE SHELL TOOLS - Terminal Command Execution\n  // ========================================================================\n\n  const sendToShellTool = vscode.lm.registerTool(\"sendToInteractiveShell\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as ShellCommandArgs;\n\n        // Call real implementation\n        const result = await sendToInteractiveShell(args);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ ${\n              result.message ?? \"Command dispatched to interactive shell\"\n            }\\n` + (result.sessionId ? `Session: ${result.sessionId}` : \"\")\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(`❌ Error: ${error.message}`),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(sendToShellTool);\n\n  // ========================================================================\n  // JSO BLACKBELT TOOLS - JSONL/JSON Processing\n  // ========================================================================\n\n  const readJsonlTool = vscode.lm.registerTool(\"readJsonlWindow\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          filePath: string;\n          skip?: number;\n          take?: number;\n        };\n\n        // Call real implementation\n        const records = await readJsonlWindow(\n          args.filePath,\n          args.skip,\n          args.take\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `📄 Read ${\n              records.length\n            } records from JSONL\\n\\n\\`\\`\\`json\\n${JSON.stringify(\n              records,\n              null,\n              2\n            )}\\n\\`\\`\\``\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error reading JSONL: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(readJsonlTool);\n\n  // ========================================================================\n  // POWER TOOLS - Code Search (Advanced)\n  // ========================================================================\n\n  const rgAdvancedSearchTool = vscode.lm.registerTool(\"rgAdvancedSearch\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as RgAdvancedSearchArgs;\n\n        // Call real implementation\n        const hits = rgAdvancedSearch(args);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `🔍 Found ${hits.length} matches for \"${args.pattern}\"\\n\\n` +\n              hits\n                .map(\n                  (h) =>\n                    `📍 ${h.file}:${h.line}:${h.column}\\n` +\n                    `   Line: ${h.lineText.trim()}\\n` +\n                    `   Match: \"${h.matchText}\" [offset ${h.startOffset}-${h.endOffset}]`\n                )\n                .join(\"\\n\\n\")\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error searching: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(rgAdvancedSearchTool);\n\n  // ========================================================================\n  // POWER TOOLS - File Discovery (Advanced)\n  // ========================================================================\n\n  const fdAdvancedListTool = vscode.lm.registerTool(\"fdAdvancedList\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as FdAdvancedListArgs;\n\n        // Call real implementation\n        const files = fdAdvancedList(args);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `📁 Found ${files.length} files${\n              args.cwd ? ` in ${args.cwd}` : \"\"\n            }\\n\\n` +\n              files\n                .map((f) => `- ${f.path}${f.type ? ` [${f.type}]` : \"\"}`)\n                .join(\"\\n\")\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error listing files: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(fdAdvancedListTool);\n\n  // ========================================================================\n  // PS-LINTER TOOLS - PowerShell Static Analysis\n  // ========================================================================\n\n  const lintPowerShellTool = vscode.lm.registerTool(\"lintPowerShell\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as PSLintArgs;\n\n        // Call real implementation\n        const results = await lintPowerShell(args);\n\n        // Format output\n        const formatted = formatLintResults(results);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(formatted),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error linting PowerShell: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(lintPowerShellTool);\n\n  // ========================================================================\n  // STRUCTURAL LINTER - TypeScript Syntax Validation\n  // ========================================================================\n\n  const lintTypeScriptTool = vscode.lm.registerTool(\"lintTypeScript\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as LintTypeScriptOptions;\n\n        // Call real implementation\n        const results = await lintTypeScript(args);\n\n        // Format output\n        const formatted = formatStructuralLintResults(results);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(formatted),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error linting TypeScript: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(lintTypeScriptTool);\n\n  // ========================================================================\n  // PARALLEL PROCESSING TOOLS\n  // ========================================================================\n\n  const startParallelJobTool = vscode.lm.registerTool(\"startParallelJob\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          script: string;\n          options?: any;\n        };\n\n        const jobId = await parallelTools.startParallelJob(\n          args.script,\n          args.options || {}\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ Parallel job started: ${jobId}\\n\\nUse checkJobStatus(\"${jobId}\") to monitor progress.`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error starting job: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(startParallelJobTool);\n\n  const checkJobStatusTool = vscode.lm.registerTool(\"checkJobStatus\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as { jobId: string };\n        const status = await parallelTools.checkJobStatus(args.jobId);\n\n        if (!status) {\n          return new vscode.LanguageModelToolResult([\n            new vscode.LanguageModelTextPart(`❓ Job ${args.jobId} not found`),\n          ]);\n        }\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `📊 Job Status: ${status}\\n\\nJob ID: ${args.jobId}`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error checking status: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(checkJobStatusTool);\n\n  const getJobResultsTool = vscode.lm.registerTool(\"getJobResults\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as { jobId: string };\n        const results = await parallelTools.getJobResults(args.jobId);\n\n        if (!results) {\n          return new vscode.LanguageModelToolResult([\n            new vscode.LanguageModelTextPart(\n              `❓ No results found for job ${args.jobId}`\n            ),\n          ]);\n        }\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ Job Results (${\n              results.length\n            } items):\\n\\n\\`\\`\\`json\\n${JSON.stringify(\n              results,\n              null,\n              2\n            )}\\n\\`\\`\\``\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error getting results: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(getJobResultsTool);\n\n  const cancelJobTool = vscode.lm.registerTool(\"cancelJob\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as { jobId: string };\n        const success = await parallelTools.cancelJob(args.jobId);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            success\n              ? `✅ Job ${args.jobId} cancelled successfully`\n              : `❌ Failed to cancel job ${args.jobId}`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error cancelling job: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(cancelJobTool);\n\n  const getAllJobsTool = vscode.lm.registerTool(\"getAllJobs\", {\n    invoke: async (options, token) => {\n      try {\n        const jobs = parallelTools.getAllJobs();\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `📋 All Jobs (${jobs.length}):\\n\\n\\`\\`\\`json\\n${JSON.stringify(\n              jobs,\n              null,\n              2\n            )}\\n\\`\\`\\``\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error getting jobs: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(getAllJobsTool);\n\n  const getJobsByStatusTool = vscode.lm.registerTool(\"getJobsByStatus\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          status: \"pending\" | \"running\" | \"completed\" | \"failed\" | \"cancelled\";\n        };\n        const jobs = parallelTools.getJobsByStatus(args.status);\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `📋 Jobs with status \"${args.status}\" (${\n              jobs.length\n            }):\\n\\n\\`\\`\\`json\\n${JSON.stringify(jobs, null, 2)}\\n\\`\\`\\``\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error getting jobs: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(getJobsByStatusTool);\n\n  const parforeachTool = vscode.lm.registerTool(\"parforeach\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          items: any[];\n          scriptBlock: string;\n          options?: any;\n        };\n\n        const jobId = await parallelTools.parforeach(\n          args.items,\n          args.scriptBlock,\n          args.options || {}\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ Parallel foreach started: ${jobId}`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error starting parforeach: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(parforeachTool);\n\n  const parforTool = vscode.lm.registerTool(\"parfor\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          count: number;\n          scriptBlock: string;\n          options?: any;\n        };\n\n        const jobId = await parallelTools.parfor(\n          args.count,\n          args.scriptBlock,\n          args.options || {}\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(`✅ Parallel for started: ${jobId}`),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error starting parfor: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(parforTool);\n\n  const parrangeTool = vscode.lm.registerTool(\"parrange\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          range: number[] | [number, number];\n          scriptBlock: string;\n          options?: any;\n        };\n\n        const jobId = await parallelTools.parrange(\n          args.range,\n          args.scriptBlock,\n          args.options || {}\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ Parallel range started: ${jobId}`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error starting parrange: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(parrangeTool);\n\n  const parwhileTool = vscode.lm.registerTool(\"parwhile\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          condition: string;\n          scriptBlock: string;\n          options?: any;\n        };\n\n        const jobId = await parallelTools.parwhile(\n          args.condition,\n          args.scriptBlock,\n          args.options || {}\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ Parallel while loop started: ${jobId}`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error starting parwhile: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(parwhileTool);\n\n  const paruntilTool = vscode.lm.registerTool(\"paruntil\", {\n    invoke: async (options, token) => {\n      try {\n        const args = options.input as {\n          condition: string;\n          scriptBlock: string;\n          options?: any;\n        };\n\n        const jobId = await parallelTools.paruntil(\n          args.condition,\n          args.scriptBlock,\n          args.options || {}\n        );\n\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `✅ Parallel until loop started: ${jobId}`\n          ),\n        ]);\n      } catch (error: any) {\n        return new vscode.LanguageModelToolResult([\n          new vscode.LanguageModelTextPart(\n            `❌ Error starting paruntil: ${error.message}`\n          ),\n        ]);\n      }\n    },\n  });\n\n  context.subscriptions.push(paruntilTool);\n\n  // ========================================================================\n  // UTILITY COMMANDS\n  // ========================================================================\n\n  // Supervisor Status Command\n  const supervisorStatusCommand = vscode.commands.registerCommand(\n    \"vscodepilot-toolbelt.supervisorStatus\",\n    () => {\n      const status = supervisorBridge.getStatus();\n      const message = `Supervisor Status:\n- Initialized: ${status.initialized}\n- Restart Count: ${status.restartCount}\n- Pending Requests: ${status.pendingRequests}`;\n\n      vscode.window.showInformationMessage(message);\n      console.log(\"[Supervisor Status]\", status);\n    }\n  );\n  context.subscriptions.push(supervisorStatusCommand);\n\n  // Restart Supervisor Command\n  const restartSupervisorCommand = vscode.commands.registerCommand(\n    \"vscodepilot-toolbelt.restartSupervisor\",\n    async () => {\n      try {\n        vscode.window.showInformationMessage(\"Restarting supervisor...\");\n        await supervisorBridge.shutdown();\n        await supervisorBridge.initialize(\n          context.extensionPath,\n          vscode.workspace.workspaceFolders?.[0]?.uri.fsPath || process.cwd()\n        );\n        vscode.window.showInformationMessage(\n          \"✅ Supervisor restarted successfully\"\n        );\n      } catch (error) {\n        const msg = error instanceof Error ? error.message : String(error);\n        vscode.window.showErrorMessage(`Failed to restart supervisor: ${msg}`);\n      }\n    }\n  );\n  context.subscriptions.push(restartSupervisorCommand);\n\n  // ========================================================================\n  // SUMMARY\n  // ========================================================================\n\n  const registeredTools = [\n    \"sendToInteractiveShell\",\n    \"readJsonlWindow\",\n    \"rgAdvancedSearch\",\n    \"fdAdvancedList\",\n    \"lintPowerShell\",\n    \"lintTypeScript\",\n  ];\n\n  console.log(\n    `✅ Registered ${registeredTools.length} chat tools:`,\n    registeredTools\n  );\n  console.log(\n    \"🎯 All tools using advanced implementations from copilot-toolbelt\"\n  );\n}\n\nexport function deactivate() {\n  console.log(\"👋 Copilot Toolbelt extension deactivated\");\n} |
