idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
5 | powershell/parallel-engine-cli.ps1 | {12469 1102 0.3115 4.5775} | 12931 | #Requires -Version 7.2\n\n<#\n.SYNOPSIS\n    Command-line interface for parallel-toolbox-v2 - user-friendly parallel processing.\n\n.DESCRIPTION\n    CLI wrapper that exposes parfor, parforeach, parrange, parwhile, paruntil\n    commands for easy invocation by TypeScript extension and interactive users.\n\n    Provides two execution modes:\n    1. Direct execution (sync) - Returns results immediately\n    2. Async job execution - Writes results to signal/result files\n\n    Uses the v2 hybrid architecture (class-based engine with function API).\n\n.NOTES\n    Author: azriy\n    Version: 2.0.0 (Hybrid Architecture)\n    Requires: parallel-toolbox-v2.psm1 (PowerShell 7.2+)\n\n.EXAMPLE\n    # Direct execution - returns results as JSON\n    .\\Invoke-ParallelToolbox.ps1 -Command foreach -Items 'Get-ChildItem -Recurse' -ScriptBlock '{ param($f) Get-FileHash $f.FullName }'\n\n.EXAMPLE\n    # Async job execution - writes to signal/result files\n    .\\Invoke-ParallelToolbox.ps1 -Command for -Count 100 -ScriptBlock '{ param($i) $i * 2 }' -JobId \"job_123\" -SignalPath \".\\job_123.signal\" -ResultPath \".\\job_123.jsonl\"\n\n.EXAMPLE\n    # Batch processing with progress\n    .\\Invoke-ParallelToolbox.ps1 -Command batch -InputPath .\\logs -ProcessingType LogAnalysis -ShowProgress\n#>\n\n[CmdletBinding()]\nparam(\n    # Command to execute\n    [Parameter(Mandatory)]\n    [ValidateSet('foreach', 'for', 'range', 'while', 'until', 'batch')]\n    [string]$Command,\n\n    # For 'foreach' command - PowerShell expression that returns items\n    [string]$Items,\n\n    # For 'for' command - iteration count\n    [ValidateRange(1, [int]::MaxValue)]\n    [int]$Count,\n\n    # For 'range' command - PowerShell range expression (e.g., \"1..100\")\n    [string]$Range,\n\n    # For 'while' and 'until' commands - condition scriptblock as string\n    [string]$Condition,\n\n    # ScriptBlock to execute (required for all non-batch commands) as string\n    [string]$ScriptBlock,\n\n    # Optional: Throttle limit (defaults to processor count)\n    [ValidateRange(1, 128)]\n    [int]$ThrottleLimit,\n\n    # For batch processing\n    [string]$InputPath,\n    [string]$OutputPath,\n    [ValidateSet('FileHash', 'LogAnalysis', 'FileStats', 'TextProcessing')]\n    [string]$ProcessingType = 'FileStats',\n    [switch]$ShowProgress,\n\n    # Async job support (for TypeScript extension)\n    [string]$JobId,\n    [string]$SignalPath,\n    [string]$ResultPath,\n    [string]$CancelSignalPath,\n\n    # Output format (only used in direct execution mode)\n    [ValidateSet('json', 'jsonl', 'text', 'objects')]\n    [string]$OutputFormat = 'json'\n)\n\n# ============================================================\n# MODULE IMPORT\n# ============================================================\n\n$ErrorActionPreference = 'Stop'\n\n$moduleName = \"parallel-engine-v2\"\n$modulePath = Join-Path $PSScriptRoot \"$moduleName.psm1\"\n\nif (-not (Test-Path $modulePath -PathType Leaf)) {\n    throw \"Cannot find module $moduleName.psm1 at: $modulePath\"\n}\n\ntry {\n    Import-Module $modulePath -Force -ErrorAction Stop\n}\ncatch {\n    throw \"Failed to import module $moduleName : $_\"\n}\n\n# ============================================================\n# EXECUTION MODE DETECTION\n# ============================================================\n\n# Determine if this is async job mode (has JobId + SignalPath + ResultPath)\n$isAsyncJob = -not [string]::IsNullOrWhiteSpace($JobId) -and\n              -not [string]::IsNullOrWhiteSpace($SignalPath) -and\n              -not [string]::IsNullOrWhiteSpace($ResultPath)\n\n# ============================================================\n# HELPER FUNCTIONS\n# ============================================================\n\nfunction Build-CommonParams {\n    <#\n    .SYNOPSIS\n        Build common parameters for parallel functions.\n    #>\n    $params = @{}\n\n    if ($ThrottleLimit) {\n        $params.ThrottleLimit = $ThrottleLimit\n    }\n\n    if ($CancelSignalPath) {\n        $params.CancelSignalPath = $CancelSignalPath\n    }\n\n    return $params\n}\n\nfunction ConvertTo-ScriptBlock {\n    <#\n    .SYNOPSIS\n        Safely convert string to ScriptBlock.\n    #>\n    param([string]$ScriptString)\n\n    if ([string]::IsNullOrWhiteSpace($ScriptString)) {\n        return $null\n    }\n\n    try {\n        return [scriptblock]::Create($ScriptString)\n    }\n    catch {\n        throw \"Failed to parse ScriptBlock: $_\"\n    }\n}\n\nfunction Invoke-SafeExpression {\n    <#\n    .SYNOPSIS\n        Safely evaluate PowerShell expression with error handling.\n    #>\n    param(\n        [string]$Expression,\n        [string]$ExpressionName\n    )\n\n    if ([string]::IsNullOrWhiteSpace($Expression)) {\n        throw \"$ExpressionName expression cannot be empty\"\n    }\n\n    try {\n        # Note: Using Invoke-Expression here is intentional for CLI flexibility\n        # TypeScript extension should only pass safe, validated expressions\n        return Invoke-Expression $Expression\n    }\n    catch {\n        throw \"Failed to evaluate $ExpressionName expression '$Expression': $_\"\n    }\n}\n\n# ============================================================\n# COMMAND EXECUTION BUILDER\n# ============================================================\n\nfunction Build-ExecutionScript {\n    <#\n    .SYNOPSIS\n        Build the scriptblock that executes the parallel operation.\n    #>\n\n    $executionScript = {\n        $params = Build-CommonParams\n\n        switch ($Command) {\n            'foreach' {\n                if ([string]::IsNullOrWhiteSpace($Items)) {\n                    throw \"Items parameter is required for 'foreach' command\"\n                }\n\n                if ([string]::IsNullOrWhiteSpace($ScriptBlock)) {\n                    throw \"ScriptBlock parameter is required for 'foreach' command\"\n                }\n\n                # Parse scriptblock\n                $sb = ConvertTo-ScriptBlock -ScriptString $ScriptBlock\n\n                # Safely evaluate Items expression\n                $itemsArray = Invoke-SafeExpression -Expression $Items -ExpressionName \"Items\"\n\n                # Execute parallel foreach\n                parforeach -Data $itemsArray -ScriptBlock $sb @params\n            }\n\n            'for' {\n                if ($Count -le 0) {\n                    throw \"Count parameter must be greater than 0 for 'for' command\"\n                }\n\n                if ([string]::IsNullOrWhiteSpace($ScriptBlock)) {\n                    throw \"ScriptBlock parameter is required for 'for' command\"\n                }\n\n                # Parse scriptblock\n                $sb = ConvertTo-ScriptBlock -ScriptString $ScriptBlock\n\n                # Execute parallel for\n                parfor -Count $Count -ScriptBlock $sb @params\n            }\n\n            'range' {\n                if ([string]::IsNullOrWhiteSpace($Range)) {\n                    throw \"Range parameter is required for 'range' command\"\n                }\n\n                if ([string]::IsNullOrWhiteSpace($ScriptBlock)) {\n                    throw \"ScriptBlock parameter is required for 'range' command\"\n                }\n\n                # Parse scriptblock\n                $sb = ConvertTo-ScriptBlock -ScriptString $ScriptBlock\n\n                # Safely evaluate Range expression\n                $rangeArray = Invoke-SafeExpression -Expression $Range -ExpressionName \"Range\"\n\n                # Execute parallel range\n                parrange -Range $rangeArray -ScriptBlock $sb @params\n            }\n\n            'while' {\n                if ([string]::IsNullOrWhiteSpace($Condition)) {\n                    throw \"Condition parameter is required for 'while' command\"\n                }\n\n                if ([string]::IsNullOrWhiteSpace($ScriptBlock)) {\n                    throw \"ScriptBlock parameter is required for 'while' command\"\n                }\n\n                # Parse scriptblocks\n                $conditionSb = ConvertTo-ScriptBlock -ScriptString $Condition\n                $sb = ConvertTo-ScriptBlock -ScriptString $ScriptBlock\n\n                # Execute parallel while\n                parwhile -Condition $conditionSb -ScriptBlock $sb @params\n            }\n\n            'until' {\n                if ([string]::IsNullOrWhiteSpace($Condition)) {\n                    throw \"Condition parameter is required for 'until' command\"\n                }\n\n                if ([string]::IsNullOrWhiteSpace($ScriptBlock)) {\n                    throw \"ScriptBlock parameter is required for 'until' command\"\n                }\n\n                # Parse scriptblocks\n                $conditionSb = ConvertTo-ScriptBlock -ScriptString $Condition\n                $sb = ConvertTo-ScriptBlock -ScriptString $ScriptBlock\n\n                # Execute parallel until\n                paruntil -Condition $conditionSb -ScriptBlock $sb @params\n            }\n\n            'batch' {\n                if ([string]::IsNullOrWhiteSpace($InputPath)) {\n                    throw \"InputPath parameter is required for 'batch' command\"\n                }\n\n                if (-not (Test-Path $InputPath)) {\n                    throw \"InputPath does not exist: $InputPath\"\n                }\n\n                $batchParams = @{\n                    InputPath      = $InputPath\n                    ProcessingType = $ProcessingType\n                }\n\n                if ($OutputPath) {\n                    $batchParams.OutputPath = $OutputPath\n                }\n\n                if ($ThrottleLimit) {\n                    $batchParams.ThrottleLimit = $ThrottleLimit\n                }\n\n                if ($ShowProgress) {\n                    $batchParams.ShowProgress = $true\n                }\n\n                # Execute batch processing\n                Invoke-ParallelBatchProcessing @batchParams\n            }\n\n            default {\n                throw \"Unknown command: $Command\"\n            }\n        }\n    }\n\n    return $executionScript\n}\n\n# ============================================================\n# MAIN EXECUTION\n# ============================================================\n\ntry {\n    # Build the execution scriptblock\n    $executionScript = Build-ExecutionScript\n\n    # ============================================================\n    # ASYNC JOB MODE (for TypeScript extension)\n    # ============================================================\n\n    if ($isAsyncJob) {\n        # Use Invoke-ParallelJobWithSignal for async execution\n        Invoke-ParallelJobWithSignal `\n            -ScriptBlock $executionScript `\n            -SignalPath $SignalPath `\n            -ResultPath $ResultPath `\n            -CancelSignalPath $CancelSignalPath `\n            -JobId $JobId\n\n        # No output to stdout in async mode (signal files are the communication channel)\n        exit 0\n    }\n\n    # ============================================================\n    # DIRECT EXECUTION MODE (returns results immediately)\n    # ============================================================\n\n    else {\n        # Execute and collect results\n        $results = & $executionScript\n\n        # Output results in requested format\n        if ($null -ne $results) {\n            switch ($OutputFormat) {\n                'json' {\n                    $results | ConvertTo-Json -Depth 10\n                }\n                'jsonl' {\n                    $results | ForEach-Object {\n                        $_ | ConvertTo-Json -Compress -Depth 10\n                    }\n                }\n                'text' {\n                    $results | Out-String\n                }\n                'objects' {\n                    # Return raw PowerShell objects (for interactive use)\n                    $results\n                }\n            }\n        }\n\n        exit 0\n    }\n}\ncatch {\n    # ============================================================\n    # ERROR HANDLING\n    # ============================================================\n\n    # In async mode, write error to signal file\n    if ($isAsyncJob -and $SignalPath) {\n        $errorSignal = @{\n            jobId     = $JobId\n            status    = 'failed'\n            timestamp = (Get-Date).ToString('o')\n            error     = $_.Exception.Message\n            stack     = $_.ScriptStackTrace\n        }\n\n        try {\n            $errorSignal | ConvertTo-Json | Set-Content $SignalPath -Encoding UTF8\n        }\n        catch {\n            # Best effort - if we can't write signal file, at least write to stderr\n            Write-Error \"Failed to write error signal: $_\"\n        }\n    }\n\n    # Always write to stderr for logging\n    Write-Error \"Parallel operation failed: $_\"\n    Write-Error $_.ScriptStackTrace\n\n    exit 1\n} |
