idx<int> | path<str> | attributes:{char_count<int> word_count<int> whitespace_ratio<float> entropy<float>} | length<int> | content<str> |
15 | typescript/jso-blackbelt.ts | {24206 2582 0.2068 4.9557} | 25264 | /**\n * jso-blackbelt.ts\n *\n * Type-safe TypeScript wrapper around the jso-engine PowerShell module for JSON/JSONL tooling.\n *\n * Provides:\n * - JSONL streaming and windowed reads (readJsonlWindow, streamJsonl)\n * - Indexing and Bloom filters for deduplication at scale (JsonlBinaryIndex, BloomFilterClient)\n * - Content hashing utilities (HashUtils, rolling hashes)\n * - High-level orchestration via JsoProcessor for large ETL-style workflows\n *\n * WHEN TO USE JSONL TOOLS\n * -----------------------\n * Use these tools when working with large logs or structured data where loading whole files into memory is slow or unsafe.\n * Prefer JSONL + streaming over read_file for files larger than ~10MB or with tens of thousands of records.\n *\n * readJsonlWindow / streamJsonl\n * -----------------------------\n * Use readJsonlWindow(filePath, skip, take) to page through JSONL logs by window instead of loading the entire file.\n * Typical tasks: show recent console history, inspect slices of telemetry, or review the last N events from a dump.\n * Strategy: start with a small window (e.g. take 50–200), then adjust skip/take to paginate through large files.\n *\n * BloomFilterClient\n * -----------------\n * Use BloomFilterClient when you need to detect duplicates across very large streams without keeping every seen value in memory.\n * Typical tasks: find unique error messages, track seen IDs in a log stream, or approximate set membership at scale.\n * Strategy: configure expected item count and false positive rate, then test/add items as you stream records.\n *\n * HashUtils / rolling hashes\n * --------------------------\n * Use getHash and related helpers to identify content, detect changes, or group duplicates by hash.\n * Typical tasks: find files with identical content, verify integrity, or build content-addressable identifiers.\n * Strategy: use SHA256 for integrity and security-sensitive cases, and lighter hashes for quick deduplication.\n *\n * JsoProcessor (orchestrated processing)\n * --------------------------------------\n * Use JsoProcessor when you need to process millions of records with transformations, filtering, or aggregation.\n * Typical tasks: reformat large log files, filter events into new streams, or build simple ETL pipelines over JSONL data.\n * Strategy: define transformations in PowerShell, process in streaming mode, and shard or batch when working with very large inputs.\n *\n * BEST PRACTICES\n * --------------\n * - Prefer streaming over loading: operate on windows or iterators rather than full arrays for large datasets.\n * - Use JSONL for append-only logs and high-volume event streams; use JSON for small, human-edited configs and API payloads.\n * - Combine with power-tools: use fd/rg to locate JSONL files, then use jso-blackbelt to inspect and transform their contents.\n */\n\nimport { execSync } from \"child_process\";\nimport * as fs from \"fs\";\nimport * as path from \"path\";\n\n// CommonJS __dirname (available in Node.js CommonJS context)\ndeclare const __dirname: string;\n\n// ============================================================================\n// POWERSHELL BRIDGE\n// ============================================================================\n\ninterface PowerShellResult<T = any> {\n  success: boolean;\n  data?: T;\n  error?: string;\n  stderr?: string;\n}\n\n/**\n * Escape string for PowerShell command line\n */\nfunction psEscape(str: string): string {\n  return str.replace(/\"/g, '`\"').replace(/\\$/g, \"`$\").replace(/`/g, \"``\");\n}\n\n/**\n * Execute PowerShell command with jso-engine module loaded\n */\nfunction execPowerShellWithJso(script: string): {\n  success: boolean;\n  data?: any;\n  error?: string;\n  stderr?: string;\n} {\n  // Path to jso-engine in lib/powershell (relative to compiled output in out/toolbelt/)\n  const modulePath = path.join(\n    __dirname,\n    \"..\",\n    \"..\",\n    \"lib\",\n    \"powershell\",\n    \"jso-engine.psm1\"\n  );\n\n  // Build PowerShell command with error handling\n  const wrappedScript = `\n    Import-Module \"${modulePath}\" -Force\n    $ErrorActionPreference = 'Stop'\n    try {\n      ${script}\n      exit 0\n    }\n    catch {\n      Write-Error $_.Exception.Message\n      exit 1\n    }\n  `;\n\n  try {\n    const stdout = execSync(\n      `pwsh -NoProfile -Command \"${wrappedScript.replace(/\"/g, '\\\\\"')}\"`,\n      {\n        encoding: \"utf8\",\n        stdio: [\"pipe\", \"pipe\", \"pipe\"],\n      }\n    );\n\n    // Try to parse JSON output\n    try {\n      return { success: true, data: JSON.parse(stdout.trim()) };\n    } catch {\n      // Not JSON, return raw string\n      return { success: true, data: stdout.trim() as any };\n    }\n  } catch (error: any) {\n    return {\n      success: false,\n      error: error.message,\n      stderr: error.stderr?.toString(),\n    };\n  }\n}\n\n/**\n * Generic wrapper for type-safe PowerShell execution\n */\nfunction execJsoPowerShell<T = any>(script: string): PowerShellResult<T> {\n  return execPowerShellWithJso(script) as PowerShellResult<T>;\n}\n\n// ============================================================================\n// BLOOM FILTER (Deduplication)\n// ============================================================================\n\nexport interface BloomFilterOptions {\n  expectedItems: number;\n  falsePositiveRate?: number;\n}\n\nexport interface BloomFilter {\n  size: number;\n  hashCount: number;\n  itemCount: number;\n  expectedItems: number;\n  falsePositiveRate: number;\n  // Internal: PowerShell object serialized state\n  _psState?: string;\n}\n\nexport class BloomFilterClient {\n  private filter: BloomFilter | null = null;\n\n  constructor(options: BloomFilterOptions) {\n    const script = `\n      $filter = New-BloomFilter -ExpectedItems ${\n        options.expectedItems\n      } -FalsePositiveRate ${options.falsePositiveRate ?? 0.01}\n      @{\n        size = $filter.Size\n        hashCount = $filter.HashCount\n        itemCount = $filter.ItemCount\n        expectedItems = $filter.ExpectedItems\n        falsePositiveRate = $filter.FalsePositiveRate\n      } | ConvertTo-Json -Compress\n    `;\n\n    const result = execJsoPowerShell<BloomFilter>(script);\n    if (result.success && result.data) {\n      this.filter = result.data;\n    } else {\n      throw new Error(`Failed to create bloom filter: ${result.error}`);\n    }\n  }\n\n  /**\n   * Add item to bloom filter\n   * Note: In-memory state tracking, PowerShell object not persisted between calls\n   */\n  add(item: string): void {\n    if (!this.filter) throw new Error(\"Bloom filter not initialized\");\n\n    const script = `\n      $filter = New-BloomFilter -ExpectedItems ${\n        this.filter.expectedItems\n      } -FalsePositiveRate ${this.filter.falsePositiveRate}\n      Add-BloomFilterItem -BloomFilter $filter -Item \"${item.replace(\n        /\"/g,\n        '`\"'\n      )}\"\n      'OK'\n    `;\n\n    const result = execJsoPowerShell(script);\n    if (!result.success) {\n      throw new Error(`Failed to add item: ${result.error}`);\n    }\n\n    this.filter.itemCount++;\n  }\n\n  /**\n   * Test if item might be in filter\n   */\n  test(item: string): boolean {\n    if (!this.filter) throw new Error(\"Bloom filter not initialized\");\n\n    const script = `\n      $filter = New-BloomFilter -ExpectedItems ${\n        this.filter.expectedItems\n      } -FalsePositiveRate ${this.filter.falsePositiveRate}\n      Test-BloomFilterItem -BloomFilter $filter -Item \"${item.replace(\n        /\"/g,\n        '`\"'\n      )}\"\n    `;\n\n    const result = execJsoPowerShell<boolean>(script);\n    return result.success && result.data === true;\n  }\n\n  getStats() {\n    return { ...this.filter };\n  }\n}\n\n// ============================================================================\n// JSONL STREAMING (Memory-Efficient)\n// ============================================================================\n\nexport interface JsonlStreamOptions {\n  filePath: string;\n  skip?: number;\n  take?: number;\n}\n\nexport async function* streamJsonl<T = any>(\n  options: JsonlStreamOptions\n): AsyncGenerator<T> {\n  const { filePath, skip = 0, take = 0 } = options;\n\n  // For TypeScript, we'll use Node.js native streaming instead of PowerShell bridge\n  // (PowerShell bridge better for batch operations, Node.js better for streaming)\n  const readline = await import(\"readline\");\n  const stream = fs.createReadStream(filePath, { encoding: \"utf8\" });\n  const reader = readline.createInterface({ input: stream });\n\n  let index = 0;\n  let emitted = 0;\n\n  for await (const line of reader) {\n    if (line.trim() === \"\") continue;\n\n    // Apply skip\n    if (index < skip) {\n      index++;\n      continue;\n    }\n\n    // Apply take\n    if (take > 0 && emitted >= take) {\n      break;\n    }\n\n    try {\n      const entry = JSON.parse(line) as T;\n      yield entry;\n      emitted++;\n    } catch (error) {\n      console.warn(`Skipping invalid JSON at line ${index}:`, error);\n    }\n\n    index++;\n  }\n}\n\n// ============================================================================\n// BINARY INDEXING (Fast Random Access)\n// ============================================================================\n\nexport interface JsonlIndexEntry {\n  lineNumber: number;\n  byteOffset: number;\n}\n\nexport interface JsonlBinaryIndexOptions {\n  jsonlPath: string;\n  indexPath?: string;\n}\n\nexport class JsonlBinaryIndex {\n  private indexPath: string;\n  private jsonlPath: string;\n  private entries: JsonlIndexEntry[] | null = null;\n\n  constructor(options: JsonlBinaryIndexOptions) {\n    this.jsonlPath = options.jsonlPath;\n    this.indexPath = options.indexPath || `${options.jsonlPath}.idx`;\n  }\n\n  /**\n   * Build binary index file (one-time operation)\n   */\n  async build(): Promise<void> {\n    const script = `\n      New-JsonlBinaryIndex -JsonlPath \"${this.jsonlPath}\" -IndexPath \"${this.indexPath}\"\n      @{ lineCount = (Get-Content \"${this.indexPath}\" | Measure-Object).Count } | ConvertTo-Json -Compress\n    `;\n\n    const result = execJsoPowerShell(script);\n    if (!result.success) {\n      throw new Error(`Failed to build index: ${result.error}`);\n    }\n  }\n\n  /**\n   * Read entry by line number (0-based)\n   */\n  async readByIndex<T = any>(lineIndex: number): Promise<T> {\n    const script = `\n      $entry = Read-JsonlBinaryIndex -JsonlPath \"${this.jsonlPath}\" -LineIndex ${lineIndex} -IndexPath \"${this.indexPath}\"\n      $entry | ConvertTo-Json -Compress -Depth 100\n    `;\n\n    const result = execJsoPowerShell<T>(script);\n    if (result.success && result.data) {\n      return result.data;\n    } else {\n      throw new Error(`Failed to read entry ${lineIndex}: ${result.error}`);\n    }\n  }\n\n  /**\n   * Check if index exists\n   */\n  exists(): boolean {\n    return fs.existsSync(this.indexPath);\n  }\n}\n\n// ============================================================================\n// HASHING (Content Addressing)\n// ============================================================================\n\nexport class HashUtils {\n  /**\n   * Get FNV-1a path hash (deterministic)\n   */\n  static getPathHash(filePath: string): string {\n    const script = `\n      Get-PathHash -Path \"${filePath.replace(/\"/g, '`\"')}\"\n    `;\n\n    const result = execJsoPowerShell<string>(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to hash path: ${result.error}`);\n  }\n\n  /**\n   * Get SHA256 file hash\n   */\n  static getFileSha256(filePath: string): string {\n    const script = `\n      Get-FileSha256 -FilePath \"${filePath}\"\n    `;\n\n    const result = execJsoPowerShell<string>(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to hash file: ${result.error}`);\n  }\n\n  /**\n   * Get rolling content hash\n   */\n  static getContentHash(content: string): {\n    hash: number;\n    boundaries: number[];\n    chunkCount: number;\n  } {\n    const script = `\n      Get-ContentHash -Content \"${content.replace(\n        /\"/g,\n        '`\"'\n      )}\" | ConvertTo-Json -Compress\n    `;\n\n    const result = execJsoPowerShell(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to hash content: ${result.error}`);\n  }\n}\n\n// ============================================================================\n// JSON UTILITIES\n// ============================================================================\n\nexport class JsonUtils {\n  /**\n   * Convert to canonical JSON (deterministic serialization)\n   */\n  static toCanonical(obj: any): string {\n    const script = `\n      $obj = @'\n${JSON.stringify(obj)}\n'@ | ConvertFrom-Json\n      ConvertTo-CanonicalJson -InputObject $obj -Compress\n    `;\n\n    const result = execJsoPowerShell<string>(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to canonicalize JSON: ${result.error}`);\n  }\n\n  /**\n   * Merge JSON objects (shallow merge, right precedence)\n   */\n  static merge<T = any>(...objects: any[]): T {\n    const script = `\n      $left = @'\n${JSON.stringify(objects[0])}\n'@ | ConvertFrom-Json\n      $right = @'\n${JSON.stringify(objects[1])}\n'@ | ConvertFrom-Json\n      Merge-JsonObjects -Left $left -Right $right | ConvertTo-Json -Compress -Depth 100\n    `;\n\n    const result = execJsoPowerShell<T>(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to merge objects: ${result.error}`);\n  }\n\n  /**\n   * Get JSON nesting depth\n   */\n  static getDepth(obj: any): number {\n    const script = `\n      $obj = @'\n${JSON.stringify(obj)}\n'@ | ConvertFrom-Json\n      Get-JsonDepth -InputObject $obj\n    `;\n\n    const result = execJsoPowerShell<number>(script);\n    if (result.success && result.data !== undefined) {\n      return result.data;\n    }\n    throw new Error(`Failed to get depth: ${result.error}`);\n  }\n}\n\n// ============================================================================\n// JSO-SCRIPTS ORCHESTRATOR (High-Level Operations)\n// ============================================================================\n\n/**\n * Encoder types from jso-scripts.psm1\n */\nexport type EncoderType =\n  | \"Identity\"\n  | \"Minifier\"\n  | \"Canonical\"\n  | \"WhitespaceCompact\"\n  | \"Preprocess\"\n  | \"JsonPath\"\n  | \"Tabular\"\n  | \"Custom\";\n\nexport interface EncoderOptions {\n  mode?: \"minimal\" | \"standard\" | \"aggressive\";\n  profile?: \"lossless\" | \"conservative\" | \"compact\";\n  zoneAware?: boolean;\n  groupNewlines?: boolean;\n  groupTabs?: boolean;\n  mapSingleSpaces?: boolean;\n  compactFields?: string[];\n  jsonPath?: string;\n  tableColumns?: string[];\n  autoDiscoverColumns?: boolean;\n}\n\nexport interface JsoProcessorConfig {\n  jsonDepth?: number;\n  strictClean?: boolean;\n  postMinify?: boolean;\n  sortOutput?: boolean;\n  useParallelism?: boolean;\n  parallelThrottle?: number;\n  bufferSize?: number;\n  showProgress?: boolean;\n  enableDeduplication?: boolean;\n  deduplicationKeyPath?: string;\n  falsePositiveRate?: number;\n}\n\nexport interface ShardingOptions {\n  recordsPerFile: number;\n  namingPattern?: string;\n}\n\nexport interface ProcessResult<T = any> {\n  processed: T[];\n  failed: any[];\n  stats: {\n    totalFiles: number;\n    successful: number;\n    failed: number;\n    processingTime: number;\n  };\n}\n\n/**\n * JsoProcessor - High-level JSONL orchestration\n *\n * Wraps PowerShell JsoProcessor class with fluent API:\n * - WithEncoder: Configure content transformation\n * - WithFormat: Set output format (jsonl, json, etc.)\n * - WithSharding: Split large files\n * - WithConfig: Set processing options\n * - EnableParallelism: Multi-threaded processing\n * - EnableDeduplication: Bloom filter dedup\n */\nexport class JsoProcessor {\n  private config: JsoProcessorConfig = {};\n  private encoderType: EncoderType = \"Identity\";\n  private encoderOptions: EncoderOptions = {};\n  private shardingOptions: ShardingOptions | null = null;\n  private outputFormat: string = \"jsonl\";\n\n  /**\n   * Configure content encoder\n   */\n  withEncoder(type: EncoderType, options: EncoderOptions = {}): this {\n    this.encoderType = type;\n    this.encoderOptions = options;\n    return this;\n  }\n\n  /**\n   * Set output format\n   */\n  withFormat(format: string): this {\n    this.outputFormat = format;\n    return this;\n  }\n\n  /**\n   * Enable file sharding\n   */\n  withSharding(options: ShardingOptions): this {\n    this.shardingOptions = options;\n    return this;\n  }\n\n  /**\n   * Configure processor options\n   */\n  withConfig(config: JsoProcessorConfig): this {\n    this.config = { ...this.config, ...config };\n    return this;\n  }\n\n  /**\n   * Enable parallel processing\n   */\n  enableParallelism(throttleLimit: number = 5): this {\n    this.config.useParallelism = true;\n    this.config.parallelThrottle = throttleLimit;\n    return this;\n  }\n\n  /**\n   * Enable deduplication with bloom filter\n   */\n  enableDeduplication(\n    expectedItems: number = 10000,\n    falsePositiveRate: number = 0.01,\n    keyPath?: string\n  ): this {\n    this.config.enableDeduplication = true;\n    this.config.falsePositiveRate = falsePositiveRate;\n    this.config.deduplicationKeyPath = keyPath;\n    return this;\n  }\n\n  /**\n   * Process JSONL file with configured settings\n   */\n  async processJsonlFile<T = any>(\n    filePath: string,\n    processor?: (entry: any, config: JsoProcessorConfig) => any\n  ): Promise<T[]> {\n    // Build PowerShell script\n    const encoderScript = this._buildEncoderScript();\n    const configJson = JSON.stringify(this.config);\n    const processorScript = processor\n      ? `{ param($entry, $config) ${processor.toString()} }`\n      : \"{ param($entry, $config) return $entry }\";\n\n    const script = `\n      Import-Module \"${path.join(\n        __dirname,\n        \"../../shared/jso-utils/jso-scripts.psm1\"\n      )}\" -Force\n\n      # Create processor\n      $processor = New-JsoProcessor\n\n      # Configure encoder\n      ${encoderScript}\n\n      # Configure processor\n      $config = @'\n${configJson}\n'@ | ConvertFrom-Json\n      $processor = $processor.WithConfig($config)\n\n      # Process file\n      $results = $processor.ProcessJsonlFile(\"${filePath}\", ${processorScript})\n\n      $results | ConvertTo-Json -Compress -Depth 100\n    `;\n\n    const result = execJsoPowerShell<T[]>(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to process file: ${result.error}`);\n  }\n\n  /**\n   * Split JSONL file into shards\n   */\n  async splitJsonlFile(\n    inputPath: string,\n    outputBasePath: string\n  ): Promise<any[]> {\n    if (!this.shardingOptions) {\n      throw new Error(\"Sharding not configured. Call withSharding() first.\");\n    }\n\n    const script = `\n      Import-Module \"${path.join(\n        __dirname,\n        \"../../shared/jso-utils/jso-scripts.psm1\"\n      )}\" -Force\n\n      $processor = New-JsoProcessor\n      $processor = $processor.WithSharding(${\n        this.shardingOptions.recordsPerFile\n      }, \"${this.shardingOptions.namingPattern || \"{0}_{1:D3}{2}\"}\")\n\n      $result = $processor.SplitJsonlFile(\"${inputPath}\", \"${outputBasePath}\")\n\n      $result | ConvertTo-Json -Compress -Depth 100\n    `;\n\n    const result = execJsoPowerShell(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to split file: ${result.error}`);\n  }\n\n  /**\n   * Process multiple JSONL files in batch\n   */\n  async processJsonFiles(\n    filePaths: string[],\n    processor?: (content: any, config: JsoProcessorConfig) => any\n  ): Promise<ProcessResult> {\n    const encoderScript = this._buildEncoderScript();\n    const configJson = JSON.stringify(this.config);\n    const filePathsJson = JSON.stringify(filePaths);\n    const processorScript = processor\n      ? `{ param($content, $config) ${processor.toString()} }`\n      : \"{ param($content, $config) return $content }\";\n\n    const script = `\n      Import-Module \"${path.join(\n        __dirname,\n        \"../../shared/jso-utils/jso-scripts.psm1\"\n      )}\" -Force\n\n      $processor = New-JsoProcessor\n      ${encoderScript}\n\n      $config = @'\n${configJson}\n'@ | ConvertFrom-Json\n      $processor = $processor.WithConfig($config)\n\n      $files = @'\n${filePathsJson}\n'@ | ConvertFrom-Json\n\n      $results = $processor.ProcessJsonFiles($files, ${processorScript})\n\n      $results | ConvertTo-Json -Compress -Depth 100\n    `;\n\n    const result = execJsoPowerShell<ProcessResult>(script);\n    if (result.success && result.data) {\n      return result.data;\n    }\n    throw new Error(`Failed to process files: ${result.error}`);\n  }\n\n  private _buildEncoderScript(): string {\n    if (this.encoderType === \"Identity\") {\n      return \"$processor = $processor.WithEncoder([JsoEncoder]::CreateIdentity())\";\n    }\n\n    const optionsJson = JSON.stringify(this.encoderOptions);\n    let encoderCreation = \"\";\n\n    switch (this.encoderType) {\n      case \"Minifier\":\n        encoderCreation = \"$encoder = [JsoEncoder]::CreateMinifier()\";\n        break;\n      case \"Canonical\":\n        encoderCreation = \"$encoder = [JsoEncoder]::CreateCanonical()\";\n        break;\n      case \"JsonPath\":\n        encoderCreation = `$encoder = [JsoEncoder]::CreateJsonPath(\"${this.encoderOptions.jsonPath}\")`;\n        break;\n      case \"Tabular\":\n        const cols = this.encoderOptions.tableColumns\n          ? `@(${this.encoderOptions.tableColumns\n              .map((c) => `\"${c}\"`)\n              .join(\",\")})`\n          : \"$null\";\n        encoderCreation = `$encoder = [JsoEncoder]::CreateTabular(${cols}, ${\n          this.encoderOptions.autoDiscoverColumns ? \"$true\" : \"$false\"\n        })`;\n        break;\n      case \"WhitespaceCompact\":\n      case \"Preprocess\":\n        encoderCreation = `$encoder = New-JsoEncoder -Type WhitespaceCompact -Options @'\n${optionsJson}\n'@ | ConvertFrom-Json`;\n        break;\n      default:\n        encoderCreation = \"$encoder = [JsoEncoder]::CreateIdentity()\";\n    }\n\n    return `\n      ${encoderCreation}\n      $processor = $processor.WithEncoder($encoder)\n    `;\n  }\n}\n\n// ============================================================================\n// CONVENIENCE FUNCTIONS\n// ============================================================================\n\n/**\n * Deduplicate JSONL file using bloom filter\n */\nexport async function deduplicateJsonl(\n  inputPath: string,\n  outputPath: string,\n  keyPath?: string,\n  falsePositiveRate: number = 0.01\n): Promise<{\n  originalRecords: number;\n  deduplicatedRecords: number;\n  duplicatesRemoved: number;\n}> {\n  const script = `\n    Import-Module \"${path.join(\n      __dirname,\n      \"../../shared/jso-utils/jso-scripts.psm1\"\n    )}\" -Force\n\n    Invoke-JsonlDeduplication -InputPath \"${inputPath}\" -OutputPath \"${outputPath}\" ${\n    keyPath ? `-KeyPath \"${keyPath}\"` : \"\"\n  } -FalsePositiveRate ${falsePositiveRate} | ConvertTo-Json -Compress\n  `;\n\n  const result = execJsoPowerShell(script);\n  if (result.success && result.data) {\n    return result.data;\n  }\n  throw new Error(`Failed to deduplicate: ${result.error}`);\n}\n\n/**\n * Split JSONL file with optional processing\n */\nexport async function splitJsonlWithProcessor(\n  inputPath: string,\n  outputDirectory: string,\n  recordsPerFile: number,\n  encoderType?: EncoderType,\n  encoderOptions?: EncoderOptions\n): Promise<any[]> {\n  const processor = new JsoProcessor().withSharding({ recordsPerFile });\n\n  if (encoderType) {\n    processor.withEncoder(encoderType, encoderOptions);\n  }\n\n  return processor.splitJsonlFile(\n    inputPath,\n    path.join(outputDirectory, \"split\")\n  );\n}\n\n/**\n * Get hash of content using specified algorithm\n */\nexport function getHash(\n  content: string,\n  algorithm: \"FNV1a\" | \"SHA256\" = \"FNV1a\"\n): string {\n  const script = `Get-Hash -Content \"${psEscape(\n    content\n  )}\" -Algorithm ${algorithm}`;\n  const result = execJsoPowerShell<string>(script);\n  if (!result.success || !result.data) {\n    throw new Error(`getHash failed: ${result.error}`);\n  }\n  return result.data;\n}\n\n/**\n * Get rolling hashes for content with specified window size\n */\nexport function getRollingHashes(\n  content: string,\n  windowSize: number\n): Array<{ index: number; hash: number }> {\n  const script = `Get-RollingHashes -Content \"${psEscape(\n    content\n  )}\" -WindowSize ${windowSize} | ConvertTo-Json -Compress`;\n  const result =\n    execJsoPowerShell<Array<{ index: number; hash: number }>>(script);\n  if (!result.success || !result.data) {\n    throw new Error(`getRollingHashes failed: ${result.error}`);\n  }\n  return result.data;\n} |
